// 사기범(LLM) 응답 텍스트 저장 전 마지막 손질 — **PII 마스킹(guardrails/maskPII)을 의도적으로
// 적용하지 않는다.** (라이브 신고 2026-09-06 — institutionalImpersonation 고급·kidnappingThreat
// 중급, 둘 다 텍스트 입력 경로. 서로 다른 시나리오·난이도에서 공통 재현되어 시나리오별 실수가
// 아니라 이 공유 저장 경로의 문제임을 확인했다.)
//
// 재현된 증상: "접수번호는 [계좌]번입니다", "OO은행 [계좌] 계좌로 지금 얼마 보낼 수 있어?"처럼
// 모델이 실제로 만들어 부른 그럴듯한 숫자열이 리터럴 "[계좌]" 토큰으로 대체된 채 참가자에게 그대로
// 노출됐다.
//
// ⛔ **왜 마스킹이 여기서는 틀렸는가(근거 1 — 지킬 실제 PII가 없다)**: 사기범(LLM)에게 전달되는
// 대화 이력은 이미 저장 전 `maskPII`를 거친 사용자 텍스트만 쌓인다 — `roleplay/index.ts`의
// `maskedUserText`가 `toLlmHistory`로 들어가는 유일한 이력 원천이고, 그 뒤 `llmHistory.push`에도
// `maskedUserText`(마스킹 후 값)만 넘어간다. `openingLine.ts`는 애초에 `messages: []`라 사용자
// 입력 자체가 없다. 즉 **모델은 참가자의 원문 PII를 단 한 번도 입력으로 받지 않는다** — 모델
// 응답에 참가자의 실제 개인정보가 반영돼 새어나올 경로가 구조적으로 없다.
//
// ⛔ **왜 마스킹이 여기서는 해롭기까지 한가(근거 2 — 의도된 콘텐츠를 깬다)**: `promptAssembly.ts`의
// `SCENARIO_PROGRESSION_TEMPLATE`("페이로드는 가상값만 쓴다 ... 숫자는 실제처럼 들리는 형식으로
// 또박또박 부른다")과 `ADVANCED_L3_PROCEDURAL`("접수번호를 남겨 주고")은 **14개 시나리오·3개
// 난이도 전체에 공통으로 걸려**(promptAssembly.ts가 모든 시나리오·모든 경로가 공유하는 단일 조립
// 함수라는 것이 이 파일 헤더의 설계 전제다) 모델이 8자리 이상의 그럴듯한 가짜 계좌·접수번호를
// 실제로 말하게 만든다(T85/§43). 이 값이 `guardrails/maskPII`의 `ACCOUNT_PATTERN`(8자리 이상
// 연속 숫자 휴리스틱, 실존 여부를 구분하지 않는다)에 걸려 리터럴 "[계좌]" 토큰으로 치환되면,
// 서버 내부 사정이 참가자에게 그대로 노출된다 — 이 함수가 막는 것이 정확히 그 충돌이다.
//
// ⚠️ 참가자(user) 쪽 입력은 계속 `maskPII`를 거친다(`roleplay/index.ts`의 `maskedUserText`) —
// 이 함수는 **사기범 쪽에만** 적용되며, 참가자가 실제로 자신의 개인정보를 타이핑해 넘기는 것을
// 막아 주는 AC-024 요구는 그대로 유지된다(무변경).
//
// ⚠️ 이 함수를 지금 항등 함수(identity)로 남겨 둔 이유 — 링크/신호 마커 제거(`extractLinkMarker`/
// `extractEscalationSignal`)는 이미 호출부에서 이 함수보다 먼저 끝나 있으므로, 여기서 할 일이
// 없다. 그럼에도 별도 함수로 이름 붙여 둔 것은 (1) "사기범 응답에는 PII 마스킹을 적용하지 않는다"는
// 결정을 호출부 인라인 주석이 아니라 **테스트 가능한 지점**으로 고정하고, (2) 향후 실수로
// `maskPII(...)`가 다시 이 자리에 끼어드는 것을 리뷰에서 눈에 띄게 하기 위해서다.
export function finalizeScammerReplyText(text: string): string {
  return text;
}
