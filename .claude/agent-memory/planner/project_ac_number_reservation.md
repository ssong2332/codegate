---
name: ac-number-reservation
description: AC/OQ 번호를 미리 예약해 문서에 적으면 반드시 소진된다 — 이 저장소에서 두 번 발생한 사고
metadata:
  type: project
---

**AC 번호를 "확정되면 AC-0xx로 신설한다"처럼 미리 못박아 적지 마라.** 대신 *"확정 시점의 다음 미사용 번호"*로 적고, 참고용 현재값은 날짜와 함께 괄호로만 남긴다.

**Why:** 이 저장소에서 같은 사고가 **두 번** 났다 — `docs/PRD.md` OQ-36 Decision란이 (1) 처음 "AC-070으로 신설"이라 적었는데 v1.6이 AC-070~077을 실제로 소진했고, (2) 그래서 "AC-078로"로 정정했는데 v1.8이 AC-078을 소진했다. 예약은 *적은 시점의 값*일 뿐인데 문서는 그걸 약속처럼 읽는다. 이 프로젝트는 **번호 재사용·재번호를 금지**(기존 AC 무수정·무재번호 규약)하므로 예약이 어긋나면 정정 릴리스가 한 번 더 필요해진다.

**How to apply:** open OQ의 Decision란·Risks·인계 메모에 "확정 시 AC-xxx 신설" 문구를 쓸 때마다 적용. 새 AC를 append할 때는 **먼저 현행 최대 번호를 grep으로 실측**하고(`^\| AC-0` 패턴), 예약 문구가 그 번호를 물고 있으면 같은 커밋에서 그 문구도 함께 정정한다. `docs/Tasks.md`의 T번호도 같은 함정이 있다(T93·T94·T96·T97은 행 없이 브랜치로만 소진됐다 — 번호 규약 주석이 각 섹션 서두에 있다).

관련: [[planner-ac-verifiability]]
