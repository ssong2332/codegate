# Decision Log — 안 당해본 사기는 못 막는다 (AI 금융사기 백신)

Owner: architect (see AGENTS.md). One line per decision; anything with lasting structural impact gets a full ADR in adr/.

## Rules
- Decisions are append-only. To reverse one, add a new row that supersedes it (and a new ADR if the original had one).
- "Why" is mandatory — a decision without a reason cannot be evaluated later.

| # | Date | Decision | Why | ADR |
|---|---|---|---|---|
| 1 | 2026-07-21 | 스택 확정 사용: Next.js(App Router)+Firebase(Auth/Functions/Firestore/Storage)+ElevenLabs+LLM(Claude/Gemini). 별도 백엔드 서버·다른 DB·다른 인증 프로바이더 도입 안 함. | PRD Constraints로 이미 확정. 하루 완성 제약상 표준 조합에서 벗어나면 리스크. | — |
| 2 | 2026-07-21 | Firebase Auth **Google Provider 단일**. 최초 인증=계정 자동 생성=로그인(가입 폼 없음). 미인증 시 전 라우트/콜러블 게이팅(AC-027). | OQ-U5 확정(사용자 "구글로 로그인"). 단일 프로바이더로 구현·검증 최소화, 어르신 1클릭. | — |
| 3 | 2026-07-21 | 3트랙 병렬 경계를 **Firestore 문서 계약 + Callable 함수 계약 + 이벤트 이름 계약**으로 고정(별도 API 게이트웨이 없음). T2 직후 스텁 선(先)커밋. | 병렬 가능성=하루 완성 가능성. 계약을 먼저 못 박아야 A/B/C가 서로 안 기다림. | [0001](adr/0001-track-boundaries-and-contracts.md) |
| 4 | 2026-07-21 | 본인 목소리만 등록: 파일 업로드 UI 전면 제거(마이크 캡처만) + storage.rules로 경로/소유자/`audio/*`/크기≤3MB 서버측 강제. 강 KYC는 배제. | OQ-U1 확정. 자기확인 체크만으로는 보안 배점 근거가 약함 → 서버측 원천 차단을 추가해 AC-020 의도(무단 타인 등록 차단) 충족. 강 KYC는 하루 스코프 밖(D-5). | [0002](adr/0002-own-voice-only-enforcement.md) |
| 5 | 2026-07-21 | 생성물 즉시 폐기: `session.status=ended` Firestore 트리거 `onSessionEnded`가 Storage 산출물 + **ElevenLabs voice 자체**를 삭제하고 `deletionLogs`에 감사 기록. | AC-021. UI 흐름과 분리해 사용자 이탈에도 폐기 보장. 외부(ElevenLabs)에도 클론이 남지 않아야 함. | [0003](adr/0003-immediate-artifact-purge.md) |
| 6 | 2026-07-21 | LLM 인젝션 방어를 **구조로**: 시스템 프롬프트/페르소나를 서버(Functions)에서 `scenarioId`로 조립, 클라는 보유/전송 안 함. `scenarioPrompts`는 클라 read 거부. 대화 로그는 저장 전 PII 마스킹(원문 미저장). | AC-024/AC-005/AC-013. 사용자 입력을 시스템 지시로 해석 못 하게 role 분리 + 원천 접근 차단. | [0004](adr/0004-llm-injection-defense-and-pii-masking.md) |
| 7 | 2026-07-21 | 합성 표식(AC-022): 모든 합성물 메타에 `synthetic:true`·`syntheticLabel:"AI 훈련용 합성"` 필드, UI는 화면 라벨+오디오 프리롤 이중 노출(D-3). | 보안 배점 근거를 데이터 모델·UI 양쪽에 명시. 단일 노출은 접근성상 표식을 놓칠 수 있음. | — |
| 8 | 2026-07-21 | 송금/계좌/이체 개념을 시스템에 **애초에 두지 않음**(스키마·필드·엔드포인트 어디에도). 사칭 이미지는 정적 에셋일 뿐 기능 아님. | AC-023. 실제 금전 피해 경로를 구조적으로 제거. | — |
| 9 | 2026-07-21 | **OQ-U3 잠정 확정**: 클론 생성 soft 15s·hard 45s→폴백, TTS hard 20s→사전녹화. 폴백=사전준비 `FALLBACK_VOICE_ID`/정적 녹화본. **T1 PoC 실측 p95×1.5(상한 60s)로 확정** 절차. (Architecture.md §9/§11 참조) | 데모 안정성(라이브 클론 실패 대비, PRD Risk). 근거 없는 확정 금지 원칙상 실측 전 "잠정"임을 명시하고 확정 절차를 남김. | — |
| 10 | 2026-07-21 | **OQ-U4 확정**: `MAX_USER_TURNS=10`, `MAX_SESSION_MS=6분`. 한도 도달 시 자동 종료→UX-007(AC-007). 데모 5~8턴 타겟. (Architecture.md §9 DECISIONS #7로 참조된 항목) | 데모 2분 흐름과 정합(짧으면 몰입 저하·길면 데모 초과). LLM 비용 상한. AC-007 종료 조건 충족. | — |
| 11 | 2026-07-21 | LLM은 **어댑터 1겹**(`functions/src/llm`)으로 Claude|Gemini 교체 가능하게. PoC 후 택1. (Architecture.md §9 DECISIONS #8로 참조된 항목) | 얇은 추상화(오버엔지니어링 아님). 어느 쪽 품질/지연/비용이 나을지 PoC 전 미확정이라 교체 유연성 확보. | — |
| 12 | 2026-07-21 | 실시간 상태 전이는 **Firestore onSnapshot 구독**으로 처리(클론 대기·채팅·리포트 준비). 별도 웹소켓 서버·폴링 금지. | 하루 스코프. Firestore 실시간이 이미 있어 인프라 추가 없이 상태 반영. | — |
| 13 | 2026-07-21 | 동의 로그·리포트·삭제로그는 `uid` 귀속, 본인만 read(Firestore rules). 자녀 대리/자녀 리포트 열람은 **미구현**(D-7, OQ-3/15 빌드 밖). | OQ-11 확정(소유권=본인). 빌드 스코프 밖 임의 구현은 스코프 크립. | — |
| 14 | 2026-07-23 | **(T26) 세션 채널 전이 모델**: `sessions/{}`에 옵셔널 `channel`(messenger\|voice)·`entryChannel`·`channelHistory` 증분. 하나의 sessionId·연속 turnIndex·단일 리포트로 메신저↔보이스를 잇는다. 필드명은 `phase`가 아닌 **`channel`**(UX-014 내부 phase와 명명 충돌 회피). 전이 함수는 방향 무관 `(from,to)`, MVP는 messenger→voice만 배선(AC-039). | AC-035/007/037 단일 세션·단일 리포트 보장. 기존 세션 하위호환(부재→voice). 명명 충돌 시 구현·리뷰 혼선 방지. | — |
| 15 | 2026-07-23 | **(T26) 에스컬레이션 트리거 = sentinel 토큰**(function-calling 아님). 역할극 LLM 어시스턴트 출력에 `[[SIGNAL:ESCALATE_VOICE]]`가 있으면 전이. 앱은 자유텍스트 미분류, 어시스턴트 출력에서만 신호 인정, 저장·반환 전 토큰 제거, 사용자 입력의 sentinel 형태는 수신 시 선제거. | 어댑터 무관(Claude/Gemini/mock 균일, DECISIONS #11 계승) + function-calling 배선/모크 복잡도 회피(§0.1 단순). AC-024 위배 없음 — 사용자 입력을 신호로 해석 안 함, 최악=조기 전이(가드레일 우회 아님). 보이스 Gemini Live `tools:[]` 잠금과 무관(트리거는 텍스트 단계). | — |
| 16 | 2026-07-23 | **(T26) 에스컬레이션 폴백 수치(잠정)**: `MESSENGER_ESCALATION_FALLBACK_TURNS=6`(무신호 시 자동 전이), 1턴부터 명시 전환 버튼 상시, 교차채널 세션 `maxUserTurns` 상향(예 14). **PoC(T30) 후 확정** 절차. | 사용자가 끝까지 신호 안 내도 전이 보장(AC-034 폴백). 두 채널 합산이 기존 10턴 초과 가능 → 세션 문서 필드로 상향. 근거 없는 확정 금지(OQ-U3/U4 패턴). | — |
| 17 | 2026-07-23 | **(T26) 메신저 콘텐츠·스킨 스키마**: 신규 컬렉션 없이 `scenarios`에 `channel`/`surface`/`escalation` 옵셔널 증분, `messages`에 `channel`/`attachments`(link=displayText+fakeLandingId+harmless, **실 URL 필드 부재**) 증분. UA 감지 스킨(규칙표 §13.5)은 세션 문서(`messengerSkin`/`skinSource`)에 저장, 프레젠테이션 전용·안전 미게이팅. | 표면=콘텐츠 분리 스킨 레이어(PRD Risk 완화). 실 URL 구조적 부재로 AC-032/045 강제(AC-023 송금 금지와 동형). 스킨을 세션에 두는 이유: 리포트·새로고침·수동 전환 지속. | — |
| 18 | 2026-07-23 | **(T26) 조건부 clone/목소리 선택**: 세션 `voiceSelectionSource`(recorded\|reused\|fallback_male\|fallback_female)+기존 `voiceId` 재사용. 남/여 기본 보이스 `FALLBACK_VOICE_MALE_ID`/`_FEMALE_ID` 상수. 재사용 소스=유지형 보관함(`users/{uid}/voices`, ADR-0005). 전이 시 통화는 기존 `createRealtimeCall` 재사용(엔드포인트 신설 없음). | AC-046 3경로(즉시녹음/재사용/gendered 폴백). 기존 파이프라인 재사용(§0 원칙). MVP 최소는 녹음+gendered로 성립, 재사용은 보관함 있을 때만(P-8). | — |
| 19 | 2026-07-23 | **(T26) 카카오 면책 고지 — 별도 ADR 불요.** AC-047·T24로 이미 포착된 **UI 상시 노출 콘텐츠 요건**이며 소프트웨어 구조 결정(스키마·모듈·경계)이 아니라 ADR 대상 아님. architect 보장 범위=면책 고지를 카카오 표면에서 **영구 비활성화 불가·항상 노출**한 UI 요소로 강제(데이터 필드 아님). 법적 충분성 자체는 architect 판단 밖(사용자/법무 확인 잔존). | OQ-24 위임 사항. 중복 ADR은 문서만 늘림. architect는 법률 자문 불가 — "상시 노출 UI 요건 충족" 수준까지만 확정. | — |
| 20 | 2026-07-23 | **(OQ-20 마무리) 장기 프로젝트 전환 반영.** DECISIONS.md의 과거 항목(#9 "soft/hard 타임아웃"·#10 "데모 5~8턴"·기타 "데모/하루" 표현)은 **해커톤 하루완성 시간압박 프레이밍이 아니라 데모 품질·비용·몰입 근거로 재해석**한다. 해커톤 Day1/Day2 절대 마감은 폐기됐고(PRD v1.0 OQ-20), 이후 결정은 **선행 의존성 순서**만 따른다(날짜/스프린트 제약 없음). 과거 행은 append-only 원칙상 재작성하지 않고 본 행으로 정정 고지. | OQ-20 architect 위임(planner 소유 아님). 미래 결정이 "하루 안에"로 잘못 해석되지 않도록 명문화. grep상 DECISIONS.md에 "해커톤" 문자열은 없었으나 "데모/하루" 함의가 있어 정정. | — |
| 21 | 2026-07-23 | **(T35) 챌린지 스키마 + 링크 토큰**: `challenges/{}`(creatorUid+scenarioId+voiceId+displayName+status+linkTokenHash+링크만료+보존삭제예정+resultSummary+신고필드). 토큰=`randomBytes(32)`→base64url, **SHA-256 해시만 저장**(평문 미저장), 만료 무료 3일, **1회성 소모=동의 통과 시점**(열기 아님·완료 아님). 사용자2 무로그인·전부 Functions 매개. | AC-048/044. 해시 저장으로 DB 유출 시 라이브 링크 미노출. 동의 시 소모: 크롤러 선(先)fetch 소진 방지 + 단일 taker 고정 동시 만족. 사용자2 접근 Functions 매개로 AC-041/043 규칙 강제. | — |
| 22 | 2026-07-23 | **(T35) 복제 음성 스코프 고정·기간제 보존**: 챌린지/보관 음성은 세션 즉시폐기(ADR-0003)의 **예외가 아니라 별도 opt-in 수명 클래스** — 스코프 고정·추출 차단·기간제(챌린지 기본 **30일**, 조정 7~90일)·수동 삭제, 폐기는 ADR-0003 기계 재사용. | AC-041/OQ-25. 비동기 3일 링크(AC-044) 때문에 즉시폐기 불가. ADR-0003 불변식을 약화 아닌 **범위 한정**으로 보존. | [0005](adr/0005-retained-and-challenge-scoped-clone-voice.md) |
| 23 | 2026-07-23 | **(T35) 오용 방지 상한·신고 모델**: 사용자1당 활성 챌린지 상한 **무료 3 / 유료 10**(활성=pending\|consented\|in_progress 且 미만료), 생성 콜러블이 카운트로 강제. 신고=콜러블 `reportChallenge(token,reason)`→챌린지 문서 임베드(reason enum: unwanted\|harassment\|impersonation_concern\|other), `status="reported"`로 **즉시 비활성화**. **MVP=데이터 축적+즉시 차단만, 관리자 검토·계정 조치 미채택.** | AC-049/OQ-27. 토큰 1회성이라 taker 1명 → 신고 임베드로 충분(별도 컬렉션 불요). 관리자 검토 미채택(운영 부담). | — |
| 24 | 2026-07-23 | **(T35) AC-050 검증 통과**: tier(무료/유료)가 다르게 적용되는 항목은 **개수 상한·링크 만료·보존기간(용량/기간 축)뿐**. 4대 안전제약(AC-040 동의·041 추출차단·042 정체공개·043 열람제한)과 기타 가드레일은 **tier를 조건으로 삼는 코드경로가 없음**(등급 무관 동일). 결제 게이팅 실구현 시 Architecture.md §14.6 표를 게이트 조건으로 재검증. | AC-050 명시 요구. 유료화가 안전장치 약화·우회·게이팅 안 함을 스키마·표로 못박음. | — |
| 25 | 2026-07-24 | **(T40 소급 비준) 보이스→메신저 역방향 전이 설계 3판단 비준(as-is).** ① **트리거=명시 버튼("메시지로 전환") 전용** — 구조화 신호·max-turn 폴백은 역방향 MVP 범위 밖. ② **시나리오 게이팅**=`PUBLIC_SCENARIOS[scenarioId].channel==="messenger"`인 시나리오(=메신저 콘텐츠 보유)에서만 허용. ③ **핑퐁 방지**=`ChannelTransitionEntry.turnCountAtTransition?`(`to==="messenger"`에만 기록) + `turnsSinceMessengerEntry` channelHistory 룩백. 구현 변경 불요, 장래 확장 지점만 §13.8 설계 노트로 잔존. | ① AC-034(세 트리거)는 정방향 전용, AC-039는 엔진 방향무관+역방향 배선만 요구(트리거 파리티 불요) → 명시 버튼 전용은 AC 정합. 보이스 경로엔 sendMessage식 텍스트-완성 스캔 훅이 없어(realtime 스트리밍·Gemini `tools:[]`) 구조화 신호는 DECISIONS #15가 기각한 function-calling 재도입 필요. 역방향 폴백엔 서사·AC 근거 없음. ② messenger/page.tsx가 `channel!=="messenger"`면 dead-end 오류 화면만 렌더(실측 L106–111) → 게이트 없으면 AC-039 "조용한 실패"의 변종. 게이트는 왕복 세션이라는 유일 유효 케이스를 정확히 허용. ③ channelHistory=이미 append-only 단일 진실원천, 전용 카운터는 이중 쓰기 동기화(이 버그와 동류 실패)를 재도입. 순수 함수 관례(sessionLimits/analyzeConversation) 정합. | — |
| 27 | 2026-07-24 | **(T37 착수 게이트) 사용자2 접근 = Firebase 익명 인증 재사용(정제형 A1).** 사용자2는 동의 시점에 로그인 UI 없이 익명 사인인해 **임시 uid**로 체험 `sessions/{}`를 소유 → `createRealtimeCall`·`submitRealtimeTranscript`·`endSession`·`generateReport`·`report/replay` 화면·소유자 read 규칙 **무개정 재사용**. **A1: 챌린지 clone `voiceId`는 세션 문서에 미저장**(challengeId만); `createRealtimeCall`이 challenge 문서에서 발급 시 해석+게이트 재검증. 신규 콜러블은 챌린지 문서 전용 토큰-매개 4종(landing/consent/report/result-sharing)뿐. §14.0/§14.1의 "소유자 없음/직접 접근 없음" 문구 정정. | (A vs B) B(무인증 파라미터)는 `firestore.rules`가 request-body 토큰을 못 봐 사용자2 경로를 전부 `if false`로 잠그고 UX-014/UX-018을 재작성 + 병렬 콜러블 ~8–10개 신설해야 함(§0.1·§14.2 위반, 손 재구현 토큰이 검증된 Auth 토큰보다 취약). A는 UX-014/018 무개정 재사용·격리를 앱 전체가 이미 신뢰하는 소유권 규칙으로 강제. A1은 정합성 요건 — 세션에 voiceId 두면 사용자2 직접 read로 raw clone 유출(AC-041) + `onSessionEnded`가 첫 체험 종료 시 사용자1 챌린지 clone 삭제(보존 30일 깨짐). AC-048 "무로그인"=사용자에게 로그인 요구 없음(익명 토큰 불가시)으로 충족, DECISIONS #2는 사용자1 로그인 UX 최소화지 익명 인증 금지 아님. | [0006](adr/0006-user2-anonymous-auth-access.md) |
| 29 | 2026-07-24 | **DECISIONS #11("Claude|Gemini 교체 가능 어댑터") 실현 — `getLlmClient()`가 GEMINI_API_KEY 존재 시 실 Gemini(GeminiLlmClient, `gemini-flash-latest`)를 반환하도록 활성화(사용자 실측 신고로 발견: 텍스트 대화만 실시간 음성과 달리 항상 Mock이었던 팩토리 하드코딩 버그 — 키가 이미 확보돼 있었는데도 쓰이지 않고 있었음).** Claude(LLM_API_KEY) 분기는 여전히 placeholder라 미구현, TODO로 유지. `sendMessage`/`createSession`/`consentChallenge` 3개 onCall에 `secrets:[GEMINI_API_KEY]` 선언 추가(배포 환경 secret 주입 요건). 부수 수정: `sendMessage`의 트랜잭션 update 객체가 `isMock:false`+한도 미도달+세션 활성 조합에서 완전히 빈 객체가 되어 Firestore가 거부하던 잠재 버그(그동안 isMock이 항상 true였어서 한 번도 발현 안 됨) — 빈 update는 write 자체를 건너뛰도록 수정. | 사용자가 "실시간 음성은 문맥 반영되는데 텍스트는 왜 안 되냐"고 지적 — 확인해보니 실시간 음성 경로(realtime/provider.ts)는 이미 같은 GEMINI_API_KEY로 실제 Gemini Live를 쓰고 있었는데, 텍스트 경로(llm/index.ts)만 팩토리가 키 존재 여부를 아예 확인 안 하고 항상 Mock을 반환하도록 남아 있었다(T7 시절 TODO가 완성 안 됨). 모델명은 `gemini-2.5-flash` 고정 버전이 이 프로젝트 키 계정에서 404("no longer available to new users")로 실측 거부돼 `-latest` 별칭으로 교체(계정별 구버전 차단에 흔들리지 않음). 실 Gemini 전환 후 라이브로 재검증한 AC-013(프롬프트 인젝션 방어)은 MockLlmClient의 가짜 정규식이 아니라 실제 guardrailPreamble+role 분리 구조만으로 방어됨을 실측 확인(직접적 "시스템 프롬프트 출력해" 공격에 캐릭터 유지·유출 없음). | — |
| 28 | 2026-07-24 | **(T38 통합 게이트) 챌린지 라이브 통화 voice 참조 예외 — §14.2 불변식 정밀화.** ADR-0005/§14.2의 무조건형 "raw voiceId 반환 경로 어디에도 없다"를 정정: raw **오디오 바이트**는 무조건 불변(다운로드 경로 부재), raw **voiceId**는 **단일 스코프 예외**로만 허용 = 동의 완료·게이트 통과한 단일 taker(사용자2)에게 **라이브 ElevenLabs 통화 경로(`provider==="elevenlabs"`)에서만** 통화 duration 동안. **강제(신규 요건):** challenge 세션 `createRealtimeCall` 응답은 비-elevenlabs 경로(mock/none) 전부 `voiceId=""`(텍스트 폴백 미소비). 구현: `functions/src/realtime/index.ts` 성공 경로 `challengeId && provider!=="elevenlabs" → voiceId:""`, catch L101 `voiceId: session.challengeId ? "" : effectiveVoiceId`. 정상(비challenge) 세션·`callTypes.ts`·프로바이더·클라 무변경. **A1은 불변**(세션 문서 voiceId 미저장) — A2는 A1이 안 건드린 응답 계약만 정밀화. | ElevenLabs Agents는 런타임 voice를 클라 개시 override로만 받고 서버측 voice-핀 경로 없음(get-signed-url override 미수신·실측) → challenge에서 응답 voiceId 무조건 제거 시 "본인 목소리 통화"(AC-041/044) 자체가 깨짐. 챌린지별 전용 에이전트/서버 TTS 릴레이는 §0.1·ADR-0006 A1 위배 + 한계이득 ~0. 예외가 안전한 이유: voiceId=앱 계정 전용·불투명·계정 스코프 참조(IVC 타계정 재사용 불가), signedUrl=프롬프트 서버잠금 에이전트·15분 만료라 임의 발화 추출 불가, 오디오 바이트는 여전히 다운로드 불가 → §14.2 진짜 위협(오디오 추출+앱외 재사용) 완전 차단 유지. T38 NO-GO는 (b) 비-elevenlabs 경로 voiceId 블랭킹으로 gratuitous 노출 제거 + (a) 라이브 경로는 명시적·협소 비준 예외로 해소. | [0006](adr/0006-user2-anonymous-auth-access.md) (Addendum A2) |
| 30 | 2026-07-24 | **(T47 아키텍트 게이트) 음성 없는 메신저 2인 챌린지(#20) — 채널 인지 확장(ADR-0005/0006 증분, 새 ADR 없음).** ① **스키마**: `challenges.channel?`(부재→voice, 무백필) 신규 + `voiceId` required→optional. 채널은 생성 시 `PUBLIC_SCENARIOS[scenarioId].channel`로 역정규화(OQ-29로 시나리오 불변이라 안전) — voiceId-부재를 채널 신호로 오버로드하지 않음(#21은 messenger+voiceId 병존). ② **`consentChallenge` 메신저 변형**: 세션 shape(channel/surface/entryChannel=messenger)+오프닝 합성 스킵 2곳만 분기, 소모+세션생성 트랜잭션·§14.7 익명-uid 소유권 모델은 무개정 재사용(sendMessage 소유권 검사=uid 비교뿐·voice 가정 없음, onSessionEnded=voiceId 부재 안전 no-op 실측). #20은 `createRealtimeCall` 미사용이라 A1보다 단순. ③ **resultSummary**: 메신저는 `deriveChallengeResultSummary` 채널 게이트로 `{completed}`만 파생·저장(읽기 필터 아닌 쓰기 시점 강제, AC-055/OQ-31). ④ **활성 상한**: 이미 채널 무관 전역 합산(challenge/index.ts L109–125 실측) — 변경 불요(OQ-30 충족). ⑤ 폐기 경로 `voiceId:string→undefined` 허용 필요(implementer 갭). | AC-051/053/054/055·OQ-29/30/31. 음성 없는 챌린지는 클론·voiceId·통화·onSessionEnded voice삭제 경로를 아예 안 타는 "깨끗한 부분집합"이라 ADR-0005 §14.2·ADR-0006 A1/A2가 다루는 유출 표면이 부재 — 익명-uid 격리(AC-040/042/043)만 채널 무관으로 성립. 새 구조 결정이 아니라 하위호환 옵셔널 증분이라 중복 ADR 지양(DECISIONS #19 원칙). 쓰기 시점 미저장이 읽기 필터보다 안전(A1·AC-041 선례). | [0005](adr/0005-retained-and-challenge-scoped-clone-voice.md)·[0006](adr/0006-user2-anonymous-auth-access.md) 증분 |
| 26 | 2026-07-24 | **(T33 소급 결정) `reports.resistedMoments?` 필드 신설 결정(구현은 후속 implementer 태스크).** UX-018 리플레이 해설 Empty 상태("한 번도 속지 않은 경우 → 시도된 수법 + '잘 대응한 지점' per-turn 주석")를 스키마상 충족 가능하게 `deceivedMoments`의 대칭 필드 `resistedMoments: array<{turnIndex,timeLabel,tactic,goodResponse}>?`(옵셔널 증분)를 ReportDoc에 추가한다. UX.md·PRD 변경 없이 (b)스펙 축소가 아니라 (a)필드 신설을 택함. | 저항 판정은 analyzeConversation.ts `RESISTANCE_PATTERN`이 **이미 매 턴 계산하지만 버리는** 값이라 기록만 하면 됨(대칭·저비용). AC-038 "신규 데이터 모델·분석 파이프라인 금지"와 무충돌 — 새 컬렉션·새 패스가 아니라 기존 리포트 문서 옵셔널 필드 1개. PRD AC-009/037(never-deceived=사실+수법)은 이 필드 없이도 충족되므로 PRD 무변경, UX-018은 이 필드로 spec 그대로 충족. per-turn 저항 마커를 요구하는 건 UX-018 States 셀뿐이라 UX 소관이 아닌 스키마 결정으로 정식화. | — |
| 36 | 2026-07-25 | **(UpdateRequests #4 해소) `onSessionEnded` 폐기 대상 식별 = Storage prefix 실나열(비준), ADR-0003의 "artifacts 매니페스트 순회" 원안 정정.** ADR-0003은 accepted 후 불변이므로 재작성하지 않고, API.md `onSessionEnded` 행 정정 + 본 행으로 비준한다(ADR-0006 Addendum A2와 동일 처리 방식). 폐기 대상·`deletionLogs` 기록·target별 부분 실패 재시도 등 나머지 ADR-0003 설계는 그대로 유효. UpdateRequests #4 → `resolved`. | 구현(`functions/src/guardrails/purge.ts:15-38`)이 원안을 이탈했지만 **이탈이 원안보다 옳다**: `artifacts`는 합성물 메타 전용 스키마라 원본 녹음 `voice_input.webm`을 매니페스트로 **식별할 수 없어**, 원안대로면 녹음이 영구 잔존하는 **실질적 AC-021 위반**이 발생한다. 또 T5 합성 경로가 Storage에 파일을 쓰지 않아 매니페스트가 실제로 비어 있었다. prefix 나열은 "그 세션 경로 아래 전부"라는 AC-021 의도를 정확히 구현한다. | [0003](adr/0003-immediate-artifact-purge.md) (정정 비준) |
| 35 | 2026-07-25 | **(T57) 실패 아카이브(UX-030) — 전수(페이지) 조회 + `tacticCategory` 정규화(역정규화 컬렉션 기각).** ① 별도 "속은 순간" 컬렉션을 만들지 않고 본인 `reports`를 `uid+createdAt desc`로 **50건 단위 커서 페이징** 후 클라가 `deceivedMoments`를 평탄화한다 — 필요한 복합 인덱스가 **이미 존재**(`firestore.indexes.json:11-18`)해 **신규 인덱스 0건**. ② 아카이브 카드용으로 `reports`에 `scenarioId?`·`channel?`·`difficultyLevel?`·`challengeId?`를 **역정규화**(리포트 생성 시 이미 읽는 세션에서 — N+1 방지). 시나리오 제목은 역정규화하지 않는다(콘텐츠 수정이 과거 카드에도 반영되도록). ③ **"수법별 묶기" 키 = `DeceivedMoment.tacticCategory?`**(고정 10종 enum)를 리포트 생성 시 정규화해 저장, 표시 문구는 기존 `tactic` 원문 유지, 부재→`tactic` 문자열 폴백(무백필). 매핑은 순서 있는 규칙표(순수 함수) + **13개 시나리오 전 라벨이 `other`로 안 떨어지는지 단언하는 드리프트 테스트**. ④ 사용자2(익명) 제외는 1차 uid 소유권 격리(§14.7.2, 이미 성립) + 2차 `challengeId` 보유 리포트 배제. | OQ-U19. (a) 개인 규모(리포트 수십 건)에서 역정규화 컬렉션은 성능 이득이 없고, 같은 사실이 두 곳에 저장되면 리포트 보존·삭제 정책이 생겼을 때 **고아 레코드**가 남아 폐기 문화(AC-021)와 어긋나며, 쓰기 경로 증가는 사용자2 데이터 유입 같은 사고 표면을 늘린다. (b) `tactic`은 시나리오 콘텐츠 라벨이라 같은 수법이 "긴급성 조성"/"다급함 조성"/"마감 압박"/"촉박한 결정 압박"으로 흩어진다(실측) — 정규화 없이는 "이 수법에 3번 넘어갔습니다"가 "1+1+1"이 되어 **이 화면의 존재 이유가 사라진다**. 표시는 원문 유지라 카피 손실 없음. | — |
| 34 | 2026-07-25 | **(T57) 사용자 선택형 난이도(UX-029) — 공통 모디파이어 블록 + 명시 필드 `difficultyLevel` + 조립 순서 불변식 코드 강제.** ① 시나리오별 3벌(13×3=39) 저작을 기각하고 `promptAssembly.ts`에 공통 `DIFFICULTY_MODIFIERS`를 얹는다(`CONVERSATION_STYLE`/`SCENARIO_PROGRESSION`과 동일 패턴). **`intermediate`는 블록 미출력**(기준선) → 기존 세션 프롬프트 문자열 무변경(회귀 0). ② 필드는 **새 이름 `difficultyLevel`**(sessions·challenges·reports, 부재→intermediate) — 시나리오 메타의 기존 산문 `difficulty`는 **손대지 않는다**(AC-002·미러 드리프트 테스트 무변경, 이름 오버로드 회피). ③ **`buildSystemPrompt(prompt, opts)`로 확장하고 `guardrailPreamble`은 언제나 마지막**; 호출부의 문자열 이어붙이기를 금지한다 — **기존 위반 `openingLine.ts:56`(`buildSystemPrompt(...) + OPENING_TURN_INSTRUCTION`)을 함께 교정**한다. ④ 초급 브리핑은 `weakenedTactics`의 **라벨만** 콜러블로 노출(설명부·인용구는 서버 잔류). ⑤ 난이도는 `analyzeConversation`·`computeDefenseGrade`의 입력이 **아니다**. ⑥ ElevenLabs 실시간 경로는 프롬프트가 에이전트 쪽이라 주입 지점이 없어 v1 미적용이며, `createRealtimeCall`이 `difficultyApplied:false`로 **명시**한다(조용한 미적용·근거 없는 배지 금지). | OQ-U18·D-41/42/43. 39벌은 유지비뿐 아니라 "한 벌만 고쳐 안전 문구가 빠지는" 드리프트 위험을 3배로 만든다. ③이 핵심 — 안전 지침이 뒤에 올수록 모델이 우선한다는 이유로 가드레일을 맨 뒤에 두는 현행 설계(promptAssembly.ts:52-54, 테스트로 고정)에서 난이도 블록이 그 뒤로 밀리면 **D-42(난이도는 안전장치를 게이팅하지 않는다)가 코드 레벨에서 깨진다**. ⑤는 판정 잣대가 난이도마다 달라지면 아카이브(#35) 누적 비교가 무의미해지기 때문. ⑥은 ADR-0004(프롬프트 클라 미노출)를 지키면서 사실을 숨기지 않는 유일한 조합. | — |
| 33 | 2026-07-25 | **(T57) 즉시 되감기(UX-028) — 원 세션 미재개·별도 1회성 드릴·리포트 하위 서브컬렉션 기록.** ① 원 세션·원 리포트를 **읽기 전용으로만** 참조하고 세션을 재개·신설하지 않는다(UX 권고 채택). ② 시도 기록은 `reports/{rid}/rewindAttempts/{auto}`에 append하며 **`reports/{rid}` 문서 update·두 번째 리포트 생성·`updateDefenseGrade` 호출·`sessions` write를 전부 금지**한다(AC-007 불변식). 최상위 `db.collection("reports")` 쿼리는 서브컬렉션을 포함하지 않아 기존 집계가 오염되지 않는다. ③ 판정은 신규 콜러블 `judgeRewindAnswer`(LLM 1차 + 기존 `RESISTANCE_PATTERN`/`COMPLIANCE_PATTERN` **export 재사용** 규칙 폴백), 결과는 3값 `good\|risky\|unclear`이며 판정 불가여도 `correctAction`은 항상 반환. 답변 상한 500자·리포트당 시도 50건. ④ 음성 입력은 v1 미제공(텍스트 전용). ⑤ AC-042 순서(정체공개→강제 해설→되감기)는 기존 UX-018 강제와 같은 **클라 층위**로 유지(서버 게이트 신설 안 함). | OQ-U17. 세션 재개는 종료·폐기(ADR-0003)를 되돌려야 해 AC-007과 AC-021을 동시에 흔든다. 새 세션 생성은 `sessionCount`/`defenseGrade`를 오염시키고 히스토리에 "훈련하지 않은 세션"을 만든다. `analyzeConversation` 직접 재사용은 (i) 대화 전체 짝짓기 구조라 단일 답변에 안 맞고 (ii) 2치라 "판단하기 어렵습니다"(기권)를 표현 못 하며 (iii) 그 순간의 수법 맥락을 보지 않는다 — 그래서 전용 판정을 두되 **패턴은 복제하지 말고 공유**해 두 곳이 갈라지지 않게 한다. | [0008](adr/0008-rewind-drill-execution-model.md) |
| 32 | 2026-07-25 | **(T57) 통화 중 문자(UX-027) — 오버레이는 같은 라우트의 형제 노드, 도착은 앱 오케스트레이션(실시간 경로에서 sentinel 마커 불가), 전용 서브컬렉션.** ① 오버레이는 `/session/play` 안 조건부 렌더(early return·신규 라우트·`key` 변경 금지) — 실시간 세션·마이크·오디오·타이머가 컴포넌트 마운트에 묶여 있기 때문. 오버레이 안에 **자체 "훈련 종료"** 를 둔다(포커스 트랩이 AC-006을 가두지 않게). ② 도착 신호는 **서버 소유 문자 카탈로그 + 턴 경계 트리거**로 앱이 전달하고, "문자 보냈어요" 대사는 그 순간 주입되는 1줄 지시로 유도한다. **`[[SMS:id]]` 류 sentinel은 실시간 경로에서 불가능** — Gemini Live는 오디오 모달리티 고정이라 서버가 사기범 텍스트를 보는 지점이 없고 마커를 **모델이 소리 내어 읽는다**. 텍스트 폴백 경로(`sendMessage`)는 같은 규칙을 서버에서 계산해 같은 문서를 쓴다. ③ 문자는 **`sessions/{sid}/inCallSms` 서브컬렉션**에 둔다 — `messages`에 넣으면 `analyzeConversation`의 scammer(i)↔user(i+1) 짝짓기가 어긋나 리포트 판정이 손상된다. ④ `MessengerAttachment`는 무변경(OTP는 링크가 아니라 표시용 코드 — 부재-오버로드 회피). **`url` 필드는 어떤 스키마에도 도입하지 않는다.** ⑤ 오버레이 열림 = **마이크 입력만** 정지(`muted \|\| overlayOpen`), 재생·타이머·소켓은 계속. ⑥ `SCENARIO_PROGRESSION`의 "화면에 없는 것을 가리키지 않는다"(인증번호 요구 금지)를 **조건형으로 교정**해야 기능이 발동한다. | OQ-U16·D-35~D-38. 인과 역전(앱이 보내고 모델이 알린다)의 트레이드오프를 받아들이는 이유: 모델 주도(sentinel/function calling)는 **도착 자체를 보장하지 못해** UX가 지목한 실패 — "문자 보냈어요라고 했는데 문자가 안 온다"(UF-008 Failure (a)) — 를 그대로 남긴다. 앱 주도는 그 실패를 **구조적으로 제거**하고, 반대 실패(문자는 왔는데 언급이 없음)는 배너·aria-live·문자함으로 학습 가치가 보존된다. function calling은 `tools:[]` 보안 잠금을 건드리고 DECISIONS #15가 기각한 배선 복잡도를 프로바이더별로 되살리므로 확장 여지로만 남긴다. 턴 카운팅이 클라에 있는 것은 §13.5 스킨과 같은 프레젠테이션 층위 — 문자 내용은 서버 카탈로그에서만 나오고 서버가 `smsId` 소속을 재검증하므로 안전 판정을 게이팅하지 않는다. | [0007](adr/0007-in-call-sms-delivery-model.md) |
| 31 | 2026-07-24 | **(T55 아키텍트 게이트) generic 보이스 2인 챌린지(#23) + 체험/발송 모드 배선 — voiceMode 판별자 확장(ADR-0005/0006 증분, 새 ADR 없음).** ① **스키마**: `challenges.voiceMode?`(부재→clone, 무백필) 신규 — `channel`=voice 챌린지의 clone/generic 판별자, 생성 시 `PUBLIC_SCENARIOS[scenarioId].voiceMode`로 역정규화. **voiceId-부재를 판별자로 오버로드하지 않음**(§14.8.1이 `channel`에 대해 기각한 안티패턴 동형: 결과 요약 게이트가 양 판별자 필요·#21 messenger+voiceId 병존·store-nothing-sensitive는 부재추론 부적합). ② **`consentChallenge` 오프닝 합성 3분기**(messenger:스킵/clone:challenge.voiceId/generic:`GENERIC_VOICE_ID`) — self-training generic과 동일 값·provider. ③ **`createRealtimeCall` 무개정** — generic 수신자는 시나리오 `voiceMode="generic"`이 `getRealtimeProvider`를 Gemini Live로 라우팅, `effectiveVoiceId=""`, A2 블랭킹(`provider!=="elevenlabs"`)이 이미 `voiceId:""` 반환 → clone보다 단순(A2 예외 미탑승). ④ **resultSummary**: `deriveChallengeResultSummary`에 `voiceMode` 추가 → generic은 `{completed}`만 파생·저장(쓰기 시점 강제, OQ-32 default="완료 여부만"=AC-055 동형). ⑤ **활성 상한**: 이미 채널·음성모드 무관 전역 합산(§14.8.4 재확인) — 무변경. ⑥ **모드(self\|send) 클라 배선**: `sessionStorage` 힌트 `experienceMode`(peek 방식, `selectedTrainingType`/`selectedVoiceModeChoice` 형제) — T49가 은퇴시킨 단발 소비형 `setChallengeMode` 안티패턴을 올바른 크기(peek·enum·공용 clear 편입)로 재편입. ⑦ 서버측 `GENERIC_VOICE_ID` 상수 부재(implementer 갭). | AC-056/057/058·OQ-32. generic 보이스 챌린지는 클론·voiceId·통화 자격증명·onSessionEnded voice삭제를 아예 안 타는 §14.8(메신저 챌린지)과 동형 "깨끗한 부분집합" — ADR-0005 §14.2·ADR-0006 A2 유출 표면 부재(generic은 `provider!=="elevenlabs"`). 새 구조 결정이 아니라 하위호환 옵셔널 증분이라 중복 ADR 지양(DECISIONS #19/#30 원칙). AC-057(clone 자기체험 배제)은 순수 클라 라우팅 재배치라 스키마·콜러블 무변경. 모드 힌트가 위조돼도 서버가 재검증(createSession/createChallenge 인증·게이팅)해 안전 미게이팅. | [0005](adr/0005-retained-and-challenge-scoped-clone-voice.md)·[0006](adr/0006-user2-anonymous-auth-access.md) 증분 |
| 42 | 2026-07-26 | **(T80) 3단계 결합(문자→모의 앱 설치→통화) + 모의 설치 단계 — 신규 상태·신규 신호·신규 화면 계열 0건, 응낙만 속은 순간으로 승격(ADR 신설 없음).** ① **3단계는 3개 세션이 아니다** — 2단계(모의 설치)는 채널이 아니라 **메신저 채널 위 오버레이**(UX-023 kind=`app-install`)라 세션의 `channel`은 여전히 `messenger→voice` 1회만 전이한다. §13.1/§13.2 무변경, **신규 세션 필드 0건**, `reportId=sessionId` 멱등 유지 → **AC-007/AC-035가 구조적으로 성립**한다. ② **모의 설치는 `MessengerFakeLanding.tsx` 파일 *안에서* kind 분기**한다(props `landingKind?`, 부재→`credential-form`). 실측상 이 컴포넌트는 콘텐츠 분기가 0개이고 안전 계약(네트워크 import 부재·표식·상시 종료)이 **파일 단위로** 성립하므로, 파일을 쪼개는 순간 AC-072가 금지한 **검증 경로 이중화**가 실제로 발생한다. kind의 출처는 서버 전용 카탈로그 `functions/src/scenarios/mockScreens.ts`(`Record<scenarioId, MockScreenItem[]>` — `IN_CALL_SMS` 미러)이고 `LINK_LABELS`는 무변경. ③ **전이 신호 신설 0건** — 1→2는 사용자의 링크 탭(기존 `[[LINK:id]]`), 2→3은 기존 `[[SIGNAL:ESCALATE_VOICE]]`(이 시나리오는 이미 `escalation` 배선을 갖는다). **설치 응낙이 전이를 자동 유발하지 않는다**(그건 신규 트리거이며 AC-073 위반) — 응낙 사실은 §15.1.2/§15.1.4와 동형으로 **다음 턴 `turnInstruction` 1줄**로만 모델에 전달한다(슬롯 경합 시 문자 announce 우선·설치는 이월). ④ **중간 종료·전이 실패**: 세션이 하나라 리포트도 하나다. 오버레이 안 "훈련 종료"는 이미 구현돼 있고(`MessengerFakeLanding.tsx:50`), 전이 실패 시 **`channel`/`channelHistory` 롤백은 금지**(append-only 진실 원천 오염 — §13.8.3 계승)하고 화면에 표시 후 종료 폴백. ⑤ **승격 규칙(D-51 판정표의 데이터화)**: **가짜 "권한 허용"에 응한 경우에만** `deceivedMoments` 1건 추가, 화면이 뜨거나 닫힌 것은 표시 전용(`reports.mockScreenTimeline?`). 앵커는 **설치 링크를 실은 사기범 메시지의 `turnIndex`**이며 **앵커 미해결이면 승격하지 않는다**(인덱스 정합 보호). ⑥ **OQ-U24 판정**: `reports.stages?`에 **미도달 단계까지 전부** `{stage, reached}`로 싣고, **화면은 도달 단계만** 그리며 상단 1줄로 구조를 사후 고지한다. ⑦ `tacticCategory`는 기존 `link_or_install`로 자연 정규화 — **신규 카테고리 0건**. | AC-072/AC-073·D-49/D-50/D-51·OQ-U24. **(a) 왜 §15.6 G22("표시만, 판정 무변경")를 따르지 않았나**: G22는 *문자 이벤트* 규칙이고 그 근거 4항 중 둘이 여기선 성립하지 않는다 — **AC-072가 "속은 시점 교육 포인트화"를 명문 요구**하고(문자의 AC-059는 "함께 다뤄진다"였다), **되감기가 전제하는 "그 순간의 사기범 대사"가 설치에는 존재한다**(링크를 실은 그 메시지). 연쇄 영향(AC-062/068/010/011)은 **응낙 기준을 채널 무관하게 일관 적용**해 통제한다 — 링크 탭·문자 탭은 여전히 승격하지 않으므로 채널 간 비대칭이 생기지 않는다. **(b) 왜 세션을 쪼개지 않았나**: 3세션이면 리포트가 3개가 되어 AC-007이 깨지고, 이미 병합된 T70(되감기)·T74(아카이브)가 그 불변식 위에 서 있다. **(c) 왜 새 ADR이 아닌가**: ADR-0007(문자 전달 모델)·ADR-0008(되감기 실행 모델)의 어느 결정도 **뒤집지 않는다** — 문자에 대한 "표시 전용" 규칙은 그대로 유효하고, 이번 것은 *다른 이벤트 클래스*에 대한 신규 규칙이자 하위호환 옵셔널 증분이다(DECISIONS #19/#30/#31/#37이 확립한 "증분에는 중복 ADR을 만들지 않는다" 원칙 계승). **(d) 왜 클라가 아니라 서버 카탈로그가 kind를 정하나**: 클라가 `fakeLandingId` 문자열을 분류하면 자유문자열 판별이 되고(AC-024 원칙 위반), 위조 시 가짜 "속은 순간"이 생긴다 — 그래서 `recordMockScreenEvent`가 **시나리오 소속을 재검증**한다(§15.6 G12 동형). | — (ADR 신설 없음 — [0007](adr/0007-in-call-sms-delivery-model.md)·[0008](adr/0008-rewind-drill-execution-model.md)와 비충돌 증분) |
| 37 | 2026-07-25 | **(T68 REJECT 후속) 통화 중 문자 이벤트의 리포트·리플레이 타임라인 통합 — 리포트 생성 시점 스냅샷 + 턴 앵커 병합(ADR-0007 증분, 새 ADR 없음).** AC-059의 *"문자 확인·링크 탭·인증번호 노출은 하나의 세션 타임라인에 기록되어 리포트(AC-026)·리플레이(AC-038)에서 함께 다뤄진다"* 조항을 **설계한다(유예 아님)**. ① **수집**: `generateReportForSession`이 `sessions/{sid}/inCallSms`를 **1회 read**해 표시 전용 배열 `reports/{rid}.smsTimeline`에 **스냅샷 역정규화**(이벤트 발생 시 이중 기록 금지). ② **판정 무변경**: `analyzeConversation`의 입력이 아니라 **산출 뒤에 얹히는 배열** — `messages`·`wasDeceived`·`deceivedMoments`·`tacticsUsed`·`preventionAdvice` 전부 무변경(G3 재발 금지, 회귀 테스트 필수). ③ **AC-007 무변경**: 문서 필드 1개 추가일 뿐 두 번째 리포트·서브컬렉션 없음, 멱등 early-return 유지라 **최초 1회만** 기록. 되감기가 append 서브컬렉션을 쓴 이유(종료 후에도 계속 생기는 행위)가 여기엔 없다 — 문자 이벤트는 종료 시점에 확정된 사실. ④ **병합 축 = 턴 앵커**: `inCallSms.anchorScammerTurn`(서버가 카탈로그에서 계산, `buildInCallSmsDoc` 단일 지점) → 리포트 생성 시 실제 `turnIndex`로 해결. **시각으로 병합하지 않는다.** ⑤ **표시**: 리플레이=기존 사기범 말풍선 + 기존 주석 카드, 리포트=기존 "속은 순간" 카드 형식(문구만 다름). **신규 컴포넌트·신규 표기 형식 0건.** ⑥ **콜러블 계약 무변경**: `sms_otp_shown`은 `kind==="otp" && openedAt`의 **파생 표기**이고 `sms_overlay_closed`는 **기록하지 않는다**. `recordInCallSmsEvent`에는 `status==="active"` 검증만 추가. ⑦ **AC-061은 이미 충족** — 인증번호를 통화로 불러준 사실은 `COMPLIANCE_PATTERN`의 숫자 전용 답변 앵커가 T68 이전부터 잡는다. 이미 되는 것을 다시 만들지 않는다. | AC-059 잔여 조항. **(a) 왜 유예하지 않았나**: UX-027 Priority=Critical이고 AC-059가 명문 요구이며, 유예하려면 PRD 문면 수정(planner 소관)이 필요해 설계보다 무거운 경로다. **(b) 왜 시계로 병합하지 않나(실측)**: 실시간 경로의 `messages.createdAt`은 실제 발화 시각이 아니라 **통화 종료 시점 합성값**이다(`submitTranscript.ts:64,78` — `baseTime=Date.now()` + `i*1000`). `arrivedAt`은 통화 중 실제 시각이라 시간 병합 시 **모든 문자가 대화 맨 앞에 몰리고**, 폴백 경로는 정상 동작해 **두 경로가 갈라진다**(§15.1.2 "두 경로가 같은 컬렉션 하나" 원칙 위반). 표시용 `timeLabel`도 같은 이유로 앵커 메시지에서 파생해 `deceivedMoments`와 축을 맞춘다 — 대화 라벨 자체가 이미 근사값이므로 **정합성이 정확도보다 우선**. **(c) 왜 `deceivedMoments`로 승격하지 않나**: AC-059/UX-008 문면은 "함께 다뤄진다/표시된다"이지 판정이 아니고, 승격하면 `wasDeceived`가 링크 탭만으로 true가 되어 AC-062(되감기 진입 조건)·AC-068(아카이브)·AC-010/011(방어 등급)이 연쇄로 흔들린다. 메신저 링크 탭도 현재 판정에 안 들어가므로(실측) 문자만 승격하면 **채널 간 비대칭**이 생기고, 되감기 판정이 전제하는 "그 순간의 사기범 대사"가 문자 순간엔 없다. **(d) 왜 새 ADR이 아닌가**: ADR-0007이 정한 전달 모델·컬렉션 분리를 뒤집지 않는 **하위호환 옵셔널 증분**이다(DECISIONS #19/#30/#31 원칙 계승). | [0007](adr/0007-in-call-sms-delivery-model.md) 증분 |
| 38 | 2026-07-26 | **(T79 아키텍트 게이트) 확인 시도 무력화(축 D3)의 트리거 = ADR-0007 앱 오케스트레이션 복제 — §15.6 G4 해소(조건부).** ① **G4의 사실관계는 바뀌지 않는다**(실시간 경로에 사기범 텍스트 관찰 지점 없음 — `geminiProvider.ts:86` 오디오 고정, 전사는 종료 시 일괄, `tools:[]` 잠금, ElevenLabs는 주입 지점 자체가 부재). 대신 **"앱이 먼저 사건을 일으키고 모델에게 알리라고 시키는"** 반대 방향(T68에서 이미 라이브 동작)을 그대로 복제한다 — **신규 메커니즘 0건**. ② **층 분리**: *"확인하겠다"에 막지 않고 권하는 **대사**는 프롬프트(T81 L3/D3)가*, *UX-031 진입 **컨트롤**은 앱이* 만든다. 앱은 참가자·사기범 자유텍스트를 **어디서도 분류하지 않는다**(AC-024/034/060). ③ **write 경로 1개**(`deliverVerifyOffer`) — SMS가 두 경로로 갈린 것보다 단순. 실시간은 `instructionTurn` 주입, 폴백은 `sendMessage`가 미announce 오퍼를 읽어 `turnInstruction` 주입. ④ **렌더 소스는 `sessions/{sid}/verifyIntercept` 구독 하나**(콜러블 응답에는 모델 지시만 — DECISIONS #12 계승). ⑤ **경로 게이팅 2겹**: `createRealtimeCall`이 `verifyOffer?`를 **카탈로그 보유 && advanced && `difficultyApplied`** 일 때만 부착 + 콜러블이 소유·활성·카탈로그·난이도·**프로바이더(elevenlabs 아님)** 를 전부 재검증(G12 선례). ⑥ **AC-019 구조적 부재 유지**: `url`·`tel`·번호 입력 필드·다이얼 인텐트에 해당하는 스키마·콜러블이 **존재하지 않는다**(번호는 표시 텍스트 1개). ⑦ **OQ-U22 확정.** | AC-071/AC-074(L3)·OQ-U22. **(a) 왜 sentinel·function calling·전사 탐지가 아닌가**: 각각 구조적 불가(모델이 마커를 낭독)·`tools:[]` 보안 잠금 해제와 도착 미보장·**자유텍스트 분류 금지** 위반. §15.1.2가 같은 후보들을 같은 근거로 이미 기각했다. **(b) 받아들이는 트레이드오프**: 오퍼 **시점**은 참가자 발화가 아니라 턴 게이트가 정한다(사기범이 선제적으로 권할 수 있다) — 대신 UF-011 Failure (a)("권한다고 말했는데 컨트롤이 안 뜸")가 **구조적으로 사라진다**. ADR-0007이 SMS에서 받아들인 것과 동일한 비대칭. **(c) ElevenLabs 미적용은 새 결손이 아니다**: 그 경로는 애초에 난이도가 적용되지 않고(`difficultyApplied:false`, G6) AC-071은 **고급 전용** 능력이라 일관된 귀결이다. **(d) UX 반려 0건**: UF-011 Trigger 문면만 기술적으로 정정하며(§15.1.2가 D-36에 한 것과 동일 정정) UX-031·D-47/48/51/52·P-24/25는 그대로 성립한다. | [0007](adr/0007-in-call-sms-delivery-model.md) 재적용(새 ADR 없음) |
| 39 | 2026-07-26 | **(T79) 확인 무력화 순간의 판정 반영 = 순간 신설이 아니라 기존 순간 주석.** `analyzeConversation`(시그니처·입력·짝짓기 루프) **무변경**. 산출 뒤 순수 함수가 **재연결 대사 turnIndex 뒤**의 기존 순간에 `afterVerifyReconnect:true`를 얹고 `tactic="확인 시도 무력화"`·`tacticCategory="verification_block"`·`correctAction=VERIFY_INTERCEPT_CORRECT_ACTION`으로 **덮어쓴다**. 순간 개수·`turnIndex`·`timeLabel`·`wasDeceived`는 **한 건도 바뀌지 않는다**. D-51 판정표 ①(권유·미발신)=기록만·주석 0, ⑤(발신·미응낙)=주석 0·"잘 대응한 지점", ②(발신·응낙)=주석. 표시는 `reports/{rid}.verifyTimeline?`(표시 전용 스냅샷, §15.1.5 패턴 재사용)이며 **표시 앵커(오퍼)와 판정 앵커(재연결 대사)를 분리**한다. | AC-071 + AC-062/AC-007/AC-010/011/AC-068 동시 충족. **(a) 왜 순간을 추가하지 않나**: 확인 무력화의 응낙은 **참가자의 대화 발화**라 `analyzeConversation`이 **이미 순간으로 잡는다**(analyzeConversation.ts:136-164) — 추가하면 같은 응낙이 두 번 계상돼 아카이브 항목 수·방어등급이 부풀고 `getAnnotatedTurnIndexes`↔`deceivedMoments` 1:1 전제가 깨져 되감기가 엉뚱한 순간을 연다(G16). **(b) 왜 표시 전용으로만 두지 않나**: AC-071이 *"확인했는데도 속은 순간으로 짚는다"* 를 명문 요구한다 — §15.1.5 (5)가 문자에 건 금지와 **요구 방향이 반대**다. **(c) 왜 `correctAction`을 덮어쓰나(안전)**: `pickCorrectAction` 첫 규칙 `/확인\|전화/`가 *"알고 있는 번호로 직접 전화해 확인하세요"* 를 돌려주는데(analyzeConversation.ts:176-179), 방금 확인 전화가 소용없던 참가자에게 그대로 나가면 AC-071 무력감 방지 요건을 훼손한다. **(d) 왜 판정 앵커가 따로 필요한가**: 폴백 경로는 참가자가 말해야 재연결 대사가 나오므로 표시 앵커로 자르면 **재연결 전 순응까지 오분류**된다. | [0009](adr/0009-verify-intercept-moment-annotation.md) |
| 40 | 2026-07-26 | **(T79) OQ-U23 확정 — 모의 재연결 상대의 음색은 v1에서 바꾸지 않는다.** 대신 표면 전환 4종(발신자 라벨 `reconnectedCallerLabel` / "연결 중…" 2.5초 / 다른 담당자로서 자기소개·정형 존대 톤 전환 지시 / 앞선 요구를 "확인해 드렸다" 형태로 되풀이)으로 다른 상대를 표현한다. **확장 경로(설정만)**: 폴백 TTS 경로에 한해 카탈로그 `reconnectVoicePreset?`로 `FALLBACK_VOICE_MALE_ID`/`_FEMALE_ID` 사용 가능한 형태를 남긴다(§15.3.3 "확장 경로" 관례). 한계는 숨기지 않고 §16.6 갭 + T87 QA 라이브 측정 항목으로 등재한다. | **(a) Gemini Live 세션 중 음성 전환은 우리 구조에서 불가**: 음성은 서버가 단기 토큰의 `liveConnectConstraints`에 고정하며(geminiProvider.ts:88-91) 클라가 바꾸게 만드는 것은 **ADR-0004 잠금(모델·프롬프트·도구·음성 서버 고정)을 스스로 뚫는 것**이다. API 차원의 세션 중 `speechConfig` 갱신 가능 여부도 공식 문서로 확인되지 않았다(웹 확인 결과 상충 — **추정에 기능 가치를 걸지 않는다**). **(b) 소켓 재연결 기각**: 통화가 실제로 끊기고 Live 대화 컨텍스트가 소실돼 "앞선 요구를 확인해 주는" AC-071 요건이 무너지며 전사·앵커가 두 구간으로 갈라진다(G21류 표면 증가). **(c) 폴백만 다른 목소리 기각**: 주 경로에서 불가능한 것을 폴백에서만 하면 두 경로 체감이 갈라진다(§15.1.2 단일 체감 원칙). **(d) UX의 "같은 목소리면 재현 미달성"은 추정이며 측정된 바 없다** — 측정 전에 무거운 재연결 아키텍처를 도입하지 않는다(§0.1). | — |
| 41 | 2026-07-26 | **(T78) 수법 축(A~E) 태깅의 소재지·커버리지 산출·`tacticCategory`와의 직교성 확정.** ① **소재지**: 축 태깅은 `ScenarioMeta`/`ScenarioDoc`·Firestore `scenarios/{}`·클라 계약 **어디에도 넣지 않고** `functions/src/scenarios/axes.ts` **단일 소스**(미러 0벌)에만 둔다. ② **열거형**: 축마다 `as const` + 파생 union 5벌, 값 이름은 `<PRD코드>_<의미>`(`D3_verification_hijack`). `other` 폴백 **없음**. PRD 매핑 표의 D열 "(없음)" 4종은 **`D0_none` sentinel**로 인코딩한다(빈 배열 금지 — "수법 없음"과 "미태깅"이 구분 불가해지면 AC-070이 무력화된다). ③ **커버리지**: 빌드타임 상수 순회 순수 함수. **태깅된 값이 아니라 열거형 전 도메인을 순회**하고(그래야 E4가 0건으로 남는다), 사유를 담은 `DECLARED_COVERAGE_GAPS`와 **양방향 `deepEqual`**로 공백이 사라지는 것과 조용히 채워지는 것을 동시에 막는다. 런타임 집계·콜러블·**커버리지 화면 미신설**(D-45/D-46). ④ **태깅 누락 강제**: 타입 2겹(5축 required + 비어 있지 않은 배열) + **T91식 키 `deepEqual` 게이트**. T91의 문자열 라벨 검사는 **불채택**. ⑤ **OQ-U25 = 직교 확정**: `DeceivedMoment`에 축 필드 금지·`TACTIC_CATEGORIES`에 축 코드 금지·두 모듈 상호 import 금지. 신규 순간 2종은 기존 값 매핑(`link_or_install`/`verification_block`)이며 **enum append 0건**, 대신 `CATEGORY_RULES` 정규식 2행 확장이 필요하다. ⑥ **Database.md·API.md·`firestore.rules`·인덱스 전부 무변경**(신규 컬렉션·필드·콜러블 0). | AC-070/076/077·OQ-U25·D-46. **(a) 왜 `ScenarioMeta` 증분을 기각했나(실측)**: `scenarios/{}`는 클라 read 허용 문서라(`firestore.rules:63-67`) 그 필드는 정의상 사용자에게 갈 수 있는 값이다 — D-46을 "안 그리기"가 아니라 **"가질 수 없음"**으로 만들려면 화면 계약 밖에 둬야 한다(UX D-46 Impact가 같은 인계를 남겼다). 게다가 미러 드리프트 테스트가 실제로 지키는 범위는 **1/13 시나리오 × 3필드**뿐이고(`scenarios.test.ts:108-120`), **두 미러는 이미 `callerLabel`에서 13/13 어긋나 있다**(`publicMeta.ts:68` "가족 (사칭)" vs `familyAccidentDeepvoice.ts:74` "010-2447-8815", T75가 클라만 갱신) — 축 65칸을 **존재하지 않는 방어** 위에 얹는 셈이다. 코드 상수에만 두면 재시딩이 불요해 커버리지가 배포 상태와 무관하게 정확하다. **(b) 왜 전 도메인 순회인가**: 데이터를 순회하며 카운트를 올리면 0건 값은 키 자체가 안 생겨 **E4가 조용히 사라진다** — AC-076이 "미충족"이라고 명시한 바로 그 실패다. **(c) 왜 직교인가**: 축은 콘텐츠에 붙는 설계시 상수이고 `tacticCategory`는 한 순간에 붙는 런타임 관측값이라 **축→카테고리가 함수가 아니며**(세션마다 다르다), 정규화 방향도 반대다(축=사전 선언·`other` 없어야 함 / 카테고리=사후 흡수·`other` 있어야 함, §15.4.2). 무엇보다 직교로 두면 `DeceivedMoment`에 필드가 0개 추가되어 **AC-077의 "과거 항목 무소급"이 구조적으로 보장**된다. **(d) 도입 즉시 드러난 것**: 기계 산출이 PRD 손계산 오류 2건을 노출했다 — **A4(지인·가족 계정 도용)가 0건인데 PRD 공백 표에 없고**, **B1 count가 표기된 2가 아니라 3**이다(§15.10.9 G35/G36, planner 확인 요청). | [0010](adr/0010-scenario-tactic-axis-tagging.md) |
| 43 | 2026-07-26 | **(T81) 난이도 L3는 `SCENARIO_AXES`를 읽지 않는다 — §15.10.8 인계를 뒤집는다(하드 게이트 판정).** ① **판정 (b) 채택**: 축 표의 D 언더카운트를 T85 범위에서 고치지도(안 a), 선행 태스크로 분리해 T85를 막지도(안 c) 않는다. 대신 **표를 읽지 않아도 성립하는 판정 규칙**을 설계한다. ② **실측 재확인**: 언더카운트는 T82 reviewer가 quantify한 **D5 5행이 아니라 D1·D2·D5에 걸친 9/13행**이다(§17.1.1 전수 대조 — 일치 4행뿐). ③ **초급의 "축 D 0~1개"를 세기(count)로 구현하지 않는다** — 누락은 곧 과소 계수라 `tax-refund-scam`(실제 D2+D5, 표는 D2)이 경계에서 오분류된다. 초급은 **행동 범주 억제 상한**으로 쓴다. ④ **고급은 표에 묻지 않고 부여한다** — D4는 프롬프트 레버(§17.4), D3는 이미 배선된 `hasVerifyIntercept` 게이트(T83, 신설 0). ⑤ **표 정정 자체는 조용히 넘기지 않는다** — **G60**으로 등재하고 소관을 축 모델 계열(T78/T82 후속)로 명시하며, T85 착수 차단 요인이 아님을 못 박는다. | AC-074/AC-075 + AC-076 정확도. **(a) 왜 T85 범위(안 a)가 아닌가**: 정정 대상이 9행 전면 재검토라 T85 diff에 좌표 변경이 섞이면 **T87 (1)의 "재구성 전 고급 vs 후 고급" 대조가 두 변인을 동시에 움직인 비교**가 되어 AC-074 증거가 오염된다. 게다가 `axes.ts`는 T95가 동시 편집 중이라 병합 표면이 겹친다. **(b) 왜 선행 태스크(안 c)가 아닌가**: 표를 오늘 고쳐도 §15.10.4가 이미 *"게이트는 값을 **가졌다**는 사실만 증명하고 값이 **맞다**는 것은 증명하지 못한다"* 고 자인했다 — 손으로 유지하는 요약 표를 런타임 입력으로 삼는 **결합 자체가 결함**이라 정정은 한 번 미루는 것일 뿐이다. 그리고 사용자 지적의 직접 해소(T85)를 커버리지 정확도 작업 뒤로 미루게 되어 우선순위가 뒤집힌다. **(c) 왜 결합 자체가 결함인가**: §15.10.3이 축 표의 소비처를 *"① 테스트 단언 ② 사람이 읽는 리포트 스크립트"* **2곳뿐**으로 확정했다. 프롬프트 조립 입력으로 승격하면 **커버리지 목적의 좌표 수정이 라이브 프롬프트를 조용히 바꾼다**(T95가 신규 시나리오를 태깅하며 기존 행을 손보면 무관한 시나리오의 고급 대사가 바뀐다). **(d) 안전 방향 논증**: 표의 오류는 **전부 누락이며 허위 추가 0건**이라, "몇 개인가"를 세는 규칙만 위험하고 "무엇을 부여할까"는 애초에 표를 볼 필요가 없다. | [0011](adr/0011-difficulty-lever-assembly-model.md) ⚠️ **위임 근거 정정(reviewer Major 1)**: §15.10.8의 유보는 폴백 권고안에 걸린 것이고 "exitBlock이 L3 입력"이라는 평서문에는 유보가 없다. 이 반려는 위임 행사가 아니라 **자기 소유 문서의 근거 있는 개정**이며, **사용자 확인 후 T85 착수**한다. |
| 44 | 2026-07-26 | **(T81) 4레버 조립 모델 = 공통 블록 유지 + L3 변형 2벌(총 4벌) + 옵션 1비트. 세 난이도의 축 D 규칙은 비대칭이다.** ① **판정**: L1·L2·L4는 시나리오 무관이라 현행 공통 블록에 담기고 **L3만 담기지 않는다** → `DIFFICULTY_MODIFIERS: Record<...>` → `buildDifficultyBlock(level, opts)` 빌더로 전환. 저작 블록 **4벌**(초급 1·고급 공통 1·고급 L3 절차형 1·고급 L3 축소형 1[=빈 블록]). **13×3=39벌 기각은 유지**(§15.3.1 근거 그대로 — 안전 문구는 여전히 단일 소스이며 4벌 어디에도 복제되지 않는다). ② **결합 수단은 축 태깅이 아니라 `l3Procedural?: boolean` 옵션 1개** — `inCallSmsEnabled`/`verifyInterceptEnabled`와 같은 형태라 새 관례 0개이고, `promptAssembly.ts`가 **타입 외 import 0**인 순수 조립 함수라는 성질이 보존된다. 모드 결정은 신규 순수 모듈 `roleplay/l3Depth.ts`가 단독으로 한다(`axes.ts` import 금지). ③ **사전 정의값(임의 판단 금지)**: 초급 = 허용 D `{D1_time_pressure}` **상한 1개** · 요구 단계 **1** / 중급 = **강제 없음(블록 `null`, 기준선)** · 요구 단계 **1** / 고급 = 기준선 **+ D4 필수 + D3(메커닉 보유 시) 필수** · 요구 단계 **2**(선행 1 → 최종 1, 3단 이상 금지). ④ **규칙 성격이 비대칭이다** — 초급 = **상한(능력 제거)**, 고급 = **하한(능력 추가)**. 난이도 블록은 **안전장치를 빼는 방향으로 절대 동작하지 않는다**. ⑤ **L3 깊이 모드 선언표 13행**(`d3_and_d4` 5 / `d4_only` 4 / `reduced` 4), 판정 질문은 하나 — *"이 페르소나가 접수번호·처리 단계·기한을 말해도 캐릭터가 무너지지 않는가."* 키 `deepEqual` 게이트 + `hasVerifyIntercept` 일관성 + 분포 5/4/4를 테스트로 고정. ⑥ **조립 순서 무변경**(§15.5 1~8) — 6번 항목의 문자열만 길어져 **T83이 추가한 `[T83 전수]` 2건과 `intermediate ≡ 기준선` 1건이 무개정 통과**한다. | AC-074·AC-065·AC-064. **(a) 왜 중급의 PRD 표 값 `{D1,D2,D5}`를 강제하지 않나**: 강제하면 중급이 블록을 내보내야 해 §15.3.1 기준선 불변식과 기존 테스트(`promptAssembly.test.ts:300-308`)가 동시에 깨지고, `kidnapping-threat`의 `D6`처럼 **중급이 기존 시나리오의 수법을 빼앗는다**. PRD 중급 행은 *현행 기준선에 대한 서술*이지 강제 대상이 아니다. **(b) 왜 초급을 라벨 열거로 쓰지 않나**: 같은 수법이 시나리오마다 6가지 표기다(§15.4.2 실측) — 열거하면 표기가 다른 시나리오에서 조용히 빠져 **난이도가 시나리오마다 다른 뜻**이 된다. **(c) 왜 L4가 "요구를 미루는 것"이 아닌가**: 현행 프롬프트 주석(`promptAssembly.ts:27-39`)이 기록한 실측 사고 — **12개 시나리오 전부가 요구에 도달하지 못했다**. L4를 지연으로 쓰면 그 결함이 **고급에서만** 재발한다. 그래서 **선행 요구도 구체적 행동 지시**이고 `[진행 강제]`의 2~3턴 예산은 선행 요구에 적용된다. **(d) 왜 고급 현행 문구를 교체하나**: `promptAssembly.ts:114` *"요구에 더 빨리 도달한다"* 가 AC-074 L4와 정면 모순이라, 두면 L4를 구현해도 발동하지 않는다(G61). | [0011](adr/0011-difficulty-lever-assembly-model.md) |
| 45 | 2026-07-26 | **(T81) 고급 L3 폴백 = §15.10.8 옵션 A 채택(범위 4/13으로 축소) + 기록은 저장 필드가 아니라 파생 함수. AC-076의 D4 공백은 `loan-refinance-scam`이 채운다.** ① **옵션 A 채택** — `reduced` 4종(가족·지인 사칭)은 고급에서 L3를 내지 않되 **L1·L2·L4는 전부 적용**되므로 "중급처럼 동작"하지 않는다. 원안의 전제(*"D3/D4를 가진 시나리오가 0개"*)는 9/13에서 **성립하지 않는다** — D4가 콘텐츠가 아니라 레버이기 때문이다. **옵션 B·C는 §15.10.8 권고대로 기각**(B는 AC-064 *"난이도는 시나리오 목록을 필터링하지 않는다"* 정면 위반, C는 훈련 내부 사정 노출로 D-6/AC-066 취지와 어긋남). ② **축소 사실을 프롬프트에 쓰지 않는다** — `ADVANCED_L3_REDUCED`는 **빈 블록**이다. 문구를 저작하면 모델이 그것을 대사로 흘릴 위험이 있다. ③ **기록 수단 = 파생 순수 함수 `isL3Applied(scenarioId, level)` + 분포 5/4/4 테스트 단언.** `sessions/{}` 신규 필드도, `createRealtimeCall` 응답 증분도 만들지 않는다 → **Database.md·API.md 무변경 = AC-077 무스키마 유지.** ④ **AC-076의 D4 0건 해소 대상 = `loan-refinance-scam` 1종**(weakenedTactics 라벨 1개 + `axes.ts` D4 1값 + `DECLARED_COVERAGE_GAPS` 1행 삭제). **이것은 §17.1의 D5 언더카운트 정정과 다른 사안**이며 T85는 전자만 한다. | AC-074/AC-076/AC-077·AC-064. **(a) 왜 저장하지 않나**: `isL3Applied`는 이미 저장된 두 값(`scenarioId`·`difficultyLevel`)의 **순수 함수**다 — 파생 가능한 값을 저장하면 드리프트 원천이 된다(§15.4.1이 역정규화 컬렉션을 기각한 것과 같은 논리). `createRealtimeCall` 응답에만 실으면 텍스트·오프닝 경로에서 알 수 없어 §15.6 G5와 같은 비대칭이 생긴다 — `difficultyApplied`는 *그 경로 고유의 구조적 한계*라 응답에 실렸고 L3 축소는 **경로 무관 시나리오 속성**이라 성격이 다르다. **(b) 왜 D4 대상이 `loan-refinance-scam`인가(임의 선택 아님)**: PRD 근거 데이터 #5가 **이름으로 지목**한다 — *"대출빙자형 61% 증가 … 실제 상담처럼 **서류까지 갖춰** … **현행 `loan-refinance-scam`은 즉석 압박형**"*. planner가 이미 특정해 둔 것을 architect가 옮긴 것이다. **(c) 왜 `SCENARIO_AXES`에 난이도 오버레이를 쓰지 않나**: 그 표는 **중급 기준선 좌표표**다(§15.3.1 "현행 프롬프트 = 중급"과 동형) — 난이도별로 달라지는 값을 시나리오 단위 표에 쓰면 좌표의 의미가 무너지고 커버리지 산출이 "어느 난이도의 커버리지인가"를 답할 수 없게 된다. **(d) 정합 실측**: `"절차·서류 정당화"`는 `resolveTacticCategory`에서 이미 `authority`로 정규화된다(T82가 규칙표 확장 시 확인) — `TACTIC_CATEGORIES` enum append 불요. | [0011](adr/0011-difficulty-lever-assembly-model.md) |
| 46 | 2026-07-27 | **(T92 architect 선행 판단) "무조건 요구" 가드는 스키마 선언 필드로 올리지 않는다 — 선언은 모델이 실제로 읽는 `weakenedTactics` 문구 안에 두고, 그 존재·모순을 기계로 고정한다(3겹).** ① **`ScenarioPromptDoc` 필드 추가 기각**(T91 reviewer 제안). ② **채택 3겹**: **(A)** 지명된 무조건 요구 문구에 **무조건성 명시구**(`있든 없든`/`부인해도`/`무엇을 부인하든`) 존재 검사 · **(B)** 지명 문구가 **전제 어휘**(보유·관계·행위·자격 계열 고정 목록)를 포함하면 같은 문구 안에 (A)의 명시구가 **반드시 함께** 있어야 하는 역검사 · **(C)** 신규 `DENIAL_PIVOT_BY_SCENARIO` 14행 + 키 `deepEqual` + `startsWith` 존재 검사(T91 표와 같은 파일·같은 형태). 전부 **신규 메커니즘 0건** — T91/T95가 개별 시나리오에 이미 걸어 둔 단언의 전수 승격이다. ③ **사전 정의값 확정**: "참가자가 부인 가능한 전제"를 **P1 보유·P2 관계·P3 행위·P4 자격 / N1 사기범 주장 사건·N2 참가자 자신·N3 사기범이 지금 만드는 절차·N4 미특정 주장**의 8종으로 고정하고, 판정 질문을 하나로 못 박는다 — *"참가자가 자기 지식만으로 즉시 '아니다'라고 확정할 수 있는가."* 지명 자격·부인 대응 요건도 if/then 표로 고정(§18.3 표 2·3). ④ **스키마·시딩·클라 계약 무변경** — Database.md·API.md·`firestore.rules`·ADR 전부 무변경. ⑤ **`axes.ts` 무변경** — 부인 대응은 새 축 값이 아니라 같은 E 요구에 도달하는 경로이며, **G60 정정과 섞지 않는다**. ⑥ **`TACTIC_CATEGORIES` 값 추가 0건**(필요 시 `CATEGORY_RULES` 정규식 행만 확장 — §15.10.5 선례). | AC-005·AC-068 재사용 + T91 실측 결함. **(a) 왜 소재지 이동이 답이 아닌가**: reviewer가 지적한 것은 *"주장을 어디에 적는가"* 가 아니라 *"주장이 참인지 기계가 모른다"* 이다. §15.10.4가 축 표에 대해 이미 같은 문장을 자인했다(*"값을 **가졌다**는 사실만 증명하고 **맞다**는 사실은 증명하지 못한다"*) — 필드로 옮겨도 **검증력이 0만큼 는다**. **(b) 왜 하필 `weakenedTactics` 문구인가(실측)**: 모델에게 가는 문자열은 `personaPrompt`+`weakenedTactics`+대화방식+가드레일뿐이다(`promptAssembly.ts:266-286`). 필드는 조립에 넣지 않으면 **런타임 행동이 한 글자도 안 바뀌는 죽은 값**이고, 넣으면 같은 주장이 두 문자열로 갈라져 드리프트 원천이 되며 **AC-065/AC-075 조립 순서 불변식의 재검증 대상**이 된다. **(c) 왜 Firestore 계약 증분이 비싼가**: `ScenarioPromptDoc`은 `seed.ts:38`이 `scenarioPrompts/{id}`에 `batch.set`하는 문서 계약이라 필드 추가 = Database.md 갱신 + **재시딩 운영 단계 신설** + 시딩 전까지 코드↔Firestore 불일치. §15.10.2 실측 #4가 축 태깅에 대해 **같은 이유로** 기각한 것과 동형이다. 옵셔널로 두면 "무조건 요구 없음"과 "미기재"가 구분 불가해져 §14.8.1·§14.9.1·§15.10.1이 세 번 기각한 **부재-오버로드**가 된다. **(d) (B)가 이론이 아니라는 실측**: T91 이전 `loanScam`의 요구 문구(`loanScam.prompt.ts:35` — `'기존 대출 얼마를 …'`, 명시구 없음)와 **현행 `institutional-impersonation`의 지명 요구**(`institutionalImpersonation.prompt.ts:37` — `'잔액 얼마 있으신지 …'`)가 **둘 다 실패한다** — 전자는 사용자가 실제로 겪은 교착을 사전에 잡았을 검사이고, 후자는 T92가 고쳐야 할 대상이다. **(e) 감수하는 한계(숨기지 않는다)**: 세 겹 전부 표기 검사라 **시나리오 수준 전제**(자녀 존재·지인 관계)는 잡지 못한다 — `kidnappingThreat.prompt.ts:41`의 요구 문구에 "자녀"는 한 글자도 없다. 의미 판정을 CI에 넣으려면 LLM 게이트가 필요한데 **비결정적 게이트는 회귀 가드로 쓸 수 없고**, 판정 대상이 14개 고정 상수라 사람 검토 비용이 애초에 작다. 흔들렸던 것은 비용이 아니라 **기준**이었고 그것은 §18.3 표가 제거한다. | — (ADR 신설 없음 — 스키마·구조 결정이 아니라 **기존 계약을 건드리지 않기로 한 판정**이며 [0004](adr/0004-llm-injection-defense-and-pii-masking.md)·[0010](adr/0010-scenario-tactic-axis-tagging.md)의 어느 결정도 뒤집지 않는다. DECISIONS #19/#30/#31/#37/#42의 "증분·비충돌에는 중복 ADR을 만들지 않는다" 원칙 계승) |
| 47 | 2026-07-27 | **(OQ-U26 (a) 판정) 모의 화면 카탈로그의 안전 게이트를 "시나리오 채널·소속"이 아니라 "항목이 선언한 진입 표면 + kind"에 건다 — 게이트 삭제 0건, 신규 게이트 2건 선행.** ① **판정**: G53·G55-a/b·`channel==="messenger"` 네 단언의 **원문 사유는 전부 `kind==="app-install"`(또는 `extractLinkMarker` 렌더 경로)를 가리키며 `credential-form` 통화 경로 항목에 적용되지 않는다** — 코드 근거 3중: `turnInstruction` 주입은 `hasAppInstallMockScreen()` 뒤에만 있고(`roleplay/index.ts:213` → `mockScreens.ts:117-124`), 전제인 `consentedAt`은 `kind!=="app-install"`이면 콜러블이 거부하며(`mockScreens/index.ts:74-76`), 경합 규칙은 이미 코드다(`verifyIntercept/fallbackTurn.ts:52-65`). ② **⚠️ ux-design 실측 정정 — 깨지는 것은 테스트 2건이 아니라 3개 테스트 4개 단언이다**: G53(3종이 `LINK_LABELS`에 없다 — `linkMarker.ts:21-25`)과 G55의 `VERIFY_INTERCEPT` 절반(3종이 `verifyIntercept.ts:146-148`에도 있다)을 UX가 놓쳤다. ③ **수단 = `MockScreenItem.entrySurface: "messenger-link"｜"in-call-sms"` 필수 필드 1개** — 조건이 선언 필드 하나에 모여 게이트마다 갈리지 않고, 새 항목이 선언을 빠뜨리면 타입이 깨진다. ④ **순서 강제(G75)**: ① `entrySurface` + 신규 게이트 **G-A**(`in-call-sms` ⇒ `kind!=="app-install"`)·**G-B**(`IN_CALL_SMS.fakeLandingId` ↔ `MOCK_SCREENS` 양방향 참조 정합)·**G-C**(kind↔필드)·**G-D**(안전 고지 문자열 카탈로그 부재)·**G-E**(역검증) → ② 기존 4개 단언 정밀화(같은 커밋) → ③ 콘텐츠 항목 추가. ⑤ **콘텐츠 소유 위치 = `MockScreenItem` 필드 증분**(`headline`·`bodyLines` 기존 + `issuerLabel`·`fields?`·`submitLabel?`·`successHeadline?` 신규, `consentLabel` 필수→옵셔널) — 신규 카탈로그 파일 0건. 렌더 문자열은 `MessengerFakeLanding.tsx` 모듈 상수이고 기존 드리프트 테스트가 묶는다. ⑥ **통화 경로 랜딩의 정합 구멍(M5)을 함께 닫는다** — 지금 `inCallSms.ts`의 `fakeLandingId` 3종은 G53도 `findMockScreenItem` 재검증도 **애초에 타지 않는다**. | **(a) 왜 별도 카탈로그 파일을 기각했나(결정적 근거)**: `mockSurface` 프로파일만 `realInstitutionName`을 포함하고(`harmlessnessPatterns.ts:290-292`,`:310-318`) 그 스캔은 `collectAllSurfaces()`가 **하드코딩으로 순회하는 카탈로그**에만 걸린다(`harmlessnessGate.test.ts:126-174`) — 새 파일은 순회에 자동으로 안 들어간다. 클라 스캔은 기관명을 **의도적으로 제외**한다(`harmlessnessScreens.test.ts:80-82`: *"사칭 대상 라벨은 서버 카탈로그에서 내려온다"*). ⇒ 콘텐츠를 새 파일이나 클라에만 두면 **국세청·관세청이 어디에서도 안 걸린다** — D-58이 최대 위험으로 지목한 바로 그 지점(UX.md:1088-1089). `MockScreenItem` 필드면 등록만으로 자동 스캔. **(b) 왜 그냥 삭제가 아닌가**: M1(턴 지시 경합)·M2(app-install이 통화 경로로 열림, §15.9.1 R6)는 여전히 막아야 한다 — 사유가 사라진 게 아니라 **대상이 좁을 뿐**이다. **(c) 왜 `LINK_LABELS`에 3종을 넣지 않나(G77)**: 그 맵은 *"프롬프트가 `[[LINK:]]`로 내보내는 id"* 의 정본인데 3종은 어떤 프롬프트도 안 내보낸다 — `subsidy-apply`(UX.md:1091)와 같은 죽은 라벨이 3개 늘고 G53의 의미가 조용히 바뀐다. **(d) kind별 옵셔널 필드는 새 관례가 아니다**: `InCallSmsItem.otpCode`/`linkDisplayText`와 동형(`inCallSms.ts:30-36`)이며 kind가 유일한 판별자라는 §14.9.1 원칙은 G-C가 단언으로 지킨다. **(e) 감수하는 한계**: `credential-form` 랜딩은 여전히 승격되지 않아 참가자가 상황에 맞는 폼에 정보를 다 입력해도 `deceivedMoments`에 안 남는다 — 승격에는 AC 근거가 필요해 **OQ-A19(planner)** 로 남긴다. | [0012](adr/0012-mock-screen-gate-keying-by-entry-surface.md) |
| 48 | 2026-07-27 | **(OQ-U26 (b) · OQ-U27 판정) 통화 경로 랜딩 배선과 통화 필 자막은 둘 다 표현 계층 증분으로 끝낸다 — 서버 스키마 1필드, 신규 콜러블·컬렉션·세션 필드 0건.** ① **(b) `InCallSmsDoc.landingKind?` 추가**, `buildInCallSmsDoc`이 `scenarioId`를 받아 `resolveMockScreenKind`로 채운다(kind의 진실 원천은 서버 — §15.9.1 R3 무변경). **생략 규칙은 `extractLinkMarker`(`linkMarker.ts:58`)와 글자 그대로 동일** — 기본값이면 키를 안 만든다 ⇒ 오늘 쓰이는 문서는 **바이트 동일**. 클라는 이미 갖고 있는 `fakeLandingId`를 `landingId` prop으로 내려보낸다. ② **신규 구속 규칙 R7/R8/R9**: 콘텐츠 조회는 **kind 분기 안쪽에서만**(미스 → 같은 kind의 기본 문구, `app-install` 방향 폴백 금지 R5 무변경) / `landingId`는 **정확 일치 조회 키로만**(정규식·`startsWith`·부분 문자열 분해 금지 — AC-024 계승) / 분기는 계속 `MessengerFakeLanding.tsx` 한 파일 안(R1) ⇒ `CLIENT_SCREEN_INVENTORY` 무변경. ③ **⛔ 통화 경로에서 `recordMockScreenEvent`를 호출하지 않는다(G81)** — 호출하면 통화 세션에 `mockScreens` 문서가 처음 생겨 §15.9.5 e-2 앵커 해결·`mockScreenTimeline`·`stages` 파생이 돌기 시작한다 = R6이 피하려던 합성 타임스탬프 얽힘(G15/G21). 기록은 기존 `recordInCallSmsEvent("link_tapped")` 하나로 충분하다. ④ **(OQ-U27) 자막 소스는 `messages`가 아니라 이미 흐르고 있는 `onTranscriptTurn` 콜백이다** — 페이지 지역 state 1개 + prop 1개, `pillCaption = liveScammerCaption ?? latestScammerLine`. ⑤ **(b) 자동청취 게이팅 무변경**(UX 권고 P-20 (4) 유지와 일치 — 코드가 이미 그렇게 돼 있다: `play/page.tsx:551`·`:865`·`:877`). | **(a) ⚠️ OQ-U27의 전제 정정(실측)**: `latestScammerLine`(`play/page.tsx:820-821`)은 `messages` 구독에서 오는데, **실시간(Gemini Live) 경로에서는 통화 중 `messages`가 갱신되지 않는다** — 전사는 `transcriptRef`에 쌓였다가 **종료 직전 1회** 제출된다(`:175-192`, `:252` 주석). 그대로 쓰면 **주 경로에서 자막이 영영 안 뜬다** — 청각 접근성 회귀를 막으려던 요건이 주 경로에서만 조용히 실패한다. 반면 턴 단위 자막은 이미 클라에 도착해 있다(`GeminiVoiceSession.tsx:211-218`,`:231-235`) — 신규 데이터 경로가 필요 없다. **(b) AC-059 안전 증명(리렌더가 세션을 안 끊는 코드 근거)**: Live 세션 effect의 의존성 배열이 **`}, []);`**(`GeminiVoiceSession.tsx:537`)이고 핸들러는 `handlersRef`로 읽힌다(`:128-162`) ⇒ 부모 리렌더로 재실행·언마운트가 **불가능**하다. 오버레이는 형제 노드 조건부 렌더 그대로(`play/page.tsx:1401-1415`). **(c) 마스킹 판정(정직 고지)**: `liveScammerCaption`은 마스킹 전 원문이지만 **방금 스피커로 재생된 사기범 오디오의 전사**라 참가자가 이미 들은 것 외에 새 노출 표면을 만들지 않는다. ⛔ **사용자 턴을 필에 그리는 순간 이 논거가 무너진다**(참가자가 말한 계좌·생년월일이 마스킹 없이 화면에 남는다) — `role==="scammer"` 조건은 접근성 취향이 아니라 **안전 조건**이다(**G79**). **(d) 청크 단위 갱신 금지(G78)**: 전사는 조각으로 스트리밍되고 `turnComplete`에서 flush된다 — 조각마다 `setState`하면 통화당 수백 번 리렌더가 되어 OQ-U27 (c)가 우려한 프레임 부담이 실제로 발생한다. **(e) 왜 생략 규칙을 통일했나**: 같은 개념(`landingKind` 부재 = `credential-form`)에 생략 규칙이 두 벌이면 그 자체가 드리프트다. G-A(`in-call-sms` ⇒ `kind!=="app-install"`)가 있어 생략된 키가 `app-install`을 뜻할 수 없다. | — (ADR 신설 없음 — [0007](adr/0007-in-call-sms-delivery-model.md)의 하위호환 옵셔널 증분이고 §15.9.1 R1~R6·§15.1.1 오버레이 계층 불변식을 **하나도 뒤집지 않는다**. DECISIONS #19/#30/#31/#37/#42 원칙 계승) |
| 49 | 2026-07-27 | **(T104 reviewer Major 1 판정) G-B의 ③커밋 배치를 사후 추인하고, G75의 "같은 커밋" 하드 게이트를 §19.8.1 배치 판정표로 정밀화한다 — 게이트 완화 0건.** ① **추인**: T104 구현자가 G-A·G-C·G-D·G-E·정밀화 4건을 ①(`53240da`)에 넣고 **G-B만 ③(`6dc9884`, 콘텐츠와 같은 커밋)** 에 둔 것을 추인한다. 사유는 재량 인정이 아니라 **G75/#47 ④ 문면 자체의 결함**이다 — G-B 방향 ①(*"모든 `IN_CALL_SMS.fakeLandingId`가 같은 scenarioId의 `MOCK_SCREENS`에 존재"*)이 참조하는 데이터가 ③에서 들어오므로 ①에 두면 **중간 커밋이 반드시 실패**한다. ② **신규 배치 판정표(§19.8.1, 3행)**: **배치-1**(이번 변경이 새로 여는 위험 M1·M2를 닫는 게이트) = **①커밋, 예외 없음** / **배치-2**(base에 이미 열려 있던 위험 M5를 닫고 참조 데이터가 나중 커밋에 오는 게이트) = **참조 데이터와 같은 커밋, 그보다 뒤로는 금지** / **배치-3**(미해당) = **임의 판단 금지, architect에게 행 추가를 먼저 물을 것**. ③ **추인 범위는 G-B 1건뿐** — 배치-1 게이트의 ① 이탈은 허용하지 않는다. ④ **(reviewer Minor 2) G80의 입력 어포던스 소스 스캔 단언은 T103이 소유한다 — T104 후속 요구 0건, 신규 태스크 0건.** ⑤ **문서 갱신만**: `Architecture.md` §19.8 G75 행·G80 행 + 신설 §19.8.1, `adr/0012` 사후 노트. **AC·Tasks.md·소스 무변경.** | **(a) 왜 M2가 열리지 않았나(독립 검증)**: G75 행이 밝힌 위험은 *"`app-install`이 통화 경로로 들어와도 아무도 못 잡는다"* = **M2**인데, §19.2 (2) 판정표는 **G-B의 방어 대상을 M5로만** 적는다 — M2를 닫는 것은 ①에 있는 **G-A · 무변경 R6 · 정밀화된 channel 단언** 3건이다. ⇒ G-B 지연은 M2 방어에 결손이 없다. **(b) 왜 M5는 노출 창이 0인가(워크트리 실측)**: `inCallSms.ts:59,108,122`의 `fakeLandingId` 3종이 `LINK_LABELS`(`linkMarker.ts:21-25` — 3개뿐)에도 `MOCK_SCREENS`(`mockScreens.ts:92-94` — 1개뿐)에도 없어 **base에서 이미 어떤 정합 게이트도 안 타고 있었다** — 미루기 전후의 위험 집합이 같다. **(c) 왜 ①에 억지로 넣는 것이 더 나쁜가**: ① 시점에 `entrySurface==="in-call-sms"` 항목이 **0건**이라 G-B 방향 ②가 **공허하게 참**이 된다 — **G-E**(*"좁힌 조건이 실제로는 아무것도 안 잡는 상태"*)가 막으려던 것 그 자체다. **(d) 왜 G80이 T103인가**: `Tasks.md:212` T103 **완료 판정 필수 증거 ②** 가 이미 같은 것을 글자 그대로 요구하고(입력 어포던스 0건 + 역방향 확인), G80의 발동 조건인 D-56/D-57 구현은 §19.9 마지막 행이 **§19 범위 밖**으로 이미 판정했으며, reviewer 실측상 `InCallSmsOverlay.tsx`의 현행 위반은 **0건**이다 — T104 후속으로 또 요구하면 같은 게이트가 두 태스크로 갈라져 소유자가 사라진다. **(e) 선례**: T83 **G58**(§16.6 — 구현자 판정 → reviewer 타당성 확인 → architect 사후 추인)과 같은 처리이며, 이 프로젝트에서 두 번째다. ⇒ 반복되는 사안이므로 **일회성 추인이 아니라 판정표로 승격**했다(CLAUDE.md 1항). | [0012](adr/0012-mock-screen-gate-keying-by-entry-surface.md)(사후 노트 — 결정 무변경) |
| 50 | 2026-07-27 | **(T101 방식 선정) `functions/lib` 스테일 산출물은 후보 ① 단독으로 막는다 — 매 `test`·`build` 실행 앞에 `lib`을 통째로 삭제하고 `tsc`가 현재 소스에서만 재생성하게 한다. 삭제 수단은 Node 코어 `fs.rmSync`(신규 의존성 0), `rimraf`는 기각.** ① **채택**: `functions/scripts/clean-lib.mjs` 신설 + `functions/package.json`의 `test`·`build` **본문 안에 인라인**(`pretest` 훅 아님 — `--ignore-scripts`로 건너뛸 수 있는 자리에 두지 않는다) + `clean` 키 신설. **테스트 글롭·러너·`tsc` 호출은 한 글자도 바꾸지 않는다.** ② **기각 ②**(`tsc --build --clean`): ①의 열등 변종 — 무엇을 지울지를 buildinfo 상태에 의존하고, **이미 삭제된 소스의 출력은 열거 대상에 없다**(추정, 확인 방법은 §20.2). ③ **기각 ③**(`incremental`): 산출물 정리 동작이 아예 없고, buildinfo를 `outDir` 밖에 두면 clean 후 emit을 건너뛰어 **테스트 0건이 초록불**이 될 위험까지 있다. ④ **기각 ④**(소스 기준 실행): 기존 테스트가 `lib` 실행을 전제로 경로를 계산하고(`harmlessnessGate.test.ts:437`, `axisCoverage.test.ts:366`) `__dirname`을 쓰며 import가 확장자 없는 CJS 관례다(`axisCoverage.test.ts:19`) — 142개 소스 재작성 + **배포(CommonJS)와 다른 모듈 의미론에서 테스트**하게 된다. ⑤ **완료 판정은 §20.6의 A/B probe 절차 출력으로만** 한다(개수 일치만으로는 판정하지 않고 `[T101-probe]` 문자열 부재를 함께 본다). ⑥ **`build`까지 clean을 붙인 것은 T101 최소 범위를 넘는 architect 추가분**이며 사용자가 최소 범위를 원하면 뺄 수 있다(§20.3 고지). | **(a) 왜 ①이 E항까지 막는가(T101 행의 분기 가정을 뒤집는 근거)**: T101 행은 *"삭제 모듈 참조가 통과한다면 ②④가 필요해진다"* 고 적었고 실측상 통과가 확인됐다(§20.1, 오케스트레이터 실행 — probe 소스 삭제 후에도 `lib`의 산출물이 남아 테스트가 exit 0으로 통과). 그러나 ①은 **`lib` 전체를 매 실행 재생성**하므로 고아 산출물의 존재 창이 **0**이 되어 E를 ②④와 동등하게 막는다 — 오히려 ②는 못 막는다. 후보의 우열은 취향이 아니라 **메커니즘**에서 갈렸다. **(b) 왜 `rimraf`가 아닌가**: Node 코어가 하는 일에 배포 대상 패키지(`functions/`)의 의존성을 늘리는 판단이고, *"환경에 따라 결과가 달라진다"* 를 고치는 태스크가 **설치 성공 의존성**을 새로 들이는 것은 자기모순이다. 인라인 `node -e`도 기각 — Windows에서 npm 스크립트는 `cmd.exe`를 경유해 중첩 인용이 깨지기 쉽고, 무엇보다 T101이 요구하는 증거 ⓓ(삭제가 실제로 동작한 출력)와 경로 가드를 스크립트 파일만이 낼 수 있다. **(c) 감수하는 한계(정직 고지)**: `npm`을 거치지 않고 `npx tsc && node --test lib/...`를 손으로 치면 **여전히 오염된다** — 커밋 훅(T100)은 테스트 실행을 가로챌 수 없어 기계적 강제가 불가능하다. 완화는 CLAUDE.md "Verified Commands" 등재 권고뿐이며 그 표의 소유자는 User다. 또한 이미 문서에 기록된 오염 수치의 **소급 정정은 이 결정의 범위 밖**이다. | — (ADR 신설 없음 — 제품 구조·데이터 계약·안전 게이트를 하나도 바꾸지 않는 **빌드 툴체인** 결정이고, 되돌리는 비용이 `package.json` 3줄이다. DECISIONS #48의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 51 | 2026-07-27 | **(T106 방식 선정) ElevenLabs 에이전트 프롬프트 드리프트는 후보 ②+③+④ 조합으로 검출한다 — 검사를 "키 없는 절반"과 "키 있는 절반"으로 쪼갠다. 후보 ①(파이프라인 자동 재동기화)은 이번 범위에서 기각.** ① **(A) 키 없는 게이트**: 12종의 **조립 프롬프트 다이제스트를 저장소에 커밋**하고(`functions/src/realtime/agentPromptDigest.ts` 계열, `Record<scenarioId,{digest,length,syncedAt}>`), `npm test` 안에서 `digestOf(buildSystemPrompt(SCENARIO_PROMPTS[id])) === 표의 digest`를 항상 검사한다 — **키·네트워크 0이라 skip될 자리가 없다.** 키 게이트는 `PUBLIC_SCENARIOS` 유도값과 `deepEqual`, 죽은 게이트 방지 역검증 1건 필수. ② **(B) 키 있는 감사**: 별도 npm 키(`audit:agent-prompts`)로 `GET /v1/convai/agents/{agentId}`의 `conversation_config.agent.prompt.prompt`를 **읽기 전용** 조회해 (A)의 표와 대조한다. **PATCH는 하지 않는다.** ③ **종료코드 계약**: `0`=12/12 일치 · `1`=드리프트/조회실패/매핑충돌 · **`2`=미실행(키 없음·매핑 0건·부분 커버리지)**. **키가 없으면 exit 0을 내지 않고, 대상 0건을 "전부 일치"로 출력하지 않는다.** ④ **판정 단위**: **정규화한 전문의 동등성**이 판정이고 SHA-256 hex가 표기 수단. 정규화는 **개행 통일·NFC·양끝 trim·줄끝 공백 제거 4종만** 적용하고 **빈 줄 축약·줄 안 공백 축약은 금지**(빈 줄이 조립 블록 경계다 — `promptAssembly.ts:270,273,277,281-283`). ⑤ **조립 변형은 1종** — `buildSystemPrompt(prompt)`를 **옵션 없이** 호출한 기준선 문자열만 대상(ElevenLabs 경로엔 난이도 주입 지점이 없다 — `elevenLabsProvider.ts:56-61` `difficultyApplied:false`). ⑥ **T99 하드 게이트 비파괴**: 감사 전용 변수 **`ELEVENLABS_AUDIT_AGENT_IDS`를 신설**하고 런타임은 기존 `ELEVENLABS_AGENT_IDS`만 계속 읽는다 — 분리 성립을 **행동 테스트**(감사 변수를 채워도 `getRealtimeProvider`가 Mock 반환)로 고정한다. ⑦ **완료 판정은 §21.7의 실행 출력표로만** 한다("스크립트를 만들었다"는 완료가 아니다). | **(a) 왜 ②가 핵심인가(메커니즘)**: 긴장의 두 축(*"테스트로 만들면 키 없을 때 조용히 skip"* / *"운영 스크립트로 빼면 아무도 안 돌린다"*)은 **"이 검사는 키가 필요하다"** 는 하나의 전제에서 나온다. 다이제스트를 커밋하면 검사가 **Q1(저장소 조립 == 기록)** 과 **Q2(기록 == 라이브)** 로 쪼개지고 **Q1은 키가 필요 없어진다** — 실제 발생 경로(*"`.prompt.ts`를 고치고 재동기화를 잊는다"*, T106 A항)를 **고치는 그 커밋에서** 빨간불로 잡는다. **(b) 왜 ①을 기각하는가**: 이 저장소에는 **자동 실행되는 파이프라인이 없다** — `seed:scenarios`(`package.json:15`)도 사람이 치는 스크립트이고 CI 도입은 T106 범위 밖(T100 E항)이라, ①은 (나)를 해소하지 못한 채 **라이브 외부 시스템에 자동 쓰기**(대시보드 수정분을 조용히 덮어씀)라는 새 폭발 반경만 들여온다. T87 라이브 검증 전에 쓰기를 자동화하지 않는다. **(c) T106 행의 추정 1건을 뒤집는다**: *"매핑을 켜지 않고 **에이전트 id 목록만으로** 드리프트 검출이 가능하다"* 는 **성립하지 않는다** — 판정은 `(scenarioId, agentId)` **대응**을 요구하고 id 목록만으로는 어느 조립 결과와 비교할지 정할 수 없다. 에이전트 `name`으로 추측하는 것은 저장소가 강제하지 않는 명명 규약에 판정을 거는 것이라 기각했다. 그래서 ⑥의 **감사 전용 변수**가 대안이다. **(d) 대상 12종은 임의값이 아니다**(architect 워크트리 실측 2026-07-27): `voiceMode` 보유 10종(`publicMeta.ts:68,82,97,111,125,140,154,168,182,256`) ∪ `escalation.toChannel==="voice"` 2종(`:199`,`:239`) = **12**, 정본 14종 중 `messenger-friend-loan-kakao`·`messenger-parcel-smishing-sms` 2종은 실시간 경로에 도달하지 않는다 — 오케스트레이터 실측의 "12종"과 **독립 유도로 일치**한다. 이 유도가 있어야 "미매핑"과 "0건"을 판정할 수 있다. **(e) 감수하는 한계(정직 고지)**: (A)는 *"재동기화했다"* 를 증명하지 않는다 — 다이제스트만 갱신하고 PATCH를 안 하면 초록불이 되고, 그 거짓말을 잡는 것은 **(B)뿐**이다. 그리고 (B)는 **사람이 칠 때만 돈다**(CI 범위 밖). ⇒ **(A) 단독으로 T106을 완료 보고하지 않는다.** | — (ADR 신설 없음 — 제품 구조·데이터 계약·안전 게이트를 하나도 바꾸지 않는 **운영 검출 수단** 결정이고, **ADR-0004는 재해석하지 않았다**: §21은 ADR-0004가 만든 이중화를 전제로 받아 감시할 뿐이다. DECISIONS #48·#50의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 52 | 2026-07-27 | **(T110 전환 모델 재설계) 통화→통화는 "새로 걸기"가 아니라 호 전환(넘겨주기)으로 재현한다. 원 화자 퇴장을 강제하는 지점은 "모델에 도달하는 문자열 집합" 하나뿐이며, 그것은 서버 소유 3종(상시 블록 `VERIFY_INTERCEPT_RULE` + `announceInstruction` + `reconnectInstruction`)이다.** ① 잔류 요구(*"저는 끊지 않고 기다리겠습니다"*)를 **삭제로 끝내지 않고 퇴장 규칙으로 대체**하고, 그 규칙을 **시스템 프롬프트 상시 블록**에 둔다(1회성 턴 주입보다 지속성이 높은 유일한 자리). ② `reconnectInstruction`의 신규 발신 전제를 **전환 전제 + 복귀 금지**로 바꾼다. ③ **`displayNumber`를 카탈로그에서 제거**하고 문서·리포트 스냅샷에서는 **옵셔널로 낮춰 과거 리포트를 무백필로 살린다**. 그 필드가 지키던 안전 검사 3종은 **카탈로그 전 필드 순회 게이트로 승격**해 대체한다(현행은 3필드 하드코딩이라 신규 필드가 검사를 빠져나갔다 — **커버리지가 오히려 넓어진다**). `reconnectedCallerLabel`은 **유지**. ④ **스키마·콜러블·컬렉션 증설 0건, 필드 개명 0건**(`placedAt`은 판정 앵커·주석 경계·멱등 규칙·리포트 `outcome`이 매달린 이름이라 뜻만 넓게 읽는다). ⑤ **T103 병합을 선행으로 삼지 않는다** — T110은 P0이고 T103은 코드 0줄이며, 유일한 의미 충돌(전환 후 통화 필의 발신자 라벨)은 **계승 계약 G87**로 닫는다. ⑥ 완료 판정은 **6종 전수 게이트 + 역검증 + 전사 텍스트 기반 라이브 절차**(§22.6)로만 한다. | **(a) 원인이 모델 드리프트가 아니라 설계 요구였다**(실측): `verifyIntercept.ts:60`이 원 화자 잔류를, `:61`이 참가자 신규 발신을 **같은 세션에 요구**했고 같은 패턴이 **6종 전부**(`:54-120`)에 복제돼 있다 — 모델은 시킨 대로 했다. **(b) 서버 강제 후보를 전수 실측한 결과 강제 지점이 없다**: 세션 중 시스템 프롬프트 교체 불가(`geminiProvider.ts:77-99` — 토큰 발급 시 고정, 바꾸면 ADR-0004 잠금을 자해), 소켓 재연결은 컨텍스트 소실 + **미마스킹 전사를 통화 중 서버로 올려야 해 G79 안전 논거가 무너진다**(§16.5 기각 근거 강화), 서버측 출력 필터는 응답 모달리티 오디오 고정으로 구조적 불가(`geminiProvider.ts:86`), 클라 화자 음소거는 단일 스트림·화자 식별자 부재로 불가 ⇒ **남는 것은 문자열 집합뿐이고, 이번 결함의 직접 원인이 정확히 그 문자열이라 이 조치는 완화가 아니라 원인 제거다.** **(c) 그럼에도 보증은 없다(자기 고지)**: 모델이 자발적으로 앞 페르소나로 되돌아가는 것은 못 막는다 — **⛔ *"이제 겹치지 않는다"* 는 보고 금지**, 측정은 **OQ-A10**으로 등재. 폴백은 매 턴 프롬프트를 재조립해 규칙이 재삽입되지만 실시간은 1회 고정이라 **주 경로가 구조적으로 더 약하다**(경로 비대칭 명시). **(d) 훈련 목표가 이동한다**: *"직접 건 확인 전화도 가로채진다"* → *"넘겨준 통화는 독립 확인이 아니다"*. 둘 다 실제 수법이나 같은 교훈이 아니며, 6종 전부가 전환 모델이 되므로 **전자의 재현은 라이브러리에서 0건이 된다.** AC-071 문면은 **사용자 확정 사항**이고 architect 의견은 **(나) 신설 권고 · (다) 무변경 차선 · (가) 제자리 수정 비권고**(T83·T95의 완료 증거가 무효가 된다). **실측 정정 1건**: **MVP #29 문면(`PRD.md:431`)에는 "대표번호"·번호 안내가 없어 어긋남이 0건**이고, `VERIFY_INTERCEPT_CORRECT_ACTION`(`verifyTimeline.ts:51`)도 **무변경으로 유효**하다 — 재저작이 필요한 것은 `what` 문구 **3종뿐**이다. **(e) T103 diff는 존재하지 않는다**(상태 todo, 커밋 0건) — 선후 판정은 **§19.6의 architect OQ-U27 확정 + T103 행 범위**라는 문서 근거로 했고, 코드 diff를 보지 않았음을 §22.4에 명시했다. | **ADR-0013** |
| 53 | 2026-07-27 | **(T103 착수 게이트) OQ-U27 (a)(b)를 확정한다 — (a) 통화 필 자막 = 최신 1~2줄 + `aria-live="polite"` + 2줄 클램프 + 턴 단위 갱신 + 사기범 턴만(§19.6 (2) 재확인, 뒤집지 않음), (b) 자동청취 게이팅 = 유지(무변경 — 코드가 이미 그렇게 돼 있다).** 부수 확정 6건: ① **주/부 반전은 D-35를 깨지 않는다**(형제 노드 구조 유지한 채 className·JSX 편집 + prop 1개로 가능 — 실측). ② **읽기 전용 경계(AC-060)는 기존 G80 게이트가 계속 지키며, T103은 스캔 대상 파일을 하나로 유지하고(신규 컴포넌트 파일 0건) 역검증 오염 샘플을 말풍선 시대 형태로 갱신한다**; **T108은 T103의 착수 게이트가 아니고 순서는 T103 → T108 유지**. ③ AC-006/022/059는 **기존 기계 게이트 + 렌더 구조**가 보장하며 T103은 3개 계약(트랩 안 유지·헤더 고정·종료 경로 즉시 언마운트)만 지키면 된다. ④ **D-56(자동 전환 부재)은 현행 코드가 이미 만족**하므로 **R-9 미확정을 착수 게이트로 삼지 않는다**. ⑤ 전환은 **CSS 키프레임 + `prefers-reduced-motion` 폴백**(기존 `globals.css` 선례 재사용), **퇴장 연출은 컴포넌트 내부 상태로만** 하고 호스트의 `{smsOverlayOpen && (` 조건부 렌더를 바꾸지 않는다. ⑥ **G87 계승 계약 = 필은 기존 `callerLabel` prop 하나만 읽는다**(새 발신자 소스 신설 금지). **신규 AC·서버·스키마·라우트·컴포넌트 파일 전부 0건.** | **(a) 게이트를 새로 판정할 여지가 실제로는 없었다** — OQ-U27 (a)(b)는 §19.6이 이미 코드 근거로 확정했고(실시간 경로에서 `messages`가 통화 중 갱신되지 않아 `latestScammerLine`만 쓰면 주 경로에서 자막이 영영 안 뜬다는 실측 포함), 이 패스가 한 일은 **착수 게이트로서의 확인**이다. 재판정하면 T110·T104가 이미 의존하는 판정이 흔들린다. **(b) 반전의 실행 가능성은 추정이 아니라 실측이다**: 오버레이는 이미 `fixed inset-0`(`InCallSmsOverlay.tsx:115`)에 모바일 전체화면(`:129`)이고 통화 축소 바(`:133-143`)가 필의 전신이라, 반전은 **`sm:` 접두 제거 + 축소 바 승격**으로 성립한다 — 라우트·언마운트를 건드릴 이유가 구조적으로 없다. **(c) 진짜 위험은 반전이 아니라 퇴장 연출이었다**: 언마운트를 지연시키면 `callContinuity.test.ts:37` 단언이 깨질 뿐 아니라 **한도 도달 시 종료 고지가 연출 시간만큼 가려진다**(AC-059) — 그래서 A4/A5를 구조 계약으로 못 박았다(G89·G91). **(d) 읽기 전용 게이트의 실효는 "파일 하나"라는 전제에 매달려 있다**: G80은 `InCallSmsOverlay.tsx` 한 파일만 보고(`:15`·`:23`), 화면 등록부(`harmlessnessScreens.test.ts:94-119`)는 **무해화 문구 스캔만** 강제해 공백을 못 메운다 ⇒ 파일 분할은 게이트를 조용히 무력화한다(G90). **(e) 자기 고지**: 이 게이트는 소스 텍스트 스캔이라 **고의 난독화에 완전하지 않다**(T108 등재 사안) — **⛔ *"이제 입력창이 들어올 수 없다"* 는 보고 금지.** 또 이 저장소엔 렌더러 테스트 러너가 없어 **마운트 유지는 자동 관측 불가**라 완료 증거 ①은 소스 게이트 + 런타임 관찰을 **둘 다** 요구한다. **(f) 실측 정정 1건(planner 인계)**: T103 행 완료 증거 ⑥의 기준선 `functions 442 / 루트 165`는 **낡았다**(현행 표기는 `CLAUDE.md`·T108 행 기준 **functions 462 / 루트 171**이며 이 패스에서 테스트를 실행하지 않아 architect가 직접 재측정하지도 않았다) — **착수 시 재측정**이 정답이고 `docs/Tasks.md`는 planner 소유라 고치지 않았다. | — (ADR 없음 — §19.6·§22.4의 하위 확인이자 표현 계층 한정이라 새 구조 결정이 아니다. DECISIONS #19/#30/#31 원칙 계승) |
| 54 | 2026-07-27 | **(T108 착수 게이트 — 방식 선정) 소스 텍스트 스캔 게이트 4곳(+경계 1곳)의 검출은 후보 ②(AST 파싱 + 좁은 상수 폴딩)를 축으로 채택하고, 현행 리터럴 검사와 **합집합**으로 건다. 후보 ③(허용목록 역전)은 기각, ①은 강등 경로, ④는 어느 쪽이든 동반 필수.** ① **채택 근거는 능력의 경계다** — QA 재현 우회 3종 중 **동적 태그 2종은 ②만이 막는다**(정규식은 `"inp"+"ut"`를 평가할 수 없다). ② **N1(⭐)**: 현행 `includes(token)` 단언을 **삭제하지 않는다** — 강화는 교체가 아니라 합집합이라 **누락은 구조적으로 불가능**하고 새로 생기는 것은 오탐뿐이며 오탐은 전수 확인이 잡는다. ③ **N2**: JSX 태그가 식별자인데 import도 파일 내 선언도 아니면 **값을 몰라도 실패**시킨다(폴딩 실패 = 조용한 통과를 막는다. 실측 오탐 0건). ④ **⚠️ ② 실행 가능성은 절반만 확인됐다** — 루트 러너는 `node --experimental-strip-types --test`(`package.json:11`), `typescript`는 **루트 devDependency 5.9.3**(실측)이라 **신규 의존성 0건**으로 닿지만, **strip-types 모드에서 CJS `typescript`의 default import가 실제로 되는지는 미확인**(선례 0건)이다 ⇒ **P-1 프로브 → 실패 시 ①+④ 강등**을 if/then 표로 고정했고, **원인 불분명이면 임의 판단 금지**. ⑤ **경계 사례(`harmlessnessScreens.test.ts:121-142`)는 포함** — 단 `foldedStringLiterals` 축 1개만(한계비용 ≈ 0, 금지 목록 무변경). ⑥ **스캔 대상 파일 집합의 일반화는 범위 밖(요구 층)**, 대신 **사각의 가시화**(오버레이의 로컬 import 집합 단언)만 포함. ⑦ **금지 토큰 diff 0줄**이 완료 조건(§24.10 ⑧) — 요구는 무변경, 검출 방식만 바뀐다. | AC-060·AC-045·AC-072·R8의 **소스 수준 증명**을 유지하기 위한 검출 수단 결정(제품 AC 신설 0건 — T106 선례). **(a) 왜 ③을 기각했나(정량)**: 대상 2파일 실측 결과 JSX 요소는 각각 **33건/10종 · 53건/13종**인데, 정규식 허용목록은 **제네릭 타입 인자 8건**(`useRef<HTMLDivElement>`·`Record<string, …>` 등 — `InCallSmsOverlay.tsx:48,49,50,91`·`MessengerFakeLanding.tsx:160,238,348`)을 즉시 오탐하고, 무엇보다 **4곳 중 3곳(AC-060·AC-045·R8)은 요소명 게이트가 아니라** 호출·멤버·import·URL 리터럴 게이트라 **대응 축이 없다** ⇒ 방식이 두 벌이 된다(§19.4 #3 위반). 게다가 허용목록은 **T103이 표시 태그를 늘릴 때마다 빨간불**이 되어 *"통과시키려면 목록을 늘려라"* 를 학습시킨다(현행 6토큰은 T103과 무관하다 — `callContinuity.test.ts:130-135`). **(b) 넓은 스캔이 정상 코드를 문 실례가 이 저장소에 있다**: `harmlessnessScreens.test.ts:80-82`가 `operationalPayload`(8자리+ 숫자)를 *"React 코드의 타임스탬프·해시에 우연히 걸린다"* 며 정본 패턴군에서 **의도적으로 제외**했다. **(c) 왜 파일 집합 일반화가 범위 밖인가**: 입력 어포던스 금지는 **파일마다 답이 다르다** — `MessengerFakeLanding.tsx`에는 `<form`·`<input`이 **정당하게 있다**(`:281`·`:288`, 안전 계약 근거 `callContinuity.test.ts:125-128`) ⇒ 집합을 넓히려면 *"어느 파일이 입력 없이 있어야 하는가"* 라는 **새 요구 표**가 필요하고 그것은 AC 층이다(T108 행 G항 정면 해당). **(d) 감수하는 한계(정직 고지 — ⛔ *"이제 우회 불가"* 보고 금지)**: 런타임 조립 문자열은 **정적으로 결정 불가**라 4번째 우회 형태를 만드는 비용은 여전히 낮다; 표현식이 낀 템플릿은 접히지 않는다; 폴딩은 **새 오탐 표면**을 만든다; 렌더 결과는 관측 불가(렌더러 러너 없음 — `callContinuity.test.ts:6-8`); **CI가 없어 사람이 `npm test`를 칠 때만 돈다**. **(e) 기준선은 인용값이다**: `functions 470 / 루트 171`은 **측정 커밋 `95cc684`**(`CLAUDE.md:74-75`)이며 **architect는 셸이 없어 이번 패스에서 실행하지 않았다** — base가 `2fb1e98`로 옮겼고 T110 병합으로 또 바뀌므로 **착수 시 재측정**이 완료 조건이다. | — (ADR 신설 없음 — 제품 구조·데이터 계약·안전 게이트의 **요구**를 하나도 바꾸지 않는 **검출 수단** 결정이고, 되돌리는 비용이 테스트 파일 3개 + 헬퍼 1개다. DECISIONS #48·#50·#51의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 55 | 2026-07-27 | **(T118 방향 판정) §22.9가 유보한 후보 (i)을 채택한다 — 단 운반·주기·내용을 바꿔서. 실시간 경로에 신설하는 것은 "퇴장 규칙의 재주입"이 아니라 층 A5 = 전환 **상태 단언** 1줄이며, 발동 조건은 "매 턴"이 아니라 "직전 주입 이후 사용자 발화 턴 ≥1"이다. 운반 수단은 P-1 프로브로 확정한다(α: `turnComplete:false` 컨텍스트 추가 / β: 기존 `instructionTurn` 큐 최하위 `persona`).** ① **§22.9 유보 사유 2건 판정**: *"측정 전에 복잡도를 늘리지 않는다"* 는 **해소**(라이브 실측 2건 — 인용값), *"턴 슬롯 경합을 G31/G58에 다시 얹어야 한다"* 는 **§25.4 표로 해소** — **G58은 폴백 전용이라 영향 0**(이 절은 `fallbackTurn.ts`·`roleplay/index.ts`·`promptAssembly.ts`를 **한 줄도 안 고친다**), **G31은 α면 무변경 / β면 rank 3행(`sms`0·`verify`1·`persona`2) + 코얼레싱 명시 예외**. ② **원인 확정을 둘로 쪼갠다**: ㉠ 최신성(폴백은 매 턴 프롬프트 선두 재조립, 실시간은 setup 1회) + **㉡ ⭐ 지시어의 참조 대상 부재**(상시 블록이 *"이 프롬프트에 함께 들어온 턴에만"*·*"전환 지시가 온 턴부터"* 라는 **턴 상대 지시어**를 쓰는데, 실시간에서는 시스템 프롬프트가 세션 내내 안 바뀌고 지시가 **별도 client-content 턴**으로 와서 그 조건이 **어느 턴에도 참이 되지 않는다**). **①(오퍼 재발화)의 지배적 원인은 ㉡**이다. ③ **A5 문자열은 카탈로그 필드**(`VerifyInterceptItem.transferStateLine`, 6종 전수) — 응답 전용 상수·클라 하드코딩 **금지**. ④ **(가)/(나) 판별은 방향 선택보다 선행하지 않는다** — (가) 경로는 판별 없이 **코드로 이미 확정**이라(`verifyIntercept/index.ts:141-147`이 `placedAt` 유무와 무관하게 `announceInstruction`을 무조건 반환 + `page.tsx:450-453` 롤백) **R-1·R-2로 봉쇄**하고, (나)는 **A5**로 다룬다. **둘은 독립이며 서로를 대체하지 않는다.** ⑤ **실시간에 `announcedAt`을 찍지 않는다** — 의미 오버로드이고 이번 증상(전환 **이후** 재발화)을 못 막는다. `placedAt`+R-1이 답이다. ⑥ **신규 AC 0건 · AC 번호 예약 0건 · Firestore 스키마 0건 · 신규 콜러블 0건**. `API.md`는 승인 후 **2행 델타**. ⑦ **순서: T118 → T113·T114.** | AC-071·AC-075 재사용(요구가 아니라 **주 경로 성립**의 문제 — T106·T108·T113·T117 선례). **(a) ⭐ 실측이 하나를 배제했다**: 퇴장 규칙 문면은 **실시간에도 이미 있다** — `promptAssembly.ts:126-130`이 `geminiProvider.ts:77-83` → `:99` `systemInstruction`으로 토큰에 고정된다. ⇒ 결함은 *"규칙이 없다"* 가 아니므로 **A5는 규칙을 다시 보내는 것이어서는 안 된다**(G100). 이것이 §22.9 문면(*"1줄 퇴장 지시 재주입"*)을 그대로 따르지 않은 이유다. **(b) ⭐ "매 턴 재주입"은 자기 구동 루프다**: 실시간 주입은 `sendClientContent({turnComplete:true})`(`GeminiVoiceSession.tsx:573`)라 **주입 자체가 모델 응답을 유발**한다 — 사기범 turnComplete마다 무조건 주입하면 주입→발화→turnComplete→주입의 루프가 되어 참가자가 끼어들 수 없다. 그래서 **사용자 발화 턴을 발동 조건에 넣었고**(관측 지점은 이미 있다 — `:214` `onTranscriptTurn("user",…)`), 그 결과 **회수 상한이 불필요**해졌다(세션 한도가 상한). **(c) 왜 카탈로그 필드인가(결정적)**: §22.3 G86-a/b/c가 이미 `Object.entries(item)` **전 필드 순회**로 확정돼 있어 카탈로그에 두면 번호 형태·`REAL_WORLD_FORBIDDEN`·url/tel 검사망에 **자동 편입**된다 — 응답 전용 상수는 그 순회 밖이다. **(d) 왜 `announcedAt`이 답이 아닌가**: 현행 뜻은 *"폴백 턴 조립기가 실었다"* 이고 폴백 큐 판정의 입력이다(`roleplay/index.ts:250-258`·`fallbackTurn.ts:12,43,62`). 실시간에서 같은 이름을 찍으면 *"클라가 주입을 **시도**했다"*(성공 보장 없음 — `GeminiVoiceSession.tsx:574` catch)가 되어 **한 필드에 두 뜻**이 생긴다(§14.9.1 계열 오염). **(e) 실행 가능성을 못 박지 않았다**: `turnComplete:false` 컨텍스트 추가가 응답을 유발하지 않는지는 **미확인**이라 P-1 프로브 → if/then 강등으로 고정했다(§24.3 P-1 선례, *"참조 대상이 나중에 오는 것을 앞에 못 박지 말 것"* §19.8.1 취지). **원인 불명이면 임의 판단 금지.** **(f) 감수하는 한계(⛔ *"이제 겹치지 않는다"* 보고 금지)**: §22.2 판정은 그대로 유효하고 A5가 바꾸는 것은 **상태 정보의 최신성** 하나뿐이다; ㉠·㉡의 분리 측정은 **하지 않았다**; **전환 *이전*** 의 중복 권유 창은 R-2로 좁히되 닫지 않았다; 모델에 도달하는 서버 소유 문자열이 **3종→4종**이 된다(§22.2 F행의 "정확히 3개"는 이후 4개로 읽어야 하며 **§22.2 표는 고치지 않았다**); **architect는 셸이 없어 테스트·라이브·git을 한 번도 실행하지 않았다**(508/204는 인용값, 착수 시 재측정). **(g) 기준선 불일치 고지**: 워크트리 HEAD `cd00c12`(지시된 base와 일치)인데 `refs/heads/main`은 **`7b549b0`** 이다 — 조상 관계 미확인. | — (ADR 신설 없음 — **ADR-0013(호 전환 모델)의 하위호환 증분**이고 제품 구조·데이터 계약을 하나도 바꾸지 않는다. **ADR-0004는 재해석하지 않았다** — 클라 오버라이드 방향은 채택하지 않았고 A5 문자열도 서버 소유다. DECISIONS #48·#50·#51·#54의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 56 | 2026-07-28 | **(T119 게이트 값 재판정) `availableAfterScammerTurns`의 권고 범위 2~3을 폐기하지 않고 **계열 2행으로 쪼갠다** — 계열 A(확인 우회가 본론인 전용 저작 시나리오) **2~3(확정 2)** · 계열 B(다른 수법에 확인 무력화를 얹은 시나리오) **4~5(확정 4)**. 6종 중 **5건 변경 · 1건 유지**.** ① **E① = (a)** — 범위 자체를 다시 본다(값만 조정하는 (b)는 **불가능**하다: `verifyIntercept.test.ts:310-317`이 `>=2 && <=3`을 6종 전수에 기계로 못 박고 있어 값 상향이 곧 게이트 파손이다). ② **값**: `institution`·`card`·`tax`·`courier` **2→4**, `loan` **3→4**, `bank-security-verify-desk` **2 유지**(전용 시나리오 — 올리면 T95의 저작 목적이 깎인다). ③ **게이트 형태 교체**: 범위식이 아니라 **offerId→값 6행 전수 대응표 단언**(§22.6 ⑦ 형식) — 범위식으로 두면 계열 A/B가 갈리지 않아 **계열 B가 다시 2로 내려와도 초록불**이다(G110). ④ **축은 유지**(E④) — 대화 단계 탐지는 §16.1.1/§22.2 D의 구조적 불가, 참가자 발화 분류는 AC-024 위반, 시간 축은 §16.1.3 결정론 원칙 위반, **프롬프트 조건절로 모델이 억제하는 변형은 §16.1.2 E행의 단방향 보장(컨트롤이 뜨면 대사도 발행)을 잃는다.** ⑤ **계열을 카탈로그 필드로 선언하지 않는다**(§18.1 선례 — 죽은 필드 또는 드리프트 원천). 계열은 값 표 자체로 표현된다. ⑥ **신규 필드 0건 · 신규 상수 0건 · AC 신설 0건 · 번호 예약 0건 · 소스 변경은 상수 5개 + 주석 1곳 + 테스트 1블록.** | **(a) ⭐ 범위가 자기 근거절을 지키지 못했다**: 원 권고(`Architecture.md:1691`)의 조건은 *"**요구가 한 번은 나온 뒤**"* 인데 사기범 턴 1~2는 전형적으로 **인사·사안 고지·본인 확인**이라 **하한 2에서 그 조건이 성립하지 않는다** — 값 2는 **범위 안에 있으면서 범위의 근거절을 위반**한다. 관측 근거(인용값): T118 행 A항 ②에서 전환 **이후**에도 사기범이 *"박OO님 맞으시죠? 최근에 통장 개설하신 적 있으세요?"* 로 **본인 확인을 진행 중**이었다. **(b) 쏠림의 원인은 값이 아니라 범위였다**: 전용 시나리오만 2를 쓸 명시 근거를 코드 주석에 갖고 있는데(`verifyIntercept.ts:155-162`) 근거 없는 4종이 같은 하한에 몰렸다 — **범위가 계열 구분을 제공하지 않았기 때문**이다. **(c) ⭐ 사용자 신고를 값 하나로 환원하지 않았다**: *"갑자기 초반에"* 는 ㉠타이밍·㉡선제성·㉢개연성 세 갈래이며 **이 결정은 ㉠만 다룬다.** ㉡(참가자가 확인 의사를 밝히지 않아도 사기범이 먼저 제안)은 **§16.1.4 `:1690`이 명시적으로 받아들인 트레이드오프**라 값으로 사라지지 않고, ㉢은 문면·저작 층(AC-071 사용자 확정 대기)이다. **⛔ 값 변경을 "신고 해결"로 보고하지 말 것.** **(d) 상한 5는 데이터가 아니라 울타리다(정직 고지)**: 속는 시점(`deceivedMoment`)의 턴 분포가 이 저장소에 **없다** ⇒ *"근거 없는 구간으로 넘어가지 않는다"* 는 보수 규칙으로 정했고, 5를 넘기려면 분포 측정이 선행이다(OQ 제안 — 등재는 planner). **(e) 올려서 잃는 것 2건**: 대기 창이 2턴→4턴으로 넓어지고(`promptAssembly.ts:127`의 *"잠시만요"* 로 최대 3턴을 버틴다), ⚠️ **그 대기 규칙이 실시간에서 판정 불가일 수 있다** — §25.2 ㉡이 실측한 턴 상대 지시어 결함이 같은 블록 `:128`에 있고 `:127`도 같은 형태다(**추정 · 미검증**). **오퍼가 안 뜰 위험은 무시할 수준**(`MAX_USER_TURNS=100`/60분, `shared/constants.ts:15-16`). **(f) T118과 충돌 0건**: R-1이 *"이른 오퍼가 재발화를 악화시킨다"* 는 P1 상향 논거의 (가) 갈래를 이미 봉쇄했고(⇒ **P2 유지**), 값 상향은 전환 이후 구간을 줄여 **A5의 부담을 덜어 준다**(추정). **(g) 실측 정정 9건**: T119 행의 인용 줄번호가 전부 어긋난다(`verifyIntercept.ts:85→109` 외 8건) — **값은 planner가 맞았고** 원인은 T118 병합 전 트리를 읽은 것으로 **추정**된다. `docs/Tasks.md`는 planner 소유라 고치지 않았다. **(h) architect는 셸이 없다** — 테스트·라이브·git을 한 번도 실행하지 않았고 깨지는 테스트 **1건**은 **grep 기반 판정**이다(518/216은 인용값, 착수 시 재측정). | — (ADR 신설 없음 — **ADR-0009/ADR-0013의 콘텐츠·설계 파라미터** 조정이고 제품 구조·데이터 계약·안전 게이트를 하나도 바꾸지 않는다. 되돌리는 비용이 상수 5개다. DECISIONS #48·#50·#51·#54·#55의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 58 | 2026-07-28 | **(T121 / OQ-U29 판정) 안전계좌 문자에 링크를 싣는 방식은 (가) 같은 항목 병기다 — `kind`는 판별자로 유지하고 링크 필드의 kind 스코프만 `"link"` → `"link"\|"account"` 로 넓히며 `"otp"`에는 금지한다. 넓힌 자리는 타입이 아니라 게이트(G111)가 지킨다. 그리고 D-60("속은 순간 = 제출")은 **화면 층에서만 수용하고 기록 층에서는 수용하지 않는다** — 신규 이벤트·스키마·컬렉션·콜러블 0건.** ⚠️ **행 위치 고지: 이 행은 #57 아래가 아니라 #56과 #57 사이에 놓였다**(편집 순서상 삽입 지점이 어긋났다 — **내용·번호는 정확하며 표의 정렬만 어긋난다**. 다음 패스에서 정렬을 바로잡을 수 있고, 그때까지 `#` 열이 정본이다). ① **신규 `kind` 0건**(UX 의견에 동의) — `InCallSmsKind`는 **저장 enum**이라(`shared/types.ts:194` Firestore, `report/smsTimeline.ts:146` 리포트 스냅샷) 값을 늘리면 `Database.md` 변경 + 과거 리포트와 두 값 체계 공존 + `deriveSmsEvents` 파생 규칙표 이중화가 딸려 온다. `MockScreenKind` 신설도 기각(`mockScreens.ts:119-122` 원문 그대로 — 이 화면도 *"정보를 입력하게 만드는 화면"*). ② **신규 구속 규칙 R10/R11**: kind는 *"문자가 무엇을 요구하는가"*, 링크 부착은 그와 **직교하는 옵셔널 부착**이며 **양방향 되추론 금지** / 링크 두 필드는 **항상 짝**이고 한쪽만 있는 상태를 **G111**이 금지한다(역검증 필수). ③ **확정값**: 랜딩 `safe-account-register` · `credential-form` · `entrySurface:"in-call-sms"` · 소속 `institutional-impersonation` · `landingKind` 키는 **생성하지 않는다**(기본값 생략 규칙 무변경). ④ **D-60은 층을 가른다** — ①노출은 기존 `recordInCallSmsEvent("link_tapped")`로 **이미 성립**, ②미확정은 **기록 경로 자체가 없어** 성립, **③제출은 성립하지 않는다.** 만들지 않는 이유 3중: AC 근거 부재(**OQ-U31** open) · **R6 얽힘**(통화 세션에 `mockScreens` 문서가 처음 생기면 앵커·`mockScreenTimeline`·`stages` 파생이 돌기 시작한다 — **G81**) · **OQ-A19가 글자 그대로 같은 질문**이라 신규 OQ를 만들지 않는다. ⑤ **커밋 배치는 §19.8.1 표 그대로** — 게이트 재작성(배치-1) → 스키마·배선(배치-1) → 콘텐츠(배치-2, 참조 데이터와 같은 커밋). ⑥ **갭 G111~G115** · **`Database.md` T121 사유 변경 0줄** · **AC 신설·번호 예약 0건** · **소스 0줄**. ⑦ **부수 소진**: §22.3 `:3125`가 예약해 둔 `API.md`·`Database.md` 델타(T110 `displayNumber` 제거 반영)를 **이번 패스에서 반영했다.** | **(a) ⭐ 상류 전제 정정 1건이 이 판정의 출발점이다**: UX v1.16 (c)가 *"기존 이벤트 `fake_landing_shown`/`fake_landing_submitted`가 이미 이 경계를 그린다"* 고 적었으나 **두 이름은 코드에 존재하지 않는다**(`**/*.{ts,tsx}` 전수 grep 0건). 실재하는 것은 `recordMockScreenEvent("shown"\|"consented")`(`mockScreens/index.ts:55`)와 `recordInCallSmsEvent("opened"\|"link_tapped")`(`inCallSms/types.ts:15`)뿐이고 **"제출" 이벤트는 어디에도 없으며**, `consented`는 **app-install 전용이라 credential-form에는 콜러블이 `invalid-argument`로 거부한다**(`mockScreens/index.ts:74-76`). credential-form 제출은 **컴포넌트 로컬 state**를 벗어나지 않는다(`MessengerFakeLanding.tsx:239`,`:247`). ⇒ D-60은 *"행동 변경 0건의 명문화"* 가 **아니다.** **(b) 왜 (가)가 §14.9.1을 어기지 않는가**: 원문이 기각한 것은 ***"link인데 `fakeLandingId`가 없는"*** 상태 — **kind가 약속한 필드의 부재가 성격 판정이 되는** 경우다(`shared/types.ts:188-190`). (가)는 그 역이고, R11의 짝 규칙이 *"칩은 떴는데 열 랜딩이 없다"*·*"랜딩은 있는데 칩이 없다"* 를 **카탈로그 층에서 구조적으로 막는다.** 명시 boolean 필드는 같은 정보를 두 곳에 두는 것이라 기각(§19.4 #3 · §18.1 · §22.3 `displayNumber` 교훈). **(c) 기존 게이트는 대부분 무변경이다(실측)**: **G-B는 kind를 보지 않고 `fakeLandingId` 존재만 본다**(`mockScreens.test.ts:244`)라 병기 항목도 **자동으로** 양방향 정합을 탄다 — **확장 0건**. G53(messenger-link 한정)·G55-a/b·channel 단언(`app-install` 보유 시나리오 한정)·R6·G-A·G-C·G-D **전부 무변경**. **깨지는 것은 2건뿐**이고 둘 다 `inCallSms.test.ts`다(`:71-74` account⇒링크 부재, `:174-175` buildDoc 반영) — **삭제가 아니라 G111/G112로 좁게 재작성**한다. **(d) AC-079는 이미 필드 단위로 검증 가능하다**: `DIVERGENCE_FIELDS`(`mockScreens.test.ts:629`)+`fieldDivergenceStats`(`:654`)가 4필드 쌍별 비교와 비교쌍/제외쌍/수렴쌍 통계를 내고 대상은 카탈로그 전수라 **자동 확장**된다 — 유일한 하드코딩은 `:690` `items.length === 5`이고 **삭제하지 말고 6으로만** 갱신한다(**G114**). UX가 낸 4필드 대조표는 **의도이지 판정 증거가 아니다**(UX 자기 고지). **(e) "국민안전지원센터" 판정(⚠️ 정적 판정 — architect는 셸이 없어 게이트를 실행하지 못했다)**: 문자 `body`는 **이미 `mockSurface` 스캔 대상**이고(`harmlessnessGate.test.ts:101`) `realInstitutionName`은 **리터럴 11개 부분 문자열 검사**인데(`harmlessnessPatterns.ts:67-79`,`:282`) 그 이름은 11개 중 어느 것도 포함하지 않는다 ⇒ **걸리지 않는다(정적)**. 확정은 implementer의 **P-2 프로브**(착수 0단계 전건 실행)이며 결과별 처방을 **if/then 5행**으로 못 박았다 — 특히 **기존 문면이 위반이면 T121에서 고치지 말고 별건 분리**(대조표 미끼 인용·`BAIT_TO_LANDING` 앵커가 함께 밀린다), **신규 문자열이 위반이면 가상 표기를 바꿔 해소**(⛔ 게이트 완화·`skip` 등록 금지). **(f) 감수하는 한계(정직 고지)**: ***"이제 실제로 걸리는지 판정할 수 있다"* 는 보고 금지** — 제출은 어디에도 기록되지 않으므로 말할 수 있는 최대치는 *"응낙 행위가 화면에 생겼고 그 직전 단계(링크 탭)까지 기록된다"* 다. 부수적으로 `deceivedMoments` 증가가 0이라 **AC-062 불변식은 손상되지 않는다.** **T116(렌더 층 미검증)도 미해결**이라 카탈로그 값 대조까지만 검증된다. **(g) R-10은 사용자 확정 대기이며 architect가 정하지 않았다** — 이 결정은 **①(계좌 정보 제출)을 전제로만** 성립하고, ②(모의 이체)가 오면 `credential-form` 재사용이 무너져 **신규 kind가 불가피**해지며 P-24 (1)의 *"경로가 없어서 불가능"* 이 *"버튼은 있지만 아무 일도 안 한다"* 로 강등된다 ⇒ **T121 증분으로 받을 수 없고 AC 신설 + 신규 절 + 새 ADR이 선행**이다(파급은 §27.9). **(h) architect는 셸이 없다** — 테스트·게이트·라이브·`git log`를 한 번도 실행하지 않았다(518/216은 인용값). base 검증만은 실측이다: 워크트리 HEAD·`refs/heads/main` **둘 다 `7a4f53b`**(`.git` 직접 판독). | — (ADR 신설 없음 — **ADR-0007(통화 중 문자 전달 모델)·ADR-0012(entrySurface 게이트 키잉)의 하위호환 증분**이고 제품 구조·데이터 계약·안전 게이트의 **요구**를 하나도 바꾸지 않는다. 되돌리는 비용이 카탈로그 항목 2개 + 조건식 3곳이다. DECISIONS #48·#50·#51·#54·#55·#56·#57의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 59 | 2026-07-28 | **(T125 판정 — 조건 분리) 확인 시도 저지는 "모순"이 아니라 "적용 범위 미분리"다. 수법(`weakenedTactics`·`personaPrompt`)은 **한 글자도 고치지 않고**, `promptAssembly.ts`의 상시 블록 `VERIFY_INTERCEPT_RULE`에 **적용 범위 선언 1문장(+반대 조건 1문장)** 을 얹는다 — 후보 (B) 단독 채택, (A)·(C)·(D) 기각.** ① **사용자 확정(2026-07-28)**: *"둘 중 하나가 틀린 게 아니다. 끊으려는 걸 막는 건 사기범의 정상 행동이고 유지돼야 한다"* ⇒ **삭제·약화는 후보가 아니다**(§28.0 1). ② **(B) 채택의 결정적 근거는 조건 정합이다** — 그 블록은 `verifyInterceptEnabled === true`(= 카탈로그 && **고급**, `roleplay/index.ts:186`·`geminiProvider.ts:80`)일 때만 부착되고(`promptAssembly.ts:323`), 그것이 곧 **"가짜 확인 창구가 실재하는 세션"** 이다. 반면 `weakenedTactics`·`personaPrompt`는 **무조건 방출**(`:302`·`:309`·`:311`)이라 난이도도 메커닉 상태도 모른다 ⇒ (A)로 좁히면 **창구가 없는 중급에서도 수법이 무뎌진다** = 사용자 확정 위반. ③ **행동 판정표 4행**(확인 목적 이탈 → 막지 않는다 / 그 밖의 이탈 → **현행 수법대로 막는다** / 제3자·신고 → 범위 밖 / 애매 → 확인 목적으로 처리) + **문면 요건 F1~F6**(범위 선언 · **반대 조건 명문 필수** · 라벨 열거 금지 · 2문장 이내 · 자리는 상시 블록 하나 · 기존 4항 문면 무변경). ④ **T118 §25.9 ⑦ / §25.0 2 / G104는 T118 범위 한정이며 T125에 적용되지 않는다**(§28.3.1) — 원문이 스스로 *"이 절의 변경은 실시간 경로 전용"* 이라 적었고 diff 0줄은 **그 패스의 완료 증거**이지 영구 동결이 아니다. **원문은 한 글자도 고치지 않았고 §28.3.1이 갱신 고지다.** ⑤ **소스 게이트 신설**(6종 전수 조립 결과 대조 + 역검증 2건: 선언 제거 사본 실패 / `false` 조립엔 선언 부재) + **`*.prompt.ts` diff 0줄**이 "약화 없음"의 기계적 증거. ⑥ **AC 신설 0건 · 번호 예약 0건 · 스키마 0건 · 신규 필드 0건 · 조립 순서 변경 0건.** | **(a) ⭐ 원인 귀속 정정 1건(실측 인용)**: base(게이트 **2**)에서도 저지가 재현됐다 ⇒ **T119의 게이트 상향(2→4)은 원인이 아니다** — reviewer가 물었던 *"노출 시간 증가가 원인인가"* 는 **아니오**. 누적 재현은 **4/4**이며 `docs/Tasks.md` T125 행의 `2/2`는 낡았다(정정은 planner 소유). **(b) 충돌 표면은 "2곳"이 아니다(grep 실측)** — 카탈로그 보유 6종 중 5종에 **17개 문자열**(weakenedTactics 11 + **personaPrompt 6**)이 정면 충돌하고, `bank-security-verify-scam`만 **설계상 0건**이다(`scenarios.test.ts:242-254`가 이미 금지). personaPrompt는 템플릿 리터럴이라 **그 안이 곧 모델 지시**이고 T92 내부표기·G71 인용구·T109 요구 계수 게이트가 걸려 있어 (A)의 편집 비용·회귀 위험이 (B)의 수십 배다. **(c) 왜 "확인 시도만" 예외인가**: AC-071(`PRD.md:573`)이 요구하는 것은 *"막지 않고 오히려 권한다"* + *"확인했는데도 속은 순간"* 이라, **참가자가 가짜 창구에 도달하지 못하면 그 순간 자체가 생기지 않는다**; 게다가 저지가 성공하면 참가자가 확인을 포기하거나 통화를 끊어 **AC-071 계열이 훈련으로 성립하지 않는다**. **(d) 그럼에도 반대 조건(F2)이 필수인 이유**: 예외를 목적 조건부로 못 박지 않으면 모델이 *"이탈 저지 전반 금지"* 로 과일반화해 **AC-074 L2와 축 D5가 통째로 꺼진다** — 사용자 확정에 정면으로 반한다(**G117**, 라이브 반대 방향 1회로 검증). **(e) 사용자 대안(*"한 번은 막아 보고"*)은 L3와 중복이 아니다** — L3는 *"어떤 D 수법을 쓰는가"* 이고 대안은 *"이미 켜진 D3와 D5의 **순서·횟수**"* 로 §17에 축이 없다. "고급에서만"은 조건을 **추가하지 않는다**(확인 무력화는 이미 고급 전용) ⇒ AC-065 저촉 0. 비용은 (i) **모델에 카운터가 없다**(턴 상대 지시어가 실시간에서 판정되지 않는 것은 §25.2 ㉡ 실측), (ii) 게이트 4턴과 곱해진 지연, (iii) 첫 저지에 물러서면 체험 미성립 — **(iii)은 미측정 추정**. ⛔ **채택 여부는 사용자 확정이며 이 결정은 무조건 우선(F1)으로 설계했다.** **(f) 감수하는 한계(⛔ *"이제 확인 저지가 없어진다"* 보고 금지)**: 프롬프트 층 수단이라 **보장이 아니다** — 규칙은 **이미 뒤에 있었는데도**(`:323`) 저지가 나왔다는 것이 이번 결함 자체의 반례다; 실시간은 setup 1회 고정이라 폴백보다 구조적으로 약하다(§22.8 (3)); *"확인하려고 끊겠다"* 의 판정은 **원리적으로 모델이 한다**(서버 분류는 AC-024 위반); personaPrompt 6건은 그대로 남고, 재현 시 다음 후보는 (C)다. **(g) 폴백 경로도 함께 바뀐다 — 이번엔 그것이 의도다**(충돌이 두 경로 모두에 있다). 대신 **T113 축 순회 증거가 낡으므로 재실행이 완료 증거에 포함**된다. **(h) architect는 셸이 없다** — 테스트·라이브·git을 한 번도 실행하지 않았다(519/216은 인용값, 4/4와 base 재현은 오케스트레이터 인계 인용값). base 검증만 실측이다: `.git/refs/heads/main` = **`89c9b57`**(직접 판독, 지정 base와 일치). | — (ADR 신설 없음 — **ADR-0009/ADR-0013이 세운 확인 무력화 흐름의 프롬프트 문면 조정**이고 제품 구조·데이터 계약·안전 게이트의 **요구**를 하나도 바꾸지 않는다. 되돌리는 비용이 상시 블록 문장 1~2개다. DECISIONS #48·#50·#51·#54·#55·#56·#57·#58의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 57 | 2026-07-28 | **(T118 후속 판정) 전환 이후 모델이 즉흥으로 만든 **잔류 암시형 3인칭 서술**에 대해, G항 (다)의 판정 기준을 **사후 판정 층(㉰)에서만 넓힌다.** 런타임 차단(㉮)은 **구조적 불가**이고 카탈로그 소스 게이트(㉯) 확장은 **검출력 증가 0**이다. 넓힌 항목 **(다-2)** 는 **합격 축이 아니라 기록 축**이다.** ① **관측**(라이브 1회, `bank-security-verify-scam`): *"앞서 담당자는 현재 다른 업무를 진행 중이십니다."* — reviewer·QA 양쪽 **문면 기준 합격**(이름·직책·1인칭 0회), reviewer가 **의도 기준 미흡**(잔류 암시)을 별도로 짚었다. ② **㉮ 불가**: 응답 모달리티 오디오 고정(`geminiProvider.ts:86`)이라 서버가 사기범 텍스트를 **세션 중** 쥐는 지점이 없다(§22.2 D 그대로). ③ **㉯ 무의미**: `positiveResidencyDemands`(`verifyIntercept.test.ts:586`)·`returnAllowingMentions`(`:638`)의 입력은 **카탈로그 합성 문자열**(`:740`·`:745`)이라 모델 즉흥 발화가 애초에 들어오지 않는다. ④ **㉰ 가능 — 여기만 넓힌다**: 전사는 종료 시 `messages`에 남고 `maskPII`가 **이름·직책을 마스킹하지 않는다**(`guardrails/index.ts:24-25`). ⑤ **(다-2)는 기록 축**: 관측 시 전사 원문·시나리오·회차를 적고 **빈도를 센다.** ⛔ 이 항목으로 라이브 증거를 **반려하지 않는다.** **승격 조건: 독립 3회 중 2회 재현.** ⑥ **재현 시 처방은 문면만 확정해 두고 지금 채택하지 않는다** — 자리는 **상시 블록 한 곳**(`promptAssembly.ts:126-130`), ⛔ `transferStateLine`에 넣으면 **G100 정면 위반**. ⑦ **§25.9 행 9·§22.6 4 ①의 문면은 한 글자도 고치지 않았다**(얹기 관례). ⑧ **implementer 할 일 0건 · AC 신설 0건.** | **(a) ⭐ 인계문의 벽은 절반만 맞다(이 결정의 핵심 정정)**: *"§22.2가 그 영역을 기각했으니 넓힐 수 없다"* 는 **㉮에만** 해당한다 — §22.2가 기각한 것은 **세션 중 서버가 모델 텍스트를 쥐는 것**이고, **사후 전사 판정은 §22.6이 이미 채택해 쓰고 있는 경로**다. ⇒ *"못 한다"* 로 닫는 것은 **게이트 능력의 과소 서술**이 된다. **(b) 그러나 넓어지는 것은 "무엇을 보는가"이지 "무엇을 막는가"가 아니다(정직 고지)**: 이 결정으로 그 문장이 덜 나오게 되는 효과는 **0건**이다. **⛔ *"잔류 암시를 막았다"* 라고 보고하지 말 것.** **(c) 왜 합격 축이 아닌가**: 관측 **1회**이고 빈도·재현성이 **미검증**이다. 합격 축으로 올리면 비결정적 모델 출력 1건이 라이브 증거 전체를 반려시켜 **일일 20건 예산을 태운다**(T117 오보가 겪은 것과 같은 계열의 절차 실패). §22.9가 *"측정 전에 복잡도를 늘리지 않는다"* 로 유보한 것과 **같은 자리**이며, 그 선례는 실제로 T118에서 측정 후 채택으로 이어졌다. **(d) 왜 상시 블록 규칙을 지금 넣지 않는가(3중)**: 관측 1회 · 상시 블록이 길수록 모델이 **지시를 대사로 낭독**할 위험이 커진다(§25.3 F2) · `promptAssembly.ts`는 **폴백과 공유**라 손대면 §25.9 ⑦(세 파일 diff 0줄)의 무회귀 전제와 T113 축 순회 증거가 함께 낡는다(G104 계열). **(e) 남는 한계**: ㉮가 불가인 한 판정은 **영구히 사후**이고, (다-2)의 판정은 **사람이 한다**(기계로 갈리지 않는다). 빈도가 낮으면 승격 조건이 **영영 충족되지 않을 수 있고** 그 경우 이 항목은 **열린 채 남는다** — 닫힌 것으로 읽지 말 것. **(f) §22.2를 뒤집지 않는다** — 오히려 §22.2가 기각한 층(㉮)과 채택한 층(㉰)의 **경계를 명시**할 뿐이다. **(g) architect는 라이브를 열지 않았다** — 관측 원문과 reviewer·QA 판정은 **전부 인용값**이다. | — (ADR 신설 없음 — **판정 기준·관측 절차** 결정이고 제품 구조·데이터 계약·안전 게이트의 **요구**를 하나도 바꾸지 않는다. 코드 변경 0줄. DECISIONS #54·#55 관례 계승) |
| 61 | 2026-07-28 | **(T125 후속 — 사용자 확정 채택) §28.5 대안(*"고급에서는 한 번 막아보고, 참가자가 재차 고집하면 그때 확인 창구로 넘긴다"*)이 **사용자 확정으로 채택**됐다 — **1회 만류를 버그가 아니라 설계로 받는다.** `personaPrompt`는 **무변경**(`*.prompt.ts` diff 0줄 유지)이고, `promptAssembly.ts`의 상시 블록 `VERIFY_INTERCEPT_RULE`에서 **무조건형 2문장만 조건형으로 교체**한다(F1 = `:146` 첫 문장 · `:147` 첫 절). **F2(반대 조건)는 한 글자도 바꾸지 않는다.**** ① ⭐ **(C) 조건부 치환의 §29.2.3 대상 4건을 대상에서 철회한다**(§29.2의 채택 대상 집합을 뒤집는 갱신 고지) — 그 문장(*"상대가 확인하려 하면 압박한다"*, institutional `:36`·card `:35`·courier `:29`·loan `:30`)이 기술하는 행동이 **정확히 채택된 설계**이므로 치환하면 **방금 채택한 설계를 스스로 지운다**. ② **단 (C) 자체는 철회가 아니라 보류** — ⭐ **실측으로 충돌이 한 줄 아래로 이동했다**: 각 대상의 **바로 다음 줄**에 **지속 압박 절**이 있고(institutional `:37`·card `:36`·courier `:30`·loan `:31`·tax `:32`, 전부 *"한 번으로 끝내지 않고 … 계속 밀어붙인다/압박한다"*) 이것이 **채택의 후반부(*"재차 고집하면 넘긴다"*)와 정면 충돌**한다. **발동은 §30.1.4 if/then 표가 유일한 정본이며 지금 구현하지 않는다**(G131). ③ ⭐⭐ **"한 번"의 정의 — 세 갈래 판정**: **(가) 횟수 문면 불가**(모델에 카운터가 없다 · 턴 인덱스는 §25.2 ㉡ 실측으로 실시간에서 참조 대상 부재), **(나) 서버 계수 불가**(오퍼 게이트가 세는 것은 **사기범 턴 경계**이지 참가자 의사가 아니다 — `verifyIntercept.ts:33-40`의 입력이 `scammerTurns`이고 `alreadyRequested`는 **참가자 액션이 아니라 중복 방지 래치**다(`page.tsx:444`·`:449`); 의사를 세려면 **참가자 발화 분류**가 필요해 **AC-024 정면 위반**), **(다) ⇒ 순서·조건 근사를 채택** — *"먼저 … 그러고도 상대가 **다시** …하면"*. ⭐ **㉡와 갈리는 결정적 차이는 참조 대상의 위치**다: ㉡의 *"전환 지시가 온 턴"* 은 **프롬프트 밖**이라 어느 턴에도 참이 되지 않았지만, *"상대가 다시 말한다"* 는 **대화 기록 안**에 있다(폴백은 `roleplay/index.ts:103-104`→`:122`가 이력 전량 재조립, 실시간은 Live 세션 컨텍스트) ⇒ **모델이 세지 않고 관찰한다.** 문면 요건 **F11**(횟수 금지·순서 필수)·**F12**(만류용 새 압박 문장 저작 금지 — 기존 목록을 가리킨다)·**F13**(*"더 붙잡지 않는다"* 명시)·**F14**(F2 무변경), **F4(2문장 이내) 유지**(초안이 2문장으로 성립함을 확인 — 완화하지 않았다). ④ **오퍼 게이트(계열 B 4)와의 곱셈 위험**: ⭐ **실측이 리스크를 좁힌다** — 오퍼는 **참가자 요구와 무관하게 사기범 턴 수만으로 자동 발동**하고(`page.tsx:433-449`) `announceInstruction`이 **사기범이 먼저 제안**하게 한다(`scenarios/verifyIntercept.ts:110`) ⇒ 첫 만류에 물러서도 **오퍼는 온다**, 미성립 경로는 ***"참가자가 실제로 통화를 끊는 경우"*** 로 좁혀진다. 완화 **(ㄱ) F12 채택**(증강 금지이지 약화가 아니다) · **(ㄴ) *"저지 후 게이트 무시"* 기각**(게이트를 참가자 의사에 연동 = AC-024 위반) · **(ㄷ) 값 재검토는 T119 인계**(⛔ **한 글자도 바꾸지 않았다**, G132). ⑤ **검증 — 판정 대상이 바뀐다**: ~~*"저지가 사라졌는가"*~~ → ***"만류 후 재차 고집하면 넘어가는가"***. ⭐ **⓵ base "재차 고집" 3회를 첫 번째로 잰다**(§29.5가 자기 고지한 **미측정 조각**이며 **아무도 그 다음 턴을 본 적이 없다** — **(C) 발동 판정표의 유일한 입력**), ⓶ 후속 5회, **⓷ 반대 방향 1회는 계속 필수**(사용자 요구 — 저지 소멸 시 **즉시 반려**). 합계 **9건 · 같은 날**(§29.4.1 울타리 계승). 회차 결과는 **4범주(㈎이양·㈏압박지속·㈐회피·㈑판정불가)** 로 분류하고 ⛔ **㈐를 ㈎로 세지 않는다**(G134). ⑥ **난이도 축 — §28.5의 두 판정 모두 유지**: 조건 추가 **0건**(`roleplay/index.ts:186`·`geminiProvider.ts:80-81` 재확인 — 이 블록이 있는 세션은 전부 고급), **AC-065 저촉 0**, **L3 중복 아님**(L3는 *"어떤 D 수법"*, 채택은 *"D3·D5의 순서"* — §17에 순서 축 없음). ⭐ **신규 판정: §17에 순서 축을 신설하지 않는다**(레버가 아니라 조건부 블록의 내용 · §18.1의 죽은 필드 회피) — `l3Depth.ts`·§17.4 표 무변경. ⑦ **AC 신설 0건 · 번호 예약 0건 · 스키마 0건 · `API.md`/`Database.md` 무변경 · ADR 0건 · 소스 0줄 · `*.prompt.ts` diff 0줄 · 조립 순서 변경 0건.** | **(a) ⭐ 이 결정이 §28·§29를 뒤집는 것이 아니라 전제를 바꾼다**: §29.1의 **문면 층 2회 실패 실측**은 지금도 옳다. 바뀐 것은 **무엇을 이기려 했는가**다 — 두 패스 모두 *"1회 저지를 없앤다"* 를 목표로 삼았는데 **그 목표가 사용자 확정으로 철회됐다.** **(b) ⭐ 그래서 이번 F1 교체는 G122(세 번째 문면안 금지)를 밟지 않는다**(4행 판정): G122가 금지한 것은 *"같은 목표를 문면 강도로 다시 시도"* 인데 **목표가 다르고**, 강도 **동일**·길이 **2문장 유지**·위치 **`:343` 그대로**라 *"더 세게/길게/앞에"* 셋 다 아니며, 무엇보다 ⭐ **전반부는 이기려는 대신 `:36` 계열과 편이 된다** — 이것이 채택의 최대 이득이고 §29.5 가설의 확정 부분이다. **(c) ⚠️ 그러나 충돌은 절반만 사라졌다(가장 중요한 한계, ⛔ *"모순이 해소됐다"* 보고 금지)**: 후반부는 `:37` 계열과 **여전히 충돌**하며 **그 충돌이 실제로 이기는지는 관측 0건**이다 ⇒ 그래서 (C)를 **철회가 아니라 보류**로 두고 ⓵ 3회를 첫 번째로 잰다. **(d) ⭐ `:37` 계열을 지금 손대지 않는 결정적 이유**: 그 5줄은 **`[말투/톤] (2026-07-24 사용자 요청 — 압박이 한 번에 안 먹히면 물러서지 않고 강도를 높인다)` 헤더 아래에 있다**(각 파일 직접 판독) — **이전 사용자 확정으로 저작된 문장**이라 좁히는 것은 **G124를 밟으면서 그 확정을 뒤집는 일**이고 **architect 단독 사안이 아니다.** 게다가 **두 층을 동시에 바꾸면 어느 쪽이 들었는지 판정할 수 없다**(§29.7 2와 같은 논리). **(e) F1을 덧쓰지 않고 교체하는 이유**: 병합된 `:146` 첫 문장(*"…경우에는 아래 항목이 그 목록보다 **우선한다**"*)과 `:147` 첫 절(*"막지 않는다 — 붙잡지도 않는다"*)은 **둘 다 무조건형**이라 채택과 정면 모순이다 — 덧쓰면 **한 블록 안에 모순 2문장**이 생겨 **§28이 없애려던 상태를 자기 안에 재생산**하고 라이브에서 어느 쪽이 적용됐는지 **판정 불가**가 된다(G128). ⛔ **되돌리기(revert)는 하지 않는다** — §29.3의 병합 4근거는 채택으로 **하나도 무효화되지 않았고**, 특히 **⓸ 반대 방향 통과**는 채택과 무관하게 유효하다. **(f) 보고 문장을 이번엔 더 좁게 고정한다**: ⛔ *"확인 저지가 없어졌다"* 뿐 아니라 ⭐ ***"저지가 줄었다"* 류도 금지**한다 — **1회 만류가 남는 것이 설계**이므로 그런 표현은 **설계를 잘못 기술하는 것**이다. 쓸 수 있는 최대치는 *"base N회에서 재차 고집 이후 압박 지속이 x/3 관찰된 조건에서, 후속 5회 중 y회에서 이양이 관찰됐고, 확인과 무관한 이탈에는 저지가 유지됐다"* 까지다. **(g) 줄번호 정정 5건**: §29의 base `03e0102`와 현행 `f9af6df` 사이에 **PR #121이 병합돼 `promptAssembly.ts`가 약 20줄 밀렸다**(`personaPrompt` `:309`→**`:329`**, `weakenedTactics` `:302`·`:311`→**`:322`·`:331-332`**, 삽입 지점 `:323`→**`:343`**, 옵션 `:267`→**`:287`**, 순서 주석 `:286-289`→**`:306-317`**). **`*.prompt.ts` 대상 4건과 `roleplay/index.ts:186`은 변동 없다. §29 표는 고치지 않았고 §30.1.1이 갱신 고지다.** **(h) ⭐ §29가 읽지 못한 것을 이번엔 읽었다**: PR #121의 F1·F2 문면(`promptAssembly.ts:145-150`)을 직접 판독해 **§28.4 F1~F6대로 구현됐음을 실측 확인**했다 — §29가 *"전제로 받는다"* 고 한 인계가 사실이었다. **(i) 감수하는 한계**: 여전히 **프롬프트 층이라 보장이 아니고**(채택은 확률을 올리는 방향이지 보증 수단 0건), `weakenedTactics` 11문자열은 그대로 남으며, *"다시 고집했는지"* 의 판정도 **원리적으로 모델이 한다**(서버 분류는 AC-024 위반), **통화 이탈 위험은 실제로 커지고 정량화되지 않았다**(이탈 분포 데이터 부재), 경로 비대칭·**ElevenLabs 미도달**은 이 절도 닫지 않는다. ⚠️ **`promptAssembly.test.ts`에 F1 문면 리터럴 단언이 있으면 이번 교체로 깨진다**(PR #121이 신설한 게이트) — **착수 0단계에서 확인하고 삭제·`skip` 대신 새 문면으로 갱신**할 것. **(j) architect는 셸이 없다** — 테스트·라이브·`git log`를 한 번도 실행하지 않았다(519/216·라이브 5회·예산은 인용값). base 검증만 실측이다: `.git/refs/heads/main` = **`f9af6df`**(직접 판독, 지정 base와 일치). | — (ADR 신설 없음 — **ADR-0009/ADR-0013이 세운 확인 무력화 흐름의 프롬프트 문면 조정**이고 제품 구조·데이터 계약·안전 게이트의 **요구**를 하나도 바꾸지 않는다. 되돌리는 비용이 상시 블록 문장 2개다. DECISIONS #48·#50·#51·#54~#60의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 60 | 2026-07-28 | **(T125 후속 판정) §28의 (B)는 틀린 게 아니라 불충분했다 — **문면 층(층 1)을 닫고 조건부 치환 층(층 2 = 후보 (C))으로 내려간다.** PR #121은 **병합**하되 조건 M1·M2를 붙인다. 효과 판정의 최소 관측은 **한 날 안의 9건 세트**(base 3 + 후속 5 + 반대 방향 1)이며, ⛔ **오늘은 라이브를 0건 쓴다**(잔여 2건으로 세트 미성립).** ① ⭐ **층위 판정 — *"선언을 더 강하게 쓰면 되는가"* 는 아니오. 반례가 둘이다**: 1차 = **`promptAssembly.ts:127`이 base에 이미 갖고 있던 무조건 금지문**(*"'끊고 직접 확인해 보겠다'고 하면 막지 않는다 — 끊지 마시라고 붙잡지도 않는다"*) → **BASE-1 저지 재현**; 2차 = §28의 F1 범위 선언 + F2 → **PR 3회 중 2회 재현**. **서로 다른 문면 전략 2개가 같은 자리에서 졌다** ⇒ 남은 변형(*더 세게/길게/앞에*)은 §25.3 F2·§15.5가 이미 막아 둔 방향이라 **층 1을 닫는다**(G122). ② **후보 (C) 채택** — §28.3의 (C) 기각 사유 3건 중 **2건 불성립**: (i) *"조립 축이 하나 더 는다"* → **새 옵션 0건**(이미 있는 `opts.verifyInterceptEnabled`를 재사용, 호출부 3곳 무변경), (ii) *"지목 수단이 라벨뿐"* → **틀렸다. `personaPrompt`는 라벨 목록이 아니라 문장**이고 지목은 **문장 원문 완전 일치**라 §17.4.2/G66의 *조용한* 실패가 **소리 나는 실패**로 바뀐다(G125의 전수 대응표). (iii)(소스만 보고 방출 문자열을 알 수 없다)만 **비용으로 인수**하고 F7(치환 쌍 한 표에 나란히)로 완화. ③ ⭐ **(A) 기각 사유를 (C)는 상속하지 않는다** — (A)의 문제는 *"personaPrompt를 좁힌다"* 가 아니라 ***"조건을 알 수 없는 자리에서 좁힌다"*** 였고(`:309`는 무조건 방출), **조립 함수는 조건을 아는 자리**다 ⇒ 중급·초급·비카탈로그 8종 조립 결과는 **바이트 동일**(역검증 (b)가 기계로 증명). **`*.prompt.ts` diff 0줄도 유지**된다. ④ **치환 대상 = 4건**(institutional `:36` · card `:35` · courier `:29` · loan `:30`) — **§28.2의 "personaPrompt 6" 정정**: 그 값은 *"확인 목적 이탈"* 문장과 *"확인 언급 없는 일반 이탈"* 문장을 **섞어 센 것**이고, 후자는 **반대 방향 회차가 통과시킨 층**이라 **대상이 아니다**(G124). ⭐ **`tax-refund-scam`은 대상 0건** ⇒ **6종 전수 치환 단언은 정상 상태에서 빨간불**이 된다(G125). ⑤ **PR #121 병합** — 근거: (1) **미측정이 아니라 한 갈래 측정 성공**(F2 → ⓸ 반대 방향 저지 유지, §28.6 5 ⓸가 **사전에 못 박은 반려 게이트를 통과**), (2) ⭐ **(C)는 (B)를 대체하지 않고 전제로 한다**((C)는 좁힐 뿐 *"대신 무엇을 하라"* 를 말하지 않는다 — 그 지시는 `:127-128`과 F1에만 있어 (B) 없이 (C)만 넣으면 모델이 `weakenedTactics` 11문자열로 되돌아간다), (3) 되돌리면 T113 축 순회 재실행 + 곧 같은 형태로 다시 만들 게이트 폐기, (4) F4가 **2문장 상한**이라 표면 증가는 조건부 블록 1개 안이고 **8종 토큰 증가 0**. **조건 M1**(효과 범위를 주석·커밋에 정직히 표기 — ⛔ *"해소했다"* 금지) · **M2**((C) 실패 시 (B) 선언도 **자동 존치가 아니라 재판정 대상**). ⑥ **관측 세트 9건 · 판정표 5행 · 고정 보고 문장** — ⛔ *"부분적으로 작동한다"* 금지, 쓸 수 있는 것은 **재현율 두 개를 나란히**(base x/3 · 후속 y/5)뿐(G123). base ≤2/3이면 **그날 세트 전체 무효**, 후속 ≥2/5면 **층 2 실패 → 층 4(사후 판정·기록)로** 가고 ⛔ **네 번째 안을 만들지 않는다**. ⑦ **AC 신설 0건 · 번호 예약 0건 · 스키마 0건 · `API.md`/`Database.md` 무변경 · ADR 0건 · 소스 0줄.** | **(a) ⭐ 대안 원인 1건을 코드로 배제했다(이 결정의 전제)**: `institutional-impersonation`은 `voiceMode:"generic"`이라 `getRealtimeProvider`(`provider.ts:43-66`)에서 **ElevenLabs가 우선**이고, **그 경로는 `buildSystemPrompt`를 호출하지 않는다**(`agentMap.ts:3-11`·`elevenLabsProvider.ts:7`) — 그랬다면 §28 선언은 **모델에 도달조차 못 했고** 원인 분석 전체가 무의미해진다. 배제 근거는 `elevenLabsProvider.ts:61`의 **하드코딩 `difficultyApplied:false`** 다 ⇒ 인계된 `difficultyApplied=true`·`isMock=false`는 **Gemini Live 확정**(`geminiProvider.ts:168`·`mockProvider.ts:22`와의 3분기 판정표는 §29.1.3). **이후 라이브 회차는 이 표로 프로바이더를 판정한다 — ⛔ 추정 금지**(G126). **(b) ⭐ *"뒤에 오는 블록이 이긴다"* 가 처음으로 정면 시험되어 실패했다**: `promptAssembly.ts:286-289`는 순서 불변식의 근거를 *"뒤에 올수록 우선해 따르는 **경향**"* 으로 적었고, 선언은 `:323`으로 `personaPrompt`(`:309`)보다 확실히 뒤였는데도 **2회 졌다.** ⚠️ **그렇다고 §15.5를 약화하지 않는다** — `guardrailPreamble`이 최후미여야 할 이유는 *"뒤가 이긴다"* 하나가 아니라 **정면 충돌하는 앞선 지시가 존재하지 않는다**(안전 경계는 전 난이도·전 시나리오 단일 소스)가 함께 받친다. 무효화된 것은 **"뒤에 놓기만 하면 앞의 충돌 문장을 이긴다"는 확장 해석**뿐이고, **§15.5·`:286-289` 원문은 한 글자도 고치지 않았다**(§29.1.1이 갱신 고지). **(c) 왜 조립 함수 안인가(호출부 전수)**: `roleplay/index.ts:265`·`openingLine.ts:72`·`geminiProvider.ts:77` **셋 전부**가 이 함수를 지나고 ElevenLabs만 지나지 않는다 ⇒ 조립 층에 두면 **도달 가능한 세 경로에 한 번에** 적용되고 **호출부는 한 줄도 안 바뀐다** = G63 계열(*"한 곳이 빠져 그 경로만 조용히 축소"*) 재발 여지 0. **(d) (B)+(C)는 §28.3이 기각한 (D)가 아니다**: (D)의 문제는 **같은 규칙 2벌**이었는데 (C)는 **규칙을 추가하지 않고 충돌 문장을 조건부로 좁힐 뿐**이라 규칙의 자리는 여전히 **상시 블록 하나**다(§22.7 G82·G100 불변). **(e) 왜 `weakenedTactics` 11문자열은 이번에 안 건드리나(확신이 아니라 순서다)**: 라이브 발화가 **`personaPrompt` 문면과 먼저 일치했고**(PR-3 *"수사 기밀"* · PR-2 *"비협조적인 것으로 기록"* ↔ `institutional:36`의 두 지정 수단), 표면이 4 대 11이며, 무엇보다 **한 번에 두 층을 바꾸면 어느 쪽이 들었는지 판정할 수 없다.** ⇒ 실패 시에도 **11문자열 확장이 아니라 층 4로 간다**(G122와 같은 이유). **(f) 3과 5는 검정력이 아니라 울타리다(정직 고지)**: 발화 분포 데이터가 저장소에 **없어** 유의성 계산이 불가능하다(§26 (d) *"상한 5는 데이터가 아니라 울타리다"* 와 같은 성격). 9건 = 일일 20의 45%로 **하루 한 세트 + 재시도 여지**가 근거다. ⚠️ **세트를 이틀에 쪼개면 base와 후속이 다른 날의 모델 상태를 비교**하게 되어 대조 자체가 무너진다(G127). **(g) §28.5 대안의 매력은 올라갔다 — 그러나 결정하지 않는다**: 대안이 기술하는 *"한 번은 막는다"* 는 **모델이 이미 하고 있는 행동**이라, 채택하면 **버그가 아니라 설계로 받아들이는 길**이 열리고 **층 1·층 2가 모두 불필요해질 수 있다**(personaPrompt 무변경 + F1만 교체). §28.5의 비용 (i)(**모델에 카운터가 없다** — §25.2 ㉡ 실측)은 그대로지만 비용 (iii)의 반대편에 관측이 생겼다. ⚠️ **결정적 데이터는 아직 없다** — *저지를 받은 뒤 재차 고집했을 때* 무엇이 나왔는지가 인계에 없다(미측정). ⛔ **사용자 확정 사항이며 architect는 이번에도 답하지 않았다.** **(h) 감수하는 한계(⛔ *"이제 확인 저지가 없어진다"* 보고 금지)**: 여전히 프롬프트 층이라 **보장이 아니고**, `weakenedTactics` 11문자열은 그대로 남으며(`institutional:48`·`:62`는 무조건 방출), **판정은 원리적으로 모델이 한다**(서버 분류는 AC-024 위반), 인과는 **아직 추정**이고 §29.4 세트는 증명이 아니라 **증거**를 준다, 실시간/폴백 비대칭과 **ElevenLabs 미도달**은 이 절도 닫지 않는다, 라이브 검증은 **`institutional` 한 종뿐**이고 나머지 3종은 **소스 게이트가 유일한 증거**다. **(i) architect는 셸이 없다** — 테스트·라이브·`git log`를 한 번도 실행하지 않았고 **PR #121의 diff도 읽지 못했다**(519/216·라이브 5회·예산 18/20은 전부 인용값). base 검증만 실측이다: `.git/refs/heads/main` = **`03e0102`**(직접 판독, 지정 base와 일치). | — (ADR 신설 없음 — **ADR-0009/ADR-0013이 세운 확인 무력화 흐름의 프롬프트 조립 조정**이고 제품 구조·데이터 계약·안전 게이트의 **요구**를 하나도 바꾸지 않는다. **ADR-0004도 재해석하지 않았다** — 치환은 전부 서버 안에서 일어나고 클라 오버라이드 방향은 채택하지 않았다. 되돌리는 비용이 상수 표 1개 + 조건식 1곳이다. DECISIONS #48·#50·#51·#54~#59의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 62 | 2026-07-28 | **(T123 착수 게이트 판정 — AC-080 집행 설계) ⭐ R6 얽힘은 실재한다. 단 §27.3이 기술한 형태와 다르며, 결론은 더 강하다 — 통화 경로에서 `mockScreens` 문서를 만드는 길은 "위험한 길"이 아니라 ⛔ AC-080을 만족시킬 수 없는 길이다. 그래서 그 문서를 만들지 않고(G81·R6 무변경), 제출 사실을 **각 표면이 이미 갖고 있는 문서에 타임스탬프 1필드**로 남긴다 — 통화는 `inCallSms.landingSubmittedAt`, 메신저는 `mockScreens.submittedAt`. 신규 컬렉션·콜러블·컴포넌트 파일·`kind` **0건**, 늘어나는 것은 **enum 값 2개 + 옵셔널 필드 2개**뿐이다.** ① ⭐ **착수 게이트의 답(실측)**: `mockScreens` 앵커 리졸버의 **유일한 입력**은 `messages[].attachments[].fakeLandingId`인데(`report/mockScreenTimeline.ts:70-89` · `generateReportCore.ts:70-79`) 그 `attachments`는 **`[[LINK:id]]` 마커에서만** 생기는 **메신저 전용** 산물이고(`roleplay/linkMarker.ts:29-72` · `roleplay/index.ts:277-278` 원문 주석 *"보이스 세션의 응답에는 애초에 마커가 없다"*), 통화 중 문자의 링크는 **다른 컬렉션**에 있다(`inCallSms/buildDoc.ts:33-36`) ⇒ 통화 세션의 `mockScreens` 문서는 **항상 `anchorResolved:false`**(`:78-82`) → `consented` 강제 false(`:133`) → **승격 0건**(`:144`). ② ⭐ **§27.3 (2) 2의 과다 서술 2건 정정**(원문 무변경, §31.1 (2)가 갱신 고지): **`stages`는 얽히지 않는다**(`deriveReportStages`가 `entryChannel!=="messenger"`면 즉시 `[]`, `mock_install` 도달은 **app-install id 집합만** 본다 — `:206-208`·`:216`), **G15의 시계 병합도 새로 깨지지 않는다**(두 리졸버 **모두** `timeLabel`을 앵커 메시지 `createdAt`에서 파생 — `:83-88`·`smsTimeline.ts:116-125`). 실제로 도는 것은 `mockScreenTimeline` 하나뿐이다. ③ ⭐ **해법이 성립하는 결정적 이유**: 통화 문서는 앵커를 **이미 갖고 있고**(`InCallSmsDoc.anchorScammerTurn`, `shared/types.ts:215` — 서버가 경로별 ±보정까지 끝내 기록, `inCallSms/buildDoc.ts:62-76`), 그 값을 푸는 **`resolveAnchor`(`smsTimeline.ts:98-126`)가 바로 G15/G21을 이미 푼 리졸버**다 ⇒ 얽힘이 **열리지 않는다.** ⛔ **`MockScreenDoc`에 앵커 필드를 더하는 대안은 기각** — 앵커 규칙이 두 벌이 되어 §15.1.5 (6)(*"리졸버는 단 하나"*)을 정면으로 어기고 `displayNumber` 교훈(§18.1·§22.3)을 반복한다. ④ ⭐ **게이트 충돌 — 충돌하지 않는다. `mockScreenCopy.test.ts` 재작성 0건**: 금지 토큰 21개(`:32-59`)·AST 축(`:301-324`, 그 키 집합이 토큰 집합과 **같아야 한다는 단언** `:338-342`) 어디에도 걸리지 않는다. 기록 주체는 **페이지**이고 컴포넌트는 **무인자 콜백만** 올린다 — `:105-112`가 **이미 요구하는 형태**다. 게이트는 **지우지 않고 더한다**(**G138** + 역검증 1건 같은 커밋). ⑤ **이벤트 이름 — `"consented"` 재사용 기각**: 그 값은 소스상 *"가짜 **권한 허용**에 응했다"* 전용이다(`mockScreens/index.ts:74-76` kind 가드 · `shared/types.ts:294-296` · `scenarios/mockScreens.ts:87-88` 지시문) ⇒ 재사용은 **가드 약화**를 요구하고 설치 지시가 계좌 제출에 붙는 경로를 연다. 확정: **`"landing_submitted"`(문자 대상) · `"submitted"`(랜딩 대상)**. ⛔ `fake_landing_submitted`는 만들지 않는다(코드 0건인 UX 인용 이름). **리포트 표시 이벤트는 0건** — 승격된 `deceivedMoment` 하나가 AC-080 (d)를 충족하고, 더하면 **G17 이중 렌더** + 저장 enum 확장이다. ⑥ **OQ-A19 — 착수를 막지 않고 이 판정으로 해소된다**(요구가 *"AC 근거"* 단일 조건이고 AC-080이 충족). ⚠️ **줄번호 정정 2건: 정본은 `Architecture.md:2726`이며 §27.3의 `:2718`도 planner 인계값 `:2720`도 둘 다 틀렸다**(양쪽 인계값이 모두 어긋난 사례). ⑦ **갭 G135~G138** · 프로브 **P-3** + if/then 강등표 6행 · 커밋 배치 5단 · **OQ-A20 1건** · **AC 신설 0건 · 번호 예약 0건 · ADR 0건 · 소스 0줄.** ✅ `API.md`·`Database.md`는 이번에 갱신했다. | **(a) ⭐ 이 판정은 §27.3을 뒤집지 않는다 — 사유를 한 칸 아래로 옮긴다.** §27.3은 *"위험하다"* 였고 이 결정은 ***"불가능하다"*** 다. T121 범위에서 만들지 않는다는 판정은 **그 태스크 안에서는 여전히 옳고**(만드는 것은 T123이다), §27.3의 3중 사유 중 **1(AC 근거 부재)은 AC-080이, 2(R6)는 §31.1이, 3(OQ-A19)은 §31.4가** 각각 해소했다. **(b) ⭐ 가장 중요한 함정 — G135**: `resolveAnchor` 규칙 1(`anchorScammerTurn <= 0`)은 **`{ anchorTurnIndex: -1, anchorResolved: true }`** 를 돌려준다(`smsTimeline.ts:108-110`). `resolveMockScreenAnchor`에는 이 경로가 **없어**(항상 실제 메시지 turnIndex) **통화 경로에만 있는 함정**이며, `anchorResolved`만 보고 승격하면 turnIndex −1에는 메시지가 없어 `getAnnotatedTurnIndexes`↔`deceivedMoments` **1:1 전제가 깨지고 되감기가 엉뚱한 순간을 연다**(§15.6 G16 재발 = **AC-062 위반**). ⇒ 승격 조건은 **`anchorResolved === true && anchorTurnIndex >= 0`**. 현행 카탈로그로 도달 가능한지와 **무관하게** 방어한다. **(c) G22는 뒤집히지 않는다 — 예외가 조건과 함께 명문화된다**: G22의 금지는 `sms_received`·`opened`·`otp_shown`·**`link_tapped`** 4종에 **그대로 유효**하고 **AC-080 (b)** 가 그것을 *"협상 대상이 아니다"* 로 재확인한다(`PRD.md:582`). 승격되는 것은 **제출 하나**이며 G22가 든 사유(*"AC 근거 없이 판정을 바꾸면 AC-062/068/010/011이 연쇄로 흔들린다"*)는 **AC 근거가 생겨 해소**됐다. **(d) ⚠️ 정직 고지 — 연쇄는 실제로 일어나며 그것이 설계다**: `wasDeceived`는 병합 후 배열로 **재계산**되므로(`generateReportCore.ts:200`) 제출 승격은 `wasDeceived`를 뒤집고 **방어등급(AC-010/011, `:272-274`)·실패 아카이브(AC-068)** 에 반영된다. **T84의 `consented` 승격이 이미 만든 것과 글자 그대로 같은 형태**다. ⛔ **완료 보고에 *"판정에 영향이 없다"* 라고 쓰지 말 것** — 그 문장은 §27.3 (3)의 *"AC-062 불변식은 손상되지 않는다"* 를 **T123에 그대로 옮겨 적는 오류**가 된다(그 문장은 *"승격 0건"* 을 전제로만 참이었다). **(e) planner 실측 1건 정정**: *"현행 `link_tapped`도 `InCallSmsOverlay.tsx`가 기록한다"* 는 **한 단계 부정확하다** — 오버레이는 콜백 prop을 올릴 뿐이고(`InCallSmsOverlay.tsx:52-53`·`:76`) 콜러블을 부르는 것은 **페이지**다(`play/page.tsx:1473`). 패턴은 *"부모가 기록"* 이 아니라 ***"잎은 콜백, 페이지가 기록"*** 이며 이번 설계는 **3단 위임**이다. **(f) ⭐ planner 인계 — T123 선행 ③이 실측과 어긋난다(차단 아님)**: *"T121 병합"* 이 선행으로 적혀 있으나 `credential-form` 랜딩은 **이미 4종**이 카탈로그에 있고(`scenarios/mockScreens.ts:152`·`:173`·`:195`·`:217`) 그중 **3종이 `entrySurface:"in-call-sms"`** 다 ⇒ **T121 없이 착수·완료 검증이 가능**하다. T121이 병합되면 6번째 항목이 **같은 경로를 자동으로 탄다**(카탈로그 전수 순회). ⛔ **architect는 `docs/Tasks.md`를 고치지 않았다.** **(g) 범위는 architect가 정하지 않았다 — OQ-A20**: AC-080 문면은 표면을 한정하지 않고 두 표면 모두에 랜딩이 실재하므로 **경로 A만 구현하면 AC-080이 절반만 충족**된다. architect 의견은 *"둘 다가 문면에 맞고 증분도 작다"* 이나 **범위는 planner·사용자 확정 사항**이며, 그래서 **배치 2a/2b를 미리 갈라** 잘라낼 수 있게 했다. **(h) 감수하는 한계(⛔ 보고 금지 문장 포함)**: 소스 스캔 게이트는 **스캔 대상 파일 집합이 고정이라 파일 분할로 무력화된다**(`callContinuity.test.ts:223-224` 원문), **T116 미해결**이라 *"참가자가 실제로 그 화면을 보고 제출했다"* 는 렌더 층에서 여전히 미검증이다 ⇒ ⛔ ***"이제 실제로 걸리는지 완전히 판정할 수 있다"* 라고 일반화하지 말 것.** 그리고 메신저 표면에서는 **제출했는데 `MockScreenTimelineEntry.consented`가 false**인 항목이 생긴다(그 필드는 *"권한 허용"* 만 뜻한다) — 스키마를 늘리지 않았으므로(§18.1 죽은 필드) **표시 카피 판정은 ux-design 인계**다. **(i) architect는 셸이 없다** — 테스트·게이트·라이브·`git log`를 **한 건도 실행하지 않았다**(525/216은 **인용값**, 착수 시 **P-3**로 재측정). base 검증만 실측이다: `C:\codegate\.git\refs\heads\main` = **`e75237a07c46521291607d9668b02568d7ebd8e4`**(직접 판독, 지정 base `e75237a`와 일치). 깨질 것으로 **예상되는** 단언 목록(§31.7 (1))은 **정적 판정**이며 확정은 implementer의 실행 출력이다. | — (ADR 신설 없음 — **ADR-0007(통화 중 문자 전달 모델)·ADR-0012(entrySurface 게이트 키잉)의 하위호환 증분**이고, 제품 구조·데이터 계약의 **형태**를 바꾸지 않는다(컬렉션 0·콜러블 0·문서 종류 0). 되돌리는 비용이 **enum 값 2개 + 옵셔널 필드 2개 + 함수 1개**다. DECISIONS #48·#50·#51·#54~#61의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승. ⚠️ **단 판정 층 파급은 ADR급이 아니라고 보는 근거를 명시한다**: `deceivedMoments` 승격 자체는 **T84(ADR 없이 DECISIONS #42로 결정)** 가 이미 세운 메커니즘이고 이번엔 **입력 조건 하나가 느는 것**이다) |
| 64 | 2026-07-28 | **(T116 착수 게이트) ⭐ 랜딩 렌더 층 검증은 "신규 의존성 0건 경로를 먼저 재고, 실행 가능성이 미확인이므로 프로브 P-4 + if/then 강등표로 남긴다" — ⛔ 이 결정은 신규 의존성을 확정하지 않는다.** ① **후보 6종 전수 판정**(§32.1): **A**(러너가 `.tsx` 직접 import + `react-dom/server` 정적 렌더) **1순위** · **A2**(러너 변환 플래그 추가) **조건부 2순위**(기존 24개 전건 통과 출력 + 루트 스위트 시간 before/after가 **하드 조건**) · **B**(`typescript` 인메모리 transpile 하네스) **3순위** — **셋 다 신규 의존성 0건**이다(`react-dom`은 `package.json:20`의 **기존 prod 의존성**, `typescript`는 `package.json:30`의 devDep이며 **테스트가 이미 런타임에 import한다** — `src/lib/sourcescan/scanSource.ts:24`). **D(순수 함수 추출) 기각** · **E(브라우저) 기각** · **F(AST 형상 단언)는 강등 목적지로만** · **C(렌더 테스트 라이브러리) ⛔ 미확정 → OQ-A21**. ② **프로브 P-4**(0단계 · ⓐ→ⓐ′→**ⓑ**→ⓒ→ⓓ) + **if/then 강등표 9행** — T108 P-1·T118 선례 형식. ③ **범위 확정**: `MessengerFakeLanding` **한 컴포넌트** · **도달 가능 랜딩 전수**(대표 1종 축소 **금지**) · **초기 렌더 상태 한정** · **G76·AC-079와 병존**(⛔ 대체 금지). ④ **갭 G139~G145** · **OQ-A21 1건**(조건부). ⑤ **AC 신설 0건 · 번호 예약 0건 · ADR 0건 · 소스 0줄 · `API.md`/`Database.md` 무변경 · `Tasks.md`/`PRD.md`/`UX.md` 무편집.** | **(a) ⭐ 왜 확정이 아니라 "확정 + 프로브"인가**: T116 행이 D항에서 *"타입 스트리핑은 JSX를 변환하지 않는다"* 를 **planner 추정으로 명시**했고 architect도 **셸이 없어 확인하지 못했다.** 이 저장소는 **실행 가능성 미확인 상태의 확정으로 이미 데인 적이 있어**(T108이 P-1을, T118이 같은 형식을 둔 이유) **프로브를 착수 0단계로 두고 강등 규칙을 함께 못 박는 것**이 관례다. **(b) ⭐ 왜 D(순수 함수 추출)를 기각했나 — 이것이 이 판정의 핵심이다**: T116이 지목한 두 버그 형태(**조건부 미렌더 · 순서 뒤바뀜**)는 **JSX 안에 있다**(`MessengerFakeLanding.tsx:295-299` `bodyLines.map` · `:303-319` `fields.map`). 추출 함수는 그 둘을 **그대로 통과시키므로 층이 바뀌지 않고 T116이 미해결로 남는다** — 즉 D는 *"비용이 큰 대안"* 이 아니라 **요구를 충족하지 못하는 대안**이다(부수적으로 기존 게이트 4곳 `mockScreenCopy.test.ts:416`·`:497`·`:530`·`:549`도 깨진다). **(c) ⭐ 비자명한 발견 — A만으로는 역검증이 성립하지 않는다**: T116 F②③이 요구하는 **오염 사본 렌더**를 직접 import로는 할 수 없고, 우회 3안이 각각 **형제 파일 금지(R1 — `mockScreenCopy.test.ts:86-89`)** · **상대 import 해석 붕괴** · **G139(정상/오염 같은 수단 강제)** 에 걸린다 ⇒ **A가 P-4 ⓐ를 통과해도 ⓑ에서 막히면 B로 내려간다.** 그래서 P-4에 ⓑ를 **독립 단계**로 두었다. **(d) 왜 G76·AC-079와 병존인가 — planner 권고는 부차적 근거다**: 결정적 근거는 **신규 렌더 게이트가 `successHeadline`(제출 후에만 렌더 — `:271-273`)과 `consentLabel`(permission phase에만 — `:464`)을 비대상으로 갈라 놓기 때문**이다 ⇒ **교체하면 그 두 필드가 어떤 검사도 타지 않는다.** 합집합이지 교체가 아니다(T108 **N1**과 같은 형식). **(e) 오탐은 "임계 완화"가 아니라 "대상 분리"로 푼다**: 현행 `RENDERED_FIELDS` 7종(`mockScreenCopy.test.ts:264-272`)을 초기 렌더에 그대로 대면 **정상 카탈로그가 즉시 빨간불**이다 ⇒ **도달 상태 등록부(G141)를 필수 산출물로 고정**했다. AC-079 (b)의 옵셔널 제외 규칙과 같은 형식이며 §24.4가 정량 기각한 *"오탐 → 게이트 삭제"* 경로를 미리 막는다. **(f) ⭐ opt-in으로 만들지 않는 이유 — T115 선례를 조건 없이 상속하지 않는다**: T115가 opt-in(`emu:check`)을 고른 근거는 **외부 프로세스 의존**(에뮬레이터 없는 개발자가 막힌다)인데, 이 게이트는 **인메모리라 외부 의존이 0**이다. 조건이 다르면 선례도 다르다. **(g) ⭐ T126/D-61은 T116의 대상이 아니며, 그 이유가 "범위"만이 아니다 — 실측상 D-61은 아직 구현되지 않았다**: `src/app/report/page.tsx:710-719`가 `consented` 한 필드로 이분해 `consented:false`에 **무조건 부정 서술**(*"잘 대응한 지점입니다 — 권한 허용에 응하지 않았습니다"*)을 렌더하고, `:694`의 `ReportMockScreenTimelineItem`이 **`deceivedMoments`를 prop으로 받지 않아** D-61의 3분기가 **구조적으로 불가능**하다. 그리고 그 상태의 항목은 **실제로 생성된다**(`functions/src/report/mockScreenTimeline.ts:169`·`:173-177`·`:185-188`). `docs/Tasks.md`에 **`D-61` 0건 · 최대 태스크 T126** ⇒ **담당 태스크가 없다.** ⇒ **검증 공백이 아니라 미구현이며 별건**이다 — 섞으면 T116의 완료 조건 *"오탐 0건"* 이 구조적으로 성립하지 않는다. ⚠️ **architect는 라이브로 재현하지 못했다**(코드 판독 판정 — 확인 방법은 §32.4 (3)). **(h) ⛔ 신규 의존성을 architect가 확정하지 않은 이유**: `CodingRules.md:6`이 *"No new dependencies without an entry in DECISIONS.md"* 를 규정하지만, **등재 이전에 필요한 것은 "정말 필요한가"의 근거**이고 그 근거는 **P-4 출력 없이는 존재하지 않는다.** 그래서 OQ-A21을 **조건부**로 두고 *"P-4 출력 전에는 올리지 않는다"* 를 함께 못 박았다. **(i) architect는 셸이 없다** — 이 패스에서 **테스트·러너·게이트·`git log` 실행 0건**이고 실측은 **`.git` 판독 · 파일 열람 · grep 셋뿐**이다: `C:\codegate\.git\refs\heads\main` = **`06048cacd01103a2e97993cb75a5d560529e9b01`**(지정 base `06048ca`와 일치). **554/220은 인용값**이며 **P-4 0단계가 before의 정본**이다. **(j) 인계 줄번호 5건이 어긋나 있었다**(T116 행 A항 ↔ 실측): AC-079 `PRD.md:546`→**`:591`** · G76 `:185`→**`:331`** · 대조 지점 `:199`→**`:345`** · 텍스트 읽기 `:13-14`→**`:20-22`** · `codeOnly` `:22`→**`:26-28`**(kind 분기 `:70-83`→**`:78-90`**). **§32.1의 정정표가 정본이고 `docs/Tasks.md` 원문은 고치지 않았다**(planner 소유). **(k) 감수하는 한계**: 이 방식이 닫는 것은 **초기 렌더 한정**이다 — **제출 후 화면·설치 진행 화면은 열린 채 남고**(이벤트 시뮬레이션 = 신규 의존성 = 범위 밖), 따라서 **§31.6 (4)의 한계 문단은 절반만 갱신된다.** ⛔ 완료 보고에 *"랜딩 렌더가 검증됐다"* 로 일반화하지 말 것(**G145**). | — (ADR 신설 없음 — **스택·데이터 계약·안전 경계를 바꾸는 결정이 아니라 검출 층의 수단 선정**이며 **신규 의존성 확정 0건**이다. DECISIONS #50·#51·#54(T108 — 같은 부류인 "검출 방식 선정")의 관례를 그대로 따른다. **C 채택이 필요해지면 그때는 의존성 도입이므로 별도 DECISIONS 행 + ADR 여부 재판정**이 따른다) |
| 65 | 2026-07-28 | **(T125 후속 · §30 검증 절차 정정) ⭐ 라이브 예산 단위를 "회차"에서 "호출"로 바꾸고, §30.4.1 세트를 이틀로 분할한다 — ⛔ §30의 설계 판정(순서 선언 채택 · (C) 보류 · F1/`:147` 교체 대상)은 한 건도 뒤집지 않았다.** ① ⭐ **예산 공식 정본화**: `1회차 = 1(createSession 오프닝) + N(참가자 발화 수 = sendMessage 호출 수)` — 호출원은 직접 판독으로 전수 확인했다(`functions/src/session/index.ts:127` → `roleplay/openingLine.ts:74` · `roleplay/index.ts:261` · `challenge/userAccess.ts:130` · `rewind/index.ts:102` · 리포트는 **0건**, `report/analyzeConversation.ts:5` 주석 원문). ⇒ **§30.4.1 세트 = 9회차 = 26호출**이고 **캡은 일일 20**(`geminiClient.ts:36` `GEMINI_TEXT_MODEL="gemini-3.6-flash"` · 429 원문 **인용값** `GenerateRequestsPerDayPerProjectPerModel-FreeTier … "quotaValue":"20"`) ⇒ ⭐ **하루 완주가 구조적으로 불가능**하다. ② **분할 계획 확정** — **D1: ⓶ 5회차(15) + ⓷ 1회차(2) = 17, 여유 3은 새 회차 금지 / D2: ⓵ base 보강 1회차(3, ⭐ base 문면 트리에서)** + if/then 6행. **G127(*"세트는 같은 날 안에서"*)은 충족 불가능**해져 **대체 규칙**(구간 경계에서만 분할 · 매 회차 모델 id 확인 · 날짜/커밋 SHA/문면 트리 기록)으로 갱신했다. ③ **⓵ 실측 2/3(유효 2/2) 기록**: **P2 = ㈏ 압박 지속 2/2**(3회차는 429 → Mock 강등 = ㈑ 제외). ⛔ **확정 승격 금지 · 비율 표현 금지.** **(C)는 ⓶ 전에 발동 불가(0건).** ④ ⭐ **§30.1.4 행 2·행 4 동시 발동 충돌을 발견해 행 4에 단서**를 붙였다(*"저지가 없어 **재차 고집 대본이 성립하지 않는** 경우"* 에만 발동). ⑤ ⭐ **관측 지점 2분할**(P1 = 필수 기록·전제 검사 입력 / P2 = 성패 판정 대상 정본) — **범주 신설 0건 · 이전 관측 재분류 0건**. ⑥ **⓶ 해석 if/then 7행**(§33.4 (5)) · 갭 **G146~G150** · **OQ-A22 1건**(조건부). ⑦ **AC 신설 0건 · 번호 예약 0건 · ADR 0건 · 소스 0줄 · `API.md`/`Database.md` 무변경 · `Tasks.md`/`PRD.md`/`UX.md` 무편집 · §30 원문 삭제 0건(갱신 고지 6곳 append).** | **(a) ⭐ 오류의 출처를 "틀렸다"가 아니라 "미실행 고지의 귀결"로 적는 이유**: **§30.7 7이 *"architect는 테스트도 라이브도 실행하지 않았다"* 를 미리 문장으로 남겼고**, 45% 계산은 **정확히 그 미실행 영역**(1건=1호출 가정)에서 나왔다. ⇒ **정직한 고지가 있었고 그 대가가 실제로 나타난 첫 사례**이며, 고지는 오류를 막지 못했지만 **출처를 즉시 특정하게 했다**(정정 비용 = 라이브 0건). 같은 계열의 다음 예방책이 **G146**(예산 단위를 호출로 못 박고, 대본을 바꾸면 예산을 재계산하게 한다)이다. **(b) ⭐ 왜 ⓶를 먼저 돌리는가 — 이것이 이 결정의 비자명한 부분이다**: **⓵ 보강은 결론을 바꿀 수 없다.** 유효 2/2(㈏)에서 3회차가 **㈏면 3/3, ㈎면 2/3, ㈐면 2/3** ⇒ **어느 경우에도 §30.1.4 행 2**이고 **㈑면 분모가 안 늘어 미충족 유지**다(경우 전수). ⇒ 보강은 **절차 요건이지 판정 입력이 아니다** ⇒ 뒤로 미뤄도 **판정 손실 0**이고, 대신 **D1 여유 3호출**을 확보한다(⓵을 D1에 끼우면 20/20이라 429 한 번에 세트가 무너진다). **(c) 그런데도 보강을 필수로 남긴 이유**: §30.4.2가 ㈑에 *"회차를 다시 돌린다"* 를 명시했고 §30.1.4 **행 3**이 분모 3을 전제하며, 무엇보다 **행 2의 다음 행동이 *"2026-07-24 사용자 확정 문장(`:37` 계열)을 좁힐 것인가"* 라는 질문**이라 **n=2로 올릴 수 없다.** **(d) ⭐ 왜 (C)를 ⓶ 전에 올리지 않는가 — 근거가 4건이다**: ① 행 2의 다음 행동은 원문부터 *"⛔ 곧바로 구현하지 말 것 — 사용자에게 되돌린다"* 다. ② ⭐ **⓶에서 ㈎ ≥4/5가 나오면 §30.4.3 행 2 = *"(C) 완전 철회 확정"*** 이다 — 즉 **⓶ 결과가 (C)의 필요성 자체를 바꾼다** ⇒ 먼저 올리면 **불필요할 수도 있는 질문으로 사용자 확정을 뒤집자고 하는 것**이 된다. ③ §30.6·§30.8이 *"관측 없이 먼저 묻지 않는다"* 를 이미 못 박았고 지금은 ⓵이 3회가 아니다. ④ (e)의 충돌이 미해소였다. **(e) ⭐ 행 2/행 4 동시 발동 — 표에 없는 형태였다**: 관측이 **P2 ㈏ 2/2(행 2)** 와 **P1 저지 없음 2/2(행 4 = 세트 무효)** 에 동시에 걸렸다. **행 4의 사유 문장 자체**(*"저지가 없으면 '재차 고집' 대본이 성립하지 않는다"*)가 판정을 준다 — **이번엔 참가자가 저지 없이도 재차 고집을 수행해 판정 대상 턴이 실제로 관측됐으므로 대본은 성립했고 사유가 소멸한다** ⇒ 행 4 불발동. ⭐ **이 단서의 순효과는 *"유효 전사 2회차를 폐기하지 않는 것"* 하나뿐**이며 **(C)를 발동시키지 않는다.** 반대 해석(엄격 적용 → 세트 무효)이 옳다고 보면 되돌릴 수 있게 **원문을 그대로 남겼다.** **(f) ⭐ 관측이 설계 전제를 건드리는 방식 — 모순이 아니라 보완, 그리고 사용자 확정 사항 1건**: ⚠️ **base는 "문면 없는 상태"가 아니다** — PR #121의 **무조건형** F1/F2가 이미 들어 있다(`promptAssembly.ts:146-147`). 그 아래에서 **P1 = ㈎(지시대로 이양) · P2 = ㈏(지시를 어기고 OTP 재압박)** 이 나왔다 ⇒ ① **P2의 ㈏는 §30.7 2가 자기 고지한 *"후반부는 `:37` 계열과 여전히 충돌한다"* 의 첫 직접 증거**(보완), ② **P1의 ㈎는 문면 층이 P1에서는 이긴다는 증거**라 §30 전반부의 실현 가능성 근거(보완), ③ ⚠️ **동시에 *"한 번 막아보고"* 가 *있는 만류의 정당화*가 아니라 *없는 만류의 신설*일 수 있음을 시사**한다 — ⛔ **n=2라 설계를 뒤집지 않고 OQ-A22로 사용자에게 남겼다.** **모순 항목은 0건**이다. **(g) ⭐ 왜 P1을 성패 분모에 넣지 않는가(G150)**: 넣으면 §30.4.3의 임계(≥4/5 등)가 **두 지점 혼합 분모**가 되어 §30의 모든 판정을 다시 정해야 하고, 무엇보다 **P1의 ㈎는 채택 설계와 반대 방향**(채택은 P1에서 **만류**를 원한다)이라 **합산하면 부호가 뒤집힌다** — 실제로 *"P1이 이미 ㈎이니 §30.4.3 행 1(표기 정합 강등)에 해당한다"* 는 오독이 가능하고, 그 행의 ㈎는 **P2 기준**이다. **(h) 범주를 늘리지 않은 이유**: 범주 신설은 **이전 관측 전부를 재분류 대상**으로 만든다. 같은 4범주를 **지점별로** 적는 방식은 **재분류 0건**으로 같은 목적을 달성한다. **(i) 감수하는 한계 3건(정직 고지)**: ① ⭐ **분할이 강제되므로 모델 상태 드리프트를 완전히 제거할 수 없다** — 모델 id가 같아도 서버측 변경 관측 수단이 없다 ⇒ ⛔ *"동일 조건에서 비교했다"* 라고 쓰지 말 것(쓸 수 있는 최대치는 *"같은 모델 id·대본·시나리오·난이도에서 이틀에 나눠 관측했다"*). ② **429로 거부된 요청도 20건에 카운트되는지 미확인**(⚠️ 추정 — 보수적으로 포함 가정, 확인 방법은 §33.1 (5) ㉮). ③ **Gemini Live 음성 경로가 별도 쿼터 버킷인지 미확인**(`geminiClient.ts:18-19`가 *"음성 전용 모델과 다르다"* 로 적어 **가능성이 높다는 추정**뿐 — §33.1 (5) ㉯). **(j) architect는 셸이 없다** — 이 패스에서 **테스트·라이브·에뮬레이터·`git log` 실행 0건**이고, **QA의 라이브 관측(전사 원문·회차 수·429 응답 원문·1회차=3호출)은 전부 인용값**이다. 직접 실측한 것은 **`.git` 판독**(`C:\codegate\.git\refs\heads\main` = **`8fba5011a55d79285e11060806c3860b34cd0d02`**, 지정 base `8fba501`과 일치) **· 파일 열람 · grep** 셋뿐이다. **554/220도 인용값.** | — (ADR 신설 없음 — **제품 구조·데이터 계약·안전 경계를 바꾸는 결정이 아니라 검증 절차·측정 단위의 정정**이며 소스 0줄이다. DECISIONS #48·#50·#51·#54~#64의 *"구조 결정이 아니면 ADR을 만들지 않는다"* 관례 계승. **§30의 설계 판정을 뒤집는 결정이 나오면 그때는 별도 행 + ADR 여부 재판정**이 따른다) |
| 66 | 2026-07-28 | **(T128 판정) 토큰 무효화 취급은 "처리 부재"가 아니라 **처리 방식의 결함**이다 — Firebase SDK가 이미 자동 로그아웃하고 있고, 결함은 그 로그아웃이 **훈련 중 화면 트리를 통째로 언마운트한다**는 것과 **`auth/invalid-refresh-token` 계열만 조용히 남는다**는 것 둘이다. 감지는 **콜러블 `unauthenticated` 분기(주) + 훈련 진입 직전 프리플라이트 1회(보조)** 로 하고, **`onIdTokenChanged` 추가는 기각**한다. 처방은 **상태별 7행 if/then**으로 갈라 훈련 밖은 현행 유지(코드 0줄), 훈련·리포트 중은 **리다이렉트·언마운트 금지 + 비차단 배너**, **익명(사용자2)은 재인증 불가라 종료 안내**로 간다.** ⚠️ **행 위치 고지: 이 행은 #65 아래가 아니라 #63 위에 놓였다**(#58과 같은 형태 — **내용·번호는 정확하며 표의 정렬만 어긋난다**. `#` 열이 정본이다). ① **감지 지점 전수 판정 6후보**: ①`onIdTokenChanged` **기각**(검출력 증가 0) · ②`onAuthStateChanged` **현행 유지** · ③콜러블 `unauthenticated` **채택(주)** · ④서버 감지 **기각**(서버는 "죽은 토큰"과 "비로그인"을 구별 못 하고 신호 채널도 그 토큰을 요구) · ⑤프리플라이트 `getIdToken(true)` **채택(보조, 1회)** · ⑥Firestore 권한 오류 **기각**(규칙 위반과 구별 불가 = §24.4 형태). **신규 구독 0건.** ② **처방 표 7행 + 불변식 U1~U5**(배너 중 콜러블 0건 ⇒ **AC-007 불변식 손상 0** · **AC-027과 비충돌**: 금지 대상은 *"이후 단계로 진행"* 이고 진행은 U1로 전부 닫힌다 · **라우트가 바뀌면 즉시 현행 규칙 복귀**). ③ **P1 유지**(성립 시나리오가 운영자 행위 3종으로 좁고, `emulator.ts:5`가 밝히듯 아직 에뮬레이터 단계) **+ P0 상향 근거 1건 병기**(발생 시 훈련 세션 소실 · 익명은 복구 불가). ⛔ **컷 확정은 planner·사용자.** ④ **검증 프로브 P-1/P-2/P-3 + if/then 강등표 6행** — **P-1(에뮬레이터 계정 말소)은 프로덕션 삭제와 같은 코드 경로라 등가**, **P-2(저장 토큰 훼손)는 프로덕션과 다르다고 명시**. ⑤ **갭 G151~G158 · OQ-A23 1건 · AC 신설 0건 · 번호 예약 0건 · 스키마·콜러블·`API.md`·`Database.md` 변경 0건 · 소스 0줄 · `Tasks.md`/`PRD.md`/`UX.md` 무편집 · §0~§33 원문 삭제 0건**(§7에 갱신 고지 1줄 append). | **(a) ⭐ 상류 전제 정정 1건이 이 판정의 출발점이다(직접 실측)**: 인계문의 *"앱이 감지도 처리도 하지 않는다"* 는 **절반만 맞다.** `@firebase/auth` **1.13.3**에서 **`getIdToken()`이 `_logoutIfInvalidated`로 감싸여 있고**(`index-80e3689c.js:1764-1765`,`:1339-1354`), 무효화 판정 집합은 **`auth/user-disabled`·`auth/user-token-expired` 2개**(`:1355-1358`)인데 ⭐ **이 앱에서 성립하는 폐기 시나리오 3종이 전부 그리로 매핑된다** — `USER_NOT_FOUND`→`user-token-expired`·`TOKEN_EXPIRED`→`user-token-expired`(`:819-820`)·`USER_DISABLED`→`user-disabled`(`:962-963`) ⇒ **삭제·비활성·강제 폐기는 이미 자동 signOut되고 `RouteGuard`가 `/login`으로 보낸다**(`RouteGuard.tsx:29-32`,`:42`). **(b) ⭐ 그래서 진짜 결함이 뒤바뀐다**: 자동 로그아웃이 **훈련 중**에 일어나면 `session/play` 트리가 언마운트되는데, **그 파일이 스스로** *"early return·상위 래퍼·`key` 변경·router.push 중 어느 하나라도 하면 … 실시간 세션·마이크·타이머가 끊긴다"* 라고 못 박아 둔 자리다(`session/play/page.tsx:1462-1465`, G10) ⇒ **T128이 *"이렇게 되면 안 된다"* 고 적은 동작이 이미 현행 동작이다.** **(c) T122의 SDK 서술은 정확했다(재확인)**: `ProactiveRefresh.iteration`은 네트워크 오류가 아니면 조용히 `return`(`:1427-1438`)이고 `REFRESH_TOKEN` 문자열은 **파일 전체 0건**이라 `INVALID_REFRESH_TOKEN`은 일반 변환으로 `auth/invalid-refresh-token`이 되어 **무효화 집합 밖**이다(`:965-968`). ⚠️ **다만 삼킴은 signOut보다 뒤에서 일어난다** — `iteration()`이 부르는 것이 `getIdToken(true)`(`:1429`)라서 **무효화 코드는 삼켜지기 전에 이미 로그아웃을 발동시킨다** ⇒ **조용히 남는 것은 (라) 계열뿐**이다. **(d) ⭐ ③을 주경로로 고른 결정적 근거**: `@firebase/functions`의 `ContextProvider.getAuthToken()`이 **토큰 획득 실패를 통째로 삼키고 `undefined`를 반환해 무토큰으로 요청을 보낸다**(`functions/dist/index.cjs.js:342-354`, 주석 원문 *"leave it off."* · `:390`) ⇒ **조용한 실패가 반드시 서버 `unauthenticated`로 귀결된다.** **이 경로를 잡는 후보는 ③뿐**이고, 반대로 ①은 **상태 변화가 0이라 발화 자체가 없다** — 그래서 planner 제안을 기각했다(⛔ *"onIdTokenChanged를 안 걸어서 생긴 문제"* 라는 서술은 틀렸다). **(e) ⭐ 익명 분기는 설계로 복구 불가다**: `signInAnonymously`는 **새 uid**를 만들고(`challenge/join/page.tsx:118`) 소유권은 uid로 걸려 있으며, 세션 포인터는 `sessionStorage`에 살아남지만(`pendingSession.ts:25`,`:45-48`) **새 uid로는 읽히지 않는다.** `/login`은 Google 버튼뿐이라 **막다른 길이자 AC-048 위반**(`PRD.md:147`) ⇒ 처방은 *"재로그인 유도"* 가 아니라 **종료 안내**이고, **참가자에게 무엇을 약속하는지는 제품 판단이라 OQ-A23으로 남겼다.** 선행 실측: ⭐ **`isAnonymous` 저장소 전체 0건** ⇒ 현재 코드는 사용자1/2를 **인증 객체로 구분하지 못한다**(SDK 제공 필드라 신규 필드 0건으로 해결된다). **(f) 이 저장소가 장치를 잃는 3가지 방식에 각각 대응했다**: **오탐**(§24.4) — 판정 소스를 `unauthenticated` **한 코드**로 한정하고 **역검증(정상 구간 배너 0회)** 을 완료 조건으로(G156); **속도**(T113) — 폴링·타이머 **0건**(G157), 추가 네트워크는 프리플라이트 1회이고 토큰이 살아 있으면 캐시 히트(⛔ 그래도 *"성능 영향 0"* 보고 금지 — 미측정); **강제**(T115) — 배너는 비차단이라 **강제를 늘리는 게 아니라 줄인다**(현행이 강제 이탈이다) ⇒ opt-in 스위치 불필요. **(g) 감수하는 한계(정직 고지)**: ⛔ ***"토큰 무효화를 처리하게 됐다"* 로 보고 금지** — 바꾸는 것은 *"무효화됐을 때 참가자가 무엇을 보는가"* 뿐이다; **감지가 수요 기반**이라 아무것도 누르지 않는 참가자에게는 발화하지 않는다; **네트워크 실패가 `unauthenticated`로 오지 않는다는 것은 추정**(P-1/P-2에서 확인); **통화가 유지된다(U4)는 추정**(P-3 미실행); **익명 복구는 이 설계로도 불가**(uid 이관은 ADR-0006 수정 + 새 ADR 선행). **(h) architect는 셸이 없다** — 테스트·빌드·에뮬레이터·라이브·`git log` 실행 **0건**(루트 228 / functions 574는 **인용값**, 착수 시 재측정). ⚠️ **`node_modules`가 워크트리에 없어 메인 체크아웃 `C:\codegate\node_modules`를 직접 열어 읽었다** — SDK 판독은 **인용값이 아니라 실측**이다. base 검증도 실측: `C:\codegate\.git\refs\heads\main` = **`21d4d6c17b75296e756e0bd26f419b9b1359a41b`** · 워크트리 HEAD **동일**(지정 base `21d4d6c`와 일치). | — (ADR 신설 없음 — **인증 프로바이더·데이터 귀속·Firestore 규칙·콜러블 계약을 하나도 바꾸지 않는다**(§7 표 무변경). 되돌리는 비용이 **파일 2개 + 분기 2곳**이다. DECISIONS #48·#50·#51·#54~#65의 *"구조 결정이 아니면 ADR을 만들지 않는다"* 관례 계승. ⚠️ **OQ-A23에서 uid 이관이나 "리포트 없는 결과 화면"이 채택되면 데이터 귀속 경계가 움직이므로 그때는 ADR이 필요하다** — §34.10 (f)) |
| 63 | 2026-07-28 | **(T123 후속 · 문서 정합 패스) ⭐ OQ-A20을 close한다 — 답은 "둘 다"(통화 + 메신저 두 표면)이며 architect가 답한 것이 아니라 사용자가 2026-07-28에 확정하고 두 표면이 모두 구현·리뷰·QA·병합된 사실을 등재하는 것이다.** ① **close 근거 3중**: (i) **사용자 확정** *"둘 다 진행해"*(원문은 오케스트레이터 전달 — `docs/PRD.md:8`), (ii) **planner 등재** `docs/PRD.md` **v1.10 → v1.11**(사실 등재 1건 · **AC 신설 0건 · AC-080 문면 수정 0건 · MVP Scope 행 신설 0건** — `PRD.md:4`·`:7`·`:11`), (iii) **집행 완료** T123 PR **#130** · 병합 커밋 **`597bc8d`**(`docs/Tasks.md:446`). ② **close 처리한 자리 6곳 전부 갱신 고지**(⛔ **원문 삭제 0건**): §31.8 표·절 제목 · §31.9 *"사용자 판단 대기 — 1건"* · 배치 2b · §31.7 (3) 행 6 · §31.4 표 · §31.9 planner ⓑ. 상세는 **§31.10**. ③ ⭐ **§31.7 (1)의 정적 판정이 실행으로 확정됐다** — *"틀렸다"* 가 아니다: 표 5행 중 *"깨진다"* 예상은 **0건**이었고(**"안 깨진다" 2행 + "미확인" 3행**) **실행 결과 깨진 단언 0건 · `mockScreenCopy.test.ts` 재작성 0건**이며 §31.7 (3)에서 발동한 것은 **행 1(강등 없음)** 뿐이다. ⚠️ **인계문의 *"깨질 것으로 본 5건"* 은 범주 혼합**이라 §31.10 (2)에서 정정했다. ④ ⭐ **G135 함정은 실재했다** — implementer 역검증 **인용값** *"`anchorResolved`만 봤을 때 승격됐을 건수=1 / 실제 승격=0"*. **DECISIONS #62 (b)가 *"도달 가능성과 무관하게 방어한다"* 로 요구한 조건이 실제로 1건을 막았다.** ⇒ 이후 이 조건을 *"불필요한 방어"* 로 완화하려는 시도는 **이 행을 근거로 반려**한다. ⑤ **AC-045가 실행으로 뒷받침됐다**(QA **인용값**): **타입 우회 raw HTTP**로 계좌번호·예금주명을 실어 보내도 **저장 문서에 그 필드가 없다** ⇒ §31.2의 구조적 금지 판정이 **서버 기록 층에서도** 성립. ⑥ ⛔ **미측정 3건은 그대로 남는다** — **AC-007·AC-063은 구조 검토 근거**이고(실행 검증 아님) **세션 종단**(제출 → 리포트 생성 → 재호출 불변)은 **예산 보호로 실행하지 않았다.** ⛔ *"검증됐다"* 로 옮겨 적지 말 것. ⑦ **AC 신설 0건 · 번호 예약 0건 · 신규 OQ 0건 · 신규 갭 0건 · ADR 0건 · 소스 0줄 · `API.md`/`Database.md` 무변경.** | **(a) ⭐ 왜 이 패스가 필요했나(이것이 결정의 본체다)**: 확정·구현·병합·PRD 등재가 **모두 끝났는데 `docs/Architecture.md`의 OQ-A20만 *"사용자 판단 대기"* 로 열려 있었다** ⇒ **다음 사람이 설계 문서만 보면 "미확정"으로 읽혀 드리프트 원천이 된다**(T123 reviewer **Major 2**). 이 저장소는 문서 간 상태 불일치로 반복 사고를 냈고, **닫는 비용이 6곳 append로 끝나는데 방치 비용은 재논의 1회**다. **(b) 왜 원문을 지우지 않는가**: 등재 시점 문장들은 **그때는 정확했다**(architect는 실제로 답하지 않았고 범위는 실제로 사용자 소관이었다). 지우면 *"애초에 열린 적이 없었던 것"* 처럼 보여 **판단 이력이 사라진다** — 이 저장소의 갱신 고지 관례(§27.3·§28.2·§30 정정 선례)를 그대로 따른다. **(c) ⭐ architect 의견과 사용자 확정이 일치했다는 사실 자체는 근거가 아니다**: §31.8은 *"둘 다가 문면에 맞고 증분도 작다"* 는 **의견**을 적으면서도 **범위를 planner·사용자 소관으로 넘기고 배치 2a/2b를 미리 갈라** 잘라낼 수 있게 했다. 결과적으로 자르지 않았지만 **그 분리가 잘못된 준비였던 것은 아니다** — 확정 전에 되돌릴 여지를 남긴 것이 옳았다. **(d) 병합 커밋 정정 1건(직접 실측)**: 인계문은 *"PR #130이 병합됐다(현재 main `ff54be9`)"* 로만 적었으나 **`ff54be9`는 병합 커밋이 아니다.** `.git/logs/refs/heads/main` **140행**이 `021f952 → 597bc8d`를, **141~143행**이 그 뒤 `76402ed → 9417c36 → ff54be9` 세 번의 추가 전진을 남긴다 ⇒ **T123 병합 tip은 `597bc8d`이고 현재 main은 3 pull 앞**이다. **(e) 테스트 수 — 세 시점을 갈라 적었다**: §31 인용 **525/216**(낡음) · P-3 after **534/220**(main `597bc8d`) · 현행 **554/220**(main `9417c36`, T115 포함). ⚠️ **554/220은 현재 base `ff54be9`에서 잰 값이 아니고**(reflog 143행 이후 재측정 기록 없음) **554−534=+20은 T123이 만든 것이 아니다**(T123 순증은 functions +9 / 루트 +4). ⇒ **다음 착수자는 `CLAUDE.md` 원문 명령으로 재측정할 것.** **(f) ⛔ 열지 않은 것 — §31.9 planner ⓒ**: AC-080 (c) *"불변식 보존"* 문면 vs `wasDeceived` 파급은 planner가 *"성격이 달라 등재하지 않았다"* 고 명시했고(`PRD.md:13`) **AC 문면 정합은 planner·사용자 소관**이다. **architect는 OQ로 등재하지도 번호를 예약하지도 않았다**(⛔ 예약 금지 — 소진 사고 4회). **인계로만 남겼다.** **(g) 줄번호 검증 결과 — 이번엔 0건 어긋났다**: planner 인계 6개 줄번호(`:5404`·`:5428`·`:5394`·`:5385`·`:5266`·`:5425`)가 **전부 정확**했다(§31이 등재 후 편집되지 않았기 때문). ⚠️ 다만 **소스 쪽은 밀렸다** — §31.6 G135가 인용한 `smsTimeline.ts:108-110`은 현재 **`:125`** 이고 가드 본체는 **`:262`** 다(§31.10 (3) ②가 그 정정). **(h) architect는 셸이 없다** — 이 패스에서 **테스트·게이트·라이브·`git log`를 한 건도 실행하지 않았다.** 실측한 것은 **`.git` 판독 · 파일 열람 · grep 셋뿐**이다: `C:\codegate\.git\refs\heads\main` = **`ff54be9e4c12c4c3baeaaabb9d329dea3d57857a`**(지정 base와 일치), reflog 139~143행, `functions/src/mockScreens/types.ts:12`, `functions/src/report/smsTimeline.ts:262`(**G135 가드 실재 — reviewer ⓑ 충족**). **534/220·554/220·G135 역검증 출력·QA raw HTTP 결과는 전부 인용값**이다. **(i) 감수하는 한계**: **§31 헤더 버전 갭이 더 벌어졌다**(헤더 PRD v1.7.1·UX 1.13 ↔ 현행 PRD **v1.11**·UX **v1.17** ⇒ 각 **4건** 뒤처짐, 등재 시점 3건에서 증가). **이번에도 전진시키지 않았다** — 헤더 전진은 *"§31 전체를 현행 PRD/UX로 재검증했다"* 는 뜻인데 **이 패스는 상태 드리프트 정리이지 재검증이 아니다.** ⇒ **다음 architect 설계 패스의 선행 항목**으로 남긴다. | — (ADR 신설 없음 — **결정을 새로 내린 것이 아니라 이미 내려진 사용자 확정을 설계 문서에 반영하는 상태 동기화**다. 제품 구조·데이터 계약·안전 게이트의 요구를 하나도 바꾸지 않았고 소스 0줄이다. DECISIONS #48·#50·#51·#54~#62의 "구조 결정이 아니면 ADR을 만들지 않는다" 관례 계승) |
| 67 | 2026-07-28 | **(T129 판정) 한 turnIndex에 랜딩 2건 → D-61 분기 오분류 — ⭐ (a)도 (b)도 채택하지 않고 제3안(지금 고치지 않고 트립와이어 1건)으로 간다.** ① ⭐ **선실측(⓪)**: 승격 순간 타입에 **`landingId`가 없다**(`analyzeConversation.ts:30-38` — 필드 5개뿐) ⇒ **(b)는 표시 층 수정이 아니라 영속 스키마 확장**이다(`ReportDoc.deceivedMoments`는 저장 필드 — `shared/types.ts:405-410`). D-61 implementer의 진술은 **정확했다.** ② **전수 판정 5안**: **(a) 계약+게이트 기각** — 계약 문면(*"같은 turnIndex에 동시 존재 금지"*)은 **turnIndex가 런타임 산물이라 정적 검사 불가**이고, 검사 가능한 것은 **더 넓은 대리 조건**(*"시나리오당 랜딩 ≤1"*)뿐이라 **문면보다 넓은 것을 강제**하게 된다(T113 교훈 정면 — 정당한 다단계 콘텐츠를 미래에 막는다); **(b) `landingId` 인식형 키 — 기각이 아니라 유보**(유일하게 정확하나 영속 스키마 확장 + `UX.md:1700` 금지 ② 정면 + 병합 몇 시간 된 D-61 재작업 + **레거시 리포트는 결국 turnIndex 폴백**); **(b-lite) 개수 기반 기각 — 이득 0**(모호하면 전부 "나"로 떨어뜨리는 것이 **이미 오늘 동작**이라 출력이 한 글자도 안 바뀐다); **(c) ⭐ 채택 — 조건 1에 트립와이어**; **(d) `extractLinkMarker` 1개 제한 기각** — 두 번째 마커를 조용히 버리면 **참가자가 본 칩과 리포트가 어긋난다**(오탐→삭제의 교과서적 형태). ③ ⭐ **결함 성립은 4중 연접**(시나리오당 랜딩 ≥2 → 두 마커가 같은 메시지 → 참가자가 둘 다 열기 → 정확히 한쪽만 승격)이고 **조건 1이 유일하게 기계 관측 가능한 층**이다 — 2~4는 모델·참가자 행동이라 게이트를 걸 층이 아니다(G130 계열). ④ **G159 트립와이어**는 기존 헬퍼 `reachableLandingKeys()`(`mockScreens.test.ts:514`) 재사용 + 순수 헬퍼 1 + 단언 1 + **역검증 1(같은 커밋)**, **테스트 파일 1개만 · 소스 0줄 · 신규 파일 0건 · 기존 단언 삭제 0건**. ⭐ **실패 메시지가 처방을 담는다**(*"콘텐츠를 되돌리지 말고 (b)를 선행 커밋으로"*) — 이것이 (a)와 갈리는 지점이다. ⑤ **발동 if/then 7행이 유일한 정본**(**G160**) · **갭 G159~G161** · **신규 OQ-A24 1건** · **AC 신설 0건 · 번호 예약 0건 · 스키마·콜러블·`API.md`·`Database.md` 변경 0건 · `Tasks.md`/`PRD.md`/`UX.md` 무편집 · §0~§34 원문 삭제 0건**. 상세는 **§35**. | **(a) ⭐ 상류 전제 정정 3건(직접 실측 — 인용값 아님)**: **(정정1)** *"프롬프트가 시나리오당 마커 1종으로 제약한다"*(오케스트레이터 인용값)는 참이나 **근거가 그보다 강하다** — 실제 방어는 **카탈로그 기수**다(`mockScreens.ts:93-110`·`:148-166`·`:250-256` — **시나리오 5종 × 랜딩 각 1건**)이고 **기계 게이트도 이미 있다**(`mockScreens.test.ts:541-550` AC-078 (a) 양방향 집합 일치 + 총계 5). **(정정2)** ⛔ **그러나 그 게이트는 이 결함을 겨냥한 것이 아니다** — `:549`는 **총계**이지 **시나리오별 기수**가 아니라, 기존 시나리오에 두 번째 랜딩을 얹고 **숫자를 6으로 고치면 전부 초록불로 통과한다** ⇒ planner의 **P1(검출 견고성) 판정이 유지된다**(T112·T113과 같은 양식). **(정정3)** ⭐ **reviewer M1의 *"유일성 미증명"* 은 절반이 닫힌다** — **유기적 순간과의 충돌은 구조적으로 불가능**하다: 유기적 순간은 **사용자 턴** 인덱스를 싣고(`analyzeConversation.ts:157-158`) 랜딩 앵커는 **사기범 메시지** 인덱스이며(`mockScreenTimeline.ts:82-84`) `turnIndex`는 **메시지 전역 시퀀스**라 두 메시지가 값을 공유할 수 없다(`roleplay/index.ts:103-110`·`:299`). T83 주석은 **map(길이 불변)**, 미해결 앵커는 **키 비교 전에 "나"로 차단**된다(`mockScreenTimelineCopy.ts:72`) ⇒ **충돌원은 "같은 메시지의 다른 랜딩" 하나로 좁혀진다.** ⚠️ 이것은 *"유일성이 증명됐다"* 가 아니라 **"오늘 코드에서 다른 경로가 발견되지 않았다"**(정적 판독). **(b) ⭐ 왜 지금 발동하지 않는가** — §22.9·§29의 *"측정 전에 복잡도를 늘리지 않는다"* 선례: **오늘 도달 콘텐츠 0건**(4중 연접이 조건 1에서 끊긴다) + **오류 방향이 안전**(거짓 주장이 아니라 **정당한 칭찬 누락**이며 D-61이 고른 안전 폴백과 **같은 방향** — `UX.md:1700`) + **지금 고치는 비용이 영속 스키마 확장**. ⭐ **안 고치는 유일한 실질 비용은 *"콘텐츠가 바뀌면 조용히 깨진다"* 인데 (c)가 정확히 그 "조용히"만 제거한다**(빨간불로 깨지게 만든다). **(c) ⭐ 다중 마커는 우연이 아니라 배선된 능력이다**: `linkMarker.ts:13`(전역 정규식)만이 아니라 `generateReportCore.ts:76-81`이 `landingIds`를 **배열**로 만들고 `mockScreenTimeline.ts:57`·`:83`이 `.includes()`로 받는다 — **세 모듈이 복수를 전제**한다 ⇒ `linkMarker.test.ts:33-42`는 그 배선의 **표현이지 우연한 단언이 아니다.** ⚠️ **정직 고지**: *"다중 마커를 요구한다"* 고 적은 **AC·UX·PRD 조항은 전수 grep 0건**이다 ⇒ 판정은 ***"요구되지 않았으나 배선된 능력"*** 이며 **제거에는 근거가 필요한데 그 근거가 없어** 유지한다. **(d) 이 저장소가 게이트를 잃는 3방식 대조**: **오탐→삭제** — (c)는 조건 1과 **정확히 일치**해 오탐 0((d)는 정반대라 기각); **느려짐→축소** — 정적 계산이라 무관; **강제→끔** — ⭐ **(a)와 (c)가 갈리는 핵심**: (a)는 *"콘텐츠를 되돌려라"* 라 **저작 자유를 영구 제약**하지만 (c)의 빨간불 처방은 *"(b)를 먼저 넣어라"* 라 **게이트가 콘텐츠를 막는 게 아니라 순서를 강제**한다. **(e) ⭐ 역검증 3프로브 + 강등표 5행**(⛔ 실행 가능성 미확인 — T108 P-1·T116 P-4·T128 P-1 선례): **P-3(결함 재현)** 이 핵심으로, `applyMockScreens`+`resolveMockScreenBranch`를 **순수 함수로 직접 호출**해 같은 turnIndex의 랜딩 A(제출)·B(미제출)에서 **B가 "나"로 나오는 것**(정답은 "다")을 보인다 — 이것이 **결함 실재의 유일한 기계적 증거이자 (b) 발동 시의 회귀 기준선**이다. ⭐ **P-3에서 B가 이미 "다"로 나오면 이 절의 전제가 틀린 것이므로 GO 금지·architect 반려**(⛔ *"고칠 게 없었다"* 로 마무리 금지). **(f) 감수하는 한계**: ⛔ ***"오분류를 고쳤다/불가능해졌다"* 로 보고 금지** — 오분류 로직은 **한 글자도 안 바뀐다**(`mockScreenTimelineCopy.ts:73` 무변경); **트립와이어는 조건 1만 본다**(2~4는 어느 층에서도 관측 안 함); **정정3은 정적 판독**(P-3이 사실로 바꾸는 유일한 절차); ⭐ **(b)를 유보한 대가는 실재한다** — 콘텐츠 경로 밖에서 조건 1이 참이 되면 **칭찬은 여전히 누락된다**; **레거시 리포트는 (b)로도 안 고쳐진다**(과거 순간에 `landingId`가 없어 turnIndex 폴백). **(g) OQ-A24가 이 판정을 뒤집는 유일한 입력**: *"한 시나리오에 랜딩 2건 이상을 저작할 계획이 있는가"*(UF-012가 스스로 *"2단계"* 라 부르는 구조 — `mockScreens.ts:90-92`). **저작한다로 확정되면 (c)는 불충분하고 (b)를 선제 발동한다.** ⛔ **콘텐츠 로드맵 판단이라 architect가 정하지 않았다.** **(h) architect는 셸이 없다** — 이 패스에서 **테스트·빌드·라이브·`git log` 실행 0건**(functions 574 / 루트 228은 `CLAUDE.md` 표의 **인용값**, 착수 시 재측정). 실측한 것은 **`.git` 판독·파일 열람·grep 셋뿐**이다: base `C:\codegate\.git\logs\refs\heads\main` **156행** = `21d4d6c → 49f3738` · 워크트리 HEAD **동일** ⇒ **지시문 base `49f3738`과 일치**. ⚠️ **planner가 정정한 `linkMarker.test.ts:33-42`(다중 마커 테스트) 줄번호는 직접 열어 확인했고 정확하다**(reviewer가 적은 `:35`는 문자열 리터럴 줄). | — (ADR 신설 없음 — **스키마·콜러블·Firestore 규칙·안전 계약을 하나도 바꾸지 않고**(§7 표 무변경) **소스 0줄**이며, 되돌리는 비용이 **테스트 블록 1개 삭제**다. DECISIONS #48·#50·#51·#54~#66의 *"구조 결정이 아니면 ADR을 만들지 않는다"* 관례 계승. ⚠️ **§35.6에 따라 (b)가 발동되면 그때는 영속 스키마(`DeceivedMoment`)가 움직이므로 ADR이 필요하다** — §35.10 (f)) |
