# PRD — 안 당해본 사기는 못 막는다 (AI 금융사기 백신)

Owner: planner (see AGENTS.md). Others read-only.
Document Version: v1.1 · Last Updated: 2026-07-23

### 변경 요약 (v1.0 → v1.1)
사용자 결정(2026-07-23) — **수익화 모델 신설 + 무료 티어 챌린지 링크 만료 3일 확정**:
- **수정: 무료 티어 챌린지 "링크" 만료 = 3일**. AC-048(링크 토큰)·AC-049(활성 챌린지 개수)의 "예 7일"을 무료 티어 **3일**로 구체화. ⚠️ **AC-041의 "복제 음성 보존기간"과는 별개 개념** — 음성 보존기간은 이번 지시로 바뀌지 않았고, 챌린지 "링크" 만료 값만 3일로 확정(각각 별도 명시).
- **신규 섹션 "수익화 모델(장기 로드맵)"**: 개인 구독(무료/유료 티어 표) + B2B/B2G 채널. **원칙(최우선): 안전장치는 등급 무관 전 사용자 동일 강제, 유료화는 안전장치를 약화·우회·게이팅하지 않음, 핵심 안전훈련 콘텐츠(기본 시나리오+리플레이 해설)는 무료, 광고 미채택** → **AC-050으로 명문화(안전장치 게이팅 절대 금지)**.
- **구현 시점**: 결제·구독 시스템은 **장기 로드맵**(즉시 구현 대상 아님). **MVP Scope 표에는 미포함**(별도 후속 계획으로 분리). Tasks.md T23~T40 범위 밖.
- **신규 AC-050. 기존 ID 재번호 없음.**

### 변경 요약 (v0.10 → v1.0)
사용자 결정(2026-07-23) — **남은 Open Question 7건(OQ-20·21·23·24·25·26·27) 전부 확정 → 모든 OQ resolved.** 동시에 프로젝트를 해커톤 스코프에서 **장기 프로젝트로 전환**(시간 압박 없음). 이로써 v1.0으로 승격:
- **OQ-20 일정 제약 삭제(장기 전환)**: 기존 "해커톤 Day1/Day2 절대 마감"을 **해당 없음(장기 프로젝트, 시간 압박 없음)**으로 갱신. 태스크는 선행 의존성 순서만 따르고 날짜/스프린트 제약 없음. ⚠️ `docs/DECISIONS.md`의 해커톤 일정 관련 항목 정리는 **architect 위임**(planner 소유 아님).
- **OQ-21 스미싱 링크 = 인앱 가짜 랜딩 목업**: 링크 탭 시 외부 이동 없이 앱 내 가짜 로그인/정보입력 랜딩으로 전환, 입력값은 서버 미전송·UI상 "입력함" 가짜 피드백. 실 브랜드/기관명 없이 무해화. **신규 AC-045.**
- **OQ-23 전이 음성 = 조건부 clone + 목소리 선택 서브플로우**: 메신저 시나리오 선택 시 "통화로 이어질 수 있어 녹음이 필요할 수 있다" 안내 → 즉시 녹음/기존 목소리 재사용/최종 폴백(남·여 기본 보이스 선택). **신규 AC-046.**
- **OQ-24 카카오 상표 완화 = 상시 면책 고지**: "카카오톡 실제 서비스와 무관한 훈련용 재현" 상시 노출. **신규 AC-047.** 마케팅/스토어 노출 제한은 **이번에 채택 안 함**(면책 고지만) — 과잉 반영 금지 위해 명시.
- **OQ-25 복제 음성 = 기간제 보존**: 사용자 설정 기간(예 7/30일, 기본값 architect 확정) 후 자동 삭제 + 수동 삭제 가능. 즉시삭제·무기한 아님. **AC-041 구체화.**
- **OQ-26 링크 토큰 = 단순 토큰+만료+1회성**: 랜덤 토큰·시간 만료(예 7일)·1회 소모, 사용자2 로그인 불필요. **신규 AC-048.** 스키마 상세 architect(T35).
- **OQ-27 오용 방지 = planner 추천안 채택**: 사용자1당 활성 챌린지 개수 제한(예 동시 3개)+만료 + 사용자2 "원치 않는 챌린지" 신고 기능. 관리자 수동 검토 미채택. **신규 AC-049.**
- **신규 AC-045~AC-049, AC-041 구체화. OQ-20~27 전부 resolved(잔여 open OQ 없음).** architect 위임 세부(토큰 스키마·목소리 선택 UX·신고 데이터 모델·보존 기본값)는 각 resolved 셀에 명시. Tasks.md T24~T29·T34~T40 범위·의존성 갱신, 일정 제약 제거.

### 변경 요약 (v0.9.1 → v0.10)
사용자 결정(2026-07-23) — 확정 4건 + 제안 4종 채택. 메신저 확장에 2인 소셜 모델·리플레이 해설을 얹고 트리거·양방향 방침을 재확정:
- **확정1 — 양방향 설계·순차 구현**: 전이 엔진을 방향 무관(direction-agnostic)으로 설계하되 MVP는 메신저→보이스 우선, 보이스→메신저는 fast-follow. **OQ-22 resolved, AC-039 신설, MVP Scope #16 갱신.**
- **확정2 — 트리거 재정의(LLM 판단+구조화 신호+폴백)**: v0.9.1의 "하이브리드(명시+고정 키워드, LLM 제외)"를 뒤집어, **대화 LLM이 의심을 캐릭터로 감지→구조화 신호(function-call/sentinel) 발신→앱이 읽어 전환**. 앱은 자유텍스트 직접 분류 안 함. 폴백(max-turn/명시 버튼). 인젝션 방어 불변. **AC-034 갱신, OQ-16 재확정, Risks 갱신.**
- **확정3 — 사후 리플레이 해설 P0 승격**: 제안 P-1의 리플레이 해설을 MVP(P0)로. 기존 리포트/transcript 재사용. **AC-038 신설, MVP Scope #18, P-1 이관.**
- **확정4 — 2인 소셜 훈련(비동기 챌린지 링크)**: 사용자1이 자기 목소리를 복제해 지인 사용자2에게 딥보이스 체험을 보내는 모델. **4대 안전제약을 AC로 강제**(AC-040 사용자2 사전 동의·AC-041 복제 음성 유출 차단·AC-042 강제 정체 공개·AC-043 결과 열람 제한), **AC-044 비동기 챌린지 흐름**. MVP Scope #19. 구조 상세(음성 스코프·삭제/링크 토큰/오용 방지)는 architect 위임 **OQ-25/26/27 신설**.
- **제안 채택**: P-2(신고 원스톱, MVP 근접)·P-7(결과 카드, MVP 근접)·P-8(내 목소리 금고, MVP 권고·AC-041 직결)·P-9(취약 지점 프로파일, P2) 추가/갱신.
- **신규 AC-038~AC-044, 신규 OQ-25~27. 기존 ID 재번호 없음.** Tasks.md에 리플레이 해설·2인 소셜·양방향 설계·트리거 재정의 태스크 확대/신설.

### 변경 요약 (v0.9 → v0.9.1)
사용자 결정(2026-07-23) — 메신저 확장 핵심 OQ 4건 확정, resolved 처리:
- **OQ-16(에스컬레이션 트리거) = 하이브리드** — 명시 이벤트 전환 + 고정 화이트리스트 의심 키워드. LLM 자유 감지는 MVP 제외. 키워드는 고정 화이트리스트·사용자 입력 지시 취급 금지(AC-024 불변). **AC-034 갱신.**
- **OQ-17(기기 구분) = 자동 감지 + 스킨** — UA로 iOS/삼성 스킨 자동 적용, 감지 실패·데스크톱은 기본 스킨 폴백 + 수동 전환 옵션. **AC-031 갱신.** UA 신뢰도 구현 확정은 architect(T26).
- **OQ-18(카카오 재현) = 정확 모사(accepted risk)** — 사용자가 상표·저작권 리스크 인지 후 채택. UI 외형만 정확 모사, 사기 콘텐츠 무해화(AC-005/AC-032) 불변. Assumptions·Risks·Constraints에 accepted risk 명시. 완화책·ADR 필요 여부는 architect 판단 → **OQ-24 신설.**
- **OQ-19(MVP 범위) = 표면 넓게** — 카카오톡 + 문자(iOS/삼성) 둘 다 + 시나리오 복수 종 + 에스컬레이션 완결. **AC-030 갱신, MVP Scope #14/#17 확대(#17 P1→P0(확장) 상향).**
- **미해결 유지**: OQ-20(일정)·OQ-21(링크 클릭 재현)·OQ-22(역방향)·OQ-23(전이 음성 모드) 및 신규 OQ-24(카카오 ADR·architect 위임). Tasks.md T24/T27/T29 범위를 표면 2종·복수 시나리오·자동 스킨·하이브리드 트리거로 확대.

### 변경 요약 (v0.8 → v0.9)
사용자 결정(2026-07-23) — **메신저피싱 훈련 기능 대규모 확장 기획**. 기존 보이스피싱(실시간 통화) 루프는 무변경으로 유지하고, 그 위에 메신저피싱 훈련과 채널 간 에스컬레이션을 얹는다. 핵심 반영:
- **최상위 훈련 유형을 2분류로 재편**: (A) 보이스피싱, (B) 메신저피싱. **보이스피싱 하위**는 기존 데이터 모델의 `voiceMode`(clone=본인 목소리 복제 / generic=기본 AI 음성)를 그대로 재활용(신규 데이터 모델 아님) — **AC-028**.
- **선택 UX를 단계적/몰입형으로 전환**: 지금의 평면 목록 동시 노출 대신 "유형을 고르면 → 그 유형의 시나리오가 드러나는" 흐름. **화면·전환 연출 상세는 ux-design 소관**이며 PRD는 흐름 요건(AC)만 규정 — **AC-029**(구체 화면은 OQ-U 계열/ux-design 위임).
- **메신저 표면(surface)**: 카카오톡형 + 문자메시지 기본앱형. 문자 표면은 **iOS 기본 메시지 / 삼성 메시지 UI를 구분**해 재현(사용자 기기에 맞는 채팅 UI) — **AC-030/AC-031**.
- **채널 간 에스컬레이션(핵심)**: 메신저 진행 중 사용자가 의심을 표하면 동일 시나리오가 **보이스피싱으로 넘어가(사기범이 전화를 거는 형태)** 이어진다. 메신저 세션과 보이스 세션을 **하나의 연결된 세션**으로 취급(세션·맥락·기록 연속성, 단일 리포트) — **AC-034~AC-037**. 트리거 감지 방식·범위는 **OQ-16** 등에서 확정 필요.
- **메신저 안전 원칙 명문화**: 메신저의 핵심 수법인 "링크 클릭 유도"를 재현하되 **실존·클릭 가능한 악성 URL 절대 금지**, 무해한 표기·비이동·모의 표식으로만 재현 — **AC-032/AC-033**(기존 AC-005·AC-022의 메신저 확장, `functions/src/scenarios/__tests__/scenarios.test.ts`류 금지 패턴 검증을 메신저 콘텐츠로 확장). 프롬프트 인젝션 방어(guardrailPreamble) 구조 유지.
- **추가 기능 제안(planner)** 6종을 신설 섹션 "추가 기능 제안"에 가치/난이도/우선순위와 함께 정리(대부분 P1/P2 로드맵).
- **신규 AC-028~AC-037**, **신규 OQ-16~OQ-23** 추가. 기존 AC/OQ ID는 재번호하지 않음. MVP Scope에 확장 행 #13~#17 추가.
- **주의(스코프·일정)**: 원래 PRD의 "하루 완성(Day1/Day2 해커톤)" 절대 제약이 이 확장에도 적용되는지, 아니면 별도 후속 스프린트인지는 **OQ-20(미확정)**. 확정 전까지 Tasks.md의 확장 태스크(T23~)는 우선순위·의존성만 규정하고 일정은 붙이지 않는다.

### 변경 요약 (v0.7 → v0.8)
사용자 결정(2026-07-22) — 역할극을 실시간 음성 통화로 전환(Phase A):
- **"실시간 통화·STT는 제외" 결정을 뒤집는다.** 기존 Out of Scope 항목(v0.1 이래 유지) "실시간 음성 통화(전화망 연동)"·"실제 STT"를 재검토 후, **STT(브라우저 음성 인식)와 사기범 응답 음성 자동재생은 In Scope로 전환**한다. 단 **"전화망(PSTN) 연동"은 여전히 Out of Scope로 유지** — AC-019("실제 전화망 연동 없이 인앱 재생 전용") 문구·취지는 변경 없음. 즉 바뀐 것은 "텍스트만 vs 음성도"이지, "앱 내부 vs 실제 전화망"은 그대로다.
- **경위**: 사용자가 처음 "훈련인지 사용자가 인지하면 안 된다"(사전고지·동의 자체를 생략)를 요청 → AC-012/017/023(사전고지·동의 게이팅·종료고지) 정면 제거는 동의 없는 심리적 실험에 해당해 **거부**, 절충안으로 "사전 동의는 유지하되 통화 중 몰입은 깨지 않는다"로 합의. 이어서 "전화가 기본 모드, 진짜 음성 대화(마이크 입력)"·"진짜 전화"(재확인 결과 PSTN 아닌 **앱 내 통화형 UI**)로 확정.
- **AC-012/017/023은 무변경** — 사전 고지·명시적 동의·종료 시 "훈련이었습니다" 고지는 그대로 하드 요구사항이다.
- 상세는 `docs/UX.md`(UX-006 갱신)·`docs/Architecture.md`(§ 금지 목록 갱신)·`docs/Tasks.md`(신규 태스크) 참조.

### 변경 요약 (v0.6 → v0.7)
사용자 결정(2026-07-21) 2건 반영:
- **OQ-8 확정(age-gate 최소 연령 = 만 14세).** 근거: 국내 개인정보보호법상 만14세 미만 법정대리인 동의 기준선과 일치하며, 별도 보호자 동의 플로우를 만들지 않을 때 구현이 가장 간단. **AC-014**의 "정확한 연령은 OQ-8"을 "만 14세"로 구체화. OQ-8 Status를 resolved로 전환. age-gate는 여전히 P1(MVP Scope #10, T14) — 값만 확정.
- **OQ-5(게임화 산정식)는 open 유지.** 사용자가 "지금은 간단한 플레이스홀더로 진행"이라고만 했고 최종 확정 아님. Decision란에 "임시 플레이스홀더 구현 진행 중(T13), 최종 산정식은 여전히 미정" 한 줄만 추가(Status 변경 없음).

### 변경 요약 (v0.5 → v0.6)
음성 목업 임시 도입 결정(2026-07-21). **AC·MVP Scope·차별점 변경 없음 — Risks 표에 1행 추가만 한 문서(다운스트림 플로우/설계 재검증 불필요).**
- 배경: ElevenLabs API 키가 아직 준비 안 됨(사용자가 OQ-14 크레딧 확인 미완료). 다른 트랙이 막히지 않도록 **음성 부분을 임시 목업(Mock AI 음성)으로 대체**해 개발 진행하기로 결정.
- **핵심 컨셉("본인 목소리 클론")은 절대 포기하지 않음.** 목업은 개발 편의용 임시 대체일 뿐이며, **최종 데모는 반드시 사용자 본인 목소리를 ElevenLabs로 실제 클론해 재생**한다(차별점 1번·AC-018/AC-019 변경 없음).
- **Risks 신규 행 추가**: "목업이 실수로 최종 데모까지 남을 위험 — 핵심 차별점 무효화." Mitigation은 T16(데모 안정화) 체크리스트에 "실클론 교체 확인" 필수 항목화 등(Risks 표 참조).
- docs/Tasks.md: T1(실클론 PoC)에 "API 키 준비 전 착수 보류" 명시, 신규 **T19(음성 프로바이더 인터페이스 + 목업)** 추가, T4 의존성을 목업 우선→실클론 교체 흐름으로 갱신, OQ-14 게이팅 갱신.

### 변경 요약 (v0.4 → v0.5)
ux-design 정합화(2026-07-21): 사용자 결정으로 **로그인(계정 생성·인증)이 P0로 상향**됐다(OQ-U2 resolved — "로그인 있이 가자", docs/UX.md v1.1 UX-013). UX는 온보딩(UF-001) 최초 진입점에 로그인/가입 화면을 P0로 두어 "인증 없이는 온보딩 자체가 시작되지 않는다". PRD v0.4는 계정(AC-016)을 P1로 두고 있었으므로 다음을 정합화한다:
- **로그인=P0 / 히스토리 열람=P1 분리를 명확화.** 이번에 P0로 오르는 것은 "로그인 자체(계정 생성·인증·게이팅)"이며, "세션/리포트 히스토리 열람"은 여전히 P1이다. 전자는 신규 **AC-027**로, 후자는 기존 AC-016으로 각각 관리한다(AC-016은 재번호하지 않음).
- **MVP Scope에 로그인/가입(P0) 신규 행 #12 추가**(AC-027). 기존 #11(AC-016)은 "히스토리 열람"만 P1로 남기고, 계정 생성·로그인 부분이 #12/AC-027로 분리됐음을 명기. 기존 #1~#11의 # 번호와 AC ID는 그대로 유지(UX/Tasks 참조 무결성 보존).
- **AC-027 신규 추가**: 온보딩(사전 고지·동의) 전 Firebase Auth 인증 필수, 미인증 시 진행 불가, 이후 데이터는 본인 계정(uid) 귀속.
- **Risks에 신규 행 추가**: 로그인 P0 상향으로 Day1 오전에 P0 UI 태스크 1개(로그인 화면·게이팅)가 추가되어 일정 압박이 소폭 증가.
- 기술 스택은 원래부터 Firebase Auth 포함(Constraints)이라 새 인프라는 아니지만, "로그인 화면이 P0 온보딩 필수 첫 단계"라는 점을 반영.
- **인증 프로바이더 방식(이메일/구글 소셜/전화번호 등)은 architect 소관**(docs/UX.md OQ-U5). PRD로 옮겨 적지 않음(문서 소유권).
- docs/Tasks.md: 로그인/가입 화면·인증 게이팅을 신규 **T18(P0, Day1 오전)**로 추가하고 일정 영향(P0 태스크 T1~T11 → +T18)을 Task Planning에 명시.

### 변경 요약 (v0.3 → v0.4)
사용자 결정(2026-07-21): **차별점 2번 "가족 원격 훈련"을 발표(피칭) 내러티브에서는 비중 있게 강조하되, Day1 실제 빌드 범위는 그대로(본인 온보딩만) 유지**한다. 핵심은 "발표 내러티브 강조"와 "빌드 스코프"를 명시적으로 분리한 것.
- **차별점 2번 서술**에 "발표 내러티브 강조 확정(2026-07-21), 단 Day1 빌드 범위는 미확장(본인 온보딩만)"을 명기. OQ-3·OQ-15는 빌드 스코프 관점에서 여전히 open.
- **Risks에 신규 행 추가**: 발표 스토리(가족 원격 훈련) vs 실제 Day1 데모/빌드 범위(본인 온보딩만) 괴리로 인한 오버클레임 리스크 + Mitigation(발표팀 가이드라인).
- **OQ-3·OQ-15**: Status는 여전히 open 유지(빌드 스코프 미변경). Decision 란에 "발표 내러티브 강조는 확정했으나 빌드 스코프 결정과는 별개"라는 구분 한 줄만 추가.
- docs/Tasks.md는 태스크/구조 변경 없음(스코프 미확장). T9 설명에 발표 vs 구현 분리 명확화 한 줄 보강.

### 변경 요약 (v0.2 → v0.3)
사용자가 open이던 Open Question 2건을 결정하여 반영:
- **OQ-13 확정**(가족 원격 훈련 vs 본인 동의 가드레일 충돌): "본인이 직접 등록(녹음)하고, 자녀는 계정 생성 UI만 보조한다. 실제 녹음 버튼은 부모 본인이 누른다"로 확정(기존 제안 안전 기본값 채택). 차별점 2번·Target Users 어르신 행·Assumptions에 반영.
- **OQ-13 확정에서 논리적으로 따라 나오는 OQ-11 resolved**: 동의 주체 = 본인(등록 대상자), 데이터 소유권 = 본인 계정 소유로 확정.
- **OQ-3는 open 유지**: 녹음이 본인 직접임은 확정됐으나, 자녀 보조 UI의 구체 조작 권한 범위·MVP 실제 포함 여부는 미확정.
- **OQ-14는 open 유지(Decision 갱신)**: "T1 착수 전 사용자가 직접 크레딧/rate limit 확인 후 진행"으로 확인 **절차**는 확정. 단 구체 상한액 수치 자체는 여전히 미정이라 open 유지.
- **신규 OQ-15 추가**: 부모 리포트를 자녀가 열람할 수 있는지(차별점 2번 서술 종속) 미확정.

### 변경 요약 (v0.1 → v0.2)
사용자 확정안으로 전면 갱신. 핵심 변화:
- **핵심 진화**: "본인 목소리 AI 클론"을 제품의 중심으로 격상. 사용자가 30초 녹음 → 그 목소리로 가족 사칭 딥보이스 전화를 앱 내에서 재현하여 위협을 신체적으로 자각시킨다.
- **스코프 축소(하루 완성)**: 3개 시나리오 → 한국어 "가족 납치" 1종 집중(여유 시 "기관사칭" 1종). 로맨스스캠 제외.
- **채널/음성/언어 확정**: 웹(Next.js), 실시간 음성 통화형 역할극(브라우저 STT 입력 + 클론 오디오 자동재생, v0.8 갱신 — 텍스트 입력은 폴백으로 유지) + 클론 오디오 재생. 실제 전화망(PSTN) 연동은 아님(AC-019 무변경). 한국어/한국.
- **보안·윤리 가드레일 5종을 검증 가능한 AC로 명문화**(보안 배점 20% 대응).
- **일정을 절대 제약으로 반영**: Day1 하루 개발 완료, Day2 오전 11시 최종 마감.
- Open Questions 12개 중 답이 나온 6개 resolved 처리, 신규 2개(OQ-13 가족 원격 훈련 vs 본인 동의 충돌, OQ-14 총 예산) 추가.
- 신규 AC-018 ~ AC-026 추가(음성 클론·딥보이스 재생·가드레일 5종·리포트 타임라인·사칭 이미지). 기존 AC ID는 재번호하지 않음.

## Goal
사용자 **본인의 목소리를 AI로 복제**해, "내 목소리로 가족에게 사기 전화가 가면?"을 직접 체험시키는 AI 금융사기 백신을 만든다. 사용자가 30초 목소리를 녹음하면 AI가 그 목소리로 가족 사칭 사기 전화를 앱 내 오디오로 재현하고("이렇게 감쪽같이 당한다"를 체감), AI 사기범과의 실시간 역할극 후 "어디서·언제 속았는지"를 짚는 취약점 리포트와 방어 훈련(게임화)을 제공한다. 약화된 실전 공격에 미리 노출시키는 예방접종(inoculation)형 서비스다.

**(v0.9 확장)** 훈련 범위를 보이스피싱에서 **메신저피싱(카카오톡형·문자메시지형)까지** 넓히고, 실제 사기의 전형인 **채널 간 에스컬레이션**(문자·메신저로 접근 → 의심하면 전화로 넘어옴)을 하나의 연결된 시나리오로 재현한다. 사용자는 유형을 고르면 그 유형의 시나리오가 드러나는 몰입형 흐름으로 훈련한다. 상세 요건은 MVP Scope #13~#17·AC-028~AC-037 참조.

**(v0.10 확장)** 여기에 (1) **사후 리플레이 해설**(끝나고 대화를 되짚으며 "여기 이 말이 사기 신호였어요"를 짚어 주는 교육 단계, AC-038), (2) **2인 소셜 훈련**(사용자1이 자기 목소리를 복제해 지인 사용자2에게 딥보이스 사기를 안전하게 체험시키는 비동기 챌린지 모델 — 딥보이스 사기의 실제 작동을 정면으로 체감시키는 핵심 흐름, AC-040~044)을 더한다. 2인 모델은 사용자2 사전 동의·복제 음성 유출 차단·강제 정체 공개·결과 열람 제한의 **4대 안전제약이 출시 전제조건**이다. 상세는 MVP Scope #18~#19·AC-038~AC-044 참조.

### 해결하려는 문제
- 기존 안티사기 서비스는 탐지·차단(사후) 중심이거나, 고정 시나리오 재연 교육에 그친다. 사람들은 사기의 존재를 지식으로는 알아도, 실제 상황의 심리적 압박(권위·긴급·공포·가족애)을 겪어본 적이 없어 막상 당한다.
- 격차는 "지식"이 아니라 "경험·자각"에 있다. 특히 **본인 목소리가 얼마나 쉽게 탈취·악용되는지**를 신체적으로 자각할 방법이 없다.

### 근거 데이터 (조사 기준일 2026-07-21)
- 부산 60대 딥보이스 가족사칭 실사례, 2천만원 송금 피해.
- 2026년 1~5월 보이스피싱 피해액 2,564억원(전년 동기 대비 +50%).
- 목소리 탈취가 "여보세요" 한마디로도 가능할 만큼 쉬움. 경찰청 딥페이크 "자녀 납치" 사기 주의보.
- 기존 예방 대책은 "가족 인증 키워드 정하기" 등 수동 교육뿐.

### 차별점 (참신점 3가지)
1. **본인 목소리 복제**로 위협을 신체적으로 자각(경쟁사는 고정 시나리오/타인 음성).
2. **가족 원격 훈련** — 자녀가 부모의 계정 생성 UI를 보조하고, 목소리 등록(녹음)은 반드시 부모 본인이 직접 수행(안전 기본값 확정, OQ-13 resolved → AC-020 정합). 부모가 안전하게 당해보고 리포트를 남긴다(리포트를 자녀가 열람 가능한지는 OQ-15 미확정). 자녀 보조 UI의 구체 권한 범위·MVP 실제 포함 여부는 OQ-3.
   - **발표 내러티브 강조 확정(2026-07-21), 단 Day1 빌드 범위는 미확장(본인 온보딩만).** 발표(피칭)에서는 이 "가족 원격 훈련"을 비중 있게 내세우기로 확정했으나, Day1 실제 빌드 범위는 지금 그대로 유지한다: 본인 온보딩만 구현하고, 자녀는 계정 생성 UI 보조 수준까지만(로드맵 방향성). **OQ-3(자녀 보조 UI의 MVP 실제 포함 여부)·OQ-15(자녀 리포트 열람 가능 여부)는 빌드 스코프 관점에서는 여전히 open이다.** 발표에서 강조한다는 방침과 빌드 스코프 결정은 별개 사안임(발표팀 가이드라인은 Risks 표 참조).
3. **게임화** — 사기 방어 등급, 신종 수법 자동 업데이트(구독 확장 방향).

경쟁사 대비: 국내 은행(우리 '하마터면', 신한 '보이스피싱제로')·해외 B2B(Brightside/Hoxhunt/Wizer/Doppel)·통신사앱(시티즌코난/익시오)은 고정 재연·기업 직원 대상·탐지 차단(사후)에 머문다. 우리는 개인·가족(어르신) B2C + 한국어 + 본인 목소리 감정 체험 + 사전 예방접종.

## Target Users
공통 핵심 문제: "사기를 지식으로는 알지만 실제로 당해본 적이 없어 막상 당한다."

| 유형 | 주 표적 사기 | 핵심 니즈 / 차이점 |
|---|---|---|
| 어르신 (자녀가 결제·설정 보조) | 딥보이스 가족 사칭 | 단순 UI·큰 글씨, 낮은 강도부터, 심리적 충격 최소화(완화 톤·쉬운 종료·종료 후 안심). 자녀 대리설정 니즈 → **목소리 녹음은 본인(어르신)이 직접, 자녀는 계정 생성 UI만 보조**(OQ-13 확정, AC-020 정합). 자녀 보조 UI 권한 범위는 OQ-3. |
| 사회초년생 | 취업/투자·기관 사칭 | 디지털 친숙, 몰입감·게임화에 반응, 자기 주도 훈련. |
| 기업 보안교육 (B2B, 확장 방향) | 사회공학·업무 사칭 | 조직 단위 이수·통계. **이번 스코프 제외**, 시장성 로드맵으로만 언급. |

지불주체(시장성 35% 스토리): 통신사·은행(고객보호 KPI)·기업 CISO·지자체 어르신 교육 예산. 확장 방향: "신종 수법 자동 업데이트 구독."

## Priority 기준
| 등급 | 의미 |
|---|---|
| P0 | 하루 완성 필수. 데모 승부처 핵심 루프 + 보안/윤리 필수. 없으면 제품이 성립하지 않음. |
| P1 | 여유 시 구현. 가치·리텐션에 중요하나 핵심 루프는 없이도 동작. |
| P2 | 로드맵(이번 데모 안 함, 발표에서 말로만 언급). Out of Scope. |

## MVP Scope
하루 완성 기준. Timeline은 목표 완료 시점(Day1 오전/오후/야간, Day2 오전). P2는 Out of Scope 참조.

| # | Feature | Priority | Timeline | Acceptance Criteria |
|---|---|---|---|---|
| 1 | 온보딩 + 명시적 동의 게이팅 + 본인 목소리 등록(30초 대본 녹음) | P0 | Day1 오전~오후 | AC-017, AC-012, AC-020 |
| 2 | 30초 목소리 클론 + 딥보이스 가족사칭 오디오 인앱 재생 | P0 | Day1 오후 | AC-018, AC-019, AC-022 |
| 3 | 시나리오 콘텐츠(한국어 "가족 납치" 1종, 여유 시 "기관사칭" 1종) | P0 | Day1 오후 | AC-001, AC-002 |
| 4 | AI 사기범 역할극 엔진(텍스트 채팅, 인격 유지·약화 수법·악용 거부·실시간 적응) | P0 | Day1 야간 | AC-003, AC-004, AC-005, AC-013, AC-024 |
| 5 | 세션 라이프사이클 + 상시 안전 종료 + 디스컬레이션 + "훈련이었습니다" 고지 | P0 | Day1 야간 | AC-006, AC-007, AC-015, AC-023 |
| 6 | 취약점 리포트(속은 시점 타임라인·놓친 위험 신호·올바른 대처법) | P0 | Day1 야간 | AC-008, AC-009, AC-026 |
| 7 | 보안 가드레일 — 생성물 즉시 폐기(삭제 로그·서버 미저장) | P0 | Day1 야간 | AC-021 |
| 8 | 사칭 이미지 1종(가짜 송금완료 화면 등) | P1 | Day1 야간(여유 시) | AC-025 |
| 9 | 게임화 — 사기 방어 등급/레벨(기본형) | P1 | Day2 오전(여유 시) | AC-010, AC-011 |
| 10 | 연령 확인(age-gate) | P1 | 여유 시 | AC-014 |
| 11 | 세션/리포트 **히스토리 열람** (계정 생성·로그인 부분은 #12/AC-027로 P0 분리됨) | P1 | 여유 시 | AC-016 |
| 12 | **로그인/가입 (Firebase Auth 계정 생성·인증)** — 온보딩 최초 진입점. 인증 없이는 동의·녹음·세션·리포트로 진행 불가(사용자 결정으로 P0 상향, OQ-U2 resolved) | P0 | Day1 오전 (온보딩 진입점) | AC-027 |

### 메신저피싱 확장 (v0.9)
> 아래는 메신저피싱 기능 확장 스코프다. 우선순위 P0(확장)=이 확장의 핵심 루프(없으면 확장이 성립 안 함), P1(확장)=가치 높으나 핵심 루프는 없이도 동작. **일정 제약 없음(OQ-20 확정, 장기 프로젝트 전환)** — Timeline 열은 날짜가 아니라 선행 의존성(선행 태스크/OQ 완료)만 표시한다.

| # | Feature | Priority | Timeline | Acceptance Criteria |
|---|---|---|---|---|
| 13 | 최상위 2분류(보이스피싱/메신저피싱) + 보이스 하위(clone/generic) + **단계적/몰입형 시나리오 선택 흐름** | P0(확장) | 선행 의존성만(일정 제약 없음) | AC-028, AC-029 |
| 14 | 메신저피싱 표면 **2종(카카오톡형 + 문자메시지형)** + **한국어 시나리오 복수 종**(채팅 재현, 시작 전 메타 표기) + **카카오 상시 면책 고지**(OQ-24) — OQ-19 확정: 표면 넓게 | P0(확장) | 선행 의존성만(일정 제약 없음) | AC-030, AC-033, AC-002, AC-047 |
| 15 | 메신저 안전 가드레일 — 링크 유도 무해화(실 악성 URL 금지·비이동·모의 표식) + **스미싱 링크 인앱 가짜 랜딩 목업**(OQ-21) + 콘텐츠 금지 패턴 검증 확장 | P0(확장) | 선행 의존성만(일정 제약 없음) | AC-032, AC-033, AC-045 |
| 16 | **메신저→보이스 채널 에스컬레이션**(LLM 판단+구조화 신호 트리거 → 사기범 전화 전환, 세션·맥락·기록 연속성, 단일 리포트, 기존 음성 파이프라인·안전장치 재사용) + **조건부 clone/목소리 선택 서브플로우**(OQ-23). **전이 엔진은 양방향 설계·MVP는 메신저→보이스 우선**(보이스→메신저는 fast-follow) | P0(확장·핵심) | 선행 의존성만(일정 제약 없음) | AC-034, AC-035, AC-036, AC-037, AC-039, AC-046 |
| 17 | 기기별 문자메시지 UI **자동 감지 + 스킨**(iOS 기본 메시지 / 삼성 메시지, 폴백·수동 전환 포함) — OQ-17 확정. **MVP 포함으로 상향**(표면 넓게 결정에 따라 P0(확장)) | P0(확장) | 선행 의존성만(일정 제약 없음) | AC-031 |
| 18 | **사후 리플레이 해설**(종료 후 대화를 되짚으며 사기 신호 시점·대처 짚기) — 기존 리포트/transcript 재사용, 모든 훈련 공통. **P-1에서 MVP(P0) 승격** | P0(확장) | 선행 의존성만(일정 제약 없음) | AC-038 |
| 19 | **2인 소셜 훈련(비동기 챌린지 링크)** — 사용자1 클론→챌린지 생성→공유 링크→사용자2 사전 동의→복제 음성 체험→정체 공개+결과. **4대 안전제약(사전 동의·유출 차단·정체 공개·결과 열람 제한)을 AC로 강제** | P0(확장·신규 모델) | 선행: T35(2인 안전 구조) 완료 | AC-040, AC-041, AC-042, AC-043, AC-044 |

## Acceptance Criteria
| ID | Verifiable Condition |
|---|---|
| AC-001 | 시나리오 라이브러리는 최소 한국어 "가족 납치/사고(딥보이스)" 1종을 포함하고, 세션 시작 전 목록에서 선택할 수 있다(여유 시 "기관사칭(검찰/은행) 문자+링크" 1종 추가). |
| AC-002 | 각 시나리오는 시작 전 제목·사기 유형·예상 소요 시간·난이도(강도) 라벨을 표시한다. |
| AC-003 | 세션이 시작되면 AI는 선택된 시나리오의 사기범 인격으로 대화를 시작하고 세션 내내 그 인격을 유지한다(명시적 안전 종료 경로 제외하고 캐릭터를 깨지 않는다). |
| AC-004 | AI는 각 사용자 턴에 대해 정의된 지연 목표 내에 응답한다(목표: 95% 턴 기준 ≤10초, 정확한 값은 OQ-9). |
| AC-005 | AI의 수법은 사전 정의된 "약화된" 수법 집합에서만 나오며, 현실에서 사기를 실행하는 데 쓰일 수 있는 운영 정보(실제 계좌번호, 실제 송금 절차, 실제 악성 앱/링크)는 절대 제공하지 않는다. |
| AC-006 | 사용자는 모든 턴에서 항상 보이는 단일 컨트롤로 세션을 즉시 종료할 수 있고, 종료 시 세션이 끝나고 리포트로 진행한다. |
| AC-007 | 세션은 정의된 종료 조건(사용자 완료/속아 넘어감, 사용자 종료, 최대 턴·시간 한도 도달) 중 하나로 끝나며, 종료된 모든 세션은 정확히 1개의 리포트를 생성한다. |
| AC-008 | 세션 종료 후 리포트는 최소한 (1) 사용자가 속았거나 응했을 특정 메시지/순간, (2) 사용된 조작 수법, (3) 구체적 예방 조언 1개 이상을 식별한다. |
| AC-009 | 사용자가 한 번도 속지 않은 경우, 리포트는 이를 명시하고 그럼에도 시도된 수법들을 나열한다. |
| AC-010 | 세션 완료 시 세션 결과로부터 방어 점수/등급이 산정되어 사용자에게 표시된다(기본형, 정확한 산정식은 OQ-5). |
| AC-011 | 사용자의 등급/레벨과 세션 횟수는 세션 간 지속되며 프로필에서 확인할 수 있다. |
| AC-012 | 첫 세션 전(및 요청 시 언제든) 사용자는 해당 콘텐츠가 훈련용 시뮬레이션이며 실제 금전·자격증명이 관여하지 않는다는 점과 종료 방법을 안내받는다. |
| AC-013 | 사용자가 실제 사기 실행 방법을 요청하거나 AI를 타인 사기 학습에 이용하려 하면, 시스템은 거부하고 운영 가능한 사기 지침을 제공하지 않는다. |
| AC-014 | 정의된 최소 연령 미만 사용자의 접근을 제한하는 연령 확인이 존재한다(최소 연령 = **만 14세**, OQ-8 확정). |
| AC-015 | 세션 종료 시 사용자에게 상호작용이 시뮬레이션이었음을 상기시키는 디스컬레이션(안심) 메시지가 표시된다. |
| AC-016 | 사용자는 과거 세션과 리포트의 시간순 히스토리를 볼 수 있다(P1). ※ "계정 생성/접근(로그인·인증)" 부분은 P0로 상향되어 **AC-027**로 분리 관리한다. AC-016은 히스토리 열람 기능(P1)만을 가리킨다. |
| AC-017 | 최초 사용 시, 사용자는 어떤 시나리오도 시작되기 전에 시뮬레이션된 기만 경험에 대해 명시적으로 동의해야 하며, 동의 없이는 세션이 시작되지 않는다. |
| AC-018 | (음성 클론) 사용자가 앱에서 30초 분량의 지정 대본을 녹음하면, ElevenLabs Instant Voice Cloning으로 해당 목소리의 클론이 생성되고 임의 한국어 문장을 그 목소리로 합성해 재생할 수 있다. |
| AC-019 | (딥보이스 재생) 클론된 목소리로 "가족 납치/사고" 사칭 대사(예: "엄마 나 사고났어, 합의금 지금…")가 **앱 내 오디오로 재생**된다. 실제 전화망/통화 연동 없이 인앱 재생 전용이다. |
| AC-020 | (가드레일1·본인 동의) 사용자는 자신의 목소리만 등록할 수 있으며, 녹음/등록 전 본인 동의 및 본인 확인 절차를 통과해야 한다. 타인 목소리를 무단 등록·클론하는 경로가 UI에 존재하지 않는다. |
| AC-021 | (가드레일2·즉시 폐기) 훈련 세션 종료 시 생성된 클론 음성(및 기타 합성물) 파일이 스토리지에서 삭제되고 서버에 영구 저장되지 않으며, 삭제 이벤트가 로그로 남는다. |
| AC-022 | (가드레일3·합성 표식) 모든 생성물(재생되는 합성 음성·이미지)에 "AI 훈련용 합성" 표식(오디오 안내 문구 또는 화면 라벨)이 포함된다. |
| AC-023 | (가드레일4·송금 유도 없음) 시뮬레이션은 실제 계좌·송금·이체 기능을 포함하지 않으며, 세션 종료 시 "이것은 훈련이었습니다"를 명시적으로 고지한다. |
| AC-024 | (가드레일5·PII/인젝션) 대화 로그의 개인정보는 마스킹/토큰화되고, 사용자 입력과 시스템 프롬프트가 분리되어 프롬프트 인젝션으로 사기범 인격·가드레일이 해제되지 않는다. |
| AC-025 | (P1·사칭 이미지) 사칭 이미지 1종(예: 가짜 송금완료 화면)이 생성되어 시뮬레이션 중 표시되며, AC-022의 합성 표식을 포함한다. |
| AC-026 | 취약점 리포트는 사용자가 속은 시점을 시간축(예: "15초 시점에 속았습니다")으로 표시하고, 그 순간 취했어야 할 올바른 대처(끊고 직접 전화 등)를 제시한다. |
| AC-027 | (로그인·P0) 사용자는 온보딩 시작(사전 고지·명시적 동의) 전에 Firebase Auth로 계정을 생성하거나 로그인해야 하며, 인증되지 않은 사용자는 이후 단계(동의·목소리 녹음·세션·리포트)로 진행할 수 없다. 인증 성공 후 발급된 본인 계정(uid)에 이후 모든 데이터(동의 로그·리포트·메타)가 귀속된다. 구체 인증 프로바이더(이메일/구글 소셜/전화번호 등) 선택은 architect 소관(docs/UX.md OQ-U5). |
| AC-028 | (메신저 확장·2분류) 훈련 유형 선택은 최상위에서 (A) 보이스피싱 / (B) 메신저피싱 2개 분류로 제시되고, 보이스피싱 분류 하위는 기존 `voiceMode`를 재활용해 "본인 목소리 복제(clone)" / "기본 AI 음성(generic)"으로 구분된다. 신규 음성 데이터 모델을 도입하지 않는다(구체 화면 배치는 ux-design 소관). |
| AC-029 | (메신저 확장·단계적 선택) 시나리오 선택은 "유형(분류)을 먼저 선택하면 해당 유형에 속한 시나리오만 이후 단계에서 노출되는" 단계적 흐름으로 동작하며, 모든 시나리오를 하나의 평면 목록으로 동시에 나열하지 않는다. 구체 화면·전환 연출은 ux-design 소관이며, 본 AC는 "유형 선택 → 시나리오 노출" 순서 요건만 규정한다. |
| AC-030 | (메신저 확장·표면·복수 종) 메신저피싱 유형은 **한국어 시나리오 복수 종**을 포함하고, **카카오톡형과 문자메시지형 두 표면 모두**로 대화가 재현되며(OQ-19 확정: 표면 넓게), 세션 시작 전 제목·사기 유형·예상 소요 시간·난이도를 표시한다(AC-002 정합). |
| AC-031 | (메신저 확장·기기별 문자 UI, 자동 감지) 문자메시지 표면을 재현하는 시나리오는 접속 기기(User-Agent 등)를 **자동 감지**해 iOS 기본 메시지 / 삼성 메시지 스킨을 자동 적용한다(OQ-17 확정). **감지 실패·데스크톱 등은 기본 스킨으로 폴백**하고, 사용자가 **수동으로 스킨을 전환할 수 있는 옵션**을 제공한다. UA 감지 판정 로직·신뢰도 한계의 구현 확정은 architect(T26) 소관. |
| AC-032 | (메신저 확장·링크 무해화, 안전) 메신저 단계의 "링크 클릭 유도" 수법은 실존·클릭 가능한 악성 URL을 절대 포함하지 않는다. 링크는 무해한 표기(비활성 텍스트·가짜/모의 도메인 표기)로만 재현되고, 사용자가 이를 눌러도 외부로 이동하지 않으며 "AI 훈련용 모의" 표식이 노출된다(AC-005·AC-022의 메신저 확장). |
| AC-033 | (메신저 확장·콘텐츠 안전 정합) 메신저 시나리오의 모든 텍스트(대화·표면 요소)는 실계좌번호·실제 송금 절차·실제 기관/앱명·실 URL을 포함하지 않으며, 기존 콘텐츠 금지 패턴 검증(현재 `functions/src/scenarios/__tests__/scenarios.test.ts`)이 메신저 콘텐츠에도 동일하게 적용된다. 프롬프트 인젝션 방어(guardrailPreamble) 구조를 유지한다. |
| AC-034 | (메신저 확장·에스컬레이션 트리거, LLM 판단 + 구조화 신호) 메신저 세션 진행 중 사용자의 의심/거부/확인 시도를 **대화를 연기하는 LLM이 캐릭터 내에서 감지**하면, LLM이 **구조화된 신호(function-call 또는 약속된 sentinel 토큰)**를 내보내고 앱이 이를 읽어 화면·음성 세션을 보이스피싱 단계로 전환한다(사기범이 전화를 거는 형태). **자유텍스트 분류로 앱이 직접 판정하지 않는다**(구조화 신호만 신뢰). **안전 폴백**: 사용자가 끝까지 트리거를 발생시키지 않으면 max-turn 도달 시 전환하거나 명시 전환 버튼을 노출한다. **제약(불변)**: 사용자 입력은 데이터이지 지시가 아니다 — 구조화 신호 발신 여부와 무관하게 프롬프트 인젝션으로 인격·가드레일이 해제되지 않는다(AC-024). 구조화 신호 스키마·세션 접합 상세는 architect(T26) 소관. (OQ-16: v0.9.1 하이브리드 → v0.10 LLM 판단+구조화 신호로 재확정.) |
| AC-035 | (메신저 확장·세션·기록 연속성) 에스컬레이션 시 메신저 단계와 보이스 단계는 하나의 연결된 세션으로 취급되어 세션 식별자·대화 맥락·훈련 진행 기록이 연속으로 유지되고, 종료 시 두 단계를 함께 다루는 단일 리포트가 생성된다(AC-007 정합 — 종료된 세션은 정확히 1개의 리포트). |
| AC-036 | (메신저 확장·음성 파이프라인 재사용·안전장치 유지) 에스컬레이션의 보이스 단계는 기존 실시간 음성 통화 셸(docs/UX.md UX-014)과 음성 파이프라인을 재사용하며, 진입 시 사전 고지·동의(AC-012/AC-017), 상시 종료(AC-006), 합성 표식(AC-022), 종료 고지·디스컬레이션(AC-015/AC-023), PII·인젝션 방어(AC-024)가 그대로 유지된다. 실제 전화망(PSTN) 연동은 하지 않는다(AC-019 무변경, 인앱 통화형 UI). |
| AC-037 | (메신저 확장·교차 채널 리포트) 종료 리포트는 사용자가 메신저 단계에서 취약했던 시점(예: 링크 클릭·개인정보 입력)과 보이스 단계에서 취약했던 시점을 모두 식별하고, 각 시점에서 취했어야 할 올바른 대처를 제시한다(AC-008/AC-009/AC-026 정합). 한 번도 속지 않은 경우 이를 명시하고 시도된 수법만 나열한다. |
| AC-038 | (사후 리플레이 해설·P0) 세션 종료 후, 사용자는 대화(transcript)를 시간순으로 되짚으며 각 사기 신호 시점에 "여기 이 말이 사기 신호였어요 / 이렇게 대응했어야 해요"를 짚어 주는 **리플레이 해설**을 볼 수 있다. 기존 리포트·transcript 인프라(AC-008/AC-026, 현재 T9)를 재사용하며, 메신저·보이스·2인 세션 모두에 적용된다. "면역됨" 류 과신 표현을 쓰지 않고 "개선 영역" 프레임을 유지한다. |
| AC-039 | (에스컬레이션·양방향 설계·순차 구현) 채널 전이 엔진은 **방향 무관(direction-agnostic)** 하게 설계되어 메신저→보이스와 보이스→메신저를 동일 메커니즘으로 표현할 수 있어야 한다. **단 MVP 구현은 메신저→보이스 방향을 먼저 완결**하고, 보이스→메신저 방향은 그 배선 검증 후 fast-follow(후속 태스크로 분리)로 추가한다. (OQ-22 확정.) |
| AC-040 | (2인 소셜·사용자2 사전 동의, 안전·필수) 2인 소셜 훈련에서 초대받은 사용자2는 시나리오가 시작되기 전 랜딩에서 "○○님이 보이스피싱 인식 테스트를 준비했고, 이제부터 받는 전화·문자는 실제가 아닌 훈련"이라는 고지에 **명시적으로 동의(탭)해야** 진입한다. 동의 없이는 어떤 복제 음성 전화·문자도 재생되지 않는다. 시나리오의 구체 내용·타이밍은 서프라이즈로 유지하되 "시뮬레이션임" 사실은 사전 고지한다. **무동의 상태로 실존 인물을 복제 음성으로 기만하는 경로는 UI·API에 존재하지 않으며, 사후 정체 공개는 사전 동의를 대체하지 못한다.** |
| AC-041 | (2인 소셜·복제 음성 유출 차단·기간제 보존, 안전·필수) 사용자1의 클론 오디오는 **누구도 다운로드·추출할 수 없고**, 해당 챌린지 스코프에 고정되어 다른 챌린지·용도로 재사용되지 않는다. 보존은 **기간제**로, **사용자가 정한 기간(예: 7일/30일, 기본값은 architect/구현 단계 확정) 후 자동 삭제되며 언제든 수동 삭제도 가능**하다(OQ-25 확정 — "1회용 즉시삭제"나 "무기한 보관"이 아니라 사용자 설정 가능한 기간제). 스코프 고정·기간제 삭제·추출 차단의 구체 구조는 architect(T35) 소관. |
| AC-042 | (2인 소셜·정체 공개, 안전·필수) 2인 세션은 종료 시 반드시 사용자2에게 "이것은 훈련이었습니다"를 고지하고(AC-023 정합) 리플레이 해설(AC-038)을 제공한다. 정체 공개·해설은 옵션이 아니라 강제 단계다. |
| AC-043 | (2인 소셜·결과 열람 제한, 프라이버시) 사용자1은 **사용자2가 동의한 경우에만** 결과를 열람할 수 있고, 열람 범위는 대화 전문이 아니라 "완료 여부 / 의심 시점" 수준으로 제한된다. 사용자2 미동의 시 사용자1은 상세 결과를 볼 수 없다. |
| AC-044 | (2인 소셜·비동기 챌린지 흐름) 사용자1은 로그인→본인 목소리 클론→딥보이스 시나리오 선택 후, 앱이 **챌린지 레코드**(scenarioId + 사용자1 voiceId + 표시이름)를 생성하고 **공유 링크(토큰)**를 발급하면 이를 실제 카톡/문자로 사용자2에게 전달한다. 사용자2는 링크를 열어 AC-040 동의 후 체험하고 AC-042로 종료한다. **동기(실시간 조종) 방식은 MVP 제외.** 링크 토큰 정책은 AC-048, 오용 방지는 AC-049. |
| AC-045 | (메신저 확장·스미싱 링크 인앱 목업, 안전) 사용자가 메신저 대화 중 스미싱 링크를 탭하면 **실제 외부 URL로 이동하지 않고 앱 내부의 가짜 로그인/정보입력 랜딩 화면**으로 전환되어 "정보 입력 유도" 상황을 재현한다(OQ-21 확정). 입력된 값은 **실제 서버로 전송되지 않고** UI상으로만 "입력함" 가짜 피드백 처리 후 시나리오가 이어진다. **외부 네비게이션은 절대 없고**(AC-005/AC-032 연장), 가짜 랜딩에도 실제 브랜드·기관명·실 URL을 쓰지 않고 무해화하며 "AI 훈련용 모의" 표식을 노출한다. |
| AC-046 | (에스컬레이션·조건부 clone + 목소리 선택 서브플로우) 메신저→보이스 전이가 가능한 시나리오를 선택하는 시점에 "이 시나리오는 통화로 이어질 수 있어 목소리 녹음이 필요할 수 있습니다" 안내를 표시한다(OQ-23 확정). 사용자는 (1) 그 자리에서 즉시 녹음하거나, (2) 이미 녹음해 둔 목소리(복수 보유 시 선택)를 재사용하거나, (3) 둘 다 없을 때 최종 폴백으로 **최소한 남자 목소리 / 여자 목소리 중 선택하는 기본 보이스 옵션**을 제공받는다(단일 generic 무선택보다 한 단계 나은 폴백). 안내 문구 위치·녹음 진입 화면 등 UX 상세는 ux-design(T23 계열) 소관. |
| AC-047 | (메신저 확장·카카오 상표 완화, 면책 고지) 카카오톡을 정확 모사하는 훈련 화면에는 "카카오톡 실제 서비스와 무관한 훈련용 재현입니다" 류 **면책 고지가 상시 노출**된다(OQ-24 확정 — 상단 고정 배너 또는 최초 진입 표시 후 지속 배지 등, 구체 형태는 ux-design). **마케팅/스토어 노출 제한은 이번에 채택하지 않았다**(면책 고지만 채택) — 과잉 반영 금지. |
| AC-048 | (2인 소셜·링크 토큰 정책) 챌린지 공유 링크는 **랜덤 토큰 + 시간 만료 + 1회성 소모** 방식이며, 사용자2는 **별도 계정/로그인 없이** 링크만으로 진입한다(OQ-26 확정). **무료 티어의 링크 만료 기간 = 3일**(v1.1 확정), 유료 티어는 연장(예 7일+, 수익화 모델 참조). ※ 이 "링크 만료"는 AC-041의 "복제 음성 보존기간"과 별개 개념이다(링크가 만료돼도 음성 보존은 AC-041 정책을 따름, 그 반대도 마찬가지). 토큰 생성·검증 스키마는 architect(T35) 소관. |
| AC-049 | (2인 소셜·오용 방지) 오용 방지는 (1) **사용자1당 활성 챌린지 개수 제한**(무료 티어 = 동시 3개, 유료 확장) + 링크 만료(AC-048), (2) **사용자2의 "원치 않는 챌린지 수신" 신고 기능**으로 구성된다(OQ-27 확정, planner 추천안 채택). **관리자 수동 검토는 채택하지 않는다**(운영 부담, MVP 제외). 신고 데이터 모델·상한값 상세는 architect(T35) 소관. |
| AC-050 | (수익화·안전장치 게이팅 절대 금지) 요금제 등급(무료/유료/B2B/B2G)은 **용량·편의·심화분석·시나리오 확장 축에서만** 기능을 구분한다. **안전장치는 등급과 무관하게 전 사용자에게 동일하게 강제된다** — 사전 동의(AC-012/AC-017/AC-040), 유출 차단(AC-021/AC-041), 정체 공개(AC-015/AC-023/AC-042), 결과 열람 제한(AC-043), 본인 목소리만 등록(AC-020), 합성/모의 표식(AC-022/AC-032/AC-045/AC-047), PII·인젝션 방어(AC-024), 콘텐츠 무해화(AC-005/AC-013/AC-033) 등 모든 보안·윤리 가드레일. **유료화가 안전장치를 약화·우회·게이팅하는 설계는 금지된다.** 핵심 안전훈련 콘텐츠(보이스+메신저 기본 시나리오 전체 + 리플레이 해설 기본 AC-038)는 무료로 유지한다. **광고 모델은 채택하지 않는다**(신뢰 훼손 리스크). |

## Out of Scope (Future)
사용자가 하루 완성을 위해 명시적으로 제외한 항목.

- **영상 딥페이크(talking-photo, D-ID 등)** — 완전 제외. 로드맵으로만 언급(P2). 사용자 명시 지시.
- **실제 전화망(PSTN) 연동** — 여전히 완전 제외(v0.8에도 무변경) → 앱 내 오디오 재생/통화형 UI(AC-019)로 대체. Twilio 등 텔레포니 미도입.
- ~~실제 STT(음성 인식 대화) — 제외~~ **v0.8에서 In Scope로 전환**: 브라우저 STT(Web Speech API)로 사용자가 실제 말해서 입력하고, 사기범 응답도 음성 자동재생(사기범 응답 TTS 신설). 텍스트 입력은 STT 미지원 브라우저용 폴백으로 유지(침묵 실패 금지 원칙).
- **다국어·다수 시나리오** — 제외 → 한국어 "가족 납치" 1종 집중(여유 시 "기관사칭" 1종).
- **기업 관리자 대시보드 / 조직 단위 분석** — 이번 스코프 제외(B2B는 시장성 로드맵으로만).
- **AI Hub 딥페이크 변조 영상 데이터셋 코어 도입**(탐지 모델 학습) — 2일 내 학습 불가·승인/용량/반입 리스크. 코어 미도입, 데모용 샘플 전시 정도만 제한적 활용 가능.
- 로맨스스캠 시나리오(v0.1엔 포함, 하루 스코프서 제외).
- 리딩방·투자 딥페이크 영상 시나리오(영상 제외에 종속).
- 실시간 사람 개입(human-in-the-loop) 코칭.
- 통신사·은행 실사기 경보 시스템 연동.
- **(메신저 확장) 보이스 → 메신저 역방향 에스컬레이션** — 사용자 요구는 메신저 → 보이스 단방향만. 역방향은 이번 스코프 제외(OQ-22로 확인 필요, 확정 시 재검토).
- **(메신저 확장) 실제 메신저 계정/전화망 연동** — 카카오톡·통신사 문자망에 실제로 연결하지 않는다. 모든 표면은 앱 내 재현(모의) UI다. UI 외형은 정확 모사하되(OQ-18 확정, accepted risk), 실 계정·실 전송·실 서비스 연동은 하지 않는다.
- **(메신저 확장) 실제 악성 링크·가짜 앱 실제 설치 흐름** — 완전 제외. 링크는 무해 표기·비이동만(AC-032).

## 추가 기능 제안 (planner 제안, 2026-07-23)
> planner 관점의 후보 기능. 대부분 확장 로드맵(P1/P2)이며, 채택·우선순위 최종 확정은 사용자 몫. 가치=사용자·시장 가치, 난이도=구현 복잡도 추정(추정치 — architect 확인 필요), 우선순위=이번 확장 기준 제안값.

| # | 제안 기능 | 가치 | 난이도 | 제안 우선순위 | 근거·비고 |
|---|---|---|---|---|---|
| P-1 | **훈련 후 상세 피드백·행동 코칭 강화 / 사후 리플레이 해설** — 대화를 되짚으며 "여기 이 말이 사기 신호였어요" 짚기 | High | Low~Med | **P0(MVP 승격)** | **사용자 확정(2026-07-23): 리플레이 해설을 MVP(P0)로 승격 → AC-038·MVP Scope #18로 이관.** 기존 리포트/transcript 재사용. |
| P-2 | **실제 신고·차단 연계 안내(정적) / 신고 원스톱 안내** — 112/1332·금융감독원·지급정지 절차를 훈련 종료 후 정적 안내 | High | Low | **P1(MVP 근접, 사용자 채택)** | 사회적 가치·시장성(지자체·통신사 스토리). ⚠️ 실 연동 아님·실 클릭 링크 없이 정보만(AC-005 준수). **사용자 채택(2026-07-23), MVP 근접 권고.** |
| P-3 | **접근성·어르신 모드 강화** — 큰 글씨·음성 안내·1탭 플로우, (프라이버시 확정 시) 보호자용 요약 | High | Med | P1 | Target Users 핵심(어르신). 메신저 UI가 늘며 접근성 리스크↑ → 조기 고려 권장. |
| P-4 | **사기 유형별 마이크로러닝 카드 + 퀴즈** — 훈련 전/후 짧은 학습 카드로 지식·경험 결합 | Med | Low~Med | P1~P2 | 리텐션·게임화(T13)와 결합. 유형 2분류(AC-028) 구조와 잘 맞음. |
| P-5 | **난이도 적응(adaptive difficulty)** — 방어 등급(T13)에 따라 시나리오 강도 자동 조절 | Med | High | P2 | LLM·게임화 산정식(OQ-5) 종속. 오버엔지니어링 주의 → 로드맵. |
| P-6 | **가족 공유·초대 & 취약점 요약 공유** — 차별점 2번(가족 원격 훈련) 실체화 | High | Med~High | P2 | ⚠️ OQ-15(자녀 리포트 열람)·프라이버시·동의 재검토 선행 필수. 확정 전 미구현. 2인 소셜 모델(MVP Scope #19)이 이 방향의 안전한 첫 구현체다. |
| P-7 | **결과 카드(공유용)** — 사용자2 완료 후 "나는 ○초 만에 의심 / 여기서 넘어갈 뻔" 공유 카드 → 바이럴 루프 | High | Low~Med | **P1(MVP 근접, 사용자 채택)** | 2인 모델(#19)과 시너지. ⚠️ 공유 카드에 상대·본인 PII·복제 음성 미포함(AC-024/AC-041 준수). **사용자 채택(2026-07-23).** |
| P-8 | **내 목소리 금고(신뢰 대시보드)** — 사용자1이 자기 클론이 어디(어느 챌린지) 쓰였는지 보고 언제든 삭제 | High | Med | **P0~P1(MVP 권고, 사용자 채택)** | 딥보이스 신뢰 확보. **안전장치2(AC-041 유출 차단·삭제)와 직결** → 2인 모델 도입 시 사실상 동반 필요. **사용자 채택(2026-07-23), MVP 권고.** |
| P-9 | **취약 지점 프로파일** — 유형별(권위/긴급성/가족애/이익유혹) 취약도 추적·추천 | Med | Med~High | P2(후속, 사용자 채택) | 게임화(T13)·리포트 누적 데이터 재사용. 로드맵. **사용자 채택(2026-07-23), 후속 권고.** |

## 수익화 모델 (장기 로드맵, 2026-07-23 확정)
> **구현 시점 명시**: 결제·구독 시스템 자체는 **이번 세션의 즉시 구현 대상이 아니라 장기 로드맵 항목**이다. MVP Scope 표(#1~#19)에는 포함하지 않는다 — 향후 별도 계획으로 분리한다.

### 원칙 (다른 모든 수익화 항목보다 우선 — AC-050으로 강제)
- **안전장치는 등급과 무관하게 전 사용자 동일 강제.** AC-040(사전 동의)·AC-041(유출 차단)·AC-042(정체 공개)·AC-043(결과 열람 제한) 및 기타 모든 보안·윤리 가드레일은 무료/유료/B2B/B2G 구분 없이 동일하게 적용된다. **유료화는 절대 안전장치를 약화·우회·게이팅하지 않는다**(AC-050).
- **핵심 안전훈련 콘텐츠는 무료 유지**: 보이스+메신저 **기본 시나리오 전체**, **리플레이 해설 기본**(AC-038)은 무료.
- 유료화는 **"용량·편의·심화분석·시나리오 확장" 축에서만**.
- **광고 모델 미채택**(신뢰 훼손 리스크).

### 개인 구독(프리미엄) 티어
| 구분 | 무료 | 유료 |
|---|---|---|
| 목소리 복제 | 1개 저장, 보존기간 짧게(AC-041 기간제 정책) | 여러 목소리 프로필 저장(내 목소리 금고 P-8 확장) + 보존기간 연장 |
| 챌린지 링크 | 동시 활성 **3개**, **만료 3일**(AC-048/AC-049, v1.1 확정) | 동시 개수 확장(예 10개+) + 만료 연장(예 7일+) |
| 리포트 | 리플레이 해설 기본(AC-038) | 취약 지점 프로파일 심화(P-9 연계) + 개인 맞춤 학습 커리큘럼 추천 |
| 시나리오 | 기본 시나리오 전체(보이스+메신저) | 신규/특화 시나리오 우선 접근(최신 사기 수법 지속 업데이트) |
| 결과 카드(P-7) | 기본 공유 | 커스터마이징(문구·디자인) |

### B2B/B2G 채널 (개인 구독과 병행)
- **B2B**: 기업 임직원 정기 안티피싱 교육 라이선스 + 관리자 대시보드(이수율·취약유형 통계). ※ 관리자 대시보드는 기존 Out of Scope(개인 MVP 밖)였던 항목의 유료 채널 실체화.
- **B2G**: 지자체·복지관 대상 대량 좌석 라이선스 — **최종 사용자(어르신)는 무료, 기관이 비용 부담**. 취약계층 도달 미션과 정합.

### 로드맵 메모
- 등급별 기능 게이팅 로직·결제 연동(PG)·구독 상태 관리는 **별도 후속 계획**에서 태스크화한다(이번 Tasks.md T23~T40 범위 밖). 게이팅 구현 시 **AC-050(안전장치 게이팅 금지)을 게이트 조건으로** 검증해야 한다.

## Assumptions
- MVP는 웹(Next.js) 단일 채널, 텍스트 채팅 기반이며 사용자는 인터넷 연결 기기에서 텍스트를 읽고 입력하고 마이크로 30초 녹음할 수 있다.
- ElevenLabs Instant Voice Cloning API가 1~3분(본 스코프는 30초) 샘플로 사용 가능한 품질의 한국어 클론을 하루 내 산출한다(전날 리허설로 검증 — Risk 참조).
- 지속적 인격 롤플레이와 가드레일을 감당할 LLM(Claude/Gemini)을 세션당 합리적 비용/지연으로 사용할 수 있다.
- 사용자는 자발적 훈련 대상이며 공격자가 아니다.
- **자녀 대리설정 시, 목소리 녹음은 반드시 본인(등록 대상자)이 직접 수행하며 자녀는 계정 생성 UI 조작만 보조한다. 실제 녹음 버튼은 본인이 누른다**(OQ-13 확정). 따라서 동의 주체는 본인, 데이터 소유권은 본인 계정 소유다(OQ-11 확정).
- 1차 언어·시장은 한국어/한국(확정, OQ-7 resolved).
- **(메신저 확장·추정) 보이스 단계는 기존 실시간 음성 파이프라인·통화 셸(UX-014, `VoiceProvider`, LLM 어댑터)을 재사용할 수 있다.** 에스컬레이션은 "새 통신 스택"이 아니라 기존 세션의 단계 전이로 구현 가능하다고 가정(추정 — architect가 세션 모델·상태 전이 접합점을 확인·확정해야 함).
- **(메신저 확장·추정) 메신저 단계는 텍스트 기반이므로 기존 역할극 엔진(sendMessage·인격 유지·가드레일)을 표면(채팅 UI)만 바꿔 재사용할 수 있다**(추정 — architect 확인 필요). 메신저 특유 요소(링크 카드·읽음 표시 등)는 표면 레이어에서 처리.
- **(메신저 확장) 사용자 기기 유형(iOS/삼성) 구분은 재현 UI 스킨 차이일 뿐, 실제 OS 기능 연동은 아니다.** 기기 스킨은 User-Agent 등으로 자동 감지하되(OQ-17 확정), 감지 신뢰도에는 한계가 있어 폴백(기본 스킨)·수동 전환을 전제로 한다.
- **(메신저 확장·accepted risk) 카카오톡 UI 정확 모사는 사용자가 상표·저작권 리스크를 인지하고 내린 결정이다**(OQ-18 resolved). 완화책으로 **상시 면책 고지(AC-047, OQ-24 확정)**를 채택했다(마케팅/스토어 제한은 미채택). 면책 고지의 법적 충분성·ADR 필요 여부 판단은 architect에 위임한다. 정확 모사는 UI 외형에 한정되고 사기 콘텐츠 무해화(AC-005/AC-032)는 변경되지 않는다.
- **(사후 리플레이 해설·추정) 리플레이 해설은 기존 리포트 분석(analyzeConversation)·transcript·타임라인(AC-008/AC-026·T9) 인프라를 재사용해 구현 가능하다**(추정 — 리포트를 "정적 요약"에서 "대화 되짚기 뷰"로 확장하는 수준. UI/데이터 상세는 ux-design·architect 확인).
- **(2인 소셜·전제) 이 기능의 4대 안전제약(AC-040 사전 동의·AC-041 유출 차단·AC-042 정체 공개·AC-043 결과 열람 제한)은 옵션이 아니라 구현 전제조건이다.** 하나라도 미충족이면 2인 모델은 출시하지 않는다. 무동의 상태로 실존 인물을 복제 음성으로 기만하는 버전은 명시적으로 금지한다.
- **(2인 소셜·추정) 비동기 챌린지 링크 방식은 실시간 서버 세션 없이 챌린지 레코드(Firestore) + 토큰 링크로 구현 가능하다**(추정 — 데이터 구조·토큰·오용 방지 상세는 architect OQ-25/26/27). 동기(실시간 조종)는 MVP 제외.

## Constraints
- **기술 스택(확정)**: Next.js + Firebase(Auth/Functions/Firestore/Storage) + LLM(Claude/Gemini, 사기범 역할극) + ElevenLabs Instant Voice Cloning API($5/월, 1~3분 샘플로 클론).
- **팀 구성**: 개발 코어 3명(제윤·진우·수홍) 병렬 작업 / 기획·발표 2명(한솔·광훈).
- ~~**절대 마감(가장 강한 제약)**: 실질 개발은 Day1 하루(무박 야간 포함)에 전부 완료. Day2 오전은 개발 신규 금지. 최종 마감 = Day2 오전 11시.~~ → **해당 없음(2026-07-23, OQ-20 확정): 장기 프로젝트로 전환, 시간 압박 없음.** 해커톤 Day1/Day2 시간 제약은 폐기한다. 태스크는 날짜/스프린트 제약 없이 **선행 의존성 순서만** 따른다. (위 MVP Scope #1~#12 표의 "Day1 오전/오후/야간" Timeline 표기는 해커톤 당시 완료 기록의 역사적 흔적이며, 더 이상 유효한 제약이 아니다. ⚠️ `docs/DECISIONS.md`의 해커톤 일정 관련 항목 정리는 architect 소관.)
- **대회 배점**: 시장성 35% / 보안 20% / 기술·완성도·발표 45%. 보안 가드레일(AC-020~AC-024, AC-017, AC-015, AC-012)이 보안 배점의 핵심 근거.
- **법적/윤리 (필수)**: 현실 사기 운영 지침 미제공(AC-005, AC-013), 실제 금전 피해 방지(실계좌·실송금·실링크 금지, AC-023), 본인 목소리만 등록(AC-020), 생성물 즉시 폐기·미저장(AC-021), 합성 표식(AC-022), PII 마스킹·PIPA 준수(AC-024), 사전/사후 고지(AC-012, AC-015). 미성년자 보호(AC-014, P1).
- **예산**: ElevenLabs $5/월 확정. 총 예산·API 크레딧 상한액 수치는 미확정이나, **T1(ElevenLabs 클론 PoC) 착수 전 사용자가 직접 크레딧/rate limit을 확인한 뒤 진행**하기로 확인 절차는 확정(OQ-14).
- **(메신저 확장·안전, 필수)**: 메신저 표면·대화 어디에도 실존·클릭 가능한 악성 URL, 실계좌번호, 실제 기관/앱명, 운영 가능한 사기 절차를 넣지 않는다(AC-032/AC-033, 기존 AC-005 확장). "링크 클릭 유도"는 무해 표기·비이동·모의 표식으로만 재현. 콘텐츠 금지 패턴 검증(현재 `functions/src/scenarios/__tests__/scenarios.test.ts`)을 메신저 콘텐츠로 확장한다.
- **(메신저 확장·법적, accepted risk)**: 카카오톡 UI를 **정확 모사**하기로 사용자가 상표·저작권 리스크를 인지하고 확정했다(OQ-18 resolved, accepted risk). **UI 외형만 정확 모사하고 사기 콘텐츠 자체는 무해화 원칙 불변**(실 악성 URL·실계좌·실 기관명 금지 — AC-005/AC-032 유지). 모든 표면에 "AI 훈련용 모의" 상시 표식 유지. **완화책(OQ-24 확정): "카카오톡 실제 서비스와 무관한 훈련용 재현" 상시 면책 고지(AC-047).** 마케팅/스토어 노출 제한은 채택하지 않음(면책 고지만). 면책 고지의 법적 충분성·ADR 필요 여부는 architect 판단.
- **(메신저 확장·에스컬레이션)**: 보이스 단계로의 전환은 실제 전화망(PSTN) 연동이 아니라 기존 인앱 통화형 UI(UX-014) 재사용이다(AC-036, AC-019 무변경).
- **(2인 소셜·안전, 필수·게이팅)**: 2인 소셜 훈련은 4대 안전제약(AC-040 사용자2 사전 동의, AC-041 복제 음성 유출 차단·삭제, AC-042 강제 정체 공개, AC-043 결과 열람 제한)을 **모두** 충족해야만 출시 가능하다(옵션 아님). 복제 음성이 챌린지 밖으로 나가는 스코프·삭제 정책, 링크 토큰 인증/만료, 오용 방지(대량 생성·타깃 무동의)는 출시 전 architect가 확정해야 하는 게이팅 항목(OQ-25/OQ-26/OQ-27)이다.

## Risks
확정안 리스크 + v0.1 리스크 중 이번 스코프에서도 유효한 것만 통합.

| Risk | Impact | Mitigation |
|---|---|---|
| 윤리 Q&A 공격("악용되면?") — 발표 심사에서 정면 공격 예상 | High | 보안 가드레일 5종(AC-020~AC-024)으로 방어: 본인 목소리만·즉시 폐기·워터마크·송금 유도 없음·PII/인젝션 방어. 발표 시 명시. |
| 음성 클론 품질(한국어·감정·지연) 미달 | High | ElevenLabs 전날 리허설로 사전 테스트 필수. 실패 대비 사전 녹화본 확보. PoC를 Day1 오전 최우선 태스크로 선(先)수행해 리스크 조기 제거. |
| 영상 유혹으로 인한 스코프 크립 | High | 영상 딥페이크 완전 제외(Out of Scope), 로드맵으로만 언급. 하루 스코프 사수. |
| API 비용/rate limit 초과 | Medium | 전날 크레딧·한도 확인. 총예산·상한은 OQ-14. LLM 지연 목표(AC-004). |
| 라이브 클론 데모 실패(네트워크·API 장애) | High | Day2 오전 데모 안정화 + 실패 대비 녹화본 재생 경로 준비. |
| 어르신이 롤플레이임에도 실제 심리적 충격/불안 | High | 강도 라벨·사전 동의(AC-017)·상시 종료(AC-006)·종료 후 안심 메시지(AC-015), 완화 톤, 낮은 강도부터. |
| 훈련받았다는 과신(false confidence) | Medium | 리포트를 "개선 영역" 프레임으로 서술, "이제 면역됨" 류 표현 금지, 반복 재훈련 유도. |
| 생성물·민감 세션 기록 유출(프라이버시) | High | 생성물 즉시 폐기·서버 미저장(AC-021), PII 토큰화(AC-024). 자녀 대리설정 시에도 녹음은 본인 직접·동의 주체는 본인·소유권은 본인 계정(OQ-13·OQ-11 확정). 자녀의 리포트 열람 가능 여부는 OQ-15(미확정)로 프라이버시 재검토 필요. |
| **음성 목업(임시 AI 음성)이 실수로 최종 데모까지 남아있을 위험 — 핵심 차별점(본인 목소리 클론)이 통째로 무효화됨.** ElevenLabs 키 미준비로 임시 목업(Mock TTS)으로 개발을 진행하는데, 교체를 잊으면 "타인/합성 음성"으로 데모하게 되어 제품의 참신점 1번(본인 목소리 복제)이 사라지고 발표 승부처가 붕괴한다. | High | **T16(데모 안정화) 체크리스트에 "실제 본인 목소리 클론으로 교체 확인"을 필수 항목으로 명시**하고, **목업 상태로는 T16 통과 불가**로 게이팅한다. ElevenLabs API 키 확보 즉시 T1(실클론 PoC) 착수 → T4 음성 구현체를 Mock→ElevenLabs로 교체. 목업 구현체는 인터페이스 뒤에 격리(T19)해 교체점을 단일화하고, 목업 오디오에 명백한 플레이스홀더 표식을 넣어 육안 식별 가능하게 한다. |
| **로그인 P0 상향으로 Day1 오전 일정 압박 소폭 증가** — 사용자 결정으로 로그인/가입 화면·인증 게이팅(T18, AC-027)이 P0로 추가됐다. Firebase Auth는 기존 스택에 있어 새 인프라는 아니지만 Day1 오전 P0 UI 태스크가 1개 늘어(트랙 C: T18 로그인 → T3 온보딩) 착수 순서가 직렬화된다. | Medium | Firebase Auth 기본 프로바이더로 최소 구현(가입·로그인·게이팅만), 프로필/히스토리 등 부가기능은 P1로 유지. 인증 프로바이더 선택(OQ-U5)을 architect가 조기 확정해 구현 재작업 방지. T18을 트랙 C 최우선에 두어 T3 온보딩 착수 지연 최소화. |
| **발표 스토리 vs 실제 데모/빌드 범위 괴리(오버클레임 위험)** — 발표에서 파는 "가족 원격 훈련"과 실제 Day1 데모/빌드 범위(본인 온보딩만)가 다르다. 심사위원이 "자녀가 실제로 뭘 할 수 있냐"고 물으면 답이 궁색해지거나 오버클레임처럼 보일 위험. | High | **발표팀(한솔·광훈) 가이드라인(2026-07-21 확정):** 발표 스크립트에서 "가족 원격 훈련"을 **현재 구현된 정확한 범위 내에서만** 설명하고 과장하지 않는다. 구체적으로 — (1) **실제 라이브 데모는 본인 1인 플로우로 진행**(본인이 직접 등록·녹음→클론 재생→역할극→리포트). (2) 자녀 측 기능은 "본인이 직접 등록, 자녀는 계정 생성 UI 보조까지"를 **로드맵 방향성**으로만 언급하고, 자녀가 지금 당장 리포트를 열람하거나 원격으로 훈련을 돌린다고 말하지 않는다(OQ-3·OQ-15는 빌드 미포함). (3) "향후 확장"과 "현재 데모 범위"를 발표 중 명확히 구분해 말한다. |
| **(메신저 확장) 카카오톡 UI 정확 모사의 상표/트레이드드레스 리스크** — 실 브랜드 UI를 그대로 모사하면 상표·부정경쟁 이슈 소지. **사용자가 리스크를 인지하고 정확 모사를 채택함(OQ-18 resolved, accepted risk).** | High(수용됨) | 사기 콘텐츠 무해화 원칙 불변(실 URL·실계좌·실 기관명 금지, AC-005/AC-032). 모든 표면에 "AI 훈련용 모의" 상시 표식. **완화책 채택: 상시 면책 고지(AC-047, OQ-24 확정)** — "카카오톡 실제 서비스와 무관한 훈련용 재현". 마케팅/스토어 노출 제한은 미채택. 면책 고지의 법적 충분성·ADR 필요 여부는 architect 판단. |
| **(메신저 확장) 링크 클릭 재현이 실제 위험 행동을 학습시킬 오용 리스크** — "링크를 눌러도 된다"는 잘못된 습관 형성 또는 실 악성 URL 혼입. | High | 링크 무해화 하드 요건(AC-032): 비이동·모의 도메인·모의 표식. 콘텐츠 금지 패턴 검증을 메신저로 확장(AC-033). 리포트에서 "링크 클릭이 왜 위험했는지"를 명시적 교육 포인트로 전환(AC-037). |
| **(메신저 확장) 에스컬레이션 상태 전이 복잡도** — 메신저↔보이스 세션 접합, 맥락·기록 연속성, 단일 리포트 보장(AC-035)이 세션 모델을 복잡하게 만들어 구현·QA 리스크↑. | High | architect가 세션 모델·전이 계약을 조기 확정(단일 세션 문서에 phase 필드 등). 트리거 방식은 결정적 옵션(명시 버튼/규칙) 우선 검토(OQ-16). QA 시나리오에 "전이 중 종료"·"전이 실패 폴백" 포함. |
| **(메신저 확장) LLM 전이 판단의 오탐/미탐·인젝션 리스크** — v0.10에서 트리거를 **LLM 판단+구조화 신호**로 재확정(OQ-16). 자유텍스트 분류 특성상 조기·지연 전환 가능성과 인젝션 표면이 있다. | Medium~High | 앱은 **자유텍스트가 아니라 구조화 신호(function-call/sentinel)만** 신뢰(자유텍스트 직접 판정 금지). 안전 폴백(max-turn 전환·명시 버튼)으로 미탐을 흡수. 사용자 입력=데이터 원칙·인젝션 방어(AC-024) 불변. 구조화 신호 스키마·세션 접합은 architect(T26) 확정. |
| **(2인 소셜) 무동의 타인 기만 오용 리스크** — 실존 인물을 복제 음성으로 동의 없이 속이는 악용 가능성(가장 큰 윤리 리스크). | High | AC-040(사용자2 사전 동의 게이팅)을 하드 요건으로 강제 — 동의 없이는 어떤 복제 음성도 재생 안 됨. 사후 정체 공개는 사전 동의를 대체 못 함. 무동의 경로는 UI·API에 미존재. 오용 방지(대량 생성·타깃 무동의)는 architect(OQ-27). |
| **(2인 소셜) 복제 음성 유출·오남용** — 사용자1 클론이 챌린지 밖으로 추출·재사용될 위험. | High | AC-041(추출·다운로드 불가·챌린지 스코프 고정·보존기한 삭제). 내 목소리 금고(P-8)로 사용처 가시화·삭제. 스코프·삭제 구조는 architect(OQ-25). |
| **(2인 소셜) 챌린지 링크 무단 확산·토큰 악용** — 링크가 제3자에게 전달·재사용될 위험. | Medium~High | 링크 토큰 인증·만료·1회성 등 정책을 architect(OQ-26)가 확정. 사용자2 동의 게이팅(AC-040)이 최종 방어선. |
| **(메신저 확장) 표면 2종 × 기기 스킨(iOS/삼성)으로 QA 매트릭스 증가** — 표면 넓게 결정(OQ-19)·자동 스킨(OQ-17)으로 조합이 늘어 검증 비용↑. | Medium~High | **표면을 스킨 레이어로 분리**해 콘텐츠(대화·수법)를 표면·기기와 무관하게 재사용하고, 스킨은 프레젠테이션만 교체(T24/T29 설계 원칙). UA 자동 감지는 폴백(기본 스킨)·수동 전환으로 실패를 흡수(AC-031). QA는 "각 표면 1개 시나리오 × 각 스킨 + 폴백 경로"를 대표 매트릭스로 우선 검증. |

## Open Questions
| # | Question | Status | Decision |
|---|---|---|---|
| 1 | MVP 채널을 웹 / 모바일 앱 / 카카오톡 중 무엇으로 할지 | resolved | 웹(Next.js)로 확정. 기술 스택 확정안 근거. |
| 2 | 실시간 음성(보이스피싱 재현)을 MVP에 포함할지, 텍스트 채팅으로 시작할지 | resolved | **v0.8 갱신**: 실시간 음성 통화형(브라우저 STT 입력 + 사기범 응답 TTS 자동재생) + 클론 오디오 인앱 재생(AC-019). 텍스트 입력은 폴백으로 유지. 전화망(PSTN) 연동은 여전히 Out of Scope. |
| 3 | 어르신 대상 자녀 대리설정 온보딩 플로우(자녀 보조 UI)를 MVP에 포함할지, 자녀가 UI를 어디까지 조작할 수 있는지 구체 권한 범위 | open | OQ-13 확정으로 **녹음은 본인 직접·자녀는 계정 생성 UI만 보조**라는 방식은 확정됐으나, 자녀 보조 UI를 Day1 빌드 스코프에 실제 포함할지와 자녀의 구체 조작 권한 범위는 여전히 미확정. 현재 태스크(T3)는 본인 온보딩만 포함. 사용자 확정 필요. **(2026-07-21) 발표 내러티브에서는 가족 원격 훈련을 강조하기로 확정했으나, 이는 빌드 스코프 결정과는 별개다 — 빌드 스코프상 자녀 보조 UI 포함 여부는 여전히 open.** |
| 4 | 기업 보안교육용 관리자 대시보드를 MVP에 포함할지 | resolved | 이번 스코프 제외(Out of Scope). B2B는 시장성 로드맵 방향으로만 언급. |
| 5 | "사기 방어 등급" 게임화 시스템의 구체 스펙(레벨 수, 점수 산정 방식) | open | P1(Day2 여유 시). 레벨 수·산정식 미확정. **(2026-07-21) 임시 플레이스홀더 구현 진행 중(T13), 최종 산정식은 여전히 미정.** |
| 6 | 수익모델(무료/유료/기업 라이선스) 구체안 | open | 지불주체(통신사·은행·CISO·지자체)·구독 확장 방향만 있고 구체 모델 미확정. |
| 7 | 1차 언어·타겟 시장 확정 | resolved | 한국어/한국으로 확정. |
| 8 | age-gate 최소 연령 기준값 | resolved | **사용자 확정(2026-07-21): 만 14세.** 국내 개인정보보호법상 만14세 미만 법정대리인 동의 기준선과 일치하며 별도 보호자 동의 플로우 없이 구현 최단. AC-014에 반영. age-gate 자체는 여전히 P1(값만 확정). |
| 9 | 턴 응답 지연 목표값(≤10초 p95 가정) 확정 | open | 가정값 유지, 정확값 미확정. |
| 10 | 민감한 세션 대화 기록의 보관·삭제·암호화 정책 | resolved | 생성물은 세션 종료 시 즉시 폐기·서버 미저장(AC-021), 대화 로그 PII 토큰화(AC-024). 토큰화 로그의 보관 기간 세부는 필요 시 추후 조정. |
| 11 | 어르신/자녀 대리설정 시 동의 주체와 데이터 소유권 | resolved | OQ-13 확정(녹음은 본인 직접·본인 확인 절차 통과·계정은 본인 소유)에서 논리적으로 따라 나옴: **동의 주체 = 본인(등록 대상자), 데이터 소유권 = 본인 계정 소유.** |
| 12 | 일정·예산 제약 | resolved | 일정 확정: Day1 하루 개발 완료, Day2 오전 신규 개발 금지, 최종 마감 Day2 오전 11시. (예산 총액은 OQ-14로 분리.) |
| 13 | **[충돌]** 차별점 "가족 원격 훈련"(자녀가 부모 계정 대신 설정)과 가드레일1 "본인 목소리만 등록"(AC-020)이 문면상 충돌한다. 자녀가 계정 UI 설정을 대행하는 것과, 목소리 녹음 자체는 반드시 본인(등록 대상자)이 직접 낭독해야 한다는 것이 같은 의미인가 다른 의미인가? | resolved | **사용자 확정(2026-07-21):** "본인이 직접 등록(녹음)하고, 자녀는 계정 생성 UI만 보조한다. 실제 녹음 버튼은 부모 본인이 누른다"로 확정(안전 기본값 채택). AC-020(본인 목소리만 등록)과 정합. 잔여 세부(자녀 UI 권한 범위)는 OQ-3, (동의 주체·소유권)은 OQ-11에서 처리. |
| 14 | **[예산]** 총 예산 및 API 크레딧 상한(ElevenLabs $5/월 외 LLM/인프라 포함) | open | **사용자 확정(2026-07-21): T1(ElevenLabs 클론 PoC) 착수 전 사용자가 직접 크레딧/rate limit을 확인한 뒤 진행하기로 확인 절차는 확정.** 단 구체 상한액 수치 자체는 여전히 미정이므로 open 유지(절차만 확정, 금액 미정). |
| 15 | **[신규]** 가족 원격 훈련에서 부모(본인)의 취약점 리포트를 자녀가 열람할 수 있는지 여부 | open | 차별점 2번 "리포트를 자녀도 확인" 서술에서 파생. OQ-13 결정(녹음은 본인 직접)에서 자동으로 따라 나오지 않으며, 자녀의 리포트 열람은 프라이버시·동의 재검토가 필요. 사용자 확정 필요. **(2026-07-21) 발표 내러티브에서는 가족 원격 훈련을 강조하기로 확정했으나, 이는 빌드 스코프 결정과는 별개다 — 빌드 스코프상 자녀의 리포트 열람 가능 여부는 여전히 open.** |
| 16 | **[메신저 확장·핵심]** 메신저→보이스 에스컬레이션의 "사용자 의심" 트리거 감지 방식 | resolved | **최신 확정(2026-07-23, v0.10): LLM 판단 + 구조화 신호 + 폴백.** 대화를 연기하는 LLM이 사용자의 의심/거부/확인 시도를 캐릭터로 감지해 **구조화 신호(function-call 또는 sentinel 토큰)**를 내보내고 앱이 그걸 읽어 화면·음성 세션을 전환한다(앱은 자유텍스트를 직접 분류하지 않음). 폴백: max-turn 도달 시 전환 또는 명시 버튼 노출. 인젝션 방어(사용자 입력=데이터)·구조화 신호만 신뢰 원칙 불변. **경위**: v0.9.1에서 하이브리드(명시+고정 키워드, LLM 제외)로 확정했으나, 사용자 최신 결정으로 LLM 판단 방식으로 재확정(v0.10). AC-034 갱신. 구조화 신호 스키마·세션 접합은 architect(T26). |
| 17 | **[메신저 확장]** 기기 구분(iOS 기본 메시지 / 삼성 메시지 UI) 결정 방식·MVP 범위 | resolved | **사용자 확정(2026-07-23): 자동 감지 + 스킨 채택.** 접속 기기(User-Agent 등)를 감지해 iOS 기본 메시지 / 삼성 메시지 스킨을 자동 적용한다. **감지 실패·데스크톱은 기본 스킨으로 폴백**하고 **수동 전환 옵션**을 제공한다. UA 감지 신뢰도의 구현 한계·판정 로직은 architect가 T26에서 확정(여기선 요구사항·폴백만 규정). AC-031에 반영. |
| 18 | **[메신저 확장·법적]** 카카오톡 등 실 메신저 UI 재현 수준(정확 모사 vs 브랜드 비특정 추상화) | resolved | **사용자 확정(2026-07-23): 정확 모사 채택 — 단, 상표·저작권 리스크를 인지한 상태의 결정(accepted risk).** Assumptions·Risks에 "리스크 인지 후 채택"으로 명시. **사기 콘텐츠 자체의 무해화 원칙은 불변**(실 악성 URL·실계좌·실 기관명 금지, AC-005/AC-032 유지) — UI 외형만 정확 모사하고 사기 내용은 여전히 약화·모의다. 모든 표면에 "AI 훈련용 모의" 상시 표식 유지. **잔여(architect 위임)**: 상표 리스크 완화책(배포 채널 제한·면책 고지 등)과 별도 ADR 필요 여부는 **OQ-24**로 신설(architect 판단). |
| 19 | **[메신저 확장]** 메신저피싱 MVP의 표면·시나리오 범위(카카오톡형+문자형 둘 다 vs 하나부터, 시나리오 몇 종) | resolved | **사용자 확정(2026-07-23): 표면 넓게 채택.** MVP에 **카카오톡 + 문자메시지(iOS/삼성 스킨) 둘 다** 포함, **메신저 시나리오 복수 종** 포함, **메신저→보이스 에스컬레이션 완결**까지. MVP Scope #14/#17·T24/T27/T29 범위를 이에 맞춰 확대. AC-030에 반영. |
| 20 | **[메신저 확장·일정]** 이 확장의 개발 일정·마감 — 원래 "하루 완성(Day1/Day2 해커톤)" 절대 제약이 확장에도 적용되는지, 별도 후속 스프린트인지 | resolved | **사용자 확정(2026-07-23): 일정 제약 삭제, 장기 프로젝트로 전환.** 해커톤 Day1/Day2 시간 압박은 **해당 없음**. 태스크는 선행 의존성 순서만 따르고 날짜/스프린트 제약 없음. Constraints의 "절대 마감" 문구 갱신, Tasks.md "일정 미부여" 상태 해제. ⚠️ `docs/DECISIONS.md`의 해커톤 일정 항목 정리는 architect 위임(planner 소유 아님). |
| 21 | **[메신저 확장·안전]** 메신저 "링크 클릭"을 어떻게 재현할지 | resolved | **사용자 확정(2026-07-23): 인앱 가짜 랜딩 목업 채택(후보 a).** 링크 탭 시 외부 이동 없이 앱 내 가짜 로그인/정보입력 랜딩으로 전환, 입력값은 서버 미전송·UI상 "입력함" 가짜 피드백. 실 브랜드/기관명·실 URL 없이 무해화, 모의 표식 노출. AC-045 신설. |
| 22 | **[메신저 확장]** 역방향(보이스→메신저) 에스컬레이션 지원 여부 | resolved | **사용자 확정(2026-07-23): 설계는 양방향, 구현은 순차.** 전이 엔진을 방향 무관(direction-agnostic)하게 설계하되, MVP 구현은 메신저→보이스를 먼저 완결하고 보이스→메신저는 배선 검증 후 fast-follow(후속 태스크 분리). AC-039에 반영. |
| 23 | **[메신저 확장]** 에스컬레이션의 보이스 단계에서 사용할 음성 모드(clone/generic) | resolved | **사용자 확정(2026-07-23): 조건부 clone + 목소리 선택 서브플로우.** 메신저 시나리오 선택 시 "통화로 이어질 수 있어 녹음이 필요할 수 있다" 안내 → (1) 즉시 녹음 / (2) 기존 목소리 재사용(복수 시 선택) / (3) 최종 폴백으로 남·여 기본 보이스 선택. AC-046 신설. 안내 문구 위치·녹음 진입 화면 UX 세부는 ux-design(T23 계열) 위임. |
| 24 | **[메신저 확장·법적]** 카카오톡 정확 모사(OQ-18 확정) 채택에 따른 상표 리스크 완화책·별도 ADR 필요 여부 | resolved | **사용자 확정(2026-07-23): 상시 면책 고지 채택.** "카카오톡 실제 서비스와 무관한 훈련용 재현" 상시 노출(AC-047). **마케팅/스토어 노출 제한은 채택 안 함**(면책 고지만). **잔여(architect 위임)**: 면책 고지가 상표 리스크에 충분한지 법적 판단·DECISIONS.md ADR 등재 필요 여부는 architect가 판단(planner 소유 아님). |
| 25 | **[2인 소셜·안전]** 복제 음성의 챌린지 스코프·보존·삭제 정책 | resolved | **사용자 확정(2026-07-23): 기간제 보존.** 사용자 설정 기간(예 7/30일, 기본값 architect 확정) 후 자동 삭제 + 수동 삭제 가능(AC-041). **잔여(architect 위임)**: 스코프 고정 방식(서명 URL·서버 재생 전용 등)·추출 차단 구조·기본 보존기간 값은 architect(T35)가 확정. planner는 정책(AC-041)만 규정. |
| 26 | **[2인 소셜·안전]** 챌린지 링크 토큰 인증·만료 정책 | resolved | **사용자 확정(2026-07-23): 단순 토큰 + 만료 + 1회성, 사용자2 로그인 불필요**(AC-048). **무료 티어 링크 만료 = 3일(v1.1 확정)**, 유료 연장. **잔여(architect 위임)**: 토큰 생성·검증 스키마는 architect(T35)가 확정. planner는 정책(AC-048)만 규정. |
| 27 | **[2인 소셜·안전]** 오용 방지(대량 생성·타깃 무동의) 정책 | resolved | **사용자 확정(2026-07-23): planner 추천안 채택** — (1) 사용자1당 활성 챌린지 개수 제한(예 동시 3개)+만료, (2) 사용자2 "원치 않는 챌린지" 신고 기능. **관리자 수동 검토 미채택**(AC-049). **잔여(architect 위임)**: 신고 데이터 모델·개수 상한값 상세는 architect(T35)가 확정. planner는 정책(AC-049)만 규정. |
