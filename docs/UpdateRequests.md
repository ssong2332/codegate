# Documentation Update Requests — {{project-name}}

Owner: docs (see AGENTS.md). Appended when docs finds a document it doesn't own has drifted from the implementation. docs never edits the target document directly — it logs the drift here so the owning agent can act on it, even across sessions.

| # | Date | Target Document | Section | Stale Content | What the Code Actually Shows | Owning Agent | Status |
|---|---|---|---|---|---|---|---|
| 1 | {{YYYY-MM-DD}} | {{docs/PRD.md}} | {{section}} | {{...}} | {{...}} | {{planner}} | open |
| 2 | 2026-07-21 | docs/Architecture.md | §6.5 인젝션 방어(구조적 분리), 표 "인가(rules)" 행 | "시스템 프롬프트(페르소나+약화수법+가드레일)는 Functions가 scenarioId로 서버에서 조립"하되 "페르소나/수법 원문은 `scenarioPrompts/{id}`(클라 읽기 거부, Functions만 읽음)에 저장"이라고 서술 — Functions가 런타임에 Firestore `scenarioPrompts/{id}` 문서를 읽는다는 뉘앙스. | `functions/src/roleplay/openingLine.ts`(3-9행)와 `functions/src/roleplay/index.ts`(15, 49행)는 `functions/src/scenarios/index.ts`의 컴파일된 TS 상수 `SCENARIO_PROMPTS`를 직접 import해서 쓰며, Firestore를 런타임에 읽지 않는다. `functions/src/scenarios/seed.ts`가 `scenarioPrompts` 컬렉션에 쓰기만 할 뿐 어떤 콜러블도 그 컬렉션을 읽지 않는다(`functions/src/scenarios/index.ts` 1-3행 자체 주석도 "Firestore 문서를 통해 소비"라고 적어 실제 구현과 어긋남). 클라이언트가 프롬프트를 못 보는 보안 목표(파일이 functions/src/에만 있고 firestore.rules도 deny)는 여전히 만족하지만 데이터 소스 서술이 실제와 다르다. docs/adr/0004-llm-injection-defense-and-pii-masking.md(16, 23행)도 동일한 서술을 포함. | architect | resolved |
| 3 | 2026-07-21 | docs/Database.md | `sessions/{sessionId}` 스키마 표(32-47행) | `SessionDoc`의 필드 표에 `voiceProvider`, `llmProvider`가 없음. | `functions/src/shared/types.ts`의 `SessionDoc`에 `voiceProvider?: VoiceProviderName`(51행, T19 추가)과 `llmProvider?: LlmProviderName`(57행, T7 추가) 옵셔널 필드가 실제로 존재하며 mock/실연동 추적 용도로 쓰인다(`functions/src/roleplay/index.ts` 111행에서 `llmProvider: "mock"` 기록). `ArtifactDoc`에도 동일한 목적의 `voiceProvider`(80행)가 있어 Database.md의 artifacts 스키마도 함께 확인 필요. | architect | resolved |
| 4 | 2026-07-21 | docs/API.md (§Trigger Functions, `onSessionEnded` 처리 ①, 90행); 참고: docs/adr/0003-immediate-artifact-purge.md (Decision 12행, Follow-ups required 23행 — ADR은 accepted 후 불변이므로 architect가 직접 편집하지 않고 후속 ADR/보완 기록으로 처리해야 함) | `onSessionEnded` 트리거 처리 절차 / ADR-0003 Decision·Follow-ups | "생성물 폐기 시 `sessions/{sid}/artifacts` 서브컬렉션을 매니페스트로 순회 → Storage `users/{uid}/sessions/{sid}/**` 삭제 대상 식별"이라고 서술. | `functions/src/guardrails/purge.ts`(T10, 15-17행 `sessionStoragePrefix`, 28-38행 `PurgeDeps.listStorageFiles` 주석)를 직접 읽어 재검증함 — artifacts 서브컬렉션은 전혀 read하지 않고, Storage `users/{uid}/sessions/{sid}/` prefix를 `bucket.getFiles({prefix})`로 실제 나열해 삭제 대상을 식별한다. 사유(코드 주석·T5/T10 구현 보고서로 교차 확인): (1) `artifacts` 서브컬렉션은 T5의 `synthesizeDeepvoice`가 Storage에 실제 파일을 쓰지 않고 Mock data URI만 반환해 실제로 한 번도 write된 적이 없어 항상 비어 있음. (2) Database.md의 `artifacts` 스키마는 애초에 합성 오디오/이미지 메타데이터만 다루도록 설계돼 있어 원본 녹음 `voice_input.webm`은 그 매니페스트로 절대 식별할 수 없었음. T10 reviewer는 이 이탈이 원안(ADR-0003대로 매니페스트 순회)보다 더 나은 설계라고 판단함(원안대로였다면 voice_input.webm이 영구히 삭제되지 않는 실질적 AC-021 위반이 발생했을 것) — architect 승인/ADR 갱신 필요, docs는 코드를 임의로 문서에 맞추거나 문서를 임의로 코드에 맞출 권한이 없어 등재만 함. 나머지 설계(ElevenLabs voice 삭제, deletionLogs 기록, target별 부분 실패 처리)는 코드와 API.md/ADR-0003이 일치함. | architect | resolved |
| 5 | 2026-07-27 | docs/Architecture.md | §15.3.3 "확장 경로(코드 변경 없이 설정만)" (1010행) | "`ELEVENLABS_AGENT_IDS`의 항목 형식을 `scenarioId@difficultyLevel:agentId`까지 허용하도록 `parseAgentMap`을 확장"하는 작업을 **"코드 변경 없이 설정만"** 으로 서술. | `functions/src/realtime/agentMap.ts:20-34`의 `parseAgentMap`은 `entry.indexOf(":")` 한 번으로 `scenarioId:agentId`만 파싱하며 `@`를 전혀 인식하지 않는다(형식 주석 13-14행도 `scenarioId:agentId`만 규정). `@difficultyLevel`을 실제로 지원하려면 파서 확장 + `functions/src/realtime/provider.ts`의 선택 로직 + `difficultyApplied` 반환값 결정까지 **전부 코드 변경**이 필요하다 — "설정만"이라는 서술을 그대로 믿고 착수하면 범위를 오판한다(T99 태스크 행에서 동일 사실을 이미 실측·기록했으나 §15.3.3 본문은 아직 정정되지 않음). | architect | resolved |
| 6 | 2026-07-27 | docs/Tasks.md | T85·T86·T98·T100 행의 `상태` 열 | T85·T86 행은 `review`, T98 행은 `review`(주석에 "브랜치 `fix/T98-gemini-thinking-budget`, **main 미병합**, 2026-07-26 grep 실측: `functions/src/llm/geminiClient.ts`에 `thinkingConfig`/`thinkingBudget` 문자열 0건"), T100 행은 `todo`(주석에 "담당 배분을 사용자가 확정하기 전에는 착수하지 말 것")로 기록돼 있다. | 이 워크트리(main 기준)에서 직접 확인한 결과 `functions/src/roleplay/l3Depth.ts`(T85 산출물)·`functions/src/scenarios/__tests__/harmlessnessPatterns.ts`(T86 산출물)가 실재하고, `functions/src/llm/geminiClient.ts`에 `thinkingConfig`/`thinkingBudget`(T98 산출물)이 이미 존재하며, `.githooks/pre-commit`·`.githooks/pre-push`(T100 산출물)와 `docs/GitWorkflow.md`·`docs/PromptRules.md`의 T100 갱신 내용도 이미 반영돼 있다 — 네 태스크 모두 코드·문서가 이미 main에 병합된 상태인데 Tasks.md 상태 열은 이를 반영하지 않는다. | planner | resolved |
| 7 | 2026-07-27 | docs/Tasks.md | T107·T109·T110 행의 `상태` 열 | T107 행은 `review`(implementer 구현 완료 주석), T109 행은 `todo`(주석 "등재만 완료(planner) — 착수 전"), T110 행은 `review`(implementer 재작업 완료 주석, reviewer/QA 라운드 기록은 있으나 최종 병합 확인 없음)로 기록돼 있다. | 이 워크트리(main 체크아웃 `C:\codegate`)에서 직접 확인한 결과 세 항목 모두 산출물이 main에 실재한다 — ① T107: `functions/src/scenarios/__tests__/mockScreens.test.ts:607-753`에 `[AC-079]`/`[AC-079 (c) 역검증]`/`[AC-079 (b)]` 테스트가 있고 `docs/PRD.md:10-28`에 AC-079 v1.9 신설 기록이 있다. ② T109: `functions/src/scenarios/institutionalImpersonation.prompt.ts:19-70`에 "T109" 명시 주석과 함께 확인 질문이 "한 차례에 하나씩"으로 분리된 프롬프트 본문이 있고, `functions/src/scenarios/__tests__/scenarios.test.ts:958-1186`에 `[T109/요구몰림]`·`[T109/역검증]`·`[T109/오탐]` 게이트가 전부 존재한다(카드사·택배통관·대환대출·환급금 4개 시나리오도 동일 패턴 확인). ③ T110: `functions/src/scenarios/verifyIntercept.ts:13,22,27` 주석이 "`displayNumber` 필드도 [T110에서] 사라졌다"고 명시하고, 실제로 `deskLabel`/`reconnectedCallerLabel`만 남아 있으며 `displayNumber` 필드가 카탈로그 6종 어디에도 없다 — `docs/Tasks.md` T110 행이 보고하는 최종 테스트 수치(functions 508 / 루트 173)도 `CLAUDE.md` Verified Commands 표의 현재 기준선(측정 커밋 `dc67c49`)과 정확히 일치한다. 세 항목 모두 코드·테스트가 이미 main에 병합된 상태로 보이는데 상태 열은 이를 반영하지 않는다(PR 번호·리뷰어/QA 최종 판정 자체는 이 세션에서 git 이력으로 재확인하지 못했다 — 파일 실측 근거만이다). | planner | **resolved — planner 정정 완료(2026-07-27, base main `65cfdf6`에서 파일 실측).** 세 행의 상태 열을 전부 `merged`로 바꿨다: **T107** `review`→`merged`(근거 `functions/src/scenarios/__tests__/mockScreens.test.ts:688`의 `[AC-079]` 게이트) · **T109** `todo`→`merged`(근거 `functions/src/scenarios/institutionalImpersonation.prompt.ts:20-21`·`:38`·`:66`·`:70` 프롬프트 재작성 + `functions/src/scenarios/__tests__/scenarios.test.ts:1115`·`:1133`·**`:1146`(역방향 확인)**·`:1180`·`:1186` 게이트) · **T110** `review`→`merged`(근거 `functions/src/scenarios/verifyIntercept.ts:11`·**`:22`(`displayNumber` 제거 명시)**·`:49`·`:64`·`:72`). 이전 상태 문구는 세 행 모두 *"이하 이전 상태 기록(원문 보존)"* 뒤에 원문 그대로 남겼다(T95·T104 갱신 선례). **⚠️ 범위 확대 고지**: 이 행은 T107·T109·T110만 지목했으나 planner가 **같은 패스에서 T117도 병합 상태로 정정**했다 — 그 사실은 아래 **행 #8**이 독립적으로 등재하고 있으므로 두 행은 중복이 아니라 **동일 패스의 분할 등재**다. **⛔ planner가 확인하지 못한 것**: PR 번호·reviewer/QA 최종 판정·병합 커밋. planner에 셸이 없어 `git log`를 실행하지 못했고 `.git/logs/refs/heads/main`에는 `pull --ff-only` 기록만 남아 병합 커밋 메시지가 없다 — **이 행의 등재 문구가 고지한 한계와 동일하며, 파일 실측 근거만으로 판정했다.** **함께 정정한 것(부분 정정 방지)**: T87 행의 재개 조건 ① 충족 표기, 공통 기준값 주석 2곳. **미정정 2건(다음 패스 인계 — 위치는 줄 번호가 아니라 행 ID로 지시한다)**: (1) `95cc684` 기준 `470/171`을 인용하는 5곳(**T103·T106·T108 행 + 섹션 서두 주석 2곳**) — 정정하려면 재측정이 선행돼야 하는데 planner에 셸이 없다, (2) **T114 행**의 *"`fix/T110-warm-transfer` 브랜치에만 있어 파일:줄 미실측"* — T110 병합으로 이제 실측 가능해졌으나 이번 위임 범위 밖이다. |
| 8 | 2026-07-27 | docs/Tasks.md | T117 행의 `상태` 열 | 상태 `todo — 사후 등재(planner). 구현은 이미 진행 중이다 — 브랜치 `fix/T98-regression-thinking-config`(오케스트레이터 보고)`로 기록돼 있고, 본문 B'항도 여전히 옛 코드(`GEMINI_TEXT_MODEL = "gemini-flash-latest"`, `GEMINI_THINKING_BUDGET = 0`, `isThinkingConfigRejected()`)의 파일:줄을 인용한다. | main(`C:\codegate`, HEAD `65cfdf6`, PR #102 병합 — 오케스트레이터 보고를 docs가 파일로 재확인)의 `functions/src/llm/geminiClient.ts`를 직접 열람한 결과: `GEMINI_TEXT_MODEL = "gemini-3.6-flash"`(36행, 고정 버전)이고, `thinkingConfig`/`thinkingBudget`/`isThinkingConfigRejected` 문자열은 전부 주석(52-74행, 삭제 근거 설명)에만 남아 있을 뿐 실행 코드에는 0건이다(`complete()` 본문 81-99행에 `config: { systemInstruction: input.systemPrompt }`만 있고 try/catch 재시도 로직이 없다) — T117이 요구한 수정(`thinkingConfig` 제거 + 모델 고정)이 이미 main에 반영된 상태인데 Tasks.md 상태 열·B'항 인용은 병합 전 상태를 그대로 서술한다. | planner | **resolved(부분 — 아래 고지 필독) — planner 정정 완료(2026-07-27, base main `65cfdf6`에서 파일 실측).** T117 상태 열 `todo`→`merged`. **근거(planner 직접 실측)**: `functions/src/llm/geminiClient.ts:36` `GEMINI_TEXT_MODEL = "gemini-3.6-flash"`(부동 별칭 제거) · `functions/src` **전수 grep에서 `thinkingConfig`/`thinkingBudget` 코드 참조 0건**(잔여 히트는 `geminiClient.ts:52-73`의 삭제 근거 주석과 `functions/src/llm/__tests__/geminiClient.test.ts:130-148`의 *"싣지 않는다"* 회귀 테스트뿐) · 모델 고정 게이트 `geminiClient.test.ts:160-161`(`assert.equal(GEMINI_TEXT_MODEL, "gemini-3.6-flash")` + `latest` 포함 금지 단언)·`:167-168`(실제 요청 URL 확인). 이전 상태 문구는 *"이하 이전 상태 기록(원문 보존)"* 뒤에 원문 보존. **⚠️ 부분 해소 고지 — 이 행이 지목한 두 가지 중 하나만 고쳤다**: **(1) 상태 열 = 정정 완료.** **(2) B'항의 옛 코드 파일:줄 인용(`gemini-flash-latest` · `GEMINI_THINKING_BUDGET = 0` · `isThinkingConfigRejected()`) = 의도적으로 고치지 않았다.** 근거: T117 행은 **장애 보고서**이고 B'항은 *"장애 당시 코드가 이랬다"* 는 **과거 기록**이라, 이 저장소가 세워 온 관례(과거 기록 소급 재작성 금지 — 선례 T86 행의 `427`·MVP #28·T98 행 무수정)상 재작성 대상이 아니다. 고치면 그 행이 근거로 제출한 증거의 문면이 사후에 달라진다(T107 D항이 명문화한 원칙). **대신 현재 코드의 파일:줄을 상태 열에 새로 적어** 독자가 옛 인용을 현재 상태로 오해하지 않게 했다. **이 판단을 뒤집어 B'항을 갱신하려면 사용자·오케스트레이터 확정이 필요하다.** **⛔ planner가 확인하지 못한 것**: PR #102 병합 커밋 자체(셸 부재 — 행 #7과 같은 사유), 그리고 **T117 G항 ①~⑥의 라이브 증거**(`result.isMock === false` 등). **소스 상태는 라이브 복구를 증명하지 않으므로 T87 재검증 요구는 그대로 유효하다.** **⚠️ 기준선 문구에 대한 판단(근거 기록)**: `CLAUDE.md` Verified Commands 표가 그 사이 **508 / 173 · 측정 커밋 `65cfdf6`** 으로 갱신됐음을 직접 열람해 확인했다. 그 결과 planner가 `docs/Tasks.md`에 적어 둔 *"`dc67c49` 이후 3커밋 전진했으니 508/173을 오늘 값으로 단정하지 말 것"* 경고는 **지금은 과하다.** **그럼에도 고치지 않았다** — 이유 3건: (a) 오케스트레이터가 이 패스의 편집 경계를 `docs/UpdateRequests.md`로 한정했고 `docs/Tasks.md`는 이미 커밋됐다, (b) *"변동 없음(508/173)"* 은 **오케스트레이터 실측이고 planner는 셸이 없어 재현하지 못했다** — 재현하지 않은 값을 근거로 경고를 지우면 그것이야말로 근거 없는 단정이 된다, (c) 그 경고의 운영 지시(*"착수 시 재측정하라"*)는 지금도 옳다. **문구 완화 여부는 사용자·오케스트레이터 판단 사항으로 남긴다.** |

## Rules
- Rows are appended by docs only; docs never rewrites or deletes a past row.
- The Status column is the one exception: the named Owning Agent (planner/ux-design/architect) may update it from `open` to `resolved` once they've acted on the row — mirrors implementer's narrow Status-only exception on docs/Tasks.md.
- Everyone else is read-only.
