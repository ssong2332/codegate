# UX — 안 당해본 사기는 못 막는다 (AI 금융사기 백신)

Owner: ux-design (see AGENTS.md). Others read-only.
Update this document in place — do not recreate it from scratch. Preserve existing sections unless explicitly superseded by a newer decision. The **UX Decision Log** is append-only — never rewrite or delete a past entry; append a new one if a decision changes.
Flow IDs and Screen IDs are immutable once assigned — never renumber existing IDs, even if one becomes Deprecated. New flows/screens always get the next available ID.

## Overview
| Item | Value |
|---|---|
| Document Version | 1.11 |
| Based on PRD Version | v1.4 · 2026-07-24 (T50 체험/발송 선택 재배치 + generic 보이스 챌린지 신설 AC-056/057/058; 이전 반영 유지) — **⚠️ v1.11이 설계한 신규 기능 4건(통화 중 문자 오버레이·즉시 되감기·난이도 단계·실패 아카이브)은 PRD v1.4에 대응 AC가 아직 없다(Change Workflow의 planner 단계 미선행). 각 Flow는 재사용되는 기존 AC로만 추적을 걸었고, 신규 능력 부분의 AC 신설은 planner 소관 — 추적성 갭은 OQ-U15.** |
| Last Updated | 2026-07-25 |

이 문서는 PRD v0.8의 **P0 핵심 루프(예방접종 루프)**에 대한 사용자 플로우·화면·인터랙션 스펙을 정의한다. 채널은 웹(Next.js) 단일, 언어는 한국어, 상호작용은 **실시간 음성 통화형 역할극(브라우저 STT 입력 + TTS 자동재생, v1.3 갱신 — 텍스트는 폴백)** + 클론 오디오 인앱 재생이다. 설계는 **하루 완성(Day1) 제약**을 최우선으로 반영해 화면 수와 상태 복잡도를 최소화했다(UX Decision Log D-1 참조).

**변경 이력 v1.10 → v1.11 (2026-07-25 — 사용자 승인 신규 기능 4건: 통화 중 문자 오버레이 · 즉시 되감기 · 난이도 단계 · 실패 아카이브):** 사용자가 라이브 앱을 직접 쓰며 제기한 요구 4건을 **한 번의 패스로** 설계한다(전부 같은 문서라 분리 시 충돌). 기존 화면·플로우는 **삭제·재번호 없이** 유지하고, 신규 Screen ID **UX-027~UX-030**·신규 Flow ID **UF-008~UF-010**을 부여했다(직전 최대 UX-026·UF-007). 반영 내용:
- **(1) 통화 중 문자(SMS) 오버레이 — UX-027 신설 · UF-008 신설 · D-35~D-38)**: 사용자 요구 원문 — *"저금리 대출 시나리오를 진행할때, 돈을 요구하고 계좌를 알려주는 것까지는 좋은데, 이걸 문자 메세지로 링크 형태로 보내고, 문자로 전환되는데, 전화 연결은 유지되도록하는 ux 경험이 있으면 좋을 거 같아"*. **설계 전제(코드로 확인된 제약)**: `/session/play`(통화)와 `/session/messenger`(메신저)는 별도 라우트라 이동하면 통화 컴포넌트가 언마운트되고 실시간 세션·마이크가 끊긴다 → **라우트 이동 없이 통화 화면 위에 겹쳐 뜨는 오버레이**로만 설계한다(D-35). 통화 셸(UX-014)에 문자 도착 배너·"문자함" 재열기 컨트롤을 **신규 state로** 추가하고(신규 ID 아님), 문자 본문을 보는 **UX-027**을 오버레이 화면으로 신설한다. 오버레이가 떠 있어도 통화 타이머·발신자·"AI 훈련용 합성" 표식·"훈련 종료"는 그대로 살아 있어 **통화가 끊기지 않았음이 시각적으로 계속 보인다**(P-20). 오버레이 안의 링크는 **기존 인앱 가짜 랜딩 UX-023(AC-045)을 그대로 재사용**한다(신규 랜딩 미설계 — UX-023은 이미 라우트가 아닌 in-page 오버레이 컴포넌트로 구현돼 있어 통화 위에서도 성립, D-37). 사기범이 "문자 보냈어요"라고 말하는 **그 턴에 실린 구조화 마커**로 도착을 트리거해 발화와 도착을 같은 사건으로 묶는다(기존 `[[LINK:]]`·sentinel 패턴과 동형, UX는 자유텍스트를 분류하지 않음 — AC-024 원칙 유지, D-36).
- **(1-b) 같이 해결되는 사용자 신고 — "화면에 뜨는 인증번호를 불러달라는데 아무것도 보이는 게 없다"(D-38)**: 인증번호(OTP) 문자를 **UX-027의 콘텐츠 종류 하나**로 처리한다(신규 화면 없음). 사기범이 "방금 문자로 간 인증번호 불러 주세요"라고 하면 실제로 오버레이에 6자리 모의 코드가 도착해, 사용자가 "그걸 불러주는 순간이 곧 속는 순간"임을 체감할 수 있다. 코드·발신번호·본문은 전부 무해화 모의값이며 실제 인증 체계와 무관하다(AC-032/033 계승).
- **(2) 통화 후 즉시 되감기 — UX-028 신설 · UF-009 신설 · D-39/D-40)**: 사용자 요구 — *"종료 직후 '이 순간 뭐라고 했어야 했나'를 그 자리에서 한 턴만 다시 해보기"*. 기존 **UX-018(리플레이 해설)과 명확히 구분**한다: UX-018은 대화를 **읽으며 주석을 보는 복기**(입력 없음), UX-028은 그 턴으로 되돌아가 **사용자가 다시 답을 하고 즉시 판정을 받는 재연 드릴**(입력 있음). 데이터 원천은 리포트의 `deceivedMoments`(속은 시점·수법·올바른 대처)이며, **`deceivedMoments`가 비어 있으면(안 속은 세션) 되감기 진입점을 띄우지 않고** UX-018로 안내한다 — "아깝게 넘어갈 뻔한 순간(near-miss)" 같은 개념은 현재 분석 산출물에 존재하지 않으므로 **발명하지 않는다**(D-40, 신설 여부는 OQ-U20/R-8).
- **(3) 난이도 단계 — UX-029 신설 · UF-003 확장 · D-41~D-43)**: 사용자 요구 — *"초급은 티가 나는 수법, 고급은 실제처럼 정교하게"*. 지금 시나리오 카드의 "난이도: 쉬움~중간"은 **시나리오마다 고정된 설명 텍스트**(`src/content/scenarios/*.ts`의 `difficulty` 문자열)라 사용자가 고를 수 없다. 이를 **사용자가 고르는 축**으로 바꿔, 드릴다운의 **시나리오 선택 직후·세션/챌린지 생성 직전**에 난이도 선택 화면 **UX-029(초급/중급/고급)**를 넣는다(D-41). **난이도는 어떤 안전장치도 게이팅하지 않는다** — 사전 고지·동의·상시 종료·합성 표식·종료 고지·2인 안전제약은 난이도와 무관하게 동일하다(스킨=프레젠테이션 전용이라 어떤 안전 판정도 게이팅하지 않는다는 §13.4 원칙과 동형, D-42). 초급의 "힌트"는 **세션 시작 전 사전 브리핑 카드**로 제공하고 **대화 중 실시간 판정 표시는 하지 않는다** — D-6("속은 시점 판정은 리포트 단계에서만")을 뒤집지 않는다(D-43; 실시간 힌트 도입 여부는 R-7).
- **(4) 실패 아카이브 — UX-030 신설 · UF-010 신설 · D-44/D-45)**: 사용자 요구 — *"내가 당했던 순간만 모아 보는 화면(반복 학습)"*. 기존 `/history`(UX-012)는 **세션 목록**이지만 이 화면은 **세션이 아니라 '속은 순간' 단위**로 모은다(같은 세션에서 3번 속았으면 3개 항목). 그래서 UX-012의 확장이 아니라 별도 신규 화면으로 설계한다(D-44). 정렬은 **최신순 기본 + 수법별 묶기**만 두고 검색·필터·통계 대시보드는 만들지 않는다(요청되지 않은 기능 = 스코프 크립, D-45). 항목에서 "그때 대화 보기"(UX-018 해당 시점)와 "이 시나리오 다시 훈련"(UX-029 난이도 선택 경유)로 **재훈련 동선**을 연결한다.
- **의사결정**: D-35~D-45 append(기존 D-1~D-34 무변경, supersede 없음 — 이번 4건은 기존 결정을 뒤집지 않고 얹는다). **특히 D-6(실시간 판정 미표시)·D-2(즉시 종료)·D-11/D-12(통화 셸 연속성)·P-15(무동의 차단)는 그대로 유지된다.**
- **인터랙션 패턴**: P-20(통화 유지 오버레이)·P-21(되감기 드릴)·P-22(난이도 표기 일관성)·P-23(속은 순간 카드) 신설.
- **안전 게이트 무변경(확인)**: 상시 종료(AC-006)·사전 고지(AC-012)·AI 합성 표식(AC-022)·2인 챌린지 안전제약(AC-040/042/043)을 약화시키는 설계는 이번 패스에 **없다**. 오히려 UX-027(오버레이 내에서도 "훈련 종료"·모의 표식 상시)·UX-029(고급 선택 시 심리적 부담 사전 고지, 2인 챌린지는 UX-021 동의 문구에 난이도 명시)로 **고지 면을 넓혔다**.
- **architect 인계 flag(ux-design 소관 아님)**: ① **문자 오버레이의 상태 관리·신호 경로** — 오버레이가 떠 있는 동안 실시간 세션(오디오 컨텍스트·STT·타이머)을 유지하는 방식, 문자 도착 신호를 서버가 어떤 구조로 실어 보낼지(`attachments` 유사 구조 재사용 vs 신규 필드), 오버레이 열림 중 자동청취 게이팅(**OQ-U16**). ② **되감기 재연의 백엔드 모델** — 한 턴 재실행을 원 세션 재개로 볼지 별도 연습 실행으로 볼지, 원 리포트(AC-007 "세션당 정확히 1리포트") 오염 방지, 사용자 새 답변의 판정 주체(**OQ-U17**). ③ **난이도의 데이터 모델** — 시나리오별 3종 프롬프트 vs 공통 난이도 모디파이어 블록, 세션·챌린지에 난이도를 싣는 필드, 기존 고정 `difficulty` 문자열과의 관계(**OQ-U18**). ④ **아카이브의 쿼리 방식** — `reports` 전수 조회 후 클라 평탄화 vs `deceivedMoments` 역정규화 인덱스, 페이지네이션(**OQ-U19**). **스키마·콜러블·상태관리 설계는 architect(다음 단계).**
- **범위 경계**: 코드·스키마는 작성하지 않았다(ux-design 역할 범위). 기존 Deprecated 항목(UX-004/005/006)은 손대지 않았고, 이번 패스에서 새로 Deprecated로 옮긴 화면·플로우는 **없다**. MVP #21(에스컬레이션 가능 메신저 챌린지)·OQ-28은 여전히 범위 밖.

**변경 이력 v1.9 → v1.10 (2026-07-24, PRD v1.4 · T54 — AC-056/057/058 체험/발송 선택 상향 + generic 보이스 챌린지):** 사용자가 라이브 앱을 직접 테스트해 T50(planner) 기획이 아직 구현 반영되지 않았음(앱이 여전히 T48/T49 배치대로 동작)을 확인함에 따라, Change Workflow(Feature addition → Planner[T50 완료] → **UX-design[T54]** → Architect → …)의 ux-design 단계로 **PRD v1.4 AC-056/057/058을 화면·흐름으로 재설계**한다. 핵심은 **"본인이 체험 / 지인에게 보내기" 선택 지점을 시나리오 선택 직후(D-30 배치)에서 유형 선택 직후(UX-015 직후)로 상향**하는 것 — 즉 UX-026이 이제 **목소리 방식 선택(UX-016)·시나리오 선택(UX-017/UX-024)보다 먼저** 온다. **UX-026은 신규 ID를 만들지 않고 위치만 상향(재배치)한다**(ID 불변 규약 — "본인이 체험/지인에게 보내기"라는 화면의 의미는 그대로이고 흐름상 위치만 앞당겨짐). 반영 내용:
- **(체험/발송 선택 상향, AC-056 · D-31)**: UX-015(유형 선택) 직후 **채널 무관 단일 화면 UX-026**에서 "본인이 체험 / 지인에게 보내기"를 먼저 고르고, 그 결과가 이후 노출될 시나리오 집합과 (보이스의) 목소리 방식 노출 여부를 결정한다. 보이스·메신저 두 유형 모두에 적용. D-30의 "별도 챌린지 만들기 진입점 은퇴·통합 선택 지점" 원칙은 유효하게 계승하되, **위치만 시나리오 선택 직후 → 유형 선택 직후로 이동**. D-30은 삭제하지 않고 Status를 "위치 부분 supersede by D-31"로 표기(append-only 규약).
- **(보이스+본인이 체험 = generic 전용·clone 배제·UX-016 스킵, AC-057 · D-32)**: 보이스 유형에서 "본인이 체험"을 고르면 **generic 시나리오만** 노출하고 clone(본인 목소리 복제) 시나리오는 자기 체험에서 완전히 배제한다. 목소리 방식이 generic으로 강제되므로 **UX-016(clone/generic 선택)을 노출하지 않고**(무의미한 1-옵션 화면 방지, D-1/AC-057) UX-026("본인이 체험")→UX-017(generic 필터)→통화(UX-014)로 직행한다. 기존 "본인 목소리 딥보이스 자기 체험"(UF-002 clone 분기)은 이제 **오직 "지인에게 보내기" 2인 챌린지로만 도달**한다(AC-057) — clone 콘텐츠·녹음 메커니즘(UX-002/003·UX-014)은 무변경, 자기훈련 진입 경로에서만 제거.
- **(보이스+지인에게 보내기 = clone·generic 모두·generic 보이스 챌린지 신설, AC-058 · D-33)**: 보이스 "지인에게 보내기"에서는 **UX-016(clone/generic)을 노출**하고 두 방식 모두 챌린지로 보낼 수 있다. clone 챌린지는 기존(AC-044) 무변경. **generic 보이스 챌린지는 신규 능력** — 클론 온보딩(UX-002/003)을 건너뛰고 GENERIC_VOICE_ID 폴백으로 성립하는 깨끗한 부분집합(음성 없는 메신저 챌린지 AC-051과 동형). UX-019(챌린지 만들기)에 **clone vs generic 카피 구분**을 둔다("내 목소리(딥보이스)로 걸려오는 전화를 보냅니다" vs "기본 AI 음성으로 걸려오는 전화를 보냅니다").
- **(메신저 두 분기 = 무변경, 진입 경로만 상향)**: 메신저+본인이 체험은 기존 UF-006 그대로(에스컬레이션 가능 포함 전체 시나리오), 메신저+지인에게 보내기는 기존 AC-051 그대로(비에스컬레이션 시나리오만·voiceId 없음). 두 경우 모두 **UX-026이 이제 시나리오 선택(UX-024)보다 먼저** 오도록 진입 순서만 바뀐다. 즉 UX-024는 UX-026에서 고른 모드(self/send)에 따라 노출 시나리오 집합이 갈린다(self=전체, send=비에스컬레이션).
- **(OQ-32 default 적용, D-34)**: generic 보이스 챌린지의 발신자 결과 열람은 planner 권고 default인 **"완료 여부만"**(메신저 챌린지 AC-055와 동일 계층)을 적용해 UX-020을 설계한다. 사용자가 명시적으로 재검토하지 않았으므로 default임을 명시하고 블로킹하지 않는다(OQ-U13).
- **(OQ-33 = narrative 정합, UX 소관 아님)**: Goal/차별점 1 문면·플래그십 데모 시연 방식의 정합 갱신은 PRD narrative 결정이라 planner/사용자 소관 — UX 설계는 이 확정과 무관하게 진행 가능하며 화면에 영향 없음(재검토 필요 R-6로 남김).
- **의사결정**: D-31(체험/발송 선택 상향)·D-32(보이스 본인체험 generic 전용·UX-016 스킵)·D-33(generic 보이스 챌린지·UX-019 카피 구분)·D-34(OQ-32 default 완료여부만) append. D-30 Status에 위치 부분 supersede 표기.
- **architect 인계 flag(스키마·백엔드, ux-design 소관 아님)**: ① generic 보이스 챌린지 스키마 판별(`challenges.channel="voice"`·`voiceId` 부재·`voiceMode="generic"` 조합)과 사용자2 통화 세션이 GENERIC_VOICE_ID 폴백으로 합성하도록 라우팅하는 접합(§14.8.1의 voiceId 옵셔널화가 이 조합을 수용하는지) — planner도 AC-058에 flag. ② UX-026에서 고른 모드(self|send)가 시나리오 선택(UX-016 노출 여부·UX-017/UX-024 필터)과 하류 세션/챌린지 생성까지 전달되는 클라 상태 배선(구 UX-024 "챌린지 생성 모드 플래그"가 이제 UX-026 상류로 이동). **스키마·콜러블·클라 상태관리 설계는 architect(다음 단계).**
- **범위 경계**: MVP #21(에스컬레이션 가능 메신저 챌린지)·OQ-28(전이 음성 소스)은 여전히 범위 밖 — 설계하지 않음. clone 자기체험 배제(AC-057)는 clone 콘텐츠·메커니즘 삭제가 아니라 자기훈련 진입 경로에서의 제거일 뿐(챌린지 수신자[사용자2] 경험에서 clone 딥보이스는 그대로 성립).

**변경 이력 v1.8 → v1.9 (2026-07-24, D-30 · T48 — 사용자 직접 지시로 D-27 반전):** 사용자가 재작도된 IA(다이어그램)를 직접 검토·확정해 **T46의 D-27 결정("지인에게 보내기" 발신 진입점을 별도 "챌린지 만들기" 메뉴로 유지, 시나리오 카드 미통합)을 반전**했다. 이제 **"본인이 체험" vs "지인에게 보내기"를 시나리오 선택 직후 하나의 결정 지점으로 통합**한다(두 별도 진입 경로 → 하나의 통합 선택). D-27은 삭제하지 않고 **D-30으로 supersede**(Decision Log append-only 규약 준수), D-27 Status를 Superseded로 표기. 반영 내용:
- **(신규 화면 UX-026 · 통합 체험 선택)**: 드릴다운에서 특정 시나리오를 고른 직후, "본인이 체험 / 지인에게 보내기"를 큰 버튼 2개로 묻는 **경량 후속 선택 화면**을 신설한다(신규 Screen ID UX-026 — 기존 최고 ID UX-025의 다음). **인라인 카드 2-CTA가 아니라 별도 후속 화면**을 택한 이유(D-1 저인지부하): 모든 시나리오 카드에 CTA 2개를 얹으면 D-27이 지적했던 "카드마다 매 선택시 결정 부하 증가" 문제가 그대로 재발한다 — 브라우징 단계는 카드당 액션 1개(시나리오 고르기)로 유지하고, 이분 선택은 **하나의 시나리오를 확정한 뒤 단 한 번의 전용 화면**으로 분리하는 편이 드릴다운의 "한 번에 하나씩"(P-12)·저인지부하 원칙에 더 부합한다.
- **(UF-003 드릴다운에 종착 선택 단계 추가)**: 기존 3단계(유형 UX-015 → (보이스)방식 UX-016 → 시나리오 노출 UX-017/UX-024) 뒤에 **UX-026(체험 선택)** 단계를 추가한다. "본인이 체험"→기존 자기훈련 경로(UF-002 보이스 / UF-006 메신저), "지인에게 보내기"→UX-019(챌린지 만들기). 메커니즘은 무변경, **진입 경로만** 이 통합 선택을 통과하도록 바뀐다.
- **(별도 "챌린지 만들기" 진입점 은퇴)**: 발신자가 별도 메뉴로 진입하던 기존 경로를 **은퇴**하고, 드릴다운→UX-026 "지인에게 보내기"를 챌린지 생성의 **유일 경로**로 만든다. 근거: (1) 챌린지는 생성 시점에 시나리오 하나로 확정(OQ-29/AC-051)되므로 "시나리오를 안 고르고 챌린지부터 만드는" 시나리오-무관 경로가 애초에 없어 은퇴가 어떤 흐름도 깨지 않는다, (2) 정식 내비게이션 메뉴 없는 선형 IA(기존 원칙)와 일관, (3) UX-019로 가는 경로가 하나로 좁혀져 경로 드리프트 위험 제거. UF-004(챌린지 생성)·UX-019(챌린지 만들기 화면) 자체는 **유지**(은퇴 대상은 "별도 진입점"이라는 내비게이션 개념이지 화면/플로우 ID가 아님 — 삭제·Deprecated 이관 없음).
- **(보이스·메신저 동일 패턴)**: 통합 선택 UX-026은 채널 무관 동일 UX다. 보이스는 UX-017(clone 시나리오)에서, 메신저는 UX-024(비에스컬레이션 시나리오)에서 UX-026으로 수렴한다. T46이 "UX-024의 챌린지 생성 모드"(별도 메뉴 진입)라 부르던 것은 이제 UX-026 "지인에게 보내기"로 진입하는 것으로 대체·정형화된다.
- **(선택이 실제로 있을 때만 UX-026 노출 — 저인지부하)**: "지인에게 보내기"가 애초에 성립하지 않는 시나리오(=generic 보이스[챌린지는 딥보이스=clone만, AC-044]·에스컬레이션 가능 메신저[#21/OQ-28 보류])에서는 선택지가 하나뿐이라 UX-026을 띄우지 않고 기존대로 자기훈련으로 직행한다(무의미한 1-옵션 화면 방지). 즉 UX-026은 **voice-clone 시나리오·비에스컬레이션 메신저 시나리오**에서만 등장 — 이는 정확히 구(舊) "별도 챌린지 진입점"이 커버하던 시나리오 집합과 동일하다.
- **(AC·스키마 영향 없음 — 확인)**: 이 변경은 **내비게이션/진입점 재배치**일 뿐 새 능력이 아니다. **AC-044**("딥보이스 시나리오 선택 후...챌린지 레코드 생성")·**AC-051**("메신저 시나리오를 골라...챌린지를 만들 수 있고")는 이미 "시나리오 선택 후 챌린지 생성"이라는 순서로 기술돼 있어 통합 선택(선택 직후 분기)과 오히려 더 잘 맞는다 — **AC 문안 변경 불요**. Architecture §14.8(T47, `createChallenge`가 `scenario.channel`로 채널 결정·voiceId 옵셔널·안전장치 채널 무관 동일 코드경로)은 **진입점 불문(entry-point-agnostic)**임을 확인 — 스키마·콜러블이 "별도 메뉴로 왔는지 카드에서 왔는지"를 전혀 참조하지 않으므로 **architect 재방문 불요**(§14.8.1 `createChallenge` 채널 분기는 진입 맥락과 독립). AC-052/#21(에스컬레이션 가능 메신저 챌린지)은 여전히 범위 밖 — 진입점 통합은 #21과 무관하다(#21은 OQ-28 확정에 게이팅되며 "사용자가 어떻게 도달하는가"에 게이팅되지 않는다). 설계하지 않음.

**변경 이력 v1.7 → v1.8 (2026-07-24, PRD v1.3 · T46):** PRD v1.2/v1.3의 **메신저 2인 소셜 챌린지 확장(P0·MVP #20, AC-051/053/054/055)**을 반영한다. 기존 2인 모델(UF-004/005)이 보이스 딥보이스(clone) 전용이던 것을 **음성 없는 순수 텍스트 메신저(카톡/문자) 챌린지**까지 확장한다. **새 화면·플로우 ID를 만들지 않고** 기존 UF-004/UF-005·UX-019/020/021/022/023을 **채널 인지(channel-aware) 변형으로 재사용**한다(신규 메커니즘 없음, ADR-0006 익명 인증+토큰 그대로). 반영 내용:
- **(발신자·UF-004/UX-019) 클론 온보딩 스킵 경로 추가(AC-051)**: 메신저 시나리오 챌린지는 UF-004 step1(UX-002 녹음→UX-003 클론)을 **건너뛰고** UX-015(유형 선택)→UX-024(메신저 시나리오, 챌린지 생성 모드·비에스컬레이션만)→UX-019(챌린지 만들기)로 간다. 클론·voiceId·통화 자격증명(createRealtimeCall)을 생성·저장·발급하지 않는 깨끗한 부분집합. **진입점은 기존 "챌린지 만들기" 별도 메뉴 유지 — 시나리오 카드에 "지인에게 보내기" 버튼을 통합하지 않는다(D-27, 권고).**
- **(OQ-30) 활성 챌린지 상한 = 전역 3개(보이스+메신저 합산)**: UX-019의 개수 초과 상태·안내는 채널 무관 전역 카운트를 반영한다. 현행 UX-019 문구("동시에 만들 수 있는 챌린지 수를 초과했습니다")는 이미 채널 비특정이라 **문구 변경 불요** — Business Rules에 전역 합산 명시만 추가.
- **(OQ-29) 생성 시점 시나리오 확정**: 메신저 챌린지도 기존 보이스 모델과 동일하게 만들 때 시나리오 하나로 확정·전용 링크 발급(시나리오-무관 링크 미채택). UX-019 구조 무변경.
- **(수신자·UF-005/UX-021) 채널 인지 동의 랜딩**: UX-021 동의 게이트(AC-040)는 채널 무관이라 그대로. 다만 문구가 음성 전제("복제 음성 전화")면 메신저 챌린지엔 부정확하므로 **채널 인지 변형** — 메신저면 "지금부터 받는 메시지(카톡/문자)는 실제가 아닌 훈련" 프레이밍, 음성 프레이밍 제거. Exit는 채널 분기: 보이스 챌린지→UX-014, **메신저 챌린지→UX-022(채팅 셸)**(D-28).
- **(수신자·UX-022/023 무약화 재사용, AC-053/054)**: 사용자2 메신저 체험은 단독 훈련과 **동일한 UX-022(스킨 채팅)·UX-023(가짜 랜딩)**와 동일한 정확 모사+무해화 안전속성(AC-032/033/045/047/031·§13.4/13.7)을 그대로 쓴다. 2인 래퍼(익명 인증·토큰·동의 게이트)는 콘텐츠 무해화·모의 표식·면책 배너·상시 "훈련 종료"를 **어떤 방식으로도 약화하지 않는다**(AC-053). P0 챌린지는 **비에스컬레이션 시나리오만** — UX-022의 "전화로 확인"/escalation 상태·UX-025는 이 흐름에서 비활성(에스컬레이션 챌린지는 #21/P1·OQ-28 보류, 설계 안 함).
- **(강제 정체 공개·해설 재사용, AC-042/054)**: 종료 시 UX-007(2인 변형 문구 "○○님이 준비한 훈련이었습니다…")·리플레이 UX-018 그대로 재사용. 두 화면 문구가 "전화가 끝났다" 식 음성 특정 표현을 쓰지 않아 **메신저 전용 변형 불요**(확인함).
- **(발신자 결과·AC-055/OQ-31) 완료 여부만**: UX-020은 보이스 챌린지에서 "완료/의심 시점"을 보이나(AC-043), **메신저 챌린지에서는 "완료 여부"만** 보이고 의심 시점(스미싱 링크 탭·가짜 랜딩 입력·에스컬레이션 도달)은 **어떤 형태로도 노출 안 함**(D-29).
- **의사결정**: D-27(발신 진입점 별도 유지·권고), D-28(채널 인지 동의 랜딩+UX-022/023 무약화 재사용), D-29(메신저 발신자 결과=완료 여부만) append. **OQ-U11(2인 문자 체험 지원 여부)은 이 패스로 P0 텍스트 케이스 resolved.**
- **architect 인계 flag(스키마·백엔드, ux-design 소관 아님)**: ① `challenges.voiceId` 옵셔널화 + 챌린지 채널 구분(`scenario.channel` 파생 또는 `challenges.channel`) — 수신 랜딩/세션이 UX-014 vs UX-022 라우팅을 알아야 함(planner도 AC-051에 flag). ② `consentChallenge`의 **메신저 변형** — 익명 uid 소유 `sessions/{}`를 channel="messenger"·voiceId 미저장·createRealtimeCall 미발급으로 생성(§14.7이 보이스만 다룸; 메신저는 통화 자격증명 경로를 아예 안 타 §14.7의 voiceId 해석 복잡성이 소거되는 **더 단순한 부분집합**임을 확인 요청). ③ `setChallengeResultSharing`의 resultSummary를 메신저 챌린지에서 **완료 플래그만**(suspicionTimeLabel 등 제외)으로 파생(AC-055). ④ 활성 챌린지 상한 카운트를 채널 무관 전역으로(OQ-30). **스키마·콜러블 설계는 architect(다음 단계).**
- **범위 경계**: MVP #21(에스컬레이션 가능 메신저 챌린지)·OQ-28(전이 음성 소스)은 이번 범위 밖 — 설계하지 않음. 에스컬레이션 관련 화면(UX-025·전이 트리거)은 P0 챌린지 흐름에서 발생하지 않으므로 무변경(메신저 챌린지 관점 고려사항은 위 architect flag ①의 채널 구분뿐).

**변경 이력 v1.6 → v1.7 (2026-07-23, T24 · T25):** architect 확정 구조(Architecture.md §13.1~13.7)에 **정합**하여 메신저 표면(T24)·에스컬레이션 전이(T25)를 설계. 새 메커니즘을 만들지 않고 §13의 세션 필드(`channel`·`messengerSkin`·`skinSource`·`escalation`·`voiceSelectionSource`)·sentinel 신호·재사용 원칙을 그대로 따랐다. 반영 내용:
- **(T24) 메신저 훈련 흐름 UF-006 + 신규 화면 3종**: **UX-024(메신저 시나리오 선택)** → **UX-022(메신저 채팅 셸)** → (스미싱 링크 탭 시) **UX-023(인앱 가짜 랜딩)**. UX-022는 카카오톡형·문자형을 **하나의 스킨 레이어 화면**으로 처리(문자형은 iOS/삼성/기본 스킨 UA 자동 감지+폴백+수동 토글, §13.5). **스킨은 프레젠테이션 전용이라 어떤 안전 판정도 게이팅하지 않음**(§13.4)을 화면에도 명시. 카카오 표면은 "카카오톡 실제 서비스와 무관" **영구 비활성화 불가 상시 면책 배너**(AC-047/§13.7). 스미싱 링크는 `displayText` 탭 → `fakeLandingId`가 가리키는 UX-023으로 이동(실 URL 스키마에 없음·서버 미전송·가짜 피드백, AC-045/§13.4). 모든 표면 "AI 훈련용 모의" 표식·상시 "훈련 종료"·시작 전 메타(AC-002/032). **UX-015의 메신저 스텁(D-17)을 실제 목적지 UX-024로 연결(D-17 Superseded → D-22).**
- **(T25) 에스컬레이션 전이 흐름 UF-007 + 신규 화면 1종 + 연출**: **UX-025(조건부 목소리 선택 서브플로우, AC-046)** — "통화로 이어질 수 있어 목소리가 필요할 수 있습니다" 안내 + 3경로(①즉시 녹음=UX-002/003 재사용 ②기존 보관 목소리 재사용 ③남/여 기본 보이스). **안내·목소리 확보 시점을 에스컬레이션 발생 순간이 아니라 메신저 시나리오 선택 직후로 배치**(이음새 없는 전이를 위해 voiceId 선확보, D-25). 전이 트리거 3경로(sentinel 자동전이/max-turn 폴백/명시 "전화로 확인" 버튼, §13.2·13.3)는 **모두 서버가 `escalation:{toChannel:"voice"}` 플래그로 전달**하며 UX는 그 플래그 수신 시 "사기범이 전화를 거는" 연출로 **기존 통화 셸 UX-014 수신 phase에 이음새 없이 인계**(P-18). 사용자에게 sentinel 토큰은 절대 노출되지 않음(서버 제거 전제). 사전 고지·동의·상시 종료·종료 고지는 UX-014 그대로 유지(재설계 없음, AC-036). 양방향은 설계 목표로만(연출 재사용 가능 구조 확인), 실제 화면은 메신저→보이스만(AC-039).
- **의사결정**: D-22(메신저=단일 스킨 레이어·D-17 supersede), D-23(스미싱 링크→가짜 랜딩), D-24(면책 상시 배너), D-25(목소리 선택 시점=시나리오 선택 직후), D-26(전이 연출 3경로·이음새 없음·양방향 설계) append. **D-17 Superseded 표시.**
- **인터랙션 패턴**: P-16(메신저 스킨 전환)·P-17(스미싱 링크 무해화)·P-18(에스컬레이션 전이 연출)·P-19(상시 면책 배너) 신설.
- **범위 경계**: 폴백 수치(6턴 등)·UA 판정 로직·토큰은 architect(§13.3/13.5) 소관 — 화면은 "감지됨/폴백됨/수동전환됨/전이됨" 상태만. voice→messenger 역방향 화면은 범위 밖(AC-039). 2인 메신저 챌린지는 여전히 범위 밖(OQ-U11 갱신).

**변경 이력 v1.5 → v1.6 (2026-07-23, T32 · T34):** T23 승인(OQ-U9=카드 목록 확정, resolved 기록) 후 **T32(사후 리플레이 해설)·T34(2인 소셜 훈련)**를 함께 설계. 반영 내용:
- **(T32) 리플레이 해설을 신규 화면 UX-018로 신설**(UX-008 확장이 아닌 별도 화면, D-18 근거). 세션 종료 후 transcript를 시간순으로 되짚으며 각 사기 신호 시점에 "여기 이 말이 사기 신호 / 이렇게 대응했어야" 주석을 보는 화면(AC-038). 기존 리포트·transcript 인프라(T9/AC-008/026) 재사용, **보이스·메신저·2인 세션 모두 공통**. "면역됨" 류 과신 표현 금지·"개선 영역" 프레임 유지. 인터랙션 패턴 P-13(해설 스텝 내비게이션) 신설.
- **(T34) 2인 소셜 훈련 신규 Flow 2종 + 신규 화면 3종 신설**: **UF-004(사용자1: 클론→챌린지 생성→공유→제한적 결과 열람)**, **UF-005(사용자2: 링크→동의→체험→강제 정체공개→해설)**. 신규 화면 **UX-019(챌린지 만들기·공유 링크)**, **UX-020(사용자1 결과 열람 — AC-043 완료여부/의심시점만, 대화 전문 화면 미설계)**, **UX-021(사용자2 동의 랜딩 — AC-040 무동의 차단 게이팅·신고 버튼·만료 링크 상태)**. 체험 단계는 **기존 통화 셸 UX-014 재사용**, 정체 공개는 **UX-007 재사용(2인 변형 문구)** 후 UX-018 해설로 인계. 인터랙션 패턴 P-14(링크 공유=OS 공유 시트+복사 폴백)·P-15(무동의 차단 게이팅) 신설.
- **의사결정**: D-18(리플레이=신규 화면), D-19(2인 흐름 구조·사용자2 무로그인 링크 진입·무동의 차단), D-20(링크 공유는 앱이 대신 전송 안 함·OS 공유+복사), D-21(사용자1 결과 열람 제한) append.
- **범위 경계(준수)**: 링크 토큰 정책(AC-048)·오용 방지 수치(AC-049 활성 챌린지 상한·신고 데이터 모델)·복제음성 보존 기본값(AC-041)은 **architect(T35) 소관** — 화면에는 "제한에 걸렸을 때의 상태"(개수 초과·만료 링크·신고 버튼 위치)만 설계하고 정책 수치·백엔드 로직은 규정하지 않는다. 메신저 표면(카카오톡/문자)은 **T24 미설계**라 2인 문자 체험 재사용 목적지는 OQ-U12로 남긴다(2인은 AC-044상 딥보이스=음성 중심으로 설계). 동기(실시간 조종) 방식은 AC-044대로 배제.

**변경 이력 v1.4 → v1.5 (2026-07-23, PRD v0.9 · T23):** PRD v0.9의 **AC-028(최상위 2분류 + 보이스 하위 clone/generic)·AC-029(단계적/몰입형 선택 흐름)**를 반영해, 기존 평면 목록형 시나리오 선택(UX-004)을 **단계적 드릴다운**으로 재설계한다(D-16 신설). 반영 내용:
- **(1) 신규 Flow UF-003(단계적 훈련 유형 선택) + 신규 화면 3종 신설**: 훈련 유형 선택(UX-015: 보이스피싱/메신저피싱) → 보이스 하위 목소리 방식 선택(UX-016: clone/generic) → 해당 조건의 시나리오만 노출(UX-017). "유형을 먼저 고르면 그 유형의 시나리오만 이후 단계에서 드러나는" 흐름으로, 모든 시나리오를 한 화면에 평면 나열하지 않는다(AC-029). 보이스 하위는 신규 음성 데이터 모델 없이 기존 `voiceMode`(clone/generic)를 그대로 재활용한다(AC-028, D-16).
- **(2) 구 UX-004(시나리오 선택, 평면 목록)를 Deprecated로 이관**(삭제 금지, 사유·이관 대상 명시). UX-004의 AC-001/002는 UX-017로 이관되어 커버리지 손실 없음. UX-004가 담당하던 clone→UX-002 / generic→UX-014 분기는 UX-017이 그대로 계승한다.
- **(3) 메신저피싱 분기는 IA상 스텁으로만 표시(T24 범위 밖)**: UX-015에서 "메신저피싱"을 고르면 메신저 훈련(T24)으로 이어진다는 흐름의 **존재만** 향후 확장 지점으로 반영하고, 메신저 하위 화면(채널 선택·채팅 재현)·채널 간 전이(T25)는 이번 패스에서 설계하지 않는다(D-17). 라우팅 목적지·메신저 하위 구조는 OQ-U10.
- **(4) 드릴다운 전용 인터랙션 패턴 P-12 신설**(뒤로가기·단계 표시·전환 방향성). 어르신 저인지부하 원칙(큰 글씨·넉넉한 터치영역·색+아이콘 이중표기)은 기존 Accessibility 섹션 그대로 유지.
- **미해소 질문**: 최종 노출 단계(UX-017)에서 필터된 시나리오를 카드 목록으로 보여주는 것이 사용자의 "한 번에 하나씩" 몰입 의도와 충분히 부합하는지는 OQ-U9로 남긴다(임의 결론 금지).
- **범위 밖 무변경**: 통화(UX-014)·녹음(UX-002)·클론 대기(UX-003)·종료(UX-007)·리포트(UX-008) 등 UF-002 본체와 안전장치(AC-006/012/017/019/022/023)는 무변경. T24(메신저 화면)·T25(전이)·T32/T34 등 다른 태스크는 이번 패스에서 건드리지 않았다.

**변경 이력 v1.3 → v1.4 (2026-07-22):** 사용자 피드백으로 **통화 경험을 화면 단위 수입(claude.ai/design 원안)에서 이 앱 고유의 "단일 연속 통화"로 재설계**한다. PRD(기능 범위·시나리오 정의)는 무변경 — 화면 흐름·정보구조·상호작용만 다시 그렸다. 반영 내용:
- **(1) 딥보이스 재생(구 UX-005) + 사기범 역할극(구 UX-006)을 하나의 연속 통화 화면 UX-014로 통합**(D-11 신설). 사용자 불만("화면 7→8이 하나의 통화처럼 안 느껴짐 / 받았다가 채팅창으로 넘어가는 느낌")의 원인은 두 라우트(`/session/play`·`/session/chat`) 사이의 full-page 전환과 그 사이의 "통화 종료 → 계속(역할극 시작)" 중간 상태였다. UX-014는 수신→오프닝 딥보이스→실시간 역할극을 **하나의 고정된 통화 셸(발신자·통화 타이머·AI 합성 표식·훈련 종료) 안에서 phase 전이로만** 처리해 단절을 제거한다(D-12: 중간 "계속" 버튼·화면 전환 제거).
- **(2) 구 UX-005·UX-006은 삭제하지 않고 Deprecated로 이관**(사유·이관 대상 AC 명시). D-10(STT/TTS In Scope 전환) 결정은 **반전이 아니라 UX-014로 이관**되어 유효하다(D-11에서 근거 명시).
- **(3) 온보딩 순서(Phase B) 문서 정합화**(D-14 신설). IA 다이어그램·실제 코드(`src/app/scenarios/page.tsx`, `age-gate/page.tsx`)는 이미 "시나리오 선택 → (clone일 때만) 녹음→클론대기" 순서였으나, Screen Catalog의 Entry/Exit·UF Steps·화면-플로우 소속이 구(舊)순서(동의→녹음→클론→시나리오)로 남아 문서 내부가 모순이었다. 이를 실제 순서로 정정하고 녹음(UX-002)·클론대기(UX-003)의 소속을 UF-001→UF-002로 옮겼다.
- **(4) clone/generic 시나리오를 같은 통화 셸로 처리하되 음성 모드 라벨로만 구분**(D-13). "당신의 목소리로 합성"(clone) vs "기본 합성 음성"(generic) — 근거 없는 "본인 목소리" 표기 금지(코드는 이미 `voiceMode`로 분기). 단 이로 인한 차별점 희석 우려 등은 임의 결론 대신 신설 **"재검토 필요(기능 방향)"** 섹션에 질문으로 남겼다.
- **안전장치 무변경**: AC-006(상시 종료)·AC-012/017/023(사전 고지·동의·종료 고지)·AC-019(PSTN 없음, 인앱 전용)·AC-022(합성 표식 상시)는 그대로 유지. 통화 몰입 극대화는 "통화 중" 구간에 한하며 사전 동의·종료 고지는 절대 생략하지 않는다(D-10 근거 계승).

**변경 이력 v1.2 → v1.3 (2026-07-22):** 사용자 결정으로 **UX-006을 텍스트 채팅에서 실시간 음성 통화형으로 전환**(D-10 신설). 반영 내용: (1) UX-006 Primary Actions/States/Accessibility/Architect Handoff 갱신(마이크 탭-투-토크·자동 음성재생·텍스트 폴백), (2) PRD "실시간 통화·STT는 제외" Out of Scope 결정을 뒤집음(단 **실제 전화망(PSTN) 연동은 여전히 제외**, AC-019 무변경), (3) 이 결정 이전에 사용자가 요청했던 "훈련인지 인지하면 안 됨"(사전고지·동의 생략)은 AC-012/017/023 정면 제거에 해당해 **거부**하고 절충안(사전 동의 유지+통화 중 몰입 유지)으로 대체됐음을 D-10에 근거로 남김. 상세는 D-10 참조.

**변경 이력 v1.1 → v1.2 (2026-07-21):** 사용자 결정으로 **인증 수단을 Google 소셜 로그인으로 확정**(OQ-U5 resolved — "구글로 로그인", Firebase Auth Google Provider만 사용). 반영 내용: (1) UX-013을 "Google로 로그인" 버튼 1개 기준으로 구체화(Primary Actions·States·Failure에 팝업 취소/차단·네트워크 실패 등 OAuth 특유 케이스 반영, 이메일/비밀번호 폼 제거), (2) UX-013 Architect Handoff의 External Dependencies를 **Firebase Auth Google Provider(OAuth)**로 확정, `signup_succeeded` 이벤트 제거(Google 최초 인증이 계정 자동 생성=로그인 단일 동작), (3) Google 계정 미보유 어르신 처리는 임의 결론 대신 **OQ-U6** 신설(OQ-3 자녀 대리설정 논의와 연계 권장).

**변경 이력 v1.0 → v1.1 (2026-07-21):** 사용자 결정으로 **P0에 로그인 도입**(OQ-U2 resolved — "로그인 있이 가자"). 반영 내용: (1) 로그인/가입 화면 **UX-013 신설**, (2) UF-001에 로그인 단계 추가, (3) UX-001의 진입점·의존성 갱신(로그인 없이 진행 가정 제거), (4) UX-010·UX-012의 "로그인 종속(OQ-U2)" 문구를 "로그인으로 해소"로 갱신, (5) Information Architecture에 로그인 단계 반영, (6) UX Decision Log **D-9** append, (7) 구체 인증 프로바이더(이메일/구글/전화 등) 선택은 architect 기술 판단 사항이라 **OQ-U5**로 신설. 상세는 D-9 참조.

**스코프 경계 (반드시 준수):**
- **본인 1인 온보딩만 설계한다.** 자녀가 부모 계정을 대신 만드는 화면, 자녀가 부모 리포트를 열람하는 화면은 **설계 대상이 아니다**(OQ-3·OQ-15 여전히 open, 빌드 스코프 밖 — PRD v0.4). 발표 내러티브 전용이므로 화면으로 만들지 않는다.
- **P0(핵심 루프 + 보안 가드레일 + 로그인)만 정식 Flow/Screen으로 설계**했다. **로그인/가입(UX-013)은 사용자 결정으로 P0**이며, AC-016의 "계정 생성/접근" 절반을 실현한다. 나머지 P1(사칭 이미지 AC-025, 게임화 AC-010/011, age-gate AC-014, 세션/리포트 **히스토리 열람** AC-016의 나머지 절반)은 "여유 시 추가" 경량 스케치(UX-009~UX-012)로만 남긴다.
  - 참고: PRD v0.4 MVP Scope는 계정(AC-016)을 P1로 분류하나, 사용자가 로그인을 P0로 상향 결정(2026-07-21). 이 상향은 UX 설계에 반영했으며, PRD 문면과의 정합은 planner가 필요 시 반영(문서 드리프트는 docs가 UpdateRequests로 라우팅).

**시스템 레벨 AC(화면이 아니라 백엔드 동작이지만 UI에서 트리거/표기됨):**
- **AC-021**(생성물 즉시 폐기): 세션 종료 시 백엔드 삭제 동작. UI로 노출되는 것은 UX-007의 "폐기 완료" 표기뿐. 화면 자체가 아니라 UX-007 Architect Handoff의 이벤트/기대출력으로 다룬다.
- **AC-024**(PII 마스킹·프롬프트 인젝션 방어): 백엔드/프롬프트 설계 사항. UX-014(실시간 역할극)의 Architect Handoff에 제약으로 명시하되, 별도 화면을 만들지 않는다.

**보안 가드레일 5종 화면 노출 지점(보안 배점 20% 근거):**
| 가드레일 | AC | 노출 화면 |
|---|---|---|
| 사전 고지(시뮬레이션·실금전 없음·종료 방법) | AC-012 | UX-001 |
| 명시적 동의 게이팅(동의 없이는 세션 불가) | AC-017 | UX-001 |
| 본인 동의·본인 확인(본인 목소리만 등록) | AC-020 | UX-002 |
| 합성물 "AI 훈련용 합성" 표식 | AC-022 | UX-014(오프닝·이미지 시), UX-009 |
| 세션 종료 시 "이것은 훈련이었습니다" 고지 + 디스컬레이션 | AC-015, AC-023 | UX-007 |
| 생성물 즉시 폐기(로그) | AC-021 | UX-007(트리거·표기) |
| 실제 사기 실행 정보 미노출 | AC-005, AC-013 | UX-014, UX-008(리포트 문구 수준 주의) |

## User Flows

### 로그인·사전 고지·동의·연령 확인 (Flow ID: UF-001)
> **v1.4 갱신(2026-07-22, D-14)**: 녹음(UX-002)·클론 생성 대기(UX-003)는 시나리오 선택(UX-004) 이후에 조건부로 일어나므로 UF-002로 이관했다. UF-001은 이제 "인증→동의→(연령)→시나리오 선택 진입"까지만 다룬다. 이전 Steps(동의 직후 녹음)는 아래로 대체됐다.
| Item | Value |
|---|---|
| Actor | 훈련 대상 본인(어르신 또는 일반 사용자). 자녀 보조는 계정 생성 UI까지만(빌드 스코프 밖이므로 이 플로우는 본인 단독 경로만 다룸). |
| Trigger | 사용자가 서비스에 처음 진입한다. |
| Related Acceptance Criteria | AC-012, AC-017, AC-016(계정 생성/접근 부분), AC-014(P1 연령 확인) (MVP Scope #1 일부·#10·#12, 로그인은 사용자 결정으로 P0) |
| Steps | 1. 로그인/가입 화면(UX-013)에서 Google 계정으로 로그인한다(유효 세션이 있으면 이 단계를 건너뛴다). 2. 사전 고지 화면(UX-001)에서 "이것은 훈련 시뮬레이션이며 실제 금전·자격증명이 관여하지 않고, 언제든 종료할 수 있다"를 읽는다. 3. 명시적 동의 체크 후 "동의하고 시작"을 누른다(동의 없이는 다음 단계 잠금, AC-017). 4. (P1, 미구현 시 생략) 연령 확인(UX-011)을 통과한다(AC-014). 5. 훈련 유형 선택(UX-015, UF-003 드릴다운 시작)으로 이동한다(v1.5 D-16 — 구 "시나리오 선택 UX-004" 대체). |
| Alternative Flow | (a) 재로그인: 유효 세션 보유 시 UX-013을 건너뛰고 UX-001로 직행. (b) 이미 연령 확인을 통과한 계정: UX-011을 재질문 없이 통과(코드상 계정 단위 플래그). |
| Failure Flow | (a) 로그인/가입 실패(인증 오류·팝업 취소/차단·네트워크) → UX-013이 오류 상태로 전환, 재시도(Interaction Pattern P-4). (b) 동의 거부 → 다음 단계 시작 불가, UX-001에 머무름(AC-017). (c) 연령 기준 미달 → UX-011이 접근 제한 안내 상태(AC-014). |
| Success Criteria | 인증·명시적 동의(·연령 확인)를 마치고 훈련 유형 선택 화면(UX-015, 드릴다운 시작)으로 진입한다(v1.5 — 구 UX-004 대체). 이후 모든 데이터는 인증된 계정(uid)에 귀속된다. |

### 시나리오 선택·(조건부)목소리 등록·통화·종료·리포트 (Flow ID: UF-002)
> **v1.11 갱신(2026-07-25, D-41·D-35·D-39)**: 세 지점이 추가된다(본체 메커니즘 무변경). **(1) 난이도 선택**: 시나리오를 고른 뒤 세션 생성 **직전**에 UX-029(초급/중급/고급)를 거친다 — Step 1과 Step 3 사이에 삽입되며, 고른 난이도가 `createSession`으로 함께 전달된다(D-41). **(2) 통화 중 문자**: Step 3의 통화(UX-014) 진행 중 사기범이 문자를 보내면 통화를 유지한 채 UX-027 오버레이가 뜬다(UF-008로 분기했다가 되돌아옴 — 통화 종료가 아님, D-35). **(3) 즉시 되감기**: Step 6(UX-007)·Step 7(UX-008)에서 `deceivedMoments`가 1건 이상이면 UX-028(즉시 되감기, UF-009)로 갈 수 있다(D-39). 안 속았으면 되감기 진입점을 띄우지 않고 기존대로 UX-018 해설을 권한다(D-40).
> **v1.10 갱신(2026-07-24, D-31·D-32, T54, AC-056/057)**: **보이스 자기훈련은 이제 generic 시나리오만** 다룬다 — clone(본인 목소리 복제) 자기 체험은 AC-057로 완전히 배제되고, clone은 오직 UF-004("지인에게 보내기" 2인 챌린지)로만 도달한다. 따라서 UF-002 보이스 경로의 진입은 **UX-015(보이스) → UX-026("본인이 체험") → (UX-016 스킵·generic 강제) → UX-017(generic 시나리오) → `createSession` → UX-014**다. 아래 Steps의 "clone 시나리오: 녹음(UX-002)→클론(UX-003)→본인목소리 오프닝" 분기는 **UF-002 자기훈련에서는 더 이상 발생하지 않고**(그 clone 녹음·재생 메커니즘은 UF-004 챌린지 생성·UF-005 사용자2 수신에서 그대로 재사용됨), UF-002 자기훈련은 generic 분기(녹음/클론 생략 → 기본 합성 음성 오프닝)만 남는다. 통화 셸·안전장치 등 통화 본체 메커니즘은 무변경. (v1.9의 "clone 자기훈련이 UX-026을 경유" 서술은 이 노트로 대체됨 — clone 자기훈련 경로 자체가 사라졌다.)
> **v1.9 갱신(2026-07-24, D-30, T48)**: 보이스 **clone** 시나리오의 자기훈련 진입은 이제 UX-017에서 곧바로 UX-002로 가지 않고 **UX-026(체험 선택)에서 "본인이 체험"을 고른 뒤** UX-002로 온다(같은 clone 시나리오를 "지인에게 보내기"로 보내는 경로가 UX-026에 함께 생겼기 때문). **generic 시나리오는 챌린지 대상이 아니라 UX-026을 거치지 않고 기존대로 `createSession`→UX-014로 직행**(무변경). 통화·녹음 등 UF-002 본체 메커니즘은 무변경. *(v1.10 D-32로 대체 — 위 노트 참조.)*
> **v1.5 갱신(2026-07-23, D-16)**: UF-002의 시작점이 단일 화면 "시나리오 선택(구 UX-004)"에서 **드릴다운 플로우 UF-003**(유형 선택 UX-015 → 보이스 방식 선택 UX-016 → 시나리오 노출 UX-017)으로 바뀐다. 따라서 UF-002의 Trigger·Step 1의 "UX-004"는 **UX-017(시나리오 노출·선택)**로 읽는다(구 UX-004는 Deprecated). clone→UX-002 / generic→UX-014 분기는 UX-017이 계승하므로 Step 2 이후는 무변경. 아래 Steps의 "UX-004" 표기는 이 노트로 UX-017로 갱신된 것으로 본다.
> **v1.4 갱신(2026-07-22, D-11·D-14)**: (1) 녹음(UX-002)·클론 대기(UX-003)를 UF-001에서 이관받아 "시나리오 선택 후 clone 시나리오일 때만" 일어나는 조건부 단계로 배치. (2) 딥보이스 재생(구 UX-005)+역할극(구 UX-006)을 단일 연속 통화 **UX-014**로 통합. 이전 Steps(별개 재생 화면→채팅 화면)는 아래로 대체됐다.
| Item | Value |
|---|---|
| Actor | 훈련 대상 본인(UF-001 완료 상태). |
| Trigger | UF-001 완료 후 시나리오 선택 화면 진입. |
| Related Acceptance Criteria | AC-001, AC-002, AC-018, AC-020, AC-003, AC-004, AC-005, AC-006, AC-007, AC-008, AC-009, AC-013, AC-015, AC-019, AC-021, AC-022, AC-023, AC-024, AC-026 (MVP Scope #1 녹음 부분·#2~#7) |
| Steps | 1. 시나리오 선택(UX-004)에서 시나리오의 제목·사기 유형·예상 소요·난이도와 **음성 모드(본인 목소리 등록 필요 여부)**를 확인하고 선택한다(AC-001/002). 2. 분기: **(clone 시나리오, 예: 가족 납치/사고 딥보이스)** 목소리 녹음(UX-002)에서 본인 확인 문항에 동의하고 30초 대본을 녹음한 뒤 "클론 생성" → 클론 생성 대기(UX-003)를 거친다(AC-020/018). **(generic 시나리오, 예: 기관 사칭)** 녹음/클론을 생략하고 바로 다음 단계로 간다. 3. 통화(UX-014)로 진입한다 — ① 수신 화면에서 발신자(가족/기관 사칭)를 확인하고 "받기"(또는 "거절"=훈련 종료). ② 받으면 오프닝 딥보이스(clone은 본인 목소리, generic은 기본 합성 음성)가 "AI 훈련용 합성" 표식과 함께 자동 재생된다(AC-019/022). ③ 오프닝이 끝나면 **화면 전환 없이 그대로** 사기범과 실시간 음성(또는 텍스트 폴백) 역할극이 이어진다(AC-003/004/005/013/024). 4. 통화 전 구간에서 상시 노출된 "훈련 종료" 컨트롤로 언제든 종료할 수 있다(AC-006). 5. 종료 조건(사용자 종료/최대 턴·시간 한도 도달) 중 하나로 통화가 끝난다(AC-007). 6. 세션 종료 화면(UX-007)에서 "이것은 훈련이었습니다" 고지 + 디스컬레이션(안심) 메시지를 본다. 이 시점에 생성물 즉시 폐기가 트리거된다(AC-021/015/023). 7. 취약점 리포트(UX-008)에서 속은 시점 타임라인·놓친 위험 신호·올바른 대처법을 확인한다(AC-008/009/026). |
| Alternative Flow | (a) 재녹음(clone): 녹음이 마음에 안 들면 UX-002에서 재녹음. (b) 한 번도 속지 않은 경우 → 리포트(UX-008)가 이를 명시하고 시도된 수법만 나열(AC-009). (c) 사용자가 사기 실행 방법을 요청 → AI가 거부(AC-013), 캐릭터·가드레일 유지. |
| Failure Flow | (a) 마이크 권한 거부(녹음) → UX-002가 권한 안내 상태(P-6). (b) 클론 생성 실패/타임아웃 → UX-003이 오류 상태, 재시도 또는 데모 폴백 음성 경로(P-1). (c) 오프닝 딥보이스 합성/재생 실패 → UX-014가 폴백 음성/자막만으로 통화를 이어감(P-1, 핵심 루프 비차단). (d) 실시간 LLM/음성 합성 실패·지연 초과 → UX-014가 인라인 재시도(P-4), 통화는 유지. (e) 통화 도중 종료 → 즉시 UX-007로 이동하고 그때까지의 로그로 리포트 생성(AC-007: 종료된 모든 세션은 정확히 1개 리포트). |
| Success Criteria | 세션이 정확히 1개의 리포트를 생성하고, 사용자가 "언제·어디서 속을 뻔했는지"와 대처법을 확인하며, 생성물이 폐기된다. |

### 단계적 훈련 유형 선택 (드릴다운) (Flow ID: UF-003)
> **v1.11 갱신(2026-07-25, D-41·D-42·D-43 — 난이도 단계 신설)**: 드릴다운의 **마지막 단계로 난이도 선택(UX-029)**이 추가된다. 순서는 **UX-015(유형) → UX-026(체험/발송) → [보이스+발송]UX-016(방식) → UX-017/UX-024(시나리오) → UX-029(난이도) → 세션 생성 또는 UX-019(챌린지 만들기)**. 난이도를 **시나리오 뒤**에 둔 이유(D-41): 난이도는 "어떤 상황인가"가 아니라 "그 상황을 얼마나 세게 겪을 것인가"라 시나리오를 정한 뒤에 물어야 의미가 서며, 시나리오보다 앞에 두면 난이도가 시나리오 목록을 필터링한다는 오해를 준다(난이도는 어떤 시나리오도 감추지 않는다 — 모든 시나리오에 3단계가 다 있다). 기존 시나리오 카드의 고정 `difficulty` 텍스트는 삭제하지 않고 **"기본 성향"** 맥락으로 남긴다(AC-002 표기 요건 유지, 라벨 정합은 OQ-U21). 난이도는 **어떤 안전장치도 게이팅하지 않는다**(D-42).
> **v1.10 갱신(2026-07-24, D-31·D-32·D-33, T54, AC-056/057/058)**: **체험/발송 선택(UX-026)이 유형 선택(UX-015) 직후로 상향**된다 — 목소리 방식 선택(UX-016)·시나리오 선택(UX-017/UX-024)보다 **먼저** 온다(AC-056). 드릴다운 순서가 **UX-015(유형) → UX-026(체험/발송) → [보이스+발송일 때만]UX-016(방식) → 시나리오 노출(UX-017/UX-024) → 자기훈련/챌린지 생성**으로 재편된다. 모드(self/send)가 이후 단계를 좌우한다: (보이스+본인체험)=UX-016 스킵·generic 시나리오만·통화 직행(AC-057), (보이스+지인에게)=UX-016(clone/generic)·시나리오 노출·UX-019(AC-058, generic 보이스 챌린지 신설), (메신저+본인체험)=UX-024 전체 시나리오(에스컬레이션 포함)·자기훈련(무변경), (메신저+지인에게)=UX-024 비에스컬레이션만·UX-019(무변경). UX-026은 이제 **두 채널 모두에서 항상** 등장한다(채널 레벨에서 self·send가 늘 둘 다 유효 — 무의미한 1-옵션 화면 아님). v1.9의 "UX-026을 시나리오 확정 직후에 배치·조건부 노출"은 이 노트로 대체된다(위치가 앞당겨지고 상시 노출).
> **v1.9 갱신(2026-07-24, D-30, T48 — D-27 반전)**: 드릴다운에 **종착 선택 단계(UX-026 체험 선택)**를 추가한다. 시나리오를 확정한 직후(UX-017 clone / UX-024 비에스컬레이션) "본인이 체험 / 지인에게 보내기"를 묻고, 그 결과로 자기훈련(UF-002/UF-006) 또는 챌린지 생성(UF-004·UX-019)으로 분기한다. **"지인에게 보내기"가 성립하지 않는 시나리오(generic 보이스·에스컬레이션 가능 메신저)는 UX-026을 띄우지 않고 기존대로 자기훈련으로 직행**(무의미한 1-옵션 화면 방지). Related AC에 AC-044·AC-051(지인에게 보내기 분기) 추가. 기존 Step 4의 "UX-017 Exit → UF-002 직행"은 아래 Steps로 대체됐다(clone·비에스컬레이션은 UX-026 경유). *(v1.10 D-31로 위치 상향 — 위 노트 참조.)*
> **v1.5 신설(2026-07-23, D-16, PRD v0.9 · T23)**: PRD AC-028/029를 실현하는 **드릴다운 선택 흐름**. 기존 평면 목록(구 UX-004)을 대체한다. "유형을 먼저 고르면 그 유형의 시나리오만 이후 단계에서 드러나는" 단계적 흐름으로, 사용자가 한 번에 하나씩 좁혀 들어가 "이 상황에 들어간다"는 몰입을 얻게 한다. **메신저피싱 하위(T24)·채널 간 전이(T25)는 이번 범위 밖** — UX-015의 분기 존재만 향후 확장 지점으로 둔다.
| Item | Value |
|---|---|
| Actor | 훈련 대상 본인(UF-001 완료 상태 — 로그인·동의(·연령 확인) 통과). |
| Trigger | UF-001의 사전 고지·동의(UX-001)(·age-gate UX-011) 완료 직후 훈련 유형 선택 화면(UX-015) 진입. |
| Related Acceptance Criteria | AC-028, AC-029 (MVP Scope #13). 종착 시나리오 메타 표기는 AC-002도 계승(UX-017). **(v1.10) 체험/발송 선택(UX-026)의 상향 배치는 AC-056; "지인에게 보내기"는 AC-044(보이스 clone 챌린지)·AC-058(generic 보이스 챌린지)·AC-051(메신저 챌린지)로 이어지고, 보이스 "본인이 체험"의 generic 전용·clone 배제는 AC-057.** |
| Steps | 1. 훈련 유형 선택(UX-015)에서 최상위 2분류 **(A) 보이스피싱 / (B) 메신저피싱** 중 하나를 고른다(AC-028 상위 분류). 2. **(v1.10, D-31) 체험/발송 선택(UX-026)**: 유형을 고르자마자 — 목소리 방식·시나리오보다 **먼저** — "**본인이 체험** / **지인에게 보내기(챌린지)**"를 고른다(AC-056, 채널 무관 단일 화면). 이 모드(self/send)가 이후 단계를 좌우한다. 3. 분기별 다음 단계: **(A 보이스 + 본인이 체험)** UX-016을 **건너뛰고**(방식이 generic으로 강제, AC-057·무의미한 1-옵션 방지) UX-017에 **generic 시나리오만** 노출 → 선택 → `createSession` → UX-014(통화). **(A 보이스 + 지인에게 보내기)** 목소리 방식 선택(UX-016)에서 **clone / generic**을 고르고(AC-028 하위·AC-058) → UX-017에 그 방식의 시나리오만 노출 → 선택 → UX-019(챌린지 만들기). **(B 메신저 + 본인이 체험)** UX-024에 **전체 메신저 시나리오(에스컬레이션 포함)** 노출 → 선택 → (에스컬레이션 시 UX-025 후)UX-022(UF-006). **(B 메신저 + 지인에게 보내기)** UX-024에 **비에스컬레이션 메신저 시나리오만** 노출 → 선택 → UX-019(챌린지 만들기). 4. "지인에게 보내기" 하류(UX-019 진입) 세부: 보이스 clone은 클론 확보(UX-002/003) 후, 보이스 generic·메신저는 클론 스킵(AC-058/AC-051) 후 챌린지 레코드 생성. 어느 단계에서도 전체 시나리오가 한 화면에 평면 나열되지 않는다(AC-029). |
| Alternative Flow | (a) **(B 메신저피싱 선택 시)** UX-015→UX-026→UX-024(메신저 시나리오 선택, UF-006/UF-004)로 이어진다(v1.7 D-22에서 목적지 확정, OQ-U10 resolved; v1.10에서 UX-026이 UX-024보다 먼저). (b) 각 단계에서 "뒤로"로 직전 선택 단계로 되돌아가 다시 고를 수 있다(P-12). (c) 특정 방식(예: send+clone)에 해당하는 시나리오가 0건이면 UX-017이 빈 상태 안내(현재 데이터상 clone·generic 모두 최소 1종 존재하므로 실질적으로 발생하지 않음). |
| Failure Flow | (a) 시나리오 콘텐츠 로드 실패(UX-017) → 재시도(P-4). (b) generic "시작" 시 세션 생성 실패 → UX-017이 오류 상태, 재시도(UX-004에서 계승). |
| Success Criteria | 사용자가 유형→(보이스면)방식→시나리오를 단계적으로 좁혀 선택하고, 선택된 시나리오 1건과 그 음성 모드가 UF-002(녹음 또는 통화)로 전달된다. 어느 단계에서도 전체 시나리오가 한 화면에 평면 나열되지 않는다(AC-029). |

### 2인 소셜 훈련 — 사용자1(챌린지 생성·공유·결과 열람) (Flow ID: UF-004)
> **v1.11 갱신(2026-07-25, D-41·D-42)**: 발신자도 시나리오를 고른 뒤 **UX-029(난이도)**를 거쳐 UX-019(챌린지 만들기)로 간다 — 즉 발신자가 **수신자가 겪을 강도**를 정한다. UX-019는 고른 난이도를 요약에 명시하고(예: "고급 — 실제처럼 정교하게"), **고급을 고르면 "상대가 강한 압박을 겪게 됩니다"를 사전 고지**한다. 그 난이도는 수신자의 동의 랜딩(UX-021)에도 표시되어 **AC-040 사전 동의의 정보량을 늘린다(안전장치 강화 방향, 약화 아님)**. 난이도는 AC-040/041/042/043 어떤 안전제약도 게이팅하지 않는다(D-42).
> **v1.10 갱신(2026-07-24, D-31·D-33, T54, AC-056/058)**: (1) **진입 순서 상향**: "지인에게 보내기"를 고르는 UX-026이 이제 유형 선택 직후(시나리오 선택보다 먼저)에 온다 — 발신자는 UX-015(유형)→UX-026("지인에게 보내기")→(보이스면 UX-016 방식)→시나리오 선택→UX-019로 간다(v1.9의 "시나리오 선택 후 UX-026"에서 순서만 앞당김). (2) **generic 보이스 챌린지 신설(AC-058)**: 보이스 "지인에게 보내기"에서 clone뿐 아니라 **generic 시나리오도** 챌린지로 보낼 수 있다. generic 보이스 챌린지는 클론 온보딩(UX-002/003)을 **건너뛰고**(voiceId 없음, GENERIC_VOICE_ID 폴백) UX-019로 직행 — 음성 없는 메신저 챌린지(AC-051)와 동형의 "clone/voiceId 경로 미탑재" 부분집합. clone 챌린지는 기존(AC-044) 무변경. UX-019에 **clone vs generic 카피 구분**을 둔다(D-33). Related AC에 **AC-056·AC-058** 추가.
> **v1.9 갱신(2026-07-24, D-30, T48 — D-27 반전)**: 챌린지 생성의 **진입점이 바뀐다**(메커니즘·화면은 무변경). 기존엔 발신자가 **별도 "챌린지 만들기" 진입점**으로 시작해 드릴다운을 챌린지 모드로 타고 UX-019에 도달했으나, 이제는 **일반 드릴다운으로 시나리오를 고른 뒤 UX-026(체험 선택)에서 "지인에게 보내기"를 선택**해 UF-004에 진입한다. **별도 "챌린지 만들기" 진입점은 은퇴**(retire)한다 — 챌린지는 시나리오 하나로 확정되므로(OQ-29/AC-051) 시나리오를 안 고르고 챌린지부터 만드는 경로가 없어 은퇴가 어떤 흐름도 깨지 않는다. 순서 정합: 보이스 clone 챌린지의 **클론 온보딩(UX-002→003)이 이제 시나리오 선택·"지인에게 보내기" 선택 이후**에 온다(자기훈련 UF-002가 이미 "시나리오 먼저→녹음"인 것과 동일한 순서로 정렬됨). 아래 Trigger·Steps 1~2의 "별도 진입점으로 시작·클론 먼저" 서술은 이 노트로 "UX-026 지인에게 보내기 경유·시나리오 먼저"로 갱신된 것으로 읽는다. Related AC·화면(UX-019/020)·안전제약은 무변경.
> **v1.8 갱신(2026-07-24, D-27, T46, AC-051/055)**: 발신자 흐름에 **음성 없는 메신저 챌린지 변형**을 추가한다. 발신자가 메신저(카톡/문자, 비에스컬레이션) 시나리오를 보낼 때는 **step1의 클론 온보딩(UX-002→UX-003)을 건너뛰고**(AC-051) UX-013(로그인)→UX-015(유형 선택)→**UX-024(메신저 시나리오 선택, 챌린지 생성 모드·비에스컬레이션만)**→UX-019(챌린지 만들기, voiceId 없음)로 간다. 이 변형은 voiceId·클론·createRealtimeCall을 타지 않는다. **진입점은 기존 "챌린지 만들기" 별도 메뉴를 그대로 쓰고 시나리오 카드에 "지인에게 보내기"를 통합하지 않는다(D-27, 권고)**. 활성 챌린지 상한(3개)은 보이스+메신저 전역 합산(OQ-30). 챌린지는 생성 시점에 시나리오 하나로 확정(OQ-29, 기존 패턴 동일). 결과 열람은 메신저에서 **완료 여부만**(AC-055, UX-020 변형). Related AC에 **AC-051·AC-055·AC-053·AC-054** 추가. **에스컬레이션 가능 메신저 챌린지(#21)·OQ-28은 범위 밖 — UX-024는 이 흐름에서 비에스컬레이션 시나리오만 노출한다.** UX-024의 챌린지-생성 모드 Exit(→UX-019, createSession 대신)는 진입 맥락에 따른 재사용이며 세부는 architect.
> **v1.6 신설(2026-07-23, D-19, T34)**: 사용자1이 자기 목소리를 클론해 지인(사용자2)에게 비동기 딥보이스 챌린지를 보내는 흐름의 **발신자(사용자1) 측**. 링크 발급·공유까지가 사용자1의 능동 단계이고, 결과 열람은 사용자2 완료·동의 후 제한적으로 가능. 동기(실시간 조종) 방식은 배제(AC-044).
| Item | Value |
|---|---|
| Actor | 사용자1(챌린지를 만들어 보내는 사람 — 로그인된 본인 계정). |
| Trigger | 사용자1이 유형 선택(UX-015) 직후 **UX-026(체험 선택)에서 "지인에게 보내기"를 선택**하고, (보이스면 UX-016 방식 선택 후) 시나리오를 고른다(v1.10, D-31 — v1.9의 "시나리오 선택 후 UX-026"에서 순서만 앞당김). |
| Related Acceptance Criteria | AC-044(비동기 clone 챌린지 흐름), **AC-058(generic 보이스 챌린지 신설·클론 미탑재)**, **AC-056(체험/발송 선택 상향)**, AC-041(복제 음성 스코프 고정·기간제·추출 차단 — clone 챌린지에만, 화면은 삭제/보존 표기 수준), AC-043(결과 열람 제한 — clone은 완료/의심 시점, generic 보이스·메신저는 완료 여부만 AC-055/OQ-32), AC-049(오용 방지 — 화면은 개수 초과 상태만, 전역 합산), AC-042(사용자2 종료 정체 공개 — 흐름상 연결) |
| Steps | 1. 로그인(UX-013, 기존 재사용) 상태에서 유형 선택(UX-015)→**UX-026("지인에게 보내기")**→(보이스면 UX-016에서 clone/generic 선택)→시나리오 선택(UX-017/UX-024)까지 드릴다운으로 좁힌다(UF-003 재사용, v1.10 순서). 2. **클론 확보 분기**: **(보이스 clone 챌린지)** 본인 목소리 클론 온보딩(UX-002 녹음 → UX-003 클론 대기, 기존 재사용)을 완료한다(이미 클론 보유 시 재사용, AC-041 스코프 고정). **(보이스 generic 챌린지, AC-058)** 클론 온보딩을 **건너뛴다**(voiceId 없음·GENERIC_VOICE_ID 폴백). **(메신저 챌린지, AC-051)** 클론 온보딩을 건너뛴다(voiceId 없음). 3. 챌린지 만들기(UX-019)에서 **표시이름**(사용자2에게 보일 "○○님이 준비" 문구용)을 입력하고 "챌린지 만들기"를 누르면 앱이 챌린지 레코드(scenarioId + [clone일 때만]voiceId + 표시이름)를 생성하고 **공유 링크(토큰)**를 발급한다(AC-044/058). 화면은 챌린지 종류(clone 딥보이스 / generic 보이스 / 메신저)에 맞는 카피를 보인다(D-33). 4. UX-019의 공유 UI(OS 공유 시트 또는 링크 복사, P-14)로 링크를 받아 **사용자1이 직접** 카톡/문자에 붙여 넣어 사용자2에게 보낸다(앱이 대신 전송하지 않음, D-20). 5. 이후 사용자2가 완료하면, 챌린지 결과 열람(UX-020)에서 **사용자2가 동의한 경우에만** 제한된 결과를 본다 — **clone 보이스 챌린지는 "완료 여부 / 의심 시점"**(AC-043), **generic 보이스·메신저 챌린지는 "완료 여부만"**(AC-055/OQ-32 default, D-34). |
| Alternative Flow | (a) 이미 등록해 둔 목소리 재사용: 새로 녹음하지 않고 기존 클론으로 챌린지 생성(AC-046 목소리 재사용 취지와 정합, 다만 상세 선택 UI는 이번 범위 밖 — 복수 목소리 보관은 P-8 "내 목소리 금고"·architect T35). (b) 사용자2 미동의/미완료: UX-020은 상세 결과를 감추고 "아직 완료되지 않았거나 상대가 결과 공유에 동의하지 않았습니다"만 표기(AC-043). |
| Failure Flow | (a) 활성 챌린지 개수 상한 도달(AC-049) → UX-019가 "동시에 만들 수 있는 챌린지 수를 초과했습니다" 상태(수치·정책은 architect, 화면은 상태만). (b) 링크 발급 실패(네트워크 등) → UX-019 오류 + 재시도(P-4). (c) 클론 미완료 상태로 챌린지 시도 → 온보딩(UX-002/003)으로 되돌림(진입 가드). |
| Success Criteria | 사용자1이 자기 클론으로 챌린지 레코드를 만들고 공유 링크를 발급받아 직접 공유하며, 사용자2 동의 시에 한해 제한된 결과(완료/의심 시점)만 확인한다. 복제 음성은 다운로드·추출 경로가 UI에 없다(AC-041). |

### 2인 소셜 훈련 — 사용자2(동의·체험·정체공개·해설) (Flow ID: UF-005)
> **v1.11 갱신(2026-07-25, D-35·D-39·D-42)**: (1) 동의 랜딩(UX-021)이 **발신자가 고른 난이도**를 함께 보여준다(고급이면 "강한 압박" 고지 — AC-040 동의 정보량 증가, 게이트 로직 무변경). (2) 사용자2의 통화 체험(UX-014 재사용) 중에도 **통화 중 문자 오버레이(UX-027, UF-008)**가 동일하게 성립한다 — 익명 세션이라도 오버레이는 라우트 이동이 아니라 화면 위 계층이라 진입 맥락과 무관하다. (3) 사용자2도 종료 후 `deceivedMoments`가 있으면 **UX-028(즉시 되감기)**를 쓸 수 있으나, **강제 정체 공개(UX-007)→강제 리플레이 해설(UX-018) 순서를 건너뛰지 않는다** — 되감기는 UX-018 **이후**의 선택 단계다(AC-042의 강제 단계를 대체하지 않음).
> **v1.8 갱신(2026-07-24, D-28·D-29, T46, AC-053/054/055)**: 수신자 흐름에 **메신저(카톡/문자) 체험 변형**을 추가한다. 챌린지 채널이 메신저면 UX-021 동의 랜딩은 **채널 인지 문구**("지금부터 받는 메시지(카톡/문자)는 실제가 아닌 훈련")로 표시되고(음성/전화 프레이밍·"복제 음성" 표현 제거, 게이트 로직 AC-040은 채널 무관 그대로), 동의 후 **UX-014(통화) 대신 UX-022(메신저 채팅 셸)+UX-023(가짜 랜딩)**로 진입한다 — 단독 훈련과 동일한 정확 모사+무해화 안전속성을 **무약화 재사용**(AC-053/054·§13.4/13.7). 종료 시 강제 정체 공개(UX-007 2인 변형)·리플레이(UX-018)는 그대로 재사용(문구가 음성 특정이 아니라 메신저 변형 불요). 결과 공유 동의 시 발신자에게는 **완료 여부만** 전달(AC-055, 의심 시점 미전달·D-29). Related AC에 **AC-053·AC-054·AC-055** 추가. 사용자2는 ADR-0006대로 로그인 없이 익명 인증+토큰 진입 — 익명 uid 소유 messenger 세션 생성(consentChallenge 메신저 변형)은 architect 인계(§14.7이 보이스만 다룸). **P0는 비에스컬레이션만 — 통화 전이(UF-007)·UX-025는 이 흐름에서 발생하지 않는다(#21/OQ-28 보류).**
> **v1.6 신설(2026-07-23, D-19, T34)**: 초대받은 사용자2가 링크로 진입해 **명시적 동의(AC-040) 후에만** 복제 음성 체험을 하고, 종료 시 **강제 정체 공개(AC-042)** + 리플레이 해설을 받는 흐름. 사용자2는 별도 계정/로그인 없이 링크만으로 진입한다(AC-048).
| Item | Value |
|---|---|
| Actor | 사용자2(초대받은 지인 — 로그인 없이 링크로 진입). |
| Trigger | 사용자2가 카톡/문자로 받은 챌린지 링크를 연다. |
| Related Acceptance Criteria | AC-040(사전 동의·무동의 차단), AC-042(강제 정체 공개+해설), AC-044(비동기 진입), AC-048(무로그인 링크 진입·만료 — 화면은 만료 상태만), AC-049(신고 — 화면은 신고 버튼 위치만), AC-038(리플레이 해설), AC-022/AC-023(합성 표식·종료 고지 재사용) |
| Steps | 1. 링크를 열면 동의 랜딩(UX-021)이 뜬다 — "○○님이 보이스피싱 인식 테스트를 준비했고, 지금부터 받는 전화·문자는 실제가 아닌 훈련입니다"를 읽고 **명시적으로 탭 동의**한다(AC-040). **동의 전에는 어떤 복제 음성도 재생되지 않으며, 무동의로 진행하는 경로 자체가 UI에 없다**(P-15). 2. 동의 후, 사용자1의 클론 목소리로 걸려오는 전화를 체험한다 — **기존 통화 셸 UX-014 재사용**(수신→오프닝 딥보이스(사용자1 클론)→실시간 역할극, 합성 표식·상시 종료 유지). (문자 표면 재사용은 T24 미설계 — OQ-U12.) 3. 종료 조건(사용자 종료·한도) 도달 시 **강제 정체 공개** — UX-007(종료 고지) 재사용, 문구를 2인 변형("○○님이 준비한 훈련이었습니다. 실제 상황이 아닙니다")으로. 정체 공개는 옵션이 아니라 필수 단계다(AC-042). 4. 이어서 리플레이 해설(UX-018)로 대화를 되짚으며 사기 신호·대처를 확인한다(AC-042·AC-038). 5. (선택) 결과를 사용자1과 공유할지 동의하면, 그 결과가 UF-004의 UX-020에 "완료/의심 시점" 수준으로 전달된다(AC-043 — 미동의 시 전달 안 됨). |
| Alternative Flow | (a) 만료·소진된 링크: UX-021이 "이 링크는 만료되었습니다" 상태로 진입 차단(AC-048, 만료 수치는 architect). (b) 원치 않는 챌린지: UX-021에서 "원치 않는 챌린지 신고" 버튼으로 신고(AC-049 — 화면은 버튼·확인만, 정책·데이터 모델은 architect). |
| Failure Flow | (a) 동의 없이 이탈 → 세션 미시작(복제 음성 미재생, AC-040 충족). (b) 통화 체험 중 오류 → UX-014의 기존 Failure(P-1/P-4) 재사용, 통화 유지. (c) 링크 유효하나 챌린지 데이터 로드 실패 → UX-021 오류 + 재시도(P-4). |
| Success Criteria | 사용자2가 명시적 동의 후에만 체험하고, 종료 시 반드시 정체가 공개되며 리플레이 해설을 본다. 무동의 상태로 복제 음성이 재생되는 경로가 존재하지 않는다(AC-040). |

### 메신저피싱 훈련 (Flow ID: UF-006)
> **v1.11 갱신(2026-07-25, D-41·D-39·D-35)**: (1) 메신저 시나리오를 고른 뒤에도 **UX-029(난이도)**를 거쳐 `createSession`(또는 에스컬레이션 시 UX-025)으로 간다. (2) 종료 후 `deceivedMoments`가 있으면 UX-028(즉시 되감기, UF-009)로 갈 수 있다. (3) **통화 중 문자 오버레이(UX-027)는 메신저 채팅(UX-022)에서는 발생하지 않는다** — 이미 메시지 표면 안이라 "통화를 유지한 채 문자를 본다"는 문제 자체가 없기 때문. 단 UF-007로 통화(UX-014)에 전이한 뒤에는 UX-027이 정상 성립한다.
> **v1.10 갱신(2026-07-24, D-31, T54, AC-056)**: 메신저 자기훈련 진입 순서가 바뀐다 — 이제 **UX-015(메신저)→UX-026("본인이 체험")→UX-024(메신저 시나리오 선택)**로, 체험/발송 선택이 시나리오 선택보다 **먼저** 온다(AC-056). "본인이 체험" 모드에서는 UX-024가 **에스컬레이션 가능 시나리오를 포함한 전체 메신저 시나리오**를 노출한다(자기훈련은 에스컬레이션 제약 없음). 비에스컬레이션 시나리오는 `createSession`→UX-022, 에스컬레이션 가능 시나리오는 UX-025(목소리 선택)→UX-022로 이어진다(둘 다 무변경). 채팅 셸·스미싱·전이 등 UF-006 본체 메커니즘은 무변경. (v1.9의 "시나리오 선택 후 UX-026 경유"는 이 노트로 순서만 앞당겨진다.)
> **v1.9 갱신(2026-07-24, D-30, T48)**: 메신저 **비에스컬레이션** 시나리오의 자기훈련 진입은 이제 UX-024에서 곧바로 UX-022로 가지 않고 **UX-026(체험 선택)에서 "본인이 체험"을 고른 뒤** (createSession 후) UX-022로 온다(같은 시나리오를 "지인에게 보내기"로 보내는 경로가 UX-026에 함께 생겼기 때문). **에스컬레이션 가능 시나리오는 챌린지 대상이 아니라(#21/OQ-28 보류) UX-026을 거치지 않고 기존대로 UX-025(목소리 선택)→UX-022로 직행**(무변경). 채팅 셸·스미싱·전이 등 UF-006 본체 메커니즘은 무변경. *(v1.10 D-31로 순서 상향 — 위 노트 참조.)*
> **v1.7 신설(2026-07-23, D-22, T24)**: UX-015 최상위 2분류에서 "메신저피싱"을 고른 사용자의 훈련 흐름. D-17에서 스텁으로 남겼던 메신저 분기의 실제 목적지다. 카카오톡형·문자메시지형 표면을 스킨 레이어로 재현하며, 스미싱 링크·에스컬레이션 등 메신저 고유 수법을 안전하게 체험시킨다. Architecture §13.4/13.5와 정합.
| Item | Value |
|---|---|
| Actor | 훈련 대상 본인(UF-001 완료 상태). |
| Trigger | UX-015(훈련 유형 선택)에서 "메신저피싱"을 선택. |
| Related Acceptance Criteria | AC-030(표면 2종·복수 시나리오), AC-031(스킨 자동 감지·폴백·수동 전환), AC-032(콘텐츠 무해화), AC-045(스미싱 가짜 랜딩), AC-047(카카오 면책 고지), AC-002(메타 표기), AC-035/037(종료 시 단일 리포트·교차채널 정합 — 에스컬레이션은 UF-007) |
| Steps | 1. UX-015에서 "메신저피싱" 선택 → **UX-026(체험 선택)에서 "본인이 체험"**(AC-056, v1.10) → 메신저 시나리오 선택(UX-024)에서 채널(카카오톡/문자)·제목·사기 유형·예상 소요·난이도(AC-002/030)를 확인하고 하나를 고른다("본인이 체험" 모드라 에스컬레이션 가능 시나리오도 노출됨). 2. **분기(에스컬레이션 가능 시나리오, scenario.escalation 존재)**: 조건부 목소리 선택(UX-025, UF-007)을 거쳐 통화용 voiceId를 미리 확보한다(D-25). **비(非)에스컬레이션 시나리오**: 이 단계를 건너뛴다. 3. 메신저 채팅 셸(UX-022)로 진입 — 접속 기기 UA로 문자 스킨(iOS/삼성/기본)이 자동 적용되고(§13.5), 카카오형이면 상시 면책 배너가 고정된다(AC-047). 모든 표면에 "AI 훈련용 모의" 표식·상시 "훈련 종료"가 노출된다. 4. 대화 중 스미싱 링크(displayText)를 탭하면 인앱 가짜 랜딩(UX-023)으로 이동해 정보 입력 유도를 체험한다(입력값 서버 미전송·가짜 피드백, AC-045) → 채팅으로 복귀. 5. (에스컬레이션 발생 시 UF-007로 전이) 또는 종료 조건(사용자 종료·max-turn) 도달 → 세션 종료·고지(UX-007) → 리포트(UX-008)·리플레이 해설(UX-018). |
| Alternative Flow | (a) 스킨 오판정: 사용자가 UX-022의 수동 스킨 토글로 iOS/삼성/기본을 직접 전환(AC-031, P-16) — 콘텐츠·안전은 불변. (b) 링크를 탭하지 않고 진행: 가짜 랜딩(UX-023) 없이 대화만으로 종료 가능. (c) 에스컬레이션 가능 시나리오라도 사용자가 끝까지 신호를 유발하지 않으면 max-turn 폴백으로 UF-007 전이(§13.3) 또는 대화 종료. |
| Failure Flow | (a) LLM 응답 실패·타임아웃(안전필터 차단·네트워크 등) → **T43 갱신(2026-07-24)**: 화면에 오류·재시도 버튼을 띄우지 않고 `completeWithFallback`(functions/src/llm/index.ts)이 그 턴만 조용히 규칙 기반 목업으로 대체해 대화를 끊김 없이 이어간다(이미 원자적으로 커밋된 사용자 턴이 답 없이 남는 것을 막기 위함, reviewer 리뷰 반영) — 사용자에게는 P-4의 "재시도" 버튼 대신 정상적인 다음 대사로만 보인다. (b) 가짜 랜딩 로드 실패 → 조용히 생략하고 채팅 지속(핵심 루프 비차단). (c) UA 감지 실패 → 기본 스킨 폴백(§13.5, skinSource="fallback") + 수동 토글 안내. |
| Success Criteria | 사용자가 표면(카카오/문자)에 맞는 채팅으로 메신저 사기를 체험하고, 스미싱 링크·면책 고지·모의 표식 등 안전장치가 유지된 채 종료 시 단일 리포트(AC-035)로 인계된다. 실 URL 이동·실 전송이 어디서도 발생하지 않는다(AC-032/045). |

### 메신저 → 보이스 에스컬레이션 전이 (Flow ID: UF-007)
> **v1.7 신설(2026-07-23, D-25·D-26, T25)**: 메신저 대화 중 사용자가 의심을 표하면 "사기범이 전화를 거는" 형태로 보이스 단계로 이어지는 전이 흐름. Architecture §13.1~13.3·13.6과 정합 — 트리거는 **서버가 전달하는 `escalation` 플래그**만 신뢰하고, 전이 연출·목소리 선택 UI만 UX가 설계한다. 통화 셸(UX-014)·안전장치는 기존 그대로 재사용(재설계 없음).
| Item | Value |
|---|---|
| Actor | 훈련 대상 본인(UF-006 메신저 세션 진행 중). |
| Trigger | (1) 역할극 LLM이 캐릭터로 발신한 sentinel 신호를 서버가 감지 → `escalation:{toChannel:"voice"}` 플래그, 또는 (2) 메신저 max-turn 폴백 자동 전이, 또는 (3) 사용자가 상시 노출 "전화로 확인" 명시 전환 버튼 탭(§13.2/13.3). |
| Related Acceptance Criteria | AC-034(트리거), AC-035(세션·기록 연속성·단일 리포트), AC-036(통화 셸·안전장치 재사용), AC-037(교차채널 리포트), AC-039(양방향 설계·MVP 단방향), AC-046(조건부 목소리 선택) |
| Steps | 1. (선행, 메신저 시나리오 선택 직후) 에스컬레이션 가능 시나리오면 조건부 목소리 선택(UX-025)에서 "이 시나리오는 통화로 이어질 수 있어 목소리가 필요할 수 있습니다" 안내를 보고 ①즉시 녹음(UX-002/003 재사용) ②기존 보관 목소리 재사용 ③남/여 기본 보이스 중 하나로 통화용 voiceId를 **미리** 정한다(D-25, §13.6). 2. 메신저 채팅(UX-022) 진행 중 위 세 트리거 중 하나가 발생하면 서버가 `escalation` 플래그를 실어 보낸다(사용자에게 sentinel 토큰은 노출되지 않음). 3. UX가 "사기범이 전화를 거는" 전이 연출(P-18)로 채팅 화면을 통화 수신으로 전환 → 기존 통화 셸 **UX-014의 incoming(수신) phase**로 이음새 없이 인계한다. 4. 이후는 UX-014 기존 흐름 — 수신→오프닝(미리 정한 voiceId로 합성)→실시간 역할극. 사전 고지·동의(이미 완료)·상시 종료·합성 표식·종료 고지 모두 유지(AC-036). 5. 종료 시 두 채널(메신저+보이스)을 함께 다루는 **단일 리포트**(UX-008)·리플레이 해설(UX-018)로 인계(AC-035/037 — 메신저 취약 시점 + 보이스 취약 시점 병합). |
| Alternative Flow | (a) 명시 전환 버튼: 사용자가 신호·폴백 전에 "전화로 확인"을 눌러 능동 전이(§13.3, 신호와 독립). (b) 목소리를 ③기본 보이스로 택한 경우: 통화 오프닝이 본인 클론이 아닌 남/여 프리셋으로 재생됨(clone 아님 정합, voiceSelectionSource="fallback_*"). (c) 사용자가 끝까지 신호를 안 만들면 max-turn 폴백으로 자동 전이. |
| Failure Flow | (a) 전이 시 통화 자격증명 발급 실패 → 재시도(P-4); 실패 지속 시 메신저 종료로 폴백해 단일 리포트는 보장(AC-035, 침묵 실패 금지). (b) 목소리 미확보 상태로 전이 시도(예외) → ③기본 보이스로 폴백(§13.6). (c) 조기 신호(사용자가 LLM 유도) → 조기 전이일 뿐 가드레일 우회 없음(§13.2, 통화 안전장치 그대로). |
| Success Criteria | 메신저에서 의심을 표한 사용자가 끊김 없이 통화로 이어지는 압박을 체험하고, 하나의 세션·하나의 리포트로 두 채널의 취약 시점을 모두 확인한다. 전이 연출이 방향 무관하게 재사용 가능한 구조로 설계된다(AC-039, 화면은 메신저→보이스만). |

### 통화 중 문자(SMS) 수신·확인 — 통화 유지 (Flow ID: UF-008)
> **v1.11 신설(2026-07-25, D-35~D-38)**: 통화 도중 사기범이 "문자 보냈어요"라며 계좌·링크·인증번호를 문자로 밀어 넣는 실제 수법을, **전화를 끊지 않은 채** 체험시키는 흐름. 사용자 요구 원문: *"돈을 요구하고 계좌를 알려주는 것까지는 좋은데, 이걸 문자 메세지로 링크 형태로 보내고, 문자로 전환되는데, 전화 연결은 유지되도록하는 ux 경험"*. **설계 제약(코드 확인)**: `/session/play`와 `/session/messenger`는 별도 라우트라 이동 시 통화 컴포넌트가 언마운트되어 실시간 세션·마이크가 끊긴다 → 이 흐름은 **라우트 이동을 하지 않고 통화 화면 위 오버레이로만** 성립한다(D-35). 같은 흐름이 사용자 신고 *"화면에 뜨는 인증번호를 불러달라는데 아무것도 보이는 게 없다"*도 해소한다(D-38).
| Item | Value |
|---|---|
| Actor | 통화 세션(UX-014) 진행 중인 사용자 — 본인 자기훈련(UF-002), 2인 사용자2(UF-005), 에스컬레이션 통화(UF-007) 어느 맥락이든 동일. |
| Trigger | 사기범 턴에 실린 구조화 문자 마커를 서버가 파싱해 "문자 도착"으로 전달한다(사기범이 "문자 보냈어요"라고 말하는 **그 턴**이 곧 도착 시점, D-36). UX는 서버가 준 구조만 렌더링하고 자유텍스트를 분류하지 않는다(AC-024 원칙 계승). |
| Related Acceptance Criteria | **재사용**: AC-045(인앱 가짜 랜딩 재사용 — 링크 탭 시 외부 이동 없음), AC-032/AC-033(콘텐츠 무해화·실 URL 금지), AC-005/AC-013(실제 사기 실행 정보 미노출), AC-022(합성·모의 표식 상시), AC-006(상시 종료 — 오버레이 안에서도), AC-019(PSTN 없음 — 인앱 전용), AC-026(문자 확인·인증번호 구술도 리포트 타임라인의 판정 입력). **⚠️ "통화 중 문자 오버레이"라는 신규 능력 자체를 규정하는 AC는 PRD에 아직 없다 — planner 소관(OQ-U15).** |
| Steps | 1. 통화(UX-014) 진행 중 사기범이 "계좌는 문자로 보내드렸어요 / 방금 인증번호 문자 갔죠?"라고 말한다. 2. 같은 순간 통화 화면 **상단에 문자 도착 배너**가 내려온다(발신번호 라벨 + 본문 앞부분 1줄 + "AI 훈련용 모의" 표식). 통화는 그대로 진행 중이다 — 타이머·발신자·"훈련 종료"가 계속 보인다. 3. 배너를 탭하면 **문자 오버레이(UX-027)**가 통화 화면 **위에** 열린다. 통화 셸의 축소 표시(발신자·경과 타이머·"통화 중" 상태·"훈련 종료")가 오버레이 상단에 **계속 남아 통화가 살아 있음을 보여준다**(P-20). 4. 오버레이에서 문자 본문(계좌 안내 / 링크 / 인증번호 중 하나)을 읽는다. **링크형이면** 링크 칩을 탭해 **기존 인앱 가짜 랜딩(UX-023)**으로 전환하고, 닫으면 오버레이로 → 다시 통화로 되돌아온다(D-37). **인증번호형이면** 6자리 모의 코드가 표시되어, 사용자가 그걸 통화로 불러주는 행위가 그대로 "속은 시점" 판정 입력이 된다(D-38). 5. "통화로 돌아가기"(또는 오버레이 바깥 탭·Esc)로 닫으면 **끊김 없이** 통화로 복귀한다. 6. 이후 통화 셸에 **"문자함"(N)** 컨트롤이 남아 언제든 다시 열 수 있다(도착한 문자 누적). 7. 통화 종료(사용자 종료·한도)는 기존대로 UX-007 → UX-008. 문자 확인·링크 탭·인증번호 구술은 하나의 세션 타임라인에 함께 기록된다(AC-026/AC-035 정합). |
| Alternative Flow | (a) 배너를 무시하고 통화만 이어가기: 문자를 한 번도 열지 않아도 통화·종료·리포트는 정상 진행된다(문자 확인은 필수 단계가 아니다). (b) 통화 중 여러 통의 문자 도착: 배너는 최신 1건만 띄우고 "문자함"의 미확인 수가 증가한다(배너 스택 미설계 — 저인지부하 D-1). (c) 오버레이를 연 채 사기범이 계속 말함: 사기범 음성은 계속 재생되고 자막도 계속 누적된다(통화가 멈추지 않음을 소리로도 알린다). |
| Failure Flow | (a) 사기범이 "문자 보냈어요"라고 말했는데 **마커가 없어 문자가 도착하지 않음** → UX는 자유텍스트를 분류하지 않으므로 이 불일치를 화면에서 보정할 수 없다. **콘텐츠 저작 책임**(시나리오 프롬프트가 문자 발송 대사와 마커를 함께 내도록 지시)이며 architect·implementer 인계 사항이다(OQ-U16). (b) 오버레이 렌더 실패 → 조용히 생략하지 말고 배너 자리에 "문자를 열지 못했습니다" 1줄 + 재시도(P-4). 통화는 유지된다(핵심 루프 비차단). (c) 가짜 랜딩(UX-023) 로드 실패 → 조용히 생략하고 오버레이로 복귀(기존 UF-006 Failure (b)와 동일 원칙). (d) 오버레이가 열린 상태에서 통화 한도 도달 → 오버레이를 자동으로 닫고 UX-007로 인계(고지 문구가 오버레이에 가려지지 않게 한다). |
| Success Criteria | 사용자가 **전화를 끊지 않은 채** 문자를 읽고 링크·인증번호를 다루는 실제 수법을 체험하며, 화면 어디에서도 실 URL 이동·실 전송·실 인증이 발생하지 않는다(AC-032/045). 오버레이가 열린 동안에도 "훈련 종료"(AC-006)와 "AI 훈련용 모의" 표식(AC-022)이 계속 도달 가능하다. |

### 통화 후 즉시 되감기 — 그 턴 다시 답해보기 (Flow ID: UF-009)
> **v1.11 신설(2026-07-25, D-39·D-40)**: 사용자 요구 — *"종료 직후 '이 순간 뭐라고 했어야 했나'를 그 자리에서 한 턴만 다시 해보기"*. **기존 UX-018(리플레이 해설)과의 차이가 이 흐름의 존재 이유**다: UX-018은 대화를 처음부터 읽으며 주석을 보는 **읽는 복기**(사용자 입력 없음, 전체 대화 대상), UF-009/UX-028은 속은 그 턴 **하나**로 되돌아가 **사용자가 직접 다시 답하고 즉시 판정을 받는 재연 드릴**(입력 있음, 단일 순간 대상)이다. 두 화면은 대체 관계가 아니라 "읽기 → 해보기"의 연속이다.
| Item | Value |
|---|---|
| Actor | 방금 세션을 마친 사용자(단독 UF-002/UF-006, 또는 2인 사용자2 UF-005 — 단 사용자2는 강제 정체 공개·강제 해설을 마친 뒤에만). |
| Trigger | 세션 종료 후 리포트가 생성되고 `deceivedMoments`가 **1건 이상**일 때, UX-007(종료 고지)·UX-008(리포트)·UX-018(해설)·UX-030(실패 아카이브)에서 "그 순간 다시 해보기"를 누른다. |
| Related Acceptance Criteria | **재사용**: AC-026(속은 시점·그 순간의 올바른 대처 — 이 흐름의 데이터 원천), AC-008/AC-009(리포트 분석 결과 재사용·안 속았을 때 명시), AC-038(사후 학습 단계라는 성격), AC-007(원 세션은 정확히 1리포트 — 되감기가 이를 훼손하지 않아야 함), AC-005/AC-013(실행 정보 미노출), AC-024(마스킹된 transcript만 표시). **⚠️ "재연 드릴"이라는 신규 능력 자체를 규정하는 AC는 PRD에 없다 — planner 소관(OQ-U15).** |
| Steps | 1. 종료 직후 UX-007(고지·안심)에서 "그 순간 다시 해보기"를 누른다(리포트 준비 완료 후 활성). 2. **UX-028**이 열려 **속은 순간의 맥락**을 보여준다 — 그 시점 앞뒤 대화 몇 턴(읽기 전용, 마스킹) + 사기범의 **문제의 그 대사**를 강조 + "이때 당신은 이렇게 답했습니다"(원래 답변). 3. "여기서 다시 답해보기" 입력창에 사용자가 **다시 답한다**(텍스트 기본, 음성은 선택 — 짧은 드릴이라 마이크 재권한 요구를 강제하지 않는다). 4. 즉시 피드백을 받는다 — 3단계 판정(**잘 대응했습니다 / 아직 위험합니다 / 판단하기 어렵습니다**) + 이유 1줄 + **모범 대처**(그 순간의 `correctAction`) 나란히 비교. 과신 표현("이제 안전/면역")은 쓰지 않는다(P-8). 5. 속은 순간이 여러 건이면 "다음 순간 다시 해보기"로 이동한다. 6. "리포트로 돌아가기"(UX-008) 또는 "대화 전체 되짚어보기"(UX-018) 또는 "실패 아카이브"(UX-030)로 나간다. |
| Alternative Flow | (a) **한 번도 속지 않은 세션**: 되감기 진입점을 **아예 띄우지 않고**, UX-007/UX-008이 기존대로 "속지 않았습니다"(AC-009)를 명시하며 "잘 대응한 지점 되짚어보기"(UX-018)를 권한다. 근거: 되감을 순간의 데이터(`deceivedMoments`)가 비어 있고, "아깝게 넘어갈 뻔한 순간(near-miss)"은 현재 분석 산출물에 존재하지 않아 **발명할 수 없다**(D-40, 신설 여부 OQ-U20/R-8). (b) 다시 답하지 않고 모범 대처만 보기: 입력 없이 "모범 대처 보기"만 눌러도 된다(강제 입력 아님). (c) 나중에 되감기: 훈련이 끝난 뒤에도 UX-030(실패 아카이브)에서 과거의 속은 순간으로 진입할 수 있다(UF-010). |
| Failure Flow | (a) 리포트 생성 지연으로 `deceivedMoments`가 아직 없음 → UX-007의 되감기 버튼은 **비활성 + "리포트를 준비하고 있어요" 1줄**(침묵 실패 금지). (b) 사용자 새 답변의 **판정 실패·타임아웃** → 조용히 넘기지 말고 "이번 답변은 판정하지 못했습니다"를 명시하고 **모범 대처만** 보여준다(학습 가치 보존, P-4). (c) transcript/분석 로드 실패 → 재시도(P-4). (d) 직접 URL 진입 등으로 `deceivedMoments`가 비어 있는 상태 진입 → UX-028의 Empty 상태("되감을 순간이 없습니다") + UX-018 안내. |
| Success Criteria | 사용자가 속은 그 순간으로 되돌아가 **직접 다시 답해 보고** 즉시 피드백과 모범 대처를 비교한다. **원 세션의 리포트는 바뀌지 않는다**(AC-007 — 되감기는 새 리포트를 만들지 않으며, 원 리포트를 덮어쓰지 않는다. 저장 여부·모델은 architect, OQ-U17). |

### 실패 아카이브 — 속은 순간 모아보기·반복 학습 (Flow ID: UF-010)
> **v1.11 신설(2026-07-25, D-44·D-45)**: 사용자 요구 — *"내가 당했던 순간만 모아 보는 화면(반복 학습)"*. 기존 `/history`(UX-012)가 **세션 목록**인 데 반해 이 흐름은 **'속은 순간' 단위**로 모은다 — 한 세션에서 3번 속았으면 항목 3개다. 데이터 원천은 리포트의 `deceivedMoments`(속은 시점·수법·올바른 대처).
| Item | Value |
|---|---|
| Actor | 로그인된 본인(자기 데이터만). 2인 챌린지의 **사용자2(익명)는 대상이 아니다** — 계정이 없어 누적 기록이 성립하지 않고, 발신자에게 누적 노출되는 경로를 만들지 않는다(AC-043/AC-055 결과 열람 제한과 정합). |
| Trigger | UX-008(리포트) 또는 UX-012(히스토리)에서 "내가 속았던 순간 모아보기"를 누른다. |
| Related Acceptance Criteria | **재사용**: AC-026(속은 시점·수법·올바른 대처 — 항목의 3요소), AC-008/AC-009(리포트 분석 결과 재사용), AC-016(본인 계정 데이터 시간순 열람 — 세션 단위에서 순간 단위로 확장), AC-011(반복 학습·게임화 취지, P1), AC-005/AC-013(실행 정보 미노출), AC-043/AC-055(2인 결과 열람 제한 — 사용자2 데이터 미포함). **⚠️ "속은 순간 단위 아카이브"라는 신규 능력 자체를 규정하는 AC는 PRD에 없다 — planner 소관(OQ-U15).** |
| Steps | 1. UX-030(실패 아카이브)에 들어가면 **속은 순간 카드 목록**이 최신순으로 보인다. 카드 1장 = 속은 순간 1건: 언제(세션 날짜 + "N초 시점") · 어떤 시나리오·채널(보이스/메신저) · **어떤 수법에 넘어갔는지**(`tactic`) · **그때 올바른 대처**(`correctAction`, 접어두기 기본). 2. "수법별로 묶기"를 켜면 같은 `tactic`끼리 묶여 **"이 수법에 3번 넘어갔습니다"**처럼 반복 패턴이 드러난다(반복 학습 목적, D-45). 3. 카드에서 두 가지 재훈련 동선으로 나간다 — **"그때 대화 보기"** → UX-018(리플레이 해설, 해당 시점으로 이동) / **"그 순간 다시 해보기"** → UX-028(즉시 되감기, UF-009). 4. **"이 시나리오 다시 훈련"** → **시나리오 목록에 해당 시나리오가 프리셋으로 선택된 채 재진입** → "다음"으로 UX-029(난이도 선택) → 새 세션. ⚠️ **UX-029 직행이 아닌 이유**(2026-07-25 구현 판단, reviewer m2): 노출 필터(AC-029/AC-057/AC-051) 재검증이 시나리오 목록 화면에 있어 직행하면 그 검증 지점이 사라진다 — 1탭이 늘지만 더 방어적인 쪽을 택했다. 난이도는 다시 고르게 한다(같은 난이도 반복이 목적이 아니라 강도를 올려 재도전할 수 있어야 하므로). |
| Alternative Flow | (a) 항목이 많을 때: 최신순으로 일정 개수씩 보여주고 "더 보기"로 이어 붙인다(페이지네이션 방식·수치는 architect, OQ-U19). (b) 이미 삭제·폐기된 세션의 순간: 리포트 분석 결과는 남고 음성·합성물은 폐기되므로(AC-021) 카드는 **텍스트 정보만** 보여준다(오디오 재생 경로 없음). |
| Failure Flow | (a) 목록 로드 실패 → 재시도(P-4). (b) 특정 항목의 원 세션 transcript가 없어 "그때 대화 보기"가 불가 → 해당 버튼만 비활성 + 사유 1줄(침묵 실패 금지), 나머지 정보는 그대로 표시. |
| Success Criteria | 사용자가 자기가 실제로 넘어간 순간들만 모아 보고, **같은 수법에 반복해 넘어가는 패턴**을 스스로 인지하며, 거기서 곧바로 되감기·재훈련으로 이어간다. 어떤 항목도 과신 표현("이제 면역")을 쓰지 않고 "개선 영역" 프레임을 유지한다(P-8). |

## Screen Catalog
> 화면은 플로우 진행 순서로 나열한다. 로그인(UX-013)은 플로우상 첫 화면이지만 ID는 신설 순서에 따라 UX-013이다(ID 불변 규칙 — 기존 ID를 재번호하지 않는다).

### 로그인 / 가입 (Screen ID: UX-013)
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-001 |
| Acceptance Criteria | AC-016(계정 생성/접근 부분) |
| Purpose | P0 진입점. 사용자가 **Google 계정으로 로그인**해 이후 동의·클론·리포트를 본인 계정에 귀속시킨다(소유권=본인, OQ-11 확정 정합). Google 최초 인증 시 계정이 자동 생성되므로 별도 가입 단계가 없다. |
| User Goal | Google 계정으로 로그인하여 훈련을 시작한다. |
| Entry | 서비스 최초 진입(랜딩). |
| Exit | 로그인/가입 성공 → UX-001. |
| Primary Actions | "Google로 로그인" 버튼 하나(Firebase Auth Google Provider OAuth 팝업/리다이렉트 시작). 별도 이메일/비밀번호 입력 필드·가입 폼 없음(Google 계정 최초 인증 시 계정이 자동 생성되므로 로그인=가입 단일 동작). |
| Secondary Actions | 재시도(취소·실패 후). |
| States | Loading: "Google로 로그인" 탭 후 OAuth 팝업/리다이렉트 진행 중 스피너(버튼 비활성) / Empty: N/A(버튼 1개 노출) / Error: (a) 사용자가 Google 팝업을 닫음/취소, (b) 팝업 차단됨, (c) 네트워크·OAuth 실패 → 각 케이스별 안내 문구 + "다시 시도" / Success: 인증 완료(uid 확보) 후 UX-001로 이동. |
| Validation | 입력 폼이 없어 필드 검증 없음. 처리 중 중복 탭 방지(버튼 비활성, P-4). |
| Failure | Google OAuth 특유 실패: (a) 팝업 사용자 취소(auth/popup-closed-by-user), (b) 팝업 차단(auth/popup-blocked — 리다이렉트 방식 폴백 안내), (c) 네트워크/제공자 오류 → 오류 상태 + "다시 시도"(Interaction Pattern P-4). |
| Accessibility | 큰 글씨·단순 화면(어르신 우선, 버튼 1개로 인지부하 최소). "Google로 로그인" 버튼은 스크린리더 라벨 명시·큰 터치 타깃(≥48px)·키보드로 도달·Enter 실행. 오류는 아이콘+텍스트(색 단독 금지). 팝업 차단 시 텍스트 안내. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical (사용자 결정으로 P0 상향) |
| Business Rules | 이후 모든 화면(동의·녹음·클론·세션·리포트)은 인증된 사용자 계정에 귀속된다. 계정 소유권=본인(OQ-11 확정). **자녀가 부모 계정을 대신 만드는 경로는 설계·구현하지 않는다**(D-7, 빌드 스코프 밖). 생성물(클론 음성)은 계정에 영구 저장하지 않고 세션 종료 시 폐기(AC-021)되며, 계정에는 리포트·메타만 귀속. |
| Data Required | Google OAuth로 반환되는 사용자 식별자(uid)·기본 프로필(이름/이메일), 인증 토큰/세션, 사용자 프로필 레코드(uid). |
| Data Operations | Create(최초 로그인 시 프로필 자동 생성), Read(세션 검증), Update(세션 갱신) |
| External Dependencies | **Firebase Auth — Google Provider(OAuth)** (`signInWithPopup`/`signInWithRedirect` 계열). 프로필 레코드는 Firestore. 이메일/비밀번호·전화번호 등 다른 프로바이더는 사용하지 않음(OQ-U5 확정). |
| Permissions | None(Google OAuth 동의 화면은 Google 측에서 처리) |
| Navigation Targets | UX-001 |
| Events Emitted | `login_succeeded`(최초 로그인은 계정 자동 생성 포함), `auth_failed`(취소/차단/네트워크 구분 사유 포함) |
| Expected Outputs | 인증된 사용자 세션(uid) — 이후 모든 화면의 데이터 귀속 키. |
| Assumptions | 네트워크 연결됨. Firebase Auth Google Provider 사용 가능(T2 스캐폴딩에 포함). **사용자가 사용할 Google 계정을 이미 보유하고 있다고 가정**(미보유 시 처리는 OQ-U6). |

### 사전 고지 + 명시적 동의 게이팅 (Screen ID: UX-001)
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-001 |
| Acceptance Criteria | AC-012, AC-017 (AC-023 사전 문구 일부) |
| Purpose | 첫 세션 전 훈련 성격을 고지하고, 명시적 동의를 받아야만 다음 단계로 넘어가게 하는 보안 게이트. |
| User Goal | 이 서비스가 안전한 훈련임을 이해하고 동의하여 시작한다. |
| Entry | UX-013 로그인/가입 완료 후(유효 로그인 세션 보유 시 재진입 포함). |
| Exit | 동의 완료 → UX-011(age-gate, P1) 또는 UX-015(훈련 유형 선택 — 드릴다운 시작, v1.5 D-16; 구 UX-004 대체). *(v1.4: Phase B 순서 정합화 — 이전 "→ UX-002"는 잘못된 구(舊)순서였다. 녹음은 시나리오 선택 이후로 이동, D-14.)* |
| Primary Actions | 동의 체크박스 선택, "동의하고 시작" 버튼. |
| Secondary Actions | 고지 전문(시뮬레이션·실금전 없음·종료 방법) 펼쳐보기, 종료/나가기. |
| States | Loading: 없음(정적 콘텐츠) / Empty: N/A / Error: N/A / Success: 동의 체크 시 "동의하고 시작" 활성화 후 다음 화면 이동. |
| Validation | 동의 체크박스가 선택되지 않으면 "동의하고 시작" 비활성(AC-017). 체크 없이 진행 불가. |
| Failure | 사용자가 동의하지 않으면 화면에 머무름(세션 시작 차단). |
| Accessibility | 큰 글씨·단순 문구(어르신 우선). 동의 문구는 아이콘+텍스트로 명확. 키보드로 체크박스·버튼 도달 가능. 스크린리더가 고지 전문과 체크 상태를 읽음. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical |
| Business Rules | 동의(explicit consent) 없이는 어떤 세션도 시작될 수 없다(AC-017). 고지 문구에는 "시뮬레이션임·실제 금전/자격증명 미관여·언제든 종료 가능"이 반드시 포함된다(AC-012). |
| Data Required | 동의 이벤트(동의 여부·타임스탬프·동의 문구 버전). |
| Data Operations | Create(동의 기록) |
| External Dependencies | Firebase Auth(로그인은 UX-013에서 완료됨 — 이 화면은 인증된 세션을 전제로 함), Firestore(동의 로그를 사용자 계정에 연결 저장). |
| Permissions | None |
| Navigation Targets | UX-011(P1) 또는 UX-015 |
| Events Emitted | `consent_granted`, `session_flow_started` |
| Expected Outputs | 다음 단계로 넘길 동의 완료 상태(사용자 계정에 연결된 동의 로그 레코드). |
| Assumptions | 네트워크 연결됨. **사용자는 UX-013에서 이미 로그인/인증된 상태다**(OQ-U2 resolved: P0에 로그인 필요). 동의 로그는 로그인 계정에 귀속된다. |

### 본인 목소리 녹음 + 본인 확인 (Screen ID: UX-002)
> **v1.4 갱신(2026-07-22, D-14)**: 소속 플로우를 UF-001 → **UF-002**로 이관. Entry가 "UX-001 동의 완료"에서 "UX-004에서 clone 시나리오 선택 후"로 바뀐다(Phase B — 시나리오 선택이 녹음보다 먼저). generic 시나리오는 이 화면을 아예 거치지 않는다.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002 |
| Acceptance Criteria | AC-020, AC-018(녹음 부분) |
| Purpose | 본인 확인·본인 동의를 거친 뒤 지정 대본 30초를 녹음해 클론 입력을 만든다. 타인 목소리 무단 등록 경로를 UI에서 원천 차단(AC-020). **clone 시나리오(본인 목소리 사칭)에서만 진입** — generic 시나리오(기관 사칭)는 이 화면을 생략한다. |
| User Goal | 내 목소리를 30초 녹음해 등록한다. |
| Entry | UX-017에서 clone 시나리오 선택(v1.5 — 구 UX-004 대체; 동의·연령 확인 통과가 진입 가드로 강제됨 — 미충족 시 UX-001/UX-011로 되돌림, 코드 `onboarding/record` 가드). |
| Exit | 녹음 확정 + "클론 생성" → UX-003. |
| Primary Actions | 본인 확인 문항 체크("이 목소리는 본인의 것이며 본인이 직접 녹음합니다"), 녹음 시작/정지, 녹음 재생 확인, "클론 생성". |
| Secondary Actions | 재녹음, 대본 다시 보기. |
| States | Loading: 녹음 업로드 중 스피너 / Empty: 녹음 전(대본·녹음 버튼 노출) / Error: 마이크 권한 거부·녹음 실패·업로드 실패 안내 + 재시도 / Success: 30초 녹음 확보 → "클론 생성" 활성화. |
| Validation | 본인 확인 체크 필수(미체크 시 녹음 버튼 잠금, AC-020). 녹음 길이 최소치(≈30초) 미달 시 "클론 생성" 비활성. 대본 낭독 유도(정확 대사 일치 검증은 하지 않음 — 하루 스코프). |
| Failure | 마이크 권한 거부 → 권한 안내 상태(Interaction Pattern P-6). 녹음/업로드 실패 → 재시도. |
| Accessibility | 녹음 버튼은 큰 터치 타깃(≥48px)·명확한 라벨. 녹음 중 상태를 색+아이콘+텍스트로 표기(색만으로 구분 금지). 스크린리더가 녹음 상태("녹음 중", "녹음 완료")를 읽음. 대본은 큰 글씨. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical |
| Business Rules | 본인 목소리만 등록 가능 — 파일 업로드/타인 음성 입력 경로를 UI에 두지 않는다(AC-020). 녹음/등록 전 본인 확인·본인 동의 통과가 강제된다. |
| Data Required | 30초 오디오 blob, 본인 확인 동의 플래그, 지정 대본 텍스트. |
| Data Operations | Create(오디오 업로드), Read(대본) |
| External Dependencies | 브라우저 MediaRecorder/마이크 API, Firebase Storage(임시 오디오 업로드 — 세션 종료 시 폐기 대상, AC-021). |
| Permissions | 마이크(getUserMedia) |
| Navigation Targets | UX-003 |
| Events Emitted | `identity_confirmed`, `voice_recorded`, `voice_uploaded` |
| Expected Outputs | 클론 입력용 오디오 파일 참조(Storage 경로)와 본인 확인 로그. |
| Assumptions | 사용자 기기에 마이크가 있고 권한을 부여한다. 본인 확인은 경량 자기확인(체크+문구)으로 처리하며 강한 신원인증(KYC)은 하루 스코프 밖(OQ-U1). |

### 클론 생성 대기 (Screen ID: UX-003)
> **v1.4 갱신(2026-07-22, D-14)**: 소속 플로우를 UF-001 → **UF-002**로 이관. Exit이 "→ UX-004"에서 "→ UX-014(통화)"로 바뀐다(Phase B — 시나리오는 이미 UX-004에서 골랐으므로 클론 완료 후 곧바로 통화로 진입. 코드 `clone/wait`가 `createSession` 후 `/session/play`로 이동).
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002 |
| Acceptance Criteria | AC-018 |
| Purpose | ElevenLabs 클론 생성(수 초~수십 초) 동안 "멈춘 것처럼 보이는" 상황을 막고 데모 흐름을 유지한다. |
| User Goal | 내 목소리 클론이 만들어지는 동안 기다린다. |
| Entry | UX-002에서 "클론 생성". |
| Exit | 클론 생성 완료 → UX-014(통화). |
| Primary Actions | 없음(대기). |
| Secondary Actions | 취소/뒤로(재녹음으로 복귀). |
| States | Loading: 단계형 진행 표시("목소리 분석 중 → 클론 생성 중 → 준비 완료")·예상 시간 안내·진행 애니메이션(정지 인상 방지) / Empty: N/A / Error: 생성 실패·타임아웃 → 재시도 버튼 + 데모 폴백(사전 준비 음성) 안내 / Success: "준비 완료" 후 자동/버튼 이동. |
| Validation | N/A |
| Failure | 클론 생성 API 실패/타임아웃 → 오류 상태(재시도 또는 폴백 경로, Interaction Pattern P-1·P-4). |
| Accessibility | 진행 상태를 텍스트로도 안내(스크린리더). 애니메이션은 감소 모션 설정 존중. 대기 문구 큰 글씨. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical |
| Business Rules | 데모 안정성상 최대 대기 시간·타임아웃 후 폴백(사전 준비 클론/녹화본) 경로가 필요(OQ-U3). 진행 표시는 실제 단계와 무관해도 "멈춘 인상"을 주지 않아야 함. |
| Data Required | 클론 생성 작업 상태(진행/완료/실패), 생성된 voice/clone ID. |
| Data Operations | Create(클론 생성 요청), Read(작업 상태 폴링) |
| External Dependencies | Firebase Functions → ElevenLabs Instant Voice Cloning API. 상태 확인용 Firestore/폴링. |
| Permissions | None |
| Navigation Targets | UX-014 (성공), UX-002 (취소/재녹음) |
| Events Emitted | `clone_started`, `clone_succeeded`, `clone_failed` |
| Expected Outputs | 합성에 사용할 클론 voice ID. |
| Assumptions | ElevenLabs 클론이 하루 내 사용 가능한 한국어 품질을 낸다(PRD Assumption·T1 PoC로 사전 검증). 지연 목표·타임아웃 값은 T1 결과로 확정. |

### 훈련 유형 선택 (드릴다운 1단계) (Screen ID: UX-015)
> **v1.5 신설(2026-07-23, D-16, PRD v0.9 · T23)**: 드릴다운의 최상위 화면. 구 UX-004(평면 목록)를 대체하는 3화면 중 첫 번째. 여기서 사용자는 시나리오가 아니라 **훈련 "유형"**을 먼저 고른다.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-003 |
| Acceptance Criteria | AC-028(최상위 2분류), AC-029(단계적 흐름의 1단계) |
| Purpose | 훈련을 최상위 2분류 **(A) 보이스피싱 / (B) 메신저피싱**로 제시해, 사용자가 유형을 먼저 고르면 그 유형의 하위만 이후 단계에서 드러나게 하는 드릴다운의 진입점(AC-028/029). |
| User Goal | 어떤 종류의 사기를 훈련할지(전화 vs 메신저) 고른다. |
| Entry | UF-001 완료(UX-001 동의, P1 UX-011 age-gate 구현 시 그 통과 직후). |
| Exit | **(v1.10, D-31)** 보이스·메신저 **어느 유형이든 UX-026(체험/발송 선택)**으로 간다 — 유형 선택 직후 "본인이 체험 / 지인에게 보내기"를 먼저 묻는다(AC-056). 방식 선택(UX-016)·시나리오 선택(UX-017/UX-024)은 UX-026 이후로 이동. *(구 v1.9: 보이스→UX-016 / 메신저→UX-024 직행 — UX-026이 상향되며 대체.)* |
| Primary Actions | 유형 카드 2개 중 하나 선택 → 다음(또는 카드 탭 즉시 진입). "보이스피싱"·"메신저피싱" 각각 큰 카드 1개. |
| Secondary Actions | 각 유형 한 줄 설명 보기(예: 보이스="전화로 걸려오는 사기", 메신저="카카오톡·문자로 오는 사기"). |
| States | Loading: 없음(정적 2카드) / Empty: N/A(항상 2분류 노출) / Error: N/A / Success: 유형 선택 시 **UX-026(체험/발송 선택)**으로 이동(v1.10, 보이스·메신저 공통). |
| Validation | 유형 미선택 시 "다음" 비활성(카드 즉시 진입 방식이면 해당 없음). |
| Failure | 다음 단계 로드 실패 시 재시도(P-4). |
| Accessibility | 카드 2개는 큰 글씨·큰 터치 타깃(≥48px). 유형 구분을 색 아닌 라벨+아이콘(전화/말풍선)으로 이중 표기. 키보드로 두 카드 도달·Enter 선택. 스크린리더가 유형명과 설명을 읽음. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical(확장 P0 — 이 화면이 없으면 드릴다운 진입 불가) |
| Business Rules | 최상위 분류는 정확히 2개(보이스피싱/메신저피싱)로 고정(AC-028). 보이스피싱 하위는 신규 음성 데이터 모델을 도입하지 않고 기존 `voiceMode`(clone/generic)를 재활용(AC-028) — 단 방식 선택(UX-016)은 **UX-026 이후·"지인에게 보내기" 경로에서만** 노출된다(AC-057, D-32). **(v1.10, D-31) 이 화면은 유형만 정하고, 다음 단계는 유형 무관하게 UX-026(체험/발송 선택)이다**(AC-056) — 방식·시나리오·챌린지 여부는 모두 UX-026 하류에서 갈린다. |
| Data Required | 없음(유형 2종은 정적 상수). 다음 목적지는 유형 무관 UX-026. |
| Data Operations | 없음(선택 상태는 클라 로컬) |
| External Dependencies | 없음. |
| Permissions | None |
| Navigation Targets | **UX-026(체험/발송 선택 — 보이스·메신저 공통, v1.10)** |
| Events Emitted | `training_type_selected`(값: voice|messenger) |
| Expected Outputs | 선택된 훈련 유형(voice|messenger) → UX-026으로 전달(그 화면이 self/send 모드를 정함). |
| Assumptions | 사용자는 UF-001(로그인·동의)을 이미 통과함. 메신저 시나리오 콘텐츠(채널·표면 메타)는 T6/T29에서 준비됨. |

### 보이스피싱 목소리 방식 선택 (드릴다운 2단계) (Screen ID: UX-016)
> **v1.10 갱신(2026-07-24, D-32·D-33, T54, AC-057/058)**: 이 화면은 이제 **보이스 + "지인에게 보내기"(챌린지) 경로에서만** 노출된다. **보이스 + "본인이 체험"은 이 화면을 건너뛴다** — AC-057로 자기 체험은 generic 전용이라 방식이 하나로 강제되므로 UX-016을 띄우면 무의미한 1-옵션 화면이 되기 때문(D-1/D-32). 반대로 "지인에게 보내기"에서는 **clone·generic 두 방식 모두 유효한 챌린지 종류**(clone 딥보이스 챌린지 AC-044 + **generic 보이스 챌린지 신설 AC-058**)라 두 방식을 그대로 묻는다. 즉 진입 조건이 "보이스 유형"에서 "보이스 유형 + send 모드"로 좁혀졌고, 두 방식의 의미는 무변경. Entry가 UX-015에서 **UX-026("지인에게 보내기")**로 바뀐다.
> **v1.5 신설(2026-07-23, D-16, PRD v0.9 · T23)**: 보이스피싱 유형을 고른 사용자에게 **목소리 방식(clone/generic)**을 묻는 드릴다운 2단계. AC-028의 "보이스피싱 분류 하위는 기존 voiceMode를 재활용해 clone/generic으로 구분"을 화면으로 실현.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-003, UF-004(보이스 챌린지의 방식 선택 단계 — v1.10, send 모드 전용) |
| Acceptance Criteria | AC-028(보이스 하위 clone/generic), AC-029(단계적 흐름), **AC-057(본인이 체험 경로에서는 미노출)**, **AC-058(generic도 유효 챌린지 방식)** |
| Purpose | **"지인에게 보내기"를 고른 보이스 발신자**에게 챌린지 목소리 방식을 **본인 목소리 복제(clone 딥보이스 챌린지) / 기본 AI 음성(generic 보이스 챌린지)**으로 나눠, 이후 노출될 시나리오 집합과 클론 온보딩 필요 여부를 정한다(AC-028/058). |
| User Goal | 지인에게 **내 목소리(딥보이스)**로 보낼지, **기본 AI 음성**으로 보낼지 고른다. |
| Entry | **(v1.10, D-32)** UX-026에서 **보이스 + "지인에게 보내기"** 선택. *(보이스 + "본인이 체험"은 이 화면을 건너뛰고 UX-017 generic으로 직행 — AC-057.)* |
| Exit | clone 선택 → UX-017(clone 시나리오만 노출) → (클론 온보딩 후)UX-019. generic 선택 → UX-017(generic 시나리오만 노출) → (클론 스킵, AC-058)UX-019. |
| Primary Actions | 방식 카드 2개 중 하나 선택 → 다음(또는 즉시 진입). **① 본인 목소리 복제(clone)** = "내 목소리 30초 녹음 → 지인에게 그 목소리(딥보이스)로 걸려오는 전화를 보냄", **② 기본 AI 음성(generic)** = "녹음 없이 기본 AI 음성으로 걸려오는 전화를 보냄"(AC-058 신규). |
| Secondary Actions | 뒤로(**UX-026 체험/발송 선택**으로 복귀, P-12 — v1.10). 각 방식의 소요/필요 조건 설명 보기. |
| States | Loading: 없음(정적 2카드) / Empty: N/A / Error: N/A / Success: 방식 선택 시 UX-017로 이동. |
| Validation | 방식 미선택 시 "다음" 비활성(즉시 진입 방식이면 해당 없음). |
| Failure | N/A(정적 분기). |
| Accessibility | 두 방식 카드 큰 글씨·큰 터치 타깃(≥48px). "녹음 필요/불필요"를 색 배지 단독이 아니라 텍스트+아이콘(🎙/🔊)으로 이중 표기(구 UX-004 개편에서 검증된 관례 계승). 키보드 도달·Enter 선택. 스크린리더가 방식명·조건을 읽음. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical(확장 P0) |
| Business Rules | 방식은 정확히 2개(clone/generic)이며 **기존 `voiceMode` 값을 그대로 사용**한다 — 신규 음성 데이터 모델·신규 필드를 도입하지 않는다(AC-028). **(v1.10, D-32) 이 화면은 send 모드(지인에게 보내기)에서만 노출**되고, self 모드(본인이 체험)에서는 상류(UX-026)가 이 화면을 건너뛰고 generic으로 강제한다(AC-057). 이 화면은 필터 조건(voiceMode)만 정하고 세션·챌린지·녹음은 하지 않는다(그 분기는 UX-017 이후). **generic도 유효한 챌린지 방식**이며(AC-058) clone과 달리 클론 온보딩을 요구하지 않는다 — 이 화면은 방식만 정하고 클론 온보딩 필요 여부 판정은 UX-017/UX-019 하류. |
| Data Required | 상류에서 전달된 모드(send)·유형(voice). 방식 2종은 정적 상수(기존 `VoiceMode` 타입 재사용). |
| Data Operations | 없음(선택 상태는 클라 로컬). |
| External Dependencies | 없음. |
| Permissions | None |
| Navigation Targets | UX-017, **UX-026(뒤로 — v1.10)** |
| Events Emitted | `voice_mode_selected`(값: clone|generic) |
| Expected Outputs | 선택된 voiceMode + 모드(send) → UX-017의 시나리오 필터 조건·하류 챌린지 종류(clone 딥보이스 vs generic 보이스)로 전달. |
| Assumptions | 시나리오 데이터에 clone·generic 각각 최소 1종이 존재한다(현행: clone 2종·generic 7종). 특정 방식에 시나리오가 0건이면 UX-017이 빈 상태로 안내한다. 이 화면 진입 시점에 모드는 이미 send로 확정(UX-026). |

### 시나리오 노출·선택 (드릴다운 3단계) (Screen ID: UX-017)
> **v1.11 갱신(2026-07-25, D-41)**: Exit가 바뀐다 — 시나리오를 고르면 곧바로 `createSession`/UX-019로 가지 않고 **UX-029(난이도 선택)를 먼저 거친다**(self·send 모드 양쪽 동일). 카드에 이미 있는 고정 `difficulty` 문자열은 **삭제하지 않고** "이 시나리오의 기본 성향"으로 남겨 AC-002(시작 전 난이도 표시)를 계속 충족하되, **사용자가 고르는 난이도는 UX-029에서 별도로 정해진다**(두 표기의 라벨 정합은 OQ-U21). 시나리오 목록 자체는 난이도로 **필터링되지 않는다** — 모든 시나리오에 3단계가 전부 있다.
> **v1.10 갱신(2026-07-24, D-31·D-32·D-33, T54, AC-056/057/058)**: 이 화면은 이제 **UX-026에서 이미 정해진 모드(self/send)에 따라 노출 시나리오와 Exit가 갈린다** — 모드는 시나리오 선택보다 먼저 정해졌으므로(AC-056) 이 화면은 그 결과를 받아 동작한다. **① 본인이 체험(self)**: 상류(UX-026)가 UX-016을 건너뛰고 generic으로 강제했으므로 **generic 시나리오만** 노출 → 선택 → `createSession` → UX-014(통화). clone 시나리오는 self 모드에 **아예 노출되지 않는다**(AC-057, clone 자기체험 배제). **② 지인에게 보내기(send)**: UX-016에서 고른 voiceMode(clone|generic)의 시나리오만 노출 → 선택 → **UX-019(챌린지 만들기)**. clone은 클론 온보딩(UX-002/003) 후, generic은 클론 스킵(AC-058) 후 UX-019. v1.9의 "clone→UX-026 / generic→createSession→UX-014" 분기는 이 노트로 대체된다(선택 순서가 뒤집혀 모드가 먼저).
> **v1.9 갱신(2026-07-24, D-30, T48)**: **clone 시나리오**의 Exit가 UX-002(녹음) 직행에서 **UX-026(체험 선택)**으로 바뀐다 — clone 시나리오는 "지인에게 보내기"(챌린지)가 가능하므로 "본인이 체험/지인에게 보내기"를 먼저 묻는다. **generic 시나리오**는 챌린지 대상이 아니라(딥보이스=clone만, AC-044) UX-026을 거치지 않고 기존대로 `createSession` 후 UX-014로 직행(무변경). 아래 Exit의 "clone→UX-002"는 이 노트로 "clone→UX-026"으로 갱신된 것으로 읽는다. *(v1.10 D-31로 재편 — 위 노트 참조.)*
> **v1.5 신설(2026-07-23, D-16, PRD v0.9 · T23)**: 앞 두 선택(유형=보이스, 방식=clone|generic)에 **해당하는 시나리오만** 드러나는 드릴다운 종착 화면. 구 UX-004의 시나리오 선택 기능(AC-001/002)과 clone/generic 분기(→UX-002/UX-014)를 계승한다. 전체 시나리오를 한 화면에 평면 나열하지 않는다는 점(AC-029)이 구 UX-004와의 핵심 차이.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-003 (→ Exit에서 self는 UF-002, send는 UF-004로 합류) |
| Acceptance Criteria | AC-029(필터된 노출), AC-001, AC-002 (구 UX-004에서 계승), **AC-057(self=generic만)**, **AC-058(send generic=챌린지)** |
| Purpose | 상류에서 정해진 모드·방식 조건에 맞는 시나리오만 노출하고, 그 성격(제목·사기 유형·예상 소요·난이도)을 이해시킨 뒤 하나를 골라 확정한다(AC-001/002). "고를수록 이 상황에 들어간다"는 몰입을 위해 앞 단계 선택(유형·모드·방식)을 이 화면 상단에 맥락으로 표시한다. |
| User Goal | 좁혀진 목록에서 체험/발송할 사기 시나리오를 고른다. |
| Entry | **(v1.10)** self 모드(본인이 체험) → UX-026에서 직행(방식=generic 강제, UX-016 미경유). send 모드(지인에게 보내기) → UX-016에서 방식(clone|generic) 선택 후. |
| Exit | **(v1.10, D-31·D-32·D-33)** **self 모드**: generic 시나리오 선택 → `createSession` 후 UX-014(통화). **send 모드**: 시나리오 선택 → **UX-019(챌린지 만들기)** — clone은 UX-002(녹음)→UX-003(클론) 경유 후, generic은 클론 스킵(AC-058) 후. |
| Primary Actions | (필터된) 시나리오 카드 선택 → "시작". |
| Secondary Actions | 뒤로(UX-016 방식 선택으로 복귀, P-12). 시나리오 상세(난이도·발신자 설명) 보기. |
| States | Loading: 시나리오 목록 로드 / generic "시작" 후 세션 생성 중 스피너 / Empty: 선택 방식에 해당 시나리오가 0건이면 "이 방식의 시나리오가 아직 없습니다" 안내 + 뒤로(현행 데이터상 실질 미발생) / Error: 목록 로드 실패·세션 생성 실패 재시도(P-4) / Success: 선택 카드 강조 후 시작. |
| Validation | 시나리오 미선택 시 "시작" 비활성(P-5). |
| Failure | 콘텐츠 로드 실패·세션 생성 실패 → 재시도(P-4). |
| Accessibility | 노출되는 카드는 큰 글씨·큰 터치 타깃, 선택 상태를 색+체크 아이콘+굵은 테두리로 이중 표기(구 UX-004 개편 관례 계승). 각 카드에 제목·사기 유형·예상 소요·난이도를 텍스트로 명시(색 배지 단독 금지, AC-002). 상단 맥락("보이스피싱 › 내 목소리 복제")은 스크린리더가 현재 위치로 읽음. 키보드로 카드 이동/선택. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical(확장 P0) |
| Business Rules | 노출 목록은 **유형=보이스 + 모드/방식으로 필터된 부분집합만** 포함한다 — 전체 시나리오를 한 화면에 나열하지 않는다(AC-029). 각 시나리오는 제목·유형·예상 소요·난이도 라벨 표시(AC-002). 최소 한국어 "가족 납치/사고(딥보이스)" 1종 포함(AC-001, clone 방식 하위). **(v1.10, D-31·D-32·D-33) 필터·Exit는 상류 모드에 종속**: **self 모드**는 **generic 시나리오만** 노출하고(clone 배제, AC-057) 선택 시 `createSession` 후 UX-014(무변경 로직 계승). **send 모드**는 UX-016에서 고른 voiceMode(clone|generic)의 시나리오만 노출하고 선택 시 **UX-019(챌린지 만들기)**로 — clone은 UX-002/003 경유, generic은 클론 스킵(AC-058). 이 화면은 세션 생성을 **self 모드에서만** 하고, send 모드에서는 챌린지 생성(하류 UX-019)에 위임한다. |
| Data Required | 시나리오 메타데이터(id·제목·fraudType·estimatedDuration·difficulty·voiceMode·callerLabel). 필터 조건(유형=voice, 모드=self→generic 강제 / send→선택 voiceMode). |
| Data Operations | Read(시나리오 목록·필터), Create(**self 모드에서만** `createSession`; send 모드는 세션·챌린지 생성 안 함) |
| External Dependencies | Firestore 또는 정적 콘텐츠(시나리오 메타, T6 산출물). self generic 경로는 `createSession` 콜러블. |
| Permissions | None |
| Navigation Targets | **UX-014(self generic)**, **UX-002(send clone)**, **UX-019(send generic — 클론 스킵)**, UX-016/UX-026(뒤로) |
| Events Emitted | `scenario_selected` (구 UX-004에서 계승; 모드·voiceMode 컨텍스트 포함) |
| Expected Outputs | 선택된 시나리오 id + 모드 + voiceMode → self는 세션 생성 후 UX-014로, send는 (clone은 UX-002/003 후 / generic은 클론 스킵) UX-019로 전달. |
| Assumptions | 시나리오 콘텐츠(메타)는 T6에서 준비됨. voiceMode 필터는 기존 데이터 필드로 충분하며 스키마 변경이 필요 없다. **모드(self/send)는 UX-026에서 이미 확정돼 클라 상태로 전달됨**(architect 인계 — 모드 배선). generic 보이스 챌린지의 `challenges` 스키마 판별·GENERIC_VOICE_ID 통화 라우팅은 architect §14.8(AC-058 flag). |

### 시나리오 선택 (Screen ID: UX-004) — DEPRECATED (v1.5)
> **⛔ DEPRECATED (2026-07-23, v1.5, D-16)**: 모든 시나리오를 한 화면에 평면 나열하던 이 화면은 **드릴다운 3화면(UX-015 유형 선택 → UX-016 보이스 방식 선택 → UX-017 시나리오 노출)으로 대체**됨. PRD v0.9 AC-029("전체 평면 목록 동시 노출 금지")·AC-028(2분류+clone/generic) 반영. AC-001/002는 UX-017로 이관되어 커버리지 손실 없음. clone→UX-002 / generic→UX-014 분기도 UX-017이 계승. 아래 스펙은 삭제하지 않고 설계 이력으로 보존한다(구현은 UX-015~017을 따를 것). Deprecated 섹션도 참조.
> **v1.4 갱신(2026-07-22, D-14)**: UF-002의 **시작 화면**. Entry가 "UX-003 클론 완료"에서 "UX-001 동의(또는 UX-011 age-gate) 완료"로 바뀐다(Phase B — 시나리오 선택이 녹음/클론보다 먼저). Exit은 선택한 시나리오의 음성 모드로 분기한다.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002 |
| Acceptance Criteria | AC-001, AC-002 |
| Purpose | 세션 시작 전 시나리오를 선택하고 그 성격(난이도·음성 모드)을 미리 이해시킨다. 음성 모드에 따라 이후 경로(녹음 필요 여부)가 갈리므로 여기서 먼저 고른다. |
| User Goal | 체험할 사기 시나리오를 고른다. |
| Entry | UX-001 동의 완료(P1 UX-011 age-gate 구현 시 그 통과 직후). |
| Exit | clone 시나리오 선택 → UX-002(녹음) / generic 시나리오 선택 → `createSession` 후 UX-014(통화)로 직행. |
| Primary Actions | 시나리오 카드 선택 → "시작". |
| Secondary Actions | 시나리오 상세(난이도·음성 모드 설명) 보기. |
| States | Loading: 시나리오 목록 로드 / generic "시작" 후 세션 생성 중 스피너 / Empty: (하루 스코프상 최소 1종 항상 존재하므로 실질 없음) — 목록이 비면 오류로 간주 / Error: 목록 로드 실패·세션 생성 실패 재시도 / Success: 선택 카드 강조 후 시작. |
| Validation | 시나리오 미선택 시 "시작" 비활성. |
| Failure | 콘텐츠 로드 실패·세션 생성 실패 → 재시도. |
| Accessibility | 각 카드에 제목·사기 유형·예상 소요·난이도(강도) + **음성 모드(본인 목소리 등록 필요/불필요)** 라벨을 텍스트로 명시(색 배지 단독 금지, AC-002). 큰 글씨, 키보드로 카드 이동/선택. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical |
| Business Rules | 최소 한국어 "가족 납치/사고(딥보이스)" 1종 포함(AC-001). 각 시나리오는 제목·유형·예상 소요·난이도 라벨 표시(AC-002). "기관사칭"은 여유 시 추가. |
| Data Required | 시나리오 메타데이터(id·제목·사기 유형·예상 소요·난이도 라벨). |
| Data Operations | Read(시나리오 목록), Create(generic 시나리오는 이 화면에서 `createSession`) |
| External Dependencies | Firestore 또는 정적 콘텐츠(시나리오 대본/메타데이터, T6 산출물). generic 경로는 `createSession` 콜러블. |
| Permissions | None |
| Navigation Targets | UX-002(clone), UX-014(generic) |
| Events Emitted | `scenario_selected` |
| Expected Outputs | 선택된 시나리오 id + 음성 모드 → clone은 UX-002 녹음 경로로, generic은 세션 생성 후 UX-014로 전달. |
| Assumptions | 시나리오 콘텐츠(대본·메타데이터)는 T6에서 준비됨. |

### 딥보이스 재생 (Screen ID: UX-005) — DEPRECATED (v1.4)
> **⛔ DEPRECATED (2026-07-22, v1.4, D-11)**: 단일 연속 통화 **UX-014의 "오프닝 재생(opening-playback) phase"로 흡수**됨. 별개 재생 화면 + 이어지는 "계속(역할극 시작)" 버튼이 통화 몰입을 깨뜨린다는 사용자 피드백에 따라 통합. AC-019/022/006는 UX-014로 이관. 아래 스펙은 삭제하지 않고 설계 이력으로 보존한다(구현은 UX-014를 따를 것). Deprecated 섹션도 참조.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002 (→ UX-014로 이관) |
| Acceptance Criteria | AC-019, AC-022, AC-006 (→ UX-014) |
| Purpose | 본인 클론 목소리로 재현된 가족 사칭 대사를 인앱 오디오로 재생해 위협을 신체적으로 자각시킨다(데모 승부처). |
| User Goal | "내 목소리로 가족 사칭 전화가 오면?"을 직접 듣는다. |
| Entry | UX-004 시나리오 선택. |
| Exit | 재생 후 → UX-006(역할극 채팅). |
| Primary Actions | 재생/다시 듣기, "계속(역할극 시작)". |
| Secondary Actions | "훈련 종료"(상시 노출, AC-006). |
| States | Loading: 합성 오디오 준비 스피너 / Empty: N/A / Error: 합성/재생 실패 → 재시도 또는 폴백 음성(Interaction Pattern P-1) / Success: 재생 완료 후 "계속" 노출. |
| Validation | N/A |
| Failure | TTS 합성 실패·오디오 재생 실패 → 재시도/폴백. |
| Accessibility | "AI 훈련용 합성" 표식을 화면 라벨 + 오디오 프리롤 안내로 이중 노출(AC-022, Interaction Pattern P-3). 재생 컨트롤에 스크린리더 라벨. 자막(대사 텍스트) 동시 제공 권장(청각 접근성). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical |
| Business Rules | 모든 합성 음성에 "AI 훈련용 합성" 표식 필수(AC-022). 실제 전화망/통화 연동 없이 인앱 재생 전용(AC-019). "훈련 종료" 컨트롤 상시 노출(AC-006). |
| Data Required | 클론 voice ID, 시나리오 사칭 대사 텍스트, 합성 오디오 URL(임시), 합성 표식 문구. |
| Data Operations | Create(오디오 합성 요청), Read(대사·합성 결과) |
| External Dependencies | Firebase Functions → ElevenLabs TTS(클론 voice). 임시 오디오는 Firebase Storage(세션 종료 시 폐기 대상, AC-021). |
| Permissions | None(오디오 자동재생 정책상 사용자 탭 필요할 수 있음) |
| Navigation Targets | UX-006, UX-007("훈련 종료" 시) |
| Events Emitted | `deepvoice_played`, `session_end_requested`(종료 시) |
| Expected Outputs | 재생 완료 상태 → 역할극 진입. |
| Assumptions | 클론 voice ID가 준비됨(UX-003). 브라우저 오디오 재생 가능. |

### 사기범 역할극 채팅 (Screen ID: UX-006) — DEPRECATED (v1.4)
> **⛔ DEPRECATED (2026-07-22, v1.4, D-11)**: 단일 연속 통화 **UX-014의 "실시간 역할극(live) phase"로 흡수**됨. D-10(STT/TTS In Scope 전환) 결정은 **반전되지 않고 UX-014로 이관**되어 그대로 유효하다(탭-투-토크·자동 청취·사기범 응답 자동재생·텍스트 폴백 전부 계승). AC-003/004/005/006/007/013/024는 UX-014로 이관. 아래 스펙은 삭제하지 않고 설계 이력으로 보존한다(구현은 UX-014를 따를 것).
> **v0.8 갱신(2026-07-22, D-10)**: 텍스트 채팅 → **실시간 음성 통화형**으로 전환. 텍스트 입력은 STT 미지원 브라우저용 폴백으로 상시 유지.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002 (→ UX-014로 이관) |
| Acceptance Criteria | AC-003, AC-004, AC-005, AC-006, AC-007, AC-013, AC-024 (P1 이미지 시 AC-025/AC-022) (→ UX-014) |
| Purpose | AI 사기범과 음성(또는 텍스트 폴백)으로 역할극하며 심리적 압박을 안전하게 경험시킨다. 상시 종료·가드레일이 관통한다. |
| User Goal | 사기범과 대화하며 상황을 겪고, 원할 때 종료한다. |
| Entry | UX-005 재생 후. |
| Exit | 종료 조건 충족 → UX-007. |
| Primary Actions | **탭-투-토크 마이크 버튼(발화 1회 = 전송 1회)**, 텍스트 입력·전송(폴백, 상시 노출), "훈련 종료"(상시 노출·모든 턴에서, AC-006). |
| Secondary Actions | 대화 스크롤, 사기범 응답 음성 수동 재생(자동재생 실패 대비 native controls). |
| States | Loading: AI 응답 대기(타이핑 인디케이터, 지연 목표 내) / Listening: 마이크 인식 중("듣고 있어요") / Empty: 세션 시작 시 사기범 첫 메시지 노출(빈 대화 아님, 오프닝 음성도 자동재생) / Error: LLM 응답 실패·지연 초과 → **T43 갱신**: 재시도 버튼 대신 그 턴만 규칙 기반 목업으로 조용히 대체(completeWithFallback, 대화 끊김 없음), 마이크 권한 거부/미지원 → 텍스트 입력으로 조용히 폴백(단, 왜 텍스트 모드인지 화면에 1줄 고지 — 침묵 실패 금지) / Success: 턴 진행·종료 조건 도달. |
| Validation | 빈 메시지 전송 방지. 전송 대기 중 중복 전송 방지(버튼 비활성). |
| Failure | LLM 실패/타임아웃 → 재시도. 최대 턴·시간 한도 도달 → 자동 종료로 UX-007. 음성 합성 실패 → 텍스트만 표시(핵심 루프 비차단, P-4). |
| Accessibility | 입력창·전송·마이크·"훈련 종료" 키보드 접근. "훈련 종료"는 항상 화면에 고정 노출(스크롤과 무관). 메시지 발신자를 색 아닌 라벨로 구분. 스크린리더가 새 메시지 읽음(aria-live). 음성 응답도 반드시 텍스트 자막을 함께 표시(청각 접근성, AC-024와 별개로 항상 병행). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical |
| Business Rules | 사기범 인격을 세션 내내 유지(명시적 안전 종료 제외, AC-003). 응답은 사전 정의된 "약화된" 수법 집합에서만 나오며 실제 운영 정보(실계좌·실송금·실링크) 미제공(AC-005). 사기 실행 방법 요청·악용 시도 거부(AC-013). 사용자 입력과 시스템 프롬프트 분리·인젝션 방어(AC-024). 대화 로그 PII 마스킹/토큰화(AC-024). 지연 목표 내 응답(AC-004, 목표 p95 ≤10초·OQ-9). "훈련 종료"는 모든 턴에서 즉시 종료(AC-006). **실제 전화망(PSTN) 연동 없음(AC-019 무변경) — 브라우저 내 STT/TTS만 사용.** AC-012/017/023(사전고지·동의·종료고지)은 D-10에서도 무변경. |
| Data Required | 세션 id, 시나리오 인격/약화 수법 프롬프트, 대화 히스토리(마스킹), 턴 수·경과 시간, 최대 턴/시간 한도, 사기범 응답 합성 음성 URL(1회성). |
| Data Operations | Create(사용자·AI 메시지), Read(대화 히스토리·시나리오 프롬프트), Update(세션 상태·턴 카운트) |
| External Dependencies | Firebase Functions → LLM(Claude/Gemini) + VoiceProvider(ElevenLabs TTS, 현재 Mock). Firestore(세션·대화 로그, PII 토큰화). 브라우저 `SpeechRecognition`/`webkitSpeechRecognition`(비표준 Web API, Safari 등 미지원 — 폴백 필수). |
| Permissions | 마이크(getUserMedia 기반, STT 브라우저 API 경유) |
| Navigation Targets | UX-007 |
| Events Emitted | `message_sent`, `ai_replied`, `session_end_requested`, `limit_reached`, `abuse_request_refused` |
| Expected Outputs | 리포트 생성을 위한 전체 대화 로그(마스킹)와 종료 사유(AC-007). |
| Assumptions | 클론·시나리오 준비 완료. "속은 시점" 판정은 리포트 생성 단계(UX-008)에서 수행하며 채팅 중 실시간 표시하지 않음(D-6). STT는 Chrome/Edge 계열에서만 신뢰 가능(Safari/Firefox 미지원 가능성 — 텍스트 폴백 전제). |

### 통화 세션 — 딥보이스 오프닝 → 실시간 역할극 (단일 연속 통화) (Screen ID: UX-014)
> **v1.11 갱신(2026-07-25, D-35·D-36·D-42 — 통화 중 문자 + 난이도 배지)**: 이 화면에 **신규 Screen ID를 만들지 않고 state·컨트롤을 추가**한다(화면의 의미는 그대로 "하나의 통화"). 추가분: **(1) `sms-arrived` 배너 state** — 사기범 턴에 실린 문자 마커를 서버가 전달하면 통화 셸 상단에 문자 도착 배너가 내려온다(발신번호 라벨 + 본문 앞 1줄 + "AI 훈련용 모의" 표식). phase 전이가 아니라 **셸 위에 얹히는 알림 레이어**라 live/opening-playback 어느 phase에서도 뜰 수 있다. **(2) "문자함"(N) 상시 컨트롤** — 문자가 1건 이상 도착한 뒤부터 통화 셸에 남아 언제든 UX-027을 다시 연다(미확인 수 배지). **(3) `sms-overlay-open` 겹침 state** — UX-027이 위에 열려 있는 동안에도 이 화면은 **언마운트되지 않고 통화가 계속된다**(라우트 이동 금지, D-35/P-20): 타이머는 계속 흐르고 사기범 오디오·자막도 계속 누적된다. 오버레이가 열린 동안 **자동청취(마이크)는 일시 게이팅**한다(사용자가 화면을 읽는 중 주변 소리를 발화로 오인하지 않도록 — R-1 우려와 정합). **(4) 난이도 배지** — UX-029에서 고른 난이도를 통화 셸에 텍스트 배지로 상시 표기(색 단독 금지, P-22). 안전장치(AC-006 상시 종료·AC-022 표식·AC-012/017/023)는 이 추가분 전부에서 **무변경**이다.
> **v1.4 신설(2026-07-22, D-11·D-12·D-13)**: 구 UX-005(딥보이스 재생) + 구 UX-006(사기범 역할극)을 **하나의 통화 경험**으로 통합한 화면. 사용자 불만("화면 7→8이 하나의 통화처럼 안 느껴짐 / 받았다가 채팅창으로 넘어가는 느낌")을 해소하기 위해, 수신→오프닝→실시간 역할극을 **고정된 통화 셸 안에서 phase 전이로만** 처리한다(화면 전환·중간 "계속" 버튼 없음). claude.ai/design 원안(화면 1a 수신형·1d 상단바 채팅)에 얽매이지 않고 이 앱 고유의 "전화 몰입 + 안전장치 병존" 톤으로 새로 그렸다.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002, **UF-005(2인 사용자2 체험 재사용, v1.6)**, **UF-007(메신저 에스컬레이션 전이 목적지, v1.7)** |
| Acceptance Criteria | AC-019, AC-022, AC-006, AC-003, AC-004, AC-005, AC-007, AC-013, AC-024 (P1 이미지 시 AC-025) (2인 재사용 시 AC-036 안전장치 유지·AC-040 동의 전제) (에스컬레이션 진입 시 AC-034/035/036/037) |
| Purpose | 하나의 전화 통화 안에서 (1) 본인/기관 사칭 오프닝(clone은 본인 목소리 딥보이스, generic은 기본 합성 음성)을 듣고 (2) **화면 전환 없이 그대로** 사기범과 실시간 음성 역할극을 이어가, "받았다가 못 끊고 말려드는" 심리적 압박을 안전하게 체험시킨다. 통화 셸(발신자·통화 타이머·AI 합성 표식·훈련 종료)이 전 구간 고정되어 단절을 제거한다. |
| User Goal | "내 목소리(또는 기관)로 전화가 오면?"을 받고, 대화에 말려드는 압박을 겪은 뒤 원할 때 끊는다. |
| Entry | generic 시나리오: UX-017에서 `createSession` 직후(v1.5 — 구 UX-004 대체). clone 시나리오: UX-003 클론 완료 후. **2인(UF-005): UX-021 동의 통과 후 사용자1 클론 목소리로 진입(v1.6, AC-040 동의 전제).** **에스컬레이션(UF-007): UX-022 메신저 채팅에서 전이 트리거(sentinel/폴백/명시버튼) 발생 시 전이 연출(P-18)을 거쳐 이 화면의 incoming(수신) phase로 이음새 없이 진입(v1.7, voiceId는 UX-025에서 선확보, AC-036 안전장치 그대로).** |
| Exit | 종료 조건(사용자 종료·최대 턴/시간 한도) 충족 → UX-007. **2인은 UX-007(정체 공개, 2인 변형) → UX-018(강제 해설).** |
| Primary Actions | **① 수신 phase**: "받기"(통화 시작) / "거절"(= 훈련 종료, AC-006). **② 실시간 phase**: 탭-투-토크 마이크(자동 청취 재개 포함) / 텍스트 입력·전송(폴백, 접이식) / "훈련 종료"(전 구간 상시, AC-006). |
| Secondary Actions | 오프닝 다시 듣기(실시간 진입 전 한정), 사기범 응답 수동 재생(자동재생 실패 대비 native controls), 텍스트 입력 토글. |
| States (phase 전이) | **incoming(수신)**: 벨/발신자 라벨·아바타, AC-022 프리롤 고지 문구, "받기/거절". **connecting**: 오프닝 첫 대사 합성 대기 짧은 로딩("연결 중…"). **opening-playback**: 오프닝 대사 자동 연속 재생 + 자막 + 음성 모드 라벨(clone="당신의 목소리로 합성" / generic="기본 합성 음성", D-13) + AI 합성 표식 상시. **live(실시간 역할극)**: 오프닝 종료(오디오 onEnded) 시 화면 전환 없이 자동 청취("듣고 있어요…") 시작 → STT 발화 → 사기범 TTS 자동재생 → 재청취. 타이핑 인디케이터(응답 대기). **error/retry**: LLM/합성 실패 시 인라인 재시도(P-4), 통화 유지. **ended**: 한도 도달 자동 종료 또는 사용자 종료 → UX-007 인계. |
| Validation | 빈 메시지 전송 방지, 전송 대기 중 중복 전송 방지(버튼 비활성). |
| Failure | 오프닝 합성/재생 실패 → 폴백 음성/자막만으로 통화 지속(P-1, 비차단). 실시간 LLM 실패/지연 초과 → 재시도(P-4). 마이크 권한 거부/STT 미지원 → 텍스트 입력으로 폴백 + "왜 텍스트 모드인지" 1줄 고지(침묵 실패 금지). 사기범 음성 합성 실패 → 자막만 표시(핵심 루프 비차단). 최대 턴/시간 한도 도달 → 자동 종료 UX-007. |
| Accessibility | 통화 셸 요소(발신자·타이머·합성 표식·훈련 종료)에 스크린리더 라벨. "훈련 종료"는 스크롤과 무관하게 항상 포커스 도달(AC-006). 오프닝·사기범 음성 대사는 **항상 자막 텍스트 동시 제공**(청각 접근성). 신규 대사 aria-live. 발신자·발신자 구분은 색 아닌 라벨. AI 합성 표식은 점멸 금지·상시 고정(AC-022, 불안 유발 방지). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical |
| Business Rules | (구 UX-005/006 흡수) 사기범 인격을 통화 내내 유지(안전 종료 제외, AC-003). 응답은 사전 정의된 "약화된" 수법에서만, 실제 운영 정보(실계좌·실송금·실링크) 미제공(AC-005). 사기 실행 방법 요청·악용 시도 거부(AC-013). 사용자 입력/시스템 프롬프트 분리·인젝션 방어 + 대화 로그 PII 마스킹(AC-024). 지연 목표 내 응답(AC-004, p95 ≤10초·OQ-9). 모든 합성 음성/이미지에 "AI 훈련용 합성" 표식(AC-022, 상시 고정). 실제 전화망(PSTN) 연동 없음 — 브라우저 내 STT/TTS만(AC-019 무변경). "훈련 종료"는 모든 phase에서 즉시 종료(AC-006). 한도 도달 시 자동 종료→정확히 1리포트(AC-007). **오프닝 딥보이스 대사는 대화 로그의 사기범 첫 턴(turnIndex 0~)으로 취급되어 리포트 타임라인(AC-026)의 기점이 된다.** AC-012/017/023(사전고지·동의·종료고지)은 무변경 — 통화 몰입 극대화는 "통화 중"에 한정. |
| Data Required | 세션 id, 시나리오 인격/약화 수법 프롬프트, 시나리오 오프닝 대사(deepvoiceLines)·callerLabel·voiceMode, clone voice ID 또는 generic voice ID, 대화 히스토리(마스킹), 턴 수·경과 시간, 최대 턴/시간 한도, 오프닝·사기범 응답 합성 음성 URL(1회성). |
| Data Operations | Create(오디오 합성 요청·사용자/AI 메시지), Read(대사·시나리오 프롬프트·대화 히스토리), Update(세션 상태·턴 카운트) |
| External Dependencies | Firebase Functions → LLM(Claude/Gemini) + VoiceProvider(ElevenLabs TTS, 현재 Mock). Firestore(세션·대화 로그, PII 토큰화, onSnapshot 구독). 브라우저 `SpeechRecognition`/`webkitSpeechRecognition`(비표준, Safari/Firefox 미지원 — 텍스트 폴백 필수). 임시 오디오는 Firebase Storage(세션 종료 시 폐기, AC-021). |
| Permissions | 마이크(getUserMedia 기반 STT). 오디오 자동재생은 브라우저 정책상 "받기" 사용자 제스처 이후 시작(정책 회피점을 수신 phase의 탭에 둔다). |
| Navigation Targets | UX-007 |
| Events Emitted | `call_answered`, `deepvoice_played`, `message_sent`, `ai_replied`, `session_end_requested`, `limit_reached`, `abuse_request_refused` |
| Expected Outputs | 리포트 생성을 위한 전체 대화 로그(마스킹, 오프닝 포함)와 종료 사유(AC-007). |
| Assumptions | clone voice ID(UX-003) 또는 generic voice ID(UX-004)가 준비됨. "속은 시점" 판정은 리포트 단계(UX-008)에서 수행하고 통화 중 실시간 표시하지 않음(D-6). STT는 Chrome/Edge 계열에서만 신뢰(폴백 전제). |
| **아키텍처 트레이드오프 (architect 판단 위임)** | "단일 연속 통화"는 UX 요구(몰입 단절 제거)이지 라우트 구현을 규정하지 않는다. 두 구현안: **(A) 단일 라우트 + phase 상태머신** — 통화 셸을 한 컴포넌트에 두고 phase만 전이. 몰입 단절이 원천적으로 없고 오디오/타이머 컨텍스트가 자연히 연속. 단 딥보이스 재생 + STT/TTS 역할극 로직이 한 라우트에 모여 복잡도↑, 기존 `/session/play`·`/session/chat` 통합 리팩터 필요. **(B) 공유 레이아웃(통화 셸) + 두 라우트 유지** — 셸을 공통 레이아웃으로 올리고 play→chat 전환을 full navigation이 아닌 클라이언트 사이드 내용 교체로. 리팩터는 적으나 라우트 경계에서 통화 타이머·오디오 컨텍스트·STT 상태 연속성 유지가 까다로움. **UX 권고: (A) 단일 라우트가 "하나의 통화" 요구에 가장 부합.** 최종 선택(상태관리·오디오 컨텍스트 연속성)은 architect. 관련 미결: OQ-U7. |

### 통화 중 문자(SMS) 오버레이 — 인앱 문자함 (Screen ID: UX-027)
> **v1.11 신설(2026-07-25, D-35~D-38)**: 통화를 **끊지 않은 채** 사기범이 보낸 문자를 읽는 오버레이. **라우트가 아니라 통화 화면(UX-014) 위에 겹치는 계층**이다 — `/session/play`와 `/session/messenger`가 별도 라우트라 이동 시 실시간 세션·마이크가 끊긴다는 코드 제약 때문(D-35). 선례가 있다: 기존 UX-023(가짜 랜딩)도 라우트가 아니라 `role="dialog" aria-modal` in-page 오버레이로 구현돼 있어(`src/components/MessengerFakeLanding.tsx`) 같은 패턴을 통화 위에 재사용한다. 이 화면은 **메신저 채팅 셸(UX-022)의 축소판이 아니다** — 대화·입력·스킨 토글이 없는 **읽기 전용 문자함**이며, 사용자가 답장을 보내는 채널이 아니라 사기범이 통화 중에 밀어 넣는 정보(계좌·링크·인증번호)를 보는 곳이다(저인지부하 D-1, 신규 대화 표면을 만들지 않음).
| Item | Value |
|---|---|
| Belongs to Flow(s) | **UF-008**(주), UF-002·UF-005·UF-007(통화가 일어나는 모든 맥락에서 동일하게 성립) |
| Acceptance Criteria | **재사용**: AC-045(링크 탭 → 인앱 가짜 랜딩 UX-023, 외부 이동 없음), AC-032/AC-033(콘텐츠 무해화·실 URL 부재), AC-022(모의·합성 표식 상시), AC-006(오버레이 안에서도 "훈련 종료" 도달), AC-005/AC-013(실계좌·실링크·실행 정보 미노출), AC-019(인앱 전용·PSTN 없음), AC-026(문자 확인·인증번호 구술이 리포트 타임라인 판정 입력). **신규 능력 AC는 PRD 부재 — OQ-U15.** |
| Purpose | "계좌는 문자로 보냈어요 / 인증번호 불러 주세요"라는 실제 수법을, **전화가 연결된 상태 그대로** 문자를 확인하는 경험으로 재현한다. 동시에 사용자 신고 *"화면에 뜨는 인증번호를 불러달라는데 아무것도 보이는 게 없다"*를 해소한다 — 인증번호가 실제로 도착해 보인다(D-38). |
| User Goal | 통화를 유지한 채 방금 왔다는 문자를 확인하고, (링크면) 눌러 보고, (인증번호면) 읽어 본 뒤 통화로 돌아간다. |
| Entry | UX-014의 문자 도착 배너 탭, 또는 통화 셸의 "문자함"(N) 컨트롤 탭. **오직 통화 중에만 진입 가능**(통화가 없는 맥락에서는 존재하지 않는 화면). |
| Exit | "통화로 돌아가기"·오버레이 바깥 탭·Esc → UX-014로 복귀(통화 계속). 링크 칩 탭 → **UX-023**(가짜 랜딩) → 닫으면 이 화면으로 복귀. "훈련 종료" → UX-007(오버레이를 먼저 닫고 인계, 고지 문구가 가려지지 않게). |
| Primary Actions | 문자 본문 읽기(스크롤), **링크 칩 탭 → UX-023**, **인증번호 코드 영역 확인**(길게 눌러 선택 가능하되 클립보드 자동 복사는 하지 않음 — "복사해서 어딘가에 넣는" 실행 동선을 앱이 대신 만들지 않는다), "통화로 돌아가기", "훈련 종료"(상시, AC-006). |
| Secondary Actions | 이전에 도착한 문자 다시 보기(도착 순 목록), 발신번호 라벨 확인. **답장 입력·전달·삭제는 제공하지 않는다**(읽기 전용 — 신규 대화 표면 미설계). |
| States | **Empty**: 아직 도착한 문자가 없을 때 — 이 화면은 애초에 열리지 않는다(진입점이 없음). 예외적 진입 시 "아직 도착한 문자가 없습니다" 명시. **Arrived(기본)**: 문자 1건 이상 목록 + 최신 건 펼침. **Content 종류 3종**: ① **계좌·요구형**(모의 계좌·금액 텍스트 — 실존하지 않는 기관·형식만 실제처럼, AC-005 유지), ② **링크형**(`displayText` 칩만 표시, 실 URL 없음 — 탭하면 UX-023), ③ **인증번호형**(6자리 모의 코드 + "타인에게 알려주지 마세요"류 문구까지 재현 — 사기범이 그걸 불러달라고 하는 모순 자체가 학습 포인트). **Call-alive(항상)**: 상단에 통화 축소 표시(발신자·경과 타이머·"통화 중" 상태)가 고정되어 통화가 살아 있음을 보여준다. **Error**: 본문 로드 실패 → "문자를 열지 못했습니다" + 재시도(P-4), 통화는 유지. |
| Validation | N/A(읽기 전용 화면 — 입력 필드 없음). 링크 탭 이후의 입력 검증은 UX-023이 담당하며 그 값은 **서버로 전송되지 않는다**(AC-045). |
| Failure | 본문·링크 라벨 로드 실패 → 인라인 오류 + 재시도, 통화 비차단. 마커가 없어 문자가 도착하지 않는 경우는 **콘텐츠 저작 문제**로 UX가 보정하지 않는다(자유텍스트 분류 금지, AC-024 — UF-008 Failure (a)·OQ-U16). 오버레이 열린 중 통화 한도 도달 → 오버레이 자동 닫힘 후 UX-007 인계. |
| Accessibility | `role="dialog" aria-modal="true"` + 화면 이름 라벨(UX-023과 동일 관례). **열릴 때 포커스를 오버레이 제목으로 이동**, 닫으면 **직전 트리거(배너 또는 "문자함" 버튼)로 포커스 복귀**. 포커스 트랩 안에 "훈련 종료"를 반드시 포함(AC-006 — 오버레이가 종료 컨트롤을 가두면 안 된다). 문자 도착은 **aria-live로 알림**("문자 1건이 도착했습니다") — 시각 배너에만 의존하지 않는다. 링크 칩은 스크린리더에 **"모의 링크"**로 안내(실제 링크 오인 방지, P-17 계승). 인증번호는 **한 자씩 읽히도록** 라벨링(예: "인증번호 4 8 2 9 1 7"). 통화가 계속되고 있다는 사실을 텍스트로도 안내("통화 연결 중 · 경과 1분 12초"). 모의 표식은 색 배지 단독이 아니라 텍스트 포함·점멸 금지. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | **Critical (사용자 최우선 요구 — 4건 중 1순위)** |
| Business Rules | **오버레이는 라우트 이동이 아니다** — 이 화면이 열려도 통화 컴포넌트는 언마운트되지 않고 실시간 세션(오디오 컨텍스트·STT·타이머·세션 상태)이 유지되어야 한다(D-35, UX 하드 요구). 문자 도착은 **서버가 파싱해 구조화한 신호**로만 전달하며 클라이언트는 자유텍스트를 분류하지 않는다(AC-024, 기존 `[[LINK:]]`/sentinel 처리 원칙과 동형 — `functions/src/roleplay/linkMarker.ts`가 선례). **실 URL 필드를 도입하지 않는다** — 링크는 표시 텍스트 + 가짜 랜딩 참조로만 표현하고 외부 네비게이션 경로가 스키마에 존재하지 않아야 한다(AC-032/045의 구조적 금지 계승). 인증번호·계좌·발신번호는 전부 **실존하지 않는 모의값**이며 실제 인증·금융 체계와 어떤 연결도 없다(AC-005/AC-013). "훈련 종료"는 오버레이 안에서도 즉시 종료로 도달 가능해야 한다(AC-006). 오버레이 열림 중 자동청취는 게이팅하되 **사기범 오디오 재생과 세션 타이머는 멈추지 않는다**(통화가 살아 있다는 것이 이 기능의 전부). 문자 확인·링크 탭·인증번호 노출은 세션 타임라인에 기록되어 리포트(AC-026)·리플레이(AC-038)에서 함께 다뤄져야 한다. |
| Data Required | 세션 id·통화 상태(경과 시간·발신자 라벨), 도착 문자 목록(발신번호 라벨·본문·도착 시각·종류[계좌/링크/인증번호]), 링크형의 표시 텍스트 + 가짜 랜딩 참조, 인증번호형의 모의 코드. |
| Data Operations | Read(도착 문자·세션 상태), Update(문자 확인 여부·링크 탭 여부 등 세션 타임라인 기록). **Create/Delete 없음**(문자는 사기범 턴의 파생물이지 사용자 생성물이 아니다). **전송(Send) 조작 없음** — 답장 경로가 존재하지 않는다. |
| External Dependencies | Firestore(세션·메시지 구독 — 기존 onSnapshot 경로 재사용). LLM/역할극(마커를 실어 보내는 주체). **신규 외부 의존성 없음.** 기존 UX-023 컴포넌트(`MessengerFakeLanding`) 재사용. |
| Permissions | None(오버레이 자체는 추가 권한 불요). 마이크 권한은 통화가 이미 보유 — 오버레이는 권한을 새로 요구하지 않는다. |
| Navigation Targets | UX-014(복귀), UX-023(링크 탭), UX-007("훈련 종료"·한도 도달) |
| Events Emitted | `sms_received`, `sms_opened`, `sms_link_tapped`, `sms_otp_shown`, `sms_overlay_closed` |
| Expected Outputs | 통화가 유지된 상태의 문자 확인 기록(리포트·리플레이 입력). 실 URL 이동·실 전송·실 인증은 **0건**(구조적으로 불가능해야 함). |
| Assumptions | 통화 세션이 이미 진행 중이다(이 화면은 통화 밖에서 존재하지 않는다). 사전 고지·동의는 UX-001에서 이미 완료됐다. 시나리오 콘텐츠가 문자 발송 대사와 마커를 함께 내도록 저작돼 있다(콘텐츠 책임 — 미충족 시 문자가 오지 않고 UX는 이를 보정하지 않는다). |
| **아키텍처 판단 위임(ux-design 소관 아님)** | ① **오버레이 열림 중 실시간 세션 유지 방식** — 통화 컴포넌트를 살린 채 오버레이를 렌더하는 계층 구조(포털/조건부 렌더), 오디오 컨텍스트·STT 인스턴스 생존 보장. ② **문자 도착 신호의 전달 구조** — 기존 `MessengerAttachment`(kind:"link"·displayText·fakeLandingId·harmless) 재사용·확장인지, 통화 채널 전용 신규 구조인지. 인증번호형은 링크가 아니라 **새 kind**가 필요할 수 있다. ③ **자동청취 게이팅 정책** — 오버레이 열림/닫힘과 마이크 상태의 결합 지점. ④ 시나리오 프롬프트에 문자 마커 지시를 넣는 위치(공유 조립 블록 vs 시나리오별). **전부 OQ-U16.** |

### 세션 종료 + 디스컬레이션 + "훈련이었습니다" 고지 (Screen ID: UX-007)
> **v1.11 갱신(2026-07-25, D-39·D-40)**: Primary Actions에 **"그 순간 다시 해보기" → UX-028(즉시 되감기, UF-009)**를 추가한다. **표시 조건**: 리포트 생성이 끝나고 `deceivedMoments`가 **1건 이상**일 때만 노출한다 — 안 속은 세션에서는 이 버튼을 **띄우지 않고** 기존 "대화 되짚어보기"(UX-018)만 권한다(D-40, 없는 데이터를 발명하지 않음). 리포트 준비 중에는 비활성 + "리포트를 준비하고 있어요" 1줄(침묵 실패 금지). **2인(UF-005)에서는 강제 정체 공개 → 강제 리플레이 해설(AC-042) 순서를 되감기가 대체하거나 앞지르지 않는다** — 되감기는 UX-018 이후의 선택 단계로만 노출한다(안전 단계 무약화 금지). Navigation Targets에 UX-028 추가.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002, **UF-005(2인 사용자2 강제 정체 공개 재사용, v1.6)** |
| Acceptance Criteria | AC-007, AC-015, AC-023 (AC-021 트리거) (2인 재사용 시 AC-042 강제 정체 공개) |
| Purpose | 세션을 정확히 마감하고 안심(디스컬레이션) + "이것은 훈련이었습니다" 고지 후 리포트로 인계한다. 생성물 폐기를 트리거한다. **2인(UF-005)에서는 이 화면이 사용자2에 대한 강제 정체 공개 지점으로 재사용된다(AC-042 — 옵션 아님).** |
| User Goal | 훈련이 끝났고 안전하다는 것을 확인한다. |
| Entry | UX-014에서 종료(사용자 종료·완료·최대 턴/시간 한도 도달). |
| Exit | "리포트 보기" → UX-008. 리플레이 해설(UX-018)로도 진입 가능. **2인(UF-005): 정체 공개 후 UX-018(강제 해설, AC-042)로 인계.** |
| Primary Actions | "리포트 보기". (v1.6) "대화 되짚어보기(리플레이 해설)" → UX-018. |
| Secondary Actions | 처음으로/종료. |
| States | Loading: 폐기 처리·리포트 준비 스피너 / Empty: N/A / Error: 폐기/리포트 준비 실패 → 재시도(단, 고지 메시지는 항상 먼저 노출) / Success: 고지·안심 메시지 표시 + "폐기 완료" 표기 + "리포트 보기". |
| Validation | N/A |
| Failure | 리포트 생성 지연 → 대기 상태(고지 문구는 즉시 노출해 심리적 안심 우선). |
| Accessibility | 안심 메시지는 완화 톤·큰 글씨. 고지 문구를 색 아닌 아이콘+텍스트로 강조. 종료 즉시 포커스를 고지 문구로 이동(스크린리더 우선 안내). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical |
| Business Rules | 종료된 모든 세션은 정확히 1개 리포트를 생성(AC-007). "이것은 훈련이었습니다" 명시 고지 + 디스컬레이션 메시지 필수(AC-015, AC-023). 세션 종료 시 클론 음성·합성물 파일을 스토리지에서 삭제하고 서버 미저장, 삭제 이벤트를 로그로 남김(AC-021). **2인(UF-005) 재사용 시: 사용자2에게 정체 공개는 강제 단계이며(AC-042), 문구를 2인 변형("○○님이 준비한 훈련이었습니다. 실제 상황이 아니었습니다")으로 표시하고 이어서 리플레이 해설(UX-018)로 반드시 인계한다. 사용자1의 클론 음성 폐기/보존은 AC-041 기간제 정책을 따른다(챌린지 단위 스코프).** |
| Data Required | 세션 종료 사유, 대화 로그(리포트 생성 입력), 폐기 대상 생성물 파일 참조. |
| Data Operations | Create(리포트·삭제 로그), Delete(클론 음성·합성물 파일), Update(세션 상태=ended) |
| External Dependencies | Firebase Storage(삭제), Firebase Functions(폐기 처리·리포트 생성 트리거), Firestore(삭제 로그·세션 상태). |
| Permissions | None |
| Navigation Targets | UX-008, UX-018(리플레이 해설; 2인은 강제) |
| Events Emitted | `session_ended`, `artifacts_purged`, `report_generation_started`, `identity_revealed`(2인) |
| Expected Outputs | 폐기 완료 상태 + 리포트 생성 트리거. |
| Assumptions | 세션 로그는 마스킹된 형태로 리포트 생성에 사용 가능. 폐기 대상은 UX-002 녹음·UX-014 통화(오프닝·실시간)에서 생성된 임시 오디오·합성물. |

### 취약점 리포트 (Screen ID: UX-008)
> **v1.11 갱신(2026-07-25, D-39·D-44·D-22)**: Primary Actions·Navigation Targets에 두 진입점을 추가한다. **(1) "그 순간 다시 해보기" → UX-028(즉시 되감기, UF-009)** — `deceivedMoments`가 1건 이상일 때만 노출(안 속았으면 미노출, D-40). 리포트 타임라인의 **각 속은 순간 항목 옆에도** 같은 진입점을 두어 "이 순간"을 직접 지목해 되감을 수 있게 한다. **(2) "내가 속았던 순간 모아보기" → UX-030(실패 아카이브, UF-010)** — 이번 세션이 아니라 **누적된 과거 전체**를 보는 진입점이다. 리포트 자체의 내용·판정·톤(과신 표현 금지)은 무변경. 세션 중 문자 오버레이(UX-027)에서 일어난 링크 탭·인증번호 노출도 기존 타임라인 항목과 같은 형식으로 함께 표시된다(신규 표기 형식 없음).
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002 |
| Acceptance Criteria | AC-008, AC-009, AC-026 (AC-005/AC-013 문구 주의) |
| Purpose | 사용자가 언제·어디서 속을 뻔했는지 타임라인으로 짚고, 놓친 위험 신호와 올바른 대처법을 제시한다. |
| User Goal | 내 취약점과 다음에 할 행동을 이해한다. |
| Entry | UX-007에서 "리포트 보기". |
| Exit | 처음으로/종료(P1: 게임화 등급 UX-010 또는 히스토리 UX-012). **대화 되짚어보기 → UX-018(리플레이 해설, v1.6).** |
| Primary Actions | 리포트 열람(타임라인·수법·대처법), **"대화 되짚어보기(리플레이 해설)" → UX-018(v1.6, AC-038)**, "처음으로". |
| Secondary Actions | (여유 시) 다시 훈련하기, 등급 보기. |
| States | Loading: 리포트 생성 대기 스피너 / Empty: 한 번도 속지 않은 경우 → "속지 않았습니다"를 명시하고 시도된 수법만 나열(AC-009) / Error: 리포트 생성 실패 → 재시도 / Success: 타임라인·수법·대처법 표시. |
| Validation | N/A |
| Failure | 리포트 생성 실패 → 재시도. |
| Accessibility | 타임라인 항목을 텍스트로 명확히("15초 시점에 속았습니다" 등). "개선 영역" 프레임의 완화 톤(과신 방지, PRD Risk). 색만으로 위험도 표기 금지. 큰 글씨·키보드 스크롤. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical |
| Business Rules | 리포트는 최소 (1) 속았/응했을 순간, (2) 사용된 조작 수법, (3) 예방 조언 1개 이상을 포함(AC-008). 속지 않은 경우 이를 명시 + 시도된 수법 나열(AC-009). 속은 시점을 시간축으로 표시 + 그 순간 올바른 대처 제시(AC-026). **실제 사기 실행 정보(실계좌·실송금 절차·실링크)를 절대 노출하지 않는다**(AC-005/AC-013). "이제 면역" 류 과신 표현 금지, "개선 영역" 프레임(PRD Risk). |
| Data Required | 세션 대화 로그(마스킹), 속은 시점/수법/대처법(LLM 분석 결과), 시나리오 메타. |
| Data Operations | Create(리포트 생성), Read(리포트) |
| External Dependencies | Firebase Functions → LLM(리포트 생성). Firestore(리포트 저장 — 단, 생성물 음성은 폐기됨). |
| Permissions | None |
| Navigation Targets | UX-018(리플레이 해설, v1.6), UX-015(다시 훈련 — 드릴다운 시작, v1.5 구 UX-004 대체), P1: UX-010, UX-012 |
| Events Emitted | `report_viewed`, `retrain_requested`(선택) |
| Expected Outputs | 사용자에게 표시된 취약점 리포트. (P1) 방어 점수 산정 입력. |
| Assumptions | 리포트는 마스킹된 로그만으로 생성 가능하며 실제 운영 정보는 프롬프트·출력에서 배제된다. |

### 사후 리플레이 해설 (Screen ID: UX-018)
> **v1.6 신설(2026-07-23, D-18, T32, AC-038)**: 세션 종료 후 transcript를 시간순으로 되짚으며 각 사기 신호 시점에 주석을 다는 화면. UX-008(요약 리포트)의 **확장이 아니라 별도 화면**으로 설계(D-18 근거: 요약 vs 대화 되짚기의 상호작용 모델이 다르고, 2인 종료(AC-042)에서 강제 단계로 독립 재사용되어야 함). 보이스·메신저·2인 세션 **모두 공통** 재사용. 기존 리포트·transcript 인프라(T9/AC-008/026) 재사용, 신규 백엔드 없음.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002(단독 세션), UF-005(2인 사용자2 종료 강제 단계) |
| Acceptance Criteria | AC-038 (AC-008/AC-026 재사용) |
| Purpose | 사용자가 자신이 한 대화를 처음부터 시간순으로 다시 보며, 각 사기 신호가 나온 지점에 "여기 이 말이 사기 신호였어요 / 이렇게 대응했어야 해요" 주석을 붙여 학습을 강화한다(AC-038). 요약(UX-008)이 "무엇을·언제"라면 리플레이 해설은 "대화 흐름을 따라가며 왜"를 짚는다. |
| User Goal | 내 대화를 되짚으며 어디서 신호를 놓쳤고 어떻게 대응했어야 하는지 이해한다. |
| Entry | UX-008(리포트)에서 "대화 되짚어보기(리플레이 해설)". 2인(UF-005)에서는 정체 공개(UX-007 재사용) 직후 **강제**로 진입(AC-042). |
| Exit | 처음으로/종료. 단독 세션은 UX-008로 복귀 가능. 2인 사용자2는 (선택)결과 공유 동의 → 종료. |
| Primary Actions | transcript 스텝 이동(이전/다음 신호로 점프 또는 순차 스크롤), 각 주석 펼쳐보기. |
| Secondary Actions | 요약 리포트(UX-008)로 이동(단독 세션), 처음으로. (2인 사용자2)결과 공유 동의 토글(AC-043). |
| States | Loading: transcript·분석 로드 스피너 / Empty: 한 번도 속지 않은 경우 → 해당 사실을 명시하고, 시도된 수법과 "잘 대응한 지점"만 주석으로 표시(AC-009 정합, 과신 표현 금지) / Error: transcript/분석 로드 실패 → 재시도(P-4) / Success: 시간순 대화 + 신호 시점 주석 표시. |
| Validation | N/A(열람 화면). |
| Failure | 분석/transcript 로드 실패 → 재시도. 신호가 하나도 식별되지 않으면 Empty 상태 처리(침묵 금지 — "이번 대화에서는 뚜렷한 위험 신호가 없었습니다"류 명시). |
| Accessibility | 대화 항목·주석을 텍스트로 명확히("32초 시점: 이 말이 긴급성 압박 신호입니다"). 발신자 구분은 색 아닌 라벨. 신호 강조는 색만이 아니라 아이콘+텍스트(색 단독 금지). "개선 영역" 완화 톤(과신 방지, P-8). 큰 글씨·키보드 스텝 이동. 스크린리더가 주석을 대화 순서대로 읽음(aria). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High(확장 P0 — AC-038, 2인 종료 강제 단계라 UF-005 필수) |
| Business Rules | 리플레이 해설은 **기존 transcript(마스킹) + 리포트 분석 결과(속은 시점·수법·대처, T9/AC-008/026)를 재사용**하며 신규 데이터 모델·신규 분석 파이프라인을 도입하지 않는다(AC-038). "면역됨/이제 안전" 류 과신 표현 금지, "개선 영역" 프레임 유지(PRD Risk, P-8). 실제 사기 실행 정보(실계좌·실송금·실링크) 노출 금지(AC-005/AC-013 계승). 보이스·메신저·2인 세션 모두 동일 화면으로 처리(교차 채널 세션은 두 단계 신호를 하나의 시간축에 병합, AC-037 정합). 2인(AC-042)에서는 옵션이 아니라 강제 단계. |
| Data Required | 마스킹된 transcript(발신자·시각·textMasked), 리포트 분석 결과(deceivedMoments·tacticsUsed·correctAction/preventionAdvice), 시나리오 메타. |
| Data Operations | Read(transcript·리포트 분석) — 신규 write 없음(리포트는 UX-007/UX-008 경로에서 이미 생성). |
| External Dependencies | Firestore(reports/{sid}·sessions/{sid}/messages, T9 산출물). LLM 재분석은 하지 않음(기존 분석 재사용). |
| Permissions | None |
| Navigation Targets | UX-008(단독 세션 복귀), 처음으로 |
| Events Emitted | `replay_viewed` |
| Expected Outputs | 시간순 주석이 달린 대화 되짚기 뷰. (2인)사용자2 결과 공유 동의 상태(AC-043 게이트 입력). |
| Assumptions | UX-008 리포트 생성이 선행되어 분석 결과가 존재한다. transcript는 마스킹된 형태로만 표시된다(AC-024). 신호 판정은 현재 T9 규칙 기반(실 LLM 전환 시 정확도 재검증은 T9 알려진 갭 — 화면은 무관). |

### 즉시 되감기 — 그 순간 다시 답해보기 (Screen ID: UX-028)
> **v1.11 신설(2026-07-25, D-39·D-40)**: 세션 종료 직후 속은 그 턴으로 되돌아가 **사용자가 다시 답해 보고 즉시 판정을 받는 재연 드릴**. **UX-018(리플레이 해설)과의 구분이 이 화면의 존재 이유**다 — UX-018은 전체 대화를 시간순으로 **읽으며 주석을 보는 복기**(사용자 입력 없음), UX-028은 속은 **한 순간**에 대해 **직접 다시 연기해 보는 것**(사용자 입력 있음). UX-018 확장으로 만들지 않은 이유(D-39): 상호작용 모델이 근본적으로 다르고(열람 vs 입력·판정), 대상 범위가 다르며(전체 대화 vs 단일 순간), UX-018은 2인(AC-042)에서 **강제 단계**라 입력을 요구하는 드릴을 그 안에 섞으면 강제 단계의 성격이 바뀐다.
| Item | Value |
|---|---|
| Belongs to Flow(s) | **UF-009**(주), UF-002·UF-006(단독 세션 종료 후), UF-005(2인 사용자2 — 단 강제 해설 이후 선택 단계), UF-010(아카이브에서 과거 순간 진입) |
| Acceptance Criteria | **재사용**: AC-026(속은 시점·그 순간의 올바른 대처 — 데이터 원천), AC-008/AC-009(리포트 분석 재사용·안 속았을 때 명시), AC-038(사후 학습 성격), AC-007(원 세션 = 정확히 1리포트 — 되감기가 훼손하지 않음), AC-024(마스킹 transcript만), AC-005/AC-013(실행 정보 미노출). **신규 능력 AC는 PRD 부재 — OQ-U15.** |
| Purpose | "이 순간 뭐라고 했어야 했나"를 **읽는 것이 아니라 그 자리에서 해보게** 한다. 속은 순간의 맥락을 다시 세우고, 사용자가 다시 답한 것에 대해 즉시 피드백 + 모범 대처를 나란히 보여 학습을 행동으로 굳힌다. |
| User Goal | 내가 넘어간 그 순간에 어떻게 답했어야 했는지를 **직접 해보고** 바로 확인한다. |
| Entry | UX-007(종료 직후, `deceivedMoments` ≥ 1), UX-008(리포트 전체 또는 타임라인 항목별), UX-018(해설 중 특정 신호 지점), UX-030(실패 아카이브 카드). |
| Exit | "리포트로 돌아가기" → UX-008. "대화 전체 되짚어보기" → UX-018. "속았던 순간 모아보기" → UX-030. "처음으로". |
| Primary Actions | **① 다시 답하기**(텍스트 입력 기본 · 음성 입력은 선택 — 짧은 드릴이라 마이크 재권한을 강제하지 않는다), **② 판정·모범 대처 확인**, **③ "다음 순간 다시 해보기"**(속은 순간이 복수일 때). |
| Secondary Actions | 입력 없이 **"모범 대처만 보기"**(강제 입력 아님), 맥락 대화 더 보기(앞뒤 턴 펼치기), 같은 순간 **한 번 더 답해보기**(재시도 — 횟수 제한 없음, 학습 목적). |
| States | **Loading**: transcript·분석 로드 / 판정 대기(입력 후) 스피너. **Empty**: `deceivedMoments`가 비어 있음 → **"이번 대화에서는 되감을 순간이 없습니다"**를 명시하고 "잘 대응한 지점 되짚어보기"(UX-018)로 안내(정상 진입 경로에서는 애초에 이 화면이 열리지 않음 — 직접 URL 진입 등 예외 대비, D-40). **Context(기본)**: 속은 순간 앞뒤 몇 턴 + 사기범의 문제의 대사 강조 + "이때 당신은 이렇게 답했습니다"(원래 답변). **Answered**: 사용자 새 답변 표시 + **3단계 판정**(잘 대응했습니다 / 아직 위험합니다 / 판단하기 어렵습니다) + 이유 1줄 + **모범 대처**(`correctAction`) 나란히. **Judge-failed**: 판정 실패 → "이번 답변은 판정하지 못했습니다" 명시 + 모범 대처만 표시(학습 가치 보존, 침묵 금지). **Error**: 로드 실패 → 재시도(P-4). |
| Validation | 빈 답변 전송 방지(전송 버튼 비활성, P-5). 판정 대기 중 중복 전송 방지. 입력 길이 상한 안내(구체 수치는 architect). |
| Failure | 판정 LLM 실패·타임아웃 → Judge-failed 상태(조용한 실패 금지). transcript/분석 로드 실패 → 재시도. 리포트가 아직 준비되지 않은 상태 진입 → 진입점 비활성 + "리포트를 준비하고 있어요"(UX-007 담당). |
| Accessibility | 맥락 대화의 발신자 구분은 **색 아닌 라벨**. 강조된 "문제의 대사"는 색만이 아니라 **아이콘+텍스트** 이중 표기. 판정 3단계도 색 단독 금지(텍스트로 명시). 진입 시 포커스는 "다시 답해보기" 입력 라벨로. 판정 결과는 **aria-live**로 알림. 텍스트 입력이 기본이라 마이크 없이도 전 기능 사용 가능(키보드 완전 조작). 큰 글씨·48px 이상 주요 버튼. 톤은 "개선 영역" 완화 프레임(P-8) — **"이제 안전/면역" 류 과신 표현과 "또 틀렸습니다" 류 질책 표현 둘 다 금지**(어르신 심리적 안전, PRD Risk). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High |
| Business Rules | **원 세션의 리포트를 변경하지 않는다** — 되감기는 새 리포트를 만들지도, 기존 리포트의 `wasDeceived`/`deceivedMoments`를 덮어쓰지도 않는다(AC-007 "종료된 모든 세션은 정확히 1개 리포트" 불변식 보호). 되감기 답변에 대한 판정은 **그 순간의 수법(`tactic`)과 모범 대처(`correctAction`)를 기준**으로 하며, 새로운 사기 대사를 이어서 생성하지 않는다(한 턴 드릴 — 대화가 계속되는 것이 아니다). 사용자 입력은 기존과 동일하게 **데이터로 감싸 처리**하고 지시로 해석하지 않는다(AC-024 인젝션 방어 계승). PII 마스킹 계승. 실제 사기 실행 정보를 판정·모범 대처 어디에도 노출하지 않는다(AC-005/AC-013). 판정 불가 시 **모범 대처는 반드시 보여준다**(학습 최소 보장). 과신·질책 표현 금지(P-8). **2인 사용자2(UF-005)에서는 강제 정체 공개(AC-042)·강제 리플레이(UX-018) 이후의 선택 단계로만 노출**하며 그 강제 순서를 대체하지 않는다. |
| Data Required | 리포트의 `deceivedMoments`(turnIndex·timeLabel·tactic·correctAction), 해당 순간 앞뒤 마스킹 transcript 몇 턴, 시나리오 메타(제목·채널·난이도), 사용자 새 답변, 판정 결과(3단계 + 이유 1줄). |
| Data Operations | Read(리포트 분석·transcript). Create(되감기 시도 기록 — **저장 여부·위치는 architect 판단**, OQ-U17). **원 리포트·원 세션에 대한 Update/Delete 없음**(불변식). |
| External Dependencies | Firestore(reports·messages 기존 경로 재사용). LLM(새 답변 판정 — **신규 판정 경로**). 음성 입력을 선택 제공할 경우 브라우저 STT(기존 폴백 관례 동일). |
| Permissions | None(텍스트 기본). 음성 입력을 쓸 때만 마이크. |
| Navigation Targets | UX-008, UX-018, UX-030, 처음으로 |
| Events Emitted | `rewind_opened`, `rewind_answer_submitted`, `rewind_judged`, `rewind_judge_failed`, `rewind_model_answer_viewed`, `rewind_next_moment` |
| Expected Outputs | 사용자 새 답변에 대한 3단계 판정 + 이유 + 모범 대처 비교 뷰. 원 리포트는 불변. |
| Assumptions | 리포트 생성이 선행되어 `deceivedMoments`가 존재한다. transcript는 마스킹된 형태로만 표시된다. 판정 정확도는 현행 분석 파이프라인 수준에 종속된다(화면은 판정 실패를 정직하게 노출하는 것으로 대응). |
| **아키텍처 판단 위임(ux-design 소관 아님)** | ① **재연의 실행 모델** — 원 세션을 재개하는가(위험: AC-007 리포트 불변식·세션 상태 오염), **별도 일회성 드릴 실행**으로 두는가(권고 방향이나 최종 판단은 architect), 시도 기록을 저장할지·어디에 둘지. ② **판정 주체·프롬프트** — 기존 리포트 분석기 재사용인지 신규 판정 콜러블인지, 3단계 라벨의 판정 기준. ③ 음성 입력 제공 여부(마이크 재권한 비용 대비). **전부 OQ-U17.** |

### 챌린지 만들기·공유 링크 (Screen ID: UX-019)
> **v1.11 갱신(2026-07-25, D-41·D-42)**: 발신자가 UX-029에서 고른 **난이도**를 이 화면 요약에 명시한다(예: "저금리 대환대출 빙자 · **고급 — 실제처럼 정교하게**"). **고급을 고른 경우 "상대가 강한 압박을 겪게 됩니다"를 만들기 전에 고지**한다(발신자가 지인에게 무엇을 보내는지 알고 보내게 하는 것 — AC-040 사전 동의 취지의 발신 측 대응). 고른 난이도는 챌린지 레코드에 함께 실려 수신자의 동의 랜딩(UX-021)과 세션에 전달된다. **난이도는 활성 챌린지 상한(AC-049 전역 3개)·링크 토큰(AC-048)·결과 열람 범위(AC-043/055)를 포함해 어떤 안전·정책 판정도 바꾸지 않는다**(D-42). 표시이름 입력·링크 발급·공유·개수 상한 등 화면 골격은 무변경.
> **v1.10 갱신(2026-07-24, D-33, T54, AC-058)**: **generic 보이스 챌린지(신규 능력)를 이 화면에 추가**한다 — 보이스 "지인에게 보내기"에서 clone뿐 아니라 generic 시나리오도 챌린지로 보낼 수 있다. 이 화면이 다루는 챌린지 종류는 이제 **3종**: ① **clone 보이스 챌린지**(딥보이스, 본인 voiceId 필요·UX-002/003 경유), ② **generic 보이스 챌린지**(신규 AC-058 — 클론 온보딩 스킵·voiceId 없음·GENERIC_VOICE_ID 폴백), ③ **메신저 챌린지**(음성 없음·voiceId 없음, AC-051). **종류별 카피 구분(D-33)**을 둔다: clone="○○님에게 **내 목소리(딥보이스)**로 걸려오는 전화를 보냅니다", generic 보이스="○○님에게 **기본 AI 음성**으로 걸려오는 전화를 보냅니다", 메신저="○○님에게 **메시지(카톡/문자)**를 보냅니다". 음성 관련 표기(복제 음성 스코프·추출 차단)는 clone 챌린지에만 유효하고 generic 보이스·메신저에서는 대상 부재로 무효(AC-058/AC-054 — AC-041 음성 특정 조항만 무효, AC-040/042/043은 그대로). 활성 챌린지 상한은 채널·음성모드 무관 전역 합산(AC-049/OQ-30, 무변경). **generic 보이스 챌린지 스키마 판별(channel="voice"+voiceId 부재+voiceMode="generic")·GENERIC_VOICE_ID 통화 라우팅은 architect 인계(AC-058 flag).** 표시이름 입력·링크 발급·공유·개수 상한 등 화면 골격은 무변경.
> **v1.9 갱신(2026-07-24, D-30, T48 — D-27 반전)**: 이 화면의 **진입점이 바뀐다**(화면 스펙·메커니즘은 무변경). 기존엔 별도 "챌린지 만들기" 진입점으로 도달했으나, 이제는 드릴다운 시나리오 선택 후 **UX-026(체험 선택)에서 "지인에게 보내기"를 고른 뒤** 도달한다(보이스 clone은 그 뒤 클론 온보딩 UX-002/003을 거쳐, 메신저는 클론 스킵으로 바로). **별도 "챌린지 만들기" 메뉴는 은퇴** — UX-019로 오는 경로는 UX-026 "지인에게 보내기" 하나뿐이다(D-30). 표시이름 입력·링크 발급·공유·개수 상한 등 화면 자체는 무변경.
> **v1.8 갱신(2026-07-24, D-27, T46, AC-051)**: 메신저 챌린지에도 이 화면을 재사용한다 — 발신자가 UX-024(메신저 시나리오, 비에스컬레이션)를 골라 이 화면에 오면 **voiceId 없이** 챌린지 레코드(scenarioId + 표시이름, voiceId 부재)를 만든다(AC-051, 클론 온보딩 스킵). 음성 관련 표기(복제 음성 스코프·다운로드/추출 차단)는 음성 없는 챌린지에서 대상 부재로 무효(AC-054 — AC-041 음성 특정 조항만 무효, AC-040/042/043은 그대로). 활성 챌린지 개수 초과 안내는 **보이스+메신저 전역 합산 3개** 기준이며(OQ-30), 현행 문구("동시에 만들 수 있는 챌린지 수를 초과했습니다")는 채널 비특정이라 그대로 두되 Business Rules에 전역 합산을 명시한다. **`challenges.voiceId` 옵셔널화·챌린지 채널 구분 스키마는 architect 인계.**
> **v1.6 신설(2026-07-23, D-19·D-20, T34, AC-044)**: 2인 소셜 훈련의 사용자1이 클론·시나리오 선택을 마친 뒤 챌린지 레코드를 만들고 공유 링크를 발급·공유하는 화면. **앱은 대신 전송하지 않고** 공유 시트/복사 UI만 제공(D-20).
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-004 |
| Acceptance Criteria | AC-044(clone 챌린지 레코드·공유 링크), **AC-058(generic 보이스 챌린지 신설·클론 미탑재)**, AC-049(개수 초과 상태·전역 합산), AC-041(복제 음성 스코프 고정 — clone 챌린지에만·표기 수준), AC-051(메신저 챌린지·voiceId 없음) |
| Purpose | 선택한 시나리오 + 표시이름(+ clone 챌린지일 때만 본인 voiceId)으로 챌린지 레코드를 만들고, 공유 링크(토큰)를 발급해 사용자1이 직접 지인에게 보내게 한다. 챌린지 종류(clone 딥보이스 / generic 보이스 / 메신저)에 맞는 카피를 보인다(D-33). |
| User Goal | 고른 시나리오를 지인에게 보낼 챌린지 링크를 얻는다(내 목소리 딥보이스 / 기본 AI 음성 / 메시지 중 하나). |
| Entry | **(v1.10, D-31·D-33)** UX-026에서 "지인에게 보내기" → (보이스면 UX-016 방식 선택 →) UX-017/UX-024 시나리오 선택 후 진입. **clone 보이스**는 클론 온보딩(UX-002/003) 경유, **generic 보이스**(AC-058)·**메신저**(AC-051)는 클론 스킵으로 바로. *(구: 별도 "챌린지 만들기" 진입점 — 은퇴.)* |
| Exit | 링크 발급·공유 후 UX-020(내 챌린지 결과 열람)으로 이동하거나 처음으로. |
| Primary Actions | **표시이름 입력**(사용자2에게 보일 "○○님이 준비" 문구), "챌린지 만들기"(레코드 생성+링크 발급), 발급된 링크 **공유하기(OS 공유 시트)** 또는 **링크 복사**(P-14). |
| Secondary Actions | 시나리오·목소리 다시 선택(뒤로), 만든 챌린지 결과 보기(UX-020). |
| States | Loading: 챌린지 생성·링크 발급 중 스피너 / Empty: N/A / Error: (a) 활성 챌린지 개수 초과(AC-049) → "동시에 만들 수 있는 챌린지 수를 초과했습니다. 기존 챌린지가 끝나거나 만료되면 다시 만들 수 있어요"(수치·정책은 architect) (b) 링크 발급 실패 → 재시도(P-4) / Success: 링크 표시 + 공유/복사 버튼 활성. |
| Validation | 표시이름 미입력 시 "챌린지 만들기" 비활성(P-5). 처리 중 중복 생성 방지(버튼 비활성). |
| Failure | 개수 초과·발급 실패 → 위 Error 상태. 공유 시트 미지원 브라우저 → 링크 텍스트+복사 버튼으로 폴백(침묵 실패 금지, P-14). |
| Accessibility | 표시이름 입력·"챌린지 만들기"·공유·복사 버튼 큰 터치 타깃(≥48px)·명확한 라벨. 발급된 링크는 스크린리더가 읽을 수 있는 텍스트로도 노출(아이콘 단독 금지). 복사 완료를 색 아닌 텍스트("복사됨")로 안내. 큰 글씨. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High(확장 P0·신규 모델) |
| Business Rules | 챌린지 레코드 = scenarioId + 표시이름 (+ **clone 보이스 챌린지에서만** 사용자1 voiceId, AC-044). **(v1.10, D-33) 3종 지원**: ① clone 보이스(voiceId 有·복제 음성은 이 챌린지 스코프에 **고정**·다운로드/추출 UI 없음, AC-041), ② **generic 보이스(신규 AC-058 — voiceId 없음·voiceMode="generic"·GENERIC_VOICE_ID 폴백으로 통화 합성)**, ③ 메신저(voiceId 없음·음성 없음, AC-051). generic 보이스·메신저 챌린지에는 복제 음성이 없어 AC-041 음성 특정 조항(추출 차단·보존기간)이 대상 부재로 무효(AC-058/054 — AC-040/042/043은 그대로 적용). 링크는 **랜덤 토큰+만료+1회성**(AC-048, 스키마·만료 수치는 architect T35). 활성 챌린지 개수 상한(AC-049) 도달 시 생성 차단·안내 — **상한은 채널·음성모드 무관 전역 합산 3개(OQ-30, 수치·정책은 architect)**. **앱이 대신 카톡/문자를 전송하지 않는다**(D-20) — 공유 시트/복사만. **architect 인계: `challenges.voiceId` 옵셔널화(§14.8.1 확정)에 더해 generic 보이스 챌린지 판별(channel="voice"+voiceId 부재+voiceMode="generic")·GENERIC_VOICE_ID 통화 라우팅 접합(AC-058 flag).** |
| Data Required | 사용자1 uid, 선택 scenarioId, 챌린지 종류(clone|generic 보이스|메신저) + (clone일 때만)voiceId, 표시이름, 발급된 링크 토큰·만료, 현재 활성 챌린지 개수(전역). |
| Data Operations | Create(챌린지 레코드·링크 토큰), Read(활성 챌린지 개수·상한 판정) |
| External Dependencies | Firebase Functions(챌린지 생성·토큰 발급 콜러블, T35). Firestore(challenges 레코드). Web Share API(navigator.share)·Clipboard API(공유/복사, 클라). |
| Permissions | None(클립보드/공유는 사용자 제스처 기반) |
| Navigation Targets | UX-020, 처음으로 |
| Events Emitted | `challenge_created`, `challenge_link_shared`(공유/복사 구분) |
| Expected Outputs | 챌린지 레코드 + 공유 링크(토큰). |
| Assumptions | 사용자1은 시나리오 선택을 마쳤고, **clone 챌린지면 클론(UX-003)까지 완료**한 상태(generic 보이스·메신저 챌린지는 클론 없이 진입). 챌린지 종류는 상류(UX-016 방식·UX-017/UX-024 시나리오 채널)에서 확정돼 전달됨. 토큰 정책·개수 상한 수치·generic 보이스 스키마/라우팅은 architect(T35·§14.8)가 확정하며 화면은 상태·카피만 표현. 복수 목소리 보관·선택(P-8 내 목소리 금고)은 이번 범위 밖. |

### 사용자1 챌린지 결과 열람 (Screen ID: UX-020)
> **v1.10 갱신(2026-07-24, D-34, T54, AC-058·OQ-32)**: **generic 보이스 챌린지(AC-058)의 결과도 "완료 여부"만** 표시한다 — planner 권고 default(OQ-32) 적용. 즉 "완료 · 의심 시점: 약 N초"는 이제 **clone 보이스 챌린지에만** 적용되고, **generic 보이스 챌린지·메신저 챌린지는 "완료 · 상대가 체험을 마침"(시점 없음)**으로 묶인다. 근거(OQ-32): "완료 여부만"은 복제 음성 유출 리스크가 아니라 **수신자 프라이버시**를 위한 것(OQ-31)이라 clone 유출 위험이 없는 generic 보이스에도 동일 동기가 적용되고, generic 보이스 챌린지는 음성 없는 메신저 챌린지와 동일 안전·프라이버시 계층이라 같은 열람 규칙으로 묶는 것이 일관적이다. **default임을 명시(OQ-U13)** — 사용자가 "generic은 유출 위험이 없으니 clone처럼 의심 시점까지 열자"고 확정하면 반대로 뒤집을 수 있으며, 그때는 이 화면의 generic 보이스 케이스를 clone 케이스와 동일하게 되돌린다. resultSummary를 generic 보이스에서 완료 플래그만으로 파생하는 것은 architect 인계(메신저 파생 로직과 동형).
> **v1.8 갱신(2026-07-24, D-29, T46, AC-055)**: 메신저 챌린지 결과는 **"완료 여부"만** 표시한다 — 보이스 챌린지의 "완료 · 의심 시점: 약 N초"(AC-043, States Success (c))와 달리, 메신저에서는 의심 시점(스미싱 링크 탭·가짜 랜딩 정보입력·에스컬레이션 도달)을 **어떤 형태로도 노출하지 않는다**(AC-055·OQ-31). 즉 States Success (c)의 "의심 시점: 약 N초"는 **보이스 챌린지에만** 적용되고, 메신저 챌린지의 완료+동의 항목은 **"완료 · 상대가 체험을 마침"(시점 없음)**으로 표시한다. resultSummary를 메신저에서 완료 플래그만으로 파생하는 것은 architect 인계. *(v1.10 D-34: "보이스 챌린지"를 "clone 보이스 챌린지"로 좁힘 — generic 보이스는 메신저와 함께 완료 여부만.)*
> **v1.6 신설(2026-07-23, D-21, T34, AC-043)**: 사용자1이 자기가 보낸 챌린지의 결과를 **사용자2 동의 시에만·제한된 범위(완료 여부/의심 시점)로만** 보는 화면. **대화 전문 열람 화면은 만들지 않는다**(AC-043, D-21).
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-004 |
| Acceptance Criteria | AC-043(결과 열람 제한), AC-041(음성 삭제/보존 표기 수준) |
| Purpose | 사용자1이 보낸 챌린지의 진행 상태와, 사용자2가 결과 공유에 동의한 경우에 한해 "완료 여부 / 의심(의심 표한) 시점" 수준의 요약만 확인한다(AC-043). 대화 전문·상대 발화 원문은 제공하지 않는다. |
| User Goal | 내가 보낸 챌린지를 상대가 해봤는지, 어디서 의심했는지 정도만 확인한다. |
| Entry | UX-019에서 챌린지 발급 후, 또는 재방문 시 챌린지 목록 진입. |
| Exit | 처음으로. |
| Primary Actions | 내 챌린지 목록·상태 열람, (동의된 항목) 제한 결과 보기, (선택) 챌린지·클론 음성 수동 삭제(AC-041). |
| Secondary Actions | 새 챌린지 만들기(UX-019). |
| States | Loading: 목록·상태 로드 / Empty: 만든 챌린지 없음 → "아직 만든 챌린지가 없습니다" + 만들기 유도 / Error: 로드 실패 재시도(P-4) / Success(항목별): (a) 미완료 → "상대가 아직 해보지 않았습니다" (b) 완료+미동의 → "상대가 완료했지만 결과 공유에 동의하지 않았습니다"(상세 감춤, AC-043) (c) 완료+동의 → **(clone 보이스 챌린지)** "완료 · 의심 시점: 약 N초"(AC-043) / **(generic 보이스 챌린지·메신저 챌린지, v1.10 D-34)** "완료 · 상대가 체험을 마침" — 의심 시점 미표시(AC-055·OQ-31·OQ-32 default). |
| Validation | N/A(열람 화면). 삭제는 되돌릴 수 없어 확인 다이얼로그(P-9). |
| Failure | 로드 실패 → 재시도. |
| Accessibility | 각 챌린지 상태를 색 아닌 텍스트 라벨로 명시. 목록 항목 큰 터치 타깃. 삭제 버튼은 명확한 라벨 + 확인 단계. 큰 글씨. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High(확장 P0·프라이버시 게이트) |
| Business Rules | **사용자2가 결과 공유에 동의한 경우에만** 상세 결과 표시(AC-043). 열람 범위는 **clone 보이스 챌린지**에서만 "완료 여부 / 의심 시점"으로 제한 — **대화 전문·상대 원문 전개 화면을 만들지 않는다**(D-21). **generic 보이스 챌린지(v1.10, D-34·OQ-32 default)·메신저 챌린지(v1.8, D-29)는 "완료 여부"만**이며 의심 시점(스미싱 링크 탭·가짜 랜딩 입력·에스컬레이션 도달·통화 의심 시점)을 어떤 형태로도 노출하지 않는다(AC-055·OQ-31·OQ-32) — resultSummary를 generic 보이스·메신저에서 완료 플래그만으로 파생(architect, 동형 로직). 미동의 시 상세 감춤. 복제 음성은 clone 챌린지에서만 기간제 보존·수동 삭제 대상(AC-041) — 삭제 실행 시 실제 폐기는 백엔드(architect T35), 화면은 삭제 트리거·상태만. |
| Data Required | 사용자1의 챌린지 목록(상태·완료 여부), 사용자2 결과 공유 동의 플래그, (동의 시)의심 시점 요약값. |
| Data Operations | Read(챌린지 목록·제한 결과), Delete(수동 삭제 트리거 — 실제 폐기는 백엔드) |
| External Dependencies | Firestore(challenges·결과 요약). Firebase Functions(삭제 처리, T35·AC-041). |
| Permissions | None |
| Navigation Targets | UX-019, 처음으로 |
| Events Emitted | `challenge_result_viewed`, `challenge_deleted`(수동 삭제 시) |
| Expected Outputs | 제한된 결과 요약 표시(동의 시) 또는 상태만(미동의). |
| Assumptions | 결과 공유 동의 여부는 사용자2가 UF-005 종료 시 결정(UX-018). 상한·보존 정책은 architect(T35). 전문 열람이 필요하다는 요구는 없으며 AC-043이 이를 금지한다. |

### 사용자2 동의 랜딩 (Screen ID: UX-021)
> **v1.11 갱신(2026-07-25, D-42·D-35)**: (1) 동의 문구에 **발신자가 고른 난이도**를 함께 표시한다 — 특히 **고급이면 "강한 압박이 이어질 수 있습니다"를 동의 전에 명시**한다. 이는 AC-040 사전 동의의 **정보량을 늘리는 방향**이며 게이트 로직·무동의 차단(P-15)은 무변경이다. **난이도는 동의 게이트를 우회·완화·게이팅하지 않는다**(D-42). (2) 동의 후 통화 체험(UX-014 재사용)에서 **통화 중 문자 오버레이(UX-027)**가 동일하게 나타날 수 있음을 전제한다 — 익명 세션이어도 오버레이는 라우트가 아닌 화면 위 계층이라 진입 맥락과 무관하며, 오버레이 안에서도 "훈련 종료"(AC-006)·모의 표식(AC-022)이 유지된다.
> **v1.8 갱신(2026-07-24, D-28, T46, AC-053/054)**: 메신저 챌린지도 이 랜딩을 재사용한다. 동의 게이트(AC-040·무동의 차단·P-15)는 채널 무관이라 그대로. 다만 **채널 인지 문구 변형**을 둔다 — 챌린지 채널이 메신저면 고지를 "○○님이 보이스피싱 인식 테스트를 준비했고, 지금부터 받는 **메시지(카톡/문자)**는 실제가 아닌 훈련입니다"로 표시하고 **음성/전화 프레이밍·"복제 음성" 표현을 쓰지 않는다**(음성 없는 챌린지엔 재생할 복제 음성이 없음, AC-054). Exit는 채널 분기: **보이스 챌린지→UX-014, 메신저 챌린지→UX-022(채팅 셸)**. 사용자2 익명 인증+토큰 진입·만료·신고(AC-048/049) 무변경. **메신저 세션 생성(consentChallenge 메신저 변형: channel="messenger"·voiceId 미저장·통화 자격증명 미발급)은 architect 인계(§14.7은 보이스만 다룸).**
> **v1.6 신설(2026-07-23, D-19·P-15, T34, AC-040)**: 초대받은 사용자2가 링크로 처음 만나는 화면. **명시적 탭 동의 없이는 어떤 복제 음성도 재생되지 않으며, 무동의로 진행하는 경로 자체가 UI에 없다**(AC-040). 사용자2는 로그인 없이 링크만으로 진입(AC-048).
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-005 |
| Acceptance Criteria | AC-040(사전 동의·무동의 차단), AC-048(무로그인 링크 진입·만료 상태), AC-049(신고 버튼) |
| Purpose | 사용자2에게 "○○님이 보이스피싱 인식 테스트를 준비했고, 지금부터 받는 전화·문자는 실제가 아닌 훈련"임을 사전 고지하고, **명시적 탭 동의**를 받아야만 체험(복제 음성)이 시작되게 하는 안전 게이트(AC-040). 시나리오의 구체 내용·타이밍은 서프라이즈로 두되 "시뮬레이션임" 사실은 사전 고지. |
| User Goal | 이게 지인이 준비한 안전한 훈련임을 이해하고 동의해 체험을 시작한다(또는 원치 않으면 신고/이탈). |
| Entry | 사용자1이 공유한 링크 열기(로그인 없이 진입, AC-048). |
| Exit | 동의 → **채널 분기(v1.8): 보이스 챌린지=통화 체험(UX-014), 메신저 챌린지=메신저 채팅(UX-022)** 재사용. 신고/이탈 → 종료. |
| Primary Actions | 고지 문구 확인, **"동의하고 시작"(명시적 탭, AC-040)**. |
| Secondary Actions | "원치 않는 챌린지 신고"(AC-049 — 위치는 이 랜딩, 정책은 architect), 나가기. |
| States | Loading: 링크·챌린지 검증 스피너 / Empty: N/A / Error: (a) 만료·소진된 링크 → "이 링크는 만료되었습니다"(진입 차단, AC-048, 만료 수치는 architect) (b) 유효하나 데이터 로드 실패 → 재시도(P-4) / Success: 고지+동의 버튼 표시. |
| Validation | 명시적 동의 탭 전에는 체험(통화) 진입 불가 — **무동의 진행 버튼/경로가 화면에 존재하지 않는다**(AC-040, P-15). |
| Failure | 만료 링크 → 차단 안내. 동의 없이 이탈 → 세션 미시작(복제 음성 미재생). |
| Accessibility | 고지 문구 큰 글씨·단순 문장(어르신 포함). 동의 버튼 큰 터치 타깃(≥48px)·명확 라벨. 신고 버튼은 색 아닌 텍스트+아이콘. 스크린리더가 고지 전문과 동의 상태를 읽음. 만료·오류는 아이콘+텍스트(색 단독 금지). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Critical(확장 P0·핵심 안전 게이트) |
| Business Rules | **동의 없이는 어떤 복제 음성 전화·문자도 재생되지 않으며, 무동의로 실존 인물을 복제 음성으로 기만하는 경로가 UI·API에 존재하지 않는다**(AC-040). **(v1.8) 메신저 챌린지에서도 동일 — 무동의 상태로 실존 인물에게 메신저 기만(카톡/문자 대화)을 재생하는 경로가 UI·API에 존재하지 않는다(AC-054, AC-040 확장). 문구만 채널 인지 변형("메시지(카톡/문자)")이고 게이트 로직은 무변경.** 사후 정체 공개(AC-042)는 사전 동의를 대체하지 못한다. 사용자2는 별도 계정/로그인 없이 링크(토큰)만으로 진입(AC-048). 만료·1회 소진된 토큰은 진입 차단(AC-048, 수치·검증은 architect T35). "원치 않는 챌린지 수신" 신고 버튼 제공(AC-049 — UI만, 데이터 모델·처리 정책은 architect). 시뮬레이션 사실은 사전 고지하되 시나리오 구체 내용은 서프라이즈 유지(AC-040). |
| Data Required | 링크 토큰, 챌린지 메타(표시이름·scenarioId — 음성 자체는 재생 직전까지 노출 안 함), 토큰 유효/만료 상태, 동의 이벤트(여부·타임스탬프). |
| Data Operations | Read(토큰 검증·챌린지 메타), Create(동의 로그·신고 이벤트) |
| External Dependencies | Firebase Functions(토큰 검증·동의 기록·신고 접수, T35). Firestore(challenges·consent·reports). |
| Permissions | None(동의 전) — 통화 진입(UX-014) 시점에 마이크 등 권한 요청. |
| Navigation Targets | UX-014(보이스 챌린지 체험), **UX-022(메신저 챌린지 체험, v1.8)**, 종료(신고/이탈) |
| Events Emitted | `challenge_consent_granted`, `challenge_reported`, `challenge_link_expired`(만료 진입 시) |
| Expected Outputs | 명시적 동의 상태(→ UX-014 체험 게이트 통과) 또는 신고/만료 종료. |
| Assumptions | 토큰 정책·만료·신고 데이터 모델은 architect(T35). **(v1.8) 사용자2 메신저 체험 표면은 이제 UX-022/023 재사용으로 설계됨(OQ-U11 P0 텍스트 케이스 resolved)** — 메신저 챌린지는 동의 후 UX-022로, 보이스 챌린지는 UX-014로 분기. (보이스 챌린지) 복제 음성은 동의 통과 후 UX-014에서만 재생되고, 음성 없는 메신저 챌린지에는 재생할 복제 음성이 없다(AC-051/054). |

### 메신저 시나리오 선택 (Screen ID: UX-024)
> **v1.11 갱신(2026-07-25, D-41)**: UX-017과 동일하게 Exit에 **UX-029(난이도 선택)**가 삽입된다 — 시나리오 확정 → UX-029 → (self·비에스컬레이션)`createSession`→UX-022 / (self·에스컬레이션 가능)UX-025→UX-022 / (send)UX-019. 즉 **난이도는 목소리 선택(UX-025)보다 앞**에 온다(난이도는 시나리오 강도이고 UX-025는 전이 대비 음성 확보라 성격이 다르며, 난이도가 전이 도달 속도에 영향을 주므로 먼저 정해져야 한다). 시나리오 필터(self=전체 / send=비에스컬레이션만)는 **무변경** — 난이도는 어떤 시나리오도 감추지 않는다.
> **v1.10 갱신(2026-07-24, D-31, T54, AC-056)**: **체험/발송 선택(UX-026)이 이 화면보다 먼저** 오도록 상향됐다(AC-056) — 이 화면의 Entry가 UX-015에서 **UX-026**으로 바뀌고, **UX-026에서 고른 모드(self/send)에 따라 노출 시나리오 집합이 갈린다**: **self 모드(본인이 체험)**는 **에스컬레이션 가능 시나리오를 포함한 전체 메신저 시나리오**를 노출(자기훈련은 에스컬레이션 제약 없음), **send 모드(지인에게 보내기)**는 **비에스컬레이션 메신저 시나리오만** 노출(P0 챌린지는 에스컬레이션 미포함, #21/OQ-28 보류). Exit: **self + 비에스컬레이션** → `createSession`(channel="messenger") 후 UX-022, **self + 에스컬레이션** → UX-025(목소리 선택)→UX-022, **send(비에스컬레이션)** → UX-019(챌린지 만들기). v1.9의 "비에스컬레이션→UX-026" Exit는 이 노트로 대체된다(모드가 이미 상류에서 정해져 이 화면은 시나리오만 확정).
> **v1.9 갱신(2026-07-24, D-30, T48 — D-27 반전)**: **비에스컬레이션** 메신저 시나리오의 Exit가 `createSession`→UX-022 직행에서 **UX-026(체험 선택)**으로 바뀐다 — 비에스컬레이션 시나리오는 "지인에게 보내기"(챌린지)가 가능하므로 "본인이 체험/지인에게 보내기"를 먼저 묻는다. T46이 "별도 진입점으로 들어오면 챌린지 생성 모드"라 부르던 것(v1.8 note)은 **은퇴**하고, 챌린지 생성은 이제 **UX-026 "지인에게 보내기"→UX-019**로만 진입한다(별도 "챌린지 만들기" 메뉴 없음, D-30). **에스컬레이션 가능 시나리오**는 챌린지 대상이 아니라(#21/OQ-28 보류) UX-026을 거치지 않고 기존대로 UX-025(목소리 선택)→UX-022로 직행(무변경). 즉 이 화면에는 별도 "챌린지 생성 모드 플래그"가 더는 필요 없다 — 모드는 UX-026에서 갈린다. *(v1.10 D-31로 순서 상향 — self/send 모드가 노출 시나리오를 필터. 위 노트 참조.)*
> **v1.8 갱신(2026-07-24, D-27, T46, AC-051)**: 이 화면을 **2인 메신저 챌린지 생성(UF-004 발신자)**에도 재사용한다 — 발신자가 별도 "챌린지 만들기" 진입점으로 들어오면 이 화면이 **챌린지 생성 모드**로 동작한다: (1) 노출 시나리오를 **비에스컬레이션 메신저 시나리오로 한정**(P0 챌린지는 에스컬레이션 미포함, #21/OQ-28 보류), (2) 선택 후 Exit가 `createSession`(본인 체험) 대신 **UX-019(챌린지 만들기)**로 간다. 자기 체험 모드(UF-006)일 때의 Exit·escalation 분기는 무변경. 챌린지 생성 모드 라우팅·모드 플래그 세부는 architect 인계.
> **v1.7 신설(2026-07-23, D-22, T24)**: UX-015에서 "메신저피싱"을 고른 사용자에게 **메신저 채널 시나리오만** 노출하는 드릴다운 화면(보이스의 UX-017에 대응). 채널이 곧 유형이라 별도 방식(clone/generic) 단계 없이 유형 선택 → 시나리오 노출 2단계로 동작(AC-029). UX-017의 메타 카드 패턴 재사용.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-006, UF-004(2인 메신저 챌린지의 시나리오 선택 단계 — **v1.9(D-30)부터 UX-026 "지인에게 보내기" 경유**, 별도 챌린지-생성 모드 아님) |
| Acceptance Criteria | AC-030(표면 2종·복수 시나리오), AC-002(메타 표기), AC-029(단계적 노출), **AC-056(체험/발송 선택 상향 — 모드가 이 화면보다 먼저 정해짐)**, **AC-051(챌린지 대상=비에스컬레이션 메신저 시나리오 한정, send 모드에서만 노출)** |
| Purpose | 메신저(카카오톡형/문자형) 채널에 속한 시나리오만 노출하고, 시작 전 표면·제목·사기 유형·예상 소요·난이도를 이해시켜 하나를 고르게 한다(AC-030/002). 전체 시나리오 평면 나열 아님(AC-029). |
| User Goal | 체험할 메신저 사기 시나리오를 고른다. |
| Entry | **(v1.10, D-31)** UX-026에서 메신저 유형 + 모드(본인이 체험 | 지인에게 보내기) 선택 후. *(구 v1.9: UX-015에서 "메신저피싱" 선택 직행 — UX-026이 상향되며 대체.)* |
| Exit | **(v1.10, D-31) 모드별 분기**: **self(본인이 체험) + 비에스컬레이션** → `createSession`(channel="messenger") 후 UX-022. **self + 에스컬레이션 가능**(scenario.escalation 존재) → UX-025(조건부 목소리 선택, UF-007) 후 UX-022. **send(지인에게 보내기, 비에스컬레이션만)** → UX-019(챌린지 만들기, voiceId 없음). *(send 모드에서는 에스컬레이션 가능 시나리오가 애초에 노출되지 않는다 — #21/OQ-28 보류.)* |
| Primary Actions | (메신저 채널로 필터된) 시나리오 카드 선택 → "시작". |
| Secondary Actions | 뒤로(UX-015 유형 선택으로 복귀, P-12). 시나리오 상세(표면·난이도) 보기. |
| States | Loading: 메신저 시나리오 목록 로드 / 세션 생성 중 스피너 / Empty: 메신저 시나리오 0건이면 "메신저 시나리오가 아직 없습니다"(콘텐츠 준비 상태에 따름) / Error: 로드·세션 생성 실패 재시도(P-4) / Success: 선택 카드 강조 후 시작. |
| Validation | 시나리오 미선택 시 "시작" 비활성(P-5). |
| Failure | 콘텐츠 로드·세션 생성 실패 → 재시도(P-4). |
| Accessibility | 각 카드에 **표면(카카오톡/문자)**·제목·사기 유형·예상 소요·난이도를 텍스트로 명시(색 배지 단독 금지, AC-002/030). 상단 맥락("메신저피싱 › 시나리오")을 스크린리더가 읽음. 큰 글씨·큰 터치 타깃·키보드 이동. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High(확장 P0) |
| Business Rules | `scenarios/{id}.channel==="messenger"`로 필터된 시나리오만 노출(§13.4). 각 시나리오는 표면(surface: kakao|sms)·제목·유형·소요·난이도 표시(AC-030/002). 한국어 복수 종 포함(AC-030). **(v1.10, D-31) 노출·Exit는 상류 모드(UX-026)에 종속**: **self 모드**는 에스컬레이션 가능 시나리오를 **포함한 전체** 메신저 시나리오를 노출(비에스컬레이션→createSession→UX-022, 에스컬레이션→UX-025→UX-022). **send 모드**는 **비에스컬레이션 시나리오만** 노출(→UX-019, voiceId 없음; 에스컬레이션 가능은 필터로 배제, #21/OQ-28 보류). scenario.escalation 존재 여부로 self 모드 다음 단계 분기 — 조용한 실패 금지. 이 화면은 시나리오만 확정하고 "본인 체험 vs 챌린지" 모드는 이미 UX-026에서 정해졌다(별도 챌린지-생성 모드 플래그 없음). |
| Data Required | 메신저 시나리오 메타(id·title·fraudType·estimatedDuration·difficulty·channel·surface·escalation 유무). 상류 모드(self|send)·필터 조건. |
| Data Operations | Read(메신저 시나리오 목록·모드별 필터). Create(**self + 비에스컬레이션에서만** `createSession`; self + 에스컬레이션은 UX-025 하류, send는 UX-019 하류). |
| External Dependencies | Firestore/정적 콘텐츠(메신저 시나리오, T6/T29). self 비에스컬레이션 경로는 `createSession` 콜러블. |
| Permissions | None |
| Navigation Targets | **UX-022(self 비에스컬레이션 — createSession 후)**, **UX-025(self 에스컬레이션)**, **UX-019(send 비에스컬레이션 — 챌린지)**, UX-026(뒤로) |
| Events Emitted | `scenario_selected`(channel=messenger; 모드 컨텍스트 포함) |
| Expected Outputs | 선택된 메신저 시나리오 id + surface + escalation 유무 + 모드 → self는 (비에스컬레이션 createSession→UX-022 / 에스컬레이션 UX-025), send는 UX-019로 전달. |
| Assumptions | 메신저 시나리오 콘텐츠·escalation 메타는 T6/T29에서 준비됨. 표면·스킨은 프레젠테이션 레이어라 이 화면은 시나리오만 정하고 스킨 판정은 UX-022에서 수행(§13.5). **모드(self/send)는 UX-026에서 이미 확정돼 클라 상태로 전달됨**(architect 인계 — 모드 배선). |

### 메신저 채팅 셸 — 카카오톡형 / 문자메시지형(iOS·삼성·기본) (Screen ID: UX-022)
> **v1.8 갱신(2026-07-24, D-28, T46, AC-053/054)**: 이 화면을 **2인 메신저 챌린지의 사용자2 체험**에도 재사용한다(UF-005 메신저 변형). 콘텐츠 무해화·스킨·스미싱 가짜 랜딩(UX-023)·카카오 면책 배너·"AI 훈련용 모의" 표식·상시 "훈련 종료" 등 **모든 안전속성은 단독 훈련과 동일하며, 2인 래퍼(익명 인증·토큰·동의 게이트)가 어떤 방식으로도 약화하지 않는다**(AC-053/054, §13.4/13.7 불변). 차이는 세션 소유자가 익명 uid이고 진입이 UX-021 동의 랜딩을 거친다는 점뿐(콘텐츠·안전 셸 무관). **P0 챌린지는 비에스컬레이션 시나리오만** — 이 흐름에서는 "전화로 확인" 명시 전환 버튼·escalation-pending 상태·UX-025·UX-014 전이가 **노출/발생하지 않는다**(에스컬레이션 메신저 챌린지는 #21/P1·OQ-28 보류). 익명 uid 소유 messenger 세션의 sendMessage/메시지 read가 소유자 규칙으로 성립하는지는 architect 인계(§14.7이 보이스 세션만 확인함).
> **v1.7 신설(2026-07-23, D-22·D-23·D-24, T24)**: 카카오톡형과 문자메시지형을 **하나의 스킨 레이어 화면**으로 처리한다(D-22 근거: Architecture §13.4 "표면=콘텐츠와 분리된 프레젠테이션 레이어" — 안전 셸·콘텐츠·링크·에스컬레이션은 동일하고 외형 스킨만 다르다). 문자형은 UA 자동 감지로 iOS/삼성/기본 스킨을 적용하고 수동 토글을 제공(§13.5). **스킨은 어떤 안전 판정도 게이팅하지 않는다.**
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-006 (에스컬레이션 트리거 시 UF-007로 전이), **UF-005(2인 메신저 챌린지 사용자2 체험 재사용, v1.8 — P0는 비에스컬레이션만)** |
| Acceptance Criteria | AC-030, AC-031(스킨 감지·폴백·수동 전환), AC-032(무해화), AC-045(스미싱 링크 진입점), AC-047(카카오 면책 고지), AC-002, AC-006(상시 종료), AC-034(명시 전환 버튼) |
| Purpose | 카카오톡/문자 표면을 정확 모사한 채팅으로 메신저 사기 대화를 재현하되, 실 URL 이동·실 전송 없이 안전하게 체험시킨다. 표면 스킨은 외형일 뿐이고 콘텐츠·안전장치·에스컬레이션은 표면과 무관하게 동일하다(§13.4). |
| User Goal | 내 기기에 익숙한 메신저 화면에서 사기 대화를 실감 나게 겪되 언제든 끊는다. |
| Entry | UX-024에서 비에스컬레이션 시나리오 선택 후, 또는 UX-025(목소리 선택) 완료 후. |
| Exit | 종료 조건(사용자 "훈련 종료"·max-turn) → UX-007. **에스컬레이션 트리거 발생 → 전이 연출(P-18) → UX-014(수신 phase), UF-007.** 스미싱 링크 탭 → UX-023(가짜 랜딩) 후 복귀. |
| Primary Actions | 메시지 입력·전송, 스미싱 링크(displayText) 탭 → UX-023, "훈련 종료"(전 구간 상시, AC-006), **(에스컬레이션 가능 시나리오)** "전화로 확인" 명시 전환 버튼(1턴부터 상시, §13.3). |
| Secondary Actions | **수동 스킨 토글(iOS/삼성/기본 전환, 문자형, AC-031/P-16)**, 대화 스크롤. |
| States (표면·스킨) | **surface=kakao**: 카카오 정확 모사 버블·상단바 + **영구 비활성 불가 상시 면책 배너**("카카오톡 실제 서비스와 무관한 훈련용 재현", AC-047/P-19). **surface=sms**: 문자 스킨 — `ios`(iOS 기본 메시지)/`samsung`(삼성 메시지)/`default`(폴백). skinSource 표시: `auto`(감지됨)/`fallback`(감지 실패→기본, 수동 토글 안내)/`manual`(사용자 전환). 공통: **"AI 훈련용 모의" 표식 상시**, 상시 "훈련 종료". |
| States (대화) | Loading: 상대 응답 대기(타이핑 인디케이터) / Empty: 시작 시 사기범 첫 메시지 노출 / Error: LLM 응답 실패·지연 → **T43 갱신**: 재시도 버튼 대신 그 턴만 규칙 기반 목업으로 조용히 대체(completeWithFallback, 대화 끊김 없음) / escalation-pending: 트리거 감지(서버 escalation 플래그) → 전이 연출로 전환(P-18) / ended: 종료 → UX-007. |
| Validation | 빈 메시지 전송 방지, 전송 대기 중 중복 전송 방지(버튼 비활성). |
| Failure | LLM 실패/지연 → 재시도(P-4), 대화 유지. 가짜 랜딩(UX-023) 로드 실패 → 조용히 생략하고 채팅 지속(비차단). max-turn 도달 → 종료(UX-007) 또는 (에스컬레이션 시나리오)폴백 전이(§13.3). UA 감지 실패 → 기본 스킨 폴백 + 수동 토글 안내(침묵 실패 금지). |
| Accessibility | "훈련 종료"·"전화로 확인"·스킨 토글·링크 키보드 접근, "훈련 종료"는 스크롤 무관 상시 노출. 발신자 구분을 색 아닌 라벨로. 신규 메시지 aria-live. **"AI 훈련용 모의" 표식·카카오 면책 배너는 색 아닌 텍스트 포함, 점멸 금지·상시 고정**(AC-047). 스미싱 링크는 "링크(모의)"임을 스크린리더가 알 수 있게 라벨(실제 링크로 오인 방지). 스킨은 외형만 — 스킨 전환이 콘텐츠·안전 문구를 바꾸지 않음을 스크린리더 순서로도 유지. 큰 글씨. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High(확장 P0·핵심 표면) |
| Business Rules | 표면(surface)·스킨은 **프레젠테이션 전용 — 어떤 안전 판정도 게이팅하지 않는다**(§13.4/13.5). 스킨은 `sessions.messengerSkin`(ios|samsung|default)·`sessions.skinSource`(auto|manual|fallback)에 기록(UA 판정 규칙표 §13.5, 판정 로직은 architect). 스미싱 링크는 `messages.attachments[].{displayText,fakeLandingId,harmless:true}`로만 표현 — **실 URL 필드가 스키마에 없고 외부 네비게이션 경로가 없다**(AC-032/045/§13.4). 탭 시 `fakeLandingId`가 가리키는 인앱 가짜 랜딩(UX-023)으로만 이동. 카카오 면책 고지는 **영구 비활성화 불가·항상 노출·재진입 시에도 유지**하는 UI 요건(데이터 필드 아님, AC-047/§13.7). 모든 합성/모의 요소에 "AI 훈련용 모의" 표식(AC-032/022 계승). "훈련 종료"는 모든 턴 즉시 종료(AC-006). 사기 콘텐츠 무해화(실계좌·실송금·실기관명·실URL 없음, AC-032/033). 인젝션 방어·PII 마스킹 유지(AC-024). "전화로 확인" 명시 전환 버튼은 escalation 시나리오에서만 노출(§13.3). |
| Data Required | 세션 id·channel(=messenger)·messengerSkin·skinSource, 시나리오 표면(surface)·프롬프트, 대화 히스토리(마스킹), 메시지 attachments(link displayText·fakeLandingId), 턴 수·한도, escalation 유무·escalation 플래그. |
| Data Operations | Create(사용자·AI 메시지), Read(대화·시나리오·attachments), Update(세션 스킨/상태·턴 카운트) |
| External Dependencies | Firebase Functions → LLM(sendMessage, sentinel 신호는 서버가 감지·제거·`escalation` 플래그로 반환 §13.2). Firestore(세션·메시지·attachments, PII 토큰화). UA는 클라 판정 후 세션 기록(§13.5). |
| Permissions | None(문자 입력 기반; 에스컬레이션 후 UX-014에서 마이크 요청) |
| Navigation Targets | UX-023(스미싱 링크), UX-014(에스컬레이션 전이), UX-007(종료) |
| Events Emitted | `message_sent`, `ai_replied`, `smishing_link_tapped`, `skin_switched`(manual), `escalation_triggered`(서버 플래그 수신), `session_end_requested` |
| Expected Outputs | 마스킹된 교차채널 대화 로그(리포트 입력)·종료/전이 사유. |
| Assumptions | 스킨 판정 규칙·escalation 신호 처리는 서버(§13.2/13.5). sentinel 토큰은 서버가 제거해 클라에 오지 않음(UX는 노출 안 됨 전제). 메신저 콘텐츠·attachments는 T6/T29. |

### 스미싱 링크 인앱 가짜 랜딩 (Screen ID: UX-023)
> **v1.7 신설(2026-07-23, D-23, T24)**: 채팅(UX-022)에서 스미싱 링크(displayText)를 탭하면 나타나는 **인앱 가짜 로그인/정보입력 목업**. 외부로 이동하지 않고, 입력값은 서버로 전송되지 않으며 UI상 가짜 피드백만 준다(AC-045/§13.4).
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-006, **UF-005(2인 메신저 챌린지 사용자2 체험 재사용, v1.8)** |
| Acceptance Criteria | AC-045(인앱 가짜 랜딩), AC-032(무해화), AC-022(모의 표식), **AC-053(2인 무약화 재사용)** |
| Purpose | "링크를 눌렀더니 로그인/정보 입력을 요구하는" 스미싱 수법을 안전하게 체험시킨다. 실 브랜드·실 URL 없이 무해화하고, 입력해도 아무 데도 전송되지 않음을 구조적으로 보장한다. |
| User Goal | 링크를 눌러 정보를 입력하려다 "이게 사기였구나"를 자각한다. |
| Entry | UX-022에서 스미싱 링크(displayText) 탭 → `fakeLandingId`가 가리키는 이 목업. |
| Exit | 닫기 또는 (가짜)입력 완료 → UX-022 채팅으로 복귀(시나리오 계속). |
| Primary Actions | (가짜)입력 필드에 입력·"확인"(제출해도 서버 미전송), 닫기/뒤로. |
| Secondary Actions | "훈련 종료"(상시, AC-006). |
| States | Loading: 목업 로드 / Empty: N/A / Error: 목업 로드 실패 → 조용히 생략하고 채팅 복귀(핵심 루프 비차단) / Success: 가짜 랜딩 표시 → 입력 시 "입력되었습니다"류 **가짜 피드백**(실제 저장·전송 없음) 후 복귀. |
| Validation | 입력 형식 검증하지 않음(교육 목적 — 무엇을 넣든 가짜 피드백). 실제 검증·저장 없음. |
| Failure | 로드 실패 → 채팅으로 조용히 복귀(비차단). |
| Accessibility | 화면에 **"AI 훈련용 모의 화면 · 실제 로그인/전송이 아닙니다"** 표식을 색 아닌 텍스트로 상시 노출(AC-022/045). 입력 필드·버튼 키보드 접근·라벨. 종료 후 자각을 돕는 안내는 리포트/해설 단계에서(D-6 정합, 여기선 몰입 유지). 큰 글씨. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High(확장 P0·안전 핵심) |
| Business Rules | **입력값은 서버로 전송되지 않는다 — 이 화면을 위한 콜러블이 존재하지 않는다**(§13.4, AC-045). UI상 "입력함" 가짜 피드백만. **외부 네비게이션 절대 없음**(실 URL 필드 부재). 실 브랜드·기관명·실 URL 미사용(무해화, AC-032/033). "AI 훈련용 모의" 표식 노출(AC-022). |
| Data Required | `fakeLandingId`(인앱 정적 목업 식별자, 콘텐츠 T24/T29), 목업 화면 정의(가짜 필드 구성). |
| Data Operations | 없음(Read 정적 목업만; Create/전송 없음 — 구조적 금지) |
| External Dependencies | 정적 인앱 목업 에셋(T24/T29). 네트워크·콜러블 의존 없음(입력 미전송 보장). |
| Permissions | None |
| Navigation Targets | UX-022(복귀), UX-007("훈련 종료" 시) |
| Events Emitted | `fake_landing_shown`, `fake_landing_submitted`(가짜 제출 — 값 미포함, 이벤트만) |
| Expected Outputs | 가짜 입력 체험 완료 → 채팅 복귀. (리포트에 "가짜 랜딩에 정보 입력 시도" 취약 시점으로 기록될 수 있음, AC-037.) |
| Assumptions | 목업은 정적 인앱 화면. 입력 미전송은 "콜러블 부재"로 구조적으로 보장(임의 서버 저장 금지). |

### 조건부 목소리 선택 서브플로우 (Screen ID: UX-025)
> **v1.7 신설(2026-07-23, D-25, T25, AC-046)**: 에스컬레이션 가능 메신저 시나리오를 고른 사용자에게 "통화로 이어질 수 있어 목소리가 필요할 수 있습니다"를 안내하고 통화용 voiceId를 **미리** 정하는 화면. **에스컬레이션 발생 순간이 아니라 시나리오 선택 직후에 배치**해 전이를 이음새 없이 만든다(D-25). Architecture §13.6과 정합.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-007 (UF-006 메신저 흐름에서 진입) |
| Acceptance Criteria | AC-046(조건부 clone·목소리 선택), AC-034/036(전이 전제) |
| Purpose | 메신저→보이스 전이가 가능한 시나리오에서 통화에 쓸 목소리를 세 경로(①즉시 녹음 ②기존 목소리 재사용 ③남/여 기본 보이스)로 미리 정해, 나중 전이 시 녹음으로 몰입이 끊기지 않게 한다(AC-046/§13.6). |
| User Goal | 통화로 이어질 때 어떤 목소리로 들을지 미리 정한다(또는 최소 폴백을 고른다). |
| Entry | UX-024에서 에스컬레이션 가능 시나리오(scenario.escalation 존재) 선택 후. |
| Exit | voiceId 확보 → `createSession`(channel="messenger", voiceId·voiceSelectionSource) 후 UX-022(채팅). |
| Primary Actions | 세 경로 중 선택 — **①즉시 녹음**(→ 기존 UX-002 녹음 → UX-003 클론 대기 재사용 후 이 지점 복귀) / **②기존 보관 목소리 재사용**(보관 목소리 있을 때만 활성, 선택) / **③남/여 기본 보이스 선택**(FALLBACK_VOICE_MALE/FEMALE, 최종 폴백). |
| Secondary Actions | 뒤로(UX-024로), 안내 자세히 보기. |
| States | Loading: 보관 목소리 조회·세션 생성 / Empty: 보관 목소리 없음 → ②를 비활성/숨김하고 ①·③만 노출(§13.6 "MVP는 ①+③로 성립") / Error: 녹음/세션 생성 실패 재시도(P-4) / Success: 경로 선택·voiceId 확보 후 UX-022로 진입. |
| Validation | 경로 미선택 시 진행 비활성(P-5). ③ 선택 시 남/여 중 하나 필수. |
| Failure | 녹음 실패 → UX-002 Failure(P-6) 재사용. 보관 목소리 로드 실패 → ①·③로 폴백. |
| Accessibility | 세 경로를 큰 카드/버튼·색 아닌 텍스트 라벨로 구분("지금 30초 녹음" / "저장된 내 목소리 사용" / "기본 남성·여성 목소리"). 안내 문구 큰 글씨·단순. 키보드 도달·선택. 남/여 기본 보이스는 "본인 목소리 아님"을 명시(과대표기 금지, D-13 정합). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High(확장 P0·전이 전제) |
| Business Rules | 세 경로 결과를 세션에 기록: `voiceId` + `voiceSelectionSource`(recorded|reused|fallback_male|fallback_female, §13.6). ②는 `users/{uid}/voices/{voiceId}`(유지형 보관함, ADR-0005) 항목이 있을 때만 활성 — 전체 보관함 UI(P-8)는 범위 밖. ③ 남/여 기본 보이스는 `FALLBACK_VOICE_MALE_ID`/`FALLBACK_VOICE_FEMALE_ID` 상수(clone 아님 표기 정합, AC-046). **본인 목소리만 등록** 원칙(AC-020)은 ①즉시 녹음에도 그대로 적용(UX-002 재사용). 안내·선택은 시나리오 선택 직후 1회(D-25) — 전이 순간에 녹음을 삽입하지 않는다. |
| Data Required | scenario.escalation(voiceMode·voiceScenarioId), 보관 목소리 목록(있으면), 선택된 voiceId·voiceSelectionSource. |
| Data Operations | Read(보관 목소리), Create(즉시 녹음 시 클론 파이프라인 재사용·세션 생성) |
| External Dependencies | 기존 클론 온보딩(UX-002/003·VoiceProvider). Firestore(users/{uid}/voices 재사용 소스, 세션). 남/여 프리셋 상수(설정). |
| Permissions | ①즉시 녹음 시 마이크(getUserMedia, UX-002 경유) |
| Navigation Targets | UX-002(즉시 녹음), UX-022(확보 후 채팅), UX-024(뒤로) |
| Events Emitted | `voice_selection_made`(source 포함) |
| Expected Outputs | 확정된 voiceId·voiceSelectionSource → 세션에 기록(전이 시 UX-014 오프닝 합성에 사용). |
| Assumptions | 보관함(②)·기간제 보존은 architect(T35·ADR-0005). 남/여 프리셋 id는 설정 상수. 전이 시 통화 자격증명은 기존 `createRealtimeCall`이 session.voiceId로 발급(§13.6, 엔드포인트 신설 없음). |

### 체험 선택 — 본인이 체험 / 지인에게 보내기 (Screen ID: UX-026)
> **v1.10 갱신(2026-07-24, D-31·D-32, T54, AC-056·위치 상향)**: 이 화면이 **유형 선택(UX-015) 직후로 상향**된다 — 목소리 방식 선택(UX-016)·시나리오 선택(UX-017/UX-024)보다 **먼저** 온다(AC-056). 즉 UX-026은 이제 "이 시나리오로 무엇을 할지"가 아니라 "**이 유형을 내가 체험할지, 지인에게 보낼지**"를 시나리오 확정 전에 묻는다. **신규 ID를 만들지 않고 위치만 상향**한다(ID 불변 규약 — 화면의 의미="본인이 체험/지인에게 보내기 이분 선택"은 그대로, 흐름상 위치만 앞당김). **두 채널(보이스·메신저) 모두에서 항상 노출**된다 — 채널 레벨에서 self·send가 늘 둘 다 유효하기 때문(보이스 self=generic 통화 / send=clone·generic 챌린지; 메신저 self=전체 시나리오 / send=비에스컬레이션 챌린지). 여기서 고른 모드(self/send)가 하류의 UX-016 노출 여부·UX-017/UX-024 시나리오 필터·최종 Exit를 좌우한다(architect 인계: 모드 클라 상태 배선). v1.9의 "시나리오 확정 직후·조건부 노출"은 이 노트로 대체(위치 상향·상시 노출).
> **v1.9 신설(2026-07-24, D-30, T48 — D-27 반전)**: 드릴다운에서 시나리오를 확정한 직후, "**본인이 체험** / **지인에게 보내기(챌린지)**"를 묻는 **경량 통합 선택 화면**. T46의 D-27이 "지인에게 보내기"를 **별도 진입점**으로 뒀던 것을 사용자 지시로 반전해, 두 경로를 한 지점으로 합쳤다. **인라인 카드 2-CTA가 아니라 별도 화면**을 택한 이유: 모든 시나리오 카드에 CTA 2개를 얹으면 카드마다 결정 부하가 늘어(D-27이 지적) 저인지부하 원칙(D-1)에 반한다 — 이분 선택은 단 한 번의 전용 화면으로 분리한다. *(v1.10 D-31: 위치가 시나리오 선택 직후 → 유형 선택 직후로 상향됨.)*
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-003(드릴다운 유형 선택 직후 모드 선택 단계) → 분기: "본인이 체험"=UF-002(보이스 generic)/UF-006(메신저), "지인에게 보내기"=UF-004 |
| Acceptance Criteria | **AC-056(체험/발송 선택 상향 — 유형 선택 직후·방식/시나리오보다 먼저)**, AC-057(보이스 self=generic 전용→UX-016 스킵), AC-044(clone 보이스 챌린지 분기)·AC-058(generic 보이스 챌린지 분기)·AC-051(메신저 챌린지 분기), AC-029(드릴다운 "한 번에 하나씩" 연속성 계승). "본인이 체험" 측은 기존 자기훈련 AC로 이어짐. |
| Purpose | 유형을 고른 사용자에게 — 목소리 방식·시나리오를 정하기 **전에** — "이 유형을 **내가 직접 체험**할지, **지인에게 보내(챌린지)**볼지"를 단 한 번의 이분 선택으로 묻는다(AC-056). 그 결과가 이후 노출되는 시나리오 집합과 (보이스의) 목소리 방식 노출 여부(AC-057)를 결정한다. |
| User Goal | 고른 유형(전화/메신저)을 내가 해볼지, 지인에게 보낼지 먼저 정한다. |
| Entry | **(v1.10)** UX-015에서 유형(보이스 | 메신저) 선택 직후 — 보이스·메신저 두 채널 공통으로 **항상** 진입한다. |
| Exit | **(v1.10, D-31·D-32) 4분기**: **① 보이스 + 본인이 체험** → UX-016을 **건너뛰고**(generic 강제, AC-057) UX-017(generic 시나리오)→createSession→UX-014. **② 보이스 + 지인에게 보내기** → UX-016(clone/generic 방식)→UX-017(시나리오)→UX-019(clone은 UX-002/003 경유, generic은 클론 스킵). **③ 메신저 + 본인이 체험** → UX-024(전체 시나리오, 에스컬레이션 포함)→(비에스컬레이션 createSession→UX-022 / 에스컬레이션 UX-025→UX-022). **④ 메신저 + 지인에게 보내기** → UX-024(비에스컬레이션만)→UX-019(클론 스킵, AC-051). |
| Primary Actions | 큰 버튼 2개 — **"본인이 체험"**, **"지인에게 보내기"**. |
| Secondary Actions | 뒤로(직전 유형 선택 UX-015로 복귀, P-12 — 앞선 선택 유지). 각 옵션 한 줄 설명("직접 훈련해봅니다" / "지인에게 링크로 보내 테스트합니다"). |
| States | Loading: 없음(정적 2버튼); 탭 후 다음 화면 준비 중 스피너 / Empty: N/A(항상 2옵션·양 채널 공통) / Error: 다음 단계 로드 실패 → 재시도(P-4) / Success: 선택 후 위 Exit 경로로 이동. |
| Validation | 옵션 미선택 시 진행 없음(버튼 탭 즉시 진입 방식이면 해당 없음, P-5). |
| Failure | 다음 단계 진입 실패 → 재시도(P-4). 이 화면 자체는 세션·챌린지를 만들지 않으므로 실패 표면은 하류 화면(UX-016/UX-017/UX-024/…)에서 처리. |
| Accessibility | 두 버튼 큰 글씨·큰 터치 타깃(≥48px). 옵션 구분을 색 아닌 라벨+아이콘(사람/보내기)으로 이중 표기. 상단에 유형 맥락(예: "보이스피싱")을 스크린리더가 현재 위치로 읽음(P-12 브레드크럼 계승). 키보드로 두 버튼 도달·Enter 선택. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High(확장 P0 — 유형 선택 직후 모드 분기점이자 챌린지 생성의 유일 진입 경로) |
| Business Rules | **(v1.10, D-31) 노출 조건**: 유형 선택(UX-015) 직후 **보이스·메신저 두 채널 모두에서 항상** 노출한다 — 두 채널 모두 self·send가 유효하기 때문(무의미한 1-옵션 아님). **이 화면은 모드(self|send)만 정하고 시나리오 선택·세션 생성·녹음·챌린지 생성을 하지 않는다**(그 부수효과는 하류 UX-016/UX-017/UX-024/UX-002/UX-019 소관). **선택 모드가 하류를 좌우한다**: (보이스 self) UX-016 스킵·UX-017 generic 필터·통화(AC-057), (보이스 send) UX-016 노출·UX-017 방식 필터·UX-019(AC-058), (메신저 self) UX-024 전체 노출·자기훈련, (메신저 send) UX-024 비에스컬레이션 필터·UX-019(AC-051). 모드 플래그를 **클라 상태에 실어 시나리오 선택·클론 온보딩을 거쳐 최종 목적지(통화 vs 챌린지)까지 전달**해야 한다(구현은 architect — 이 배선이 구 UX-024 "챌린지-생성 모드 플래그"를 대체·상류로 이동). 챌린지는 여전히 생성 시점에 시나리오 하나로 확정(OQ-29). |
| Data Required | 상류에서 전달된 유형(voice|messenger)만 — **시나리오·voiceMode는 이 화면 하류에서 정해지므로 아직 없음**(위치 상향의 핵심 차이). 신규 데이터·신규 필드 없음. |
| Data Operations | 없음(선택 상태는 클라 로컬; 세션·챌린지 생성은 하류 콜러블 `createSession`/`createChallenge`가 담당). |
| External Dependencies | 없음(이 화면 자체는 백엔드 콜러블을 호출하지 않음). |
| Permissions | None(마이크 등 권한은 하류 UX-002 진입 시점에 요청). |
| Navigation Targets | **UX-016(보이스 send — 방식 선택)**, **UX-017(보이스 self — generic 시나리오, UX-016 스킵)**, **UX-024(메신저 — self/send 모드 전달)**, UX-015(뒤로). |
| Events Emitted | `challenge_mode_selected`(값: self_experience | send_to_friend; 유형 channel 포함). |
| Expected Outputs | 선택 모드(self|send) + 유형(voice|messenger) → 하류 라우팅·필터 조건. send는 UF-004(→UX-019)로, self는 UF-002(보이스 generic)/UF-006(메신저)로 이어질 상태를 확정. |
| Assumptions | 상류(UX-015)가 유형을 전달함. 이 화면은 시나리오 확정 **전**이라 시나리오 메타는 아직 없다(하류에서 모드+유형으로 필터). 챌린지 채널 구분·`challenges.voiceId` 옵셔널화는 architect §14.8(T47)에서 확정됐고, **위치 상향은 그 스키마에 영향 없음(entry-point-agnostic)** — `createChallenge`는 `scenario.channel`로 채널을 정하며 진입 경로를 참조하지 않는다. **architect 신규 인계**: 모드(self|send) 클라 상태 배선(UX-016 노출 여부·UX-017/UX-024 필터·최종 라우팅) + generic 보이스 챌린지 스키마 판별·GENERIC_VOICE_ID 통화 라우팅(AC-058). |

### 난이도 선택 — 초급 / 중급 / 고급 (Screen ID: UX-029)
> **v1.11 신설(2026-07-25, D-41~D-43)**: 사용자 요구 — *"초급은 티가 나는 수법, 고급은 실제처럼 정교하게"*. 지금 시나리오 카드의 "난이도: 쉬움~중간"은 시나리오마다 **고정된 설명 텍스트**라 사용자가 고를 수 없다. 이 화면이 난이도를 **사용자가 고르는 축**으로 만든다. **드릴다운의 마지막 단계**(시나리오 선택 직후·세션/챌린지 생성 직전)에 두며, 기존 P-12(드릴다운 단계 내비게이션) 규칙을 그대로 따른다(뒤로·브레드크럼·전환 방향성).
| Item | Value |
|---|---|
| Belongs to Flow(s) | **UF-003**(드릴다운 마지막 단계·주), UF-002·UF-006(자기훈련 세션 생성 직전), UF-004(챌린지 생성 직전 — 발신자가 수신자 강도 선택), UF-010(아카이브에서 재훈련 진입) |
| Acceptance Criteria | **재사용**: AC-002(세션 시작 전 난이도[강도] 라벨 표시 — 이 화면이 그 표시를 고정값에서 선택값으로 확장), AC-029(단계적 드릴다운 — 한 화면에 전체를 평면 나열하지 않음), AC-030(메신저도 동일 표기), AC-012(사전 고지 — 고급의 심리적 부담 고지), AC-050(안전장치는 등급·설정 무관 동일 강제 — 난이도가 안전장치를 게이팅하지 않음). **⚠️ "사용자 선택형 난이도"라는 신규 능력 AC는 PRD 부재 — planner 소관(OQ-U15).** |
| Purpose | 같은 시나리오를 **얼마나 세게 겪을지**를 사용자가 정하게 한다. 초급은 수법이 눈에 띄게 드러나 "이런 게 사기구나"를 배우는 단계, 고급은 실제처럼 정교해 "알고도 넘어가는" 경험을 하는 단계다. 훈련 강도를 사용자가 통제한다는 사실 자체가 어르신 심리적 안전에도 기여한다(PRD Risk). |
| User Goal | 내가 감당할 수 있는(또는 도전하고 싶은) 강도를 골라 훈련을 시작한다. |
| Entry | UX-017(보이스 시나리오 확정 직후) 또는 UX-024(메신저 시나리오 확정 직후). UX-030(실패 아카이브)의 "이 시나리오 다시 훈련"에서도 진입(시나리오 프리셋, 난이도는 다시 고르게 한다). |
| Exit | **(self·보이스)** `createSession` → UX-014. **(self·메신저 비에스컬레이션)** `createSession` → UX-022. **(self·메신저 에스컬레이션 가능)** UX-025(목소리 선택) → UX-022. **(send)** UX-019(챌린지 만들기). "뒤로" → 시나리오 선택(UX-017/UX-024)으로 복귀(P-12). |
| Primary Actions | **초급 / 중급 / 고급** 큰 버튼 3개 중 택1 → "이 난이도로 시작"(또는 버튼 탭 즉시 진행). 각 버튼에 **한 줄 설명 + 화면에서 무엇이 달라지는지**를 함께 표기. |
| Secondary Actions | "뒤로"(시나리오 다시 고르기), **초급 선택 시** "이 대화에서 나올 수 있는 위험 신호 미리 보기"(사전 브리핑 카드 펼치기, D-43). |
| States | **Default(기본)**: 3단계 카드 + 직전에 고른 시나리오 맥락(브레드크럼). **중급이 기본 강조**(현행 체감 강도의 연속선 — 처음 오는 사용자가 무엇을 고를지 몰라 멈추지 않게). **고급 선택 시 고지**: "실제와 가깝게 강한 압박이 이어집니다. 언제든 '훈련 종료'로 멈출 수 있습니다"를 **선택 직후 인라인으로** 노출(다이얼로그 아님 — D-2/P-9 확인 다이얼로그 최소화 원칙 유지). **심리적 부담이 큰 시나리오(협박·납치류)에서 고급 선택 시** 기존 시나리오 설명의 부담 경고를 함께 강조한다. **초급 선택 시 사전 브리핑**: "이 대화에서는 ○○·○○ 같은 신호가 나옵니다"를 시작 전에 보여준다(대화 중 실시간 표시가 아니다 — D-6 유지, D-43). **send 모드**: 문구가 "상대가 겪을 강도"로 바뀐다("고급 — 상대가 강한 압박을 겪게 됩니다"). **Error**: 세션/챌린지 생성 실패 → 인라인 오류 + 재시도(P-4, UX-017에서 계승). |
| Validation | 난이도를 고르지 않으면 진행 버튼 비활성(P-5 — 제출 후 오류 대신 사전 차단). 기본 강조가 있으므로 실질적으로 막히지 않는다. |
| Failure | 세션 생성 실패 → 재시도. 난이도 값이 하류로 전달되지 않은 예외 상황 → **조용히 임의 난이도로 진행하지 않고** 중급으로 폴백하며 그 사실을 1줄 안내(침묵 실패 금지). |
| Accessibility | 3개 버튼 큰 글씨·큰 터치 타깃(≥48px). **난이도 구분을 색 아닌 텍스트 라벨 + 단계 표기**로 이중 제공(색 단독 금지 — "고급"을 빨강으로만 표시하지 않는다). 각 버튼의 한 줄 설명을 스크린리더가 버튼 라벨과 함께 읽도록 연결. 고급 고지 문구는 선택 직후 **aria-live**로 알림. 브레드크럼으로 현재 단계("보이스피싱 › 본인이 체험 › 저금리 대환대출 빙자 › ④ 난이도")를 텍스트로 안내(P-12). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | High |
| Business Rules | **난이도는 어떤 안전장치도 게이팅하지 않는다** — 사전 고지(AC-012)·명시적 동의(AC-017)·상시 종료(AC-006)·합성/모의 표식(AC-022/032)·종료 고지(AC-015/023)·생성물 폐기(AC-021)·2인 4대 안전제약(AC-040/041/042/043)·면책 배너(AC-047)는 **세 난이도에서 완전히 동일**하다(D-42, 메신저 스킨이 프레젠테이션 전용이라 어떤 안전 판정도 게이팅하지 않는다는 §13.4 원칙과 동형, AC-050 정합). 난이도가 바꾸는 것은 **압박 강도·요구 도달 속도·수법의 은밀함**이며, 무해화 경계(실계좌·실링크·실행 정보 미노출, AC-005/013/032/033)는 고급에서도 **완화되지 않는다** — "고급 = 더 진짜 같은 압박"이지 "고급 = 더 진짜에 가까운 위험 정보"가 아니다. **초급의 힌트는 세션 시작 전 브리핑으로만** 제공하고 대화 중 실시간 판정 표시는 하지 않는다(D-6 유지, D-43). 난이도는 리포트·리플레이·실패 아카이브에 **같은 라벨로 표기**되어야 한다(P-22). 2인 챌린지에서는 발신자가 고른 난이도가 챌린지에 실려 수신자 동의 랜딩(UX-021)에 표시된다(동의 정보량 증가). |
| Data Required | 직전 단계에서 확정된 시나리오 id·채널·모드(self|send), 시나리오의 기존 고정 `difficulty` 설명 문자열(기본 성향 표기용), 초급 브리핑에 쓸 "이 시나리오에서 나올 신호" 목록. |
| Data Operations | Read(시나리오 메타·초급 브리핑 콘텐츠). Create(하류에서 `createSession`/`createChallenge` 호출 시 난이도 동반 — 이 화면 자체의 쓰기는 없거나 하류 호출 1건). |
| External Dependencies | 없음(이 화면 자체는 외부 의존 없음). 하류 세션/챌린지 생성 콜러블이 난이도를 받아야 함. |
| Permissions | None |
| Navigation Targets | UX-014(self·보이스), UX-022(self·메신저 비에스컬), UX-025(self·메신저 에스컬 가능), UX-019(send), UX-017/UX-024(뒤로) |
| Events Emitted | `difficulty_selected`(값: beginner | intermediate | advanced; 시나리오·채널·모드 포함), `beginner_briefing_viewed`, `advanced_warning_shown` |
| Expected Outputs | 선택된 난이도 1건이 세션(또는 챌린지) 생성에 함께 전달되고, 세션 셸 배지·리포트·아카이브에 동일 라벨로 표기된다. |
| Assumptions | 모든 시나리오가 3단계를 전부 지원한다(난이도가 시나리오를 감추지 않는다). 기존 고정 `difficulty` 문자열은 삭제되지 않고 "기본 성향"으로 남는다(AC-002 유지). |
| **아키텍처 판단 위임(ux-design 소관 아님)** | ① **난이도의 데이터 모델** — 시나리오별 난이도 3종 프롬프트를 두는가, 시나리오 프롬프트는 하나로 두고 **공통 난이도 모디파이어 블록**을 조립 시 얹는가(후자가 13개 시나리오 × 3 = 39벌 저작을 피함 — 다만 판단은 architect). 세션·챌린지에 난이도를 싣는 필드. ② **기존 고정 `difficulty` 문자열과의 관계** — 유지·병기·라벨 변경 중 무엇인지(표기 정합은 OQ-U21). ③ **초급 브리핑 콘텐츠의 출처** — 시나리오 콘텐츠에 저작하는가, 기존 `weakenedTactics`에서 파생하는가. ④ 난이도가 리포트 판정 기준(속은 시점 판정)에 영향을 주는지(권고: 주지 않음 — 판정은 난이도 무관 동일). **전부 OQ-U18.** |

### (P1·스케치) 사칭 이미지 표시 (Screen ID: UX-009)
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002 |
| Acceptance Criteria | AC-025, AC-022 |
| Purpose | 여유 시, 통화(UX-014) 실시간 역할극 중 가짜 송금완료 화면 등 사칭 이미지 1종을 합성 표식과 함께 노출. |
| User Goal | 시각적 사칭물의 위력을 체감한다. |
| Entry | UX-014 실시간 역할극(live phase) 도중. |
| Exit | UX-014로 복귀. |
| Primary Actions | 이미지 보기/닫기. |
| Secondary Actions | "훈련 종료"(상시). |
| States | Loading: 이미지 로드 / Empty: N/A / Error: 로드 실패 시 생략(핵심 루프 비차단) / Success: 표식 포함 이미지 표시. |
| Validation | N/A |
| Failure | 로드 실패 → 조용히 생략(P1, 데모 비차단). |
| Accessibility | "AI 훈련용 합성" 화면 라벨. 이미지 대체 텍스트 제공. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Low (P1 · 여유 시 추가) |
| Business Rules | 사칭 이미지에도 "AI 훈련용 합성" 표식 필수(AC-022). 실제 송금·계좌 기능 없음(AC-023). |
| Data Required | 사칭 이미지 에셋(정적 또는 생성), 표식 문구. |
| Data Operations | Read(이미지) |
| External Dependencies | 정적 에셋 또는 이미지 생성 파이프라인(T12). Firebase Storage(폐기 대상). |
| Permissions | None |
| Navigation Targets | UX-014 |
| Events Emitted | `spoof_image_shown` |
| Expected Outputs | 표식 포함 사칭 이미지 노출. |
| Assumptions | 여유 시에만 구현. 미구현 시 UF-002 핵심 루프는 영향 없음. |

### (P1·스케치) 방어 등급/게임화 (Screen ID: UX-010)
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-002 |
| Acceptance Criteria | AC-010, AC-011 |
| Purpose | 여유 시, 세션 결과로 방어 점수/등급을 산정해 표시하고 세션 간 지속. |
| User Goal | 내 방어 등급을 확인한다. |
| Entry | UX-008 리포트 이후. |
| Exit | 처음으로. |
| Primary Actions | 등급/점수 확인. |
| Secondary Actions | 다시 훈련. |
| States | Loading: 산정 대기 / Empty: 첫 세션 전 "기록 없음" / Error: 산정 실패 시 생략 / Success: 등급·세션 횟수 표시. |
| Validation | N/A |
| Failure | 산정 실패 → 리포트만 표시하고 생략(비차단). |
| Accessibility | 등급을 색 아닌 텍스트/라벨로 표기. 큰 글씨. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Low (P1 · Day2 여유 시, 신규개발 금지 원칙과 긴장 — 컷 가능) |
| Business Rules | 산정식·레벨 수는 OQ-5 미확정(PRD). 등급/세션 횟수는 세션 간 지속(AC-011). |
| Data Required | 세션 결과, 누적 등급/레벨, 세션 횟수. |
| Data Operations | Create/Update(등급·카운트), Read |
| External Dependencies | Firestore(프로필/등급 지속). |
| Permissions | None |
| Navigation Targets | UX-015 |
| Events Emitted | `grade_calculated` |
| Expected Outputs | 표시된 방어 등급. |
| Assumptions | 지속을 위한 계정/식별자는 UX-013 로그인으로 확보됨(OQ-U2 resolved — P0에 로그인 도입). 산정식은 OQ-5 확정 후 구현. |

### (P1·스케치) 연령 확인(age-gate) (Screen ID: UX-011)
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-001 |
| Acceptance Criteria | AC-014 |
| Purpose | 여유 시, 최소 연령 미만 접근 제한. |
| User Goal | 연령 확인을 통과한다. |
| Entry | UX-001 동의 직후(코드 구현은 "직후"를 택함). |
| Exit | 통과 → UX-015(훈련 유형 선택 — 드릴다운 시작, v1.5 구 UX-004 대체). *(v1.4: Phase B 정합화 — 이전 "→ UX-002"는 구(舊)순서. 코드 `age-gate`가 통과 시 `/scenarios`로 이동, D-14.)* |
| Primary Actions | 연령 확인 입력/확인. |
| Secondary Actions | N/A |
| States | Loading: N/A / Empty: N/A / Error: 기준 미달 시 차단 안내 / Success: 통과. |
| Validation | 최소 연령값 OQ-8 미확정(PRD). |
| Failure | 기준 미달 → 접근 제한 안내. |
| Accessibility | 큰 글씨·단순 입력. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Low (P1 · 여유 시) |
| Business Rules | 정의된 최소 연령 미만 접근 제한(AC-014). 기준 연령 = 만 14세(OQ-8 resolved, PRD v0.7). |
| Data Required | 연령/생년 입력 또는 확인 플래그. |
| Data Operations | Create(확인 기록) |
| External Dependencies | 없음(클라이언트 확인) 또는 Firestore. |
| Permissions | None |
| Navigation Targets | UX-015 |
| Events Emitted | `age_verified` |
| Expected Outputs | 연령 확인 통과 상태. |
| Assumptions | 연령값은 OQ-8 확정(만 14세). 확인은 계정 단위 플래그로 1회만 수행하고 재진입 시 재질문하지 않는다(코드 `age-gate` 가드). |

### (P1·스케치) 계정·세션/리포트 히스토리 (Screen ID: UX-012)
> **v1.11 갱신(2026-07-25, D-44)**: "내가 속았던 순간 모아보기" → **UX-030(실패 아카이브)** 진입점을 추가한다(Navigation Targets 추가). **이 화면의 성격은 바뀌지 않는다** — UX-012는 여전히 **세션 단위** 시간순 목록이고, UX-030은 **속은 순간 단위** 목록이다. 두 화면을 합치지 않은 근거는 D-44 참조.
| Item | Value |
|---|---|
| Belongs to Flow(s) | UF-001 |
| Acceptance Criteria | AC-011, AC-016 |
| Purpose | 여유 시, 계정 접근 + 과거 세션·리포트의 시간순 히스토리 열람. **본인 열람만**(자녀 열람은 OQ-15 open, 미설계). |
| User Goal | 내 과거 훈련 기록을 본다. |
| Entry | 홈/리포트 이후. |
| Exit | 특정 리포트 열람 → UX-008. |
| Primary Actions | 세션/리포트 목록 열람, 항목 선택. |
| Secondary Actions | 다시 훈련. |
| States | Loading: 목록 로드 / Empty: 기록 없음 안내 / Error: 로드 실패 재시도 / Success: 시간순 목록. |
| Validation | N/A |
| Failure | 로드 실패 → 재시도. |
| Accessibility | 목록 항목 큰 터치 타깃·텍스트 라벨. |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Low (P1 · 여유 시) |
| Business Rules | 본인 계정 소유 데이터만 열람(OQ-11 확정: 소유권=본인). **자녀의 부모 리포트 열람은 미설계**(OQ-15 open). 시간순 히스토리 제공(AC-016). |
| Data Required | 본인 세션/리포트 목록(메타·타임스탬프). |
| Data Operations | Read(히스토리) |
| External Dependencies | Firestore(세션/리포트 저장). Firebase Auth(계정). |
| Permissions | None |
| Navigation Targets | UX-008 |
| Events Emitted | `history_viewed` |
| Expected Outputs | 시간순 세션/리포트 목록. |
| Assumptions | 로그인/계정은 UX-013에서 확보됨(OQ-U2 resolved — P0에 로그인 도입). 생성물 음성은 폐기되므로 히스토리에는 리포트·메타만 남고 음성은 없음(AC-021). |

### 실패 아카이브 — 내가 속았던 순간 모아보기 (Screen ID: UX-030)
> **v1.11 신설(2026-07-25, D-44·D-45)**: 사용자 요구 — *"내가 당했던 순간만 모아 보는 화면(반복 학습)"*. **UX-012(세션/리포트 히스토리)와 단위가 다르다** — UX-012는 "언제 어떤 훈련을 했나"(세션 1건 = 항목 1건), UX-030은 "내가 어디서 넘어갔나"(**속은 순간 1건 = 항목 1건**, 한 세션에서 3번 속았으면 항목 3개). 목록의 정렬·묶음·재훈련 동선 전부가 "같은 수법에 반복해 넘어가는 패턴을 스스로 보게 한다"는 목적에 맞춰져 있어 히스토리의 확장으로는 성립하지 않는다(D-44).
| Item | Value |
|---|---|
| Belongs to Flow(s) | **UF-010**(주), UF-002·UF-006(세션 종료 후 진입), UF-009(되감기에서 나가는 목적지) |
| Acceptance Criteria | **재사용**: AC-026(속은 시점·수법·올바른 대처 — 항목의 3요소), AC-008/AC-009(리포트 분석 재사용·안 속았을 때 명시), AC-016(본인 계정 데이터 시간순 열람), AC-011(반복 학습 취지, P1), AC-021(음성·합성물은 폐기되어 텍스트만 남음), AC-005/AC-013(실행 정보 미노출), AC-043/AC-055(2인 결과 열람 제한 — 사용자2 데이터 미포함). **⚠️ "속은 순간 단위 아카이브"라는 신규 능력 AC는 PRD 부재 — planner 소관(OQ-U15).** |
| Purpose | 흩어져 있던 실패 순간들을 한자리에 모아, 사용자가 **자기 취약 패턴**("나는 긴급성 압박에 계속 넘어간다")을 스스로 인지하고 곧바로 되감기·재훈련으로 이어가게 한다. |
| User Goal | 내가 어디서 반복해 넘어가는지 보고, 그 순간을 다시 연습한다. |
| Entry | UX-008(리포트)의 "내가 속았던 순간 모아보기", UX-012(P1 히스토리)의 동일 진입점, UX-028(되감기)에서 나가기. |
| Exit | 항목 → **UX-018**("그때 대화 보기", 해당 시점) / **UX-028**("그 순간 다시 해보기") / **UX-029**("이 시나리오 다시 훈련" → 난이도 선택 → 새 세션). "처음으로". |
| Primary Actions | 속은 순간 카드 목록 열람, 카드 펼쳐 **올바른 대처(`correctAction`) 보기**, 카드별 3개 동선(그때 대화 보기 / 그 순간 다시 해보기 / 이 시나리오 다시 훈련), **"수법별로 묶기" 토글**. |
| Secondary Actions | "더 보기"(추가 로드), 정렬 전환(최신순 ↔ 수법별). **검색·기간 필터·통계 대시보드는 제공하지 않는다**(요청되지 않은 기능 = 스코프 크립, D-45). |
| States | **Loading**: 목록 로드 스피너. **Empty(A) — 훈련 이력 자체가 없음**: "아직 훈련 기록이 없습니다" + "훈련 시작하기"(→UX-015). **Empty(B) — 훈련은 했지만 한 번도 속지 않음**: "지금까지 넘어간 순간이 없습니다"를 명시하되 **과신 표현("면역됐습니다") 금지**, "계속 훈련해 보세요 · 난이도를 올려볼 수 있어요"(→UX-015)로 안내(AC-009 정합, P-8). 두 Empty를 **구분해서** 보여준다(같은 빈 화면으로 뭉뜽그리지 않는다). **Success(기본)**: 속은 순간 카드 최신순. **Grouped**: "수법별로 묶기" 켬 → 같은 `tactic`끼리 묶이고 그룹 헤더에 **"이 수법에 N번 넘어갔습니다"**(반복 인지가 이 화면의 핵심 가치). **Error**: 로드 실패 → 재시도(P-4). |
| Validation | N/A(열람 화면). |
| Failure | 목록 로드 실패 → 재시도. 특정 항목의 원 transcript 부재 → **그 항목의 "그때 대화 보기"만 비활성 + 사유 1줄**(침묵 실패 금지), 나머지 정보·동선은 그대로. |
| Accessibility | 카드는 큰 터치 타깃·큰 글씨. **수법·심각도 구분을 색 아닌 텍스트 라벨 + 아이콘**으로 이중 표기(색 단독 금지). 그룹 헤더의 반복 횟수를 스크린리더가 읽도록 텍스트로 명시("긴급성 압박 — 3건"). 카드 항목은 목록 시맨틱(list/listitem)으로 제공해 스크린리더가 개수·순서를 안내. 각 카드의 3개 동선 버튼은 카드 제목과 연결된 접근 이름을 갖는다("2026년 7월 20일 저금리 대환대출 — 그 순간 다시 해보기"). 톤은 "개선 영역" 완화 프레임(P-8) — **질책 표현 금지**("또 속았습니다" 류 금지). |

**Architect Handoff**
| Item | Value |
|---|---|
| Priority | Medium(반복 학습 가치는 높으나 핵심 루프는 이 화면 없이도 동작 — 되감기(UX-028)가 세션 직후 경로를 이미 커버) |
| Business Rules | **본인 계정 소유 데이터만** 열람한다(OQ-11 소유권=본인). **2인 챌린지의 사용자2(익명) 데이터는 포함하지 않는다** — 계정이 없어 누적이 성립하지 않고, 발신자에게 수신자 실패 이력이 누적 노출되는 경로를 만들지 않는다(AC-043/AC-055 결과 열람 제한 정합, 이 원칙은 **협상 대상이 아니다**). 항목은 리포트의 `deceivedMoments`에서 파생하며 **신규 분석 파이프라인을 도입하지 않는다**(기존 분석 재사용 — UX-018과 동일 원칙). 음성·합성물은 세션 종료 시 폐기되므로(AC-021) **오디오 재생 경로가 존재하지 않는다** — 텍스트 정보만. 실제 사기 실행 정보 노출 금지(AC-005/AC-013). 과신·질책 표현 금지(P-8). 정렬은 최신순 기본 + 수법별 묶기 **2종만**(검색·필터·통계 미설계, D-45). |
| Data Required | 본인 리포트들의 `deceivedMoments`(turnIndex·timeLabel·tactic·correctAction) + 각 항목의 소속 세션 메타(세션 id·리포트 id·시나리오 제목·채널·난이도·생성 시각). |
| Data Operations | Read(리포트 목록·deceivedMoments). **쓰기 없음.** |
| External Dependencies | Firestore(reports — 기존 컬렉션). Firebase Auth(본인 식별). **신규 외부 의존성 없음.** |
| Permissions | 본인 uid 소유 리포트만 읽기(기존 규칙 계승). |
| Navigation Targets | UX-018(그때 대화 보기), UX-028(그 순간 다시 해보기), UX-029(이 시나리오 다시 훈련), UX-008(원 리포트), 처음으로 |
| Events Emitted | `archive_viewed`, `archive_grouped_by_tactic`, `archive_item_replay_opened`, `archive_item_rewind_opened`, `archive_retrain_requested` |
| Expected Outputs | 속은 순간 단위 목록(최신순 / 수법별 그룹) + 각 항목에서의 재훈련 동선. |
| Assumptions | 리포트가 이미 생성돼 있고 `deceivedMoments`가 그 안에 있다. 사용자는 로그인 상태다(UX-013). 재훈련은 새 세션이며 과거 세션을 재개하지 않는다. |
| **아키텍처 판단 위임(ux-design 소관 아님)** | ① **쿼리 방식** — 본인 `reports`를 전수 조회해 클라에서 `deceivedMoments`를 평탄화하는가, **순간 단위 역정규화 인덱스**(예: 별도 컬렉션/서브컬렉션)를 두는가. 항목 수가 세션 수보다 크게 늘어나는 구조라 정렬·페이지네이션 비용이 UX-012와 다르다. ② **"수법별 묶기"의 그룹 키** — `tactic`이 자유 문자열이면 같은 수법이 표기 차이로 갈라진다(정규화·매핑 필요 여부). ③ 페이지 크기·"더 보기" 방식. ④ 리포트 삭제·보존 정책이 생길 경우 아카이브 항목의 수명. **전부 OQ-U19.** |

## Deprecated
삭제하지 않고 사유와 이관 대상을 남긴다(설계 이력). 상세 스펙은 Screen Catalog의 해당 화면 블록에 DEPRECATED 배너와 함께 그대로 보존되어 있다.

| Screen/Flow ID | 이름 | Deprecated 시점 | 사유 | 이관 대상 |
|---|---|---|---|---|
| UX-005 | 딥보이스 재생 | 2026-07-22 (v1.4, D-11) | 별개 재생 화면 + 이어지는 "계속(역할극 시작)" 버튼이 통화 몰입을 깨뜨린다는 사용자 피드백. "받았다가 채팅창으로 넘어가는" 단절의 원인. | **UX-014**의 opening-playback phase. AC-019/022/006 이관. |
| UX-006 | 사기범 역할극 채팅 | 2026-07-22 (v1.4, D-11) | 위와 동일 — 딥보이스 재생과 별개 라우트(`/session/chat`)로 분리돼 하나의 통화로 느껴지지 않음. | **UX-014**의 live phase. AC-003/004/005/006/007/013/024 이관. D-10(STT/TTS In Scope)의 음성 결정은 반전 아님·계승. |
| UX-004 | 시나리오 선택(평면 목록) | 2026-07-23 (v1.5, D-16) | 모든 시나리오를 한 화면에 평면 나열해 PRD v0.9 AC-029(평면 목록 동시 노출 금지)·AC-028(2분류+clone/generic 드릴다운)에 부합하지 않음. "선택하면 시나리오가 드러나는" 몰입 흐름 요구(사용자 2026-07-23). | **UX-015(유형)→UX-016(방식)→UX-017(시나리오)** 드릴다운 3화면. AC-001/002는 UX-017로, clone→UX-002/generic→UX-014 분기도 UX-017로 이관. |

> 참고: UX-005/UX-006은 PRD 변경이 아니라 **UX 품질 재설계**(동일 PRD v0.8)로 통합됐다. 두 화면의 AC·상호작용은 모두 UX-014에 살아 있으므로 커버리지 손실은 없다(Consistency Check 참조).
> 참고: UX-004는 PRD v0.9(AC-028/029)에 따른 **드릴다운 재설계**로 대체됐다. 그 AC(AC-001/002)와 clone/generic 분기는 모두 UX-017에 계승되어 커버리지 손실이 없다.

## Information Architecture
선형(linear) 단일 여정 구조. 어르신 저인지부하를 위해 분기·메뉴를 최소화한다.

> **v1.11 갱신(2026-07-25, D-35·D-39·D-41·D-44)**: 네 지점이 추가된다(기존 노드·순서는 무변경). **(1) 드릴다운 끝에 난이도 선택(UX-029)** — 시나리오(UX-017/UX-024) 다음, 세션/챌린지 생성 **직전**. 4분기 전부가 이 단계를 통과한다. **(2) 통화 셸 위에 문자 오버레이(UX-027)** — 통화(UX-014)에서 **아래로 내려가는 새 경로가 아니라 위에 겹치는 계층**이다(라우트 이동 없음, D-35). 오버레이 안의 링크는 기존 UX-023으로 이어지고 닫으면 통화로 복귀한다. **(3) 종료 이후 학습 분기에 즉시 되감기(UX-028)** — UX-007/UX-008/UX-018/UX-030에서 진입하는 선택 분기이며, `deceivedMoments`가 있을 때만 나타난다. **(4) 실패 아카이브(UX-030)** — UX-008·UX-012에서 진입하는 누적 열람 화면이고, 거기서 UX-018/UX-028/UX-029로 재훈련 동선이 되돌아 나간다(선형 여정에 **처음으로 생기는 순환 고리** — 반복 학습이 목적이므로 의도된 것이며, 정식 내비게이션 메뉴를 만들지 않는 원칙은 유지한다).
> **v1.10 갱신(2026-07-24, D-31·D-32·D-33 — AC-056/057/058)**: **체험/발송 선택 UX-026을 유형 선택(UX-015) 직후로 상향**한다 — 목소리 방식(UX-016)·시나리오 선택(UX-017/UX-024)보다 **먼저**. UX-026은 이제 보이스·메신저 **두 채널 모두에서 항상** 등장하고, 여기서 고른 모드(self/send)가 하류를 4분기시킨다: (보이스 self)=UX-016 스킵·generic 통화, (보이스 send)=UX-016(clone/generic)·챌린지(**generic 보이스 챌린지 신설 AC-058**), (메신저 self)=전체 시나리오 자기훈련, (메신저 send)=비에스컬레이션 챌린지. **보이스 clone 자기 체험은 배제**되고 clone은 "지인에게 보내기"로만 도달(AC-057). v1.9의 "시나리오 확정 직후 UX-026" 배치는 이 노트로 상향된다.
> **v1.9 갱신(2026-07-24, D-30 — D-27 반전)**: 시나리오 확정 직후 **통합 체험 선택 UX-026("본인이 체험 / 지인에게 보내기")**을 신설한다. 자기훈련과 챌린지 생성이 **별도 진입점에서 시작하던 두 경로 → 시나리오 선택까지 하나로 수렴한 뒤 UX-026에서 분기**하는 구조로 재작도. 발신자(사용자1)가 별도 "챌린지 만들기" 메뉴로 시작하던 경로는 **은퇴**하고, 드릴다운→UX-026 "지인에게 보내기"가 챌린지 생성의 유일 경로다. *(v1.10 D-31: UX-026 위치가 시나리오 선택 직후 → 유형 선택 직후로 상향, 두 채널 상시 노출.)*
> **v1.5 갱신(2026-07-23, D-16)**: 단일 "시나리오 선택(구 UX-004)"을 **드릴다운 3화면(UX-015 유형 → UX-016 보이스 방식 → UX-017 시나리오 노출)**으로 대체(UF-003 신설). 메신저피싱 분기는 T24로 이어지는 **스텁**으로만 표시(이번 범위 밖). 이전 다이어그램의 "시나리오 선택 (UX-004)" 노드는 아래로 대체됐다.
> **v1.4 갱신(2026-07-22, D-11·D-14)**: (1) 딥보이스 재생(구 UX-005)+역할극(구 UX-006)을 **단일 연속 통화 UX-014**로 통합 — 별개 화면 전환과 그 사이 "계속" 버튼을 제거해 "하나의 통화"로 만든다. (2) Phase B 순서(시나리오 선택이 녹음보다 먼저; generic은 녹음/클론 생략)를 유지하되 통합 통화를 반영.

```
로그인/가입 (UX-013)   ── P0 진입점(유효 세션 시 건너뜀)
  └─ 사전고지+동의 (UX-001)
  └─ [P1] age-gate (UX-011)
  └─ 훈련 유형 선택 (UX-015)          ── UF-001 종료 / UF-003(드릴다운) 시작
       └─ 체험/발송 선택 (UX-026)     ── v1.10 D-31 · 유형 선택 직후 · 보이스/메신저 공통 · 상시 노출 (AC-056)
            ├─ "본인이 체험"
            │    ├─ (A) 보이스 ─(UX-016 스킵·generic 강제, AC-057)─ 시나리오 노출 (UX-017, generic)
            │    │                                    └─▶ 난이도 선택 (UX-029) ─▶ createSession ─▶ 통화 UX-014
            │    └─ (B) 메신저 ─ 메신저 시나리오 선택 (UX-024, 전체·에스컬레이션 포함, UF-006)
            │             └─▶ 난이도 선택 (UX-029)   ── v1.11 D-41 · 시나리오 직후 · 세션/챌린지 생성 직전
            │                   ├─ (비에스컬)  createSession(channel=messenger) ─────────▶ 메신저 채팅 UX-022
            │                   └─ (에스컬 가능) 조건부 목소리 선택 (UX-025, UF-007) ─────▶ 메신저 채팅 UX-022
            │
            └─ "지인에게 보내기" (챌린지, UF-004)
                 ├─ (A) 보이스 ─ 목소리 방식 선택 (UX-016, clone/generic)
                 │        ├─ clone   ─ 시나리오 (UX-017) ─▶ 난이도 (UX-029) ─ 녹음 UX-002 → 클론 UX-003 ──▶ 챌린지 만들기 UX-019 (딥보이스 카피)
                 │        └─ generic ─ 시나리오 (UX-017) ─▶ 난이도 (UX-029) ─(클론 스킵, AC-058)─────────▶ 챌린지 만들기 UX-019 (기본 AI 음성 카피)
                 └─ (B) 메신저 ─ 메신저 시나리오 선택 (UX-024, 비에스컬레이션만) ─▶ 난이도 (UX-029) ─(클론 스킵, AC-051)─▶ UX-019 (메시지 카피 · voiceId 없음)
        (clone 보이스 자기 체험은 배제 — clone은 "지인에게 보내기"로만 도달, AC-057. 별도 "챌린지 만들기" 진입점 은퇴, D-30/D-31)
        (난이도는 어떤 시나리오도 감추지 않고 어떤 안전장치도 게이팅하지 않는다 — D-42. send 모드에서는 "상대가 겪을 강도")

  [메신저 채팅 셸 (UX-022)] ── 카카오/문자(iOS·삼성·기본) 스킨
       │  "AI 훈련용 모의" 표식·상시 훈련 종료·카카오 면책 배너 고정·난이도 배지(v1.11)
       │   ├─ 스미싱 링크 탭 → 인앱 가짜 랜딩 (UX-023) → 복귀
       │   └─ 에스컬레이션 트리거(신호/폴백/명시버튼) → 전이 연출(P-18) ─▷ 통화 셸 UX-014(UF-007)
       └─ 세션 종료·고지 (UX-007) → 리포트(UX-008) → 리플레이 해설(UX-018)
       (※ 통화 중 문자 오버레이 UX-027은 여기서 발생하지 않음 — 이미 메시지 표면 안이라 불필요)

  [통화 세션 (UX-014)] ─── 단일 연속 통화
       │  수신 → 오프닝 딥보이스(clone=본인목소리 / generic=기본합성) → 실시간 역할극
       │  (하나의 통화 셸: 발신자·통화 타이머·AI 합성 표식·훈련 종료·난이도 배지 상시 고정)
       │                                    ── [P1] 사칭 이미지 (UX-009)
       │
       │   ┌───── v1.11 D-35 · 라우트 이동 없음 · 통화 유지 (UF-008) ─────────────────┐
       │   │  문자 도착 배너 / "문자함"(N)  ──탭──▶  [문자 오버레이 UX-027]           │
       │   │        (통화 타이머·발신자·표식·"훈련 종료"가 오버레이 위에도 계속 보임)   │
       │   │            ├─ 링크 칩 탭 ─▶ 인앱 가짜 랜딩 (UX-023) ─▶ 오버레이 복귀     │
       │   │            ├─ 인증번호(OTP) 표시 ── 사용자가 통화로 불러주는 순간이 판정 입력 │
       │   │            └─ "통화로 돌아가기" ─────────────────────▶ 통화 계속        │
       │   └────────────────────────────────────────────────────────────────────────┘
       │
       └─ 세션 종료·고지 (UX-007)   ← "훈련 종료"는 UX-014 전 구간·오버레이 안에서도 상시 진입(AC-006)
            ├─ 취약점 리포트 (UX-008)
            │    ├─ 대화 되짚어보기 → 리플레이 해설 (UX-018)
            │    ├─ [속은 순간 ≥1일 때만] 그 순간 다시 해보기 → 즉시 되감기 (UX-028, UF-009)   ── v1.11 D-39
            │    ├─ 내가 속았던 순간 모아보기 → 실패 아카이브 (UX-030, UF-010)              ── v1.11 D-44
            │    ├─ [P1] 방어 등급 (UX-010)
            │    └─ [P1] 히스토리 (UX-012) → 리포트 재열람 · 실패 아카이브(UX-030)
            └─ [속은 순간 ≥1일 때만] 그 순간 다시 해보기 → UX-028   ← 종료 직후 그 자리에서

  [실패 아카이브 (UX-030)] ── 세션이 아니라 "속은 순간" 단위 · 최신순 / 수법별 묶기
       ├─ 그때 대화 보기      ─▶ 리플레이 해설 (UX-018, 해당 시점)
       ├─ 그 순간 다시 해보기 ─▶ 즉시 되감기 (UX-028)
       └─ 이 시나리오 다시 훈련 ─▶ 난이도 선택 (UX-029) ─▶ 새 세션   ← 선형 여정의 유일한 순환 고리(반복 학습 목적)
```

- **통화 = 화면 전환 없는 phase 전이**: 수신→오프닝→실시간 역할극은 UX-014 한 화면 안의 phase일 뿐 별개 라우트 이동이 아니다(D-11·D-12). 라우트 구현 방식(단일 라우트 vs 공유 레이아웃)은 architect 판단(UX-014 Architect Handoff·OQ-U7).
- **전역 상시 컨트롤**: 통화(UX-014, [P1]UX-009 포함) 전 phase에서 "훈련 종료"가 항상 노출되어 어느 지점에서든 UX-007로 즉시 진입(AC-006). 수신 phase에서는 "거절"이 그 역할을 겸한다.
- **드릴다운 = 좁혀 들어가는 선형 진행(v1.5 D-16; v1.10 D-31 순서 상향)**: 이제 유형(UX-015)→**체험/발송(UX-026)**→[보이스+지인에게만]방식(UX-016)→시나리오(UX-017/UX-024)로 한 번에 한 단계씩만 고른다 — 체험/발송 선택이 방식·시나리오보다 **먼저** 온다(AC-056). 보이스+본인체험은 방식 단계(UX-016)를 건너뛴다(generic 강제, AC-057). 각 단계는 직전 단계로 "뒤로" 복귀 가능하고(P-12), 앞선 선택은 다음 화면 상단에 맥락(예: "보이스피싱 › 지인에게 보내기 › 내 목소리 복제")으로 남는다. 전체 시나리오를 한 화면에 동시 노출하지 않는다(AC-029).
- **메신저피싱 분기 연결됨(v1.7, D-22 — D-17 스텁 해소)**: UX-015의 (B) 메신저피싱은 이제 UX-024(메신저 시나리오 선택)→UX-022(카카오/문자 스킨 셸)로 연결된다. 스미싱 링크는 UX-023(인앱 가짜 랜딩)으로만 이동하고, 에스컬레이션 가능 시나리오는 UX-025(목소리 선택)를 거쳐 채팅 중 전이 트리거 시 통화 셸 UX-014로 이음새 없이 이어진다(UF-007). 스킨·표면은 프레젠테이션 전용이라 안전 셸은 표면과 무관하게 동일하다(§13.4).
- **에스컬레이션 = 채널 전이(하나의 세션·하나의 리포트)**: 메신저 단계와 보이스 단계는 별개 세션이 아니라 같은 세션의 채널 전이이며, 종료 시 두 채널 취약 시점을 함께 다루는 단일 리포트(UX-008)·리플레이 해설(UX-018)로 인계된다(AC-035/037, §13.1).
- **리플레이 해설(UX-018)은 종료 이후 학습 분기(v1.6, D-18)**: 세션 종료(UX-007)→리포트(UX-008)에서 "대화 되짚어보기"로 진입하는 선택 분기이며, 기존 리포트/transcript를 재사용한다. 단독 세션은 선택, 2인 사용자2는 강제(AC-042).
- **2인 소셜은 발신/수신 2흐름(v1.6 D-19; 발신 진입점 v1.9 D-30; 순서 상향 v1.10 D-31; generic 보이스 챌린지 v1.10 D-33)**: 발신자는 유형 선택 직후 **UX-026("지인에게 보내기")**에서 갈라진 뒤 (보이스면 방식→) 시나리오를 골라 챌린지 생성으로 간다. **보이스 챌린지는 이제 clone·generic 두 종**(generic 보이스 챌린지는 클론 온보딩 없이 성립, AC-058).
```
[사용자1 · UF-004 · 보이스 챌린지]
   로그인(UX-013) → 유형 선택(UX-015) → 체험/발송(UX-026) ─ "지인에게 보내기"   ── v1.10 D-31 (분기점, 시나리오보다 먼저)
   → 목소리 방식 선택(UX-016)
      ├─ clone   → 시나리오(UX-017) → 클론 온보딩(UX-002→UX-003) → 챌린지 만들기 UX-019 (딥보이스 카피, voiceId 有)
      └─ generic → 시나리오(UX-017) →(클론 스킵, AC-058)──────────────▶ 챌린지 만들기 UX-019 (기본 AI 음성 카피, voiceId 없음 · 신규)
   → 챌린지 만들기·공유 링크(UX-019) ──(OS 공유/복사, 앱이 대신 전송 안 함)──▷ 사용자2에게 링크 전달
   → (사용자2 완료·동의 후) 결과 열람(UX-020): clone=완료/의심 시점(AC-043) · generic=완료 여부만(AC-058·OQ-32 default, D-34)
   ※ 같은 UX-026에서 "본인이 체험"을 고르면 UX-016 스킵·generic 자기훈련(UF-002, 통화 UX-014)으로 감. clone 자기체험은 배제(AC-057).

[사용자2 · UF-005]  링크 열기(로그인 없음) → 동의 랜딩(UX-021, 무동의 차단 게이트 · AC-040)
   → (동의 후) 통화 체험(UX-014 재사용, clone 챌린지=사용자1 클론 음성 / generic 챌린지=GENERIC_VOICE_ID 폴백)
   → 강제 정체 공개(UX-007 재사용, 2인 변형 · AC-042) → 강제 리플레이 해설(UX-018 · AC-038/042)
   → (선택) 결과 공유 동의 → UX-020으로 전달
```
- **2인 메신저 챌린지(음성 없음) 변형(v1.8, D-27~D-29, AC-051~055; 발신 진입점 v1.9 D-30 반전)**:
```
[사용자1 · UF-004 메신저 변형]
   로그인(UX-013) → 유형 선택(UX-015) → 체험/발송(UX-026) ─ "지인에게 보내기"   ── v1.10 D-31 (분기점, 시나리오보다 먼저)
   → 메신저 시나리오 선택(UX-024, 비에스컬레이션만) →(클론 온보딩 스킵, AC-051)→ 챌린지 만들기·공유 링크(UX-019, voiceId 없음)
   ──(OS 공유/복사)──▷ 사용자2에게 링크 전달
   → (사용자2 완료·동의 후) 결과 열람(UX-020, 완료 여부만 · AC-055)
   ※ 같은 UX-026에서 "본인이 체험"을 고르면 자기훈련(UF-006, UX-024 전체 시나리오 → 메신저 채팅 UX-022)으로 감.

[사용자2 · UF-005 메신저 변형]  링크 열기(로그인 없음) → 동의 랜딩(UX-021, 채널 인지 문구·무동의 차단 · AC-040)
   → (동의 후) 메신저 채팅 체험(UX-022 재사용) ─ 스미싱 링크 → 가짜 랜딩(UX-023) → 복귀
   → 강제 정체 공개(UX-007 재사용, 2인 변형 · AC-042) → 강제 리플레이 해설(UX-018 · AC-042)
   → (선택) 결과 공유 동의 → UX-020으로 전달(완료 여부만)
   (P0는 비에스컬레이션만 — 통화 전이·UX-014·UX-025 없음. 에스컬레이션 챌린지는 #21/P1·OQ-28 보류)
```
  진입점(v1.9 D-30·v1.10 D-31): 발신자는 **유형 선택(UX-015)→UX-026 "지인에게 보내기"→UX-024(비에스컬레이션)**로 진입한다(v1.10: UX-026이 시나리오 선택보다 앞). 기존 별도 "챌린지 만들기" 메뉴는 은퇴했고, T46이 "UX-024 챌린지 생성 모드"라 부르던 것도 UX-026 분기로 대체됐다. 안전속성(무해화·스킨·면책 배너·모의 표식·상시 종료)은 단독 훈련 UX-022/023과 동일·무약화(AC-053/054).
- **통화 중 문자 = 계층이지 이동이 아니다(v1.11, D-35)**: UX-027은 통화 화면 **위에 겹치는 오버레이**이며 라우트 이동이 아니다 — `/session/play`(통화)와 `/session/messenger`(메신저)가 별도 라우트라 이동하면 통화 컴포넌트가 언마운트되어 실시간 세션·마이크가 끊기기 때문(코드로 확인된 제약). 오버레이가 열려 있어도 통화 타이머·발신자·합성 표식·"훈련 종료"가 계속 보여 **통화가 살아 있음이 시각적으로 유지**된다(P-20). 오버레이 안의 링크는 **신규 랜딩을 만들지 않고 기존 UX-023(AC-045)을 재사용**한다(UX-023은 이미 라우트가 아니라 in-page 모달로 구현돼 있어 통화 위에서도 성립, D-37). 메신저 채팅(UX-022)에서는 이 오버레이가 발생하지 않는다(이미 메시지 표면 안).
- **난이도 = 드릴다운의 마지막 단계(v1.11, D-41·D-42)**: 유형 → 체험/발송 → [방식] → 시나리오 **→ 난이도** 순으로, 난이도는 세션·챌린지 생성 **직전**에 정해진다. 난이도는 **시나리오 목록을 필터링하지 않고**(모든 시나리오에 3단계가 다 있다) **어떤 안전장치도 게이팅하지 않는다**(사전 고지·동의·상시 종료·표식·종료 고지·2인 4대 안전제약 전부 세 난이도 동일 — 메신저 스킨이 프레젠테이션 전용이라는 §13.4 원칙과 동형, AC-050 정합). 고른 난이도는 세션 셸 배지·리포트·아카이브에 **같은 라벨로** 나타난다(P-22).
- **종료 이후 학습은 3갈래로 갈라진다(v1.11, D-39·D-44)**: 같은 리포트에서 (a) **읽는 복기** UX-018(전체 대화 + 주석), (b) **다시 해보는 드릴** UX-028(속은 한 순간 · 입력·판정 있음), (c) **누적 패턴 보기** UX-030(과거 전체의 속은 순간)으로 나뉜다. (b)와 (c)는 `deceivedMoments`가 있을 때만 의미가 있어, 안 속은 세션에서는 (b) 진입점을 띄우지 않고 (a)를 권한다(없는 데이터를 발명하지 않음, D-40). **2인 사용자2는 강제 정체 공개(UX-007)→강제 해설(UX-018) 순서가 먼저이고**(AC-042) 되감기는 그 뒤의 선택 단계다 — 강제 단계를 대체하지 않는다.
- 정식 내비게이션 메뉴(햄버거/탭바) 없음 — 하루 스코프·저인지부하 위해 선형 진행 + 상시 종료만 둔다. **(v1.11 예외 1건)** UX-030(실패 아카이브)에서 UX-029(난이도)를 거쳐 새 세션으로 돌아가는 **순환 고리**가 처음 생겼다 — 반복 학습이 그 화면의 목적이므로 의도된 것이며, 그렇다고 전역 메뉴를 도입하지는 않는다(진입은 여전히 리포트·히스토리라는 **정해진 지점**에서만).

## Interaction Patterns
재사용 패턴. 구현자는 화면별로 재발명하지 말고 이 정의를 따른다.

- **P-1 클론 생성 대기 로딩**: 단계형 진행 표시("목소리 분석 중 → 클론 생성 중 → 준비 완료") + 예상 시간 문구 + 계속 움직이는 진행 애니메이션으로 "멈춘 인상" 제거. 지연이 길어도 최소 상태 문구가 주기적으로 갱신되어야 함. 타임아웃 시 재시도 버튼 + **데모 폴백(사전 준비 클론/녹화본)** 경로 노출(OQ-U3). 감소 모션 설정 존중, 진행 상태를 스크린리더용 텍스트로도 안내.
- **P-2 세션 중 상시 종료**: 통화(UX-014 전 phase, [P1]UX-009)에 "훈련 종료" 컨트롤을 스크롤과 무관하게 고정 노출. 누르면 **확인 다이얼로그 없이 즉시** 세션을 종료하고 UX-007로 이동(AC-006, 어르신 저부하 — D-2). 수신 phase에서는 "거절"이 종료를 겸한다. 종료 후 첫 포커스는 안심 고지 문구.
- **P-3 합성물 표식 노출**: 모든 합성 음성/이미지에 "AI 훈련용 합성" 표식을 **① 화면 상시 라벨(점멸 금지·고정) + ② 오디오 프리롤 안내 문구**로 이중 노출(AC-022). 이미지에는 화면 라벨 + 대체 텍스트. 표식은 색 배지 단독이 아니라 텍스트 포함.
- **P-4 응답 대기·재시도**: AI 응답/합성/리포트 대기 중 스피너 또는 타이핑 인디케이터 표시, 전송 버튼 비활성으로 중복 요청 방지. 실패/타임아웃 시 인라인 오류 + "재시도" 버튼. 핵심 루프 비차단 요소(P1 이미지 등)는 실패 시 조용히 생략.
- **P-5 입력 검증 타이밍**: 게이트(동의·본인확인·필수 선택)는 미충족 시 다음 버튼을 **비활성**으로 사전 차단(제출 후 오류 대신 사전 안내). 오류 메시지는 해당 컨트롤 근처에 아이콘+텍스트로 표시.
- **P-6 권한 거부(마이크)**: 마이크 권한 거부 시 UX-002를 "권한 필요" 안내 상태로 전환하고 재요청 방법을 텍스트로 안내. 녹음 버튼은 잠금.
- **P-7 오프라인**: 네트워크 끊김 시 진행 버튼 비활성 + "연결을 확인해 주세요" 배너. 복구 시 재시도 가능. (하루 스코프상 자동 재연결 큐잉은 구현하지 않음.)
- **P-8 디스컬레이션 톤**: 종료·리포트 화면 문구는 완화 톤·안심 우선("훈련이었습니다", "안전합니다"), "면역됨" 류 과신 표현 금지, "개선 영역" 프레임(PRD Risk).
- **P-9 확인 다이얼로그 사용 원칙**: 되돌릴 수 없는 파괴적 동작에만 확인을 둔다. 세션 종료는 되돌릴 필요가 적고 즉시성이 중요하므로 확인을 두지 않는다(P-2·D-2).
- **P-10 통화 셸 연속성(Call Shell continuity)** *(v1.4 신설, D-11·D-12)*: UX-014의 수신→오프닝→실시간 역할극은 **하나의 고정 통화 셸** 안에서 phase 전이로만 처리한다. 통화 셸 = ① 상단 AI 합성 표식(고정) + 통화 경과 타이머, ② 중앙 발신자 아바타·라벨(callerLabel) + 현재 대사 자막, ③ 하단 "훈련 종료"(상시). phase가 바뀌어도 이 셸은 자리를 지켜, full-page navigation·레이아웃 점프·"채팅창으로 넘어가는" 인상을 만들지 않는다. 오프닝 대사와 사기범 실시간 응답은 **같은 대화 로그·같은 자막 영역**에 누적된다(오프닝 = 사기범 첫 턴).
- **P-11 오프닝→실시간 이음새 없는 전환(seamless handoff)** *(v1.4 신설, D-12)*: 오프닝 마지막 대사의 오디오 재생 종료(onEnded) 시 **별도 확인·"계속" 버튼 없이** 곧바로 실시간 청취("듣고 있어요…")로 넘어간다. 오디오가 없거나 합성 실패로 재생할 게 없으면 즉시 청취로 전환(침묵 실패 금지). 중간에 "통화 종료/completed" 상태를 두지 않는다 — 통화는 사용자가 "훈련 종료"를 누르거나 한도에 도달할 때까지 끊기지 않는다.
- **P-12 드릴다운 단계 내비게이션(step drill-down)** *(v1.5 신설, D-16)*: 시나리오 선택의 3단계(UX-015 유형 → UX-016 방식 → UX-017 시나리오)는 다음 규칙을 공통으로 따른다.
  - **뒤로가기**: 각 단계에 화면 좌상단 "뒤로" 컨트롤을 두어 직전 선택 단계로 복귀한다(1단계 UX-015의 "뒤로"는 UF-001 직전 화면/홈으로). 뒤로 시 앞서 고른 선택은 유지(재입력 강요 금지). 브라우저 뒤로가기와도 일치시킨다(각 단계가 히스토리 엔트리 — 구현 방식은 architect, OQ-U7류 라우트 판단과 무관하게 스펙상 "뒤로=직전 단계").
  - **단계 표시(현재 위치)**: 각 화면 상단에 지금까지의 선택 경로를 텍스트 브레드크럼으로 표시한다(예: 1단계 "① 유형", 2단계 "보이스피싱 › ② 방식", 3단계 "보이스피싱 › 내 목소리 복제 › ③ 시나리오"). 색 단독이 아니라 텍스트로 단계를 알린다(접근성). 진행 점(dot) 표시는 텍스트 라벨을 보조할 때만 사용.
  - **전환 방향성**: 다음 단계로 진행은 "앞으로"(우→좌 슬라이드 또는 콘텐츠 교체), 뒤로가기는 그 역방향으로 애니메이션해 "좁혀 들어간다/되돌아온다"는 공간감을 준다. **감소 모션(prefers-reduced-motion) 설정을 존중**해 모션을 끄면 즉시 교체로 폴백. 애니메이션은 몰입 보조일 뿐 필수 아님(무애니메이션에서도 흐름·기능 동일).
  - **미구현 분기 처리**: UX-015의 메신저피싱처럼 목적지가 아직 없는 분기는 조용히 막지 말고 "준비 중" 안내로 명시(침묵 실패 금지, P-4/P-5와 정합).
- **P-13 리플레이 해설 스텝 내비게이션(replay walkthrough)** *(v1.6 신설, D-18)*: UX-018에서 대화를 시간순으로 되짚을 때, (a) 순차 스크롤과 (b) "다음 신호로/이전 신호로" 점프를 모두 제공한다. 각 신호 지점의 주석은 "N초 시점: 이 말이 ○○ 신호입니다 / 이렇게 대응했어야 합니다" 형식으로 대화 항목 옆에 인라인 표시하고, 신호 강조는 색만이 아니라 아이콘+텍스트로 이중 표기한다. 톤은 "개선 영역" 완화 프레임(P-8)을 따르고 "면역됨" 류 과신 표현을 쓰지 않는다. 신규 신호 주석은 aria-live로 스크린리더에 알린다. 신호가 없는 대화는 "뚜렷한 위험 신호가 없었습니다"를 명시(침묵 금지).
- **P-14 링크 공유(앱이 대신 전송하지 않음)** *(v1.6 신설, D-20)*: 2인 챌린지 링크(UX-019)는 **앱이 카톡/문자로 대신 보내지 않는다** — 사용자1이 직접 붙여 넣게 한다. UI는 (1) OS 공유 시트(`navigator.share`)가 가능하면 "공유하기" 버튼, (2) 불가/미지원이면 링크 텍스트 + "복사" 버튼으로 폴백(침묵 실패 금지). 링크는 스크린리더가 읽을 수 있는 텍스트로도 노출하고, 복사 완료는 색 아닌 텍스트("복사됨")로 안내. 발급된 링크에는 복제 음성 파일이 포함되지 않는다(토큰만, AC-041).
- **P-15 무동의 차단 게이팅(consent-first gate)** *(v1.6 신설, D-19)*: 2인 사용자2(UX-021)는 **명시적 탭 동의 전에는 어떤 복제 음성도 재생되지 않으며, 무동의로 진행하는 버튼·경로 자체가 화면에 존재하지 않는다**(AC-040). 동의 버튼 외의 유일한 대안은 "신고" 또는 "나가기"뿐이다(체험으로 우회하는 경로 없음). 사후 정체 공개(UX-007 재사용)는 이 사전 동의를 대체하지 못한다(AC-040 명시). 이 게이트는 등급·티어와 무관하게 항상 강제된다(AC-050).
- **P-16 메신저 표면 스킨 전환(presentation-only skin)** *(v1.7 신설, D-22)*: 문자형(UX-022)은 UA 자동 감지로 iOS/삼성/기본 스킨을 적용하고(§13.5 규칙표), 상단에 **수동 전환 토글**을 상시 제공한다(AC-031). 감지 실패·데스크톱은 기본 스킨으로 폴백하며 "기기를 자동 인식하지 못해 기본 화면으로 표시합니다. 직접 바꿀 수 있어요"류 안내를 1줄 노출(침묵 실패 금지). **스킨 전환은 외형만 바꾸고 대화 콘텐츠·안전 문구·"AI 훈련용 모의" 표식·면책 배너·"훈련 종료"를 절대 바꾸지 않는다**(스킨=프레젠테이션 전용, 어떤 안전 판정도 게이팅하지 않음, §13.4). 스크린리더 순서·라벨은 스킨과 무관하게 동일.
- **P-17 스미싱 링크 무해화(harmless smishing link)** *(v1.7 신설, D-23)*: 메신저 대화의 링크는 **`displayText`(모의 표기)만 보이고 실 URL이 없다**(스키마에 URL 필드 부재). 탭하면 외부로 이동하지 않고 `fakeLandingId`가 가리키는 인앱 가짜 랜딩(UX-023)으로만 전환된다. 가짜 랜딩의 입력값은 **서버로 전송되지 않고**(콜러블 부재) "입력됨" 가짜 피드백만 준 뒤 채팅으로 복귀. 링크·랜딩 모두 실 브랜드·기관명 없이 무해화하고 "AI 훈련용 모의" 표식을 노출(AC-032/045). 스크린리더는 링크를 "모의 링크"로 안내(실제 링크 오인 방지).
- **P-18 에스컬레이션 전이 연출(seamless channel escalation)** *(v1.7 신설, D-26)*: 메신저→보이스 전이는 세 경로(① 역할극 LLM sentinel 신호를 서버가 감지 → `escalation` 플래그, ② 메신저 max-turn 폴백 자동 전이, ③ 사용자가 "전화로 확인" 명시 버튼 탭)로 시작되며, **UX는 항상 서버가 준 `escalation:{toChannel:"voice"}` 플래그만 보고 연출한다**(자유텍스트 분류 안 함, sentinel 토큰은 서버가 제거해 화면에 절대 노출되지 않음, §13.2). 연출은 "사기범이 전화를 거는" 수신 화면으로 채팅을 전환해 **기존 통화 셸 UX-014의 incoming phase로 이음새 없이** 인계한다(중간 확인·별도 라우트 점프 최소화, P-11 정합). 통화용 voiceId는 UX-025에서 이미 확보돼 녹음으로 끊기지 않는다(D-25). 사전 고지·동의(이미 완료)·상시 종료·합성 표식·종료 고지는 UX-014 그대로(AC-036, 재설계 없음). 전이 실패 시 메신저 종료로 폴백해 단일 리포트를 보장(AC-035, 침묵 실패 금지). **연출 로직은 방향 무관하게 재사용 가능한 구조로 설계**하되 화면은 메신저→보이스만 구현(AC-039).
- **P-19 상시 면책 고지 배너(persistent disclaimer banner)** *(v1.7 신설, D-24)*: 카카오톡형 표면(UX-022)에는 "카카오톡 실제 서비스와 무관한 훈련용 재현입니다" 면책 고지를 **화면 상단 고정 배너로 상시 노출**한다 — **영구 비활성화 불가, 스크롤해도 사라지지 않음, 재진입 시에도 유지**(AC-047/§13.7, UI 상시 요건이며 데이터 필드 아님). 색 아닌 텍스트 포함·점멸 금지. 이 배너는 스킨 전환·대화 진행과 무관하게 자리를 지킨다.

- **P-20 통화 유지 오버레이(call-preserving overlay)** *(v1.11 신설, D-35)*: 통화(UX-014) 위에 뜨는 오버레이(UX-027, 그리고 그 안에서 열리는 UX-023)는 **라우트 이동이 아니라 화면 위 계층**이다. 규칙: (1) **통화는 멈추지 않는다** — 세션 타이머가 계속 흐르고 사기범 오디오·자막도 계속 누적된다. (2) **통화가 살아 있다는 증거를 오버레이 위에 남긴다** — 상단에 발신자·경과 시간·"통화 중" 상태를 축소 표시하고, 이를 **텍스트로도** 안내한다(색·아이콘 단독 금지). (3) **"훈련 종료"는 오버레이 안에서도 도달 가능**해야 한다(AC-006) — 포커스 트랩이 종료 컨트롤을 가두면 안 된다. (4) **자동청취(마이크)는 오버레이가 열린 동안 일시 게이팅**한다(읽는 중 주변 소리를 발화로 오인 방지) — 단 사기범 오디오 재생과 타이머는 게이팅하지 않는다. (5) 열릴 때 포커스는 오버레이 제목으로, 닫으면 **직전 트리거로 복귀**한다. (6) 도착 알림은 시각 배너와 **aria-live 동시** 제공. (7) 통화 한도 도달 시 오버레이를 자동으로 닫고 종료 고지를 가리지 않는다.
- **P-21 되감기 드릴(rewind drill)** *(v1.11 신설, D-39·D-40)*: UX-028의 재연 드릴은 다음을 공통으로 따른다. (1) **한 턴만** — 사용자가 다시 답한 뒤 사기범 대사가 이어지지 않는다(대화 재개가 아니라 단발 드릴). (2) **입력은 선택** — "모범 대처만 보기"로 입력 없이 넘어갈 수 있다(강제 입력 금지). (3) **판정은 3단계 텍스트**(잘 대응했습니다 / 아직 위험합니다 / 판단하기 어렵습니다) + 이유 1줄 + 모범 대처 나란히 비교. 색 단독 표기 금지. (4) **판정 실패는 정직하게** — "판정하지 못했습니다"를 명시하고 모범 대처는 반드시 보여준다(침묵 실패·조용한 성공 위장 금지). (5) **톤**: 과신("이제 안전/면역")과 질책("또 틀렸습니다") 둘 다 금지, "개선 영역" 완화 프레임(P-8). (6) **되감을 순간이 없으면 진입점을 만들지 않는다** — 없는 데이터를 대신할 개념(near-miss 등)을 발명하지 않는다(D-40).
- **P-22 난이도 표기 일관성(difficulty labeling)** *(v1.11 신설, D-41·D-42)*: 사용자가 고른 난이도는 **선택(UX-029) → 세션 셸(UX-014/UX-022 배지) → 리포트(UX-008) → 리플레이(UX-018) → 아카이브(UX-030) → 챌린지(UX-019/UX-021)** 전 구간에서 **같은 라벨·같은 3단계 어휘**(초급/중급/고급)로 표기한다. 색 단독 표기 금지 — 반드시 텍스트 라벨을 포함한다("고급"을 빨강으로만 표시하지 않는다). **고급 선택 시** "실제와 가깝게 강한 압박이 이어집니다 · 언제든 '훈련 종료'로 멈출 수 있습니다"를 인라인으로 고지하되 **확인 다이얼로그를 띄우지 않는다**(P-9/D-2 원칙 유지). **초급의 힌트는 세션 시작 전 브리핑 카드로만** 제공하고 **대화 중 실시간 판정 표시는 하지 않는다**(D-6 유지, D-43). 난이도는 어떤 안전 컨트롤도 숨기거나 완화하지 않는다.
- **P-23 속은 순간 카드(deceived-moment card)** *(v1.11 신설, D-44·D-45)*: UX-030(및 UX-008 타임라인·UX-018 주석)에서 "속은 순간"을 보여줄 때의 공통 표기. 1장에 **① 언제**(세션 날짜 + "N초 시점") **② 어떤 상황**(시나리오 제목·채널·난이도) **③ 어떤 수법에 넘어갔는지**(`tactic`) **④ 그때 올바른 대처**(`correctAction`, 기본 접힘)를 담는다. 수법·심각도는 **색 아닌 텍스트 라벨+아이콘 이중 표기**. 그룹으로 묶을 때는 헤더에 **반복 횟수를 텍스트로 명시**("긴급성 압박 — 3건")해 반복 패턴이 스크린리더에도 전달되게 한다. 오디오 재생 경로를 두지 않는다(생성물은 폐기됨, AC-021). 톤은 "개선 영역" 완화 프레임(P-8) — 질책 표현 금지.

## Accessibility
| Item | Value |
|---|---|
| Keyboard Navigation | 모든 상호작용 요소(체크박스·버튼·녹음·입력·전송·"훈련 종료")는 Tab으로 도달·Enter/Space로 실행 가능. "훈련 종료"는 세션 화면에서 항상 포커스 도달 가능. |
| Focus Order | 화면 진입 시 포커스는 주 제목/핵심 안내로. 세션 종료 직후 포커스는 UX-007의 "훈련이었습니다" 고지 문구로 이동(안심 우선). 오류 발생 시 포커스를 오류 메시지로. |
| Screen Reader Support | 녹음 상태("녹음 중/완료"), 클론 진행 단계, 채팅 신규 메시지(aria-live), "AI 훈련용 합성" 표식, 리포트 타임라인 항목을 텍스트로 읽을 수 있게 라벨링. 오디오 재생 대사는 자막 텍스트 동시 제공. |
| Color Contrast | 본문·라벨 최소 WCAG AA(일반 텍스트 4.5:1, 큰 텍스트 3:1). 어르신 대상 고려해 큰 글씨 기본. |
| Touch Target Size | 최소 44×44px, 어르신 주요 액션(녹음·시작·"훈련 종료")은 48px 이상 권장. |
| Error Messaging | 오류·상태·위험도·합성 표식은 **색만으로 전달 금지** — 아이콘+텍스트를 함께 사용. |

> **신규 화면 접근성 보강(v1.11 — UX-027·UX-028·UX-029·UX-030)**:
> - **Keyboard Navigation**: UX-027 오버레이는 Esc로 닫히고 **포커스 트랩 안에 "훈련 종료"를 반드시 포함**한다(AC-006 — 오버레이가 종료 컨트롤을 가두면 안 된다). UX-028은 **텍스트 입력이 기본**이라 마이크 없이 키보드만으로 전 기능을 쓸 수 있다. UX-029의 3개 난이도 버튼·UX-030의 카드별 3개 동선 버튼 모두 Tab 도달·Enter 실행.
> - **Focus Order**: UX-027은 열릴 때 오버레이 제목으로, 닫을 때 **직전 트리거(도착 배너 또는 "문자함" 버튼)로 복귀**. UX-028은 진입 시 "다시 답해보기" 입력 라벨로. UX-030은 진입 시 목록 제목으로.
> - **Screen Reader Support**: 문자 도착은 **aria-live**("문자 1건이 도착했습니다"), 되감기 판정 결과도 aria-live. 링크 칩은 **"모의 링크"**로 안내(P-17 계승), 인증번호는 **한 자씩** 읽히도록 라벨링("인증번호 4 8 2 9 1 7"). 통화가 계속되고 있다는 사실을 오버레이 안에서 **텍스트로도** 안내("통화 연결 중 · 경과 1분 12초"). UX-030 목록은 list/listitem 시맨틱 + 그룹 헤더에 반복 횟수 텍스트("긴급성 압박 — 3건").
> - **Color Contrast / Touch Target**: 기존 기준(AA·44px, 주요 액션 48px 이상) 동일 적용. UX-029의 3개 버튼·UX-030의 카드는 어르신 기준 48px 이상.
> - **Error Messaging**: 난이도 3단계는 **색 단독 표기 금지**("고급"을 빨강으로만 표시하지 않는다, P-22). 되감기 3단계 판정도 색 단독 금지. 되감기 판정 실패·아카이브 항목 비활성은 **사유를 1줄 텍스트로** 밝힌다(침묵 실패 금지).

## Responsive Behavior
웹 단일 채널. 어르신은 모바일 브라우저, 데모는 데스크톱일 가능성이 높아 두 브레이크포인트 모두 지원한다.

| Breakpoint | Layout Changes |
|---|---|
| Desktop | 중앙 정렬 단일 컬럼(가독성 위해 최대 폭 제한). 드릴다운 유형/방식 카드(UX-015/016)와 노출된 시나리오 카드(UX-017)는 가로 나열 가능. 통화(UX-014)는 중앙 컬럼 통화 셸, AI 합성 표식·통화 타이머 상단 고정, "훈련 종료" 하단 고정. |
| Tablet | 데스크톱과 동일한 단일 컬럼. 시나리오 카드 1~2열. No breakpoint-specific behavior beyond column count. |
| Mobile | 단일 컬럼 세로 스택. 시나리오 카드 1열. 녹음·"받기/거절"·"훈련 종료" 등 주요 버튼 폭 확대(터치). 통화(UX-014)의 마이크·텍스트 입력·"훈련 종료"는 하단/상단 고정으로 스크롤 무관 상시 노출. |

> **신규 화면 반응형(v1.5·v1.6)**: 드릴다운 유형/방식 선택(UX-015/016)·동의 랜딩(UX-021)은 큰 카드/버튼 중심의 단일 컬럼으로 **No breakpoint-specific behavior**(모바일은 버튼 폭만 확대). 리플레이 해설(UX-018)·챌린지 결과 열람(UX-020)은 세로 스택 목록으로 데스크톱/태블릿/모바일 동일(**No breakpoint-specific behavior**, 데스크톱만 최대 폭 제한). 챌린지 만들기(UX-019)의 공유/복사 버튼은 모바일에서 폭 확대·OS 공유 시트 우선.
> **신규 화면 반응형(v1.9 · 체험 선택)**: 체험 선택(UX-026)은 큰 버튼 2개 중심의 단일 컬럼으로 드릴다운 유형/방식 선택(UX-015/016)과 동일하게 **No breakpoint-specific behavior**(모바일은 버튼 폭만 확대, 데스크톱은 최대 폭 제한). 상단 시나리오 맥락 브레드크럼도 세 브레이크포인트 동일.
> **신규 화면 반응형(v1.11 · 문자 오버레이·되감기·난이도·아카이브)**:
> - **UX-027(통화 중 문자 오버레이)**: 세 브레이크포인트 모두 **화면을 덮는 오버레이**로 동일하게 렌더링하되, **데스크톱/태블릿에서는 통화 화면을 완전히 가리지 않는다** — 최대 폭을 제한한 중앙 패널로 띄우고 뒤의 통화 셸(타이머·발신자)이 주변에 보이게 해 "통화가 살아 있다"는 신호를 시각적으로 강화한다. **모바일은 전체 화면 시트**(폰 문자앱 모사)이며, 이때는 오버레이 **상단에 통화 축소 바를 고정**해 같은 신호를 대신한다. 어느 브레이크포인트에서도 "훈련 종료"와 모의 표식은 스크롤 무관 상시 노출. 오버레이 안에서 열리는 UX-023은 기존 UX-023 반응형 규칙 그대로.
> - **UX-028(즉시 되감기)**: 세로 스택 단일 컬럼(맥락 대화 → 입력 → 판정·모범 대처). **No breakpoint-specific behavior**(데스크톱만 최대 폭 제한, 모바일은 입력창·버튼 폭 확대 및 입력창 하단 고정).
> - **UX-029(난이도 선택)**: 큰 버튼 3개 중심 단일 컬럼으로 UX-015/016/026과 동일하게 **No breakpoint-specific behavior**(데스크톱/태블릿은 3개 가로 나열 가능, 모바일은 1열 세로 스택·버튼 폭 확대).
> - **UX-030(실패 아카이브)**: 세로 스택 카드 목록으로 UX-018/UX-020과 동일하게 **No breakpoint-specific behavior**(데스크톱만 최대 폭 제한, 모바일 1열). 그룹 헤더는 스크롤 시 상단 고정(sticky)하되 모든 브레이크포인트 동일.
> **신규 화면 반응형(v1.7 · 메신저)**: 메신저 채팅 셸(UX-022)은 **본질적으로 모바일 메신저 외형**이라 모든 브레이크포인트에서 모바일 폭 단일 컬럼으로 렌더링(데스크톱에서도 폰 폭 컨테이너로 중앙 정렬 — 카카오/문자 정확 모사 유지). 카카오 면책 배너·"AI 훈련용 모의" 표식·"훈련 종료"·스킨 토글은 상단/하단 고정으로 스크롤 무관 상시 노출. 문자 스킨(iOS/삼성/기본)은 외형 차이일 뿐 레이아웃 구조 동일. 가짜 랜딩(UX-023)·목소리 선택(UX-025)·메신저 시나리오 선택(UX-024)은 단일 컬럼(**No breakpoint-specific behavior**, 모바일 버튼 폭 확대). 에스컬레이션 전이 후 통화(UX-014)는 기존 UX-014 반응형 규칙 그대로.

## UX Decision Log
Append-only — 과거 항목을 고쳐쓰거나 삭제하지 않는다.

### D-1 하루 완성을 위해 화면/상태 최소화
| Item | Value |
|---|---|
| Decision | P0 핵심 루프를 8개 화면(UX-001~008)·선형 단일 여정으로 압축하고, 사전고지+동의를 한 화면(UX-001)에 통합, 정식 내비게이션 메뉴를 두지 않는다. 상태는 화면당 Loading/Error/Success 중심으로만 정의하고 풍부한 엣지케이스는 배제. |
| Reason | 개발이 Day1 하루(무박 포함)에 끝나야 하고 3명 병렬이며 Day2 오전은 버그픽스만 가능(PRD Constraints). 데모 스크립트가 2분 내 매끄럽게 이어져야 함. |
| Alternatives Considered | 온보딩/동의/연령/로그인 화면 분리, 리치 내비게이션, 화면별 다수 엣지 상태. |
| Rejected Because | 하루 스코프에서 구현·검증 불가하고 데모 흐름을 느리게 함. |
| Impact | Architecture / Implementation |
| Status | Active |

### D-2 세션 종료는 확인 다이얼로그 없이 즉시
| Item | Value |
|---|---|
| Decision | "훈련 종료"는 모든 세션 화면에 상시 노출되며 누르면 확인 없이 즉시 종료→UX-007로 이동. |
| Reason | AC-006이 "항상 보이는 단일 컨트롤로 즉시 종료"를 요구. 어르신 저인지부하·심리적 안전(즉시 탈출 가능해야 함). |
| Alternatives Considered | 종료 전 확인 다이얼로그(오조작 방지). |
| Rejected Because | 확인 단계가 "즉시 탈출"을 방해하고 인지부하를 늘림. 종료는 파괴적 동작이 아님(리포트로 안전 인계). |
| Impact | Implementation |
| Status | Active |

### D-3 합성물 표식 이중 노출(화면 라벨 + 오디오 프리롤)
| Item | Value |
|---|---|
| Decision | 합성 음성·이미지에 "AI 훈련용 합성"을 화면 상시 라벨과 오디오 프리롤 안내로 이중 노출. |
| Reason | AC-022 충족 + 보안 배점 20% 근거를 심사에서 명확히 보여주기 위함. |
| Alternatives Considered | 화면 라벨만, 오디오 안내만. |
| Rejected Because | 단일 노출은 청각/시각 어느 한쪽 접근성에서 표식을 놓칠 수 있음. |
| Impact | Implementation |
| Status | Active |

### D-4 클론 생성 대기를 전용 화면으로 분리
| Item | Value |
|---|---|
| Decision | 클론 생성 대기를 UX-002의 로딩 상태가 아닌 전용 화면(UX-003)으로 분리하고 단계형 진행·폴백 경로를 둔다. |
| Reason | ElevenLabs 클론이 수 초~수십 초 걸릴 수 있어 데모 중 "멈춘 것처럼" 보이는 리스크가 큼(PRD Risk: 라이브 클론 데모 실패). 전용 화면이 진행·폴백·재시도를 명확히 다룸. |
| Alternatives Considered | UX-002 인라인 스피너로 처리. |
| Rejected Because | 인라인 스피너는 장시간 대기·폴백 전환을 표현하기 어렵고 데모 리스크를 키움. |
| Impact | Architecture / Implementation |
| Status | Active |

### D-5 본인 확인은 경량 자기확인으로 설계(강한 KYC 배제)
| Item | Value |
|---|---|
| Decision | AC-020의 "본인 확인"을 체크박스+확인 문구("본인 목소리·본인 직접 녹음") 기반 경량 자기확인으로 설계하고, 파일 업로드/타인 음성 입력 경로를 UI에서 제거. |
| Reason | 하루 스코프에서 강한 신원인증(정부 ID·본인인증) 구현 불가. AC-020의 핵심 의도(타인 목소리 무단 등록 차단)는 입력 경로 제거 + 자기확인으로 충족 가능. |
| Alternatives Considered | 휴대폰 본인인증/OAuth 신원확인. |
| Rejected Because | 하루 개발·데모 범위 초과. 단, 강도 충분성은 architect/user 확정 필요(OQ-U1). |
| Impact | Architecture / Implementation |
| Status | Active |

### D-6 "속은 시점" 판정은 리포트 단계에서만(채팅 중 실시간 표시 안 함)
| Item | Value |
|---|---|
| Decision | 사용자가 속은 시점·수법 판정은 리포트 생성(UX-008)에서 수행하고, 역할극 채팅(UX-006) 중에는 실시간으로 표시하지 않는다. |
| Reason | 실시간 판정 UI는 몰입을 깨고 구현 복잡도를 키움. AC-026(타임라인)은 사후 리포트로 충족. |
| Alternatives Considered | 채팅 중 실시간 "위험!" 경고 표시. |
| Rejected Because | 몰입(예방접종 효과) 저해 + 하루 스코프 초과. |
| Impact | Implementation |
| Status | Active |

### D-7 자녀 보조/자녀 리포트 열람 화면 미설계
| Item | Value |
|---|---|
| Decision | 자녀가 부모 계정을 대신 만드는 화면·자녀가 부모 리포트를 열람하는 화면을 설계하지 않는다. 본인 1인 온보딩 경로만 설계. |
| Reason | PRD v0.4에서 OQ-3·OQ-15가 빌드 스코프 관점에서 여전히 open. 발표 내러티브 전용이며 빌드 스코프 밖. |
| Alternatives Considered | 자녀 보조 온보딩 화면 추가. |
| Rejected Because | 빌드 스코프 미확정·미포함(PRD·Tasks 게이팅). 임의 설계는 스코프 크립. |
| Impact | None(설계 제외) |
| Status | Active |

### D-8 P1 화면은 경량 스케치만
| Item | Value |
|---|---|
| Decision | P1(UX-009 사칭 이미지, UX-010 게임화, UX-011 age-gate, UX-012 히스토리)은 상태·핸드오프를 최소로만 스케치하고 "여유 시 추가"로 표시. 핵심 루프 비차단. |
| Reason | P1은 Day1 야간~Day2 여유 시 항목(PRD Priority). 핵심 루프 완성이 우선. |
| Alternatives Considered | P1도 P0 수준으로 상세 설계. |
| Rejected Because | 하루 스코프에서 P0 완성도를 희생시킴. |
| Impact | Implementation |
| Status | Active |

### D-9 P0에 로그인 도입 (OQ-U2 해소)
| Item | Value |
|---|---|
| Decision | P0 진입점에 로그인/가입 화면(UX-013)을 신설하고 UF-001의 첫 단계로 둔다. 로그인은 UX-001(사전고지+동의) **이전**에 배치하며, 이후 모든 데이터(동의·클론·세션·리포트)는 인증된 계정에 귀속된다. |
| Reason | 사용자 확정(2026-07-21) "로그인 있이 가자". 로그인이 계정 소유권(OQ-11: 소유권=본인)·동의 귀속·리포트/등급 지속(AC-011/016)의 앵커가 되어 데이터 모델이 명확해짐. 로그인 우선 배치는 인증 없는 부분 진행 상태를 없애 흐름을 단순화. |
| Alternatives Considered | (1) 로그인 없이 익명/임시 세션(OQ-U2의 원래 제안). (2) 로그인을 UX-001 동의 이후에 배치. (3) 로그인을 UX-002 녹음 직전에 배치. |
| Rejected Because | (1) 사용자가 로그인 필요로 결정. (2)·(3) 동의·부분 데이터가 익명 상태로 먼저 생기면 계정 귀속·소유권 처리가 복잡해지고, 어르신에게 중간 로그인 삽입이 흐름을 끊음. 로그인 우선이 가장 단순. |
| Impact | Architecture(Firebase Auth·데이터 귀속) / Database(사용자 계정 스키마) / API / Implementation. **D-1의 "로그인 화면 분리 rejected" 판단 중 로그인 부분만 본 결정으로 갱신**(D-1의 화면 최소화 원칙 자체는 유효·Active 유지). |
| Status | Active |

### D-10 UX-006을 텍스트 채팅에서 실시간 음성 통화형으로 전환(STT/TTS In Scope 전환)
| Item | Value |
|---|---|
| Decision | UX-006(사기범 역할극)에 브라우저 음성 인식(STT, 탭-투-토크) 입력과 사기범 응답 자동 음성 재생(TTS)을 추가한다. 텍스트 입력창은 STT 미지원 브라우저(Safari 등)를 위한 상시 폴백으로 남긴다. **실제 전화망(PSTN) 연동은 하지 않는다** — 여전히 앱 내부(브라우저)에서만 동작하는 통화형 UI다(AC-019 무변경). |
| Reason | 사용자 결정(2026-07-22). "훈련인지 사용자가 인지하면 안 된다"(사전고지·동의 자체 생략)를 먼저 요청했으나, 이는 AC-012/017/023을 정면으로 제거하는 것이라 어르신 대상 무동의 공포 유발 리스크로 **거부**. 절충안으로 "사전 동의는 유지, 통화 중 몰입은 유지"에 합의. 이어 "전화가 기본 모드"·"진짜 음성 대화(마이크 입력)"·"진짜 전화"(재확인 결과 PSTN 아닌 앱 내 통화형 UI로 확정)로 이어짐. |
| Alternatives Considered | (1) 원안(사전고지·동의 생략) — 거부. (2) 텍스트 채팅 유지, 음성은 화면 7(딥보이스 재생)에만 한정. (3) 실제 전화망(PSTN, Twilio 등) 연동. |
| Rejected Because | (1) AC-012/017/023 제거는 동의 없는 심리적 실험에 해당해 실제 심리적·법적 리스크(특히 주 타겟 어르신). 실제 사례로 동의 없는 "가짜 응급상황" 프랭크는 사망·소송 전례가 있음. (3) PSTN 연동은 AC-019가 이미 명시적으로 배제한 항목이며, 전화번호 수집·통신사업법·보이스피싱 관련 법규 검토가 하루 스코프 내 불가능. |
| Impact | UX-006 States/Primary Actions/Business Rules 갱신(아래), `functions/src/roleplay`(sendMessage에 TTS 합성 훅), `src/lib/recording/useSpeechRecognition`(신규), PRD v0.8, Architecture.md §1 금지 목록. |
| Status | Active |

### D-11 딥보이스 재생(구 UX-005) + 역할극(구 UX-006)을 단일 연속 통화 UX-014로 통합
| Item | Value |
|---|---|
| Decision | 별개 라우트였던 딥보이스 재생(UX-005)과 사기범 역할극(UX-006)을 하나의 통화 화면 **UX-014**로 통합하고, 두 화면은 Deprecated로 이관한다(삭제 금지). 통화는 수신→오프닝→실시간 역할극을 고정 통화 셸 안의 phase 전이로 처리한다. D-10(STT/TTS In Scope)의 음성 결정은 반전 없이 UX-014로 계승된다. |
| Reason | 사용자 피드백(2026-07-22): "화면 7(전화받기)→8(대화)이 하나의 통화처럼 안 느껴짐 / 받았다가 채팅창으로 넘어가는 느낌." 원인은 두 라우트(`/session/play`·`/session/chat`) 사이의 full-page 전환. 하나의 화면·하나의 셸로 합치면 통화 몰입(예방접종 효과의 핵심)이 유지된다. 화면 단위로 claude.ai/design 원안을 수입해온 접근이 단절의 근본 원인이라는 사용자 지적도 반영해, 이 앱 고유 요구(전화 몰입 + 안전장치)에서 새로 그렸다. |
| Alternatives Considered | (1) 두 화면 유지 + 공유 레이아웃(통화 셸)으로 라우트 전환만 부드럽게. (2) 현행(두 라우트 + "계속" 버튼) 유지. (3) 텍스트 채팅으로 회귀. |
| Rejected Because | (2) 현행이 바로 사용자가 지적한 단절의 원인. (3) D-10에서 이미 음성 통화형으로 결정됨(반전 불가). (1)은 유효한 구현 대안이나 UX 스펙 레벨에서는 "하나의 통화"를 단일 화면으로 규정하는 게 명확 — 라우트 구현 방식(단일 vs 공유 레이아웃)은 architect에게 트레이드오프로 위임(UX-014 Architect Handoff, OQ-U7). |
| Impact | Architecture(`/session/play`·`/session/chat` 통합 리팩터 또는 공유 레이아웃) / Implementation. UX-005·UX-006 Deprecated. UF-002 Steps·IA·Interaction Patterns(P-10/P-11) 갱신. D-1(화면 최소화)과 정합 — 화면 수를 오히려 줄인다. |
| Status | Active |

### D-12 오프닝→실시간 이음새 없는 전환(중간 "계속" 버튼·"통화 종료" 상태 제거)
| Item | Value |
|---|---|
| Decision | 오프닝 딥보이스 마지막 대사 재생 종료(onEnded) 시 별도 확인·"계속(역할극 시작)" 버튼 없이 곧바로 실시간 청취로 전환한다. 오프닝과 실시간 사이에 "통화 종료/completed" 중간 상태를 두지 않는다. 통화는 사용자가 "훈련 종료"를 누르거나 한도 도달 시에만 끝난다. |
| Reason | 현행 UX-005의 completed 상태 + "계속" 버튼이 통화를 두 동강 내 "채팅창으로 넘어가는" 인상을 준다. 실제 사기 전화는 인사→본론이 끊김 없이 이어진다 — 이음새를 없애야 심리적 압박(예방접종)이 유지된다. |
| Alternatives Considered | 오프닝 후 "역할극을 시작할까요?" 확인 단계 유지. |
| Rejected Because | 확인 단계가 몰입을 끊고, 사용자가 "이제 다른 화면"으로 인지하게 만든다. 통화 이탈은 "훈련 종료"로 언제든 가능하므로 별도 확인 불필요(P-9와 정합). |
| Impact | Implementation(UX-014 phase 전이 로직). 구 UX-005의 completed 상태·"계속" 버튼 제거. |
| Status | Active |

### D-13 clone/generic 시나리오를 동일 통화 셸로 처리하되 음성 모드 라벨로만 구분
| Item | Value |
|---|---|
| Decision | clone(본인 목소리 사칭)과 generic(기관 사칭) 시나리오를 UX-014의 동일 통화 셸로 처리하되, 오프닝 음성 모드를 라벨로 구분한다: clone="당신의 목소리로 합성", generic="기본 합성 음성". generic은 녹음/클론 단계 자체를 생략한다(코드는 이미 `voiceMode`로 분기). |
| Reason | 화면 구조를 시나리오별로 이원화하면 하루 스코프·유지비용이 커진다. 라벨 분기만으로 "근거 없는 본인 목소리 표기 금지"(generic에 '당신의 목소리로 녹음' 같은 거짓 문구 방지 — 사용자가 과거 지적)를 충족하면서 통화 프레임은 재사용할 수 있다. |
| Alternatives Considered | (1) generic 전용 별도 통화 화면. (2) generic도 "본인 목소리" 문구 그대로(거짓). |
| Rejected Because | (1) 하루 스코프 초과·중복. (2) 사실과 다른 표기(AC 정합·윤리 위반). 단, generic을 딥보이스와 같은 프레임에 넣는 것이 차별점(#1 본인 목소리 복제)을 희석하는지 여부는 ux-design이 단독 판단할 사안이 아니라 "재검토 필요(기능 방향)" 섹션 R-2로 넘긴다. |
| Impact | Implementation(UX-014 라벨 분기). Screen Catalog UX-004/UX-014. |
| Status | Active |

### D-14 온보딩 순서(Phase B) 문서 정합화 — 시나리오 선택 후 조건부 녹음
| Item | Value |
|---|---|
| Decision | Screen Catalog의 Entry/Exit·화면-플로우 소속·UF Steps를 실제 코드 순서(로그인→동의→[age-gate]→**시나리오 선택**→(clone일 때만)녹음→클론대기→통화→종료→리포트)로 정정한다. 녹음(UX-002)·클론 대기(UX-003)의 소속을 UF-001→**UF-002**로 이관한다. |
| Reason | IA 다이어그램(v1.3)과 실제 코드(`scenarios/page.tsx`·`age-gate/page.tsx`·`clone/wait`)는 이미 Phase B 순서였으나, 개별 화면의 Entry/Exit·Nav Targets·UF Steps가 구(舊)순서(동의→녹음→클론→시나리오)로 남아 **문서 내부가 서로 모순**이었다. generic 시나리오가 녹음을 생략하려면 시나리오를 먼저 골라야 분기가 가능하다는 Phase B 근거를 화면 스펙에도 반영. |
| Alternatives Considered | 구 순서 유지(문서만 옛 상태로 방치). |
| Rejected Because | 코드·IA와 화면 스펙이 어긋난 채로 두면 architect/implementer가 어느 순서를 믿을지 혼란. downstream 재검증 무결성 위반. |
| Impact | Screen Catalog UX-001/002/003/004/011 Entry·Exit·Nav·소속. UF-001/UF-002 Steps·Related AC. |
| Status | Active |

### D-15 UX-014 통화 중 "AI 훈련용 합성" 상시 배지 제거(AC-022 편차)

| Item | Value |
|---|---|
| Decision | UX-014(통화) 화면에서 통화 중 상시 노출되던 "AI 훈련용 합성" 배지(SyntheticLabel)를 제거한다. 사전 동의(UX-001)·시나리오 선택 화면의 "실제 사기가 아니라 훈련용 시뮬레이션입니다" 안내·전화받기 전 프리롤 고지(PREROLL_NOTICE)·"훈련 종료" 버튼(AC-006/012/017/023)은 이번 변경 범위가 아니며 그대로 유지된다. |
| Reason | 사용자(제품 소유자)가 명시적으로, 반복적으로 지시. AI 담당자는 통화 중 고압박 구간에서 순간적 판단력 저하 시 기댈 안전장치로서 배지 유지를 권고했고 이 우려를 사용자에게 전달했으나(2026-07-22, 세션 내 3회), 사용자가 이해한 상태에서 최종적으로 제거를 재확인·지시함 — 제품 자체의 UX/안전 톤에 대한 최종 결정권은 제품 소유자에게 있다고 판단해 반영. |
| Alternatives Considered | (1) 배지 유지(AI 권고안). (2) 배지 크기/불투명도만 축소. |
| Rejected Because | 사용자가 (1)·(2) 모두를 거부하고 완전 제거를 재확인. |
| Impact | Implementation(`src/app/session/play/page.tsx`의 `<SyntheticLabel />` 제거, import 정리). AC-022 매핑 표(§Non-Functional/Cross-Cutting)는 "UX-014(오프닝·이미지 시)" 문구가 더 이상 통화 중 상시 노출과는 무관함을 유의 — 사전 고지·프리롤 문구 쪽으로 커버리지가 좁아졌다는 것을 architect/QA가 인지해야 함. |
| Status | Active |

### D-16 평면 시나리오 목록(구 UX-004)을 단계적 드릴다운(UX-015/016/017)으로 재설계
| Item | Value |
|---|---|
| Decision | 모든 시나리오를 한 화면에 나열하던 UX-004를 Deprecated로 이관하고, **유형 선택(UX-015: 보이스피싱/메신저피싱) → 보이스 방식 선택(UX-016: clone/generic) → 해당 조건 시나리오만 노출(UX-017)**의 드릴다운 3화면 + 신규 Flow UF-003으로 대체한다. 보이스 하위 clone/generic은 신규 음성 데이터 모델 없이 기존 `voiceMode`를 재활용한다. |
| Reason | PRD v0.9 AC-028(최상위 2분류+보이스 하위 clone/generic)·AC-029(유형 먼저 고르면 그 유형 시나리오만 드러나는 단계적 흐름, 평면 목록 동시 노출 금지). 사용자 요구(2026-07-23): "목록형이 아니라 선택했을 때 시나리오가 나오는 느낌 — 한 번에 하나씩 좁혀 들어가 '이 상황에 들어간다'는 몰입". 시나리오가 9종으로 늘어 평면 목록의 훑어보기·몰입이 모두 약해진 것도 배경. |
| Alternatives Considered | (1) 구 UX-004 평면 목록 유지 + clone/generic 섹션 헤더만 구분(현행 코드). (2) 유형만 나누고 방식은 시나리오 카드 배지로 표기(2단 드릴다운). (3) 시나리오까지 한 장씩 캐러셀(완전 단건 드릴다운). |
| Rejected Because | (1) AC-029의 "평면 목록 동시 노출 금지"에 정면 위배 + 사용자가 명시적으로 거부. (2) AC-028이 보이스 하위를 clone/generic "분류"로 요구하므로 방식은 배지가 아닌 선택 단계로 두는 편이 흐름·몰입에 부합. (3) 최종 시나리오까지 단건화하는 것은 유효한 대안이나 clone/generic별 시나리오가 복수라 정보 접근성·비교가 떨어질 수 있어 이번엔 필터된 노출로 두고, 단건화 필요 여부는 OQ-U9로 남김(임의 결론 금지). |
| Impact | Implementation(구 `src/app/scenarios/page.tsx` 평면 목록 → 드릴다운 3단계로 재구성 — 소스 수정은 implementer 소관, ux-design은 스펙만). Screen Catalog(UX-004 Deprecated, UX-015/016/017 신설). User Flows(UF-003 신설, UF-002 시작점 갱신). IA·Interaction Patterns(P-12). D-14(Phase B: 시나리오 선택이 녹음보다 먼저)와 정합 — 드릴다운도 녹음 이전에 완료된다. |
| Status | Active |

### D-17 메신저피싱 분기는 IA 스텁으로만 반영(T24 범위 밖)
| Item | Value |
|---|---|
| Decision | UX-015의 최상위 2분류에 "메신저피싱"을 노출하되(AC-028 상위 구조 충족), 그 하위 화면(채널 선택·카카오톡/문자 채팅 재현)·채널 간 전이는 이번 패스에서 설계하지 않고 T24로 이어지는 향후 확장 지점(스텁)으로만 둔다. T24 구현 전까지 메신저 카드는 "준비 중"으로 처리한다. |
| Reason | 태스크(T23) 범위가 "보이스 2분류 + 보이스 하위 clone/generic + 단계적 흐름 재설계"로 한정되고, 메신저 화면(T24)·전이(T25)는 별도 태스크로 명시적으로 분리됨. 상위 2분류 구조(AC-028)를 보이려면 메신저 항목의 존재는 필요하나, 하위를 임의 설계하면 스코프 크립. |
| Alternatives Considered | (1) 메신저 카드를 아예 숨겨 보이스만 노출. (2) 메신저 하위 화면까지 이번에 함께 설계. |
| Rejected Because | (1) AC-028이 "최상위 2분류"를 요구하므로 메신저를 숨기면 상위 구조가 성립하지 않음. (2) T24/T25 범위를 T23이 침범 — 사용자가 명시적으로 이번 범위 밖으로 지정. |
| Impact | Screen Catalog(UX-015 메신저 분기 스텁·"준비 중" 상태). IA(스텁 노드). Open Questions(OQ-U10: 메신저 라우팅 목적지·하위 구조는 T24에서 확정). |
| Status | **Superseded (2026-07-23, D-22 · T24 구현)** — 메신저 분기가 이제 실제 목적지 UX-024/UF-006로 연결됨. 스텁 판단 자체는 당시 T24 범위 밖 근거로 유효했다(이력 보존). |

### D-18 리플레이 해설을 UX-008 확장이 아닌 신규 화면(UX-018)으로 설계
| Item | Value |
|---|---|
| Decision | 사후 리플레이 해설(AC-038)을 UX-008(요약 리포트) 내부 탭/모드가 아니라 **별도 화면 UX-018**로 신설한다. 데이터(transcript·리포트 분석)는 UX-008과 공유하되 화면은 분리한다. |
| Reason | (1) 상호작용 모델이 다르다 — UX-008은 "무엇을·언제" 요약, UX-018은 대화를 처음부터 시간순으로 되짚으며 각 지점에 주석(스텝 내비게이션). 한 화면에 섞으면 어르신 인지부하↑. (2) 2인(AC-042)에서 정체 공개 직후 **강제 단계**로 독립 재사용되어야 하는데, 요약 리포트에 묶여 있으면 강제 진입 지점을 걸기 어렵다. (3) 보이스·메신저·2인 공통 재사용(AC-038)이라 독립 화면이 재사용에 유리. |
| Alternatives Considered | (1) UX-008에 "되짚어보기" 탭 추가. (2) UX-008 스크롤 하단에 인라인 섹션으로 병합. |
| Rejected Because | (1)·(2) 모두 2인 강제 단계 진입점을 명확히 걸기 어렵고, 요약과 대화 되짚기의 상호작용이 섞여 저인지부하 원칙에 반함. 단 데이터·분석은 재사용하므로 신규 백엔드는 없다(AC-038 "기존 인프라 재사용" 충족). |
| Impact | Screen Catalog(UX-018 신설). UF-002/UF-005 소속. UX-007·UX-008에 UX-018 인계 배선. Interaction Patterns(P-13). 신규 데이터 모델 없음(T9 재사용). |
| Status | Active |

### D-19 2인 소셜 훈련 흐름 구조(UF-004/005·사용자2 무로그인·무동의 차단)
| Item | Value |
|---|---|
| Decision | 2인 소셜을 **사용자1(UF-004)·사용자2(UF-005) 두 흐름**으로 분리 설계한다. 사용자1은 기존 로그인·클론·드릴다운을 재사용해 챌린지를 만들고(UX-019) 결과를 제한적으로 본다(UX-020). 사용자2는 **로그인 없이 링크로 진입**(AC-048)해 동의 랜딩(UX-021)에서 **명시적 탭 동의 후에만** 체험(UX-014 재사용)하고, 종료 시 강제 정체 공개(UX-007 재사용)+리플레이 해설(UX-018)을 받는다. 무동의로 복제 음성이 재생되는 경로는 UI·흐름에 두지 않는다(AC-040, P-15). |
| Reason | AC-040(사전 동의·무동의 차단)·AC-042(강제 정체 공개)·AC-043(결과 열람 제한)·AC-044(비동기)·AC-048(무로그인 링크)를 화면으로 실현. 발신/수신 역할·권한·데이터 접근이 근본적으로 달라 흐름 분리가 명확. 체험·종료·해설은 기존 화면(UX-014/007/018) 재사용으로 중복·하루 스코프 리스크 최소화. |
| Alternatives Considered | (1) 사용자1/2를 하나의 흐름으로 묶기. (2) 사용자2도 로그인 요구. (3) 2인 전용 통화·종료·해설 화면 신설. |
| Rejected Because | (1) 역할·권한이 달라 혼란. (2) AC-048이 "별도 계정/로그인 없이 링크만으로 진입"을 명시. (3) 기존 UX-014/007/018 재사용으로 충분하고 중복 화면은 스코프·유지비용 증가. |
| Impact | User Flows(UF-004/005 신설). Screen Catalog(UX-019/020/021 신설, UX-014/007/018 재사용 배선). Interaction Patterns(P-15). Architecture(challenge/consent/report 데이터·토큰은 T35). |
| Status | Active |

### D-20 챌린지 링크는 앱이 대신 전송하지 않고 OS 공유 시트+복사 폴백
| Item | Value |
|---|---|
| Decision | UX-019의 챌린지 링크 전달은 **앱이 카톡/문자를 대신 보내지 않고**, OS 공유 시트(`navigator.share`) 또는 링크 복사 UI만 제공해 사용자1이 직접 붙여 넣게 한다(P-14). |
| Reason | AC-044가 "사용자1이 실제 카톡/문자로 사용자2에게 전달"로 규정 — 앱 대리 전송은 실 메신저/전화망 연동(Out of Scope)에 해당하고, 사용자1이 수신자를 직접 고르는 편이 오남용·오배송 리스크가 낮다. 공유 시트는 어르신에게도 익숙한 OS 기본 UX. |
| Alternatives Considered | (1) 앱이 연락처 선택 후 대신 전송. (2) 링크 복사 버튼만(공유 시트 미사용). |
| Rejected Because | (1) 실 메신저/전화망 연동은 PRD Out of Scope + 오용 리스크. (2) 공유 시트가 가능한 환경에서 복사만 강요하면 단계↑ — 단 미지원 브라우저 폴백으로 복사는 반드시 병행(침묵 실패 금지). |
| Impact | Implementation(UX-019 공유/복사, navigator.share·Clipboard API). Interaction Patterns(P-14). |
| Status | Active |

### D-21 사용자1 결과 열람은 완료/의심 시점 수준으로 제한(대화 전문 화면 미설계)
| Item | Value |
|---|---|
| Decision | 사용자1의 챌린지 결과 열람(UX-020)은 **사용자2 동의 시에만·"완료 여부/의심 시점" 수준**으로만 표시하고, **대화 전문·상대 발화 원문을 여는 화면을 설계하지 않는다**(AC-043). |
| Reason | AC-043이 열람 범위를 명시적으로 "대화 전문이 아니라 완료 여부/의심 시점"으로 제한하고 사용자2 미동의 시 상세 차단을 요구. 상대의 대화 원문 노출은 프라이버시 침해이자 복제 음성 신뢰(차별점) 훼손. |
| Alternatives Considered | (1) 사용자1도 사용자2의 리플레이 해설 전문 열람. (2) 동의와 무관하게 완료 여부는 항상 공개. |
| Rejected Because | (1) AC-043 정면 위반. (2) AC-043이 "사용자2가 동의한 경우에만" 결과 열람을 허용 — 미동의 시 상세 감춤이 원칙. |
| Impact | Screen Catalog(UX-020 제한 상태). AC-043 준수. 대화 전문 열람 화면 부재(의도적 미설계). |
| Status | Active |

### D-22 메신저 표면을 단일 스킨 레이어 화면(UX-022)으로 — 카카오/문자 통합 (D-17 supersede)
| Item | Value |
|---|---|
| Decision | 카카오톡형·문자메시지형을 **각각 별도 화면으로 만들지 않고 하나의 메신저 채팅 셸(UX-022)의 표면(surface) 변형**으로 처리한다. 문자형은 iOS/삼성/기본 스킨을 UA 자동 감지+폴백+수동 토글로 전환. 스킨·표면은 프레젠테이션 전용이라 어떤 안전 판정도 게이팅하지 않는다. UX-015의 메신저 스텁(D-17)을 실제 목적지 UX-024/UF-006로 연결한다. |
| Reason | Architecture §13.4가 "표면=콘텐츠와 분리된 프레젠테이션 레이어", §13.5가 "스킨은 프레젠테이션 전용"으로 확정. 안전 셸(모의 표식·훈련 종료·면책·무해화 링크)·대화 콘텐츠·에스컬레이션은 표면과 무관하게 동일하므로, 표면별 화면을 이원화하면 안전 셸 스펙이 중복되고 유지비용·불일치 위험이 커진다. 단일 셸+스킨이 architect 구조와 정합하고 새 메커니즘을 만들지 않는다. |
| Alternatives Considered | (1) 카카오형·문자형 별도 화면 2개. (2) 문자 스킨별(iOS/삼성/기본)로도 화면 분리. |
| Rejected Because | (1)·(2) 모두 동일한 안전 셸·콘텐츠·링크·에스컬레이션 로직을 중복 정의하게 되고, §13.4 "표면=스킨 레이어" 구조와 어긋난다. 스킨은 외형 차이일 뿐이라 화면 분리가 부적절. |
| Impact | Screen Catalog(UX-022 단일 셸·UX-024 선택·UX-023 랜딩 신설). User Flows(UF-006). D-17 Superseded. UX-015 메신저 분기 연결. Interaction Patterns(P-16/P-17/P-19). Architecture §13.4/13.5 정합. |
| Status | Active |

### D-23 스미싱 링크는 displayText 탭 → 인앱 가짜 랜딩(UX-023), 실 URL·서버 전송 없음
| Item | Value |
|---|---|
| Decision | 메신저 대화의 스미싱 링크는 `displayText`(모의 표기)만 표시하고, 탭하면 `fakeLandingId`가 가리키는 인앱 가짜 랜딩(UX-023)으로만 이동한다. 가짜 랜딩의 입력값은 서버로 전송되지 않고(콜러블 부재) 가짜 피드백만 준다. |
| Reason | Architecture §13.4가 `attachments`에 실 URL 필드를 두지 않고 `displayText`+`fakeLandingId`만 두도록 확정(AC-045/032). 외부 네비게이션·실 전송을 **구조적으로** 제거(AC-023의 송금 금지와 동형)해야 오남용이 원천 차단된다. |
| Alternatives Considered | (1) 비활성 텍스트만 보이고 탭해도 아무 일 없음. (2) 실 URL을 저장하되 클라에서 이동만 막기. |
| Rejected Because | (1) "정보 입력 유도" 체험(AC-045 핵심)을 재현 못 함. (2) 실 URL을 스키마에 두면 유출·오이동 리스크 — architect가 URL 필드 자체를 배제. |
| Impact | Screen Catalog(UX-023 신설, UX-022 링크 탭 배선). Interaction Patterns(P-17). Architecture §13.4 정합. |
| Status | Active |

### D-24 카카오 면책 고지는 영구 비활성화 불가 상시 배너(UI 요건)
| Item | Value |
|---|---|
| Decision | 카카오톡형 표면(UX-022)에 "카카오톡 실제 서비스와 무관한 훈련용 재현" 면책 고지를 **영구 비활성화 불가·항상 노출·재진입 시에도 유지**되는 상단 고정 배너로 둔다(P-19). 데이터 필드가 아니라 UI 상시 요건. |
| Reason | AC-047 확정(면책 고지만 채택, 마케팅/스토어 제한 미채택) + Architecture §13.7이 "데이터 필드가 아닌 표면 컴포넌트 요건, 영구 비활성 불가"로 확정. 사용자가 닫거나 스크롤로 사라지면 상시 노출 요건 위반. |
| Alternatives Considered | (1) 최초 진입 시 1회 안내 후 사라짐. (2) 닫기 가능한 배너. |
| Rejected Because | (1)·(2) 모두 "상시 노출"(AC-047) 위반. §13.7이 영구 비활성 불가를 명시. |
| Impact | Screen Catalog(UX-022 카카오 표면 배너). Interaction Patterns(P-19). 법적 충분성 자체는 architect/법무 범위 밖(잔존 확인은 사용자/법무). |
| Status | Active |

### D-25 조건부 목소리 선택(UX-025)을 에스컬레이션 순간이 아니라 메신저 시나리오 선택 직후에 배치
| Item | Value |
|---|---|
| Decision | AC-046 조건부 목소리 선택(①즉시 녹음 ②기존 재사용 ③남/여 기본 보이스)을 **에스컬레이션이 발생하는 통화 전환 순간이 아니라, 메신저 시나리오 선택 직후(UX-024→UX-025→UX-022)**에 배치해 통화용 voiceId를 미리 확보한다. |
| Reason | Architecture §13은 전이를 "이음새 없이"(사기범이 전화를 거는 느낌) 요구한다. 전이 순간에 30초 녹음/목소리 선택을 삽입하면 몰입이 완전히 끊긴다. voiceId를 미리 확보해 두면 escalation 플래그 수신 즉시 통화 셸로 매끄럽게 넘어갈 수 있다(§13.6 "메신저→보이스 전이가 가능한 시나리오 진입 시" 결정과도 정합). |
| Alternatives Considered | (1) 에스컬레이션 발생 시점에 목소리 선택 표시. (2) 항상 ③기본 보이스로 고정(선택 생략). |
| Rejected Because | (1) 전이 몰입을 끊음(예방접종 효과 저해). (2) AC-046이 3경로 제공을 요구하고, 본인 클론(딥보이스 차별점)을 배제하게 됨. |
| Impact | Screen Catalog(UX-025 위치·Entry/Exit). User Flows(UF-006 Step 2·UF-007 Step 1). Architecture §13.6 정합. |
| Status | Active |

### D-26 에스컬레이션 전이 연출 — 서버 플래그 기반 3경로·이음새 없음·양방향 설계
| Item | Value |
|---|---|
| Decision | 메신저→보이스 전이 연출은 **서버가 전달하는 `escalation:{toChannel:"voice"}` 플래그만 보고** 시작하며(sentinel 토큰은 서버가 감지·제거해 화면에 노출 안 됨), 세 트리거(sentinel 자동/max-turn 폴백/명시 "전화로 확인" 버튼)를 모두 다룬다. 연출은 "사기범이 전화를 거는" 수신 화면으로 채팅을 전환해 기존 통화 셸 UX-014 incoming phase로 이음새 없이 인계한다. 연출 로직은 방향 무관하게 재사용 가능한 구조로 설계하되 화면은 메신저→보이스만 구현(AC-039). |
| Reason | Architecture §13.2가 "앱은 자유텍스트를 분류하지 않고 sentinel 신호만 신뢰, 사용자에게 토큰 미노출"을, §13.3이 폴백·명시 버튼 3경로를, §13.1이 방향 무관 엔진·MVP 단방향을 확정. UX는 이 확정 구조에 맞춰 "연출"만 설계하는 것이 정합이며 새 트리거 메커니즘을 만들면 안 된다(AC-024 인젝션 방어 불변). |
| Alternatives Considered | (1) 클라가 대화 텍스트로 의심을 직접 판정해 전이. (2) 전이 시 별도 확인 화면("통화로 넘어갈까요?"). |
| Rejected Because | (1) §13.2/AC-024 정면 위반(사용자 입력=데이터). (2) 확인 단계가 "사기범이 전화를 거는" 몰입을 끊음(P-11/P-18 이음새 원칙 위반) — 이탈은 "훈련 종료"로 언제든 가능하므로 별도 확인 불필요. |
| Impact | User Flows(UF-007). Screen Catalog(UX-022 escalation 상태·UX-014 Entry). Interaction Patterns(P-18). Architecture §13.1~13.3 정합. |
| Status | Active |

### D-27 메신저 2인 챌린지 발신 진입점 — 기존 "챌린지 만들기" 별도 메뉴 유지(시나리오 카드 통합 미채택)
| Item | Value |
|---|---|
| Decision | 메신저 시나리오를 지인에게 보내는 "지인에게 보내기(챌린지)" 결정 지점을 **기존 보이스 챌린지와 동일한 별도 "챌린지 만들기" 진입점**으로 유지한다. 메신저 시나리오 선택(UX-024) 카드에 "본인이 체험" vs "지인에게 보내기"를 나란히 놓는 통합 버튼은 채택하지 않는다. 챌린지 생성 흐름은 UX-015→UX-024(챌린지 생성 모드·비에스컬레이션만)→UX-019를 재사용한다. |
| Reason | (1) 보이스 모델(UF-004)이 이미 별도 진입점 패턴이라 메신저도 같은 진입점을 쓰면 발신자 멘탈 모델이 일관되고 IA 변경이 없다. (2) 시나리오 카드에 2개 CTA를 얹으면 모든 카드(대칭성 위해 보이스 UX-017 포함)에서 매 선택마다 결정 부하가 늘어 어르신 저인지부하 원칙에 반한다. (3) 카드 통합은 UX-017/UX-024 양쪽을 동시에 바꿔야 하는 더 큰 IA 변경이다. 사용자 요구("메신저도 지인에게 보내기")는 별도 진입점만으로 충족된다. |
| Alternatives Considered | (1) UX-024 시나리오 카드에 "본인 체험/지인에게 보내기" 통합 버튼(더 큰 IA 변경). (2) 별도 "챌린지 만들기" 진입점 유지(채택). |
| Rejected Because | (1) IA 변경 폭이 크고(보이스 카드까지 대칭 변경 필요) 카드당 CTA 증가로 인지부하↑ — 추가 복잡도가 사용자 요구 대비 정당화되지 않음. |
| Impact | User Flows(UF-004 메신저 변형 진입점). Screen Catalog(UX-024 챌린지 생성 모드 Exit→UX-019, UX-019 재사용). IA(2인 메신저 변형 다이어그램). |
| Status | **Superseded by D-30 (2026-07-24, T48 — 사용자 직접 지시로 반전). 이 결정의 "별도 진입점 유지·시나리오 카드 미통합"은 더 이상 유효하지 않다. 결정 텍스트는 이력 보존을 위해 남긴다.** |

### D-28 메신저 2인 챌린지 수신자 경험 — 채널 인지 동의 랜딩 + UX-022/023 무약화 재사용
| Item | Value |
|---|---|
| Decision | 메신저 챌린지의 사용자2 경험은 (1) UX-021 동의 랜딩을 **채널 인지 문구 변형**(메신저면 "메시지(카톡/문자)" 프레이밍, "복제 음성"·전화 프레이밍 제거)으로 재사용하고, (2) 동의 후 UX-014 대신 **UX-022(채팅 셸)+UX-023(가짜 랜딩)**으로 진입해 단독 훈련과 **동일한 안전속성(무해화·스킨·면책 배너·모의 표식·상시 종료)을 무약화 재사용**한다(AC-053/054). 동의 게이트(AC-040)·정체 공개(UX-007)·리플레이(UX-018)는 채널 무관이라 변형 없이 그대로. |
| Reason | AC-053이 "단독 훈련과 동일한 UX-022/023 안전속성 무약화 재사용"을, AC-040이 채널 무관 동의 게이트를 요구한다. 안전속성은 콘텐츠·표면 레이어라 세션 소유자가 익명 uid이든 무관(§13.4). 랜딩 문구만 음성 전제라 채널 인지 변형이 필요하다(음성 없는 챌린지엔 재생할 복제 음성이 없음). UX-007/UX-018 문구는 "훈련이었습니다"류라 음성 특정이 아니어서 메신저 변형이 불요임을 확인. |
| Alternatives Considered | (1) 메신저용 신규 동의 랜딩/채팅 화면 신설. (2) UX-021 문구를 음성·메신저 공용 중립 문구로 통일(채널 인지 변형 없이). |
| Rejected Because | (1) 신규 화면은 AC-053의 "동일 화면 무약화 재사용" 취지에 반하고 안전속성 이중 관리 리스크. (2) 음성 전제 문구("복제 음성 전화")를 메신저에 그대로 쓰면 부정확하고, 완전 중립화하면 보이스 챌린지의 몰입 고지가 약해짐 — 채널 인지 변형이 양쪽 정확성을 지킴. |
| Impact | User Flows(UF-005 메신저 변형). Screen Catalog(UX-021 채널 인지 문구·Exit 분기, UX-022/023 수신자 재사용·escalation 비활성). architect 인계(consentChallenge 메신저 변형·익명 uid messenger 세션). |
| Status | Active |

### D-29 메신저 2인 챌린지 발신자 결과 열람 — 완료 여부만(의심 시점 미노출)
| Item | Value |
|---|---|
| Decision | UX-020에서 발신자가 보는 메신저 챌린지 결과는 **"완료 여부"만**이며, 의심 시점(스미싱 링크 탭·가짜 랜딩 정보입력·에스컬레이션 도달)을 **어떤 형태로도 노출하지 않는다**(AC-055·OQ-31). 보이스 챌린지의 "완료 · 의심 시점: 약 N초"(AC-043)는 보이스에만 적용되고, 메신저 완료+동의 항목은 "완료 · 상대가 체험을 마침"(시점 없음)으로 표시한다. |
| Reason | OQ-31(2026-07-24 사용자 확정)이 메신저 챌린지의 발신자 열람을 "완료 여부만" 안전 기본값으로 확정했다(수신자 프라이버시 우선). UX-020 현행 Success 상태 (c)가 "의심 시점: 약 N초"를 보이므로 메신저 케이스에 그대로 두면 AC-055보다 더 세분한 정보를 노출하게 되어 정정이 필요했다. |
| Alternatives Considered | (1) 보이스와 동일하게 의심 시점 노출. (2) 완료 여부만(채택). |
| Rejected Because | (1) AC-055/OQ-31 정면 위반 — 스미싱 탭·정보입력 등은 수신자 프라이버시 민감 정보라 발신자 노출 금지로 확정됨. |
| Impact | Screen Catalog(UX-020 메신저 결과 상태·Business Rules). architect 인계(setChallengeResultSharing의 resultSummary를 메신저에서 완료 플래그만으로 파생). |
| Status | Active |

### D-30 챌린지 진입점 통합 — "본인이 체험 / 지인에게 보내기"를 시나리오 선택 직후 단일 결정 지점(UX-026)으로 (D-27 supersede)
| Item | Value |
|---|---|
| Decision | 자기훈련과 챌린지 생성의 진입을 **시나리오 선택 직후의 통합 선택 지점 UX-026("본인이 체험 / 지인에게 보내기")**으로 합친다. 발신자가 별도 "챌린지 만들기" 메뉴로 시작하던 경로(D-27)는 **은퇴**하고, 드릴다운→UX-026 "지인에게 보내기"가 챌린지 생성의 유일 경로가 된다. UX-026은 **인라인 카드 2-CTA가 아니라 시나리오 확정 뒤의 경량 후속 화면**이며, 보이스(UX-017 clone)·메신저(UX-024 비에스컬레이션) 양 채널 공통이다. "지인에게 보내기"가 성립하지 않는 시나리오(generic 보이스·에스컬레이션 가능 메신저)에서는 UX-026을 띄우지 않고 자기훈련으로 직행한다. |
| Reason | **사용자가 재작도된 IA를 직접 검토·확정해 D-27을 반전**(2026-07-24). 두 별도 진입 경로보다 "시나리오를 고른 뒤 이 시나리오로 무엇을 할지"를 한 지점에서 정하는 편이 발신자 멘탈 모델이 명료하다. 별도 후속 화면(인라인 2-CTA 아님)을 택한 것은 D-27이 지적한 "카드마다 CTA 2개 → 결정 부하 증가"를 피하면서도 통합 선택을 실현하기 위함(D-1 저인지부하) — 브라우징은 카드당 액션 1개로 두고 이분 선택은 확정 뒤 단 한 번의 전용 화면으로 격리. 챌린지가 시나리오 하나로 확정되므로(OQ-29/AC-051) 시나리오-무관 챌린지 진입점은 애초에 없어 별도 메뉴 은퇴가 어떤 흐름도 깨지 않는다. |
| Alternatives Considered | (1) D-27 유지(별도 "챌린지 만들기" 진입점). (2) UX-017/UX-024 시나리오 카드에 "본인 체험/지인에게 보내기" 인라인 2-CTA 통합. (3) 시나리오 확정 직후 별도 통합 선택 화면 UX-026(채택). (4) 별도 메뉴를 은퇴하지 않고 UX-026과 병행 유지(이중 경로). |
| Rejected Because | (1) 사용자가 명시적으로 반전을 지시. (2) 카드마다 CTA 2개는 매 카드에서 결정 부하를 늘려 저인지부하 원칙에 반함(D-27이 정확히 이 이유로 카드 통합을 거부했었고, 그 우려 자체는 여전히 타당 — 그래서 인라인이 아닌 별도 화면으로 실현). (4) 이중 경로는 UX-019로 가는 경로가 둘로 갈려 드리프트·중복 관리 위험이 크고, 시나리오-무관 진입점이 없어 병행의 실익이 없음 — 단일 경로로 은퇴. |
| Impact | User Flows(UF-003 종착 선택 단계 추가; UF-004 진입점·순서; UF-002/UF-006 자기훈련 진입 경로). Screen Catalog(**신규 UX-026**; UX-017·UX-024 Exit; UX-019 Entry; UX-024 챌린지-생성 모드 플래그 소거). IA(메인 다이어그램·2인 발신/수신 서브다이어그램 재작도, 별도 진입점 은퇴). Interaction Patterns(P-12 브레드크럼 계승). **architect 재방문 불요** — Architecture §14.8(T47)의 `createChallenge`/`consentChallenge`/스키마는 진입점 불문(entry-point-agnostic)이며 `scenario.channel`로만 채널을 정함. **AC 문안 변경 불요**(AC-044/AC-051은 이미 "시나리오 선택 후 챌린지 생성" 순서). AC-052/#21(에스컬레이션 메신저 챌린지)은 범위 밖·무영향. |
| Status | **위치 부분 Superseded by D-31 (2026-07-24, T54 — UX-026 위치가 "시나리오 선택 직후"→"유형 선택 직후"로 상향, 두 채널 상시 노출). 단 "별도 챌린지 만들기 진입점 은퇴·통합 선택 지점·인라인 2-CTA 대신 전용 화면" 원칙은 D-31에 그대로 계승·유효.** |

### D-31 체험/발송 선택(UX-026)을 유형 선택 직후로 상향 (AC-056 · D-30 위치 부분 supersede)
| Item | Value |
|---|---|
| Decision | "본인이 체험 / 지인에게 보내기" 이분 선택(UX-026)을 **유형 선택(UX-015) 직후**로 상향한다 — 목소리 방식 선택(UX-016)·시나리오 선택(UX-017/UX-024)보다 **먼저**. **신규 Screen ID를 만들지 않고 UX-026의 위치만 상향**한다(ID 불변 규약 — 화면 의미 불변, 흐름 위치만 앞당김). UX-026은 보이스·메신저 **두 채널 모두에서 항상** 노출되며, 여기서 고른 모드(self/send)가 하류의 UX-016 노출 여부·UX-017/UX-024 시나리오 필터·최종 Exit를 좌우한다. D-30의 "별도 진입점 은퇴·통합 선택 지점·인라인 아닌 전용 화면" 원칙은 계승. |
| Reason | 사용자가 라이브 앱 테스트로 T50 기획(PRD v1.4 AC-056)의 미구현을 확인하고 재구조화를 지시. "이 유형을 내가 체험할지, 지인에게 보낼지"를 시나리오·방식보다 먼저 정하는 편이 (1) 메신저에서 "지인에게 보내기"가 시나리오 선택 뒤에야 나오던 버그(사용자 신고: 메신저 챌린지 선택 지점이 너무 늦음)를 근본적으로 해소하고, (2) 보이스에서 self=generic전용·send=clone/generic이라는 **모드가 이후 노출 시나리오 집합 자체를 결정**하므로 모드를 먼저 물어야 필터가 성립하며(AC-057/058), (3) 채널 무관 단일 결정 지점이라 발신자 멘탈 모델이 명료하다. ID를 새로 만들지 않고 위치만 옮긴 것은 프로젝트의 "ID 불변" 규약과 중복 ID 지양 원칙에 따른 것 — UX-026을 가리키던 모든 참조(UX-017/UX-024 Exit·UX-019 Entry·IA)는 어차피 위치 이동으로 함께 갱신된다. |
| Alternatives Considered | (1) D-30 유지(시나리오 선택 직후). (2) 유형 선택 직후에 **신규 Screen ID**를 만들고 UX-026은 Deprecated. (3) 유형 선택 직후로 UX-026 위치만 상향(채택). |
| Rejected Because | (1) AC-056이 명시적으로 "유형 선택 직후·방식/시나리오보다 먼저"를 요구 — D-30 위치는 이를 위배. (2) 같은 "본인이 체험/지인에게 보내기" 화면을 두 ID로 두는 것은 중복이고 ID 불변 규약상 위치 재정의로 충분 — 신규 ID는 참조 그래프만 복잡하게 함. |
| Impact | User Flows(UF-003 순서 재편; UF-002/004/006 진입 순서). Screen Catalog(UX-015 Exit→UX-026; UX-026 Entry/Exit 4분기; UX-016/UX-017/UX-024가 모드에 종속). IA(메인·2인 서브다이어그램 재작도). **architect 신규 인계**: 모드(self|send) 클라 상태 배선(UX-016 노출·UX-017/UX-024 필터·최종 라우팅). §14.8 스키마는 entry-point-agnostic이라 무영향. |
| Status | Active (partially supersedes D-30 — 위치만) |

### D-32 보이스 + 본인이 체험 = generic 전용·clone 배제·UX-016 스킵 (AC-057)
| Item | Value |
|---|---|
| Decision | 보이스 유형에서 "본인이 체험"을 고르면 **generic 시나리오만** 노출하고 clone(본인 목소리 복제) 시나리오는 자기 체험에서 완전히 배제한다. 목소리 방식이 generic으로 강제되므로 **UX-016(clone/generic 선택)을 노출하지 않고** UX-026("본인이 체험")→UX-017(generic 필터)→통화(UX-014)로 직행한다. clone 딥보이스 체험은 오직 "지인에게 보내기" 2인 챌린지로만 도달한다. |
| Reason | AC-057(사용자 확정) — 자기 목소리를 복제해 자기에게 거는 것은 "그 목소리를 아는 사람이 받는다"는 딥보이스 착시가 성립하지 않아 훈련 효과가 없다. UX-016을 건너뛰는 것은 프로젝트의 기존 "무의미한 1-옵션 화면 방지" 원칙(D-1, D-30이 generic 시나리오에서 UX-026을 안 띄운 것과 동형) — self 모드에서 방식이 하나뿐이면 그 선택 화면은 인지부하만 늘린다. clone 콘텐츠·녹음·딥보이스 재생 메커니즘(UX-002/003/014)은 삭제하지 않고 챌린지 경로(UF-004 발신·UF-005 사용자2 수신)에서 그대로 재사용한다. |
| Alternatives Considered | (1) 보이스 self에서도 UX-016을 보여주되 clone을 비활성/안내(1-옵션 화면). (2) UX-016 스킵·generic 직행(채택). |
| Rejected Because | (1) 선택지가 하나뿐인 화면은 저인지부하 원칙(D-1) 위배이자 "왜 clone은 못 고르나" 혼란만 유발 — AC-057은 clone을 self에서 아예 없애라는 것이므로 노출 자체가 부적절. |
| Impact | User Flows(UF-002 보이스=generic 전용, clone 자기훈련 경로 소멸; UF-003 Steps). Screen Catalog(UX-016 self 미노출·UX-017 self=generic 필터). IA. clone 메커니즘 화면(UX-002/003/014)은 무변경(챌린지에서 재사용). |
| Status | Active |

### D-33 generic 보이스 챌린지 신설 — UX-016을 send 경로에서 노출·UX-019 clone/generic 카피 구분 (AC-058)
| Item | Value |
|---|---|
| Decision | 보이스 "지인에게 보내기"에서는 **UX-016(clone/generic)을 노출**하고 두 방식 모두 챌린지로 보낼 수 있다. **generic 보이스 챌린지는 신규 능력** — 클론 온보딩(UX-002/003)을 건너뛰고 GENERIC_VOICE_ID 폴백으로 성립(voiceId 없음). UX-019(챌린지 만들기)는 챌린지 종류별 **카피를 구분**한다: clone="내 목소리(딥보이스)로 걸려오는 전화를 보냅니다", generic 보이스="기본 AI 음성으로 걸려오는 전화를 보냅니다", 메신저="메시지(카톡/문자)를 보냅니다". |
| Reason | AC-058 — 기존 "보이스 챌린지=항상 clone/딥보이스" 전제를 확장. generic 보이스 챌린지는 클론·voiceId·통화 자격증명 경로를 안 타는 "깨끗한 부분집합"(음성 없는 메신저 챌린지 AC-051과 동형)이라 신규 메커니즘 없이 성립한다. 카피 구분은 발신자가 "내 목소리가 가는지, 기본 음성이 가는지"를 명확히 인지해야 하기 때문(clone은 AC-041 유출 차단 대상이고 generic은 아님 — 사용자에게도 차이가 실질적). |
| Alternatives Considered | (1) 보이스 챌린지를 clone으로만 유지(AC-058 미실현). (2) 두 방식 카피를 동일하게(음성 소스 차이 은폐). (3) UX-016 노출 + 종류별 카피 구분(채택). |
| Rejected Because | (1) AC-058이 generic 보이스 챌린지를 명시 요구. (2) clone/generic은 "내 목소리 여부"라는 사용자 체감·안전 프로파일이 실제로 달라(AC-041 적용 여부) 카피를 통일하면 오인 유발. |
| Impact | Screen Catalog(UX-016 send에서 clone·generic 둘 다 유효; UX-017 send generic Exit=클론 스킵→UX-019; UX-019 3종 카피·voiceId 옵셔널). User Flows(UF-004 generic 보이스 챌린지 분기). **architect 인계**: generic 보이스 챌린지 스키마 판별(channel="voice"+voiceId 부재+voiceMode="generic")·사용자2 통화 GENERIC_VOICE_ID 라우팅(AC-058 flag, §14.8). |
| Status | Active |

### D-34 generic 보이스 챌린지 결과 열람 = "완료 여부만" (OQ-32 planner default 적용)
| Item | Value |
|---|---|
| Decision | generic 보이스 챌린지의 발신자 결과 열람(UX-020)은 planner 권고 default인 **"완료 여부만"**(메신저 챌린지 AC-055와 동일)을 적용한다. 즉 "완료 · 의심 시점: 약 N초"는 **clone 보이스 챌린지에만** 적용되고, generic 보이스·메신저 챌린지는 "완료 · 상대가 체험을 마침"(시점 없음)으로 묶는다. **default임을 명시**하며(OQ-U13), 사용자가 명시적으로 뒤집으면 clone과 동일하게 되돌린다. |
| Reason | OQ-32 planner 권고: (1) "완료 여부만"은 복제 음성 유출 리스크가 아니라 **수신자 프라이버시**를 위한 것(OQ-31)이라 clone 유출 위험이 없는 generic 보이스에도 동일 동기가 적용된다, (2) generic 보이스 챌린지는 음성 없는 메신저 챌린지와 동일 안전·프라이버시 계층이라 같은 열람 규칙으로 묶는 것이 일관적이고 "챌린지 종류마다 열람 범위가 제각각"인 혼란을 피한다, (3) 프로젝트의 "안전 기본값 우선" 패턴과 정합. 사용자가 T50 이후 OQ-32를 명시적으로 재검토하지 않았으므로 블로킹 대신 default 적용(OQ-U13). |
| Alternatives Considered | (1) clone 챌린지와 동일하게 의심 시점까지 노출(generic은 유출 위험 없으니). (2) 완료 여부만(planner default, 채택). |
| Rejected Because | (1) 유출 위험 부재가 곧 "의심 시점 노출 허용"으로 이어지지 않음 — 열람 제한의 동기는 수신자 프라이버시(OQ-31)라 채널·음성모드 무관. 단 사용자 확정 시 뒤집을 수 있는 default로 남김(OQ-U13). |
| Impact | Screen Catalog(UX-020 generic 보이스=완료 여부만·Business Rules). User Flows(UF-004 결과 열람). **architect 인계**: resultSummary를 generic 보이스에서 완료 플래그만으로 파생(메신저 파생 로직과 동형). |
| Status | Active (OQ-32 default — 사용자 확정 시 재검토 가능, OQ-U13) |

### D-35 통화 중 문자는 라우트 이동이 아니라 통화 위 오버레이(UX-027) — 통화 유지가 기능의 전부
| Item | Value |
|---|---|
| Decision | 통화 중 도착하는 문자는 **통화 화면(UX-014) 위에 겹치는 오버레이(신규 UX-027)**로만 보여준다. 메신저 화면(UX-022)으로 **이동하지 않는다**. 통화 셸(발신자·경과 타이머·"통화 중"·AI 합성 표식·"훈련 종료")은 오버레이 위에도 계속 보이고, 세션 타이머와 사기범 오디오는 멈추지 않는다. 오버레이가 열린 동안 **자동청취(마이크)만 일시 게이팅**한다. |
| Reason | 사용자 요구가 *"문자로 전환되는데, **전화 연결은 유지되도록**"*이라 통화 유지가 이 기능의 본질이다. 그리고 코드 제약이 이를 강제한다 — `/session/play`(통화)와 `/session/messenger`(메신저)는 **별도 라우트**라 이동하면 통화 컴포넌트가 언마운트되어 실시간 세션·오디오 컨텍스트·마이크가 끊긴다. 즉 "문자 화면으로 갔다가 돌아온다"는 설계는 기술적으로 통화를 죽이고, 사용자가 원한 경험을 정확히 반대로 만든다. 오버레이는 이 프로젝트에 이미 있는 패턴이기도 하다 — UX-023(가짜 랜딩)이 라우트가 아니라 in-page `role="dialog" aria-modal` 모달로 구현돼 있어(`src/components/MessengerFakeLanding.tsx`) 검증된 선례를 따르는 것이다. 자동청취만 게이팅하는 이유는 사용자가 화면을 읽는 동안 주변 소리를 발화로 오인하는 문제(R-1이 지적한 우려)를 피하되, 통화가 멈춘 것처럼 보이면 안 되기 때문이다. |
| Alternatives Considered | (1) 문자 도착 시 메신저 화면(UX-022)으로 라우트 이동 후 "통화로 돌아가기". (2) 통화 화면 안에 문자 영역을 **분할 화면**으로 상시 배치. (3) 통화 위 오버레이(채택). |
| Rejected Because | (1) 실시간 세션·마이크가 끊겨 사용자 요구("전화 연결 유지")를 정면으로 위배한다 — 기술 제약이 곧 기각 사유. (2) 통화 화면의 몰입(D-11/D-12의 "하나의 통화")을 상시 반으로 쪼개, 문자가 오지 않는 대부분의 시간에도 화면을 복잡하게 만든다(저인지부하 D-1 위배). |
| Impact | Screen Catalog(**UX-027 신설**; UX-014에 `sms-arrived` 배너·"문자함"(N) 컨트롤·`sms-overlay-open` state·자동청취 게이팅 추가). User Flows(**UF-008 신설**; UF-002/UF-005/UF-007에서 성립). IA(통화 셸 위 계층). Interaction Patterns(**P-20 신설**). **architect 인계**: 오버레이 열림 중 실시간 세션 유지 방식·자동청취 게이팅 지점(OQ-U16). |
| Status | Active |

### D-36 문자 도착 트리거는 사기범 턴에 실린 구조화 마커 — UX는 자유텍스트를 분류하지 않는다
| Item | Value |
|---|---|
| Decision | 문자 도착은 **사기범 턴에 실린 고정 구조화 마커를 서버가 파싱해 구조화된 형태로 전달**하는 것으로만 트리거한다. 클라이언트/UX는 사기범 대사나 사용자 발화를 자유텍스트로 분류해 "문자를 보냈나 보다"라고 판단하지 않는다. 결과적으로 사기범이 "문자 보냈어요"라고 **말하는 그 턴**이 곧 도착 시점이 되어 발화와 도착이 하나의 사건으로 묶인다. |
| Reason | 이 프로젝트에는 이미 동일한 문제를 푼 검증된 패턴이 두 개 있다 — 에스컬레이션 sentinel(`[[SIGNAL:ESCALATE_VOICE]]`, §13.2)과 스미싱 링크 마커(`[[LINK:id]]`, `functions/src/roleplay/linkMarker.ts`). 둘 다 **어시스턴트 출력에만 있는 고정 마커를 서버가 스캔·제거하고 구조화된 의미로 변환**하며, **사용자 입력은 절대 신호로 해석하지 않는다**(AC-024 인젝션 방어). 새 메커니즘을 발명하는 대신 이 패턴을 그대로 쓰는 것이 안전 경계를 넓히지 않는 가장 값싼 방법이다. 발화와 도착의 동시성도 자연히 따라온다 — 같은 턴이 곧 같은 사건이므로 별도 동기화 로직이 필요 없다. |
| Alternatives Considered | (1) 클라이언트가 사기범 대사를 키워드("문자 보냈", "링크 보냈")로 감지해 문자를 띄운다. (2) 시나리오에 "N번째 턴에 문자 도착" 고정 스케줄을 박는다. (3) 어시스턴트 턴의 구조화 마커(채택). |
| Rejected Because | (1) 자유텍스트 분류는 AC-024 방어 원칙과 정면 충돌하고(§13.2가 명시적으로 배제), LLM 표현 변화에 취약하다. (2) 고정 스케줄은 대화 흐름과 무관하게 문자가 튀어나와 "사기범이 방금 말한 것"과 어긋난다 — 사용자가 요구한 "말한 시점과 도착의 연결"이 깨진다. |
| Impact | Screen Catalog(UX-027 Entry·Business Rules; UX-014 배너 state). User Flows(UF-008 Trigger·Failure (a) — 마커 없이 문자 발송 대사만 나오면 문자는 오지 않으며 UX가 이를 보정하지 않는다 = **콘텐츠 저작 책임**). **architect·implementer 인계**: 마커 종류·파싱 위치·시나리오 프롬프트의 마커 지시(OQ-U16). |
| Status | Active |

### D-37 오버레이 안의 링크는 기존 인앱 가짜 랜딩(UX-023·AC-045)을 그대로 재사용 — 신규 랜딩 미설계
| Item | Value |
|---|---|
| Decision | UX-027의 링크 칩을 탭하면 **기존 UX-023(스미싱 링크 인앱 가짜 랜딩)**으로 전환하고, 닫으면 문자 오버레이 → 통화로 되돌아온다. 통화 맥락 전용 랜딩 화면을 새로 만들지 않는다. 링크는 기존과 동일하게 **표시 텍스트만 있고 실 URL이 없으며**, 입력값은 서버로 전송되지 않는다(AC-045/AC-032). |
| Reason | 재사용이 성립하는지 코드로 확인했다 — UX-023은 라우트가 아니라 **in-page 오버레이 컴포넌트**(`role="dialog" aria-modal`, `src/components/MessengerFakeLanding.tsx`)이고 어떤 API 함수도 import하지 않으며 "확인" 버튼이 로컬 state만 바꾼다. 즉 **통화 화면 위에서도 그대로 동작하고, AC-045의 구조적 금지(콜러블 부재)가 맥락과 무관하게 유지된다**. 또 사용자 입장에서 "문자 링크를 눌렀더니 가짜 로그인 페이지가 뜬다"는 경험은 메신저에서든 통화 중이든 **동일한 학습 내용**이라 화면을 나눌 이유가 없다. |
| Alternatives Considered | (1) 통화 맥락 전용 가짜 랜딩을 신규 Screen ID로 만든다. (2) 통화 중에는 링크를 아예 넣지 않고 계좌·인증번호 문자만 쓴다. (3) UX-023 재사용(채택). |
| Rejected Because | (1) 화면·안전 검증(무해화·표식·서버 미전송)을 두 벌 유지해야 하고, 한쪽만 약해지는 드리프트 위험이 생긴다 — 안전 코드 경로는 하나로 두는 것이 이 프로젝트의 일관된 원칙(§14.8/§14.9의 "깨끗한 부분집합" 판단과 동형). (2) 사용자가 요구한 원문이 *"문자 메세지로 **링크 형태로** 보내고"*라 링크를 빼면 요구 자체가 사라진다. |
| Impact | Screen Catalog(UX-027 Exit·Navigation Targets → UX-023; UX-023 자체는 **무변경** — 통화 맥락에서 닫으면 문자 오버레이로 복귀한다는 복귀 지점만 호출자가 정한다). Interaction Patterns(P-17 계승, P-20 적용). |
| Status | Active |

### D-38 인증번호(OTP) 문자를 UX-027의 콘텐츠 종류로 처리 — 사용자 신고 "인증번호가 아무데도 안 보인다" 해소
| Item | Value |
|---|---|
| Decision | 사기범이 "화면에 뜬 인증번호를 불러 주세요"라고 요구하는 경우, **UX-027에 6자리 모의 인증번호 문자가 실제로 도착**한다. 신규 화면을 만들지 않고 **문자 오버레이의 콘텐츠 종류 3종(계좌·요구형 / 링크형 / 인증번호형) 중 하나**로 처리한다. 코드·발신번호·본문(“타인에게 알려주지 마세요” 문구 포함)은 전부 무해화 모의값이며 실제 인증 체계와 어떤 연결도 없다. **앱이 자동으로 클립보드에 복사하지 않는다.** |
| Reason | 사용자 신고 *"화면에 뜨는 인증번호를 불러달라는데 아무것도 보이는 게 없다"*는 **훈련의 결정적 순간이 통째로 비어 있던 문제**다 — 인증번호를 불러주는 그 행위가 곧 속는 순간인데, 볼 것이 없으니 사용자는 무엇을 하는지도 모른 채 지나간다. 문자 오버레이가 생기면 이 케이스는 **추가 설계 없이 콘텐츠 종류 하나로 해결**된다(신규 화면 = 인지부하·유지비 증가, D-1). "타인에게 알려주지 마세요"라는 경고 문구까지 그대로 재현하는 것은, 사기범이 **그 경고를 무시하라고 요구하는 모순**이 학습 포인트 그 자체이기 때문이다. 클립보드 자동 복사를 하지 않는 이유는 앱이 "복사해서 어딘가에 붙여 넣는" 실행 동선을 대신 만들어 주지 않는다는 안전 경계(AC-023/§0.4 취지)와 정합하기 위해서다. |
| Alternatives Considered | (1) 인증번호를 통화 화면에 직접 오버레이 없이 띄운다. (2) 인증번호 전용 신규 화면. (3) 시나리오 대사에서 인증번호 요구 자체를 제거한다. (4) 문자 오버레이의 콘텐츠 종류로 처리(채택). |
| Rejected Because | (1) "문자로 왔다"는 맥락이 사라져 실제 수법 재현이 깨지고, 통화 화면에 표시 요소가 하나 더 상주하게 된다. (2) 인증번호만을 위한 화면은 문자 오버레이와 90% 동일해 중복이다. (3) 요구를 제거하면 실제로 가장 흔한 수법 하나를 훈련에서 통째로 잃는다 — 사용자 신고의 취지는 "요구를 없애라"가 아니라 "보이게 하라"였다. |
| Impact | Screen Catalog(UX-027 States의 콘텐츠 종류 ③·Accessibility의 코드 낭독 규칙). User Flows(UF-008 Step 4). **콘텐츠 인계**: 인증번호 요구 시나리오가 마커를 함께 내도록 저작 필요(OQ-U16). |
| Status | Active |

### D-39 즉시 되감기(UX-028)를 UX-018 확장이 아닌 신규 화면으로 — "읽는 복기"와 "다시 해보기"는 다른 화면
| Item | Value |
|---|---|
| Decision | 속은 순간을 **다시 연기해 보는 재연 드릴**을 신규 화면 **UX-028**로 만든다. 기존 UX-018(리플레이 해설)의 확장으로 넣지 않는다. 역할 분담: **UX-018 = 전체 대화를 시간순으로 읽으며 주석 보기(입력 없음)**, **UX-028 = 속은 한 순간으로 되돌아가 직접 다시 답하고 즉시 판정받기(입력 있음)**. |
| Reason | 사용자 요구가 *"'이 순간 뭐라고 했어야 했나'를 그 자리에서 **한 턴만 다시 해보기**"*로, 핵심이 "읽는 것"이 아니라 "해보는 것"이다. 세 가지가 다르다: (1) **상호작용 모델** — 열람 화면에 입력·판정·재시도를 섞으면 화면 성격이 흐려진다(D-18이 UX-018을 UX-008에서 분리한 것과 같은 논리). (2) **대상 범위** — 전체 대화 vs 단일 순간. (3) **강제성** — UX-018은 2인 사용자2에게 **강제 단계**(AC-042)라, 그 안에 "답을 해야 하는 드릴"을 넣으면 강제 단계가 사용자에게 입력을 요구하는 것으로 성격이 바뀐다(안전 단계를 건드리는 변경이라 피한다). |
| Alternatives Considered | (1) UX-018에 "여기서 다시 답해보기" 인라인 입력을 붙인다. (2) UX-008 리포트 안에 인라인 드릴. (3) 신규 화면 UX-028(채택). |
| Rejected Because | (1) 2인 강제 해설(AC-042)의 성격이 바뀌고, 전체 대화를 훑는 흐름 중간에 입력·판정 대기가 끼어 읽는 리듬이 끊긴다. (2) 리포트는 요약 열람 화면이라 입력·재시도 상태를 담기에 부적절하고, D-18이 이미 같은 이유로 UX-018을 분리했다. |
| Impact | Screen Catalog(**UX-028 신설**; UX-007·UX-008·UX-018·UX-030에 진입점 추가). User Flows(**UF-009 신설**; UF-002/005/006에서 연결). IA(종료 이후 학습 3갈래). Interaction Patterns(**P-21 신설**). **architect 인계**: 재연의 실행 모델·판정 주체·원 리포트 불변식 보호(OQ-U17). |
| Status | Active |

### D-40 되감기는 `deceivedMoments`가 있을 때만 — 안 속았을 때 "near-miss"를 발명하지 않는다
| Item | Value |
|---|---|
| Decision | 되감기 진입점은 리포트의 `deceivedMoments`가 **1건 이상일 때만** 노출한다. 한 번도 속지 않은 세션에서는 진입점을 **띄우지 않고**, 기존대로 "속지 않았습니다"(AC-009)를 명시한 뒤 **UX-018(리플레이 해설)에서 "잘 대응한 지점"을 보게** 안내한다. "아깝게 넘어갈 뻔한 순간(near-miss)" 같은 개념은 **만들지 않는다**. |
| Reason | 되감기의 데이터 원천은 리포트 분석 산출물인 `deceivedMoments`(turnIndex·timeLabel·tactic·correctAction)뿐이다. "속을 뻔한 순간"은 현재 어떤 분석 출력에도 존재하지 않으므로, 이를 화면에 만들려면 **신규 분석 개념·신규 판정 기준·신규 데이터**가 필요하다 — 그것은 UX가 임의로 정할 사안이 아니라 planner(무엇을 near-miss로 볼 것인가)와 architect(어떻게 산출할 것인가)의 결정이다. 없는 데이터를 그럴듯한 화면으로 메우는 것은 이 문서의 "임의 결론 금지" 원칙 위반이다. 안 속은 사용자에게 보여줄 것이 없는 것도 아니다 — UX-018이 이미 "잘 대응한 지점"과 "시도된 수법"을 다루도록 설계돼 있어(AC-009 정합) 자연스러운 대안 목적지가 존재한다. |
| Alternatives Considered | (1) 안 속았을 때도 "가장 압박이 셌던 순간"을 골라 되감기를 제공한다. (2) 되감기를 항상 노출하되 비활성 + 사유 표기. (3) 진입점 미노출 + UX-018 안내(채택). |
| Rejected Because | (1) "가장 압박이 셌던 순간"을 판정할 데이터·기준이 없다 — 발명이다(OQ-U20/R-8로 사용자 판단에 넘김). (2) 항상 보이는 비활성 버튼은 "내가 뭔가 못 하고 있다"는 인상을 줘 안 속은 사용자(=잘한 사용자)에게 부정적 피드백이 된다(P-8 완화 톤 위배). |
| Impact | Screen Catalog(UX-007/UX-008 진입점 조건부 노출; UX-028 Empty 상태). User Flows(UF-009 Alternative (a)). **planner/User 판단 대기**: near-miss 개념 신설 여부(OQ-U20·R-8). |
| Status | Active |

### D-41 난이도 선택(UX-029)을 드릴다운 마지막 단계 — 시나리오 선택 직후·세션/챌린지 생성 직전
| Item | Value |
|---|---|
| Decision | 사용자가 고르는 난이도(**초급/중급/고급**)를 신규 화면 **UX-029**로 만들고, 드릴다운의 **마지막 단계**(시나리오 확정 직후, 세션·챌린지 생성 직전)에 둔다. 순서: 유형(UX-015) → 체험/발송(UX-026) → [보이스+발송]방식(UX-016) → 시나리오(UX-017/UX-024) → **난이도(UX-029)** → 세션 또는 UX-019. 기본 강조는 **중급**. |
| Reason | 난이도는 "어떤 상황인가"(시나리오)가 아니라 "그 상황을 얼마나 세게 겪을 것인가"라, **시나리오를 정한 뒤에 묻는 것이 자연스러운 질문 순서**다. 시나리오보다 앞에 두면 두 가지 문제가 생긴다: (1) 난이도가 시나리오 목록을 **필터링한다는 오해**를 준다(실제로는 모든 시나리오에 3단계가 다 있어 아무것도 감추지 않는다), (2) 아직 어떤 상황인지 모르는 상태에서 강도를 고르게 되어 판단 근거가 없다. 반대로 세션 시작 후(통화 중 조절)로 미루면 몰입을 깨고 안전장치 표면이 늘어난다. **모드(self/send)가 시나리오 집합을 실제로 갈라놓기 때문에 UX-026을 앞에 둔 D-31과는 정반대 상황**이다 — 난이도는 집합을 가르지 않으므로 앞에 둘 이유가 없다. send 모드에서도 같은 자리에 두는 이유는 발신자가 "상대가 겪을 강도"를 시나리오와 함께 정하는 것이 하나의 결정 묶음이기 때문이다. |
| Alternatives Considered | (1) 유형 선택 직후(UX-026 근처)에 난이도를 먼저 묻는다. (2) 시나리오 카드마다 난이도 3개 버튼을 인라인으로 붙인다. (3) 세션 중 조절. (4) 시나리오 선택 직후 전용 화면(채택). |
| Rejected Because | (1) 상황을 모르는 상태의 강도 선택은 판단 근거가 없고, 목록 필터로 오해된다. (2) 카드마다 CTA 3개는 D-27/D-30이 이미 기각한 "카드당 결정 부하 증가" 문제의 재발이다(브라우징 단계는 카드당 액션 1개 유지). (3) 세션 중 조절은 몰입을 깨고 "난이도를 낮췄더니 안전장치도 바뀐 것 아닌가"라는 오해 표면을 만든다. |
| Impact | Screen Catalog(**UX-029 신설**; UX-017/UX-024 Exit에 삽입; UX-019에 난이도 요약·고급 고지; UX-021에 난이도 표시; UX-014/UX-022에 난이도 배지). User Flows(UF-003 단계 추가; UF-002/004/006 진입 경로). IA. Interaction Patterns(**P-22 신설**). **architect 인계**: 난이도 데이터 모델·세션/챌린지 필드(OQ-U18). |
| Status | Active |

### D-42 난이도는 어떤 안전장치도 게이팅하지 않는다 — 압박 강도만 바꾸고 무해화 경계는 고급에서도 동일
| Item | Value |
|---|---|
| Decision | 난이도가 바꾸는 것은 **압박 강도·요구 도달 속도·수법의 은밀함**뿐이다. 사전 고지(AC-012)·명시적 동의(AC-017)·상시 종료(AC-006)·합성/모의 표식(AC-022/032)·종료 고지(AC-015/023)·생성물 폐기(AC-021)·2인 4대 안전제약(AC-040/041/042/043)·면책 배너(AC-047)·활성 챌린지 상한(AC-049)·결과 열람 범위(AC-043/055)는 **세 난이도에서 완전히 동일**하다. **무해화 경계도 고급에서 완화되지 않는다** — "고급 = 더 진짜 같은 압박"이지 "고급 = 더 진짜에 가까운 위험 정보"가 아니다(실계좌·실링크·실행 정보 미노출, AC-005/013/032/033 불변). |
| Reason | 이 프로젝트에는 같은 형태의 원칙이 이미 두 번 확립돼 있다 — 메신저 **스킨은 프레젠테이션 전용이라 어떤 안전 판정도 게이팅하지 않는다**(§13.4/D-22), 그리고 **안전장치는 등급·티어 무관 전 사용자 동일 강제**(AC-050). 난이도는 세 번째 사례이고, 여기서 예외를 만들면 "고급에서는 종료 버튼이 덜 보인다" 같은 설계가 언젠가 정당화된다. 특히 위험한 오해는 "고급 = 실제 사기와 더 가깝다 = 실제 정보에 더 가깝다"인데, 이는 제품의 존재 근거(약화된 공격에 안전하게 노출)를 무너뜨린다 — 그래서 **고급이 세게 만드는 것은 심리적 압박이지 정보의 실행 가능성이 아니다**를 결정으로 못 박는다. 고급 선택 시 심리적 부담을 **사전 고지**하는 것은 안전장치를 늘리는 방향이라 이 원칙과 모순되지 않는다. |
| Alternatives Considered | (1) 고급에서 일부 안내·표식을 줄여 몰입을 높인다. (2) 고급에서만 더 구체적인 계좌·링크 표현을 허용한다. (3) 안전장치 전면 동일(채택). |
| Rejected Because | (1) AC-006/012/022/050을 직접 약화하는 설계라 **범위 밖**이다 — 안전 게이트는 ux-design이 조정할 수 있는 변수가 아니다. (2) 무해화(AC-005/032/033)의 정면 위반이며, "훈련용 산출물이 그대로 사기 대본이 되지 않는다"는 경계를 무너뜨린다. |
| Impact | Screen Catalog(UX-029 Business Rules; UX-019/UX-021의 난이도 표기가 안전 정책에 무영향). User Flows(UF-003/004/005). **architect 인계**: 난이도 모디파이어가 `guardrailPreamble`·무해화 제약 뒤로 밀려나지 않도록 프롬프트 조립 순서 주의(OQ-U18). |
| Status | Active |

### D-43 초급의 "힌트"는 세션 전 사전 브리핑 — 대화 중 실시간 판정 표시는 하지 않는다(D-6 유지)
| Item | Value |
|---|---|
| Decision | 초급의 학습 보조("티가 나는 수법")는 **① 사기범이 실제로 더 노골적으로 행동하는 것**과 **② 세션 시작 전 사전 브리핑 카드**("이 대화에서는 ○○·○○ 같은 신호가 나옵니다")로 제공한다. **대화 중 실시간으로 "지금 이게 사기 신호입니다"를 표시하지 않는다.** D-6("속은 시점 판정은 리포트 단계에서만, 채팅 중 실시간 표시 안 함")은 **그대로 유지되며 이 결정은 supersede가 아니다**. |
| Reason | 실시간 힌트는 두 가지를 동시에 깨뜨린다: (1) **훈련 효과** — 화면이 신호를 짚어 주는 순간 사용자는 신호를 스스로 알아채는 연습을 하지 않게 되어, "안 당해본 사기는 못 막는다"는 제품 전제 자체가 약해진다. (2) **기존 결정** — D-6은 리포트 단계 판정을 의도적 설계로 확립했고, 이를 뒤집으려면 실시간 판정 파이프라인이라는 신규 백엔드 능력까지 필요하다(비용이 큰 반전). 반면 **사전 브리핑은 같은 학습 목적을 D-6을 건드리지 않고 달성**한다 — 무엇을 조심해야 하는지 알려준 뒤 실제로 알아채는 것은 사용자에게 맡기기 때문이다. 실시간 힌트를 정말 원하는지는 UX가 단독으로 정할 사안이 아니라 훈련 설계 방향 질문이라 R-7로 남긴다. |
| Alternatives Considered | (1) 초급에서 대화 중 위험 신호를 실시간 배지로 표시(D-6 supersede 필요). (2) 힌트를 아예 두지 않고 사기범 행동만 노골적으로. (3) 사전 브리핑 + 노골적 행동(채택). |
| Rejected Because | (1) D-6 반전 + 신규 실시간 판정 파이프라인이 필요하고, 스스로 알아채는 연습을 없애 훈련 효과를 훼손한다 — 필요하면 사용자가 R-7에서 판단할 사안이지 UX가 임의로 뒤집을 사안이 아니다. (2) 사용자 요구가 *"초급은 **티가 나는** 수법"*이라 "무엇이 티인지"를 알려주는 최소 장치는 있어야 초급의 학습 가치가 성립한다. |
| Impact | Screen Catalog(UX-029 Secondary Actions·States의 초급 브리핑). Interaction Patterns(P-22). **D-6는 Active 유지(변경 없음).** **architect 인계**: 브리핑 콘텐츠 출처(시나리오 저작 vs `weakenedTactics` 파생, OQ-U18). **사용자 판단 대기**: 실시간 힌트 도입 여부(R-7). |
| Status | Active (D-6 supersede 아님 — D-6 유지) |

### D-44 실패 아카이브(UX-030)는 UX-012 확장이 아닌 신규 화면 — 단위가 세션이 아니라 "속은 순간"
| Item | Value |
|---|---|
| Decision | "내가 당했던 순간만 모아 보는 화면"을 신규 화면 **UX-030**으로 만든다. 기존 UX-012(계정·세션/리포트 히스토리)를 확장하지 않는다. **단위가 다르다** — UX-012는 세션 1건 = 항목 1건, UX-030은 **속은 순간 1건 = 항목 1건**(한 세션에서 3번 속았으면 항목 3개). **2인 챌린지의 사용자2(익명) 데이터는 포함하지 않는다.** |
| Reason | 두 화면은 목록의 단위·정렬·행동 유도가 전부 다르다. UX-012는 "언제 무엇을 했나"를 되짚는 **기록 열람**이고, UX-030은 "내가 어디서 반복해 넘어가나"를 보여주는 **학습 도구**다 — 그래서 UX-030에만 수법별 묶기("이 수법에 3번 넘어갔습니다")와 항목별 재훈련 동선(되감기·재훈련)이 붙는다. 한 화면에 두 단위를 섞으면 "세션인지 순간인지" 목록의 의미가 흐려지고, 항목 수가 세션 수보다 크게 늘어나 정렬·페이지네이션 특성도 달라진다(architect 관점에서도 다른 화면). 사용자2를 제외하는 이유는 (1) 익명이라 계정 단위 누적이 성립하지 않고, (2) 발신자에게 수신자 실패 이력이 누적 노출되는 경로를 만들면 **AC-043/AC-055의 결과 열람 제한(수신자 프라이버시)이 우회**되기 때문이다 — 이 제외는 협상 대상이 아니다. |
| Alternatives Considered | (1) UX-012에 "속은 순간" 탭을 추가한다. (2) 리포트(UX-008)에 누적 섹션을 붙인다. (3) 신규 화면 UX-030(채택). |
| Rejected Because | (1) UX-012는 P1 스케치 수준이고 단위가 달라, 탭으로 붙이면 두 목록의 의미·쿼리·동선이 한 화면에 섞인다. (2) UX-008은 **이번 세션 1건**의 리포트라, 과거 누적을 넣으면 화면의 시간 범위가 모호해진다("이번 훈련 결과"와 "지금까지의 나"가 섞임). |
| Impact | Screen Catalog(**UX-030 신설**; UX-008·UX-012에 진입점 추가). User Flows(**UF-010 신설**). IA(선형 여정에 첫 순환 고리). Interaction Patterns(**P-23 신설**). **architect 인계**: 순간 단위 쿼리·역정규화 여부·페이지네이션(OQ-U19). |
| Status | Active |

### D-45 아카이브 정렬은 "최신순 + 수법별 묶기" 2종만 — 검색·필터·통계 대시보드 미설계
| Item | Value |
|---|---|
| Decision | UX-030의 목록 조작은 **최신순(기본)**과 **수법별 묶기** **두 가지뿐**이다. 검색창·기간 필터·채널 필터·통계 차트·취약 유형 프로파일 대시보드는 **만들지 않는다**. |
| Reason | 사용자 요구는 *"내가 당했던 순간만 모아 보는 화면(**반복 학습**)"*이고, 그 목적에 실제로 기여하는 조작은 "같은 수법에 몇 번 넘어갔나"를 드러내는 묶기 하나다. 검색·필터·통계는 "목록이 있으니 있으면 좋겠다"는 이유로 딸려 오기 가장 쉬운 기능이고, 요청되지 않은 기능은 스코프 크립이라는 것이 이 문서의 명시 원칙이다(PRD에 없는 Search/Filter/Dashboard/Analytics 금지). 취약 지점 프로파일은 이미 PRD의 별도 제안(P-9, P2 로드맵)으로 존재하므로 여기서 몰래 선구현하면 우선순위 결정을 우회하게 된다. |
| Alternatives Considered | (1) 수법·채널·기간 필터 + 검색 제공. (2) 취약 유형 통계 차트 추가. (3) 최신순 + 수법별 묶기만(채택). |
| Rejected Because | (1)(2) PRD에 없고 사용자가 요청하지 않은 기능 — 스코프 크립. 취약 프로파일은 PRD P-9(P2)로 이미 별도 관리되고 있어, 필요해지면 그 경로로 정식 도입한다. |
| Impact | Screen Catalog(UX-030 Secondary Actions·Business Rules). **architect 인계**: 수법별 묶기의 그룹 키 정규화(자유 문자열 `tactic`이 표기 차이로 갈라질 위험, OQ-U19). |
| Status | Active |

## Open Questions
UX 설계 과정에서 **새로 발견된** 미해결 사항만 기재한다(PRD의 기존 OQ-3/5/8/9/14/15 등은 PRD에 있으므로 중복 기재하지 않음).

| # | Question | Priority | Owner | Reason | Blocking Impact | Suggested Resolution | Status | Decision |
|---|---|---|---|---|---|---|---|---|
| OQ-U1 | AC-020 "본인 확인"의 구체 강도 — 경량 자기확인(체크+문구, D-5)로 충분한가, 별도 신원인증이 필요한가? | High | Architect / User | 보안 배점 20% 근거이나 하루 스코프 내 강도 결정은 기술·정책 판단 필요. ux-design 단독 결정 불가. | UX-002 본인 확인 UI의 최종 형태·문구 확정이 지연됨. | 데모/하루 스코프에서는 자기확인 + 입력경로 제거로 진행하고, 발표에서 "타인 음성 등록 경로 부재"를 가드레일 근거로 제시. | open | |
| OQ-U2 | P0 데모가 로그인 없이(익명/임시 세션)로 동작하는가, Firebase Auth 로그인 화면이 P0에 필요한가? | High | Architect | 계정(AC-016)은 P1인데 동의·클론·리포트가 세션 식별을 필요로 함. 로그인 화면 존재 여부가 UX-001 진입부를 바꿈. | UX-001 진입 흐름·UX-012(히스토리)·UX-010(등급 지속) 설계가 로그인 여부에 종속. | P0는 익명/임시 세션으로 진행(로그인 화면 없음), 계정·히스토리는 P1로 미룸 권장. | resolved | **사용자 확정(2026-07-21): 로그인 있이 진행. P0에 Firebase Auth 로그인이 필요하다.** 로그인/가입 화면 UX-013 신설, UF-001 첫 단계로 배치(D-9). 구체 인증 프로바이더는 OQ-U5로 분리. |
| OQ-U3 | 클론 생성 최대 허용 대기 시간·타임아웃 값과 타임아웃 후 폴백(사전 준비 클론/녹화본) 노출 방식(UX-003, P-1). | High | Architect | T1(ElevenLabs PoC) 실측 지연에 종속하며 데모 안정성에 직결. ux-design이 수치 확정 불가. | UX-003 로딩·폴백 상태의 구체 동작·타임아웃 확정 지연. | T1 PoC 결과로 타임아웃 확정 + 실패 시 사전 준비 폴백 경로 필수화(PRD Risk 대비 녹화본과 연계). | open | |
| OQ-U4 | 세션 최대 턴 수·최대 시간 한도의 구체값(AC-007 종료 조건 표시). | Medium | Architect / User | AC-007이 한도 도달 종료를 요구하나 구체 수치는 미정. 데모 길이(2분)와도 관련. | UX-006 한도 도달 자동 종료 문구·표시의 구체화 지연. | 데모 흐름(2분)에 맞춰 짧은 상한(예: 소수 턴/수 분)으로 설정 후 architect 확정. | open | |
| OQ-U5 | 로그인/가입(UX-013)의 구체 인증 프로바이더·방식(이메일+비밀번호 / Google 등 소셜 / 전화번호 등)과 그에 따른 입력 필드·검증 규칙. | High | Architect | 인증 수단 선택은 Firebase Auth 설정·보안·구현 난이도와 직결된 **기술 판단**이라 ux-design 단독 확정 불가. UX 레벨에서는 "로그인/가입 화면이 존재하고 인증된 세션을 만든다"까지만 설계 가능. | UX-013의 입력 필드·검증(Validation)·Failure 문구 최종 확정과 T2/T3 구현 착수가 여기에 종속. | 하루 스코프·어르신 대상 저부하 고려 시 가장 단순한 수단(예: 이메일+비밀번호 또는 Google 1클릭) 권장. 어르신 타이핑 부담을 줄이려면 소셜 1클릭이 유리하나 데모 계정 준비 편의는 이메일이 유리 — architect가 T2 스캐폴딩 시 확정. | resolved | **사용자 확정(2026-07-21): Google 소셜 로그인(Firebase Auth Google Provider)만 사용.** 이메일/비밀번호·전화번호 등 미사용. UX-013을 "Google로 로그인" 버튼 1개 기준으로 구체화(States/Failure에 팝업 취소·차단·네트워크 실패 반영). |
| OQ-U6 | Google 계정이 없는 사용자(특히 어르신)의 진입 처리 — Google 계정 신규 생성을 앱이 안내/유도할지, 자녀가 미리 Google 계정을 만들어주는 것을 전제할지. | Medium | User / Planner | Google 단일 로그인(OQ-U5 확정)으로 계정 미보유자는 진입 자체가 막힌다. 이는 어르신 타겟(PRD Target Users)과 직결되나, 자녀 대리설정(OQ-3)·발표 내러티브(가족 원격 훈련)와 얽혀 ux-design 단독 결론이 부적절. | Google 계정 미보유 어르신의 첫 진입 경험(안내 문구·계정 생성 유도 UI 필요 여부)이 미확정. 단, P0 본인 온보딩 데모 자체는 로그인 계정 보유를 전제하므로 데모 흐름은 비차단. | (1) 데모/P0: 사용자가 Google 계정 보유를 전제(UX-013 Assumptions에 명시)하고 미보유 처리 UI는 만들지 않음. (2) 미보유 어르신 안내는 OQ-3(자녀 계정 생성 UI 보조) 논의와 함께 다루기를 권장 — 임의 설계는 스코프 크립. | open | |
| OQ-U7 | 통화(UX-014)를 **단일 라우트 + phase 상태머신(A)**로 구현할지, **공유 통화 셸 레이아웃 + 두 라우트 유지(B)**로 구현할지. | High | Architect | "단일 연속 통화"(D-11)는 UX 요구이며 라우트 방식은 상태관리·오디오 컨텍스트·STT 연속성 판단이 필요한 기술 결정이라 ux-design 단독 확정 불가. | UX-014의 구현 착수(기존 `/session/play`·`/session/chat` 통합 리팩터 범위)가 여기에 종속. UX 스펙(phase·통화 셸·이음새 없는 전환)은 두 방식 모두에서 동일하게 만족 가능. | UX 권고는 (A) 단일 라우트(오디오/타이머 컨텍스트가 자연히 연속, 몰입 단절 원천 제거). 최종은 architect가 리팩터 비용 대비 판단. | open | |
| OQ-U8 | 통화 경과 타이머·AC-007 한도(MAX_SESSION_MS, DECISIONS #10=6분)의 **기점** — 오프닝 딥보이스 재생 시간도 6분 한도·타이머에 포함되는가, 아니면 실시간 역할극 진입 시점부터 재는가? | Medium | Architect / User | 오프닝을 "사기범 첫 턴"으로 취급(UX-014 Business Rules)하면 타임라인 기점이 오프닝이지만, 한도 6분에 오프닝 재생을 포함할지에 따라 실제 대화 가능 시간이 달라진다. 데모 길이(2분)와도 관련. | UX-014 통화 타이머 표시·자동 종료 문구의 구체화, 리포트 타임라인(AC-026) "N초 시점" 기준 시각 확정. | 데모 흐름상 타이머·한도 기점을 "받기(call_answered)"로 통일하고 오프닝 재생을 한도에 포함(하나의 통화 = 하나의 시간축). architect 확정. | open | |
| OQ-U9 | 드릴다운 종착 단계(UX-017)에서 필터된 시나리오를 **카드 목록**으로 보여주는 것이 사용자의 "한 번에 하나씩" 몰입 의도와 충분히 부합하는가, 아니면 시나리오도 **한 장씩(캐러셀/단건)** 넘겨야 하는가? | Medium | User / ux-design | AC-029는 "전체 평면 목록 동시 노출 금지"를 요구하며 필터된 부분집합 노출(UX-017)은 이를 충족한다. 다만 사용자가 표현한 "한 번에 하나씩" 몰입은 최종 시나리오까지 단건화하는 것을 의미할 수도 있어, clone/generic별 복수 시나리오(현행 clone 2·generic 7)를 어떻게 보일지 방향이 갈린다. | UX-017의 노출 형태(필터된 카드 목록 vs 단건 캐러셀) 최종 확정 지연. 기능·구현 방향에 영향하나 AC 충족 자체는 카드 목록으로도 가능하므로 **비차단**. | 이번 패스는 필터된 카드 목록(AC-029 충족·비교 용이)로 설계하고, 단건 몰입이 더 중요하면 UX-017을 캐러셀로 후속 개편(관련 화면 UX-017). | **resolved** | **coordinator 확정(2026-07-23, T23 승인): 카드 목록 방식 그대로 채택(캐러셀 아님). UX-017 설계 변경 없음.** |
| OQ-U10 | UX-015에서 **메신저피싱** 선택 시 라우팅 목적지와 메신저 하위 구조(채널 선택: 카카오톡/문자 등)는 무엇인가? T24 완료 전까지 "준비 중" 스텁으로 두는 것이 맞는가? | Medium | Planner / Architect / ux-design(T24) | T23은 보이스 드릴다운만 설계하고 메신저 하위(T24)·채널 간 전이(T25)는 범위 밖(D-17). 상위 2분류(AC-028)상 메신저 항목은 존재해야 하나 그 하위 흐름은 미설계라 목적지가 비어 있음. | UX-015 메신저 카드의 최종 동작(라우팅 대상 화면)이 T24 설계 전까지 미확정. 단, 보이스 P0 드릴다운 데모 자체는 비차단(메신저는 "준비 중"). | T24(메신저 훈련 화면 설계) 시점에 UX-015 메신저 분기의 목적지·하위 구조를 확정하고, 그 전까지 "준비 중" 안내로 처리(임의 설계 금지, 스코프 크립 방지). | **resolved** | **T24 완결(2026-07-23, D-22): UX-015 메신저 분기 → UX-024(메신저 시나리오 선택)/UF-006 연결. 하위 구조=UX-024→UX-022(카카오/문자 스킨 셸)→(스미싱)UX-023. 에스컬레이션은 UF-007/UX-025.** |
| OQ-U11 | 2인 사용자2의 **문자(메신저) 표면 체험**을 지원할 것인가? T24로 메신저 셸(UX-022)이 생겼으므로 이제 재사용 대상 화면은 존재하나, **2인 메신저 챌린지 흐름 자체는 이번 범위 밖**이라 UF-005는 여전히 음성(UX-014) 중심으로만 설계돼 있다. | Medium | User / Planner / ux-design | AC-044는 "전화·문자"를 언급하나 2인 모델은 딥보이스(음성)가 핵심 차별점이라 이번엔 음성만 배선. 문자 2인 챌린지를 지원하려면 UF-004(사용자1 생성)·UF-005(사용자2 체험) 양쪽에 메신저 경로를 추가해야 한다(스코프 확장). | 2인 문자 챌린지 지원 여부가 미확정. 음성(UX-014) 기반 2인 자체는 비차단(현행 설계로 완결). | 2인 문자 챌린지가 필요하면 별도 태스크로 UF-004/005에 UX-022 재사용 경로를 추가(신규 흐름 확장). 이번엔 음성 중심 유지(스코프 크립 방지). | **resolved(P0 텍스트)** | **사용자 결정(2026-07-24, PRD v1.3 #20)·T46 설계: 2인 메신저(음성 없음) 텍스트 챌린지 지원 확정.** UF-004/005에 메신저 변형 추가, UX-022/023 무약화 재사용(D-27~D-29·AC-051/053/054/055). Exit 채널 분기(메신저→UX-022)·발신자 결과 완료 여부만. 에스컬레이션 음성 첨부 챌린지(#21·OQ-28)는 여전히 보류. 관련: UX-021·UX-022·UX-020·UF-004/005. |
| OQ-U12 | 복제 음성 **보존기간 기본값**과 **수동 삭제/"내 목소리 금고"(P-8) 통합 대시보드**의 위치·형태는? 현재 삭제 트리거는 UX-020(챌린지 결과 열람)에 두었으나, 여러 목소리·여러 챌린지를 한눈에 관리하는 P-8 금고 화면은 이번 범위에 설계하지 않았다. | Medium | Architect(T35) / User | AC-041은 기간제 보존(기본값 architect 확정)·수동 삭제·추출 차단을 요구하고, P-8(내 목소리 금고)은 MVP 권고이나 별도 화면. 보존 기본값 수치와 금고 화면 도입 여부가 미정. | 보존기간 기본값·금고 대시보드 화면(신규 UX-XXX 필요 여부)이 미확정. 단 UX-020의 챌린지 단위 수동 삭제로 AC-041 최소 요건(수동 삭제 경로)은 충족되어 비차단. | 보존 기본값은 architect(T35)가 확정하고, P-8 금고를 정식 화면으로 만들지는 사용자·planner 판단(만들면 신규 ID 부여). 이번엔 UX-020의 챌린지 단위 삭제로 최소 충족. | open | UX-020·UX-019 |
| OQ-U13 | generic 보이스 챌린지(AC-058)의 발신자 결과 열람 범위 — **완료 여부만**(planner default, 메신저식 AC-055) vs **완료/의심 시점**(clone 챌린지식 AC-043)? (PRD OQ-32와 동일 사안.) | Medium | User | PRD OQ-32가 open이고 planner가 default("완료 여부만")를 권고했다. UX 설계는 이 default로 UX-020·D-34를 그렸으나, 사용자가 "generic은 클론 유출 위험이 없으니 clone처럼 의심 시점까지 열자"고 확정하면 뒤집힌다. | UX-020의 generic 보이스 챌린지 결과 표시가 default로는 확정됐으나 사용자 최종 확정 대기 — **비차단**(default로 진행 가능). 뒤집히면 UX-020 generic 케이스를 clone 케이스와 동일하게 되돌리고 D-34를 supersede. | 이번 패스는 planner default("완료 여부만", D-34)로 진행. 사용자가 OQ-32를 명시적으로 열기로 확정하면 후속 소규모 갱신으로 UX-020·resultSummary 파생을 clone식으로 전환. | open(default 적용) | UX-020·PRD OQ-32 |
| OQ-U14 | 모드(self|send) 클라 상태 배선 + generic 보이스 챌린지 스키마/라우팅 — UX-026에서 고른 모드가 UX-016 노출·UX-017/UX-024 필터·최종 세션/챌린지 생성까지 전달되는 방식, 그리고 generic 보이스 챌린지(channel="voice"+voiceId 부재+voiceMode="generic")의 스키마 판별·사용자2 통화 GENERIC_VOICE_ID 라우팅 접합은? | High | Architect | UX는 "모드가 하류를 4분기시킨다"·"generic 보이스 챌린지는 클론 미탑재"까지 규정하나, 그 상태를 어떻게 들고 다니고(클라 상태/세션 필드) generic 보이스 통화를 어디로 라우팅할지는 스키마·상태관리 기술 판단이라 ux-design 소관 아님(planner도 AC-058에 flag). | 흐름 스펙(4분기·필터·카피)은 확정됐으나 구현 배선이 미정 — architect(다음 단계)가 §14.8을 이 조합으로 확장해야 implementer 착수 가능. **UX 관점 비차단**(화면·전환은 완결). | architect가 (1) 모드 플래그를 UX-026→시나리오 선택→세션/챌린지 생성까지 나르는 클라 상태를 설계하고, (2) §14.8.1의 voiceId 옵셔널화가 generic 보이스 조합을 수용하는지 + createRealtimeCall/통화 세션이 GENERIC_VOICE_ID 폴백으로 합성하도록 라우팅하는 접합을 확정. | open | UX-026·UX-016·UX-017·UX-024·UX-019 / AC-056·AC-058 |
| OQ-U15 | **v1.11이 설계한 신규 기능 4건(통화 중 문자 오버레이·즉시 되감기·난이도 단계·실패 아카이브)에 대응하는 PRD Acceptance Criteria가 없다.** planner 단계(Change Workflow: Feature addition → **Planner** → UX → Architect → …)를 거치지 않고 ux-design이 먼저 설계했다 — 각 기능에 AC를 신설할 것인가, 어떤 MVP Scope·우선순위로 둘 것인가? | **High** | **Planner / User** | ux-design은 "PRD에 없는 기능을 발명하지 않는다"·"모든 플로우는 PRD AC로 추적된다"를 규약으로 삼는다. 이번 4건은 사용자 승인 기능으로 전달됐으나 PRD v1.4에 대응 AC가 없어, UF-008/009/010과 UX-027~030은 **재사용되는 기존 AC**(AC-045/032/033/022/006/026/008/009/016/002/038/007 등)로만 추적을 걸었고 **신규 능력 자체를 규정하는 AC는 비어 있다**. | **추적성 갭**: reviewer·QA가 "이 화면이 어떤 AC를 충족하는가"로 검증할 기준이 신규 능력 부분에 없다. 또 architect가 무엇을 필수로 볼지(예: 인증번호 문자가 P0인가) 판단 근거가 없다. **UX 설계 자체는 완결**이라 architect 착수는 비차단이나, **구현 완료 판정(DoD)에서 문제가 된다.** | planner가 4건에 AC를 신설하고(예: 통화 중 문자 오버레이·인증번호 표시, 재연 드릴, 사용자 선택형 난이도 + 안전장치 무게이팅 명문화, 순간 단위 아카이브 + 사용자2 데이터 제외) MVP Scope 행·우선순위를 부여한다. UX는 AC 확정 후 각 Flow의 Related AC를 소급 갱신한다(화면 설계 변경은 불요할 전망). | open | UF-008·UF-009·UF-010 / UX-027·UX-028·UX-029·UX-030 |
| OQ-U16 | **통화 중 문자 오버레이(UX-027)의 상태 관리와 신호 경로** — (a) 오버레이가 열린 동안 통화 컴포넌트를 살린 채 렌더하는 계층 구조(포털/조건부 렌더)와 오디오 컨텍스트·STT 인스턴스·타이머 생존 보장 방식, (b) 문자 도착 신호를 서버가 어떤 구조로 싣는가(기존 `MessengerAttachment`[kind:"link"·displayText·fakeLandingId·harmless] 재사용/확장 vs 통화 전용 신규 구조; **인증번호형은 링크가 아니라 새 kind가 필요할 수 있음**), (c) 오버레이 열림/닫힘과 자동청취(마이크) 게이팅의 결합 지점, (d) 시나리오 프롬프트에 문자 마커 지시를 넣는 위치(공유 조립 블록 vs 시나리오별). | **High** | **Architect** | UX는 "라우트 이동 없이 통화를 유지한다"(D-35)·"서버가 준 구조화 신호만 렌더한다"(D-36)까지 규정하지만, 그 상태를 어떻게 들고 있고 신호를 어떤 스키마로 나를지는 상태관리·스키마 기술 판단이라 ux-design 소관이 아니다. 특히 `/session/play`와 `/session/messenger`가 별도 라우트라는 제약이 이 결정의 출발점이다. | **UX 관점 비차단**(화면·상태·전환은 완결). 다만 이 배선이 확정돼야 implementer가 착수 가능하며, **(b)가 정해지지 않으면 콘텐츠(시나리오 프롬프트)도 마커를 저작할 수 없다** — 문자 발송 대사만 나오고 문자는 오지 않는 불일치가 그대로 남는다(UF-008 Failure (a)). | 기존 `[[LINK:]]` 마커 처리(`functions/src/roleplay/linkMarker.ts`)와 sentinel(§13.2) 패턴을 그대로 확장하는 방향을 UX가 권고한다(신규 안전 경계를 만들지 않음). 오버레이 계층은 통화 컴포넌트를 언마운트하지 않는 구조라면 구현 방식은 architect 자유. **실 URL 필드는 어떤 경우에도 도입하지 않는다**(AC-032/045 구조적 금지). | open | UX-027·UX-014·UX-023 / D-35·D-36·D-38 |
| OQ-U17 | **즉시 되감기(UX-028)의 실행·판정 모델** — (a) 한 턴 재연을 원 세션 재개로 볼지 **별도 일회성 드릴 실행**으로 둘지, (b) 되감기 시도를 저장할지·저장한다면 어디에(원 리포트를 덮어쓰지 않아야 함 — AC-007 "세션당 정확히 1리포트" 불변식), (c) 사용자 새 답변의 **판정 주체·기준**(기존 리포트 분석기 재사용 vs 신규 판정 콜러블; 3단계 라벨의 판정 기준), (d) 음성 입력 제공 여부(마이크 재권한 비용 대비). | **High** | **Architect** | UX는 "한 턴만·입력 선택·3단계 판정·판정 실패는 정직하게·원 리포트 불변"까지 규정하나, 실행 모델과 판정 파이프라인은 백엔드 설계라 ux-design 소관이 아니다. 특히 (b)는 **기존 안전 불변식(AC-007)을 건드릴 수 있는 지점**이라 architect 판단이 필수다. | **UX 관점 비차단**(화면·상태·실패 처리 완결). 구현 착수는 (a)(c) 확정에 종속. (b)를 잘못 설계하면 **AC-007 위반**(세션당 리포트 1개)이 발생할 수 있어 리스크가 있다. | UX 권고: **원 세션을 재개하지 않는 별도 일회성 드릴**(원 세션·리포트를 읽기 전용으로만 참조) — 리포트 불변식을 구조적으로 보장하는 가장 단순한 형태. 판정은 새 사기 대사를 생성하지 않는 **단발 평가**이므로 대화 세션이 필요 없다. 저장 여부는 학습 가치(내가 몇 번 다시 해봤나) 대비 비용으로 architect 판단. | open | UX-028 / UF-009 · AC-007 |
| OQ-U18 | **난이도(UX-029)의 데이터 모델** — (a) 시나리오별 난이도 3종 프롬프트를 저작할지, 시나리오 프롬프트는 하나로 두고 **공통 난이도 모디파이어 블록**을 조립 시 얹을지, (b) 세션·챌린지에 난이도를 싣는 필드, (c) 기존 고정 `difficulty` 문자열(`src/content/scenarios/*.ts`)과의 관계(유지·병기·라벨 변경), (d) 초급 사전 브리핑 콘텐츠의 출처(시나리오 저작 vs `weakenedTactics` 파생), (e) 난이도가 리포트 판정 기준에 영향을 주는지. | **High** | **Architect** | UX는 "난이도가 화면에서 무엇을 바꾸는가"(압박 강도·요구 도달 속도·초급 브리핑·배지 표기)와 "무엇을 절대 바꾸지 않는가"(안전장치 전부, D-42)를 규정하나, 프롬프트·스키마 설계는 ux-design 소관이 아니다. 현재 13개 시나리오 × 3단계 = 39벌 저작은 유지비가 크다는 점만 관찰로 남긴다(판단은 architect). | **UX 관점 비차단**. 다만 **(a)의 선택이 프롬프트 조립 순서에 영향**을 주며, 난이도 모디파이어가 `guardrailPreamble`·무해화 제약보다 뒤에 붙어 그것들을 밀어내면 **D-42(안전 무게이팅)가 코드 레벨에서 깨질 수 있다** — 조립 순서 불변식(가드레일 맨 마지막)을 유지해야 한다. | UX 권고: 시나리오 프롬프트는 그대로 두고 **공통 난이도 블록을 조립 함수 1곳에 얹는** 방향(기존 `CONVERSATION_STYLE`·`SCENARIO_PROGRESSION` 블록이 이미 그 패턴). (e)는 **영향 주지 않음** 권고 — 판정 기준이 난이도마다 달라지면 아카이브(UX-030)의 누적 비교가 무의미해진다. | open | UX-029 / D-41·D-42·D-43 |
| OQ-U19 | **실패 아카이브(UX-030)의 쿼리 방식** — (a) 본인 `reports`를 전수 조회해 클라에서 `deceivedMoments`를 평탄화할지, **순간 단위 역정규화 인덱스**(별도 컬렉션/서브컬렉션)를 둘지, (b) "수법별 묶기"의 그룹 키(자유 문자열 `tactic`이 표기 차이로 갈라지는 문제 — 정규화·매핑 필요 여부), (c) 페이지 크기·"더 보기" 방식, (d) 리포트 보존·삭제 정책이 생길 경우 아카이브 항목의 수명. | **Medium** | **Architect** | UX는 "속은 순간 단위 목록·최신순/수법별 2종·재훈련 동선"까지 규정하나, 항목 수가 세션 수보다 크게 늘어나는 구조라 정렬·페이지네이션 특성이 UX-012와 다르다 — 쿼리 설계는 ux-design 소관이 아니다. (b)는 화면의 핵심 가치("이 수법에 3번 넘어갔습니다")가 데이터 정규화에 직접 종속되는 지점이라 특히 중요하다. | **UX 관점 비차단**(화면·상태·빈 상태 2종 완결). (b)가 해결되지 않으면 **묶기 기능이 사실상 동작하지 않는다**(같은 수법이 표기 차이로 흩어져 "3번"이 "1번+1번+1번"이 됨) — 이 화면의 존재 이유가 약해지므로 구현 전 확정 권장. | (a)는 초기 데이터 규모(개인 사용자의 리포트 수)를 고려하면 전수 조회로 시작해도 무방할 것으로 **추정**하나 확인 필요. (b)는 `tactic`을 자유 문자열 그대로 두지 말고 **고정 수법 카테고리로 정규화**하는 방향을 UX가 권고(리포트 분석 산출 시점에 정규화). | open | UX-030 / UF-010 · D-44·D-45 |
| OQ-U20 | 한 번도 속지 않은 세션에서 **"아깝게 넘어갈 뻔한 순간(near-miss)"** 개념을 신설할 것인가? 신설하면 되감기(UX-028)를 안 속은 사용자에게도 제공할 수 있다. | Medium | **Planner / User** | 되감기의 데이터 원천은 `deceivedMoments`뿐이고 near-miss는 현재 어떤 분석 출력에도 없다 — 신설하려면 "무엇을 near-miss로 볼 것인가"(planner)와 "어떻게 산출할 것인가"(architect)가 함께 필요하다. **ux-design이 임의로 정의하면 없는 데이터를 발명하는 것**이라 D-40으로 명시 배제했다. | **비차단** — 안 속은 사용자는 UX-018("잘 대응한 지점")로 안내되어 빈 화면을 보지 않는다(D-40). 신설되면 UX-028의 Entry 조건과 UX-007/008의 진입점 노출 조건만 확장하면 되고 화면 구조는 그대로다. | 지금은 D-40대로 진행(안 속으면 되감기 미노출). 필요하다고 판단되면 planner가 near-miss를 AC로 정의하고 architect가 분석 산출에 추가한 뒤, UX가 진입 조건만 소급 확장한다. | open | UX-028 / UF-009 · D-40 · R-8 |
| OQ-U21 | 시나리오 카드의 **기존 고정 `difficulty` 문자열**(예: "쉬움~중간 — 이익 유혹이 강한 편입니다")과 **사용자가 고르는 난이도**(초급/중급/고급)가 한 화면 흐름에 둘 다 존재한다 — 두 표기를 어떻게 정합할 것인가? (기존 문자열을 "기본 성향"으로 라벨 변경 / 병기 / 다른 처리) | Medium | **Planner / Architect** | AC-002가 "시나리오는 시작 전 난이도(강도) 라벨을 표시한다"를 요구하므로 기존 문자열을 **삭제할 수 없다**(AC 위반). 그런데 실제 문자열 내용을 보면 대부분 **심리적 부담·수법 성향 설명**("공포·협박이 강한 편이라 심리적 부담이 클 수 있습니다")이라 사용자가 고르는 강도 축과 의미가 겹치면서도 다르다. 라벨을 바꾸는 것은 AC 문면 해석에 닿아 ux-design 단독 판단이 부적절하다. | **비차단**(v1.11은 "기존 문자열을 삭제하지 않고 기본 성향 맥락으로 남긴다"로 설계해 AC-002를 계속 충족). 다만 사용자에게 **"난이도"라는 말이 두 곳에서 다른 뜻으로 쓰여 혼란**할 수 있다. | UX 권고: 카드의 기존 문자열은 **"이 시나리오의 성향/심리적 부담"**으로 라벨을 바꾸고, "난이도"라는 단어는 사용자가 고르는 3단계에만 쓴다(AC-002의 "난이도(강도) 라벨 표시"는 UX-029의 선택 결과 표시로도 충족 가능한지 planner 확인 필요). | open | UX-017·UX-024·UX-029 / AC-002 · D-41 |

## 재검토 필요 (기능 방향 — 사용자 판단 필요)
> 사용자 요청(2026-07-22)에 따라 신설한 **별도 섹션**. 위 Open Questions가 "UX 구현을 위한 architect용 기술 미결"이라면, 여기는 화면 흐름을 그리다 발견한 **기능 자체의 방향에 대한 의문**이다. **ux-design은 여기서 답을 내리지 않는다** — 기능 방향을 임의로 바꾸는 것은 ux-design 권한 밖이므로, 사용자가 판단하도록 질문 형태로만 남긴다. 판단 결과에 따라 planner(PRD)·ux-design(화면)으로 라우팅될 수 있다.

| # | 재검토 항목 | 무엇이 걸리는가(설계 중 발견) | 사용자에게 묻는 질문 | 관련 화면/AC |
|---|---|---|---|---|
| R-1 | **자동 청취(auto-listen)가 정말 이 앱 목표에 맞는가** | 현재 코드(`session/chat`)는 상대 턴이 끝나면(오디오 onEnded, 또는 재생할 오디오가 없으면 즉시) 마이크를 **자동으로** 연다. 통화 몰입엔 자연스럽지만: (a) 주변 소음·침묵을 사용자 발화로 오인, (b) 어르신이 "언제 말해야 하는지" 모른 채 마이크가 열려 불안, (c) 생각할 틈 없이 압박이 이어짐. 예방접종(압박 재현)엔 유리하나 어르신 심리적 안전(PRD Risk "실제 심리적 충격")엔 불리할 수 있어 **상충**한다. | 자동 청취를 기본으로 둘 것인가, 탭-투-토크(사용자가 말할 때만 마이크 탭)를 기본으로 두고 자동 청취는 옵션/제거할 것인가? 어르신 강도 완화 원칙과 자동 청취 몰입 중 무엇이 우선인가? | UX-014 (live phase) / AC-006, PRD Risk(심리적 충격) |
| R-2 | **기관사칭(generic)을 딥보이스와 같은 "통화" 경험으로 묶는 게 맞는가** | 이 앱의 차별점 1번은 "본인 목소리 복제"(딥보이스). generic(기관사칭)은 본인 목소리가 없어 "기본 합성 음성"으로 재생된다(D-13). 둘을 같은 통화 셸에 넣으면 사용자가 (a) "이것도 내 목소리?"로 오해하거나 (b) generic이 밋밋해 차별점이 희석될 수 있다. | 기관사칭을 딥보이스와 동일한 통화 UI로 제공하는 게 맞는가, 아니면 사용자가 두 시나리오를 **명확히 다른 경험**으로 인지하도록 톤/프레임을 분리해야 하는가? 애초에 generic 시나리오를 이번 데모 범위에 넣는 게 맞는가(PRD상 "여유 시" 항목)? | UX-004, UX-014 / AC-001, 차별점 #1 |
| R-3 | **clone 시나리오에서 사기범 실시간 응답도 본인 클론 목소리여야 하는가** | 오프닝은 본인 목소리(가족 사칭)로 재생되는데, 이어지는 실시간 역할극에서 사기범(=그 "가족")의 응답 TTS가 **어떤 목소리**인지 기능 정의가 불명확하다. 오프닝은 클론인데 대화 응답이 기본 음성이면 "같은 사람이 말하는 통화"라는 몰입이 깨진다. (아키텍처 경계와도 얽힘 — VoiceProvider가 세션 내내 clone voice를 쓰는지.) | clone 시나리오의 실시간 사기범 응답도 오프닝과 동일한 본인 클론 목소리로 합성해야 하는가(일관성·몰입)? 비용/지연(ElevenLabs 매 턴 합성) 대비 가치가 있는가? | UX-014 (opening→live) / AC-018/019, D-13 |
| R-4 | **오프닝을 "사기범 첫 턴"으로 리포트 타임라인에 넣을 때 '속은 시점' 기준** | 리포트(AC-026)가 "N초 시점에 속았습니다"를 표시하려면 시간 기점이 필요하다. 오프닝(고정 스크립트, 사용자 응답 전)을 타임라인 0점으로 삼을지, 실시간 대화 시작을 0점으로 삼을지에 따라 "속은 시점" 판정과 사용자에게 보이는 숫자가 달라진다. | 리포트 타임라인의 0초 기점을 통화 시작(오프닝 재생)으로 할 것인가, 실시간 대화 시작으로 할 것인가? (기능 정의 — OQ-U8의 타이머 기점 결정과 함께 다뤄야 함.) | UX-014, UX-008 / AC-026 |
| R-5 | **"거절(전화 안 받기)"을 어떻게 다룰 것인가** | 수신 phase에서 "거절"은 현재 곧바로 훈련 종료다. 그런데 예방접종 관점에서 전화를 안 받으면 사용자는 아무 체험도 못 한다. 반대로 실제로는 "모르는 번호 안 받기"가 정답에 가깝다. | 거절을 "잘하셨습니다(안 받는 게 최선)"로 긍정 피드백하는 미니 학습 지점으로 만들 것인가, 아니면 체험을 위해 받도록 유도(거절 시 안내 후 재수신)할 것인가? 훈련 설계 방향 질문. | UX-014 (incoming phase) / AC-006 |
| R-6 | **clone 자기체험 배제(AC-057) 후 Goal/차별점 1 서술·플래그십 데모 시연 방식 갱신 필요 여부** (PRD OQ-33과 동일 사안) | AC-057로 "본인이 자기 목소리 전화를 받는" 자기 체험 딥보이스 경로가 사라지고, clone 딥보이스 체감은 이제 2인 챌린지(발신자→지인 수신)로만 전달된다. 차별점 1("본인 목소리 복제로 위협을 신체적으로 자각 — 직접 체험")의 문면·플래그십 "가족 납치 딥보이스" 단독 데모 시연이 더는 그대로 성립하지 않는다. **이는 화면이 아니라 PRD narrative·발표 서사의 정합 문제라 ux-design 소관이 아니다** — 화면 설계는 이 확정과 무관하게 완결됐다. | Goal/차별점 1 본문·플래그십 데모 시연 방식을 "발신자가 클론을 만들어 지인(또는 시연용 제2계정)에게 챌린지를 보내 수신 시연"으로 갱신할 것인가? (planner 관찰: 차별점 1은 유효하되 전달 경로가 바뀐 것.) | PRD Goal·차별점 1 / OQ-33 (planner·User 소관) |
| R-7 | **초급 난이도에서 "대화 중 실시간 위험 신호 힌트"를 보여줄 것인가** (D-6 반전 여부) | v1.11은 초급의 힌트를 **세션 시작 전 사전 브리핑**으로만 설계했다(D-43) — D-6("속은 시점 판정은 리포트 단계에서만, 대화 중 실시간 표시 안 함")을 유지하기 위해서다. 그런데 사용자 요구는 *"초급은 티가 나는 수법"*이고, "티가 난다"를 가장 직접적으로 구현하는 방법은 대화 중에 "지금 이게 긴급성 압박입니다"를 실시간으로 짚어 주는 것이다. **상충**: 실시간 힌트는 초급 학습을 쉽게 만들지만, (a) 스스로 신호를 알아채는 연습을 없애 "안 당해본 사기는 못 막는다"는 제품 전제를 약화시키고, (b) D-6 반전 + 실시간 판정 파이프라인이라는 신규 백엔드 능력을 요구한다. | 초급에서 대화 중 실시간 힌트를 제공할 것인가(D-6 반전·신규 판정 경로 필요), 아니면 사전 브리핑 + 노골적인 사기범 행동으로 충분한가? "티가 나게 한다"의 의미가 **사기범이 서투른 것**인가 **앱이 알려주는 것**인가? | UX-029·UX-014·UX-022 / D-6·D-43 |
| R-8 | **"속을 뻔한 순간(near-miss)"을 훈련 개념으로 도입할 것인가** (OQ-U20과 동일 사안의 기능 방향 측면) | 되감기(UX-028)는 `deceivedMoments`에만 의존해, **한 번도 안 속은 사용자에게는 아무 재연 거리가 없다**. 그런데 잘 대응한 사용자야말로 "그때 조금만 흔들렸으면 넘어갔을 순간"을 되짚을 가치가 있을 수 있다. 지금은 그 데이터가 없어 D-40으로 배제했지만, 이는 "안 속은 사용자는 사후 학습에서 사실상 할 게 적다"는 비대칭을 남긴다. | 리포트 분석에 "속지는 않았지만 위험했던 순간"이라는 개념을 신설할 것인가? 신설한다면 무엇을 근거로 판정하는가(사용자가 망설인 턴? 사기범 압박이 가장 셌던 턴?)? 아니면 안 속은 사용자에게는 리플레이 해설(UX-018)만으로 충분한가? | UX-028·UX-018 / D-40·OQ-U20 |
