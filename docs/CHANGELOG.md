# Changelog

Owner: docs agent (see AGENTS.md). Format: [Keep a Changelog](https://keepachangelog.com/), newest first.

## [Unreleased]

> ⚠️ **이 블록(2026-07-29 추가분, PR #149~#158)의 기준선**: 오케스트레이터 보고에 따르면 main `73690ed`에서 **루트 255 pass / 0 fail · functions 589 pass / 0 fail**로 갱신됐다(`CLAUDE.md` Verified Commands 표 참조 — docs는 이 세션에서 재측정하지 않았다, 인용값). 각 항목의 리뷰/QA 판정 상세는 이번에 신설한 `docs/CodeReview.md`(reviewer/QA는 파일을 못 쓰므로 docs가 대신 등재 — 신설 경위는 그 문서 서두 참조)에 정본으로 남겼고, 여기서는 사용자 관측 가능한 변경만 요약한다. **⚠️ PR #151·#154는 오케스트레이터의 병합 목록(#149~#158)에 포함돼 있었으나, 그 대상·내용을 특정할 근거(Tasks.md 행·PR 제목 등)를 이 세션에서 찾지 못했다** — docs는 확인하지 못한 PR 내용을 추측으로 채우지 않는다(`gh`·git log 실행 수단 없음). 다음 패스에서 오케스트레이터가 대상을 특정해 주면 반영한다.

### Added
- **한 사기범 메시지에 랜딩 링크가 2개 이상 붙으면 D-61 표시 분기가 오분류될 수 있다 — 조기 경보(트립와이어)만 추가, 오분류 자체는 고치지 않음(T129, 2026-07-28, PR #149, reviewer APPROVED)**: `functions/src/roleplay/linkMarker.ts`의 마커 파서가 한 사기범 턴(`turnIndex`)에 서로 다른 `landingId` 마커 2개 이상을 허용하는데, T127(D-61)의 표시 분기 판정은 `turnIndex` 단독 매칭이라 그 경우 승격되지 않은 랜딩이 오분류될 수 있다(방향은 안전 — 거짓 응낙 주장이 아니라 정당한 칭찬 누락). 이번 변경은 **그 오분류를 고치는 것이 아니라**, 같은 turnIndex에 서로 다른 `landingId`가 실리는 콘텐츠가 생기면 테스트에서 경고하는 트립와이어(`findMultiLandingScenarios` + 역검증 1건)만 추가한 것이다. ⛔ **"오분류가 고쳐졌다"로 읽지 않는다 — 오늘 새로 잡는 오분류는 0건**이다. 콘텐츠·판정 로직(`mockScreenTimelineCopy.ts`·`linkMarker.ts`·`mockScreenTimeline.ts`) 변경 0줄. 테스트 functions 574→576 pass·루트 228 pass 무변경.

### Fixed
- **`npm install`이 `package.json`에 `file:..` 의존성을 몰래 추가하는 오염 — 커밋 전 사람이 매번 알아채 왔던 것을 자동 차단(트립와이어)으로 전환(T130, 2026-07-28, PR #152, reviewer APPROVED·QA GO)**: `npm --prefix functions install`을 돌리면 `package.json`에 `"fraud-vaccine-web": "file:.."`가 주입되는 현상이 하루 3건(+과거 T53에서 1건) 반복됐으나 기계적 방어가 0이었다. `.githooks/pre-commit`에 `file:` 의존성 검출 블록을 추가하고(오염 커밋 거부 exit 1 실측·정상 커밋 통과 exit 0 실측), 사용자 승인(OQ-A25)에 따라 `postinstall` 스크립트도 함께 넣었다. ⛔ **"오염 문제가 해결됐다"로 적지 않는다 — 이것은 트립와이어다**: `npm --prefix <dir> install` 경로에서는 postinstall이 아예 실행되지 않는 사각이 실측으로 남아 있고(pre-commit만 잡는다), `--no-verify`·`core.hooksPath` 미설정으로 여전히 우회된다. ⭐ **QA가 실측한 구조적 사실**: `core.hooksPath`가 절대 경로라 이 PR이 병합되기 전에는 어느 워크트리에서도 새 pre-commit 블록이 걸리지 않았다 — 즉 **병합 자체가 유일한 배포 수단**이었다. 테스트 루트 228→231 pass(+3)·functions 576 pass 무변경.
- **토큰이 폐기·비활성돼도 재로그인 유도 없이 훈련 화면이 무한 실패하던 문제 — SDK의 자동 로그아웃이 화면을 통째로 날리던 것을 멈춤(T128, 2026-07-28, PR #155, reviewer APPROVED·QA GO)**: ⛔ **"토큰 무효화를 처리하게 됐다"가 아니다** — Firebase Auth SDK는 이전에도 토큰 무효화를 감지해 스스로 `signOut`을 호출하고 있었다. 바뀐 것은 **그 자동 로그아웃이 일어나는 순간 참가자가 무엇을 보는가**다: 이전에는 진행 중이던 훈련 화면이 아무 안내 없이 사라지는 것과 동일했다(재로그인 유도 0건 — `src/` 전체에 `onIdTokenChanged` 구독이 0건이었다). `hadUser` 조건 분기로 SDK의 signOut 이벤트를 감지해 안내 배너를 띄운다. 사용자 확정(OQ-A23) — 세션 보존 대신 "솔직히 끝났다고 알리고 처음부터"를 선택. 역검증: 정상 상태 27개 라우트 완주 시 배너 0회 발화(과잉 로그아웃 없음). 신규 AC·컬렉션·콜러블 0건(`docs/Architecture.md`·`docs/API.md`·`docs/UX.md` 무변경). 테스트 루트 231→255 pass(+24)·functions 576 pass 무변경.
- **Gemini 무료 티어 일일 할당량(20호출/키) 소진 시 텍스트 대화 전체가 Mock으로 강등되던 문제 — 키 순환(failover) 구현(T132, 2026-07-28~29, 설계 PR #153 → 구현 PR #156, reviewer APPROVED·QA 조건부 GO)**: `GEMINI_KEY_SECRETS` 배열로 순서 있는 키 목록을 두고, 응답의 `quotaId`가 `PerDay`(일일 소진)일 때만 다음 키로 넘어간다(`PerMinute`·`unknown`은 키를 바꾸지 않음 — 분당 제한에 넘기면 멀쩡한 키의 하루치를 조기에 태우기 때문). 키 값은 로그에도 남기지 않는다(1-based 인덱스·슬롯 이름만). ⛔ **"할당량 문제를 해결했다"로 적지 않는다(G180)** — 1일 수요는 약 26호출인데 키 1개 캡이 20이라, 키 2개(캡 40)로도 여유는 14회분뿐이며 회차를 늘리면 다시 모자란다. **이것은 완충이지 해결이 아니다.** ⭐ **reviewer가 게이트를 직접 훼손해 "fetch 호출 횟수만 보면 같은 키로 두 번 요청해도 거짓 통과할 수 있다"를 실증**했고, 그 결과 두 번째 요청의 `x-goog-api-key` 헤더가 첫 번째와 실제로 다른지 확인하는 단언이 필수로 추가됐다. QA는 그 단언이 약 15% 간헐 실패하는 것을 발견했으나 원인은 공유 워크트리 빌드 오염(운용 실수)으로 추정돼 코드 결함이 아닌 **조건부 GO**로 판정됐다. `functions/.env.example`·README에 슬롯 이름 규약(`GEMINI_API_KEY2` …, 추가 키는 반드시 다른 GCP 프로젝트)을 반영. 테스트 functions 576→589 pass(+13)·루트 255 pass 무변경.
- **`docs/Architecture.md` §31·§32 헤더의 기준 버전(PRD v1.7.1·UX 1.13) 정합 갱신(T131, 2026-07-28, PR #150, reviewer APPROVED)**: 두 절의 헤더가 실제로는 PRD v1.11·UX 1.17까지 4건씩 뒤처져 있던 것을 재검증 후 헤더를 전진시켰다(헤더 전진 = "그 버전 기준으로 절 전체를 재검증했다"는 주장이므로 숫자만 바꾸지 않고 AC-080 적용 표면 확정·D-61 구현 반영 여부를 항목별로 재확인). reviewer가 줄번호 정확성에 대해 Major 2건을 제기했으나 **오케스트레이터가 반증** — reviewer가 브랜치가 아니라 base를 읽어 줄번호가 정확히 -5 오프셋이었던 것으로 확인됨(`docs/CodeReview.md` #2 참조). **⚠️ 문서 상태 불일치 고지**: `docs/Tasks.md`의 T131 행은 이 CHANGELOG 작성 시점에도 **"todo — 등재만 완료(planner, 2026-07-28). 착수 전."**으로만 남아 있어, review조차 거치지 않은 것으로 보이는데 PR #150이 APPROVED로 보고된 것과 어긋난다 — docs가 이 세션에서 실제 `docs/Architecture.md` §31·§32 헤더값을 직접 열어 대조하지 못했다(범위 밖 문서, architect 소유). `docs/UpdateRequests.md`에 별도 등재했다.
- **확인창구(신원 확인 우회 대응) 전환 순서 설계 — 재사용처 3곳 중 2곳만 처음 반영됐다가 전수 grep으로 나머지 1곳도 반영(§38, 2026-07-29, PR #157, reviewer CHANGES_REQUESTED → 반영 후 APPROVED)**: 확인창구로 넘어가는 순서를 규정한 설계 문서 변경. reviewer가 "같은 문장의 재사용처를 놓쳤다"는 Major를 제기했고, 전수 grep 결과 실제 재사용처가 reviewer가 지목한 2곳이 아니라 3곳이었음이 확인돼 반영 후 재승인됐다.
- **AC-081(확인-가로채기 흐름의 시크릿 선언 검증) 신설 + T136(리포트 자기모순 조사)·T137(제출 문구 정밀도) 반영(2026-07-29, PR #158, reviewer APPROVED)**: reviewer가 Major 1건을 제기했다 — AC 문면 안에 인용된 코드 문구가 이미 스테일(코드가 바뀐 뒤)이었던 것을 심볼명+문구 원문+줄번호 3종 병기로 정정. **⚠️ 문서 상태 불일치 고지**: `docs/Tasks.md`의 T136·T137 행은 이 CHANGELOG 작성 시점에도 각각 **"todo — 등재만 완료(planner, 2026-07-29). 착수 전."**으로만 남아 있어, 이 PR이 두 항목을 구현·승인까지 마쳤다는 것과 정면으로 모순된다 — docs가 이 세션에서 두 행의 본문(구현 세부)을 파일로 대조하지 못했다는 뜻이며, `docs/UpdateRequests.md`에 별도 등재했다(정정은 planner 소관).

 오케스트레이터가 main `17126a7`에서 직접 실행해 **루트 228 pass / 0 fail · functions 574 pass / 0 fail**을 실측했다(docs는 이 세션에서 재측정하지 않았다 — 인용값). 아래 각 항목의 개별 before/after 수치는 각 태스크 병합 시점의 실측치이며 위 최종 기준선과는 별개다. `docs/Tasks.md`에는 이 블록의 여러 항목(T115·T116·D-61)에 대해 **planner가 아직 반영하지 못한 상태 불일치**가 있고(각 항목에 명시), D-61은 `docs/Tasks.md`에 담당 태스크 번호 자체가 아직 없다(같은 날 planner가 신설 중 — `docs/UpdateRequests.md`에 별도 등재).

### Fixed
- **⭐ 리포트가 제출한 참가자에게 "잘 대응했습니다"라고 거짓을 말하고 있었다 — D-61 표시 분기 구현(2026-07-28, PR #140, 브랜치 `fix/D61-submitted-display`, 담당 태스크 번호 없이 구현만 진행)**: T123(AC-080)이 병합된 뒤, 메신저 표면에서 가짜 랜딩 폼을 **제출한** 참가자의 리포트에 *"잘 대응한 지점입니다 — 권한 허용에 응하지 않았습니다"*가 표시되고, **같은 리포트 위쪽에는 그 제출이 승격된 "속은 시점" 카드**가 함께 떠 한 리포트가 자기모순이었다. **원인**: `MockScreenTimelineEntry.consented`(가짜 "권한 허용"에 응함 전용 필드)를 화면(`src/app/report/page.tsx:710-719`, `report/replay/page.tsx`)이 **이분법**(응함/응하지 않음)으로 써, T123이 신설한 "제출"(`submittedAt`) 축이 있는데도 `consented:false`인 제출 건에 무조건 부정 서술이 붙었다. **⚠️ 경고가 있었는데 인계가 끊겼다**: `functions/src/report/mockScreenTimeline.ts` 주석이 이 상황을 이미 예고했고, ux-design이 `docs/UX.md`의 **OQ-U31 → D-61**로 3분기 표시(가: `consented:true` / 나: `consented:false`+승격 있음 → "이 화면을 열었습니다"에서 멈춤 / 다: `consented:false`+승격 없음 → "열었지만 응하지 않고 닫았습니다")까지 처방했으나, T123 병합 이후 구현하는 사람이 없어 **T116(렌더 층 검증 태스크) 리뷰 중 architect가 코드 판독으로 재발견**할 때까지 미구현 상태로 남아 있었다(`docs/Architecture.md` §32.4 (3)). **수정**: 3분기 판정을 순수 함수 `resolveMockScreenBranch`/`resolveMockScreenCopy`(신규 `src/lib/report/mockScreenTimelineCopy.ts`)로 뽑아 리포트·리플레이 두 화면이 **공유**하도록 배선(`report/page.tsx:714`, `report/replay/page.tsx:795`). 분기 판정 키는 필드 대조가 아니라 **승격을 조립하는 단일 지점**(`functions/src/report/mockScreenTimeline.ts`의 `buildLandingSubmitMoment` 호출부)에서 그대로 가져와 `moment.turnIndex === entry.anchorTurnIndex`로 확정했다. 대조가 구조적으로 성립하지 않는 경로(`anchorResolved`가 거짓이거나 `anchorTurnIndex < 0`)는 전부 "제출함"(중립) 쪽으로 폴백시켰다 — 애매할 때 **칭찬·비난 쪽이 아니라 중립 쪽으로** 떨어뜨리는 원칙(방향이 틀리면 참가자에게 거짓을 말하게 되므로). 스키마·서버 파생 로직·이벤트 신설은 **0건**(표시 층 판정만 바꿈). 렌더러 없는 저장소 특성상 화면이 다시 `consented`만 보는 회귀로 돌아가지 않도록 소스 스캔 단언(금지: `mockScreen.consented` 직접 분기 / 필수: `resolveMockScreenCopy(` 호출)도 같은 테스트 파일에 넣었다. **⚠️ 잔여 리스크(reviewer M1)**: 분기 매칭 키가 `turnIndex` 하나뿐인데, **한 메시지에 서로 다른 `landingId` 마커가 2개 이상 붙을 수 있는 경우**를 검증하는 테스트가 이미 존재하고 그 경우 항목 하나가 **오분류**될 수 있다. 다만 오분류 방향은 안전하다 — 거짓 응낙 주장이 아니라 **정당한 칭찬 누락**(중립으로 떨어짐)이며, 오늘 콘텐츠는 프롬프트가 마커 1종으로 제약해 실제로는 발생하지 않지만 **구조적으로 막혀 있지는 않다.** **⚠️ 브라우저 실물 확인은 하지 못했다** — 이번 검증은 서버 파생값 + 클라 판정 함수의 실행 출력이며 **실제 렌더 관측이 아니다**(T116이 다루는 렌더 층 공백과 같은 종류의 한계). `docs/Tasks.md`에는 이 항목을 담당하는 태스크 번호가 아직 없다(D-61로만 식별 가능 — planner가 같은 날 번호를 신설하는 중이라는 것이 오케스트레이터 전언이며 docs는 확인하지 못했다). 이 상태 불일치는 `docs/UpdateRequests.md`에 별도 등재했다.
- **확인 시도를 저지하는 문제 2차 시도 — 증상은 해소되지 않았다(T125 2차, 2026-07-28, 브랜치 `feat/T125-persuade-once`, base `8f82bff`, review — 라이브 검증 대기)**: 위 T125 1차 항목(사용자 확정 이후) 후속. **사용자가 목표 자체를 재확정했다**: *"고급에서는 한 번 만류해 보고, 참가자가 재차 고집하면 그때 확인 창구로 넘긴다."* ⇒ **만류가 한 번 남는 것이 이번엔 설계다** — ⛔ *"저지가 줄었다"*·*"확인 저지가 없어졌다"*로 적지 않는다. 편집 표면은 `functions/src/roleplay/promptAssembly.ts`의 `VERIFY_INTERCEPT_RULE` 2줄을 **같은 자리에서 교체**(덧쓰기 아님)했고, 확정된 순서 선언으로 바꿔 `functions/src/scenarios/*.prompt.ts` diff는 여전히 **0줄**(`git diff --stat` 출력 비어 있음 — 수법 콘텐츠 약화 없음의 기계적 증거). **반대 방향(확인과 무관한 이탈)에는 저지가 유지됨을 재확인**해 사용자 확정("수법 유지")과 AC-074 L2(압박 지속성)는 깨지지 않았다. **별건으로 F11 게이트를 보강**했다 — QA가 1차 게이트를 실제로 뚫었기 때문("세 번째 확인 요구부터는…" 같은 서수 표현이 통과됨) ⇒ 고정 15토큰 화이트리스트를 **형태 검출 7유형**으로 교체. base `8f82bff` 대비 초급·중급·비카탈로그 세션 프롬프트가 **바이트 단위로 동일함**(168조합 sha256 일치)을 확인해 무영향 범위를 실측으로 못박았다. 테스트 functions 523→524 pass(F11 게이트 +1)·루트 216 pass 무변경. **⚠️ 라이브 검증은 오늘 끝내지 못했다** — Gemini 무료 일일 한도(20건)가 소진돼 예정된 9건 세트(⓵ base 재차 고집 3회 + ⓶ 후속 5회 + ⓷ 반대 방향 1회) 중 **⓶·⓷이 0건 실행**됐다. **예산 모델 자체가 틀렸다는 것도 이번에 드러났다** — 1회차가 1호출이 아니라 **1+N호출**이라 세트 전체가 **26호출 > 일일 캡 20**이라 애초에 하루 안에 끝낼 수 없는 계획이었다. **⚠️ 충돌은 절반만 풀렸다** — `docs/Architecture.md` §30.7 2가 자기 고지한 대로, 프롬프트 전반부는 상시 규칙과 일치하게 됐지만 **후반부는 지속 압박 절과 여전히 충돌**한다(철회가 아니라 보류). ⇒ **"확인 저지 문제가 해결됐다"고 결론 내리지 않는다** — 말할 수 있는 것은 "문면이 교체됐고 소스 게이트가 그것을 고정한다"까지다.
- **세션 시작 시 토큰 갱신 400 — 조사 결과 트리거는 환경 문제였으나 프로덕션에 남는 결함을 발견(T122, 2026-07-28, 브랜치 `chore/T122-securetoken-probe`, 코드 변경 0건 — 조사 태스크)**: 오케스트레이터가 브라우저 콘솔에서 관측한 `POST .../securetoken.googleapis.com/v1/token → 400`을 조사했다. **판정: (가) 트리거 + (나) 취급.** 400 자체는 **에뮬레이터 재기동으로만 나는 환경 문제**(Auth 에뮬레이터가 인메모리라 재기동 시 사용자 레코드가 사라지고, `--import`/`--export-on-exit` 없이 기동돼 그 상태로 refresh를 시도하면 `auth/invalid-refresh-token`이 100% 재현된다 — 실측). **⭐ 그러나 "재시도가 삼킨다"는 실측으로 참이었고 프로덕션에 그대로 남는다** — Firebase Auth SDK가 `auth/invalid-refresh-token`을 `isUserInvalidated` 대상에서 빼고, `ProactiveRefresh.iteration()`이 네트워크 오류가 아닌 모든 실패를 조용히 삼키며 재스케줄도 하지 않는다(SDK 소스 직접 확인). 저장소 `src/` 전체에 `onIdTokenChanged` 구독이 **0건**이라(전수 grep), 프로덕션에서 토큰이 실제로 폐기(계정 삭제·비활성 등)되면 캐시된 idToken 만료(≤1시간) 이후 모든 인증 호출이 실패하는데 **재로그인 유도가 없다**. **⚠️ 미확인으로 남긴 것**: 2026-07-27 브라우저에서 실제로 난 그 1건의 응답 본문은 회수 불가하다(에뮬레이터가 요청을 로그로 안 남기고 디버그 로그가 이후 기동으로 덮였다) — 위 판정은 "이 엔드포인트가 400을 낼 수 있는 전 경로 열거 + 각 경로 성립 조건 대조"로 좁힌 결과이며 그날의 그 1건이 정확히 어느 경로였는지는 특정하지 못했다. 후속 등재 후보(이 태스크에서는 고치지 않음): `onIdTokenChanged` 기반 갱신 실패 감지 + 재인증 유도.
- **사실 조회(사실을 되묻는 의문형) 검출의 어형 구멍 수정(T112, 2026-07-28, 브랜치 `fix/T112-fact-inquiry-forms`, reviewer APPROVED, main `1933eea` 병합)**: T109가 세운 "한 턴 1요구" 게이트의 `factInquiry` 패턴이 *"최근에 통장 개설해 보신 일 있나요"* 류 **변형 의문형 8개 표본을 전부 놓쳤다(8/8 미검출, T109 QA 실측)** — 현행 콘텐츠 14종이 이 어형을 안 써서 오늘 오동작은 0건이지만, 다음 콘텐츠 저작자가 이 어형을 쓰면 게이트가 조용히 통과시키는 구멍이었다. **어휘 열거가 아니라 형태 슬롯**(`FACT_INQUIRY_SLOTS` 4개)으로 넓혀 8/8을 각각 독립 검출했고, 조립 전수 재스캔에서 신규 위반 0건(인용구 338종 old=8/new=8)을 확인해 **콘텐츠는 한 글자도 고치지 않았다.** 넓히는 과정에서 오탐 2건(`-는지` 내포절, `여부 확인` 규칙 산문)을 실측해 경계로 되돌렸다. reviewer가 인계 메모 2건을 남겼다 — M1: 이 확장이 **"2번째"**이며 3번째부터는 어형 열거 자체를 구조 전환(어휘 열거가 아닌 근본적 접근)해야 한다는 인계, M2: 제거했던 A/B 프로브 대신 비단언 진단 테스트로 "인용구 338종/factInquiry 매치 8건"을 커밋된 코드만으로 재현 가능하게 함. 테스트 functions 562→570→571 pass(감사 테스트 1건 추가)·루트 220 pass 무변경.
- **조립 산출물 전수 스캔이 `verifyInterceptEnabled` 축을 안 돌아 확인 무력화 전용 문구가 무검사였다(T113, 2026-07-28, PR #136, main `10fb959` 병합)**: T109가 만든 조립 전수 스캔 헬퍼(`assembledPrompts()`)가 `difficultyLevel`×`inCallSmsEnabled` 2축(시나리오당 8조합)만 돌고, `verifyInterceptEnabled`·`l3Procedural`·`turnInstruction` 3축은 순회하지 않아 **확인 무력화가 켜졌을 때만 등장하는 사기범 인용구가 한 번도 검사된 적이 없었다**(T109 QA 발견). `verifyInterceptEnabled` 축을 추가해 조합을 시나리오당 8→20(+턴지시 22벌 별도 합산) = 총 **112→302벌**로 늘렸고 검사 대상 인용구는 310→338종(+28). **신규 위반 0건**(콘텐츠 수정 0건). 옵션 키 5개가 스캔 헬퍼에 전부 반영됐는지 소스 대조로 고정(축 하나를 지운 사본에서 5/5 전부 실패하는 것을 실측). **⚠️ 남는 사각(자기 고지)**: `turnInstruction`은 곱하지 않고 합산만 해, "턴 지시가 특정 난이도 블록과 함께 있을 때만 성립하는 위반"(`stackingDirectives` ±14/15자 윈도 스캐너가 노출하는 형태)은 여전히 미검출이다. T92·T86 게이트의 스캔 확대는 이번 범위 밖(별건 등재 대상). 게이트 1 실행 시간 36.0ms→231.3ms(6.4배) — "속도 때문에 축을 쳐내지 말 것"을 주석에 박음. 테스트 functions 554→562 pass(+5)·루트 220 pass 무변경.
- **§22.1 확정 문면 ↔ 확인 무력화 게이트 화이트리스트의 기계적 연결 신설(T114, 2026-07-28, PR #136, main `10fb959` 병합)**: `verifyIntercept.test.ts`의 인용 표지·퇴장 술어 화이트리스트가 `docs/Architecture.md` §22.1 확정 문면에서 **역산**한 것인데, 그 연결이 주석뿐이라 §22.1이 갱신돼도 게이트가 옛 문면 기준으로 계속 초록불을 내는 구조였다(T106·T113과 같은 "게이트가 자기 근거 문서의 변경을 못 본다" 계열). 출처 문면을 리터럴로 박고 그 리터럴이 `docs/Architecture.md`에 실재하는지 검사하는 최소안을 채택(문서를 파싱하는 방식은 "문서를 기계 판독 정본으로 취급"하는 설계 판단이라 기각, 건수 단언만 추가하는 방식은 건수가 같은 채 문면만 바뀌는 경우를 못 잡아 기각). 역검증 7건 각각 독립 사본에서 그 문면만 지웠을 때 정확히 그 항목이 보고됨을 확인(7/7). **⭐ 역검증 작성 중 거짓 음성 1건을 실제로 밟았다** — `RECONNECT_TAIL` 문구가 6개 항목에 복제돼 있어 1회 치환으로는 게이트가 계속 초록불이었고, 전수 치환으로 고쳐 재발을 막았다. 패턴은 한 글자도 바꾸지 않음(T110/T125가 쌓은 검출력 보존). 테스트 functions 554→562 pass(+3)·루트 220 pass 무변경.
- **낡은 에뮬레이터가 옛 코드를 물고 있어도 아무 신호가 없던 문제 — 검사 도구 신설(T115, 2026-07-28, 브랜치 `feat/T115-emulator-freshness`, base main `0c74d18`)**: Functions 에뮬레이터가 기동 시점의 `lib`를 1회만 로드하고 이후 재빌드·재테스트가 있어도 아무 경고 없이 옛 코드로 계속 응답하는 것이 실측 확인됐다(T101 스테일 산출물, T106 라이브 에이전트 드리프트에 이어 이 세션의 세 번째 "코드는 맞는데 실제로 도는 건 다르다" 계열). 신규 `functions/src/devtools/`(판정 로직 + 빌드 기록 + `npm --prefix functions run emu:check` CLI)를 추가해 로드된 디렉터리·함수 목록·기동 시각을 대조하고 `FRESH`/`STALE-CODE`/`STALE-BUILD`/`STALE-EXPORTS`/`OTHER-TREE`/`UNKNOWN`/`NOT-RUNNING` 중 하나로 판정한다(판정 근거가 없으면 "괜찮다"가 아니라 `UNKNOWN`). **⭐ 거짓 `FRESH`를 라이브로 재현해 닫았다** — 특정 순서에서 도구가 `FRESH`라고 답하는데 실제로 요청을 처리하는 워커는 낡은 코드를 서빙하는 경우가 있어(코드를 붙드는 것은 부모 프로세스가 아니라 워커), 이제 그 경우 `UNKNOWN`을 낸다. `README.md`에 이미 반영된 사용 안내(3-1절)와 정합. 테스트 functions 525→540 pass(+15)·루트 216 pass 무변경. **⚠️ opt-in이다 — 검사를 안 돌리면 아무 일도 일어나지 않는다**(강제 수단 없음, 사람이 재기동을 잊는 경로가 남는다). **⚠️ 문서 상태 불일치 고지**: `docs/Tasks.md`의 T115 행은 이 CHANGELOG 작성 시점에도 "review — reviewer/QA 대기"로만 남아 있어 reviewer APPROVED·QA GO 문구를 docs가 파일에서 확인하지 못했다 — 오늘 병합된 항목이라는 것은 오케스트레이터 전언이며 병합 커밋은 docs가 대조하지 못했다. 이 불일치는 `docs/UpdateRequests.md`에 등재했다.
- **랜딩 렌더 검증 방식 판정(T116, 2026-07-28, architect) — ⚠️ 판정만이며 구현은 아직이다**: AC-079(카탈로그 필드 쌍별 비교)와 G76(소스 텍스트 스캔)이 실제 DOM 렌더 결과를 보지 않는다는 잔여 위험(T107 QA)에 대해, architect가 **신규 의존성 0건 경로(react-dom/server 정적 렌더 등) 3종을 먼저 프로브하고, 전부 막힐 때만 신규 테스트 의존성 도입 여부를 사용자에게 확정받는(OQ-A21)** 순서를 확정했다. 대상은 `MessengerFakeLanding.tsx` 1개 컴포넌트·정적 렌더로 도달 가능한 초기 상태로 한정하고, 기존 G76·AC-079는 삭제·완화하지 않고 **병존**시키기로 했다(교체 시 `successHeadline`·`consentLabel` 등 초기 렌더 비대상 필드가 무검사로 남는다). **⭐ 이 판정 과정에서 D-61(위 항목)이 아직 구현되지 않았다는 사실을 재발견**했고, T123의 제출 이벤트 경로는 정적 렌더로 도달 불가해 이번 렌더 검증 대상이 아니라는 것도 함께 판정했다. **⛔ "랜딩 렌더가 검증됐다"로 적지 않는다 — 이번에 병합된 것은 방식 판정 문서(`docs/Architecture.md` §32)뿐이고 실제 렌더 게이트 코드는 아직 없다.**
- **랜딩 폼 제출 사실 기록 — 가짜 랜딩이 "제출함"으로 남는다(T123, 2026-07-28, PRD v1.10 AC-080, PR #130, main `597bc8d` 병합)**: 사용자 확정(AC-080) — 인앱 가짜 랜딩(계좌번호·예금주명 입력 폼)을 참가자가 실제로 **제출한 사실**이 세션 타임라인에 남아 리포트에서 "이 지점에서 속았습니다"로 짚이고 되감기 진입점이 생긴다. **입력값(계좌번호·예금주명)은 서버로 전송되지 않고 리포트·아카이브·로그 어디에도 실리지 않는다** — 응낙 판정 지점은 "제출" 하나뿐이며 링크 탭·화면 노출·입력 중은 승격 대상이 아니다. 사용자 확정(OQ-A20)에 따라 **랜딩 표면(모의 화면 이벤트) + 통화 중 문자 표면(`landing_submitted`) 두 곳 모두** 구현했다. 기존 필드 확장(`MockScreenEvent` 유니온에 `"submitted"` 추가 등)만으로 처리해 신규 컬렉션·신규 콜러블은 **0건**. **⭐ QA가 타입 우회 raw HTTP로 검증**: 계좌번호·예금주명을 실어 보내도 저장 문서에 그 필드가 없음을 확인(AC-045 무변경). **⭐ 구현 중 실제로 걸린 함정(G135)**: `anchorResolved`만 봤다면 승격됐을 건수가 1건이었는데, 안전 조건(`anchorResolved && anchorTurnIndex >= 0`)을 지켜 실제 승격은 0건으로 정확히 처리됨을 확인. **⚠️ AC-007(세션당 리포트 1개)·AC-063(원 리포트 불변) 등은 구조 검토 근거이며 세션 종단 라이브 실행으로 검증한 것은 아니다.** **⚠️ 제출 승격은 `wasDeceived`를 뒤집고 방어등급·아카이브에 반영된다 — 이것은 결함이 아니라 설계다.** 라이브 소모 0건(에뮬레이터·단위 테스트로 완결, 예산 보호). 테스트 functions 525→534 pass(+9)·루트 216→220 pass(+4). 이 신설로 `docs/PRD.md`가 v1.10→v1.11로 갱신됐고(사용자 확정 사실 등재, AC 신설 0건), 그에 따른 `docs/UX.md` 소급 등재는 아래 T126 항목 참고.
- **UX 문서 — AC-080 소급 등재 + 추적성 마감(T126, 2026-07-28, ux-design 소유 `docs/UX.md` v1.17, PR #131, main `021f952` 병합)**: T123으로 신설된 AC-080을 `docs/UX.md`의 OQ-U31(Status `open`→`resolved`, Decision란에 AC-080 기입) · UX-023 · UX-027 · UF-008의 "Related Acceptance Criteria"에 소급 등재했다(화면·플로우 재설계는 없음 — OQ-U31 자신이 이미 "화면 설계 변경 불요"를 예고했었다). 이 항목은 `docs/UX.md`를 소유한 ux-design의 산출물이며, docs는 편집하지 않고 병합 사실만 기록한다.

### Fixed
- **확인 시도를 막지 않고 권해야 할 상시 규칙이 라이브에서 지켜지지 않던 문제 — 조건 분리로 시도했으나 증상은 남아 있다(T125, 2026-07-28, 브랜치 `feat/T125-verify-intercept-scope`, review — main 미병합)**: T119(아래 항목)가 오퍼 게이트를 4로 올리면서 생긴 대기 구간에서, 참가자가 "끊고 직접 확인하겠다"고 밝혀도 사기범이 `functions/src/roleplay/promptAssembly.ts:127`의 상시 규칙("막지 않는다 — 붙잡지도 않는다")을 어기고 확인을 저지하는 것이 **2/2 회차 모두** 관측됐다(T119 구현자 실측). **원인 후보(문면 대조까지가 실측, 인과는 추정)**: 같은 시스템 프롬프트 안에 정반대 지시가 동시에 들어간다 — `promptAssembly.ts:302/311`이 시나리오별 `weakenedTactics`를 무조건 방출하는데, 그 목록(`institutionalImpersonation.prompt.ts:48,62`)에 "확인 절차 차단"·"전화 끊음 저지" 수법이 실려 있고, 실제 라이브 저지 발화가 그 인용구와 거의 글자 그대로 같았다. **수정**: `VERIFY_INTERCEPT_RULE`에 적용 범위 선언 1개(F1+F2)를 추가했다(§28.3 (B) 채택) — `functions/src/scenarios/*.prompt.ts` diff는 **0줄**(카탈로그 인용구·게이트 무회귀). 소스 게이트에 카탈로그 6종 전수 순회 + 역검증 2건(선언 삭제 시 게이트 실패, `verifyInterceptEnabled:false` 조립에 선언 부재 확인)을 추가했다. 브랜치 기준 테스트 functions 519→523 pass·루트 216 pass 무변경, 기존 테스트 회귀 0건. **⚠️ 라이브 결과는 부분 성공일 뿐이다 — "고쳐졌다"고 적지 않는다**: base 대조군 1/1에서 저지가 재현됐고(대조 성립), PR 3회 중 **1회만 의도대로**("확인 부서를 연결해 드리겠습니다") 나왔다. 나머지 2회는 여전히 저지했는데 성격이 다르다 — PR-2는 카탈로그 인용구 그대로("지금 끊으시면 조사에 비협조적인 것으로 기록될 수 있습니다", `weakenedTactics:62`와 사실상 동일 — 이번 수정이 제압하려던 바로 그 수법)였고, PR-3은 "수사 기밀이어서 일반 대표번호로는 확인이 어렵습니다"로, 이 문구는 `weakenedTactics` 전체에서 **0건**이고 손대지 않은 `personaPrompt` 층에만 있었다(즉 이번 수정 범위 밖의 층이 이겼다). **반대 방향(확인과 무관한 이탈)에는 저지가 그대로 유지됨을 1/1 확인** — 사용자가 확정한 "수법 유지" 요구와 AC-074 L2(압박 지속성)는 깨지지 않았다. 다음 후보(§28.3 (C))는 architect 소관이며 이번 범위에서 열지 않았다. **⛔ 정직 고지**: 사용자 원문("전화를 끊으려고 하면 막아야 하는 거 아니야?")이 요구한 것은 수법 제거가 아니라 조건 분리였고, 시나리오 콘텐츠 diff 0줄이 "수법을 약화하지 않았다"는 근거다 — 그러나 이 작업으로 **"확인 시도 저지가 사라졌다"고는 말할 수 없다.** PR-2·PR-3은 서로 다른 실패 성격이다(PR-3=손대지 않은 층이 이김, PR-2=이번 선언이 자기 목표 범위 안에서도 지지 않는다는 보장이 없음).
- **확인창구 오퍼 게이트 값 재조정 — 사용자가 "초반에 갑자기 확인창구로 넘긴다"고 신고한 타이밍만 다룬다(T119, 2026-07-28, 브랜치 `feat/T119-offer-gate-values`)**: 사용자 원문 — *"갑자기 초반에 확인창구로 통화를 넘긴다고 하는데, 이게 흐름이 맞는지 모르겠어."* 확인 무력화 카탈로그 6종의 `availableAfterScammerTurns`(오퍼가 뜨는 사기범 턴 수) 실측 결과 **5종이 2, 1종만 3**이었다(`functions/src/scenarios/verifyIntercept.ts:85,94,103,112,121,138`). `docs/Architecture.md:1689`의 권고 범위(2~3) 안에서 architect가 §26.4 표대로 **계열 B 5종을 4로, 계열 A(`bank-security-verify-desk`)는 2로 유지**(이 항목은 확인 우회로 자체가 시나리오 본론이라 근거 주석이 이미 있었다)로 판정했다. 소스는 `verifyIntercept.test.ts`의 범위식(`>=2 && <=3`)을 offerId→값 6행 전수 대응표 `deepEqual` 단언으로 교체하고 역검증 1건(계열 B를 옛 값 2로 되돌린 테스트 코드 안 사본에서 대응표는 실패, 옛 범위식은 통과)을 추가했다. **테스트 functions 518→519 pass**(이 값은 `CLAUDE.md` Verified Commands 표의 현재 기준선 519와 일치 — main 병합 확인됨)·루트 216 pass 무변경. 라이브 2건 소모(`isMock:false`, `verifyOffer:{availableAfterScammerTurns:4}` 확인 — 오프닝 인사가 사기범 턴 1로 세어짐을 2/2로 실측 확정) — 2/2 회차 모두 오퍼 직전 턴이 "신원 확인 완료" 선언이었다. **⚠️ 정직 고지 — 이 변경은 타이밍(사용자 신고의 ㉠)만 다룬다.** 사용자 신고는 세 갈래(㉠타이밍·㉡선제성·㉢개연성)인데, ㉡(참가자가 확인 의사를 밝히지 않아도 사기범이 먼저 제안하는 것)은 설계가 명시적으로 받아들인 트레이드오프라 이번에 손대지 않았고, ㉢(개연성)은 문면·저작 층 사안이라 범위 밖이다. **⛔ "이제 신원 확인 뒤에 뜬다"는 일반화하지 않는다** — 2회 관측에서 그랬다는 것까지만 주장한다(라이브 예산 일일 20건 제약으로 표본이 작다). base(게이트 2) 대조 라이브는 돌리지 않았다 — 게이트 2에서는 오퍼가 참가자의 확인 의사 발화보다 먼저 떠 이 구간이 구조적으로 존재하지 않는다는 것은 연역이며 실측이 아니다. **architect 미해소로 남긴 관찰**: 넓어진 대기 창에서 참가자가 확인 의사를 밝히자 모델이 상시 블록("막지 않는다")을 2/2 회차 모두 지키지 않고 오히려 확인을 저지했다 — 이 관찰이 바로 위 T125 항목의 출발점이다.
- **실시간 호 전환 지속성 보강 — A5(전환 상태 재확인)+R-1+R-2, 다만 증상 해소는 증명되지 않았다(T118, 2026-07-27, 브랜치 `feat/T118-realtime-transfer-persistence`)**: 사용자가 이체 지시까지 완주한 라이브 세션에서 전환(넘겨주기) 후 **169초·173초 시점에 같은 확인창구 제안이 반복**되고, 전환 직후 사기범이 앞 대화 맥락("최근에 통장 개설하신 적 있으세요?")을 그대로 이어가는 것이 관측됐다. 원인은 `docs/Architecture.md` §22.8 (3)이 이미 자기 고지한 경로 비대칭 — 폴백 경로는 매 턴 상시 규칙을 재삽입하는데 실시간 경로는 세션 시작 시 시스템 프롬프트에 **1회만 고정**되고, 전환 지시(`reconnectInstruction`)도 1회성 턴 주입이라 2~3턴 뒤 원 화자가 컨텍스트에서 되살아난다(`functions/src/roleplay/promptAssembly.ts:118-121` 주석). **수정**: 전환 후 사기범 턴마다 전환 상태를 재확인시키는 A5-α(`turnComplete:false`) + R-1(전환이 끝난 오퍼에도 확인 권유 지시를 무조건 싣던 별도 결함 수정) + R-2를 같은 커밋에 넣었다 — `promptAssembly.ts`·`roleplay/index.ts`·`fallbackTurn.ts` **diff 0줄**(공유 폴백 경로 무회귀). 테스트 functions 508→518·루트 204→216 pass. 라이브 4건 소모(매 회 `isMock:false`·`difficultyApplied:true`·provider `gemini` 확인). **⛔ 정직 고지 — 인과가 확립되지 않았다.** implementer의 역검증(수정 끄면 재현)과 QA의 base main(패치 전 원본) 독립 완주 양쪽에서 **원 증상이 재현되지 않아 대조군이 없다** — 원 증상이 간헐적이라 이 규모의 관측으로는 못 잡는다. **다만 R-1은 A5와 무관하게 그 자체로 결함 수정이다**(전환이 끝난 오퍼에도 확인 권유 지시를 무조건 싣던 동작). **별도 관찰 1건(문면 기준 통과, 의도 기준 미해결)**: 전환 후 사기범이 즉흥적으로 "앞서 담당자는 현재 다른 업무를 진행 중이십니다"라고 말했다 — 이름·직책·1인칭 사용 0회라는 판정 기준은 문자 그대로 충족하지만, 앞 담당자가 여전히 존재한다는 **잔류 암시**이며 카탈로그 문구를 막는 기존 필터(`positiveResidencyDemands` 등)로는 모델의 즉흥 발화를 막을 수 없다 — 빈도·재현성 미검증이며 이 태스크로 닫힌 문제가 아니다. ⛔ **"이제 화자가 겹치지 않는다"는 금지**(§22.8 (1)) — 말할 수 있는 것은 "N회 관찰에서 재현되지 않았다"까지다. G② 발신자 라벨 실제 화면 스크린샷은 이 환경(마이크·`.env` 부재)에서 미해소로 남았다.
- **소스 텍스트 스캔 게이트 견고화 — AST 축을 리터럴 검사와 합집합, 신규 우회 1건은 고지만 추가(T108, 2026-07-27, main `1157d7d`, PR #108)**: T104 QA가 입력 어포던스 게이트(G80, `src/lib/incallsms/callContinuity.test.ts:136-176`)의 리터럴 6토큰 검사를 **동시에 회피하는 우회 3종**을 재현했다 — `role={"textbox"}`(JSX 표현식 컨테이너), 문자열 결합으로 만든 동적 태그명(`"inp"+"ut"`), 동적 태그+computed prop key. architect가 §24에서 **AST 파싱을 리터럴 검사와 합집합**하는 방식을 채택해 공용 헬퍼 `src/lib/sourcescan/scanSource.ts`를 신설하고, 소스 텍스트를 스캔하는 게이트 5곳(G80 입력 어포던스·AC-060 읽기 전용·AC-045/G50 네트워크·외부이동 부재·R8 `landingId` 문자열 분류 금지·T86 화면 문구 스캔)에 적용했다. reviewer APPROVED(Critical 0·Major 0)·QA GO. 테스트 루트 192→204 pass, functions 508 유지(병합 후 main 실측: 루트 204/functions 508, 0 fail). **⚠️ 이 위협 모델은 개발자의 우발적 실수이지 고의 난독화가 아니다 — "이제 우회 불가"라고 적지 않는다.** QA가 검증 중 **신규 우회 1건(QA-14)**을 발견했다: `onSubmit`을 computed key로 접어 별도 변수에 담고 참조 스프레드(`{...replyProps}`)로 붙이면 리터럴 0건·AST 0건, 완전 미탐지다. 원인은 스프레드 대상이 인라인 객체 리터럴이 아니면 속성 수집을 건너뛰기 때문 — **구현 결함이 아니라 설계(S2)가 "spread된 객체 리터럴의 키"로 범위를 명시했고 구현이 그 범위를 지킨 결과**다. 코드를 넓히지 않고 자기 고지에 6번째 항목으로 추가했고 `deepEqual([])`로 미탐지 상태를 회귀 테스트로 고정했다(참조 추적 확장은 architect 경유 사안, 이번 범위 밖). **잔여 위험은 줄지 않았다 — 고지가 정확해졌을 뿐이다.**
- **통화 중 문자 = 전면 문자함 + 통화 필 반전 — 소스 게이트는 통과했으나 라이브에서 문자함이 닫히지 않는 결함이 별도로 발견됐다(T103, 2026-07-27~28, `docs/UX.md` v1.13 D-55~D-57·P-27 확정 문면 구현)**: `docs/UX.md` v1.13이 확정한 설계대로 통화 중 문자(SMS) 화면의 주/부 관계를 반전했다 — 문자함이 주 표면이 되고 통화는 상단 고정 필로 줄어든다(라우트 이동·컴포넌트 언마운트·세션 재생성 0건, `InCallSmsOverlay.tsx` 그대로 재사용). 아코디언 카드를 말풍선 스레드로 바꾸고, 등장/퇴장 전환과 `prefers-reduced-motion` 폴백을 넣었으며, 읽기 전용 경계(입력창·답장·전달 어포던스 0건, 위 T108 게이트가 이 파일을 계속 스캔)를 유지했다. **⚠️ 다음 문단은 오케스트레이터 보고이며 docs가 이 세션에서 직접 재현하지 않았다** — `docs/Tasks.md`의 T103 행 상태 열은 이 CHANGELOG 작성 시점에도 `review`로만 남아 있고 그 행 본문에 reviewer APPROVED·QA GO 판정 문구가 없어 docs가 파일로 재확인하지 못했다: 소스 게이트 6건·reviewer APPROVED·QA GO를 전부 통과한 뒤, **라이브에서 문자함이 닫히지 않는 결함**이 추가로 발견됐다고 보고됐다 — 원인은 언마운트가 이 저장소의 어떤 테스트로도 관측할 수 없는 **단일 브라우저 이벤트**에 걸려 있었기 때문이라고 한다. 같은 보고에 따르면 **AC-026(본 문자 기록)은 미검증**으로 남았는데, 이는 결함이 아니라 검증 도구(브라우저 창) 환경에서 `IntersectionObserver`가 발화하지 않는 **계측 한계**로 판정됐다고 한다. docs는 이 문단의 사실관계를 독립적으로 확인하지 못했으며, 같은 취지로 `docs/UpdateRequests.md`에도 상태 불일치를 등재했다(정정은 planner 소관).
- **`functions/lib` 스테일 컴파일 산출물 자동 삭제 — 테스트 수치가 조용히 오염되던 문제(T101, 2026-07-27, 브랜치 `fix/T101-clean-lib`)**: `functions/package.json`의 테스트 명령이 `tsc && node --test lib/**/__tests__/*.test.js`인데 `clean`·`pretest` 키가 없고, `tsc`는 `outDir`(`lib/`)을 비우고 시작하지 않으며, `lib/`는 `.gitignore` 대상이라 브랜치를 전환해도 git이 지우지 않는다 — 그 결과 **이전 브랜치에서 컴파일된 테스트 산출물이 다음 브랜치 테스트 실행에 함께 섞여 들어갔다.** 이 오염으로 `CLAUDE.md` Verified Commands 표의 경고 블록에 기록된 대로 **2026-07-26~27 세션에서 테스트 수 오보가 3회** 발생했다(main 기준선을 415로 보고했으나 실제 405, T98 브랜치를 418로 보고했으나 실제 408 — 양쪽 다 스테일 산출물 +10건만큼 부풀려졌다). 발견 경위도 절차가 아니라 QA 두 명이 각자 clean install로 재측정해 우연히 잡아낸 것이었다. **수정**: 신규 `functions/scripts/clean-lib.mjs` + `functions/package.json`에 `clean` 키 신설, `test`/`build` 실행 전 인라인 clean을 넣었다. **완료 증거(A/B probe)**: 수정 전에는 브랜치 전환 후에도 스테일 프로브 테스트가 통과했지만(오염 재현), 수정 후에는 전환 즉시 프로브가 0건으로 사라짐을 실행 출력으로 확인했다(`P_B2 == P_B_clean == P_B2_clean = 462`). **⚠️ 미해결로 남은 것**: `npm`을 거치지 않고 `npx tsc && node --test lib/...`를 직접 실행하면 여전히 오염된다 — 이 때문에 `CLAUDE.md`에 "npm 스크립트를 거치지 않고 직접 명령을 치지 말 것" 경고 블록이 추가됐다(완화는 권고뿐, 강제 수단 없음). `docs/Tasks.md`의 T101 행 상태 열은 이 CHANGELOG 작성 시점에 `review`(reviewer/QA 대기)로 남아 있으나, `CLAUDE.md` Verified Commands 표는 이미 `clean-lib.mjs` 기반 빌드/테스트를 검증 완료로 기록하고 있다 — docs가 이 불일치를 독립적으로 해소하지는 않았다.

### Fixed
- **사기범이 대화 대신 수법 목록을 그대로 낭독하던 문제 수정 — 텍스트 LLM 100% Mock 강등(T117, 2026-07-27, PR #102 병합·main `65cfdf6`)**: 증상 — 참가자가 실제 대화를 하는 것이 아니라 사기범이 수법 목록을 그대로 읽어 내려가듯 말했고, 참가자가 할 말까지 대신 말하거나 미완성 placeholder(`OO은행 [계좌]`류)가 그대로 노출되고 같은 대사가 반복됐다. **원인**: `functions/src/llm/geminiClient.ts`의 텍스트 생성 경로가 매 호출 Mock으로 강등되고 있었다 — T98(2026-07-26)이 지연 단축을 위해 넣은 `thinkingConfig: { thinkingBudget: 0 }`이 **HTTP 400**으로 거부되는데, 재시도 안전장치 `isThinkingConfigRejected()`가 에러 메시지에서 `/thinking/i` 문자열을 찾도록 판정해 — 실제 거부 메시지("Request contains an invalid argument.")에는 그 문자열이 없어 **재시도 경로가 단 한 번도 열리지 않았다.** 근본 원인은 모델 상수(당시 `GEMINI_TEXT_MODEL = "gemini-flash-latest"`, 부동 별칭)가 가리키는 실체가 조용히 `gemini-3.6-flash`로 재매핑됐고, 그 모델이 `thinkingBudget` 설정을 받지 않기 때문이었다(T98 reviewer가 이 위험을 코드 주석으로 이미 경고했었다 — 방어 코드는 있었지만 판정식이 실제 에러 메시지와 맞지 않아 발동하지 않았다). **수정**: `thinkingConfig`/`thinkingBudget`/`isThinkingConfigRejected()`를 전부 제거해 추론 기본값으로 복귀시키고, 모델을 부동 별칭 대신 고정 버전 `gemini-3.6-flash`로 못박았다(`functions/src/llm/geminiClient.ts:36` — docs가 이 세션에서 main `65cfdf6`을 직접 열람해 코드에 `thinkingConfig`/`thinkingBudget` 참조가 남아 있지 않음을 확인). **복귀 확인(오케스트레이터 실측 보고 — docs가 직접 재현하지는 않음)**: `createSession` 응답 `result.isMock === false`, 에뮬레이터 로그에 "Mock으로 강등" 문자열 없음, 응답 소요 **3,595.696ms**(AC-004 컷오프 10,000ms 이내), 정상 대사로 복귀. **⚠️ 정직 고지**: (1) 이번 라이브 확인은 `createSession`(오프닝 대사) 경로 1회뿐이다 — `sendMessage`(대화 턴) 경로는 같은 클라이언트 팩토리·같은 `complete()`를 타는 것이 코드로는 확인됐으나 별도 라이브 호출로 검증되지 않았다. (2) 조사 중 **Gemini 무료 티어 분당 5건 제한**(`RESOURCE_EXHAUSTED`, `limit: 5`)이 새로 확인됐다 — 이를 넘기면 자동으로 Mock 강등되어 "장애처럼 보이는 정상 동작"이 재발할 수 있으므로, 데모·재현 시 호출 간격을 두어야 한다. (3) 고정 모델 `gemini-3.6-flash`가 향후 벤더에 의해 은퇴되면 이번처럼 조용히 재매핑되는 대신 **명시적으로 실패(404 등)**한다 — 의도된 트레이드오프이나 사전 알림 수단은 없어 다음에도 사용자 신고로 먼저 발견될 수 있다. **이 장애로 무효화됐던 검증**: T98의 "추론 비활성화 효과", T92·T109의 실 LLM 실동작 측정, T110(호 전환)의 라이브 재검증(트리거 자체가 안 돼 미측정), T104(상황별 랜딩)의 라이브 재검증(할당량 부족으로 미측정) — 전부 T87 계열 재검증이 아직 남아 있다(`docs/Tasks.md` T117 행 C항). **⚠️ 테스트 수치는 이 세션에서 재측정하지 못했다** — `CLAUDE.md` Verified Commands 표의 508/173은 이 수정이 반영되기 **이전** 기준선(측정 커밋 `dc67c49`)이며, main은 이후 이 PR(`65cfdf6`)로 전진했다 — 정확한 before/after 수치는 다음 갱신 시 재측정해 반영할 것.
- **사기범이 한 턴에 여러 요구를 몰아 묻던 문제 수정(T109, 2026-07-27, 사용자 라이브 재신고 — T92 후속)**: 사용자가 T92 수정 이후 같은 자리에서 다시 신고했다 — 기관 사칭류 시나리오에서 *"성함, 생년월일 불러주십시오, 최근에 통장 개설하신 적 있으세요? 지금 바로 답변 주셔야 절차가 진행됩니다"* 처럼 신원 요구와 사실조회 질문이 한 문장에 몰려 나와, 참가자가 문항 단위로 "이상하다"를 감지할 기회를 잃고 있었다. 실측(`functions/src/scenarios/institutionalImpersonation.prompt.ts:21`) 결과 같은 형태가 기관사칭·카드사 사칭·택배통관·대환대출·환급금 등 **5개 시나리오**에 그대로 복제돼 있었다. **묶음 판정 기준**을 확정해(참가자 자신에 관한 고정 신원 정보 — 예: 성함+생년월일 — 는 1건, 통장개설여부 같은 사실조회는 별개 요구로 다음 턴에, 재촉 문장은 요구로 세지 않음) 5개 시나리오의 인용구를 분리하고, 공용 프롬프트 조립부(`promptAssembly.ts`의 `buildSystemPrompt()`)에도 같은 규칙을 실었다. 위반을 기계적으로 잡는 게이트(`functions/src/scenarios/__tests__/scenarios.test.ts` `[T109/요구몰림]`)를 신설했고, **역검증**으로 사용자가 신고한 원본 발화가 실제로 이 게이트에 걸리는 것과 5개 시나리오 인용구 전수 스캔에서 오탐 0건임을 확인했다. **⚠️ 이 수정의 실제 라이브 효과는 아직 재검증되지 않았다** — 등재 당시(2026-07-27) 시도한 실 Gemini 측정은 별도 장애(위 T117 항목 — `thinkingConfig` 거부로 인한 100% Mock 강등)로 전부 무효 처리됐었다(`docs/Tasks.md` T117 행 C항 (c) — "T109 요구 분할 실동작 — 무효"). **그 장애 자체는 위 T117 항목으로 수정·병합됐다(main `65cfdf6`)** — 재검증을 막던 원인은 없어졌지만, T109를 대상으로 한 재검증은 이 시점까지 별도로 수행되지 않았다(T117 자체 범위 밖 — "T92·T104·T109·T110의 재검증은 각 행 소관"). 확정된 것은 여전히 **프롬프트 문구와 정적 게이트 통과**뿐이며, 실 모델이 이 지침을 실제로 따르는지는 T87 계열 재검증에서 확인해야 한다. **⚠️ 문서 상태 불일치 고지**: `docs/Tasks.md`의 T109 행 상태는 이 CHANGELOG 작성 시점에 `todo`(planner "등재만 완료")로 남아 있으나, 위 코드·테스트가 main에 실재해 사실상 구현이 병합된 상태다 — docs가 이 드리프트를 `docs/UpdateRequests.md`에 등재했다(정정은 planner 소관).
- **통화→통화 전환이 "새로 걸기"라서 화자가 겹치던 문제 — 호 전환(넘겨주기) 모델로 재설계(T110, 2026-07-27, 사용자 라이브 신고)**: 사용자 신고 원문 — *"통화에서 통화로 넘어가는 건 상담센터의 경우 넘겨주는 것으로 해야지, 새로운 번호를 안내받고 새로 통화를 거는 형식은 아니야... ai화자가 겹치는 거 같아."* 확인 시도(모의 재연결, T83/AC-071) 중 원 사기범에게 "저는 끊지 않고 기다리겠습니다"라고 지시하는 문구와, 재연결 상대에게 "참가자가 방금 새로 전화를 걸어 연결됐다"고 지시하는 문구가 **같은 모델 세션에 동시에 주입**되고 있어 두 화자가 뒤섞였다 — 확인-데스크 시나리오 **6종 전부**(`functions/src/scenarios/verifyIntercept.ts` 카탈로그)에 같은 패턴이 있었다. 원 화자가 반드시 퇴장하고 재연결 상대만 남도록 지시를 재설계했고, 참가자가 번호를 안내받아 "새로 거는" 것이 전제였던 `displayNumber` 필드를 제거했다(형식 고정 `/^\d{3,4}-0000$/`을 검사하던 안전 테스트는 `reconnectedCallerLabel`/`deskLabel` 검사로 대체 — 실존 기관명·대표번호 금지 등 기존 안전 불변식은 무회귀 확인). "확인 전화 걸기" 우회 4종 + 대칭 결함을 리뷰어·QA 3라운드에 걸쳐 잡아냈고(정규식 매칭의 근본 한계를 발견해 확정 문면 화이트리스트 방식으로 전환), clone 2종(가족사고·손주사칭)은 조립 문자열 해시가 동일함을 확인해 무영향 처리했다. functions 470→508·루트 171→173 통과(신규 `[T110]` 태그 게이트 42건, `docs/Tasks.md` T110 행 실측). **⚠️ AC-071 문면**(확인 전화도 결국 같은 사기 조직으로 이어진다는 서술)은 이번에 고치지 않았다 — "직접 건 확인 전화"와 "상담원이 넘겨준 통화"가 같은 교훈인지는 사용자 확정 대기 항목으로 별도 등재돼 있다(`docs/Tasks.md` T110 D항). **미검증**: 실시간 음성 통화 경로에서 실제로 화자가 겹치지 않는지는 라이브로 확인하지 못했다(quality-assurance 소관 라이브 검증 미수행 — 기계 게이트는 "모델에게 전달되는 지시 문자열 집합"까지만 보증한다). **⚠️ 문서 상태 불일치 고지**: `docs/Tasks.md`의 T110 행 상태는 이 CHANGELOG 작성 시점에 `review`로 남아 있으나, 위 코드(`displayNumber` 제거·`[T110]` 게이트)가 main에 실재하고 테스트 수치도 현재 기준선(functions 508/루트 173)과 일치해 사실상 병합된 상태로 보인다 — docs가 이 드리프트를 `docs/UpdateRequests.md`에 등재했다(정정은 planner 소관).

### Added
- **링크를 열면 항상 같은 페이지가 나오던 문제 — 시나리오별 모의 랜딩 4종(T104, 2026-07-27, PRD v1.8 MVP #33, AC-078/AC-079)**: 사용자 신고 — *"주어지는 링크를 들어가면 항상 똑같은 페이지가 나오는데, 상황에 맞는 링크 페이지가 구성되어 있어야 할 거 같아."* 통화 중 문자(T68)로 오는 링크 3종(대환대출 신청·환급금 신청·택배 통관 확인)과 메신저 택배 재배송 링크 1종에, 그동안 공용 범용 화면 하나로만 뜨던 것 대신 **상황별 전용 모의 랜딩**을 각각 만들었다(헤드라인·안내문·입력 필드 구성·완료 문구가 서로 다르다 — `functions/src/scenarios/mockScreens.ts`). 모의 앱 설치 화면(정부지원금, T84)은 무변경 유지. 신규 문구 전량이 무해화 검사(`harmlessnessPatterns.ts`의 `mockSurface` 프로파일 — 실존 기관명·URL·계좌 숫자열 금지)를 통과했고, 링크가 등장하지 않는 9개 시나리오는 대상에서 제외했다. **함께 보강한 것(T107, AC-079, 같은 날)**: QA가 "헤드라인·버튼 문구만 다르게 두고 본문·필드·완료 문구를 전부 범용으로 되돌려도 기존 검사는 통과한다"는 부분 수렴 경로를 시뮬레이션으로 미리 발견했다(**사용자 표면 오동작은 없었다** — 잠재적 회귀 방지 조치). 본문(`bodyLines`)·필드 구성·완료 헤드라인·발급처 라벨을 **개별 필드 단위로도** 서로 다른지 검사하는 게이트(`mockScreens.test.ts` `[AC-079]`)를 추가하고, 실제로 그 경로에서 실패하는 오염 샘플(위반 32건)로 역검증했다. `docs/PRD.md`는 기존 AC-078을 고치지 않고 **AC-079를 v1.9로 신규 추가**하는 방식으로 처리해, 이미 제출된 T104 완료 증거를 재작업 없이 유지했다. **⚠️ 실 사용자 클릭 경험(브라우저)은 이 환경에서 직접 재현하지 못했다** — 서버·콘텐츠 카탈로그 단위 검증과 기존 통화·문자 셸 배선 재사용으로 확인했다(`docs/Tasks.md` T104 행의 라이브 에뮬레이터 실측 참고).

### Added
- **난이도 4레버 — 시나리오별 이탈 차단 깊이(D3/D4)까지 고급 난이도에 반영(T85, 2026-07-26, PRD v1.6 MVP #31, AC-074/AC-075)**: 기존 `DIFFICULTY_MODIFIERS`(초급/중급/고급 공통 문구 1벌)만으로는 시나리오마다 다른 "이탈 차단"(수법 축 D) 깊이를 고급에서 제대로 반영할 수 없었다. `functions/src/roleplay/l3Depth.ts`를 신설해 시나리오별 D축 깊이표를 두고, 프롬프트 조립을 `buildDifficultyBlock(level, opts)` 함수로 교체했다(초급 1벌·고급 공통 1벌·고급 절차형 1벌 + 축소형 빈 블록을 실시간 통화·오프닝 대사·텍스트 대화 3개 호출부 전부에 반영). 이 작업으로 AC-076이 요구하는 D4(절차적 정당화) 커버리지 공백을 저금리 대환대출 시나리오 1종으로 채워, 축 커버리지 공백이 **4개(A4·D4·E4·E5) → 3개(A4·E4·E5)**로 줄었다. 안전 지침이 프롬프트 맨 마지막에 온다는 조립 순서 불변식은 이번에도 유지됨을 회귀 테스트로 확인했다. **⚠️ 13~14개 시나리오 × 3난이도 전체를 실 LLM 라이브 대화로 대조하는 것은 이번 범위가 아니다** — Gemini 무료 할당량 소진으로 미실시했고, `docs/Tasks.md`의 이 태스크 상태도 `review`로 남아 있다(리뷰어/QA 확정 전).
- **무해화 경계 회귀 테스트 대폭 확장 — 적용 표면 7 → 319, 조립 조합 336 → 672+63(T86, 2026-07-26, AC-075)**: 실존 기관 대표번호·실존 앱명·앱스토어 링크·실제 원격제어 절차 같은 "무해화 경계" 금지 패턴 검사가 그동안 모의 화면 카탈로그 7개 표면에만 걸려 있던 것을, `functions/src/scenarios/__tests__/harmlessnessPatterns.ts` 하나의 정본(금지 패턴 목록 + 판정표)으로 모으고 시나리오 프롬프트·공개 메타데이터·확인 무력화 카탈로그(T83)·통화 중 문자(T68)·모의 화면까지 319개 표면 전체로 넓혔다. 난이도(T85)·문자·확인 무력화가 동시에 얹힌 프롬프트까지 실제로 겹쳐서 검사하도록 조립 조합도 336개(옵션 하나씩만 켠 것)에서 672개(네 옵션 전 조합) + 63개(실제로 쓰이는 지시 문구 조합)로 늘렸다. 검사를 몰래 우회하지 못하도록 타입 게이트·카탈로그 1:1 대조·모듈/화면 등록부 대조의 3겹 방어를 뒀고, 리뷰어 검토 중 정본 정규식 1건(검출력이 없던 죽은 패턴)이 발견돼 함께 고쳤다. **⚠️ 프로덕션 콘텐츠(시나리오 문구)는 한 글자도 바뀌지 않았다 — 이번 작업은 전부 테스트 코드 확장이다.**
- **`main` 직접 커밋 방지 — 로컬 git hook 도입(T100, 2026-07-26)**: 구현 에이전트의 커밋이 실수로 공유 작업트리에서 `main`에 직접 올라가는 사고가 과거 6건 + 같은 세션 2건으로 반복 확인돼, `.githooks/pre-commit`·`.githooks/pre-push`를 신설하고 `core.hooksPath`로 저장소에 등록했다(모든 워크트리가 이 설정을 공유). `docs/GitWorkflow.md`(훅 설치 명령·워크트리 정리·로컬 `main` 갱신 절차)와 `docs/PromptRules.md`(파일을 쓰는 에이전트는 항상 워크트리 격리로 띄울 것·작업 시작/커밋 전 `git branch --show-current` 확인)도 함께 갱신됐다. **⚠️ 한계(실측)**: `git commit --no-verify`로 훅 자체를 그대로 우회할 수 있다 — 우회 불가능한 강제는 서버측 GitHub 브랜치 보호 규칙뿐이며 아직 도입되지 않았다. 또한 `core.hooksPath`는 워크트리마다 해석되는 **상대 경로**라, 이 설정이 반영되기 전에 이미 분기해 있던 오래된 브랜치에는 `.githooks/` 디렉터리 자체가 없어 훅이 존재하지 않고 강제되지 않는다.

### Fixed
- **Gemini 텍스트 경로 추론(thinking) 비활성화 시도 — 원인은 규명, 효과는 미검증(T98, 2026-07-26)**: T87 라이브 검증 중 실 LLM 호출 18건 중 **12건(66.7%)** 이 10초 타임아웃(`LLM_TIMEOUT_MS`)을 넘겨 규칙 기반 Mock으로 강등되는 것이 관측됐다(오프닝 대사만 보면 14건 중 9건). 에뮬레이터 왕복 지연은 원인이 아니었다(LLM을 경유하지 않는 `createSession` 왕복 중앙값 23ms). Gemini를 직접 호출해 확인한 결과 `thoughtsTokenCount` 588 / 출력 127 — **생성 토큰의 82%가 추론(thinking)에 쓰이고 있었다.** 이를 줄이려고 `functions/src/llm/geminiClient.ts`에 `thinkingConfig: { thinkingBudget: 0 }`을 추가하고, 거부당하면 추론 설정 없이 1회 재시도하도록 했다. **⚠️ 이 항목을 "해소"·"해결"로 적지 않는다.** `thinkingBudget: 0`을 API가 실제로 수용하는지조차 Gemini 무료 일일 한도(20회) 소진(429)으로 확인하지 못했다 — 벤더 SDK 타입 정의는 이 필드가 향후 모델에서 폐기되면 설정 자체가 에러가 될 수 있다고 명시하고 있어(재매핑되는 `gemini-flash-latest` 별칭 특성상 위험), 이 경우 Mock 강등률이 오히려 100%가 될 가능성도 리뷰어가 지적했다(미해소). 할당량 회복 후 T87과 동일 절차로 Mock 강등률을 재측정해야 실제 효과를 알 수 있다.
- **대출 시나리오에서 "대출 없다"고 하면 대화가 맴돌던 문제 수정(T91, 2026-07-25)**: 사용자 실측 신고 — 저금리 대환대출 시나리오에서 *"저는 지금 대출 받고 있는 게 없습니다"* 를 **4번 연속** 말했는데도 상대가 요구에 도달하지 못하고 같은 압박만 되풀이했다.
  원인은 "같은 압박을 반복하지 말라"는 지침을 안 지켜서가 아니라 **지킬 수단이 없어서**였다. 그 시나리오의 요구 수법 2개가 전부 **"기존 대출을 갖고 있다"를 전제**로 짜여 있어서, 참가자가 그 사실을 부인하는 순간 요구로 갈 길이 **0개**가 됐다. 남은 건 분위기용 수법(확인 막기·전화 끊지 못하게 하기)뿐이라 그것만 돌려막은 것이다.
  **현실과도 어긋난 설정이었다** — 실제 대출빙자 사기는 대출이 없는 사람에게 더 잘 통한다("신용이 깨끗해서 한도가 크게 나온다"). 그래서 부인을 받아넘기고 **신규 대출 승인 명목**(등록비·보증보험료 선입금)으로 갈아타는 경로를 넣었다. 이제 무엇을 부인하든 참가자가 **결정을 내려야 하는 순간**에 도달한다.
  **13종 전수 점검 결과**: **요구 수법이 아예 0개**인 시나리오는 이것 하나뿐이었다. 나머지 12종은 참가자가 무엇을 부인하든 꺼낼 수 있는 요구를 최소 1개씩 갖고 있다. 재발을 막기 위해 **시나리오마다 그 요구를 명시적으로 지명하는 검사**를 추가했다 — 새 시나리오를 추가하면서 이걸 빠뜨리면 검사가 먼저 막는다.
  ⚠️ **다만 "나머지는 안전하다"는 뜻은 아니다**(reviewer 지적으로 정정, 2026-07-26). 검사는 지명된 요구가 **존재하는지**만 확인할 뿐, 그 요구가 **참가자가 부인할 수 있는 사실을 전제하는지**까지는 판정하지 못한다. 실제로 같은 종류의 숨은 전제를 가진 시나리오가 4종 더 있다 — 카드사 사칭("카드번호 앞 8자리" → 그 카드사 카드가 없으면), 검찰·금감원 사칭("잔액 전액 이체" → 잔액이 없으면), 납치 협박·가족 사고("자녀가 없어요"). 부인이 흔하지 않아 아직 신고로 드러나지 않았을 뿐 구조는 같다. 택배 통관 시나리오는 이미 올바른 대응(부인을 명의도용으로 받아넘기고 요구를 재차 제시)을 갖고 있어 참고 대상이다. ⚠️ 부인 대응 문구가 아예 없는 시나리오는 **총 6종**이다(QA 실측으로 정정) — 위 4종에 국세청 환급·친구 사칭이 더해진다. 뒤 2종은 요구가 전제에 덜 묶여 교착 위험은 낮지만 부인 처리가 없는 것은 같다. **6종 전부** 후속 태스크로 추적한다.
- **통화 중 사기범이 말을 하다 마는 현상 수정(T90, 2026-07-25)**: 사용자 신고 — *"말을 하다가 마는 현상도 있는 거 같은데"*. 통화 중 상대 발화가 **문장 중간**("혹시…", "저희가…" 같은 연결어 뒤)에서 잘렸다. 대화 기록까지 같은 지점에서 잘린 것이 단서였다 — 소리만 끊긴 게 아니라 **상대가 말하기를 아예 멈춘** 것이다.
  원인은 마이크를 다시 여는 기준이었다. 상대가 말하는 동안에는 마이크를 닫아 두는데(스피커 소리가 마이크로 되돌아가 상대가 자기 말에 끼어드는 것을 막는 장치), 그 기준이 **"소리 재생이 끝났나"** 였다. 상대가 문장 중간에 잠깐 뜸을 들이면 재생할 소리가 비고, 그 순간 마이크가 열려 **방 안 소음이 사용자 발화로 오인**돼 상대가 말을 중단했다. 기준을 **"상대의 말 차례가 끝났나"** 로 바꿨다.
  같이 고친 것: 말 차례가 끝난 신호가 와도 **아직 재생될 소리가 몇 초 남아 있을 수 있어**(소리가 실시간보다 빨리 도착한다) 곧바로 마이크를 열지 않고 남은 소리가 다 나온 뒤 연다. 신호가 끝내 오지 않는 경우에도 마이크가 영영 닫히지 않도록 상한을 뒀다 — 말을 못 하게 되는 건 원래 증상보다 나쁜 고장이다.
  **리포트 정확도에 영향을 주던 결함도 함께 고쳤다**: 말이 잘렸을 때 그 조각이 기록되지 못하고 남아 있다가 **다음 발화 앞에 붙어**, 리포트가 "속은 순간"을 짚을 때 대화 짝이 한 칸씩 밀릴 수 있었다. 이제 잘린 조각을 독립된 발화로 남긴다.
  ⚠️ **실제 통화로 증상이 사라졌는지는 아직 확인하지 못했다** — 재현에 실제 마이크와 음성 대화가 필요하다. 원인 분석과 자동 검증(회귀 테스트)까지만 마친 상태다.

### Changed
- **통화 발신자 표기를 "(사칭)"에서 실제 번호로(T75, 2026-07-25)**: 사용자 지적 — *"통화할 때 이름이 (사칭)이라고 적혀 있는데 제거해달라. 보통은 모르는 사람이 전화할 테니 그냥 번호로 적자."* 화면에 "사칭"이라고 적혀 있으면 **정답을 미리 알려주는 셈이라 판단 훈련이 성립하지 않는다.** 2025년 실제 발신 형태를 반영해 070 인터넷전화·국제전화·발신번호 표시제한·대표번호 등 **번호만** 표시하도록 바꿨다(전부 가상 번호이며, 이 앱에는 전화를 거는 기능 자체가 없다). 발신자가 번호가 되면서 아바타에 "0"·"+" 같은 무의미한 글자가 뜨던 것도 저장되지 않은 발신자용 일반 아이콘으로 바꿨다. **시뮬레이션 고지는 무변경** — 수신 화면의 사전 고지와 상시 합성 표식이 계속 담당한다(이 표기는 원래 안전 표식이 아니었다).

### Added
- **지원금 안내 스미싱 — 문자 → 모의 앱 설치 → 통화, 3단계가 하나로 이어진다(T84, 2026-07-26, PRD v1.6 MVP #30, AC-072/AC-073/AC-076)**: 정부지원금 문자(스미싱) 시나리오가 이제 문자 한 통으로 끝나지 않는다. 문자 속 링크를 누르면 **"AI 훈련용 모의 화면"** 표식이 붙은 모의 앱 설치 화면이 뜨고, 여기서 가짜 "권한 허용" 버튼에 응하면 곧이어 환급 담당자를 자처하는 전화가 걸려온다 — 문자·설치 화면·통화가 **하나의 세션, 하나의 리포트**로 이어진다.
  - **"응한 순간"과 "열어봤지만 응하지 않은 순간"을 구분해서 짚는다.** 가짜 "권한 허용"에 실제로 응하면 그 순간이 리포트의 "속은 순간"으로 오르고, 전용 대처 문구가 함께 나온다: *"설치나 권한 허용을 요구받으면 그 자리에서 허용하지 말고 화면을 닫으세요. ① 앱은 기관 공식 경로에서만 설치하기 ② 설치를 권유받으면 대화를 멈추고 이미 아는 번호로 직접 확인하기 ③ 가족·가까운 창구에서 함께 확인하기 ④ 이미 허용했다면 112(경찰)·1332(금융감독원)에 신고하기."* 반대로 **화면을 열어보고도 응하지 않고 닫으면 그 판단은 "잘 대응한 지점"으로만 표시되고 속은 순간으로 잡히지 않는다** — 화면을 열어본 것 자체는 감점 대상이 아니라는 뜻이다.
  - **어디까지 갔는지가 리포트에 남는다.** 통화까지 가지 않고 설치 단계에서 훈련을 마쳐도 리포트는 여전히 정확히 1개만 만들어지고, 도달하지 못한 단계는 화면에 그리지 않되 기록에서는 "도달 못 함"으로 남는다(그런 단계가 애초에 없었던 것과 구분하기 위해).
  - **⚠️ 참가자 실제 기기에는 아무것도 설치되지 않는다.** 실제 설치 파일, 앱스토어 링크, 실존하는 앱 이름, 실제 OS 권한 요청 팝업, 외부 사이트 이동 경로가 코드·화면 어디에도 없다 — 새로운 가짜 화면 계열을 만들지 않고 기존 인앱 가짜 랜딩 화면 안에서 종류만 나눠 재사용했다.
  - **함께 고친 것 — 되감기가 엉뚱한 대사를 짚던 결함.** "그 순간 다시 해보기"가 사기범의 대사를 찾을 때, 이번처럼 참가자의 응낙이 대화 발화가 아니라 화면 클릭인 경우 원래 로직이 그 자리에서 한 칸 앞의 다른 사기범 대사를 잘못 짚고 있었다. 두 경우를 하나의 규칙으로 합쳐 바로잡았고, 기존 되감기 결과는 전부 그대로다(회귀 없음, 테스트로 고정).
  - **⚠️ 정직 고지 — 실 LLM 편차**: 이 프로젝트의 개발 환경은 실제 Gemini로 대화를 생성하는데, 설치 링크가 **6턴 안에** 제시돼야 이 기능이 발동한다. 실행마다 편차가 있어, 한 번은 6턴 안에 링크가 전혀 나오지 않았고 다시 실행하니 1턴 만에 나왔다 — `GEMINI_API_KEY`가 이 환경에 없어 반복 측정으로 편차 정도를 확인하지 못했다(관측 성공 1회·실패 1회뿐). 이 편차는 "해결됐다"고 보고하지 않는다. 브라우저 화면 클릭 검증도 이 환경에 도구가 없어 하지 못했다(서버·판정 로직은 격리 에뮬레이터 실측으로 확인).
  - **후속 라이브 검증(T87, 2026-07-26)**: 위 편차 우려를 실 LLM 호출로 다시 측정했다 — 설치 링크가 실제로 제시되는 시점을 **5회 측정한 결과 5/5 모두 turn 0(오프닝 문자)**이었다. "링크가 늦게 나와 6턴 예산 안에 설치 단계까지 도달하지 못한다"는 우려는 이번 5회 표본에서는 나타나지 않았다. **다만 5회뿐이고, 초급↔고급 대조·확인 무력화 라이브 검증 등 나머지 항목은 Gemini 무료 할당량 소진으로 아직 완료하지 못했다** — "해소됐다"가 아니라 "이번 표본에서는 우려가 재현되지 않았다"는 뜻이다.

- **확인 시도 무력화(모의 재연결) — "확인했는데도 속은 순간"을 재현하고 짚는다(T83, 2026-07-26, PRD v1.6 MVP #29, AC-071/AC-075)**: 고급 난이도 통화 중 사기범이 의심을 받으면 오히려 "정 못 미더우시면 안내해 드리는 번호로 직접 확인해 보세요"라며 스스로 확인을 권할 수 있다. 참가자가 응하면 통화 셸에 **"안내받은 번호로 확인 전화 걸기"** 버튼이 뜨고, 탭하면 **통화를 끊지 않은 채** 오버레이가 열려 "연결 중…" 뒤 같은 상대가 다른 담당자인 척 응대하는 모의 재연결이 재현된다 — 재연결 뒤 첫 마디는 앞서 하던 요구를 "확인해 드렸다"는 형태로 되풀이한다.
  - **"확인했는데도 속은 순간"을 새 항목이 아니라 기존 항목에 표식으로 남긴다.** 재연결 뒤 참가자가 다시 요구에 응하는 순간은 원래도 리포트가 "속은 순간"으로 잡고 있었다 — 그 기존 순간에 "확인 이후"라는 표식만 얹고, 그 순간의 대처 조언을 안전상 반드시 필요한 문구로 바꿔치기했다: *"확인은 지금 통화를 끊고 하세요. ① 다른 기기(가족의 전화 등)로 걸어 확인하기 ② 내가 이미 알고 있는 번호로 직접 걸기 ③ 가까운 창구에서 직접 확인하기 ④ 의심되면 112(경찰)·1332(금융감독원)에 신고하기."* 원래 기본 조언("확인 전화를 거세요")을 그대로 냈다면, 방금 확인 전화가 소용없었던 참가자에게 도로 같은 조언을 하는 모순이 생기기 때문이다. 리포트·되감기·실패 아카이브는 이 순간을 기존 화면 형식 그대로 받는다(신규 화면·신규 항목 형식 0개).
  - **⚠️ 실제 전화는 걸리지 않는다.** 이 화면은 전화 앱이 아니다 — 다이얼패드·연락처·통화기록·자유 번호 입력·`tel:` 링크·발신 인텐트·외부 이동이 화면 어디에도 없다(그런 API를 아예 import하지 않음). 참가자가 할 수 있는 것은 "걸어보기 / 그만두고 통화로 돌아가기 / 훈련 종료" 세 가지뿐이다.
  - **⚠️ "어디에 걸어도 결국 같은 조직"이라는 구조 설명은 세션 중에는 하지 않는다**(사용자 확정 — 이번에는 상황만 재현). 그 설명은 통화가 끝난 뒤 리포트에서만 한다. "내가 아는 번호로 걸기"·"다른 기기로 걸기" 같은 진짜 유효한 대처도 세션 안에서는 시뮬레이션하지 않는다 — 두면 그것마저 같은 곳으로 이어졌다는 무력감을 주거나, 정답을 세션 중에 미리 알려주게 되기 때문이다. 유효한 대처는 리포트에서만 제시한다(위 4개).
  - **정직 고지**: 실시간 음성 통화 경로(브라우저 마이크·Gemini Live)에서 지시가 실제로 어떻게 전달되는지는 이 환경에 마이크·Live API 키가 없어 라이브로 확인하지 못했다 — 단위 테스트와 구조 검증으로만 고정했다(서버·판정 로직은 격리 에뮬레이터 실측으로 확인). 브라우저 화면 클릭 검증도 이 환경 도구 부재로 하지 못했다.
  - **범위 고지**: "확인 전화조차 같은 조직으로 이어진다"는 것을 처음부터 목적의식적으로 골라 훈련하는 **전용 시나리오 신설은 이번 범위가 아니다** — 별도 태스크(T95)로 분리 등재했고 아직 착수 전이다.

- **시나리오 수법 축 체계 — 라이브러리에 어떤 수법이 비어 있는지 기계로 산출한다(T82, 2026-07-26, PRD v1.6 MVP #28, AC-070/AC-076/AC-077)**: 지금까지 "우리 시나리오 13종이 어떤 수법을 다루고 무엇이 빠져 있는가"는 사람이 표를 읽어야만 알 수 있었다. 이제 13종 전부에 **5개 축**(접근 경로 / 사칭 주체 / 압박 기제 / 이탈 차단 / 요구 형태)의 **고정된 값**이 붙어 있고, 명령 한 줄(`npm run report:axis-coverage`)로 **어느 값이 몇 개 시나리오에서 쓰이고 어느 값이 0건인지**가 표로 나온다.
  - **핵심은 "0건이 보이는 것"이다.** 흔한 실수는 태깅된 값만 세는 것인데, 그러면 **아무도 안 쓰는 값은 결과에서 아예 사라진다** — 비어 있다는 사실이 조용히 감춰진다. 그래서 산출은 데이터가 아니라 **값 목록 전체**를 훑는다. 도입 시점 실측 결과 **0건이 7개**(앱 설치 후 접근·계정 도용·확인 시도 무력화·절차 서류 정당화·앱 설치 원격제어 요구·대면 현금 전달·상품권 가상자산)로 정확히 드러났고, 각 0건에는 **왜 아직 비어 있는지 사유**가 함께 출력된다(사유 없는 공백은 검사에서 막힌다). 특히 **대면 현금 전달은 이번 재구성에서 만들지 않기로 확정된 항목**이라, 조용히 없어지지 않고 "미채택 확정"이라는 사유와 함께 계속 보고된다. (이 중 앱 설치 후 접근·앱 설치 원격제어 요구 2개는 이후 T84로 해소됐다 — 아래 T84 항목 참고.)
  - **태깅을 빠뜨린 시나리오는 등록되지 않는다.** 축이 하나라도 비면 컴파일이 실패하고, 시나리오를 추가하면서 축 표를 안 채우면 검사가 먼저 막는다. 실제로 시나리오 1종을 표에서 빼 검사가 깨지는 것까지 확인했다.
  - **사용자 화면은 한 글자도 바뀌지 않는다.** 축은 기획·커버리지 산출용 내부 좌표이며 앱 어디에도 표시되지 않는다(UX 확정 사항). 그래서 축 값을 화면으로 갈 수 있는 데이터(시나리오 공개 정보·서버 저장 문서)에 **아예 넣지 않았다** — 재시딩도 필요 없고, 기존 리포트·아카이브·되감기 기록은 **소급 수정 0건**이다. 시나리오 식별자·제목·채널·통화 방식도 무변경이다.
  - **함께 고친 것 — 앞으로 쓸 수법 이름 4개가 분류에서 새고 있었다**: 실패 아카이브의 "이 수법에 3번 넘어갔습니다" 묶기가 이번 재구성에서 새로 쓸 이름(확인 시도 무력화·확인 무력화·권한 허용 유도·절차 서류 정당화)을 **하나도 못 알아보고 "기타"로 흘리고 있었다.** 콘텐츠가 저작되는 순간 묶기가 흩어졌을 자리다. 검사를 느슨하게 푸는 대신 **분류 규칙을 넓히는 방향으로** 고쳤고, 기존 68개 수법 이름의 분류는 전후 대조에서 **한 건도 바뀌지 않았다.**
  - **정직 고지**: 축 좌표는 기획 문서의 매핑 표를 그대로 옮긴 것이라, **표가 콘텐츠보다 좁은 자리가 3곳** 있다(예: 국세청 환급 시나리오는 이미 "앱 설치 지시" 수법을 갖고 있는데 표의 요구 형태에는 반영돼 있지 않다). 좌표를 임의로 고치면 커버리지가 거짓이 되므로 그대로 두고 기록만 남겼다 — 정정은 기획·콘텐츠 작업 소관이다.

- **통화 중 문자(SMS) — 전화를 끊지 않은 채 문자를 확인한다(T68, 2026-07-25, PRD v1.5 MVP #24, AC-059/060/061)**: 사용자 요구 — *"돈을 요구하고 계좌를 알려주는 것까지는 좋은데, 이걸 문자 메시지로 링크 형태로 보내고, 문자로 전환되는데, 전화 연결은 유지되도록 하는 UX가 있으면 좋겠다."* 통화 중에 사기범이 계좌·링크·인증번호를 문자로 밀어 넣고, 참가자는 **통화를 유지한 채** 그 문자를 열어 읽고 통화로 돌아온다. 문자가 오면 통화 화면 위에 도착 배너가 내려오고(스크린리더 알림 동시), 탭하면 문자함이 통화 화면 **위에 겹쳐** 열린다. 이후 통화 셸에 "문자함"(N) 버튼이 남아 언제든 다시 볼 수 있다.
  - **통화는 멈추지 않는다.** 문자함은 **별도 화면(라우트)이 아니라 통화 화면 위에 겹치는 계층**이다 — 별도 화면으로 옮기면 통화 컴포넌트가 내려가면서 실시간 세션·마이크·타이머가 통째로 끊기기 때문이다. 문자를 보는 동안에도 경과 타이머가 계속 흐르고, 상대 음성 재생·대화 기록·세션 한도가 그대로 살아 있으며, 문자함 상단에 발신자·경과 시간·"통화 중"이 **텍스트로** 계속 표시된다. **문자를 보는 동안 멈추는 것은 마이크 입력 하나뿐**이다(읽는 중 혼잣말·주변 소음이 발화로 오인돼 상대가 엉뚱하게 반응하는 것을 막기 위해 — 음소거 버튼 표시는 사용자가 직접 켠 경우에만 바뀐다). 통화 시간 한도에 걸리면 문자함이 **먼저 닫히고** 종료 고지가 가려지지 않는다. 이 구조가 나중에 조용히 깨지지 않도록 소스 가드 테스트로 고정했다(별도 라우트 신설·조기 반환·타이머에 문자 상태 끼워 넣기를 전부 잡는다).
  - **"화면에 뜨는 인증번호를 불러달라는데 아무것도 안 보인다"는 신고가 함께 해소된다.** 이제 6자리 모의 인증번호가 **실제로 도착해 화면에 표시된다**(본문의 "타인에게 절대 알려주지 마세요" 경고까지 그대로 재현 — 사기범이 그 경고를 무시하라고 요구하는 **모순 자체가 학습 포인트**다). **앱이 인증번호를 자동으로 복사하거나 어디로도 보내지 않는다** — "복사해서 붙여 넣는" 실행 동선을 앱이 대신 만들어 주지 않는다. 인증번호를 통화로 불러준 사실은 기존 판정 경로를 통해 리포트에서 "속은 시점"으로 잡힌다.
  - **문자함은 읽기 전용이다.** 답장·전달·전송 경로가 화면에도 API에도 존재하지 않는다. 링크는 **표시 문구만 있고 실제 주소가 스키마에 아예 없어** 외부로 나가는 경로가 구조적으로 불가능하며, 누르면 **기존 인앱 가짜 랜딩**으로만 전환된다(새 가짜 화면을 만들지 않았다 — 안전 검증 경로를 둘로 늘리지 않기 위해). 문자에 실리는 발신번호·계좌·기관명·인증번호는 전부 **서버가 가진 고정 모의값**이며 AI가 만들어내지 않는다. 문자함 안에서도 "훈련 종료"와 모의 표식에 항상 도달할 수 있다.
  - **함께 고친 것 — 안 고쳤으면 기능이 영영 발동하지 않을 지점**: 직전 임시방편(T62)으로 프롬프트에 넣었던 *"문자로 방금 보낸 인증번호를 불러 달라고 요구하지 않는다"* 금지 규칙을 **문자가 실제로 도착하는 시나리오에서만** "문자로 도착한 것은 요구해도 된다"로 바꿨다(그 전제가 뒤집혔기 때문). 문자 카탈로그가 없는 나머지 시나리오는 **문구가 한 글자도 바뀌지 않는다**(테스트로 고정). 무해화 경계(실존 기관의 실제 계좌·실제 동작하는 링크·실제 연락처 금지)와 안전 지침이 프롬프트 맨 마지막이라는 순서 규칙은 세 난이도·13개 시나리오 전 조합에서 그대로 유지됨을 실측했다.
  - **정직 고지**: 본인 목소리 복제(ElevenLabs) 실시간 통화 경로에서는 **문자가 도착하지 않는다** — 사기범 프롬프트가 외부 에이전트 쪽에 저장돼 있어 "문자 보냈다고 말하라"는 지시를 넣을 지점이 없다(난이도가 이 경로에 적용되지 않는 것과 같은 구조적 이유). 무료 통화 경로(Gemini)와 텍스트 폴백 경로에서는 정상 동작한다. 또한 이번 작업의 브라우저 UI 워크스루는 **이 환경에서 검증하지 못했다**(브라우저 도구 부재) — 서버 동작은 격리 에뮬레이터 실측 29/29로 확인했다.
  - **⚠️ 문서 소유권 고지**: 이 항목은 implementer가 작성했다 — docs/CHANGELOG.md는 정식으로는 docs 에이전트 소관(AGENTS.md)이라 후속 docs 패스에서 재검토를 권장한다. 리뷰어/QA 검토 대기(review 상태, done 아님).

- **실패 아카이브 — 내가 속았던 순간 모아보기(T74, 2026-07-25)**: 사용자 요구 — "내가 당했던 순간만 모아 보는 화면(반복 학습)". 지난 훈련에서 **넘어갔던 순간만** 모아 보는 화면(`/report/archive`)을 추가했다. 기존 히스토리(`/history`)가 "언제 어떤 훈련을 했나"(세션 1건 = 항목 1건)라면, 이 화면은 "내가 어디서 넘어갔나"(**속은 순간 1건 = 항목 1건**)다 — 한 세션에서 3번 속았으면 항목이 3개로 뜬다. 리포트·히스토리·되감기 화면에서 들어갈 수 있다.
  **"수법별로 묶기"를 켜면 "이 수법에 3번 넘어갔습니다"처럼 반복 패턴이 드러난다.** 이게 되려면 표기 정리가 필요했다 — 같은 긴급성 압박도 시나리오마다 "긴급성 조성"·"다급함 조성"·"마감 압박"·"촉박한 결정 압박"으로 이름이 달라서, 그대로 두면 "3번"이 "1번+1번+1번"으로 흩어진다. 그래서 리포트를 만들 때 수법을 고정 분류(10종)로 함께 기록하도록 했다. **화면에 보이는 문구는 예전 그대로**이고 분류는 묶는 데만 쓴다. 실측: 서로 다른 표기 5건이 정규화 후 3묶음(2·2·1)으로 정확히 모였다(정규화가 없으면 5묶음). 예전에 만들어진 리포트는 분류가 없어 예전처럼 표기별로 묶인다(과거 기록을 소급 수정하지 않음).
  각 항목에서 **그때 대화 보기**(해당 시점으로 이동) · **그 순간 다시 해보기**(되감기) · **이 시나리오 다시 훈련**으로 바로 이어갈 수 있다. 빈 화면은 두 가지를 **구분**한다 — "아직 훈련 기록이 없습니다"와 "지금까지 넘어간 순간이 없습니다"(후자에 "이제 면역됐다" 같은 표현은 쓰지 않는다). 톤은 질책이 아니라 "다시 연습해볼 지점" 프레임을 유지한다.
  **⚠️ 2인 챌린지에서 지인(수신자)이 남긴 기록은 이 화면에 어떤 형태로도 들어오지 않는다** — 조회 자체가 본인 계정 데이터만 보도록 서버 규칙으로 닫혀 있고, 챌린지에서 나온 기록은 화면에서 한 번 더 걸러 낸다(이중 방어). 에뮬레이터 실측으로 발신자 조회 결과에 수신자 기록이 0건이고 직접 문서를 읽으려 해도 권한 거부되는 것을 확인했다. 세션 종료 시 폐기되는 합성 음성은 **재생 경로 자체가 없고** 텍스트 정보만 남는다. 검색·기간 필터·통계 대시보드는 넣지 않았다(요청되지 않은 기능).

- **통화 후 즉시 되감기(T70, 2026-07-25)**: 사용자 아이디어 — "종료 직후 '이 순간 뭐라고 했어야 했나'를 그 자리에서 한 턴만 다시 해보기". 리포트에서 **속았던 순간으로 돌아가 다시 답해보고 즉시 판정·모범 대처를 받는** 화면(`/report/rewind`)을 추가했다. 기존 리플레이 해설(읽는 복기)과 달리 **그 순간을 다시 연기해 보는** 것이 목적이라 별도 화면으로 만들었다. 리포트 요약·타임라인 항목·종료 화면·리플레이 4곳에서 들어갈 수 있고, **한 번도 속지 않은 세션에서는 진입점이 아예 보이지 않는다**(없는 데이터를 만들어내지 않기 위해). 판정은 AI가 하되 실패하면 규칙 기반으로 내려앉고, 어느 쪽으로 판정했는지를 숨기지 않는다.
  **⚠️ 되감기는 원래 리포트를 절대 건드리지 않는다**: 새 리포트를 만들지도, 원 판정(속았는지·속은 순간)을 바꾸지도, 방어등급을 다시 계산하지도 않는다 — 시도는 별도 하위 기록으로만 쌓인다. 에뮬레이터로 되감기 3회 실행 후 원 리포트 문서가 `updateTime`까지 완전 무변경이고 리포트 수가 1개 그대로임을 실측 확인했으며, 이 불변식이 나중에 깨지지 않도록 소스 가드 테스트로 고정했다(reviewer가 위반 코드를 일부러 주입해 테스트가 실제로 잡아내는 것까지 확인). 답변에 넣은 개인정보는 마스킹 후에만 저장된다.
- **난이도 단계 — 초급/중급/고급을 사용자가 고른다(T72, 2026-07-25, PRD v1.5 MVP #26, AC-064~067)**: 사용자 요구 — "초급은 티가 나는 수법, 고급은 실제처럼 정교하게". 지금까지 시나리오 카드의 "난이도: 쉬움~중간"은 시나리오마다 **고정된 설명 텍스트**라 사용자가 고를 수 없었다. 이제 드릴다운의 **마지막 단계**(시나리오 확정 직후·세션/챌린지 생성 직전)에 난이도 선택 화면(UX-029)이 들어가고, 고른 값이 사기범의 실제 대화 방식에 반영된다 — 초급은 수법을 눈에 띄게 쓰고 의심하면 쉽게 물러서며, 고급은 압박이 세고 요구가 빨리 오며 수법을 자연스러운 대화에 은밀히 섞는다. 고른 난이도는 세션 화면 배지·리포트·챌린지 요약·수신자 동의 랜딩에 **같은 3단계 라벨**로 표기된다(2인 챌린지에서는 발신자가 "상대가 겪을 강도"를 정하고, 고급이면 보내기 전에 고지한다). 초급을 고르면 **세션 시작 전에** "이 대화에서 나올 수 있는 위험 신호"를 미리 볼 수 있다 — 대화 중 실시간으로 짚어 주지는 않는다(스스로 알아채는 연습이 목적).
  - **안전장치는 세 난이도에서 완전히 동일하다.** 사전 고지·명시적 동의·상시 훈련 종료·AI 합성/모의 표식·종료 고지·생성물 폐기·2인 안전제약·면책 배너·활성 챌린지 상한은 난이도와 무관하게 그대로 강제된다. **"고급"은 더 진짜 같은 압박이지 더 진짜에 가까운 위험 정보가 아니다** — 실존 기관의 실제 계좌·실제 동작하는 링크·실제 연락처를 만들지 않는 무해화 경계는 고급에서도 완화되지 않는다. 이 규칙을 문구가 아니라 **프롬프트 조립 순서**로 강제한다: 안전 지침 블록은 언제나 프롬프트의 맨 마지막이며 난이도 지시는 반드시 그 앞에 들어간다(세 난이도 × 13개 시나리오 × 턴 지시 유무 78개 조합 전부 실측 확인, 자동 테스트로 고정).
  - **중급 = 지금까지와 완전히 같은 대화**다. 중급은 난이도 지시 블록을 아예 내보내지 않으므로, 난이도 도입 전과 프롬프트 문자열이 한 글자도 다르지 않다(13개 시나리오 전부 도입 전 산출물과 완전 일치 실측). 난이도를 지정하지 않은 기존 세션·리포트도 그대로 유효하다.
  - **난이도는 리포트 판정 기준을 바꾸지 않는다** — 어떤 난이도로 훈련했든 "속았는지·어떤 수법에 넘어갔는지"는 같은 잣대로 판정된다(그래야 반복 학습에서 누적 비교가 의미를 갖는다). 리포트에는 표기로만 남는다.
  - **정직 고지**: 본인 목소리 복제(ElevenLabs) 실시간 통화 경로는 사기범 프롬프트가 외부 에이전트 쪽에 저장돼 있어 **난이도를 주입할 지점이 없다.** 이 경로에서는 난이도 배지를 띄우지 않고 "이 통화 경로에서는 고른 난이도가 적용되지 않습니다"를 표시한다 — 적용되지 않은 값을 표기하지도, 조용히 무시하지도 않는다.
  - 함께 고친 것: 오프닝 대사 생성이 안전 지침 **뒤에** 별도 지시를 이어 붙이던 기존 조립 순서 위반을 제거했다(난이도 도입 전에는 실질 피해가 없었지만, 같은 방식으로 난이도를 붙였다면 그 즉시 안전 설계가 깨지는 지점이었다). 시나리오 카드의 기존 고정 문자열은 삭제하지 않고 라벨만 **"이 시나리오의 성향"**으로 바꿔, 한 화면에서 "난이도"라는 단어가 두 가지 뜻으로 쓰이지 않게 했다.
  - **⚠️ 문서 소유권 고지**: 이 항목은 implementer가 작성했다 — docs/CHANGELOG.md는 정식으로는 docs 에이전트 소관(AGENTS.md)이라 후속 docs 패스에서 재검토를 권장한다. 리뷰어/QA 검토 대기(review 상태, done 아님).

- **개발 전용 빠른 로그인(T63, 2026-07-25)**: Google OAuth 팝업은 별도 창이라 자동화로 끝까지 클릭할 수 없어, 로그인 이후의 모든 화면이 자동 검증 사각지대로 남아 있었다. 개발 빌드에서만 보이는 "익명 계정으로 빠른 로그인" 버튼을 추가해 **인증 단계만** 건너뛴다 — 동의·연령확인 화면은 그대로 거친다(안전 게이트를 개발 편의로 우회하는 경로를 만들지 않기 위해). 프로덕션 빌드에는 이 버튼도, 사인인 코드도 남지 않는다(빌드 산출물 grep으로 실증). 이 기능으로 로그인→통화→리포트 전 구간을 자동 검증할 수 있게 됐고, 실제로 직전 수정들(사기범 요구 진행·리포트 순응 판정)이 UI 경로에서 정상 동작함을 확인했다.

- **통화 중 키패드·타이핑 입력(T62, 2026-07-25)**: 사용자 신고 — "키패드가 있는데 실제로 작동하지 않는다. 타이핑으로도 통화할 수 있게 해달라". 키패드 버튼이 실제 숫자 패드(0~9·*·#)를 열고, 누른 번호가 화면에 표시되며 상대에게 전송된다(ARS 인증번호 요구 같은 상황에 대응 가능). 실시간 통화 중에도 텍스트로 말할 수 있게 됐다 — 예전엔 "지금은 음성으로 대화하는 중입니다" 안내만 뜨고 입력 수단이 없었다. 타이핑한 문장은 별도 텍스트 LLM이 아니라 **같은 실시간 세션 안으로** 들어가므로 목소리가 겹치지 않으며, 전사에도 기록돼 리포트에 정상 반영된다. 마이크가 없거나 쓸 수 없는 환경에서도 훈련·검증이 가능해진다.

### Fixed
- **훈련이 시나리오 도중 강제 종료되던 문제(T62, 2026-07-25)**: 사용자 신고 — "시나리오가 진행 중인데 강제로 끝나는 것 같다". 세션 한도가 해커톤 데모 길이 기준(10턴/6분)이라, 사기범이 실제 요구까지 진행하도록 고친 뒤로는 시나리오가 끝나기 전에 한도가 먼저 걸렸다. 정상 훈련에서는 사실상 걸리지 않는 크기(100턴/60분)로 상향했다. 완전 제거가 아니라 상향인 이유는 이 값이 무한정 열린 실시간 세션을 막는 유일한 서버측 백스톱이기 때문이다.
- **리포트가 "잘 대처했다"고 오판하던 문제 2차 수정(T62, 2026-07-25)**: 사용자 신고 — "시나리오대로 따라가서 대출을 한다고 했는데 리포트는 요구에 응하지 않았다고 나온다", "성함과 주민번호를 말하는 것이 자동으로 넘어간다". 두 개의 독립된 원인을 확인했다. **(A)** 리포트 분석은 원문이 아니라 **마스킹된 텍스트**를 보는데, 개인정보를 넘겨주면 `[주민번호]`·`[전화]`·`[이메일]` 토큰으로 바뀌고 이 토큰들이 순응 신호 목록에 없었다 — 즉 **개인정보를 넘겨준 가장 전형적인 순응 행위가 통째로 감지되지 않았다.** **(B)** 순응 표현이 "돈을 보낸다" 계열 동사에만 맞춰져 있어, 대출·설치·신청처럼 송금이 아닌 요구에 응한 경우("네 진행해주세요", "그렇게 할게요", "신청할게요", "설치했어요")가 전혀 잡히지 않았다. 키패드로 번호만 눌러 보낸 답변도 함께 인식하도록 했다. 라이브 에뮬레이터로 실제 대출 시나리오 순응 대화를 통과시켜 `wasDeceived: true`·시도된 수법·속은 시점이 모두 정확히 나오는 것을 확인했다.
- **사기범이 앱에 없는 것을 요구하던 문제(T62, 2026-07-25)**: 사용자 신고 — "화면에 뜨는 인증번호를 불러달라는데 아무것도 보이는 게 없다". 참가자가 실제로 볼 수 없는 것(방금 보낸 문자, 팝업 등)을 가리키지 않고, 지금 알거나 직접 정할 수 있는 것만 요구하도록 프롬프트에 규칙을 넣었다. 또 이름·주민번호를 받으면 곧바로 "확인됐습니다"로 넘기지 말고 되읽어 확인하거나 추가 항목을 요구하도록 해, 개인정보를 여러 번 넘기고 있다는 사실이 체감되게 했다.

- **사기범이 실제 요구에 영영 도달하지 못하던 구조적 결함 — 훈련이 "결정의 순간"에 도달 불가였음(T61, 2026-07-25)**: 사용자 신고(실사용 스크린샷) — "사기를 당하려고 해도 실질적으로 피해를 주려는 행동이 보이지 않는다". 카드사 사칭 AI가 "정말 없으세요? 다시 확인해 보세요!"만 4턴 연속 반복하고 아무 요구도 하지 않는 것이 실측됐다. **원인**: 13개 시나리오 중 11개의 수법 목록이 **전부 분위기 조성**(놀람·긴급성·확인 차단·안심 유도·끊기 저지)뿐이고 "무엇을 하라"는 요구 수법이 **하나도 없었으며**, 게다가 11개 전부에 "요구를 물으면 답을 미루라"는 항목이 명시돼 있어 **요구 자체가 프롬프트 수준에서 금지**돼 있었다. 사용자가 넘어가려 해도 시뮬레이션이 요구에 도달할 수 없어 압박만 무한 반복된 것 — "안 당해본 사기는 못 막는다"는 제품 전제가 성립하지 않던 상태다. **수정**: 모든 시나리오가 공유하는 프롬프트 조립 지점에 진행 강제 규칙을 넣고(2~3턴 안에 구체적 요구 도달, 같은 압박 반복 금지, 거절당하면 다른 카드·낮춘 조건으로 재요구, 응하면 다음 단계로 진행), 11개 시나리오의 "얼버무리기" 항목을 시나리오별 구체적 요구로 교체했다(계좌 이체·카드정보·인증번호·개인정보 요구, ATM 조작·앱 설치 지시, 상품권 대체 요구, 금액 낮춰 재요구, 비밀 유지·신고 차단 지시 등 **26개 신설**). 요구에 실려 나가는 값(계좌번호·앱 이름·링크·연락처)은 실존하지 않는 기관명을 쓰되 숫자는 실제처럼 들리는 형식으로 부른다(예: `OO은행 352-0812-4471-63` — 실존하지 않는 곳이라 어디로도 연결되지 않지만 참가자에게는 진짜 계좌를 불러주는 것과 똑같이 들려 몰입이 깨지지 않는다) — 참가자가 겪는 판단 상황은 동일하되 산출물이 그대로 쓸 수 있는 사기 대본은 되지 않는다. 안전 지침·인젝션 방어·PII 마스킹은 13개 전부 무수정. **실 LLM이 지침을 따르는 정도에 달려 있어 코드로 보장되지 않는다 — 라이브 확인 필요.**
- **AI가 자기 말을 스스로 끊을 수 있던 구조적 버그(T60, 2026-07-25)**: 실시간 통화에서 "상대가 말하는 중" 상태가 **오디오 청크 도착** 시점 기준 고정 600ms로 꺼지고 있었다. Gemini는 오디오를 실시간보다 빠르게 보내주므로(4초 발화의 데이터가 1초 만에 다 도착) 실제 재생이 끝나기 한참 전에 이 상태가 풀렸고, 그 순간 마이크가 다시 열려 스피커로 나가던 AI 목소리가 마이크로 되돌아가 AI가 자기 말을 사용자 발화로 오인해 끊을 수 있는 구조였다. 예약된 재생이 실제로 끝나는 시각 기준으로 바로잡았다. AI가 말하는 중에 사용자 파형이 잘못 켜지던 문제도 함께 해소된다.

### Changed
- **대화가 독백처럼 흐르던 문제 — 대화 방식 지침 신설(T60, 2026-07-25)**: 사용자 신고 — "사람과 대화한다는 느낌이 들지 않는다", "시나리오대로 유도하되 사용자의 말을 파악해서 흘러가야 한다". 기존 시나리오 프롬프트에는 페르소나(누구인가)와 수법 목록(무엇을 하는가)만 있고 **어떻게 주고받는가**에 대한 지침이 없어, 사용자가 무슨 말을 하든 다음 수법을 순서대로 읊는 대사가 나왔다. 모든 시나리오·모든 경로가 공유하는 프롬프트 조립 지점에 대화 방식 지침을 넣었다 — 상대가 방금 한 말에 먼저 반응하고, 한 번에 1~3문장만 말하고 차례를 넘기며, 구어체로 말하고, 시나리오의 목적지는 유지하되 경로는 상대의 반응에 맞춘다. 안전 지침은 기존대로 프롬프트 맨 마지막에 유지된다(순서 불변식을 테스트로 고정). **효과는 실 LLM이 지침을 얼마나 따르는지에 달려 있어 코드로 보장되지 않는다 — 라이브 통화로 확인 필요.**
- **"받기" 후 AI 발화까지의 지연 단축 — 수신 중 통화 자격증명 프리페치(T59, 2026-07-25)**: 사용자 신고 — "전화가 연결되서 받으면 바로 말을 하는 걸로". 전화가 울리는 동안(아직 "받기" 전) Gemini/ElevenLabs 자격증명을 미리 받아 둬, "받기" 시점엔 네트워크 왕복 없이 곧장 연결한다(마이크 접근은 여전히 "받기" 이후에만). **트레이드오프(사용자 승인)**: 이 프로젝트의 Gemini 무료 티어 하루 20건 한도(T53에서 한 번 소진된 이력) 특성상, 전화를 안 받거나 거절해도 자격증명 1건이 소모된다 — 사용자에게 명시하고 진행 승인을 받았다.

### Fixed
- **통화 오프닝 대사가 배경 설명 없이 다짜고짜 요구부터 시작(T58, 2026-07-25)**: 사용자 신고 — "처음에는 시나리오 진행에서 필요한 배경을 말하면서 시작하자". `generateOpeningLine()`(ElevenLabs/텍스트 폴백 공용)과 Gemini Live 실시간 경로의 오프닝 트리거 문구 양쪽에 "신분과 연락 이유를 먼저 밝히고 이어가라"는 지침을 오프닝 전용으로 추가했다(후속 턴 프롬프트는 무수정). Mock LLM은 이 변경의 영향을 받지 않는다(systemPrompt 문자열을 읽지 않음, 코드 확인). functions 184/184·root 42/42 무회귀.
- **generic 보이스 통화 성별 다양화(T57, 2026-07-25)**: `functions/src/realtime/geminiProvider.ts`가 Gemini Live 프리셋 음성을 `"Aoede"` 1개로 고정해, generic voiceMode 경로(보이스피싱 self 체험 + generic 보이스 챌린지 수신자 통화)가 사용자 신고대로 항상 같은 음성만 냈다. 신규 음성 풀(`Aoede`+`Puck`)에서 sessionId 해시 기반으로 결정론적으로 하나를 고르도록 교체 — 같은 통화는 재연결·Strict Mode 이중 mount로 다시 호출돼도 목소리가 바뀌지 않고, 세션마다 사실상 무작위로 분산된다. **추정 표시**: Google 공식 문서는 각 프리셋 음성을 톤 특성으로만 설명하고 성별을 공식 라벨링하지 않아, "Aoede=여성/Puck=남성" 분류는 커뮤니티 통용 추정치임을 코드 주석에 명시했다. functions test 184/184(신규 3건). 실제 음색 청취는 API 키·마이크가 없는 이 환경에서 라이브 검증 못함 — 사용자 확인 필요.

### Added
- **체험/발송 선택 지점 재배치 + generic 보이스 2인 챌린지(T54~T56, 2026-07-24, PRD v1.4 MVP #22/#23)**: "본인이 체험 / 지인에게 보내기" 결정을 시나리오 선택 직후가 아니라 **유형(보이스/메신저) 선택 직후**로 앞당겼다 — 방식(clone/generic) 선택도, 시나리오 선택도 이 결정 뒤로 밀린다. 이에 따라 보이스피싱 "본인이 체험" 경로는 이제 **기본 AI 음성(generic) 시나리오만** 제공하고, 본인 목소리 복제(clone) 시나리오는 자기 체험에서 완전히 배제된다 — 클론 딥보이스는 오직 "지인에게 보내기" 2인 챌린지로만 도달한다(자기 목소리를 복제해 자기에게 거는 것은 "그 목소리를 아는 사람이 받는다"는 착시가 성립하지 않아 훈련 효과가 없다는 판단). 대신 **generic 보이스도 2인 챌린지로 보낼 수 있게** 새로 열었다 — 발신자 클론·통화 자격증명 발급 없이 기존 기본 음성 합성 경로만으로 성립하는, 음성 없는 메신저 챌린지와 동형의 "깨끗한" 신규 능력이다. 챌린지 만들기 화면은 이제 클론/기본 음성/메신저 3종의 문구를 구분해 보여준다. generic 보이스 챌린지의 발신자 결과 열람은 메신저 챌린지와 동일하게 "완료 여부"만 노출한다(의심 시점 등 세부는 절대 노출하지 않음 — 채널·음성모드 무관하게 수신자 프라이버시를 우선하는 기존 원칙과 정합). 기존 클론 딥보이스 챌린지·음성 없는 메신저 챌린지·활성 챌린지 개수 상한(전역 3개)은 전부 무변경으로 병존한다. Planner→UX-design→Architect→Implementer 단계를 거쳤고, 라이브 에뮬레이터로 신규 능력(생성→수신자 동의→통화 자격증명 발급→결과 열람 전 구간)과 기존 클론·메신저 챌린지 무회귀를 확인했다(functions 181/181·root 42/42 테스트 통과). **⚠️ 문서 소유권 고지**: 이 항목은 implementer가 작성했다 — docs/CHANGELOG.md는 정식으로는 docs 에이전트 소관(AGENTS.md)이라, 정확한 내용이라도 소유권상 후속 docs 에이전트 패스에서 재검토를 권장한다. 리뷰어/QA 검토 대기 중(review 상태, done 아님).
### Changed
- **사용자 제공 디자인 핸드오프 앱 전체 적용(T41, 2026-07-24)**: 사용자가 직접 전달한 고정밀 디자인 핸드오프(색상·타이포·간격·컴포넌트 상태까지 확정된 7개 플로우 프로토타입)를 앱의 22개 화면 중 18개 파일에 반영했다(4개는 감사 결과 이미 토큰을 준수하고 있어 무변경). 순수 프레젠테이션 변경만 — 기능·라우팅·데이터흐름·백엔드는 전혀 건드리지 않았다. 신규 공용 컴포넌트(`src/components/ui/`: Button/Badge/SelectableCard/ProgressSteps/Banner)를 만들어 5개 플로우(온보딩·훈련·리포트·메신저+전이·챌린지)에서 재사용했다. 안전 게이트(동의 체크박스 게이팅, 2인 챌린지 무동의 재생 경로 부재, 상시 종료·AI 합성 음성 고지, 닫을 수 없는 면책 배너)는 전부 무변경 확인. 카카오톡/문자 브랜드의 정확 모사(기존 확정 결정, OQ-U6)는 새 핸드오프의 "브랜드 범용화" 권고와 충돌해 기존 결정을 우선하고 따르지 않았다. reviewer 리뷰에서 발견한 접근성 회귀(저장된 목소리 선택 목록이 라디오그룹에서 버튼으로 바뀌며 스크린리더 시맨틱스 상실) 수정 완료, APPROVED.
### Added
- **음성 없는 메신저 2인 챌린지 + 진입점 통합(T45~T49, 2026-07-24, 사용자 신고 "메신저도 지인에게 보내야 하는데 그런 기능이 없다"에서 출발)**: 기존 2인 소셜 챌린지(보이스 딥보이스 전용)를 카카오톡/문자 메신저 시나리오까지 확장했다. 발신자는 메신저 시나리오(비에스컬레이션만 — 대출 부탁·택배 스미싱 2종)를 골라 클론 온보딩 없이 챌린지 링크를 만들 수 있고, 수신자는 로그인 없이(익명 인증) 링크로 들어와 기존 메신저 채팅 셸을 그대로 체험한 뒤 강제로 정체가 공개되고 리플레이 해설로 이어진다. 발신자가 보는 결과는 "완료 여부"만 — 스미싱 링크 탭·정보입력 등 "의심 시점" 세부는 어떤 형태로도 노출하지 않는다(프라이버시 우선, 쓰기 시점부터 강제 — 나중에 필터링하는 방식이 아니라 애초에 저장하지 않음). 에스컬레이션 가능(통화로 이어지는) 메신저 시나리오는 아직 지원하지 않는다 — 통화 전이 시 발신자의 복제 목소리를 쓸지 기본 목소리로 갈지가 아직 결정되지 않아 후속 작업으로 미뤘다. **디자인 변경**: 처음엔 "지인에게 보내기"를 기존 보이스 챌린지처럼 별도 메뉴로 만들려 했으나, 사용자가 전체 화면 흐름을 다시 검토한 뒤 "시나리오를 고른 직후 본인이 체험할지 지인에게 보낼지 한 번에 묻는" 방식으로 바꾸기로 결정 — 그 결과 기존 별도 진입 메뉴는 없어지고, 보이스·메신저 어느 쪽이든 시나리오를 고르면 곧바로 이 선택 화면으로 넘어간다. Planner→UX-design→Architect→UX-design(설계 반전)→Implementer 전 단계를 거쳤고, reviewer APPROVED·QA GO(라이브 검증 포함, functions 164/164·root 31/31 테스트 통과).
- **사후 리플레이 해설 + 2인 소셜 딥보이스 챌린지(발신 측) + 보이스→메신저 역방향 전이(T33/T36/T40, 2026-07-24)**:
  - **사후 리플레이 해설(T33)**: 세션 종료 후 `/report/replay`에서 대화를 처음부터 시간순으로 되짚으며 각 사기 신호 시점에 주석을 확인할 수 있다(UX-018). 신규 백엔드·신규 분석 로직 없이 기존 리포트(T9)·transcript를 순수 병합만 한다(AC-038). 요약 리포트(UX-008)와 별도 화면 — 이후 2인 소셜(T37)에서 강제 단계로 재사용될 예정.
  - **2인 소셜 딥보이스 챌린지 — 발신(사용자1) 측(T36)**: 로그인한 사용자가 자기 목소리를 클론해 지인에게 보낼 공유 링크(토큰)를 발급한다. 챌린지 전용 음성은 항상 새로 발급되는 독립 클론이며 훈련 세션의 목소리와 절대 공유되지 않는다(ADR-0005). 공유 토큰은 해시만 저장하고 평문은 발급 시 1회만 반환한다. 활성 챌린지 3개 상한, 링크 3일 만료, 목소리 30일 기본 보존(수동/자동 삭제). 수신(사용자2) 측 동의·체험·신고는 별도 후속 작업.
  - **보이스→메신저 역방향 전이(T40, fast-follow)**: 통화 중 "메시지로 전환" 버튼으로 메신저 채팅으로 되돌아갈 수 있다(수동 버튼만 — 구조화 신호·max-turn 폴백은 설계 문서가 없어 이번 범위 밖). 메신저→보이스(T30)의 방향 무관 전이 엔진을 그대로 재사용.
- **2인 소셜 딥보이스 챌린지 — 수신(사용자2) 측(T37, ADR-0006, 2026-07-24)**: 로그인 없이 공유 링크(`/challenge/join?token=...`)만으로 진입해(AC-040/048) 발신자(사용자1)의 복제 목소리로 걸려오는 딥보이스 체험을 겪고, 종료 시 강제로 정체가 공개된 뒤(AC-042) 대화를 되짚어보는 리플레이 화면으로 인계된다(T33 재사용). 사용자2는 링크를 열 때 화면 뒤에서 조용히 Firebase 익명 인증(Anonymous Auth)에 사인인해 임시 uid를 얻고, 그 uid가 체험 세션을 소유해 기존 세션/리포트/실시간통화 인프라와 소유권 기반 firestore.rules를 전혀 개정하지 않고 그대로 재사용한다. **핵심 안전장치(A1)**: 챌린지 전용 클론 목소리(voiceId)는 사용자2의 세션 문서에 절대 저장되지 않는다 — 저장하면 (1) 사용자2가 소유자 자격으로 자기 세션을 읽을 때 사용자1의 원시 클론 id가 유출되고, (2) 세션 종료 시 작동하는 기존 폐기 트리거가 그 값을 지워버려 사용자1의 챌린지 클론이 사라진다. 대신 실시간 통화 자격증명 발급(`createRealtimeCall`) 시점에 서버가 챌린지 문서에서 매번 새로 읽어 해석한다. 결과 공유는 동의 시에만 "완료 여부" 수준으로만 사용자1에게 노출되고 대화 전문은 절대 넘어가지 않는다(AC-043). 원치 않는 링크는 무동의로도 신고할 수 있다(AC-049). reviewer APPROVED(Critical 0, Major 1 — 아래 Fixed 참고), QA GO(AC-040/041/042/043/044/048/049 라이브 에뮬레이터 45/45).
- **메신저피싱 확장 — 채널 구분 + 메신저→보이스 에스컬레이션(T27~T31, 2026-07-23~24)**: 이제 앱은 보이스피싱뿐 아니라 카카오톡/문자(SMS) 메신저피싱도 훈련할 수 있고, 메신저 대화 중 상대가 의심하면 실제 통화로 자연스럽게 넘어간다(양방향 설계·순차 구현 확정, OQ-22).
  - **신규 시나리오 8종(T27)**: 보이스 4종(카드사 사칭, 택배/통관 사기, 납치 협박, 사생활 유포 협박 — 사용자 피드백에 따라 어조를 더 차갑고 압박적으로 강화하되 구체적 폭력·성적 묘사는 넣지 않음), 메신저 4종(자녀 사칭 급전 요청·카카오톡, 지인 사칭 대출 부탁·카카오톡, 택배 스미싱·문자, 정부지원금 스미싱·문자). 총 13종. `ScenarioDoc`에 `channel`/`surface`/`escalation` 옵셔널 필드 추가(하위호환).
  - **단계 선택형(드릴다운) 시나리오 UI(T28)**: 기존 전체 목록형 대신, "보이스 vs 메신저" → "(보이스면) 복제 vs 기본 음성" 순으로 선택할 때마다 다음 화면이 나타나는 흐름으로 개편(UX-015~017).
  - **메신저 채팅 화면(T29)**: 카카오톡/문자 UI를 각각 그대로 재현(정확 모사 확정, OQ-U6)한 채팅 화면, iOS/갤럭시 자동 기기 감지+스킨, 스미싱 링크는 `[[LINK:id]]` sentinel 마커로만 표현해 실제 URL이 존재하지 않는 인앱 가짜 랜딩 목업으로 대체(AC-032, OQ-21 — `MessengerFakeLanding`은 네트워크 호출 임포트 자체가 없어 구조적으로 안전).
  - **메신저→보이스 에스컬레이션(T30)**: 트리거 3종 — 역할극 LLM이 캐릭터 판단으로 자기 응답에 심는 구조화 신호(`[[SIGNAL:ESCALATE_VOICE]]`, 자유텍스트 직접 분류 아님, OQ-16 재확정), 6턴 초과 시 max-turn 폴백, 언제든 누를 수 있는 "전화로 확인" 수동 버튼. 하나의 세션 문서·하나의 대화 로그·정확히 1개 리포트를 유지한 채 채널만 바뀐다(`channel`/`channelHistory`, Architecture.md §13). 통화로 넘어갈 때 쓸 목소리를 미리 고르는 UX-025(즉시 녹음/보관된 목소리 재사용/남·여 기본 목소리) 신설.
### Fixed
- **보이스피싱 시나리오 9종 압박감 강화(T53, 2026-07-24, 사용자 피드백: "시나리오가 실제 보이스피싱보다 너무 순하고 압박이 약하다")**: 보이스 전용 시나리오 9종(카드사 사칭·택배/통관 사기·가족 사고 딥보이스·손주 사칭·기관 사칭·납치 협박·대출 빙자·유포 협박·환급금 빙자, 메신저 4종은 사용자 피드백 범위 밖이라 무변경)의 `personaPrompt`/`weakenedTactics`를 검토한 결과, 실제 보이스피싱 패턴(다급함 조성·확인 절차 차단 등) 대비 공통으로 빠진 압박 축 세 가지를 확인했다: (1) 상대가 한 번 저항해도 다음 수법으로 그냥 넘어가고 같은 압박을 다른 표현으로 재차 밀어붙이지 않는 "어조 격화" 부재, (2) "지금 전화를 끊으려는 시도 자체"를 저지하는 명시적 수법 부재(확인 절차를 막는 수법은 있었지만 끊는 행위 자체에 대응하는 문구는 없었음), (3) 생각할 틈을 주지 않는 속사포 질문 패턴 부재. 9개 파일 전부의 `[말투/톤]`에 저항이 계속되는 동안 표현을 바꿔 재차 압박하는 문단을 추가하고, `weakenedTactics`에 "전화 끊음 저지"(가족·손주 사칭류는 감정 격화형, 납치 협박은 촉박한 결정 압박형으로 변형)를 전 시나리오에, 사무형 6종(카드사·택배·기관사칭·대출·환급금)에는 "속사포 질문"을 추가로 넣었다. `guardrailPreamble`(공유 안전 보일러플레이트)은 9개 전부 무수정이고, 신규 문구에도 실제 기관명·전화번호·계좌번호·URL 등 운영 가능 정보는 전혀 넣지 않았다(AC-005). `kidnappingThreat`/`reputationBlackmailScam`의 기존 "묘사 강도가 아니라 압박 강도만 올린다"는 2026-07-23 확정 원칙(구체적 폭력·신체 손상·성적 내용·업소명 묘사 금지)도 그대로 유지했다. 신규·수정된 `weakenedTactics` 18개 항목 전부 최소 1개의 `'인용구'`를 포함해(T42/T52에서 두 차례 재발했던 "인용구 없는 항목이 3인칭 지시문을 그대로 유출"·"대사 생성/분석 로직 불일치" 버그 클래스를 재도입하지 않음) 기존 데이터 불변식 테스트(`mockLlmContentLeak.test.ts`)가 신규 문구를 그대로 재검증해 통과했다. functions lint/tsc/build/test 전부 clean, 178/178 통과(무회귀). Mock LLM 경로로 `generateOpeningLine`/`craftEscalationLine`을 직접 호출해 카드사 사칭·가족 사고·납치 협박 3종에서 신규 문구가 3인칭 지시문 유출 없이 자연스러운 대사로 산출됨을 육안 확인(검증 스크립트는 삭제, 커밋 안 함). 실 Gemini 경로(`GEMINI_API_KEY` 필요)는 이 워크트리에 키가 없어 미검증 — 키 확보 후 재검증 권장.
- **취약점 리포트 "속았는지" 오판정 버그 수정(T52, 2026-07-24, 사용자 신고: 일부러 사기에 넘어가는 척 응했는데 리포트가 "잘 대처함/요구에 응하지 않음"으로 나옴)**: `analyzeConversation.ts`(T9)의 저항/순응 정규식 판정에서 두 가지 실제 버그를 라이브 에뮬레이터로 재현·확인했다. (1) "확인하고" 뒤에 "다시"가 없어도 저항으로 판정되던 과다매칭 — "확인하고 바로 송금하겠습니다"처럼 명백히 순응하는 발화도 필러 하나 때문에 저항 우선순위에 걸려 무시됐다. (2) 순응 판정 키워드가 비격식 어미·특정 명사(계좌/송금 등)로만 좁아 "말씀하신 대로 바로 처리하겠습니다"류 격식체 순응 표현은 아예 감지되지 않았다. 순응 키워드 집합을 보강했다. **1차 수정은 reviewer가 코드 직접 실행으로 발견한 새 버그로 REJECTED됐다**: "못 하겠다/거절" 등 부정 신호를 저항 쪽에 범용으로 추가했더니, 이 단어들이 메시지 전체에 대한 무조건적 저항 판정으로 작동해 "정말 거절하고 싶은데 무서워서 계좌번호 알려드릴게요"처럼 망설이다 결국 순응하는(현실적인, 특히 이 앱의 주 대상인 어르신이 실제로 할 법한) 발화까지 다시 "저항함"으로 오판정하는 동일 버그 계열을 재도입했다. **재수정**: 부정 판정을 범용 단어가 아니라 신규 순응 트리거 문구("말씀하신/시키는/하라는/알려주신 대로")에 직접 붙는 경우로만 좁힌 단일 패턴으로 교체(저항 우선 원칙은 유지하되 적용 범위만 정밀화). reviewer 재검토에서 원래 4개 재현 사례 + 진짜 저항 사례 2건 + 추가 adversarial 사례 3건까지 코드 직접 실행으로 전부 정상 판정 확인 후 **APPROVED**. **검증 중 "시도된 수법" 정확도 버그도 함께 발견**: 대사를 만드는 `mockClient.ts`와 분석하는 `analyzeConversation.ts`가 수법 인용구를 뽑는 로직을 각자 따로 구현하고 있다가 한쪽만 개선되며 서로 어긋나, `tacticsUsed`가 항상 빈 배열로 나오고 있었다 — 두 파일이 공용 모듈(`scenarios/tacticFlavor.ts`) 하나만 참조하도록 통합해 드리프트 재발을 구조적으로 막았다. 라이브 에뮬레이터로 수정 전/후를 대조 확인: 같은 순응 발화가 수정 전엔 `wasDeceived:false`+수법 목록 빈 배열이었다가, 수정 후엔 `wasDeceived:true`+속은 시점 타임라인+실제 시전된 수법이 정확히 채워짐을 확인했고, 진짜 저항 의도(예: "확인하고 다시 전화드릴게요")는 여전히 저항으로 정확히 판정됨을 함께 재확인했다. 대화 되짚어보기(리플레이, UX-018)는 이 리포트 데이터를 그대로 병합만 하므로 별도 수정 없이 함께 정확해진다. functions test 177/177(신규 13건 — 최초 9건 + 재수정 회귀 테스트 4건), lint/build clean. **아직 남은 갭**: 리플레이 화면의 "잘 대응한 지점" per-turn 주석(`reports.resistedMoments`)은 이번 수정과 무관한 별개의 미구현 필드로, 이미 후속 태스크로 이관돼 있다(DECISIONS #26). 격식체 "네, 알겠습니다"처럼 행동 명사(계좌/송금 등) 없이 단독으로 오는 순응 표현은 여전히 미탐지로 남지만(오탐(false positive)이 미탐(false negative)보다 더 나쁘다는 이 파일 자체의 설계 원칙과 정합, reviewer 확인) 회귀는 아님. 안전 판정 로직 자체를 바꾼 변경이라 self-check만으로 `done` 처리하지 않고 **reviewer 1차 REJECTED(부정 가드가 원래 버그를 재도입) → 재수정 → reviewer 2차 APPROVED → QA GO**를 거쳤다.
- **실시간 통화 지연 재조정 + 사용자 발화 파형 인디케이터(T51, 2026-07-24, 사용자 신고 2건)**: ①"통화 지연(턴 감지)이 여전히 길다" — Gemini Live VAD(`silenceDurationMs`)를 Google 공식 문서 실측 근거(서버 기본값 약 800ms, 100~200ms 이하가 문서상 위험 구간)로 기존 500ms→400ms로 재조정(위험 구간과 2배 여유 유지, 실제 끊김 체감은 사용자 실브라우저 확인 필요). **정정(reviewer 지적, 2026-07-24 재검토)**: 문서가 실제로 명시적으로 권장하는 값은 "발화종료 침묵 문턱값 최소 500ms"(및 서버 기본값 800ms)이며, 400ms는 이 권장 하한보다 낮다 — "200~2000ms가 문서상 안전 구간"이라던 최초 서술은 부정확해 정정했다. 400ms를 택한 근거는 문서의 직접 보증이 아니라 문서가 위험 구간으로 못박는 100~200ms와 여전히 2배 이상 떨어져 있다는 점뿐이다. ElevenLabs Agents 쪽은 클라이언트 SDK(`@elevenlabs/client`)가 턴 감지/VAD 관련 오버라이드를 전혀 노출하지 않음을 타입 정의로 확인 — 에이전트 대시보드(Advanced 탭 "Turn eagerness"/"Take turn after silence")에서만 조정 가능한 플랫폼 제약이라 코드로는 튜닝 불가(`ELEVENLABS_API_KEY`가 여전히 placeholder라 현재는 관측되지 않는 잠재 상태). ②"AI 파형처럼 내 말할 때도 파형이 움직였으면" — 기존 `CallWaveform`(무수정, boolean 계약 재사용)을 하나 더 렌더링해 사용자 발화 시에도 파형이 움직이게 했다. 신호 소스는 프로바이더별로 다르게 구현: Gemini Live 경로(`GeminiVoiceSession.tsx`)는 직접 캡처하는 마이크 `MediaStream`을 기존 전송 체인과 별개로 `AnalyserNode`에 병렬 연결(기존 무음 게인 노드 병렬 탭과 동일 기법)해 RMS 진폭을 로컬에서 계산. ElevenLabs 경로(`RealtimeVoiceSession.tsx`)는 SDK(`useConversation`)가 마이크 캡처를 내부에서 전담해 원본 스트림에 접근할 수 없어(AnalyserNode 탭이 구조적으로 불가능함을 타입 정의로 확인), 대신 SDK가 이미 제공하는 `onVadScore`(0~1 VAD 점수) 콜백을 사용 — 둘 다 400ms off-디바운스로 깜빡임을 막았다. 실시간 통화 모드에서만 노출(브라우저 STT 폴백 경로는 연속 발화 신호가 없어 범위 밖). functions test 169/169(신규 회귀 테스트 1건 포함), 루트 lint/build(25 라우트) 통과. **미검증**: 마이크 하드웨어가 없는 환경이라 실제 지연 체감·파형의 실제 마이크 반응은 사용자의 브라우저 실사용 확인이 필요(경험적 문턱값 2개 — Gemini RMS 10, ElevenLabs vadScore 0.5 — 도 실사용 후 재조정 여지 있음). **reviewer 리뷰(2026-07-24) — REJECTED**: Critical 1건(문서 중복/stale 내용, 별도 단계에서 이미 정리·해소) + Major 5건 — VAD 문서 인용 과장(위 정정 문단), 라우트 수 오기(26→25, 위 반영), `RealtimeVoiceSession.tsx`의 `onVadScore` 대안(`getInputVolume`/`getInputByteFrequencyData`) 미검토 서술, ElevenLabs 경로 음소거 게이트 부재(Gemini 경로와 비대칭), Gemini RMS/문턱값/디바운스 로직의 단위 테스트 부재. **전부 같은 브랜치 신규 커밋으로 수정**: 음소거 게이트 추가(`if (muted) return`), 대안 신호 인정 주석 보강, RMS/디바운스 로직을 `src/lib/realtime/userSpeechLevel.ts`(순수 함수)로 분리 + `userSpeechLevel.test.ts` 신규 9건. functions test 169/169·루트 test 40/40 재확인. **reviewer 재검토(2026-07-24) — APPROVED**(Critical 0건, Major 0건 신규 — 5건 전부 코드 직접 대조·Google 문서 재확인·ElevenLabs SDK 타입 직접 확인·테스트 재실행으로 해소 검증됨).
- **통화 AI 선톡 + 메신저 자동스크롤/타이핑인디케이터 + generateOpeningLine 크래시 수정(T44, 2026-07-24, 사용자 신고 3건 + 라이브 검증 중 발견)**: (1) 실시간 통화에서 사용자가 먼저 말해야 대화가 시작되던 문제 — ElevenLabs 경로는 `overrides.agent.firstMessage`(연결 직후 이 문장을 그대로 말하는 SDK 오버라이드)로, Gemini Live 경로는 그런 verbatim 오버라이드가 없어 "지금 막 연결됐으니 캐릭터로서 먼저 말을 걸라"는 오케스트레이션 트리거 턴을 보내 모델이 스스로 발화를 시작하게 해 각각 다르게 해결했다. 이미 서버가 생성해둔 오프닝 대사를 `sessionStorage`(기존 `openingAudioUrl`과 동일한 1회성 힌트 패턴)로 세션 재생 화면까지 전달. (2) 메신저(카톡/문자)에서 전송해도 로딩이 걸리는 것처럼 보이고 대화가 길어지면 자동 스크롤이 안 되던 문제 — 스크롤 앵커+자동 스크롤 이펙트, 응답 대기 중 점 3개 타이핑 인디케이터(스크린리더용 별도 announce) 추가로 해소(`docs/UX.md` P-4 패턴 실제 구현). (3) 라이브 검증 중 발견: `generateOpeningLine`이 `completeWithFallback`(T43에서 `sendMessage`에 이미 적용됐던 안전장치) 없이 Gemini를 직접 호출하고 있어, Gemini 무료 티어 쿼터 초과(실측 한도 20건/일)만으로 `createSession`/`consentChallenge` 전체가 INTERNAL로 완전히 실패했다 — 감싸서 항상 Mock 강등 후 세션 생성이 성공하도록 수정, 그 과정에서 함께 드러난 `isMock` 오판정 버그(별도 `isUsingMockLlm()` 사전 확인 방식이 completeWithFallback 도입 후 부정확해짐)도 `generateOpeningLine` 자신이 관측한 값을 직접 반환하도록 바로잡았다. 라이브 에뮬레이터로 실제 429 응답 위에서 Mock 강등 후에도 세션 생성이 성공함을 재확인. functions test 160/160, 루트 lint/build(25 라우트) clean. reviewer APPROVED(Major 2건 — CHANGELOG 갱신 누락은 본 항목으로 해소, 메신저→보이스 에스컬레이션 경로는 firstMessage 힌트를 아직 받지 않는 known gap으로 아래 별도 기록). **알려진 범위 제한**: 메신저→보이스 에스컬레이션(T30/T40)으로 전이된 세션은 `transitionChannel`이 오프닝 힌트를 생성/전달하지 않아, ElevenLabs(클론 음성) 경로가 실제로 활성화되면 이 세션들에서는 "AI 선톡" 수정이 아직 적용되지 않은 채로 남는다(Gemini Live 경로는 트리거가 무조건 발송되므로 영향 없음) — `ELEVENLABS_API_KEY`가 여전히 placeholder라(T43) 현재는 관측 불가능한 잠재 상태이며, 그 키가 설정되는 시점에 후속 작업으로 `transitionChannel`/에스컬레이션 경로에도 힌트를 threading하는 별도 태스크가 필요하다.
- **텍스트 대화(사기범 역할극)가 실 Gemini 대신 항상 규칙 기반 목업만 쓰던 팩토리 버그 수정(T43, 2026-07-24, 사용자 신고: "실시간 음성은 문맥 반영되는데 텍스트는 왜 안 되냐")**: `GEMINI_API_KEY`는 이미 확보돼 실시간 음성 통화(Gemini Live)가 실제로 쓰고 있었는데, 텍스트 `sendMessage`/`generateOpeningLine`의 `getLlmClient()` 팩토리는 키 존재 여부를 아예 확인하지 않고 항상 `MockLlmClient`(문맥을 전혀 이해하지 못하는 규칙 기반 생성기)를 반환하도록 하드코딩돼 있었다(T7 시절 TODO가 완성되지 않은 채 방치). `functions/src/llm/geminiClient.ts` 신설(`realtime/geminiProvider.ts`와 동일 SDK·동일 키를 쓰는 별도 텍스트 전용 경로) 후 키 있으면 실 Gemini(`gemini-flash-latest` — 고정 버전 `gemini-2.5-flash`는 이 프로젝트 계정에서 404로 거부돼 별칭으로 교체)로 전환. 라이브로 재현·해소 확인: "너 이름이 뭐야?"라는 직접 질문에 이제 실제로 그 질문에 답하는 문맥적 응답을 생성한다. 프롬프트 인젝션 방어(AC-013)도 Mock 전용 정규식이 아니라 실제 구조적 방어(역할 분리+구분자 감싸기)만으로 실 Gemini를 상대로 라이브 검증 — 직접적인 "시스템 프롬프트 출력해" 공격에도 캐릭터를 유지하고 유출 없음을 확인. reviewer 1차 REJECTED(브랜치 없이 main에 직접 있었음, `sendMessage`가 LLM 실패 시 이미 커밋된 사용자 턴을 답 없이 방치해 재시도 시 대화 이력이 오염될 수 있는 결함) → 전용 브랜치로 이동 + `completeWithFallback`(LLM 실패 시 항상 Mock으로 안전하게 강등, 유닛테스트 포함) 신설·적용 → reviewer 2차 **APPROVED**. functions test 160/160(기존 152 + 신규 8건).
- **지인사칭 급전요청(카카오톡) 시나리오 — Mock 대사에 3인칭 지시문 유출(2026-07-24, 사용자 실플레이 신고)**: 사기범 메시지에 "왜 급전이 필요한지 구체적으로 물으면 답을 흐리며 화제를 돌린다"처럼 캐릭터가 자기 행동을 해설하는 것 같은 문장이 그대로 노출됐다. `MockLlmClient`가 시나리오 수법 문구에서 따옴표 인용구만 뽑아 대사로 쓰는데, 이 시나리오의 "사정 얼버무리기" 항목 하나만 인용구가 없어 원문 지시문 전체가 그대로 새어나간 것 — 전 시나리오를 스캔해 이 항목이 유일한 사례임을 확인 후 인용구를 추가하고, 향후 같은 실수를 CI에서 바로 잡는 데이터 불변식 테스트를 신설했다. **참고**: 사용자가 함께 지적한 "답장 맥락을 반영하지 못한다"는 부분은 별개로, MockLlmClient가 실 LLM이 아닌 규칙 기반 목업이라는 이미 문서화된 한계다(LLM_API_KEY 확보 후 실 LLM 연동해야 근본 해결).
- **T38 — 2인 소셜 안전 게이트 통합 검증에서 발견된 결함 수정(2026-07-24)**: T38은 T36(발신)+T37(수신)을 `main` 병합 상태 그대로 4대 안전제약(AC-040/041/042/043/044/048/049)에 대해 홀리스틱하게 재감사하는 출시 게이트다(개별 diff 리뷰가 구조적으로 놓칠 수 있는 두 태스크 간 상호작용을 잡기 위한 별도 단계). **(1) Major — 재개(resume) 게이트가 잘못된 만료 기준을 씀**: Architecture.md §14.4는 "재개는 보존기간(retentionDeleteAt, 30일) 내에서만 허용"이라 명시하는데, 실제로는 최초진입 링크창(linkExpiresAt, 3일)으로 검사해 정당하게 동의를 마친 사용자2가 3일 뒤 돌아오면(보존기간 내인데도) 자기 세션조차 이어갈 수 없었다 — `decideConsentGate`의 입력을 `linkExpired`(최초진입 전용)·`retentionExpired`(재개 전용)로 분리하고 `createRealtimeCall`의 재검증도 동일하게 전환. **(2) Major — 실시간 통화 자격증명 응답의 원시 voiceId 노출**: `createRealtimeCall`이 챌린지 세션에서 사용자1의 원시 ElevenLabs voiceId를 사용자2 브라우저에 그대로 반환하고 있어 "raw voiceId를 반환하는 콜러블이 어디에도 없다"는 §14.2(ADR-0005) 명시 불변식을 위반했다 — 이는 ADR-0005(voiceId 절대 미반환)와 ADR-0006(createRealtimeCall 무개정 재사용)의 실측 충돌이라 임의 판단하지 않고 architect에게 회부, **ADR-0006 Addendum A2**로 해소: raw 오디오 바이트는 무조건 차단 유지, raw voiceId는 동의를 마친 유일한 참가자에게 `provider==="elevenlabs"`(라이브 클론 통화) 경로에서만 허용하는 스코프 한정 예외로 재정의(ElevenLabs 프로토콜상 서버측 voice 핀 메커니즘이 없어 클라 개시 TTS override에 실제로 필요함을 공식 문서로 확인 — 무조건 제거 시 "본인 목소리로 걸려오는 통화" 기능 자체가 깨짐). mock/none 폴백 경로는 클라가 그 값을 아예 쓰지 않으므로(`RealtimeVoiceSession`은 elevenlabs일 때만 마운트) voiceId를 비움. 정상(비챌린지) 세션은 무변경. 두 수정 모두 라이브 에뮬레이터로 재현→수정 후 통과 재검증(재개 게이트 4/4, voiceId 스코핑 6/6+QA 재검증 8/8). functions test 151/151, 루트 lint/build(25 라우트)/test 29/29. reviewer 1차 APPROVED WITH NOTES(Major 1) → 수정 → QA 1차 NO-GO(신규 Major 발견) → architect 회부·판정 → 수정 → QA 2차 **GO**(AC-040/041/042/043/044/048/049 전부 PASS). 2인 소셜 딥보이스 챌린지 기능(T36+T37) 전체가 이 게이트를 최종 통과해 출시 가능 상태가 됐다.
- **T37 리뷰에서 reviewer가 발견한 결함 수정(2026-07-24)**: **Major — 사용자2 최초 동의 이중 처리 레이스**. `consentChallenge`의 "링크 소모+체험 세션 생성" 판정이 read-then-write라, 아직 아무도 동의하지 않은 같은 링크를 서로 다른 두 익명 uid가 정말 동시에 호출하면 둘 다 "동의 가능" 판정을 통과해 서로 다른 두 세션이 만들어질 수 있었다 — "누가 이 딥보이스 체험을 받는가"라는 이 기능의 핵심 신원 게이트라 T36의 자원-cap 레이스(자기교정적)와 달리 방치할 수 없다고 reviewer가 판단. "소모+세션 생성"을 하나의 Firestore 트랜잭션으로 원자화(`functions/src/challenge/userAccess.ts`, `markChallengeConsumed`가 옵셔널 트랜잭션 파라미터를 받도록 확장)해 두 번째 호출이 재시도 시 이미 소모된 상태를 보고 정상 거부되도록 수정. 라이브 에뮬레이터 동시요청 재검증: 1명만 성공·1명은 "이미 다른 사람이 동의한 챌린지입니다" 거부·세션 문서 정확히 1건·A1 불변식(voiceId 미저장) 유지(5/5, QA 재검증 포함 45/45). functions test 149/149, 루트 lint/build(25 라우트)/test 29/29. reviewer APPROVED, QA GO.
- **T33/T36/T40 통합 리뷰에서 reviewer가 발견한 결함 수정(2026-07-24)**: (1) **Critical — 챌린지 문서의 민감 필드 노출**: `challenges/{id}` 문서에 담긴 ElevenLabs `voiceId`(원시 클론 id)·`linkTokenHash`가 소유자 read 허용 규칙을 통해 그대로 브라우저로 전송되고 있었다(ADR-0005 §14.2 "raw voiceId를 반환하는 경로가 어디에도 없다" 위반, 실제 유출 경로는 클라의 직접 Firestore read). 신규 `listMyChallenges` 콜러블이 민감 필드를 제외한 안전한 형태로만 목록을 반환하도록 교체하고, `firestore.rules`의 `challenges` read를 전면 거부로 좁혀 이 콜러블을 유일한 조회 경로로 만들었다. (2) **Major — 메신저 재진입 시 에스컬레이션 핑퐁**: max-turn 폴백이 세션 누적 turnCount를 그대로 비교해, T40(보이스→메신저 역방향 전이)으로 메신저에 복귀한 직후 다음 메시지 한 번에 즉시(그리고 한 번 6턴을 넘긴 세션은 그 이후 영원히) 재-에스컬레이션되는 버그가 있었다 — 채널 전이 기록에 전이 시점의 turnCount를 함께 남기고 "가장 최근 메신저 재진입 이후 턴 수"만 비교하도록 수정(세션이 한 번도 채널을 벗어난 적 없는 통상 케이스는 기존과 동일하게 동작). 부수적으로 T33/T36/T40의 `docs/Tasks.md` 상태 기록이 여러 문단에 걸쳐 작성돼 있어 markdown 표가 깨져 있던 것도 발견해 한 줄로 정리했다(내용 손실 없음). 에뮬레이터 실호출로 재검증(민감 필드 노출: 소유자 직접 read 거부·목록 응답에 두 필드 문자열 자체 없음 8/8, 핑퐁: 정방향 6턴 폴백→역방향 복귀→1턴 후 재발화 안 함→정확히 6턴째 정상 재발화 14/14). functions test 139/139, 루트 lint/build(22 라우트)/test 29/29. reviewer: 1차 REJECTED→수정 후 APPROVED, QA GO.
- **T31 통합 검증에서 reviewer가 발견한 결함 수정(2026-07-24)**: (1) `sendMessage`가 느린 LLM 호출 도중 다른 요청(예: 사용자의 "훈련 종료" 중복 탭)이 먼저 세션을 종료시켰을 가능성을 반영하지 않고 한도종료 필드·채널 전이를 덮어쓸 수 있었던 레이스 — 최종 반영 직전 트랜잭션으로 세션 상태를 다시 확인해, 이미 종료된 세션이면 종료 필드·전이 판단을 건너뛰도록 수정. (2) 구조화 신호(`[[SIGNAL:ESCALATE_VOICE]]`) 트리거가 Mock LLM 경로로는 한 번도 실제로 발화된 적이 없었던 것(신호는 personaPrompt 지침에만 있고 `weakenedTactics` 원문엔 없어 Mock이 낼 방법이 없었음) — 자녀사칭 카카오톡 시나리오의 해당 항목에 마커를 심어 재현 가능하게 하고, 정부지원금 스미싱 시나리오는 일부러 그대로 둬 max-turn 폴백 경로도 계속 Mock으로 재현 가능하도록 분리. (3) QA 재검증에서 나온 잔여 관찰 — `channelTransition`가 호출부의 사전 상태 확인과 자신의 실제 write 사이에 남는 아주 좁은 틈(그새 `endSession`이 먼저 끝내면 이미 종료된 세션에 `channelHistory`만 덧붙는 모순)을 자체 트랜잭션으로 마지막에 한 번 더 확인해 닫음(이미 종료됐으면 endSession의 멱등 처리와 동일하게 조용히 건너뜀 — "지원하지 않는 방향" 거부와는 다른 케이스). 에뮬레이터 실호출로 두 경로 모두 재검증(신호 트리거: 4번째 턴에 발화, 폴백 트리거: 6번째 턴에 발화, 우선순위 정합 확인, 채널전이 트랜잭션 추가 후에도 재확인). functions test 122/122, 루트 lint/build(21 라우트)/test 19/19. reviewer APPROVED, QA GO.
- **무료 실시간 음성 경로(Gemini Live, 2026-07-22)**: ElevenLabs(유료) 외에 **무료 티어로 쓸 수 있는** speech-to-speech 경로를 추가했다. 서버가 시나리오에 따라 자동으로 고른다 — 본인 목소리 클론이 필요 없는 generic 시나리오(기관사칭·대출·환급금)는 Gemini Live로, 클론이 핵심인 시나리오(가족·손주 사칭)는 ElevenLabs로. 둘 다 없으면 텍스트 폴백. `GEMINI_API_KEY`([aistudio.google.com](https://aistudio.google.com) 무료 발급)만 넣으면 시나리오별 설정 없이 3종이 실제 음성 대화로 동작한다. **보안**: 시스템 프롬프트·모델·도구를 단기 토큰 발급 시 `liveConnectConstraints`로 서버에서 고정해, 프롬프트가 브라우저로 내려가지 않고(ADR-0004) 클라이언트가 setup 프레임을 바꿔치기할 수도 없다. Gemini는 고정 프리셋 음성만 지원하므로 clone 시나리오에는 의도적으로 붙이지 않는다(“본인 목소리로 합성” 표기가 사실과 달라지는 것 방지). Live API는 마이크/스피커를 직접 다뤄야 해서 PCM 변환(16kHz 입력·24kHz 출력)과 오디오 큐잉을 클라에 구현했다. ⚠️ 무료 티어는 대화 내용이 구글 학습에 사용되므로 실제 배포 시 유료 티어 권장.
- **실시간 음성 통화(speech-to-speech, 2026-07-22)**: 훈련 통화가 이제 ElevenLabs Agents와의 실시간 음성 대화로 동작한다. 예전에는 턴마다 "브라우저 STT → 텍스트 LLM → TTS 합성 → 오디오 재생"을 왕복해 지연이 누적됐고 말을 끊거나 겹쳐 말할 수 없었다. **ElevenLabs를 고른 이유**: OpenAI Realtime·Gemini Live도 저지연 speech-to-speech를 제공하지만 둘 다 고정 프리셋 음성만 쓸 수 있다. 이 앱의 전제인 "참가자 본인 목소리 클론으로 걸려오는 전화"(AC-018/019)를 지키려면 런타임에 임의 클론 voice를 지정할 수 있어야 하고, 셋 중 `tts.voice_id` 오버라이드를 지원하는 건 ElevenLabs뿐이다. 한국어는 `agent.language="ko"`, 저지연 모델은 에이전트 설정에서 지정한다. **프롬프트 보안(ADR-0004)**: 오버라이드는 클라이언트가 보내는 값이라 페르소나 프롬프트를 그 경로로 넘기면 브라우저에 노출된다 — 그래서 시나리오별 에이전트에 프롬프트를 저장하고 서버는 `scenarioId→agentId` 매핑과 짧은 수명의 서명 URL만 발급한다(API 키는 서버에만 남는다). **폴백**: 키·에이전트 매핑·마이크 권한 중 하나라도 없으면 기존 텍스트 대화로 자동 강등하고 그 사실을 화면에 표시한다(조용한 실패 금지). 설정: `ELEVENLABS_AGENT_IDS`(README 참조).
- **신규 시나리오 3종(2026-07-22)**: 저금리 대환대출 빙자, 국세청 환급금 빙자(둘 다 기본 합성 음성), 손주 사칭 급전 요청(본인 목소리 클론 — 이 앱의 주 대상인 어르신을 노리는 수법). 총 5종. 공개 메타(`src/content/scenarios`)와 민감 프롬프트(`functions/src/scenarios`) 분리 저장 관례를 그대로 따르며, 수법은 전부 "약화된" 형태라 실제 계좌번호·송금 절차·기관 연락처를 포함하지 않는다(AC-005).

### Changed
- **통화 화면을 실제 전화처럼 재구성(2026-07-22)**: 수신 파동 애니메이션, 상대 발화 파형 인디케이터, 경과 시간, 음소거/키패드/종료 컨트롤 행. 시나리오 선택 화면도 카드형으로 개편해 "누가 전화를 거는지"(발신자)와 "녹음이 필요한지"를 먼저 읽히게 했다. 모션은 `prefers-reduced-motion`을 존중한다.
- **목업 대사 품질(2026-07-22)**: 규칙 기반 목업이 시나리오 수법 설명문(3인칭 지침)을 그대로 대사로 읽던 문제와, 가족 사칭 전용 표현("나야...", "지금 좀 도와줄 수 있어?")을 기관 사칭 시나리오에도 붙이던 문제를 고쳤다. 근본 해결은 실 LLM/실시간 에이전트 연동이다.

### Fixed
- **sendMessage 턴 경합(2026-07-22)**: 대화 이력 읽기 → 인덱스 결정 → 쓰기가 트랜잭션 밖이라, 같은 세션에서 요청이 겹치면 두 메시지가 같은 `turnIndex`를 쓰고 `turnCount` 증가가 유실될 수 있었다. 이력 읽기 + 사용자 메시지 쓰기 + turnCount 증가를 한 트랜잭션으로 묶었다(느린 LLM 호출은 트랜잭션 밖 유지).

### Removed
- **`synthesizeDeepvoice` 콜러블(2026-07-22)**: UX-014 화면 통합 이후 호출하는 화면이 없어졌고, 본문이 끝내 placeholder(가짜 voiceId·TODO 문자열)를 반환하는 상태였다 — 배포된 채로 두면 그 가짜 응답이 언젠가 화면에 실릴 위험이 있어 서버 콜러블·클라 래퍼·계약 타입을 함께 제거했다. `VoiceProvider.synthesize`는 남아 `createSession`/`sendMessage`의 폴백 오디오 합성에 계속 쓰인다. 시나리오의 `deepvoiceLines`는 "고정 대본 오프닝"을 되살릴 여지를 남겨 `@deprecated` 표기만 하고 보존했다.

- **프로젝트 스캐폴딩(T2)**: Next.js(App Router, TypeScript, Tailwind, 정적 export) + Firebase(Auth/Functions/Firestore/Storage) 초기화, 배포 파이프라인(`firebase deploy`), API/DB 계약 스텁, `firestore.rules`/`storage.rules`, `.env.example`(클라)·`functions/.env.example`(서버) 2분리.
- **Google 로그인 + 전역 라우트 가드(T18)**: Firebase Auth Google Provider로 로그인/가입. 인증되지 않은 사용자는 로그인 화면 외 모든 라우트에서 리다이렉트되며, 모든 데이터는 로그인한 사용자의 uid에 귀속된다. 로컬 Firebase 에뮬레이터 기준으로 빌드/lint/Firestore write까지 검증됨 — 실제 브라우저에서 Google OAuth 팝업을 끝까지 클릭해보는 것은 아직 미검증(에뮬레이터 한계).
- **한국어 "가족 납치/사고" 딥보이스 시나리오 콘텐츠(T6)**: 공개 메타데이터(제목·사기 유형·난이도 등)와 민감한 역할극 프롬프트(페르소나·약화 수법)를 분리 저장.
- **음성 프로바이더 인터페이스(T19, 임시 목업)**: 공통 `VoiceProvider` 인터페이스 뒤에 `MockVoiceProvider`를 붙여 클론/합성 파이프라인 개발을 언블록. **ElevenLabs API 키가 아직 준비되지 않아 실제 음성 클론/합성이 아닌 임시 목업(플레이스홀더 오디오)이며, 응답에 `isMock: true` 표식이 붙는다. 추후 ElevenLabs 실연동으로 교체 예정(최종 데모 필수 조건).**
- **AI 사기범 역할극 엔진(T7, 임시 목업)**: `createSession`/`sendMessage`로 시나리오 기반 대화를 주고받을 수 있다. 서버가 시스템 프롬프트를 조립하고 사용자 입력은 별도 role/구분자로 감싸 프롬프트 인젝션을 구조적으로 방어하며, 응답 시간 초과에 대한 안전망(타임아웃 처리)을 포함한다. **LLM API 키가 아직 준비되지 않아 실제 LLM이 아닌 규칙 기반 목업(`MockLlmClient`)이 응답하며, `isMock: true`로 식별된다. 인격 유지·실시간 적응 등은 실 LLM 연동 후에야 검증 가능. 추후 Claude/Gemini 실연동으로 교체 예정.** reviewer APPROVED, QA GO 통과.
- **온보딩 + 동의 게이팅 + 본인 목소리 등록 UI(T3)**: 명시적 동의 화면, 본인 확인 절차, 30초 대본 녹음 UI. 인증된 세션(T18) 전제로만 진입 가능. Firestore/Storage 에뮬레이터로 동의 write/read·녹음 업로드 경로·타인 uid 거부를 검증. **브라우저 마이크 권한 팝업·실제 녹음 재생 등 실클릭 UI 동작은 미검증**(T18과 동일한 한계, 코드 리뷰로만 확인).
- **음성 클론 파이프라인 통합(T4, Mock 기준)**: 녹음 업로드 → `createVoiceClone`(Mock `VoiceProvider`) → `createSession`이 동일 sessionId를 채택하는 흐름을 실배선. 소유 uid 불일치·녹음 없음 등 오류 처리 포함. functions test 34/34, 에뮬레이터 실호출 7/7 케이스 검증. **미검증**: 브라우저 실클릭(버튼→네비게이션→스피너). 시나리오 선택 UI(당시 `src/app/scenarios/page.tsx`)는 아직 T2/T6 스텁이라 sessionId 채택 경로가 실제 화면에서는 호출되지 않은 상태로 남음(T5가 후속 연결). 음성은 여전히 Mock — 최종 데모 전 ElevenLabs 실클론(T1)으로 교체 필수.
- **딥보이스 가족사칭 오디오 인앱 재생(T5)**: 시나리오 선택 → `synthesizeDeepvoice`로 대사 3줄 순차 재생, "AI 훈련용 합성" 표식(SyntheticLabel)과 재생 전 텍스트 프리롤 안내, 훈련 종료 버튼 상시 노출. 에뮬레이터 실호출 19/19 검증(클론→세션→합성×3→타uid 세션탈취 거부 포함). **미검증**: 오디오 자동재생·순차재생 등 브라우저 실클릭.
- **세션 라이프사이클 + 안전 종료(T8)**: `endSession`(소유권/인자 검증, 멱등 처리)과 "이것은 훈련이었습니다" 고지·디스컬레이션 안심 메시지 화면(UX-007)을 구현. 리포트 생성 트리거를 `endSession`과 자동종료(10턴 한도) 양쪽이 공유하는 단일 지점으로 통합. functions test 36/36, 에뮬레이터 16/16 검증. **알려진 갭**: 종료 사유(`endReason`)가 항상 `user_ended`로 고정(채팅 화면 미구현이라 다른 종료 경로 없음), 브라우저 실클릭 미검증.
- **취약점 리포트 생성·표시(T9)**: `generateReport`가 마스킹된 대화 로그에서 속은 시점 타임라인·시도된 수법·대처법을 산출해 리포트 화면(UX-008)에 표시. **LLM API 키 미확보로 실 LLM 대신 규칙 기반(저항/순응 키워드 매칭) 판정을 사용함을 투명 고지**(API.md는 "LLM 산출"로 서술 — 실제로는 아님). functions test 43/43(신규 7건), 에뮬레이터 시나리오 20/20 검증. **알려진 갭**: tacticsUsed 판정이 Mock 대화 특유의 flavor 문구 삽입 패턴에 의존 — 실 LLM 교체 후 매칭 정확도 재검증 필요. 브라우저 실클릭 미검증.
- **가드레일2 — 생성물 즉시 폐기(T10)**: 세션 종료(`status → ended`) 시 발화하는 Firestore 트리거 `onSessionEnded`가 Storage `users/{uid}/sessions/{sid}/**` 파일 삭제 + ElevenLabs voice 삭제 + `session.voiceId` 클리어 + `deletionLogs` 감사 기록을 수행. 부분 실패도 target별로 기록해 재시도 가능. reviewer APPROVED, QA GO. functions test 54/54(신규 11건), 에뮬레이터 21/21 검증(삭제 실측, 재발화 없음 확인). **설계 이탈(승인 필요, 상세는 UpdateRequests.md #4)**: ADR-0003/API.md는 `sessions/{sid}/artifacts` 서브컬렉션을 매니페스트로 순회하는 설계였으나, 실제 구현은 Storage prefix 실제 나열 방식 — reviewer는 이 쪽이 더 안전한 설계라고 판단(artifacts는 T5가 쓴 적이 없어 항상 비어 있었고, 애초에 원본 녹음을 포함하지 못하는 스키마였음).
- **가드레일5 하드닝 — PII 마스킹 실구현 + 프롬프트 인젝션 방어 레드팀 스팟체크(T11)**: `guardrails/maskPII`를 passthrough 스텁에서 정규식 기반 실구현으로 교체 — 이메일 / 주민번호형(`\d{6}-\d{7}`) / 전화번호형(010-xxxx-xxxx, 02-xxx-xxxx 등) / 8자리 이상 연속 숫자(계좌형) 4종을 이 순서로 토큰화(`[이메일]`/`[주민번호]`/`[전화]`/`[계좌]`)해 원문 대신 저장한다(T7이 이미 배선해 둔 `maskPII` 호출부는 무수정). `roleplay/promptAssembly.ts`의 `wrapUserInputAsData`에 구분자 하드닝(`escapeDelimiterLookalikes`)을 추가해, 사용자 입력이 `[훈련참가자입력:...]`류 가짜 구분자를 흉내 내도 전각괄호로 치환하고 실제 구분자는 래퍼가 감싼 시작/끝 1쌍만 남도록 방어. **레드팀 스팟체크**: 한국어 인젝션 변형 12종을 `INJECTION_PATTERN`에 직접 검증 — 6종 탐지, 6종 미탐. 미탐 6종 중 2종(계좌번호 요청·링크 요청)은 시나리오 콘텐츠 자체에 유출할 실제 운영정보가 없어 Mock 단계에서는 구조적으로 안전함을 확인했고, 나머지 4종(역할 재정의, 오탈자, 완곡 정체인정, 일반 지시무시)은 정규식이 놓치는 잔존 리스크로 정직하게 남김 — 실LLM 교체 후 재검증 필요. functions test 80/80(신규: maskPII 11건 + promptAssembly 구분자 2건 + injectionRedTeam 13건, 기존 69건 무회귀), 에뮬레이터 실호출 11/11 검증(저장된 `messages.textMasked`에 원문 PII 0건 확인). **알려진 한계**: 8자리 이상 숫자는 문맥과 무관하게 계좌형으로 마스킹되는 과잉마스킹(오탐) 경향이 남아 있음(ADR-0004가 이미 수용한 트레이드오프, 금액/이름 토큰화는 하카톤 스코프 밖). reviewer APPROVED, QA 조건부 GO(오케스트레이터 보고 기준 — 이 docs 세션에서 리뷰/QA 리포트 원문은 직접 확인하지 못했고, docs/Tasks.md T11 행은 이 시점까지 `review`로 남아 있어 판정 반영이 지연된 상태로 보임).
- **사칭 이미지 1종 + 합성 표식(T12)**: `SpoofImage` 컴포넌트를 HTML/CSS로 그린 가짜 은행 앱 "송금완료" 카드로 구현(이미지 생성 도구 미가용). 은행명·수취인·계좌번호·거래시각 전부 placeholder 표기 + "실제 송금이 아닙니다" 문구 명시, `SyntheticLabel` 상시 오버레이, 로드 실패 시 조용히 생략(비차단). 채팅 화면(UX-006)이 당시 스텁이라 실제 오버레이 배선은 하지 않고 통합 지점만 주석으로 남김.
- **게임화 — 사기 방어 등급(T13, 임시 플레이스홀더 v1)**: `computeDefenseGrade`가 누적 리포트의 방어율로 5단계 등급을 산정(OQ-5 미확정 — 사용자 승인 하에 "(v1)" 접미사로 임시값임을 화면에도 노출). `generateReportCore`가 리포트 생성 직후 `users/{uid}.defenseGrade`/`sessionCount`를 갱신, 실패해도 리포트 생성 자체는 막지 않음(비차단). `/grade`(UX-010) 화면 완성.
- **연령 확인 age-gate(T14)**: 만 14세 기준(OQ-8 확정) 자기신고 확인 플래그 방식으로 `AgeGate` 구현. `src/lib/age`로 판정 로직(순수 함수)과 Firestore read/write를 분리. 온보딩 동의(UX-001) 직후·녹음(UX-002) 이전에 배치, URL 직접 접근으로 우회 불가하도록 가드 추가.
- **세션/리포트 히스토리 열람(T15)**: `/history`(UX-012)에서 본인 `reports`를 시간순(최신순)으로 열람. 클라 Firestore SDK 직접 read(콜러블 불필요), 항목 클릭 시 `/report?sessionId=...`로 이동. 에뮬레이터로 소유권 분리(타uid 열람 차단) 검증.
- **역할극 채팅 화면(UX-006) 구현 — 채팅 UI 자체가 없던 마지막 P0 갭 해소**: `sessions/{sid}/messages` 서브컬렉션을 실시간 구독해 대화 내역을 렌더링하고, 입력을 `sendMessage`로 전송한다. 턴 한도(`N/maxUserTurns`) 표시, 한도 도달 시 입력 잠금 + `/session/end` 안내, LLM 응답이 Mock임을 알리는 배지(`isMock`)를 노출해 실 LLM과 혼동되지 않게 했다. 백엔드(T7 역할극 엔진, T8 세션 라이프사이클)는 이미 완성·테스트돼 있었고 화면만 없었던 상태였음. 에뮬레이터 실호출 17/17 검증(오프닝 메시지 write→`sendMessage` 실호출→turnIndex 순서·역할 정합·타uid 탈취 거부까지, 스크립트는 검증 후 삭제·커밋 안 함).
- **역할극을 실시간 음성 통화형으로 전환(T20, Phase A)**: UX-006에 브라우저 STT(탭-투-토크 마이크 버튼, `useSpeechRecognition` 신규 훅)와 사기범 응답 TTS 자동재생을 추가했다. 서버 `sendMessage`/`createSession`이 이제 `audioUrl`/`openingAudioUrl`을 함께 반환(VoiceProvider 합성 훅, 실패해도 텍스트 응답은 안 막힘 — 비차단). 텍스트 입력은 STT 미지원 브라우저(Safari 등)용 폴백으로 상시 유지. **PRD "실시간 통화·STT는 제외" 결정을 뒤집었다(v0.8) — 단 실제 전화망(PSTN) 연동은 여전히 하지 않는다(AC-019 무변경, Twilio 등 미도입).** 사전고지·동의·종료고지(AC-012/017/023)도 무변경 — 원래 요청이던 "훈련 인지 자체를 못 하게"는 이 안전장치들을 제거하는 것이라 거부되고 절충안(사전 동의 유지, 통화 중 몰입만 유지)으로 진행됐다(경위는 docs/UX.md D-10 참고). 에뮬레이터 실호출로 `audioUrl`/`openingAudioUrl` 반환 확인, 브라우저 실클릭으로 오디오 자동재생 엘리먼트·마이크 버튼·마이크 권한거부 시 텍스트 폴백까지 확인. **미검증**: 이 세션의 자동화 브라우저는 마이크가 항상 차단돼 있어 STT가 실제로 발화를 인식하는 경로 자체는 트리거해보지 못함(코드 리뷰로 대체, 실사용자 테스트 필요). Phase B(`voiceMode` 필드·신규 "기관사칭" 시나리오·온보딩 순서 변경으로 녹음 생략)는 후속 별도 태스크로 분리 제안, 이번 범위 아님.
- **UX-014 단일 연속 통화 화면 통합 + 다크 통화 UI 재디자인(T22)**: 사용자 피드백에 따라 ux-design 서브에이전트로 온보딩→통화→리포트 흐름을 재설계(`docs/UX.md` v1.4, UX-005/006을 Deprecated로 이관하고 단일 연속 통화 **UX-014** 신설)한 뒤 구현. `/session/play`(전화받기)와 `/session/chat`(역할극)을 **단일 라우트**로 통합해 phase 상태머신(수신→연결→오프닝 재생→실시간 역할극→종료)으로 전이한다 — 중간 "계속(역할극 시작)" 버튼과 페이지 전환을 없애 "받았다가 채팅창으로 넘어가는" 단절을 제거했다. 오프닝 재생은 별도 스크립트 대신 `createSession`이 이미 대화 로그(turnIndex 0)에 남겨둔 `openingAudioUrl`을 그대로 이어 쓴다. 통화 경과 타이머를 "받기" 시점 기준으로 신설했다. `/session/chat`은 삭제 대신 `/session/play`로의 리다이렉트만 남겼다. 화면은 클로드 디자인 원안(상단바 채팅 버블)에서 벗어나 전화받기 화면과 동일한 다크 통화 톤으로 재구성했다. "수사관이 목소리를 녹음했다는 문구는 거짓말" 지적은 재확인 결과 직전 태스크(T21)에서 이미 수정돼 있었음을 코드·브라우저 재검증으로 확인(추가 수정 없음). 함께 진행한 기능 방향 재검토(R-1~R-5)에서 "clone 시나리오의 실시간 응답도 클론 목소리여야 하지 않냐"는 우려는 서버가 매 턴 `session.voiceId`(오프닝과 동일한 실제 클론 voiceId)로 이미 합성하고 있음을 코드 확인으로 발견해 추가 구현 없이 해소됐다. 에뮬레이터+브라우저 실클릭으로 기관사칭 시나리오의 수신→받기→오프닝 자동재생→화면 전환 없는 실시간 전환→메시지 왕복까지 전 구간 URL이 `/session/play` 그대로임을 확인.
- **기본(비복제) TTS 시나리오 + 온보딩 순서 변경 + 채팅 자동 청취 전환(T21, Phase B)**: `ScenarioDoc`에 `voiceMode:"clone"|"generic"`/`callerLabel` 추가, voiceMode:"generic" 시나리오(신규 "기관 사칭(검찰·금융감독원)")는 본인 목소리 녹음/클론을 완전히 생략하고 바로 세션을 시작한다. 온보딩 순서를 "age-gate→녹음→클론→시나리오"에서 **"age-gate→시나리오 선택→(clone 시나리오만) 녹음→클론"**으로 변경 — `/onboarding/record`가 하던 사전 세션 id 발급을 `/scenarios`로 옮기고, `/clone/wait`는 클론 완료 시 저장해 둔 선택 시나리오로 자동 `createSession`을 호출해 `/session/play`로 직행한다(사용자가 시나리오를 다시 고를 필요 없음). 채팅 화면(T20의 탭-투-토크)을 **자동 청취**로 전환 — 상대 응답 재생이 끝나면(또는 합성 실패로 재생할 게 없으면) 탭 없이 자동으로 듣기 시작하고, 텍스트 입력은 접이식 폴백으로 남는다. **브라우저 실측 중 발견해 즉시 고친 버그**: `play/page.tsx`의 "당신의 목소리로 합성되었습니다" 문구가 녹음을 안 한 generic 시나리오에도 그대로 떠 있어 사실과 다름을 확인 — voiceMode로 분기해 "기본 합성 음성으로 재생됩니다"로 교체. `docs/UX.md` Information Architecture 다이어그램을 새 순서로 갱신(v1.3). 에뮬레이터 실호출로 generic 시나리오 `createSession`(사전 클론 없이) 5/5 검증, 브라우저 실클릭으로 순서 변경·양쪽 voiceMode 경로·자동 청취 실제 트리거(마이크 권한 요청 자동 발생)까지 확인. **미검증**: T20과 동일하게 이 자동화 브라우저는 마이크가 항상 차단돼 있어 STT가 실제로 발화를 인식하는 경로는 트리거 불가.

### Changed
- **로컬 개발 방식(사용자 결정, 2026-07-21)**: 프로토타입 단계는 실제 Firebase 콘솔 프로젝트 없이 로컬 Firebase 에뮬레이터로 진행하기로 확정. 실 프로젝트 연결은 데모 준비 시점으로 이연.

### Fixed
- **`synthesizeDeepvoice.ts` 더미 스텁 실배선 교체(T5, 크로스트랙 버그픽스)**: `session/play`(UX-005) 화면이 실제로는 백엔드를 호출하지 않고 하드코딩된 가짜 오디오(`/mock-audio/mock-deepvoice-stub.mp3`, 실존하지 않는 파일)를 재생하고 있던 것을 발견해 실제 `httpsCallable` 호출로 교체.
- **`synthesizeDeepvoice` 콜러블에 세션 소유권 검증 추가(T5, 보안 하드닝)**: `createVoiceClone`/`createSession`/`endSession`과 달리 소유 uid 검증이 없어 타uid 호출이 거부되지 않던 문제를 확인해 동일한 `permission-denied` 검증 패턴을 추가.
- **`createSession.ts`/`endSession.ts` 더미 스텁 실배선 교체(T8)**: T2 이후 더미 스텁 상태였던 것을 실제 `httpsCallable` 호출로 교체(이 교체 없이는 세션 종료 전체 파이프라인이 서버를 타지 않아 T8 자체 검증이 불가능했음).
- **`sendMessage.ts` 더미 스텁 실배선 교체(채팅 화면 구현과 함께)**: T2 이후 유일하게 남아 있던 콜러블 더미 스텁을 실제 `httpsCallable` 호출로 교체.
- **클라 타입 계약 드리프트 해소**: `CreateSessionResponse`/`SendMessageResponse`에 서버는 이미 반환 중이던 `isMock` 필드가 클라 `src/lib/api/types.ts`에는 누락돼 있던 것(T7 known gap)을 추가.

## [0.1.0] - {{YYYY-MM-DD}}
### Added
- Initial release.
