# Changelog

Owner: docs agent (see AGENTS.md). Format: [Keep a Changelog](https://keepachangelog.com/), newest first.

## [Unreleased]
### Changed
- **사용자 제공 디자인 핸드오프 앱 전체 적용(T41, 2026-07-24)**: 사용자가 직접 전달한 고정밀 디자인 핸드오프(색상·타이포·간격·컴포넌트 상태까지 확정된 7개 플로우 프로토타입)를 앱의 22개 화면 중 18개 파일에 반영했다(4개는 감사 결과 이미 토큰을 준수하고 있어 무변경). 순수 프레젠테이션 변경만 — 기능·라우팅·데이터흐름·백엔드는 전혀 건드리지 않았다. 신규 공용 컴포넌트(`src/components/ui/`: Button/Badge/SelectableCard/ProgressSteps/Banner)를 만들어 5개 플로우(온보딩·훈련·리포트·메신저+전이·챌린지)에서 재사용했다. 안전 게이트(동의 체크박스 게이팅, 2인 챌린지 무동의 재생 경로 부재, 상시 종료·AI 합성 음성 고지, 닫을 수 없는 면책 배너)는 전부 무변경 확인. 카카오톡/문자 브랜드의 정확 모사(기존 확정 결정, OQ-U6)는 새 핸드오프의 "브랜드 범용화" 권고와 충돌해 기존 결정을 우선하고 따르지 않았다. reviewer 리뷰에서 발견한 접근성 회귀(저장된 목소리 선택 목록이 라디오그룹에서 버튼으로 바뀌며 스크린리더 시맨틱스 상실) 수정 완료, APPROVED.
### Added
- **사후 리플레이 해설 + 2인 소셜 딥보이스 챌린지(발신 측) + 보이스→메신저 역방향 전이(T33/T36/T40, 2026-07-24)**:
  - **사후 리플레이 해설(T33)**: 세션 종료 후 `/report/replay`에서 대화를 처음부터 시간순으로 되짚으며 각 사기 신호 시점에 주석을 확인할 수 있다(UX-018). 신규 백엔드·신규 분석 로직 없이 기존 리포트(T9)·transcript를 순수 병합만 한다(AC-038). 요약 리포트(UX-008)와 별도 화면 — 이후 2인 소셜(T37)에서 강제 단계로 재사용될 예정.
  - **2인 소셜 딥보이스 챌린지 — 발신(사용자1) 측(T36)**: 로그인한 사용자가 자기 목소리를 클론해 지인에게 보낼 공유 링크(토큰)를 발급한다. 챌린지 전용 음성은 항상 새로 발급되는 독립 클론이며 훈련 세션의 목소리와 절대 공유되지 않는다(ADR-0005). 공유 토큰은 해시만 저장하고 평문은 발급 시 1회만 반환한다. 활성 챌린지 3개 상한, 링크 3일 만료, 목소리 30일 기본 보존(수동/자동 삭제). 수신(사용자2) 측 동의·체험·신고는 별도 후속 작업.
  - **보이스→메신저 역방향 전이(T40, fast-follow)**: 통화 중 "메시지로 전환" 버튼으로 메신저 채팅으로 되돌아갈 수 있다(수동 버튼만 — 구조화 신호·max-turn 폴백은 설계 문서가 없어 이번 범위 밖). 메신저→보이스(T30)의 방향 무관 전이 엔진을 그대로 재사용.
- **2인 소셜 딥보이스 챌린지 — 수신(사용자2) 측(T37, ADR-0006, 2026-07-24)**: 로그인 없이 공유 링크(`/challenge/join?token=...`)만으로 진입해(AC-040/048) 발신자(사용자1)의 복제 목소리로 걸려오는 딥보이스 체험을 겪고, 종료 시 강제로 정체가 공개된 뒤(AC-042) 대화를 되짚어보는 리플레이 화면으로 인계된다(T33 재사용). 사용자2는 링크를 열 때 화면 뒤에서 조용히 Firebase 익명 인증(Anonymous Auth)에 사인인해 임시 uid를 얻고, 그 uid가 체험 세션을 소유해 기존 세션/리포트/실시간통화 인프라와 소유권 기반 firestore.rules를 전혀 개정하지 않고 그대로 재사용한다. **핵심 안전장치(A1)**: 챌린지 전용 클론 목소리(voiceId)는 사용자2의 세션 문서에 절대 저장되지 않는다 — 저장하면 (1) 사용자2가 소유자 자격으로 자기 세션을 읽을 때 사용자1의 원시 클론 id가 유출되고, (2) 세션 종료 시 작동하는 기존 폐기 트리거가 그 값을 지워버려 사용자1의 챌린지 클론이 사라진다. 대신 실시간 통화 자격증명 발급(`createRealtimeCall`) 시점에 서버가 챌린지 문서에서 매번 새로 읽어 해석한다. 결과 공유는 동의 시에만 "완료 여부" 수준으로만 사용자1에게 노출되고 대화 전문은 절대 넘어가지 않는다(AC-043). 원치 않는 링크는 무동의로도 신고할 수 있다(AC-049). reviewer APPROVED(Critical 0, Major 1 — 아래 Fixed 참고), QA GO(AC-040/041/042/043/044/048/049 라이브 에뮬레이터 45/45).
- **메신저피싱 확장 — 채널 구분 + 메신저→보이스 에스컬레이션(T27~T31, 2026-07-23~24)**: 이제 앱은 보이스피싱뿐 아니라 카카오톡/문자(SMS) 메신저피싱도 훈련할 수 있고, 메신저 대화 중 상대가 의심하면 실제 통화로 자연스럽게 넘어간다(양방향 설계·순차 구현 확정, OQ-22).
  - **신규 시나리오 8종(T27)**: 보이스 4종(카드사 사칭, 택배/통관 사기, 납치 협박, 사생활 유포 협박 — 사용자 피드백에 따라 어조를 더 차갑고 압박적으로 강화하되 구체적 폭력·성적 묘사는 넣지 않음), 메신저 4종(자녀 사칭 급전 요청·카카오톡, 지인 사칭 대출 부탁·카카오톡, 택배 스미싱·문자, 정부지원금 스미싱·문자). 총 13종. `ScenarioDoc`에 `channel`/`surface`/`escalation` 옵셔널 필드 추가(하위호환).
  - **단계 선택형(드릴다운) 시나리오 UI(T28)**: 기존 전체 목록형 대신, "보이스 vs 메신저" → "(보이스면) 복제 vs 기본 음성" 순으로 선택할 때마다 다음 화면이 나타나는 흐름으로 개편(UX-015~017).
  - **메신저 채팅 화면(T29)**: 카카오톡/문자 UI를 각각 그대로 재현(정확 모사 확정, OQ-U6)한 채팅 화면, iOS/갤럭시 자동 기기 감지+스킨, 스미싱 링크는 `[[LINK:id]]` sentinel 마커로만 표현해 실제 URL이 존재하지 않는 인앱 가짜 랜딩 목업으로 대체(AC-032, OQ-21 — `MessengerFakeLanding`은 네트워크 호출 임포트 자체가 없어 구조적으로 안전).
  - **메신저→보이스 에스컬레이션(T30)**: 트리거 3종 — 역할극 LLM이 캐릭터 판단으로 자기 응답에 심는 구조화 신호(`[[SIGNAL:ESCALATE_VOICE]]`, 자유텍스트 직접 분류 아님, OQ-16 재확정), 6턴 초과 시 max-turn 폴백, 언제든 누를 수 있는 "전화로 확인" 수동 버튼. 하나의 세션 문서·하나의 대화 로그·정확히 1개 리포트를 유지한 채 채널만 바뀐다(`channel`/`channelHistory`, Architecture.md §13). 통화로 넘어갈 때 쓸 목소리를 미리 고르는 UX-025(즉시 녹음/보관된 목소리 재사용/남·여 기본 목소리) 신설.
### Fixed
- **텍스트 대화(사기범 역할극)가 실 Gemini 대신 항상 규칙 기반 목업만 쓰던 팩토리 버그 수정(T43, 2026-07-24, 사용자 신고: "실시간 음성은 문맥 반영되는데 텍스트는 왜 안 되냐")**: `GEMINI_API_KEY`는 이미 확보돼 실시간 음성 통화(Gemini Live)가 실제로 쓰고 있었는데, 텍스트 `sendMessage`/`generateOpeningLine`의 `getLlmClient()` 팩토리는 키 존재 여부를 아예 확인하지 않고 항상 `MockLlmClient`(문맥을 전혀 이해하지 못하는 규칙 기반 생성기)를 반환하도록 하드코딩돼 있었다(T7 시절 TODO가 완성되지 않은 채 방치). `functions/src/llm/geminiClient.ts` 신설(`realtime/geminiProvider.ts`와 동일 SDK·동일 키를 쓰는 별도 텍스트 전용 경로) 후 키 있으면 실 Gemini(`gemini-flash-latest` — 고정 버전 `gemini-2.5-flash`는 이 프로젝트 계정에서 404로 거부돼 별칭으로 교체)로 전환. 라이브로 재현·해소 확인: "너 이름이 뭐야?"라는 직접 질문에 이제 실제로 그 질문에 답하는 문맥적 응답을 생성한다. 프롬프트 인젝션 방어(AC-013)도 Mock 전용 정규식이 아니라 실제 구조적 방어(역할 분리+구분자 감싸기)만으로 실 Gemini를 상대로 라이브 검증 — 직접적인 "시스템 프롬프트 출력해" 공격에도 캐릭터를 유지하고 유출 없음을 확인. reviewer 1차 REJECTED(브랜치 없이 main에 직접 있었음, `sendMessage`가 LLM 실패 시 이미 커밋된 사용자 턴을 답 없이 방치해 재시도 시 대화 이력이 오염될 수 있는 결함) → 전용 브랜치로 이동 + `completeWithFallback`(LLM 실패 시 항상 Mock으로 안전하게 강등, 유닛테스트 포함) 신설·적용 → reviewer 2차 **APPROVED**. functions test 160/160(기존 152 + 신규 8건).
- **지인사칭 급전요청(카카오톡) 시나리오 — Mock 대사에 3인칭 지시문 유출(2026-07-24, 사용자 실플레이 신고)**: 사기범 메시지에 "왜 급전이 필요한지 구체적으로 물으면 답을 흐리며 화제를 돌린다"처럼 캐릭터가 자기 행동을 해설하는 것 같은 문장이 그대로 노출됐다. `MockLlmClient`가 시나리오 수법 문구에서 따옴표 인용구만 뽑아 대사로 쓰는데, 이 시나리오의 "사정 얼버무리기" 항목 하나만 인용구가 없어 원문 지시문 전체가 그대로 새어나간 것 — 전 시나리오를 스캔해 이 항목이 유일한 사례임을 확인 후 인용구를 추가하고, 향후 같은 실수를 CI에서 바로 잡는 데이터 불변식 테스트를 신설했다. **참고**: 사용자가 함께 지적한 "답장 맥락을 반영하지 못한다"는 부분은 별개로, MockLlmClient가 실 LLM이 아닌 규칙 기반 목업이라는 이미 문서화된 한계다(LLM_API_KEY 확보 후 실 LLM 연동해야 근본 해결).
- **T38 — 2인 소셜 안전 게이트 통합 검증에서 발견된 결함 수정(2026-07-24)**: T38은 T36(발신)+T37(수신)을 `main` 병합 상태 그대로 4대 안전제약(AC-040/041/042/043/044/048/049)에 대해 홀리스틱하게 재감사하는 출시 게이트다(개별 diff 리뷰가 구조적으로 놓칠 수 있는 두 태스크 간 상호작용을 잡기 위한 별도 단계). **(1) Major — 재개(resume) 게이트가 잘못된 만료 기준을 씀**: Architecture.md §14.4는 "재개는 보존기간(retentionDeleteAt, 30일) 내에서만 허용"이라 명시하는데, 실제로는 최초진입 링크창(linkExpiresAt, 3일)으로 검사해 정당하게 동의를 마친 사용자2가 3일 뒤 돌아오면(보존기간 내인데도) 자기 세션조차 이어갈 수 없었다 — `decideConsentGate`의 입력을 `linkExpired`(최초진입 전용)·`retentionExpired`(재개 전용)로 분리하고 `createRealtimeCall`의 재검증도 동일하게 전환. **(2) Major — 실시간 통화 자격증명 응답의 원시 voiceId 노출**: `createRealtimeCall`이 챌린지 세션에서 사용자1의 원시 ElevenLabs voiceId를 사용자2 브라우저에 그대로 반환하고 있어 "raw voiceId를 반환하는 콜러블이 어디에도 없다"는 §14.2(ADR-0005) 명시 불변식을 위반했다 — 이는 ADR-0005(voiceId 절대 미반환)와 ADR-0006(createRealtimeCall 무개정 재사용)의 실측 충돌이라 임의 판단하지 않고 architect에게 회부, **ADR-0006 Addendum A2**로 해소: raw 오디오 바이트는 무조건 차단 유지, raw voiceId는 동의를 마친 유일한 참가자에게 `provider==="elevenlabs"`(라이브 클론 통화) 경로에서만 허용하는 스코프 한정 예외로 재정의(ElevenLabs 프로토콜상 서버측 voice 핀 메커니즘이 없어 클라 개시 TTS override에 실제로 필요함을 공식 문서로 확인 — 무조건 제거 시 "본인 목소리로 걸려오는 통화" 기능 자체가 깨짐). mock/none 폴백 경로는 클라가 그 값을 아예 쓰지 않으므로(`RealtimeVoiceSession`은 elevenlabs일 때만 마운트) voiceId를 비움. 정상(비챌린지) 세션은 무변경. 두 수정 모두 라이브 에뮬레이터로 재현→수정 후 통과 재검증(재개 게이트 4/4, voiceId 스코핑 6/6+QA 재검증 8/8). functions test 151/151, 루트 lint/build(25 라우트)/test 29/29. reviewer 1차 APPROVED WITH NOTES(Major 1) → 수정 → QA 1차 NO-GO(신규 Major 발견) → architect 회부·판정 → 수정 → QA 2차 **GO**(AC-040/041/042/043/044/048/049 전부 PASS). 2인 소셜 딥보이스 챌린지 기능(T36+T37) 전체가 이 게이트를 최종 통과해 출시 가능 상태가 됐다.
- **T37 리뷰에서 reviewer가 발견한 결함 수정(2026-07-24)**: **Major — 사용자2 최초 동의 이중 처리 레이스**. `consentChallenge`의 "링크 소모+체험 세션 생성" 판정이 read-then-write라, 아직 아무도 동의하지 않은 같은 링크를 서로 다른 두 익명 uid가 정말 동시에 호출하면 둘 다 "동의 가능" 판정을 통과해 서로 다른 두 세션이 만들어질 수 있었다 — "누가 이 딥보이스 체험을 받는가"라는 이 기능의 핵심 신원 게이트라 T36의 자원-cap 레이스(자기교정적)와 달리 방치할 수 없다고 reviewer가 판단. "소모+세션 생성"을 하나의 Firestore 트랜잭션으로 원자화(`functions/src/challenge/userAccess.ts`, `markChallengeConsumed`가 옵셔널 트랜잭션 파라미터를 받도록 확장)해 두 번째 호출이 재시도 시 이미 소모된 상태를 보고 정상 거부되도록 수정. 라이브 에뮬레이터 동시요청 재검증: 1명만 성공·1명은 "이미 다른 사람이 동의한 챌린지입니다" 거부·세션 문서 정확히 1건·A1 불변식(voiceId 미저장) 유지(5/5, QA 재검증 포함 45/45). functions test 149/149, 루트 lint/build(25 라우트)/test 29/29. reviewer APPROVED, QA GO.
- **T33/T36/T40 통합 리뷰에서 reviewer가 발견한 결함 수정(2026-07-24)**: (1) **Critical — 챌린지 문서의 민감 필드 노출**: `challenges/{id}` 문서에 담긴 ElevenLabs `voiceId`(원시 클론 id)·`linkTokenHash`가 소유자 read 허용 규칙을 통해 그대로 브라우저로 전송되고 있었다(ADR-0005 §14.2 "raw voiceId를 반환하는 경로가 어디에도 없다" 위반, 실제 유출 경로는 클라의 직접 Firestore read). 신규 `listMyChallenges` 콜러블이 민감 필드를 제외한 안전한 형태로만 목록을 반환하도록 교체하고, `firestore.rules`의 `challenges` read를 전면 거부로 좁혀 이 콜러블을 유일한 조회 경로로 만들었다. (2) **Major — 메신저 재진입 시 에스컬레이션 핑퐁**: max-turn 폴백이 세션 누적 turnCount를 그대로 비교해, T40(보이스→메신저 역방향 전이)으로 메신저에 복귀한 직후 다음 메시지 한 번에 즉시(그리고 한 번 6턴을 넘긴 세션은 그 이후 영원히) 재-에스컬레이션되는 버그가 있었다 — 채널 전이 기록에 전이 시점의 turnCount를 함께 남기고 "가장 최근 메신저 재진입 이후 턴 수"만 비교하도록 수정(세션이 한 번도 채널을 벗어난 적 없는 통상 케이스는 기존과 동일하게 동작). 부수적으로 T33/T36/T40의 `docs/Tasks.md` 상태 기록이 여러 문단에 걸쳐 작성돼 있어 markdown 표가 깨져 있던 것도 발견해 한 줄로 정리했다(내용 손실 없음). 에뮬레이터 실호출로 재검증(민감 필드 노출: 소유자 직접 read 거부·목록 응답에 두 필드 문자열 자체 없음 8/8, 핑퐁: 정방향 6턴 폴백→역방향 복귀→1턴 후 재발화 안 함→정확히 6턴째 정상 재발화 14/14). functions test 139/139, 루트 lint/build(22 라우트)/test 29/29. reviewer: 1차 REJECTED→수정 후 APPROVED, QA GO.
- **T31 통합 검증에서 reviewer가 발견한 결함 수정(2026-07-24)**: (1) `sendMessage`가 느린 LLM 호출 도중 다른 요청(예: 사용자의 "훈련 종료" 중복 탭)이 먼저 세션을 종료시켰을 가능성을 반영하지 않고 한도종료 필드·채널 전이를 덮어쓸 수 있었던 레이스 — 최종 반영 직전 트랜잭션으로 세션 상태를 다시 확인해, 이미 종료된 세션이면 종료 필드·전이 판단을 건너뛰도록 수정. (2) 구조화 신호(`[[SIGNAL:ESCALATE_VOICE]]`) 트리거가 Mock LLM 경로로는 한 번도 실제로 발화된 적이 없었던 것(신호는 personaPrompt 지침에만 있고 `weakenedTactics` 원문엔 없어 Mock이 낼 방법이 없었음) — 자녀사칭 카카오톡 시나리오의 해당 항목에 마커를 심어 재현 가능하게 하고, 정부지원금 스미싱 시나리오는 일부러 그대로 둬 max-turn 폴백 경로도 계속 Mock으로 재현 가능하도록 분리. (3) QA 재검증에서 나온 잔여 관찰 — `channelTransition`가 호출부의 사전 상태 확인과 자신의 실제 write 사이에 남는 아주 좁은 틈(그새 `endSession`이 먼저 끝내면 이미 종료된 세션에 `channelHistory`만 덧붙는 모순)을 자체 트랜잭션으로 마지막에 한 번 더 확인해 닫음(이미 종료됐으면 endSession의 멱등 처리와 동일하게 조용히 건너뜀 — "지원하지 않는 방향" 거부와는 다른 케이스). 에뮬레이터 실호출로 두 경로 모두 재검증(신호 트리거: 4번째 턴에 발화, 폴백 트리거: 6번째 턴에 발화, 우선순위 정합 확인, 채널전이 트랜잭션 추가 후에도 재확인). functions test 122/122, 루트 lint/build(21 라우트)/test 19/19. reviewer APPROVED, QA GO.
- **무료 실시간 음성 경로(Gemini Live, 2026-07-22)**: ElevenLabs(유료) 외에 **무료 티어로 쓸 수 있는** speech-to-speech 경로를 추가했다. 서버가 시나리오에 따라 자동으로 고른다 — 본인 목소리 클론이 필요 없는 generic 시나리오(기관사칭·대출·환급금)는 Gemini Live로, 클론이 핵심인 시나리오(가족·손주 사칭)는 ElevenLabs로. 둘 다 없으면 텍스트 폴백. `GEMINI_API_KEY`([aistudio.google.com](https://aistudio.google.com) 무료 발급)만 넣으면 시나리오별 설정 없이 3종이 실제 음성 대화로 동작한다. **보안**: 시스템 프롬프트·모델·도구를 단기 토큰 발급 시 `liveConnectConstraints`로 서버에서 고정해, 프롬프트가 브라우저로 내려가지 않고(ADR-0004) 클라이언트가 setup 프레임을 바꿔치기할 수도 없다. Gemini는 고정 프리셋 음성만 지원하므로 clone 시나리오에는 의도적으로 붙이지 않는다(“본인 목소리로 합성” 표기가 사실과 달라지는 것 방지). Live API는 마이크/스피커를 직접 다뤄야 해서 PCM 변환(16kHz 입력·24kHz 출력)과 오디오 큐잉을 클라에 구현했다. ⚠️ 무료 티어는 대화 내용이 구글 학습에 사용되므로 실제 배포 시 유료 티어 권장.
- **실시간 음성 통화(speech-to-speech, 2026-07-22)**: 훈련 통화가 이제 ElevenLabs Agents와의 실시간 음성 대화로 동작한다. 예전에는 턴마다 "브라우저 STT → 텍스트 LLM → TTS 합성 → 오디오 재생"을 왕복해 지연이 누적됐고 말을 끊거나 겹쳐 말할 수 없었다. **ElevenLabs를 고른 이유**: OpenAI Realtime·Gemini Live도 저지연 speech-to-speech를 제공하지만 둘 다 고정 프리셋 음성만 쓸 수 있다. 이 앱의 전제인 "참가자 본인 목소리 클론으로 걸려오는 전화"(AC-018/019)를 지키려면 런타임에 임의 클론 voice를 지정할 수 있어야 하고, 셋 중 `tts.voice_id` 오버라이드를 지원하는 건 ElevenLabs뿐이다. 한국어는 `agent.language="ko"`, 저지연 모델은 에이전트 설정에서 지정한다. **프롬프트 보안(ADR-0004)**: 오버라이드는 클라이언트가 보내는 값이라 페르소나 프롬프트를 그 경로로 넘기면 브라우저에 노출된다 — 그래서 시나리오별 에이전트에 프롬프트를 저장하고 서버는 `scenarioId→agentId` 매핑과 짧은 수명의 서명 URL만 발급한다(API 키는 서버에만 남는다). **폴백**: 키·에이전트 매핑·마이크 권한 중 하나라도 없으면 기존 텍스트 대화로 자동 강등하고 그 사실을 화면에 표시한다(조용한 실패 금지). 설정: `ELEVENLABS_AGENT_IDS`(README 참조).
- **신규 시나리오 3종(2026-07-22)**: 저금리 대환대출 빙자, 국세청 환급금 빙자(둘 다 기본 합성 음성), 손주 사칭 급전 요청(본인 목소리 클론 — 이 앱의 주 대상인 어르신을 노리는 수법). 총 5종. 공개 메타(`src/content/scenarios`)와 민감 프롬프트(`functions/src/scenarios`) 분리 저장 관례를 그대로 따르며, 수법은 전부 "약화된" 형태라 실제 계좌번호·송금 절차·기관 연락처를 포함하지 않는다(AC-005).

### Changed
- **통화 화면을 실제 전화처럼 재구성(2026-07-22)**: 수신 파동 애니메이션, 상대 발화 파형 인디케이터, 경과 시간, 음소거/키패드/종료 컨트롤 행. 시나리오 선택 화면도 카드형으로 개편해 "누가 전화를 거는지"(발신자)와 "녹음이 필요한지"를 먼저 읽히게 했다. 모션은 `prefers-reduced-motion`을 존중한다.
- **목업 대사 품질(2026-07-22)**: 규칙 기반 목업이 시나리오 수법 설명문(3인칭 지침)을 그대로 대사로 읽던 문제와, 가족 사칭 전용 표현("나야...", "지금 좀 도와줄 수 있어?")을 기관 사칭 시나리오에도 붙이던 문제를 고쳤다. 근본 해결은 실 LLM/실시간 에이전트 연동이다.

### Fixed
- **sendMessage 턴 경합(2026-07-22)**: 대화 이력 읽기 → 인덱스 결정 → 쓰기가 트랜잭션 밖이라, 같은 세션에서 요청이 겹치면 두 메시지가 같은 `turnIndex`를 쓰고 `turnCount` 증가가 유실될 수 있었다. 이력 읽기 + 사용자 메시지 쓰기 + turnCount 증가를 한 트랜잭션으로 묶었다(느린 LLM 호출은 트랜잭션 밖 유지).

### Removed
- **`synthesizeDeepvoice` 콜러블(2026-07-22)**: UX-014 화면 통합 이후 호출하는 화면이 없어졌고, 본문이 끝내 placeholder(가짜 voiceId·TODO 문자열)를 반환하는 상태였다 — 배포된 채로 두면 그 가짜 응답이 언젠가 화면에 실릴 위험이 있어 서버 콜러블·클라 래퍼·계약 타입을 함께 제거했다. `VoiceProvider.synthesize`는 남아 `createSession`/`sendMessage`의 폴백 오디오 합성에 계속 쓰인다. 시나리오의 `deepvoiceLines`는 "고정 대본 오프닝"을 되살릴 여지를 남겨 `@deprecated` 표기만 하고 보존했다.

- **프로젝트 스캐폴딩(T2)**: Next.js(App Router, TypeScript, Tailwind, 정적 export) + Firebase(Auth/Functions/Firestore/Storage) 초기화, 배포 파이프라인(`firebase deploy`), API/DB 계약 스텁, `firestore.rules`/`storage.rules`, `.env.example`(클라)·`functions/.env.example`(서버) 2분리.
- **Google 로그인 + 전역 라우트 가드(T18)**: Firebase Auth Google Provider로 로그인/가입. 인증되지 않은 사용자는 로그인 화면 외 모든 라우트에서 리다이렉트되며, 모든 데이터는 로그인한 사용자의 uid에 귀속된다. 로컬 Firebase 에뮬레이터 기준으로 빌드/lint/Firestore write까지 검증됨 — 실제 브라우저에서 Google OAuth 팝업을 끝까지 클릭해보는 것은 아직 미검증(에뮬레이터 한계).
- **한국어 "가족 납치/사고" 딥보이스 시나리오 콘텐츠(T6)**: 공개 메타데이터(제목·사기 유형·난이도 등)와 민감한 역할극 프롬프트(페르소나·약화 수법)를 분리 저장.
- **음성 프로바이더 인터페이스(T19, 임시 목업)**: 공통 `VoiceProvider` 인터페이스 뒤에 `MockVoiceProvider`를 붙여 클론/합성 파이프라인 개발을 언블록. **ElevenLabs API 키가 아직 준비되지 않아 실제 음성 클론/합성이 아닌 임시 목업(플레이스홀더 오디오)이며, 응답에 `isMock: true` 표식이 붙는다. 추후 ElevenLabs 실연동으로 교체 예정(최종 데모 필수 조건).**
- **AI 사기범 역할극 엔진(T7, 임시 목업)**: `createSession`/`sendMessage`로 시나리오 기반 대화를 주고받을 수 있다. 서버가 시스템 프롬프트를 조립하고 사용자 입력은 별도 role/구분자로 감싸 프롬프트 인젝션을 구조적으로 방어하며, 응답 시간 초과에 대한 안전망(타임아웃 처리)을 포함한다. **LLM API 키가 아직 준비되지 않아 실제 LLM이 아닌 규칙 기반 목업(`MockLlmClient`)이 응답하며, `isMock: true`로 식별된다. 인격 유지·실시간 적응 등은 실 LLM 연동 후에야 검증 가능. 추후 Claude/Gemini 실연동으로 교체 예정.** reviewer APPROVED, QA GO 통과.
- **온보딩 + 동의 게이팅 + 본인 목소리 등록 UI(T3)**: 명시적 동의 화면, 본인 확인 절차, 30초 대본 녹음 UI. 인증된 세션(T18) 전제로만 진입 가능. Firestore/Storage 에뮬레이터로 동의 write/read·녹음 업로드 경로·타인 uid 거부를 검증. **브라우저 마이크 권한 팝업·실제 녹음 재생 등 실클릭 UI 동작은 미검증**(T18과 동일한 한계, 코드 리뷰로만 확인).
- **음성 클론 파이프라인 통합(T4, Mock 기준)**: 녹음 업로드 → `createVoiceClone`(Mock `VoiceProvider`) → `createSession`이 동일 sessionId를 채택하는 흐름을 실배선. 소유 uid 불일치·녹음 없음 등 오류 처리 포함. functions test 34/34, 에뮬레이터 실호출 7/7 케이스 검증. **미검증**: 브라우저 실클릭(버튼→네비게이션→스피너). 시나리오 선택 UI(당시 `src/app/scenarios/page.tsx`)는 아직 T2/T6 스텁이라 sessionId 채택 경로가 실제 화면에서는 호출되지 않은 상태로 남음(T5가 후속 연결). 음성은 여전히 Mock — 최종 데모 전 ElevenLabs 실클론(T1)으로 교체 필수.
- **딥보이스 가족사칭 오디오 인앱 재생(T5)**: 시나리오 선택 → `synthesizeDeepvoice`로 대사 3줄 순차 재생, "AI 훈련용 합성" 표식(SyntheticLabel)과 재생 전 텍스트 프리롤 안내, 훈련 종료 버튼 상시 노출. 에뮬레이터 실호출 19/19 검증(클론→세션→합성×3→타uid 세션탈취 거부 포함). **미검증**: 오디오 자동재생·순차재생 등 브라우저 실클릭.
- **세션 라이프사이클 + 안전 종료(T8)**: `endSession`(소유권/인자 검증, 멱등 처리)과 "이것은 훈련이었습니다" 고지·디스컬레이션 안심 메시지 화면(UX-007)을 구현. 리포트 생성 트리거를 `endSession`과 자동종료(10턴 한도) 양쪽이 공유하는 단일 지점으로 통합. functions test 36/36, 에뮬레이터 16/16 검증. **알려진 갭**: 종료 사유(`endReason`)가 항상 `user_ended`로 고정(채팅 화면 미구현이라 다른 종료 경로 없음), 브라우저 실클릭 미검증.
- **취약점 리포트 생성·표시(T9)**: `generateReport`가 마스킹된 대화 로그에서 속은 시점 타임라인·시도된 수법·대처법을 산출해 리포트 화면(UX-008)에 표시. **LLM API 키 미확보로 실 LLM 대신 규칙 기반(저항/순응 키워드 매칭) 판정을 사용함을 투명 고지**(API.md는 "LLM 산출"로 서술 — 실제로는 아님). functions test 43/43(신규 7건), 에뮬레이터 시나리오 20/20 검증. **알려진 갭**: tacticsUsed 판정이 Mock 대화 특유의 flavor 문구 삽입 패턴에 의존 — 실 LLM 교체 후 매칭 정확도 재검증 필요. 브라우저 실클릭 미검증.
- **가드레일2 — 생성물 즉시 폐기(T10)**: 세션 종료(`status → ended`) 시 발화하는 Firestore 트리거 `onSessionEnded`가 Storage `users/{uid}/sessions/{sid}/**` 파일 삭제 + ElevenLabs voice 삭제 + `session.voiceId` 클리어 + `deletionLogs` 감사 기록을 수행. 부분 실패도 target별로 기록해 재시도 가능. reviewer APPROVED, QA GO. functions test 54/54(신규 11건), 에뮬레이터 21/21 검증(삭제 실측, 재발화 없음 확인). **설계 이탈(승인 필요, 상세는 UpdateRequests.md #4)**: ADR-0003/API.md는 `sessions/{sid}/artifacts` 서브컬렉션을 매니페스트로 순회하는 설계였으나, 실제 구현은 Storage prefix 실제 나열 방식 — reviewer는 이 쪽이 더 안전한 설계라고 판단(artifacts는 T5가 쓴 적이 없어 항상 비어 있었고, 애초에 원본 녹음을 포함하지 못하는 스키마였음).
- **가드레일5 하드닝 — PII 마스킹 실구현 + 프롬프트 인젝션 방어 레드팀 스팟체크(T11)**: `guardrails/maskPII`를 passthrough 스텁에서 정규식 기반 실구현으로 교체 — 이메일 / 주민번호형(`\d{6}-\d{7}`) / 전화번호형(010-xxxx-xxxx, 02-xxx-xxxx 등) / 8자리 이상 연속 숫자(계좌형) 4종을 이 순서로 토큰화(`[이메일]`/`[주민번호]`/`[전화]`/`[계좌]`)해 원문 대신 저장한다(T7이 이미 배선해 둔 `maskPII` 호출부는 무수정). `roleplay/promptAssembly.ts`의 `wrapUserInputAsData`에 구분자 하드닝(`escapeDelimiterLookalikes`)을 추가해, 사용자 입력이 `[훈련참가자입력:...]`류 가짜 구분자를 흉내 내도 전각괄호로 치환하고 실제 구분자는 래퍼가 감싼 시작/끝 1쌍만 남도록 방어. **레드팀 스팟체크**: 한국어 인젝션 변형 12종을 `INJECTION_PATTERN`에 직접 검증 — 6종 탐지, 6종 미탐. 미탐 6종 중 2종(계좌번호 요청·링크 요청)은 시나리오 콘텐츠 자체에 유출할 실제 운영정보가 없어 Mock 단계에서는 구조적으로 안전함을 확인했고, 나머지 4종(역할 재정의, 오탈자, 완곡 정체인정, 일반 지시무시)은 정규식이 놓치는 잔존 리스크로 정직하게 남김 — 실LLM 교체 후 재검증 필요. functions test 80/80(신규: maskPII 11건 + promptAssembly 구분자 2건 + injectionRedTeam 13건, 기존 69건 무회귀), 에뮬레이터 실호출 11/11 검증(저장된 `messages.textMasked`에 원문 PII 0건 확인). **알려진 한계**: 8자리 이상 숫자는 문맥과 무관하게 계좌형으로 마스킹되는 과잉마스킹(오탐) 경향이 남아 있음(ADR-0004가 이미 수용한 트레이드오프, 금액/이름 토큰화는 하카톤 스코프 밖). reviewer APPROVED, QA 조건부 GO(오케스트레이터 보고 기준 — 이 docs 세션에서 리뷰/QA 리포트 원문은 직접 확인하지 못했고, docs/Tasks.md T11 행은 이 시점까지 `review`로 남아 있어 판정 반영이 지연된 상태로 보임).
- **사칭 이미지 1종 + 합성 표식(T12)**: `SpoofImage` 컴포넌트를 HTML/CSS로 그린 가짜 은행 앱 "송금완료" 카드로 구현(이미지 생성 도구 미가용). 은행명·수취인·계좌번호·거래시각 전부 placeholder 표기 + "실제 송금이 아닙니다" 문구 명시, `SyntheticLabel` 상시 오버레이, 로드 실패 시 조용히 생략(비차단). 채팅 화면(UX-006)이 당시 스텁이라 실제 오버레이 배선은 하지 않고 통합 지점만 주석으로 남김.
- **게임화 — 사기 방어 등급(T13, 임시 플레이스홀더 v1)**: `computeDefenseGrade`가 누적 리포트의 방어율로 5단계 등급을 산정(OQ-5 미확정 — 사용자 승인 하에 "(v1)" 접미사로 임시값임을 화면에도 노출). `generateReportCore`가 리포트 생성 직후 `users/{uid}.defenseGrade`/`sessionCount`를 갱신, 실패해도 리포트 생성 자체는 막지 않음(비차단). `/grade`(UX-010) 화면 완성.
- **연령 확인 age-gate(T14)**: 만 14세 기준(OQ-8 확정) 자기신고 확인 플래그 방식으로 `AgeGate` 구현. `src/lib/age`로 판정 로직(순수 함수)과 Firestore read/write를 분리. 온보딩 동의(UX-001) 직후·녹음(UX-002) 이전에 배치, URL 직접 접근으로 우회 불가하도록 가드 추가.
- **세션/리포트 히스토리 열람(T15)**: `/history`(UX-012)에서 본인 `reports`를 시간순(최신순)으로 열람. 클라 Firestore SDK 직접 read(콜러블 불필요), 항목 클릭 시 `/report?sessionId=...`로 이동. 에뮬레이터로 소유권 분리(타uid 열람 차단) 검증.
- **역할극 채팅 화면(UX-006) 구현 — 채팅 UI 자체가 없던 마지막 P0 갭 해소**: `sessions/{sid}/messages` 서브컬렉션을 실시간 구독해 대화 내역을 렌더링하고, 입력을 `sendMessage`로 전송한다. 턴 한도(`N/maxUserTurns`) 표시, 한도 도달 시 입력 잠금 + `/session/end` 안내, LLM 응답이 Mock임을 알리는 배지(`isMock`)를 노출해 실 LLM과 혼동되지 않게 했다. 백엔드(T7 역할극 엔진, T8 세션 라이프사이클)는 이미 완성·테스트돼 있었고 화면만 없었던 상태였음. 에뮬레이터 실호출 17/17 검증(오프닝 메시지 write→`sendMessage` 실호출→turnIndex 순서·역할 정합·타uid 탈취 거부까지, 스크립트는 검증 후 삭제·커밋 안 함).
- **역할극을 실시간 음성 통화형으로 전환(T20, Phase A)**: UX-006에 브라우저 STT(탭-투-토크 마이크 버튼, `useSpeechRecognition` 신규 훅)와 사기범 응답 TTS 자동재생을 추가했다. 서버 `sendMessage`/`createSession`이 이제 `audioUrl`/`openingAudioUrl`을 함께 반환(VoiceProvider 합성 훅, 실패해도 텍스트 응답은 안 막힘 — 비차단). 텍스트 입력은 STT 미지원 브라우저(Safari 등)용 폴백으로 상시 유지. **PRD "실시간 통화·STT는 제외" 결정을 뒤집었다(v0.8) — 단 실제 전화망(PSTN) 연동은 여전히 하지 않는다(AC-019 무변경, Twilio 등 미도입).** 사전고지·동의·종료고지(AC-012/017/023)도 무변경 — 원래 요청이던 "훈련 인지 자체를 못 하게"는 이 안전장치들을 제거하는 것이라 거부되고 절충안(사전 동의 유지, 통화 중 몰입만 유지)으로 진행됐다(경위는 docs/UX.md D-10 참고). 에뮬레이터 실호출로 `audioUrl`/`openingAudioUrl` 반환 확인, 브라우저 실클릭으로 오디오 자동재생 엘리먼트·마이크 버튼·마이크 권한거부 시 텍스트 폴백까지 확인. **미검증**: 이 세션의 자동화 브라우저는 마이크가 항상 차단돼 있어 STT가 실제로 발화를 인식하는 경로 자체는 트리거해보지 못함(코드 리뷰로 대체, 실사용자 테스트 필요). Phase B(`voiceMode` 필드·신규 "기관사칭" 시나리오·온보딩 순서 변경으로 녹음 생략)는 후속 별도 태스크로 분리 제안, 이번 범위 아님.
- **UX-014 단일 연속 통화 화면 통합 + 다크 통화 UI 재디자인(T22)**: 사용자 피드백에 따라 ux-design 서브에이전트로 온보딩→통화→리포트 흐름을 재설계(`docs/UX.md` v1.4, UX-005/006을 Deprecated로 이관하고 단일 연속 통화 **UX-014** 신설)한 뒤 구현. `/session/play`(전화받기)와 `/session/chat`(역할극)을 **단일 라우트**로 통합해 phase 상태머신(수신→연결→오프닝 재생→실시간 역할극→종료)으로 전이한다 — 중간 "계속(역할극 시작)" 버튼과 페이지 전환을 없애 "받았다가 채팅창으로 넘어가는" 단절을 제거했다. 오프닝 재생은 별도 스크립트 대신 `createSession`이 이미 대화 로그(turnIndex 0)에 남겨둔 `openingAudioUrl`을 그대로 이어 쓴다. 통화 경과 타이머를 "받기" 시점 기준으로 신설했다. `/session/chat`은 삭제 대신 `/session/play`로의 리다이렉트만 남겼다. 화면은 클로드 디자인 원안(상단바 채팅 버블)에서 벗어나 전화받기 화면과 동일한 다크 통화 톤으로 재구성했다. "수사관이 목소리를 녹음했다는 문구는 거짓말" 지적은 재확인 결과 직전 태스크(T21)에서 이미 수정돼 있었음을 코드·브라우저 재검증으로 확인(추가 수정 없음). 함께 진행한 기능 방향 재검토(R-1~R-5)에서 "clone 시나리오의 실시간 응답도 클론 목소리여야 하지 않냐"는 우려는 서버가 매 턴 `session.voiceId`(오프닝과 동일한 실제 클론 voiceId)로 이미 합성하고 있음을 코드 확인으로 발견해 추가 구현 없이 해소됐다. 에뮬레이터+브라우저 실클릭으로 기관사칭 시나리오의 수신→받기→오프닝 자동재생→화면 전환 없는 실시간 전환→메시지 왕복까지 전 구간 URL이 `/session/play` 그대로임을 확인.
- **기본(비복제) TTS 시나리오 + 온보딩 순서 변경 + 채팅 자동 청취 전환(T21, Phase B)**: `ScenarioDoc`에 `voiceMode:"clone"|"generic"`/`callerLabel` 추가, voiceMode:"generic" 시나리오(신규 "기관 사칭(검찰·금융감독원)")는 본인 목소리 녹음/클론을 완전히 생략하고 바로 세션을 시작한다. 온보딩 순서를 "age-gate→녹음→클론→시나리오"에서 **"age-gate→시나리오 선택→(clone 시나리오만) 녹음→클론"**으로 변경 — `/onboarding/record`가 하던 사전 세션 id 발급을 `/scenarios`로 옮기고, `/clone/wait`는 클론 완료 시 저장해 둔 선택 시나리오로 자동 `createSession`을 호출해 `/session/play`로 직행한다(사용자가 시나리오를 다시 고를 필요 없음). 채팅 화면(T20의 탭-투-토크)을 **자동 청취**로 전환 — 상대 응답 재생이 끝나면(또는 합성 실패로 재생할 게 없으면) 탭 없이 자동으로 듣기 시작하고, 텍스트 입력은 접이식 폴백으로 남는다. **브라우저 실측 중 발견해 즉시 고친 버그**: `play/page.tsx`의 "당신의 목소리로 합성되었습니다" 문구가 녹음을 안 한 generic 시나리오에도 그대로 떠 있어 사실과 다름을 확인 — voiceMode로 분기해 "기본 합성 음성으로 재생됩니다"로 교체. `docs/UX.md` Information Architecture 다이어그램을 새 순서로 갱신(v1.3). 에뮬레이터 실호출로 generic 시나리오 `createSession`(사전 클론 없이) 5/5 검증, 브라우저 실클릭으로 순서 변경·양쪽 voiceMode 경로·자동 청취 실제 트리거(마이크 권한 요청 자동 발생)까지 확인. **미검증**: T20과 동일하게 이 자동화 브라우저는 마이크가 항상 차단돼 있어 STT가 실제로 발화를 인식하는 경로는 트리거 불가.

### Changed
- **로컬 개발 방식(사용자 결정, 2026-07-21)**: 프로토타입 단계는 실제 Firebase 콘솔 프로젝트 없이 로컬 Firebase 에뮬레이터로 진행하기로 확정. 실 프로젝트 연결은 데모 준비 시점으로 이연.

### Fixed
- **`synthesizeDeepvoice.ts` 더미 스텁 실배선 교체(T5, 크로스트랙 버그픽스)**: `session/play`(UX-005) 화면이 실제로는 백엔드를 호출하지 않고 하드코딩된 가짜 오디오(`/mock-audio/mock-deepvoice-stub.mp3`, 실존하지 않는 파일)를 재생하고 있던 것을 발견해 실제 `httpsCallable` 호출로 교체.
- **`synthesizeDeepvoice` 콜러블에 세션 소유권 검증 추가(T5, 보안 하드닝)**: `createVoiceClone`/`createSession`/`endSession`과 달리 소유 uid 검증이 없어 타uid 호출이 거부되지 않던 문제를 확인해 동일한 `permission-denied` 검증 패턴을 추가.
- **`createSession.ts`/`endSession.ts` 더미 스텁 실배선 교체(T8)**: T2 이후 더미 스텁 상태였던 것을 실제 `httpsCallable` 호출로 교체(이 교체 없이는 세션 종료 전체 파이프라인이 서버를 타지 않아 T8 자체 검증이 불가능했음).
- **`sendMessage.ts` 더미 스텁 실배선 교체(채팅 화면 구현과 함께)**: T2 이후 유일하게 남아 있던 콜러블 더미 스텁을 실제 `httpsCallable` 호출로 교체.
- **클라 타입 계약 드리프트 해소**: `CreateSessionResponse`/`SendMessageResponse`에 서버는 이미 반환 중이던 `isMock` 필드가 클라 `src/lib/api/types.ts`에는 누락돼 있던 것(T7 known gap)을 추가.

## [0.1.0] - {{YYYY-MM-DD}}
### Added
- Initial release.
